import { api } from './services/api.js';
import * as XLSX from 'xlsx';
import { i18n } from './services/i18n.js';

class OeeApp {
    constructor() {
        this.currentTab = 'dashboard';
        this.activeMasterTab = 'machines';
        this.theme = localStorage.getItem('oee_theme') || 'dark';
        this.currentLang = i18n.getLocale();
        this.tvMode = false;
        this.filters = {
            plant_id: '',
            line_id: '',
            machine_id: '',
            shift_id: '',
            period: '30days',
            start_date: '',
            end_date: '',
        };
        this.masterData = {
            plants: [],
            lines: [],
            machines: [],
            products: [],
            downtimeReasons: [],
            defectReasons: [],
            shifts: [],
            settings: [],
        };
        
        // Initialize User from Session / LocalStorage
        let savedUser = null;
        try {
            const raw = localStorage.getItem('oee_user');
            if (raw) savedUser = JSON.parse(raw);
        } catch (e) {
            savedUser = null;
        }

        this.user = savedUser || {
            name: 'Super Administrator',
            email: 'superadmin@prodcr.yasunaga.com',
            role: 'Super Admin',
            role_keys: ['super_admin'],
        };

        this.autoRefreshTimer = null;
        this.charts = {};
        this.audioCtx = null;
        this.notifCount = 0;
    }

    /**
     * Translation helper delegating to i18n service
     */
    t(keyPath, fallback = '') {
        return i18n.t(keyPath, fallback);
    }

    /**
     * Switch application language and trigger clean re-render
     */
    setLanguage(lang) {
        if (i18n.setLocale(lang)) {
            this.currentLang = lang;
            this.renderLayout();
            this.loadCurrentTab(false);
            if (lang === 'ja' && typeof document !== 'undefined') {
                setTimeout(() => {
                    i18n.localizeDom(document.body);
                }, 50);
            }
        }
    }

    playClingSound() {
        try {
            const AudioContext = window.AudioContext || window.webkitAudioContext;
            if (!AudioContext) return;
            if (!this.audioCtx) this.audioCtx = new AudioContext();
            
            if (this.audioCtx.state === 'suspended') {
                this.audioCtx.resume();
            }

            const now = this.audioCtx.currentTime;
            
            // Dual Tone Crystal Bell Chime ("Cling-Cling")
            const osc1 = this.audioCtx.createOscillator();
            const gain1 = this.audioCtx.createGain();
            osc1.type = 'sine';
            osc1.frequency.setValueAtTime(1479.98, now); // F#6
            gain1.gain.setValueAtTime(0.3, now);
            gain1.gain.exponentialRampToValueAtTime(0.0001, now + 0.8);
            osc1.connect(gain1);
            gain1.connect(this.audioCtx.destination);
            osc1.start(now);
            osc1.stop(now + 0.8);

            const osc2 = this.audioCtx.createOscillator();
            const gain2 = this.audioCtx.createGain();
            osc2.type = 'sine';
            osc2.frequency.setValueAtTime(2217.46, now + 0.08); // C#7
            gain2.gain.setValueAtTime(0.35, now + 0.08);
            gain2.gain.exponentialRampToValueAtTime(0.0001, now + 1.2);
            osc2.connect(gain2);
            gain2.connect(this.audioCtx.destination);
            osc2.start(now + 0.08);
            osc2.stop(now + 1.2);
        } catch (e) {
            console.warn('Audio play error:', e);
        }
    }

    showNotification(title, message, type = 'create', duration = 10000) {
        // 1. Sound Effect ("Cling")
        this.playClingSound();

        // 2. Push to Real-time Notification Center Log
        const notifType = (type === 'delete' || type === 'danger') ? 'danger' : ((type === 'update' || type === 'warning' || type === 'edit') ? 'warning' : ((type === 'create' || type === 'success') ? 'success' : 'info'));
        this.pushNotification(title, message, notifType, false);

        // 3. Ensure Floating Toast Container exists (Top-Right Corner)
        let toastContainer = document.getElementById('toast-container');
        if (!toastContainer) {
            toastContainer = document.createElement('div');
            toastContainer.id = 'toast-container';
            toastContainer.className = 'fixed top-4 right-4 z-[9999] flex flex-col gap-2.5 max-w-sm w-full pointer-events-none pr-2';
            document.body.appendChild(toastContainer);
        }

        // 4. Create Toast Element
        const toastId = 'toast-' + Date.now();
        const toast = document.createElement('div');
        toast.id = toastId;
        toast.className = 'pointer-events-auto bg-slate-900/95 border border-slate-800 rounded-xl p-3.5 shadow-2xl backdrop-blur-md text-xs font-sans transform translate-x-full transition-all duration-300 ease-out flex items-start gap-3 relative overflow-hidden';

        let iconName = 'check-circle-2';
        let badgeColor = 'bg-emerald-950/80 text-emerald-400 border-emerald-800/80';
        let progressColor = 'bg-emerald-500';
        let typeLabel = 'CREATED';

        const isJa = this.currentLang === 'ja';

        if (type === 'create' || type === 'success') {
            iconName = 'check-circle-2';
            badgeColor = 'bg-emerald-950/80 text-emerald-400 border-emerald-800/80';
            progressColor = 'bg-emerald-500';
            typeLabel = isJa ? '新規登録' : 'CREATED';
        } else if (type === 'update' || type === 'edit') {
            iconName = 'pencil';
            badgeColor = 'bg-cyan-950/80 text-cyan-400 border-cyan-800/80';
            progressColor = 'bg-cyan-500';
            typeLabel = isJa ? '更新完了' : 'UPDATED';
        } else if (type === 'delete') {
            iconName = 'trash-2';
            badgeColor = 'bg-rose-950/80 text-rose-400 border-rose-800/80';
            progressColor = 'bg-rose-500';
            typeLabel = isJa ? '削除完了' : 'DELETED';
        } else if (type === 'status') {
            iconName = 'activity';
            badgeColor = 'bg-amber-950/80 text-amber-400 border-amber-800/80';
            progressColor = 'bg-amber-500';
            typeLabel = isJa ? 'ステータス変更' : 'STATUS CHANGE';
        }

        const displayTitle = isJa ? i18n.translateText(title) : title;
        const displayMessage = isJa ? i18n.translateText(message) : message;

        toast.innerHTML = `
            <i data-lucide="${iconName}" class="w-5 h-5 flex-shrink-0 mt-0.5 ${type === 'create' || type === 'success' ? 'text-emerald-400' : (type === 'update' || type === 'edit' ? 'text-cyan-400' : (type === 'delete' ? 'text-rose-400' : 'text-amber-400'))}"></i>
            <div class="flex-1 pr-4">
                <div class="font-bold text-slate-100 text-xs leading-tight flex items-center gap-1.5">
                    ${displayTitle}
                    <span class="px-1.5 py-0.2 text-[9px] font-mono rounded font-bold uppercase ${badgeColor}">${typeLabel}</span>
                </div>
                <div class="text-slate-300 text-[11px] mt-1 leading-snug font-sans">${displayMessage}</div>
            </div>
            <button class="btn-close-toast text-slate-500 hover:text-slate-200 cursor-pointer absolute top-2.5 right-2.5 p-1">
                <i data-lucide="x" class="w-3.5 h-3.5"></i>
            </button>
            <div class="toast-progress absolute bottom-0 left-0 h-0.5 ${progressColor} transition-all duration-[10000ms] ease-linear" style="width: 100%;"></div>
        `;

        toastContainer.appendChild(toast);
        if (window.lucide) window.lucide.createIcons();

        // Slide in & Progress Bar animation
        requestAnimationFrame(() => {
            toast.classList.remove('translate-x-full');
            const progress = toast.querySelector('.toast-progress');
            if (progress) progress.style.width = '0%';
        });

        // Close button and auto-dismiss after 10s
        const closeBtn = toast.querySelector('.btn-close-toast');
        const removeToast = () => {
            if (toast.parentNode) {
                toast.classList.add('opacity-0', 'translate-x-full');
                setTimeout(() => { if (toast.parentNode) toast.remove(); }, 300);
            }
        };
        closeBtn.addEventListener('click', removeToast);
        setTimeout(removeToast, duration);
    }

    escapeHtml(str) {
        if (!str) return '';
        return String(str)
            .replace(/&/g, '&amp;')
            .replace(/"/g, '&quot;')
            .replace(/'/g, '&#39;')
            .replace(/</g, '&lt;')
            .replace(/>/g, '&gt;');
    }

    getCompanyLogo(header = null) {
        if (header && header.company_logo && typeof header.company_logo === 'string' && header.company_logo.trim() !== '') {
            return header.company_logo;
        }
        if (this.companyProfile && this.companyProfile.company_logo && typeof this.companyProfile.company_logo === 'string' && this.companyProfile.company_logo.trim() !== '') {
            return this.companyProfile.company_logo;
        }
        return '/images/yasunaga-logo.png';
    }

    triggerFramePrint(frame, frameDoc, originalDocTitle, title) {
        frameDoc.title = title;
        document.title = title;

        const doPrint = () => {
            try {
                frame.contentWindow.focus();
                frame.contentWindow.print();
            } catch (e) {
                console.error('Print trigger error:', e);
            }
            setTimeout(() => {
                document.title = originalDocTitle;
            }, 2500);
        };

        const imgs = Array.from(frameDoc.images || []);
        if (imgs.length === 0) {
            setTimeout(doPrint, 250);
            return;
        }

        let pending = imgs.length;
        let done = false;
        const checkDone = () => {
            if (done) return;
            pending--;
            if (pending <= 0) {
                done = true;
                setTimeout(doPrint, 150);
            }
        };

        imgs.forEach(img => {
            if (img.complete && img.naturalWidth !== 0) {
                checkDone();
            } else {
                img.addEventListener('load', checkDone);
                img.addEventListener('error', checkDone);
            }
        });

        // Safety fallback timer
        setTimeout(() => {
            if (!done) {
                done = true;
                doPrint();
            }
        }, 800);
    }

    hasAccessToTab(tab) {
        if (!this.user) return false;
        const roleKeys = this.user.role_keys || [];
        const roleName = (this.user.role || '').toLowerCase();
        
        // Super Admin and Admin have full access
        if (roleKeys.includes('super_admin') || roleKeys.includes('admin') || roleName.includes('super') || roleName.includes('admin')) {
            return true;
        }

        const permissionMap = {
            production_manager: ['dashboard', 'daily-report', 'monitoring', 'machines', 'lines', 'shifts', 'teams', 'downtime', 'quality', 'reports', 'master', 'settings', 'database'],
            production_supervisor: ['dashboard', 'daily-report', 'ng-report', 'monitoring', 'machines', 'shifts', 'teams', 'downtime', 'reports'],
            production_leader: ['dashboard', 'daily-report', 'ng-report', 'monitoring', 'machines', 'shifts', 'teams', 'downtime', 'reports'],
            quality_control: ['dashboard', 'ng-report', 'quality', 'reports'],
            operator: ['dashboard', 'daily-report', 'ng-report', 'monitoring'],
            maintenance: ['dashboard', 'machines', 'downtime', 'monitoring'],
            management: ['dashboard', 'monitoring', 'machines', 'lines', 'shifts', 'teams', 'downtime', 'quality', 'reports', 'settings', 'database'],
        };

        for (const rk of roleKeys) {
            if (permissionMap[rk] && permissionMap[rk].includes(tab)) return true;
        }

        if (roleName.includes('manager') && permissionMap.production_manager.includes(tab)) return true;
        if (roleName.includes('supervisor') && permissionMap.production_supervisor.includes(tab)) return true;
        if (roleName.includes('leader') && permissionMap.production_leader.includes(tab)) return true;
        if (roleName.includes('quality') || roleName.includes('qc')) return permissionMap.quality_control.includes(tab);
        if (roleName.includes('operator')) return permissionMap.operator.includes(tab);
        if (roleName.includes('maintenance')) return permissionMap.maintenance.includes(tab);

        return tab === 'dashboard' || tab === 'monitoring';
    }

    /**
     * Dynamic Line Badge & Accent Style Generator
     * Provides 12 distinct harmonious color themes per production line (FX-1, FX-2, ..., FX-10+)
     */
    getLineBadgeStyle(identifier, isLight = false) {
        const palettes = [
            { light: 'bg-sky-100 text-sky-800 border-sky-300', dark: 'bg-sky-950/90 text-sky-300 border-sky-800' },          // 1: Sky
            { light: 'bg-emerald-100 text-emerald-800 border-emerald-300', dark: 'bg-emerald-950/90 text-emerald-300 border-emerald-800' }, // 2: Emerald
            { light: 'bg-amber-100 text-amber-900 border-amber-300', dark: 'bg-amber-950/90 text-amber-300 border-amber-800' },       // 3: Amber
            { light: 'bg-purple-100 text-purple-800 border-purple-300', dark: 'bg-purple-950/90 text-purple-300 border-purple-800' },    // 4: Purple
            { light: 'bg-rose-100 text-rose-800 border-rose-300', dark: 'bg-rose-950/90 text-rose-300 border-rose-800' },          // 5: Rose
            { light: 'bg-indigo-100 text-indigo-800 border-indigo-300', dark: 'bg-indigo-950/90 text-indigo-300 border-indigo-800' },    // 6: Indigo
            { light: 'bg-teal-100 text-teal-900 border-teal-300', dark: 'bg-teal-950/90 text-teal-300 border-teal-800' },          // 7: Teal
            { light: 'bg-fuchsia-100 text-fuchsia-800 border-fuchsia-300', dark: 'bg-fuchsia-950/90 text-fuchsia-300 border-fuchsia-800' }, // 8: Fuchsia
            { light: 'bg-orange-100 text-orange-900 border-orange-300', dark: 'bg-orange-950/90 text-orange-300 border-orange-800' },    // 9: Orange
            { light: 'bg-blue-100 text-blue-800 border-blue-300', dark: 'bg-blue-950/90 text-blue-300 border-blue-800' },          // 10: Blue
            { light: 'bg-lime-100 text-lime-900 border-lime-300', dark: 'bg-lime-950/90 text-lime-300 border-lime-800' },          // 11: Lime
            { light: 'bg-cyan-100 text-cyan-900 border-cyan-300', dark: 'bg-cyan-950/90 text-cyan-300 border-cyan-800' }           // 12: Cyan
        ];

        let index = 0;
        if (typeof identifier === 'number') {
            index = Math.max(0, identifier - 1) % palettes.length;
        } else if (typeof identifier === 'string') {
            const numMatch = identifier.match(/\d+/);
            if (numMatch) {
                const num = parseInt(numMatch[0], 10);
                index = Math.max(0, num - 1) % palettes.length;
            } else {
                let hash = 0;
                for (let i = 0; i < identifier.length; i++) {
                    hash = ((hash << 5) - hash) + identifier.charCodeAt(i);
                    hash |= 0;
                }
                index = Math.abs(hash) % palettes.length;
            }
        }

        const selected = palettes[index];
        return isLight ? selected.light : selected.dark;
    }

    async init() {
        console.log('Initializing OEE Performance System UI...');
        this.applyTheme();

        const isLoggedIn = localStorage.getItem('oee_logged_in') === 'true';

        if (!isLoggedIn) {
            this.renderLogin();
            return;
        }

        // If authenticated and still on /login path, update URL to / without full reload
        if (window.location.pathname === '/login' && window.history && window.history.replaceState) {
            window.history.replaceState({}, '', '/');
        }

        // Restore last active tab if valid, otherwise fallback
        const savedTab = localStorage.getItem('oee_active_tab');
        if (savedTab && this.hasAccessToTab(savedTab)) {
            this.currentTab = savedTab;
        }

        this.renderLayout();
        this.startClock();
        await this.loadMasterData();
        await this.loadCurrentTab();
        this.setupAutoRefresh();

        // Setup observer for dynamic modals, drawers, and popups
        if (typeof MutationObserver !== 'undefined' && typeof document !== 'undefined') {
            this.domObserver = new MutationObserver((mutations) => {
                if (this.currentLang === 'ja') {
                    for (const m of mutations) {
                        for (const node of m.addedNodes) {
                            if (node.nodeType === 1) {
                                i18n.localizeDom(node);
                            }
                        }
                    }
                }
            });
            this.domObserver.observe(document.body, { childList: true, subtree: true });
        }
    }

    renderLogin() {
        const app = document.getElementById('app');
        const isLight = this.theme === 'light';

        app.innerHTML = `
            <div class="min-h-screen ${isLight ? 'animate-aurora-light text-slate-800' : 'animate-aurora-dark text-slate-100'} flex items-center justify-center p-4 sm:p-6 lg:p-8 font-sans relative overflow-hidden select-none">
                
                <!-- DYNAMIC FLOATING AURA ORBS (LIVING BACKGROUND) -->
                <div class="absolute -top-24 -left-24 w-[460px] h-[460px] rounded-full blur-3xl pointer-events-none animate-orb-1 ${isLight ? 'bg-gradient-to-tr from-sky-400/25 via-cyan-300/30 to-blue-400/25' : 'bg-gradient-to-tr from-cyan-600/20 via-blue-700/20 to-indigo-800/20'}"></div>
                <div class="absolute -bottom-28 -right-28 w-[500px] h-[500px] rounded-full blur-3xl pointer-events-none animate-orb-2 ${isLight ? 'bg-gradient-to-bl from-indigo-300/30 via-purple-300/25 to-blue-300/20' : 'bg-gradient-to-bl from-purple-600/20 via-indigo-600/20 to-blue-700/20'}"></div>
                <div class="absolute top-1/4 left-1/2 -translate-x-1/2 w-[380px] h-[380px] rounded-full blur-3xl pointer-events-none animate-orb-3 ${isLight ? 'bg-gradient-to-r from-amber-300/15 via-teal-200/20 to-sky-300/15' : 'bg-gradient-to-r from-cyan-500/10 via-emerald-500/10 to-blue-500/10'}"></div>
                
                <!-- SUBTLE GEOMETRIC GRID OVERLAY -->
                <div class="absolute inset-0 ${isLight ? 'login-grid-pattern-light opacity-50' : 'login-grid-pattern opacity-40'} pointer-events-none"></div>

                <!-- MAIN SLIM LOGIN CARD -->
                <div class="max-w-3xl w-full ${isLight ? 'bg-white/95 border-slate-200 shadow-[0_20px_50px_rgba(0,0,0,0.1)] backdrop-blur-xl' : 'bg-[#091124]/95 border-[#1B2C56] shadow-[0_20px_60px_rgba(0,0,0,0.7)] backdrop-blur-xl'} border rounded-2xl overflow-hidden grid grid-cols-1 md:grid-cols-12 relative z-10">
                    
                    <!-- SISI KIRI (PT YASUNAGA INDONESIA FACTORY BG - KEN BURNS DYNAMIC & CLEAN) -->
                    <div class="md:col-span-5 relative overflow-hidden min-h-[320px] md:min-h-[440px] flex flex-col justify-end p-6 sm:p-7 border-b md:border-b-0 md:border-r ${isLight ? 'border-slate-200' : 'border-[#152347]'} bg-slate-950">
                        <!-- VIBRANT FACTORY PHOTO BACKGROUND (SMOOTH KEN BURNS SLOW ZOOM IN / ZOOM OUT) -->
                        <div class="absolute inset-0 bg-cover bg-center bg-no-repeat animate-ken-burns" style="background-image: url('/images/yasunaga-factory.jpg');"></div>
                        
                        <!-- SMOOTH BOTTOM GRADIENT SHADOW (HIGH CONTRAST TRANSPARENT) -->
                        <div class="absolute inset-0 bg-gradient-to-t from-black/95 via-black/40 to-transparent pointer-events-none"></div>

                        <!-- BAWAH: TRANSPARENT CLEAN TYPOGRAPHY -->
                        <div class="relative z-10 space-y-1 pb-1">
                            <div class="flex items-center gap-2">
                                <span class="text-[11px] sm:text-xs font-black text-amber-400 font-mono tracking-widest uppercase drop-shadow-[0_2px_4px_rgba(0,0,0,1)]">
                                    PT Yasunaga Indonesia
                                </span>
                            </div>
                            
                            <h1 class="text-xl sm:text-2xl font-black text-white leading-snug tracking-tight drop-shadow-[0_2px_8px_rgba(0,0,0,1)] drop-shadow-[0_4px_16px_rgba(0,0,0,0.9)]">
                                Overall Equipment Effectiveness
                            </h1>
                        </div>
                    </div>

                    <!-- SISI KANAN (FORM LOGIN MANUAL ONLY - SLIM & PROPORTIONAL) -->
                    <div class="md:col-span-7 p-6 sm:p-8 flex flex-col justify-center ${isLight ? 'bg-white/95' : 'bg-[#091124]'}">
                        <div>
                            <div class="flex items-center justify-between mb-6">
                                <div>
                                    <h3 class="text-xl font-black ${isLight ? 'text-slate-900' : 'text-white'} tracking-tight">Selamat Datang</h3>
                                    <p class="text-xs ${isLight ? 'text-slate-500' : 'text-slate-400'} mt-0.5">Silakan masuk menggunakan akun corporate Anda.</p>
                                </div>
                                <button id="btn-login-theme-toggle" class="p-2 rounded-xl ${isLight ? 'bg-slate-100 hover:bg-slate-200 border-slate-200 text-slate-700 shadow-sm' : 'bg-slate-900 hover:bg-slate-800 border-slate-800 text-slate-400'} border transition-colors cursor-pointer" title="Toggle Theme">
                                    <i data-lucide="${isLight ? 'moon' : 'sun'}" class="w-4 h-4 text-amber-400"></i>
                                </button>
                            </div>

                            <!-- LOGIN ERROR ALERT -->
                            <div id="login-error-alert" class="hidden mb-4 p-3 rounded-xl bg-rose-950/60 border border-rose-800/80 text-rose-300 text-xs flex items-start gap-2 font-sans">
                                <i data-lucide="alert-circle" class="w-4 h-4 text-rose-400 flex-shrink-0 mt-0.5"></i>
                                <span id="login-error-text">Email atau password yang Anda masukkan salah.</span>
                            </div>

                            <!-- FORM LOGIN MANUAL -->
                            <form id="form-login" class="space-y-4">
                                <div>
                                    <label class="block text-xs font-semibold ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1.5 flex items-center justify-between">
                                        <span>Username / Email</span>
                                        <span class="text-[10px] ${isLight ? 'text-slate-500' : 'text-slate-500'} font-mono">@prodcr.yasunaga.com</span>
                                    </label>
                                    <div class="relative">
                                        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none ${isLight ? 'text-slate-400' : 'text-slate-500'}">
                                            <i data-lucide="user" class="w-4 h-4"></i>
                                        </div>
                                        <input type="text" id="login-email" required placeholder="nama@prodcr.yasunaga.com atau username" value="" class="w-full ${isLight ? 'bg-slate-50 border-slate-300 text-slate-900 focus:border-cyan-600 focus:ring-1 focus:ring-cyan-600 placeholder-slate-400' : 'bg-[#070D1E] border-[#1B2C56] text-white focus:border-cyan-500 focus:ring-1 focus:ring-cyan-500 placeholder-slate-500'} border rounded-xl pl-9 pr-3 py-2.5 text-xs font-sans transition-all outline-none" />
                                    </div>
                                </div>

                                <div>
                                    <label class="block text-xs font-semibold ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1.5 flex items-center justify-between">
                                        <span>Password</span>
                                    </label>
                                    <div class="relative">
                                        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none ${isLight ? 'text-slate-400' : 'text-slate-500'}">
                                            <i data-lucide="lock" class="w-4 h-4"></i>
                                        </div>
                                        <input type="password" id="login-password" required placeholder="Masukkan password Anda" value="" class="w-full ${isLight ? 'bg-slate-50 border-slate-300 text-slate-900 focus:border-cyan-600 focus:ring-1 focus:ring-cyan-600 placeholder-slate-400' : 'bg-[#070D1E] border-[#1B2C56] text-white focus:border-cyan-500 focus:ring-1 focus:ring-cyan-500 placeholder-slate-500'} border rounded-xl pl-9 pr-10 py-2.5 text-xs font-sans transition-all outline-none" />
                                        <button type="button" id="btn-toggle-password" class="absolute inset-y-0 right-0 pr-3 flex items-center ${isLight ? 'text-slate-400 hover:text-slate-600' : 'text-slate-500 hover:text-slate-300'} cursor-pointer">
                                            <i data-lucide="eye" class="w-4 h-4" id="icon-eye"></i>
                                        </button>
                                    </div>
                                </div>

                                <div class="flex items-center justify-between text-xs pt-0.5">
                                    <label class="flex items-center gap-2 cursor-pointer ${isLight ? 'text-slate-600 hover:text-slate-800' : 'text-slate-400 hover:text-slate-200'}">
                                        <input type="checkbox" id="remember-me" checked class="rounded ${isLight ? 'bg-slate-100 border-slate-300 text-cyan-600' : 'bg-[#070D1E] border-slate-700 text-cyan-500'} focus:ring-0 cursor-pointer" />
                                        <span class="text-[11px]">Ingat sesi login saya</span>
                                    </label>
                                    <span class="text-[10px] ${isLight ? 'text-emerald-700' : 'text-emerald-400'} font-mono flex items-center gap-1">
                                        <span class="w-1.5 h-1.5 rounded-full ${isLight ? 'bg-emerald-600' : 'bg-emerald-400'} animate-pulse"></span> Security Auth
                                    </span>
                                </div>

                                <button type="submit" id="btn-submit-login" class="w-full py-2.5 px-4 bg-gradient-to-r from-cyan-600 to-blue-600 hover:from-cyan-500 hover:to-blue-500 text-white font-bold text-xs rounded-xl shadow-lg shadow-cyan-600/30 transition-all flex items-center justify-center gap-2 cursor-pointer mt-2">
                                    <i data-lucide="log-in" class="w-4 h-4"></i>
                                    <span>Sign In to Workstation</span>
                                </button>
                            </form>
                        </div>
                    </div>

                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();
        this.bindLoginEvents();
    }

    bindLoginEvents() {
        const formLogin = document.getElementById('form-login');
        const loginEmail = document.getElementById('login-email');
        const loginPass = document.getElementById('login-password');
        const btnTogglePass = document.getElementById('btn-toggle-password');
        const iconEye = document.getElementById('icon-eye');
        const errorAlert = document.getElementById('login-error-alert');
        const errorText = document.getElementById('login-error-text');
        const btnSubmit = document.getElementById('btn-submit-login');
        const btnTheme = document.getElementById('btn-login-theme-toggle');

        if (btnTheme) {
            btnTheme.addEventListener('click', () => {
                this.theme = this.theme === 'dark' ? 'light' : 'dark';
                this.applyTheme();
                this.renderLogin();
            });
        }

        if (btnTogglePass && loginPass) {
            btnTogglePass.addEventListener('click', () => {
                const isPass = loginPass.type === 'password';
                loginPass.type = isPass ? 'text' : 'password';
                if (iconEye) {
                    iconEye.setAttribute('data-lucide', isPass ? 'eye-off' : 'eye');
                    if (window.lucide) window.lucide.createIcons();
                }
            });
        }

        const executeLogin = async (email, password) => {
            if (errorAlert) errorAlert.classList.add('hidden');
            if (btnSubmit) {
                btnSubmit.disabled = true;
                btnSubmit.innerHTML = `<span class="inline-block animate-spin mr-2">⟳</span> Authenticating...`;
            }

            try {
                const res = await api.login({ email, password });
                const userData = res.data.data.user;
                this.login(userData);
            } catch (err) {
                console.error('Login error:', err);
                if (errorAlert && errorText) {
                    errorText.textContent = err.response?.data?.message || 'Login gagal. Periksa username dan password.';
                    errorAlert.classList.remove('hidden');
                }
                if (btnSubmit) {
                    btnSubmit.disabled = false;
                    btnSubmit.innerHTML = `<i data-lucide="log-in" class="w-4 h-4"></i><span>Sign In to Workstation</span>`;
                    if (window.lucide) window.lucide.createIcons();
                }
            }
        };

        if (formLogin) {
            formLogin.addEventListener('submit', (e) => {
                e.preventDefault();
                executeLogin(loginEmail.value.trim(), loginPass.value);
            });
        }

        document.querySelectorAll('.btn-quick-role').forEach(btn => {
            btn.addEventListener('click', () => {
                const email = btn.getAttribute('data-email');
                if (loginEmail) loginEmail.value = email;
                if (loginPass) loginPass.value = 'password';
                executeLogin(email, 'password');
            });
        });
    }

    login(userData) {
        this.user = {
            id: userData.id,
            name: userData.name,
            email: userData.email,
            role: (userData.roles && userData.roles[0]) ? userData.roles[0] : (userData.role || 'Operator'),
            role_keys: userData.role_keys || ['operator'],
            permissions: userData.permissions || [],
        };
        localStorage.setItem('oee_user', JSON.stringify(this.user));
        localStorage.setItem('oee_logged_in', 'true');
        
        if (window.history && window.history.replaceState) {
            window.history.replaceState({}, '', '/');
        }

        // Return to dashboard or allowed first tab
        this.currentTab = this.hasAccessToTab('dashboard') ? 'dashboard' : (this.hasAccessToTab('monitoring') ? 'monitoring' : 'daily-report');
        localStorage.setItem('oee_active_tab', this.currentTab);

        this.showNotification('Login Berhasil', `Selamat datang kembali, ${this.user.name} (${this.user.role})`, 'success');
        
        this.renderLayout();
        this.startClock();
        this.loadMasterData();
        this.loadCurrentTab();
        this.setupAutoRefresh();
    }

    logout() {
        localStorage.setItem('oee_logged_in', 'false');
        localStorage.removeItem('oee_active_tab');
        if (window.history && window.history.replaceState) {
            window.history.replaceState({}, '', '/login');
        }
        this.showNotification('Logged Out', 'Sesi login telah selesai.', 'status');
        this.renderLogin();
    }

    applyTheme() {
        const html = document.documentElement;
        if (this.theme === 'light') {
            html.classList.remove('dark');
            html.classList.add('light');
        } else {
            html.classList.remove('light');
            html.classList.add('dark');
        }
        localStorage.setItem('oee_theme', this.theme);

        // Update Theme Toggle Button Icon & Title in Topbar
        const btnTheme = document.getElementById('btn-theme-toggle');
        if (btnTheme) {
            btnTheme.title = this.theme === 'dark' ? 'Switch to Light Mode' : 'Switch to Dark Mode';
            btnTheme.innerHTML = `<i data-lucide="${this.theme === 'dark' ? 'sun' : 'moon'}" class="w-4 h-4 ${this.theme === 'dark' ? 'text-amber-400 hover:text-amber-300' : 'text-cyan-600 hover:text-cyan-700'}"></i>`;
            if (window.lucide) window.lucide.createIcons();
        }

        // Update Settings Select if present
        const themeSelect = document.getElementById('setting-theme-select');
        if (themeSelect && themeSelect.value !== this.theme) {
            themeSelect.value = this.theme;
        }
    }

    startClock() {
        const updateClock = () => {
            const timeEl = document.getElementById('header-clock-time');
            const dateEl = document.getElementById('header-clock-date');
            const clockEl = document.getElementById('header-clock');
            const now = new Date();

            const hours = String(now.getHours()).padStart(2, '0');
            const minutes = String(now.getMinutes()).padStart(2, '0');
            const seconds = String(now.getSeconds()).padStart(2, '0');
            const timeStr = `${hours}:${minutes}:${seconds}`;

            const day = String(now.getDate()).padStart(2, '0');
            const month = now.toLocaleDateString('en-GB', { month: 'short' });
            const year = now.getFullYear();
            const dateStr = `${day} ${month} ${year}`;

            if (timeEl) timeEl.textContent = timeStr;
            if (dateEl) dateEl.textContent = dateStr;
            if (clockEl && !timeEl) clockEl.textContent = `${timeStr} ${dateStr}`;
        };

        updateClock();
        setInterval(updateClock, 1000);
    }

    async loadMasterData(force = false) {
        if (this.masterDataLoaded && !force && this.masterData.lines && this.masterData.lines.length > 0) {
            return;
        }
        try {
            const [pRes, lRes, mRes, prodRes, dtRes, defRes, sRes, gRes, setRes, compRes, ngSecRes, defCatRes] = await Promise.all([
                api.getPlants(),
                api.getProductionLines(),
                api.getMachines(),
                api.getProducts(),
                api.getDowntimeReasons(),
                api.getDefectReasons(),
                api.getShifts(),
                api.getGroups(),
                api.getSettings(),
                api.getCompanyProfile ? api.getCompanyProfile() : Promise.resolve({ data: { data: {} } }),
                api.getNgSections ? api.getNgSections() : Promise.resolve({ data: { data: [] } }),
                api.getDefectCategories ? api.getDefectCategories() : Promise.resolve({ data: { data: [] } }),
            ]);

            this.masterData.plants = pRes.data.data || [];
            this.masterData.lines = lRes.data.data || [];
            this.masterData.machines = mRes.data.data || [];
            this.masterData.products = prodRes.data.data || [];
            this.masterData.downtimeReasons = dtRes.data.data || [];
            this.masterData.defectReasons = defRes.data.data || [];
            this.masterData.shifts = sRes.data.data || [];
            this.masterData.groups = gRes.data.data || [];
            this.masterData.settings = setRes.data.data || [];
            this.masterData.ngSections = ngSecRes.data?.data || [];
            this.masterData.defectCategories = defCatRes.data?.data || [];
            this.companyProfile = compRes.data?.data || {
                company_name: 'PT. CONNECTING ROD INDONESIA',
                company_tagline: 'High Precision Automotive Component Manufacturer',
                plant_name: 'CONNECTING ROD MANUFACTURE',
                plant_code: 'PLT-01',
                plant_location: 'Kawasan Industri Cikarang',
                address: 'Kawasan Industri GIIC Blok AB No. 12, Cikarang Pusat, Bekasi, Jawa Barat 17530',
                phone: '+62 21 8990 1234',
                email: 'production.mes@connectingrod.co.id',
                website: 'www.connectingrod-mfg.co.id',
                doc_prefix: 'MES-OEE',
                app_title: 'LinePulse | Smart Production Performance Monitoring',
                company_logo: '',
            };
            this.masterDataLoaded = true;

            this.applyCompanyBranding();
            this.populateFilterDropdowns();
        } catch (err) {
            console.error('Error loading master data options:', err);
        }
    }

    getProductsForLine(lineId) {
        if (!lineId || !this.masterData.products) return [];
        const lineObj = (this.masterData.lines || []).find(l => String(l.id) === String(lineId));
        const lineName = lineObj ? (lineObj.name || lineObj.code || '') : '';
        const lineCode = lineObj ? (lineObj.code || lineObj.name || '') : '';

        // 1. Direct match by production_line_id
        let prods = this.masterData.products.filter(p => p.production_line_id && String(p.production_line_id) === String(lineId));
        if (prods.length > 0) return prods;

        // 2. Intelligent Alias Mapping for Lines
        const norm = (lineName + ' ' + lineCode).toUpperCase();
        if (norm.includes('FX-10') || norm.includes('FX10') || norm.includes('OP10') || norm.includes('OP-10')) {
            return this.masterData.products.filter(p => {
                const pText = ((p.name || '') + ' ' + (p.sku || '')).toUpperCase();
                return pText.includes('OP10') || pText.includes('OP-10') || pText.includes('1210A4LC0A 5') || pText.includes('5.5S');
            });
        }
        if (norm.includes('FX-11') || norm.includes('FX11')) {
            return this.masterData.products.filter(p => {
                const pText = ((p.name || '') + ' ' + (p.sku || '')).toUpperCase();
                return pText.includes('FX11') || pText.includes('FX-11') || pText.includes('1210A4LC0A 97') || pText.includes('97.5S');
            });
        }
        if (norm.includes('FX-1') || norm.includes('FX1')) {
            const fx1Names = ['RT140-SKC', '12R', '1GZ', '22R', '3RZ', '3SFE', '3SG', '3VZ', '5K', '5S FE'];
            return this.masterData.products.filter(p => fx1Names.includes(p.name));
        }
        if (norm.includes('FX-2') || norm.includes('FX2')) {
            const fx2Names = ['E9', 'EA8', 'RT100/120', 'E7/8', '4D5-INA', 'ED-ADM', 'ED-YC', 'EF-ADM', 'EF-YC', 'HR12'];
            return this.masterData.products.filter(p => fx2Names.includes(p.name) && p.sku !== '1210A4LC0A 5' && p.sku !== '1210A4LC0A 97');
        }
        if (norm.includes('FX-3') || norm.includes('FX3')) {
            const fx3Names = ['RT80/90', 'SL-L', 'SL-S', 'KR-YC', '2NZ', '2NZ-CKD', 'FX3-1NR-ADM', 'FX3-1NR-TMMIN', 'FX3-2NR-ADM', 'FX3-2NR-TMMIN'];
            return this.masterData.products.filter(p => fx3Names.includes(p.name) || (p.sku && p.sku.includes('FX3')));
        }
        if (norm.includes('FX-4') || norm.includes('FX4')) {
            const fx4Names = ['KR-ADM', '1TR', '2TR', '2TR-EXP', '4N14'];
            return this.masterData.products.filter(p => fx4Names.includes(p.name) && p.sku !== '1115A481' && p.sku !== '1115A384YI');
        }
        if (norm.includes('FX-5') || norm.includes('FX5')) {
            const fx5Names = ['1ND', '1NZ', '4N13', '4N14-4X45', '604F-KR'];
            return this.masterData.products.filter(p => fx5Names.includes(p.name));
        }
        if (norm.includes('FX-6') || norm.includes('FX6')) {
            const fx6Names = ['3SZ-HL', '3SZ', 'K3-HL', 'K3'];
            return this.masterData.products.filter(p => fx6Names.includes(p.name));
        }
        if (norm.includes('FX-7') || norm.includes('FX7')) {
            return this.masterData.products.filter(p => (p.name && p.name.includes('FX7')) || (p.sku && p.sku.includes('FX7')));
        }
        if (norm.includes('FX-8') || norm.includes('FX8')) {
            const fx8Names = ['8AR', '2UR-FSE', '2UR-GSE', '3UR', '4GR'];
            return this.masterData.products.filter(p => fx8Names.includes(p.name));
        }
        if (norm.includes('FX-9') || norm.includes('FX9')) {
            return this.masterData.products.filter(p => (p.name && (p.name.includes('FX9') || p.name === 'D81F')) || (p.sku && p.sku.includes('FX9')));
        }

        return [];
    }

    applyCompanyBranding() {
        if (!this.companyProfile) return;

        // Update Document Title
        if (this.companyProfile.app_title) {
            document.title = this.companyProfile.app_title;
        }

        // Update Document Favicon with Company Logo
        if (this.companyProfile.company_logo) {
            let favEl = document.getElementById('app-favicon');
            if (!favEl) {
                favEl = document.createElement('link');
                favEl.id = 'app-favicon';
                favEl.rel = 'icon';
                document.head.appendChild(favEl);
            }
            favEl.href = this.companyProfile.company_logo;
        }

        // Update Sidebar Brand Logo & Name
        const logoContainer = document.getElementById('sidebar-brand-logo-container');
        const nameEl = document.getElementById('sidebar-brand-name');
        const tagEl = document.getElementById('sidebar-brand-tagline');

        if (logoContainer) {
            if (this.companyProfile.company_logo) {
                logoContainer.innerHTML = `<img src="${this.escapeHtml(this.companyProfile.company_logo)}" class="w-9 h-9 object-contain drop-shadow-sm" alt="Logo" />`;
            } else {
                logoContainer.innerHTML = `<i data-lucide="activity" class="w-7 h-7 text-cyan-400"></i>`;
                if (window.lucide) window.lucide.createIcons();
            }
        }
        if (nameEl) {
            nameEl.textContent = this.companyProfile.plant_name || this.companyProfile.company_name || 'OEE Sys';
        }
        if (tagEl) {
            tagEl.textContent = this.companyProfile.plant_code ? `${this.companyProfile.plant_code} • Enterprise Edition` : 'Enterprise Edition';
        }
    }

    populateFilterDropdowns() {
        const lineSelect = document.getElementById('filter-line');
        if (lineSelect && this.masterData.lines.length > 0) {
            lineSelect.innerHTML = '<option value="" class="bg-slate-900 text-slate-100">All Production Lines</option>' +
                this.masterData.lines.map(l => `<option value="${l.id}" class="bg-slate-900 text-slate-100" ${this.filters.line_id == l.id ? 'selected' : ''}>${l.code} - ${l.name}</option>`).join('');
        }

        const machineSelect = document.getElementById('filter-machine');
        if (machineSelect && this.masterData.machines.length > 0) {
            const measuringMachines = this.masterData.machines.filter(m => (m.code || '').includes('MEASURING') || (m.name || '').includes('Measuring'));
            const otherMachines = this.masterData.machines.filter(m => !(m.code || '').includes('MEASURING') && !(m.name || '').includes('Measuring'));

            let optionsHtml = `
                <option value="all_measuring" class="bg-slate-900 text-cyan-300 font-bold" ${this.filters.machine_id === 'all_measuring' ? 'selected' : ''}>⭐ All Measuring Machines (Semua Line)</option>
                <option value="" class="bg-slate-900 text-slate-100" ${!this.filters.machine_id ? 'selected' : ''}>All Machines (Semua Mesin)</option>
            `;

            if (measuringMachines.length > 0) {
                optionsHtml += `<optgroup label="── Mesin Measuring per Line ──" class="bg-slate-950 text-cyan-400 font-bold">`;
                measuringMachines.forEach(m => {
                    const lineName = m.work_center?.production_line?.name || m.work_center?.production_line?.code || '';
                    optionsHtml += `<option value="${m.id}" class="bg-slate-900 text-slate-100" ${this.filters.machine_id == m.id ? 'selected' : ''}>Measuring - ${lineName ? `${lineName} ` : ''}(${m.code})</option>`;
                });
                optionsHtml += `</optgroup>`;
            }

            if (otherMachines.length > 0) {
                optionsHtml += `<optgroup label="── Mesin Operasional (OP) ──" class="bg-slate-950 text-slate-400">`;
                otherMachines.forEach(m => {
                    optionsHtml += `<option value="${m.id}" class="bg-slate-900 text-slate-100" ${this.filters.machine_id == m.id ? 'selected' : ''}>${m.name} (${m.code})</option>`;
                });
                optionsHtml += `</optgroup>`;
            }

            machineSelect.innerHTML = optionsHtml;
        }

        const shiftSelect = document.getElementById('filter-shift');
        if (shiftSelect && this.masterData.shifts.length > 0) {
            shiftSelect.innerHTML = '<option value="" class="bg-slate-900 text-slate-100">All Shifts</option>' +
                this.masterData.shifts.map(s => `<option value="${s.id}" class="bg-slate-900 text-slate-100" ${this.filters.shift_id == s.id ? 'selected' : ''}>${s.name} (${s.start_time.slice(0,5)} - ${s.end_time.slice(0,5)})</option>`).join('');
        }
    }

    setupAutoRefresh() {
        if (this.autoRefreshTimer) clearInterval(this.autoRefreshTimer);
        this.autoRefreshTimer = setInterval(() => {
            // Jangan pernah auto-refresh pada halaman settings (profil perusahaan), database management, input laporan harian, input laporan NG, atau saat modal terbuka
            if (this.currentTab === 'settings' || this.currentTab === 'database' || this.currentTab === 'daily-report' || this.currentTab === 'ng-report' || document.querySelector('.modal-active, #modal-container:not(.hidden)')) {
                return;
            }
            console.log('Auto-refreshing dashboard metrics for tab:', this.currentTab);
            this.loadCurrentTab(false);
        }, 30000);
    }

    renderFilterBar() {
        const linesOptions = [
            `<option value="" ${!this.filters.line_id ? 'selected' : ''} class="bg-slate-900 text-slate-100">${this.t('header.all_lines', 'All Production Lines')}</option>`,
            ...this.masterData.lines.map(l => `<option value="${l.id}" class="bg-slate-900 text-slate-100" ${this.filters.line_id == l.id ? 'selected' : ''}>${l.code} - ${l.name}</option>`)
        ].join('');

        const measuringMachines = (this.masterData.machines || []).filter(m => (m.code || '').includes('MEASURING') || (m.name || '').includes('Measuring'));
        const otherMachines = (this.masterData.machines || []).filter(m => !(m.code || '').includes('MEASURING') && !(m.name || '').includes('Measuring'));

        const optMeasuringLabel = this.currentLang === 'ja' ? '⭐ 全測定器 (全ライン)' : '⭐ All Measuring Machines (Semua Line)';
        const optAllMachinesLabel = this.t('header.all_machines', 'All Machines (Semua Mesin)');
        const groupMeasuringLabel = this.currentLang === 'ja' ? '── ライン別 測定器 ──' : '── Mesin Measuring per Line ──';
        const groupOperationalLabel = this.currentLang === 'ja' ? '── 製造設備 (OP) ──' : '── Mesin Operasional (OP) ──';

        const machinesOptions = `
            <option value="all_measuring" ${this.filters.machine_id === 'all_measuring' ? 'selected' : ''} class="bg-slate-900 text-cyan-300 font-bold">${optMeasuringLabel}</option>
            <option value="" ${!this.filters.machine_id ? 'selected' : ''} class="bg-slate-900 text-slate-100">${optAllMachinesLabel}</option>
            ${measuringMachines.length > 0 ? `
                <optgroup label="${groupMeasuringLabel}" class="bg-slate-950 text-cyan-400 font-bold">
                    ${measuringMachines.map(m => {
                        const lineName = m.work_center?.production_line?.name || m.work_center?.production_line?.code || '';
                        return `<option value="${m.id}" class="bg-slate-900 text-slate-100" ${this.filters.machine_id == m.id ? 'selected' : ''}>Measuring - ${lineName ? `${lineName} ` : ''}(${m.code})</option>`;
                    }).join('')}
                </optgroup>
            ` : ''}
            ${otherMachines.length > 0 ? `
                <optgroup label="${groupOperationalLabel}" class="bg-slate-950 text-slate-400">
                    ${otherMachines.map(m => `<option value="${m.id}" class="bg-slate-900 text-slate-100" ${this.filters.machine_id == m.id ? 'selected' : ''}>${m.name} (${m.code})</option>`).join('')}
                </optgroup>
            ` : ''}
        `;

        const shiftsOptions = [
            `<option value="" ${!this.filters.shift_id ? 'selected' : ''} class="bg-slate-900 text-slate-100">${this.t('header.all_shifts', 'All Shifts')}</option>`,
            ...this.masterData.shifts.map(s => `<option value="${s.id}" class="bg-slate-900 text-slate-100" ${this.filters.shift_id == s.id ? 'selected' : ''}>${s.name} (${s.start_time.slice(0,5)} - ${s.end_time.slice(0,5)})</option>`)
        ].join('');

        const isCustom = this.filters.period === 'custom';

        return `
            <div class="flex items-center gap-2.5 flex-wrap">
                <div class="flex items-center gap-1.5 bg-slate-950 border border-slate-800 rounded-lg px-2.5 py-1 text-slate-300">
                    <i data-lucide="git-fork" class="w-3.5 h-3.5 text-cyan-400"></i>
                    <select id="filter-line" class="bg-transparent text-xs text-slate-200 focus:outline-none cursor-pointer font-sans">
                        ${linesOptions}
                    </select>
                </div>

                <div class="flex items-center gap-1.5 bg-slate-950 border border-slate-800 rounded-lg px-2.5 py-1 text-slate-300">
                    <i data-lucide="cpu" class="w-3.5 h-3.5 text-emerald-400"></i>
                    <select id="filter-machine" class="bg-transparent text-xs text-slate-200 focus:outline-none cursor-pointer font-sans">
                        ${machinesOptions}
                    </select>
                </div>

                <div class="flex items-center gap-1.5 bg-slate-950 border border-slate-800 rounded-lg px-2.5 py-1 text-slate-300">
                    <i data-lucide="clock" class="w-3.5 h-3.5 text-amber-400"></i>
                    <select id="filter-shift" class="bg-transparent text-xs text-slate-200 focus:outline-none cursor-pointer font-sans">
                        ${shiftsOptions}
                    </select>
                </div>

                <div class="flex items-center gap-1.5 bg-slate-950 border border-slate-800 rounded-lg px-2.5 py-1 text-slate-300">
                    <i data-lucide="calendar" class="w-3.5 h-3.5 text-emerald-400"></i>
                    <select id="filter-period" class="bg-transparent text-slate-200 text-xs focus:outline-none cursor-pointer font-sans">
                        <option value="30days" class="bg-slate-900 text-slate-100" ${this.filters.period === '30days' ? 'selected' : ''}>${this.t('header.last_30_days', 'Last 30 Days')}</option>
                        <option value="7days" class="bg-slate-900 text-slate-100" ${this.filters.period === '7days' ? 'selected' : ''}>${this.t('header.last_7_days', 'Last 7 Days')}</option>
                        <option value="today" class="bg-slate-900 text-slate-100" ${this.filters.period === 'today' ? 'selected' : ''}>${this.t('header.today', 'Today (Hari Ini)')}</option>
                        <option value="yesterday" class="bg-slate-900 text-slate-100" ${this.filters.period === 'yesterday' ? 'selected' : ''}>${this.t('header.yesterday', 'Yesterday (Kemarin)')}</option>
                        <option value="this_month" class="bg-slate-900 text-slate-100" ${this.filters.period === 'this_month' ? 'selected' : ''}>${this.t('header.this_month', 'This Month (Bulan Ini)')}</option>
                        <option value="last_month" class="bg-slate-900 text-slate-100" ${this.filters.period === 'last_month' ? 'selected' : ''}>${this.currentLang === 'ja' ? '先月 (Last Month)' : 'Last Month (Bulan Lalu)'}</option>
                        <option value="custom" class="bg-slate-900 text-slate-100" ${this.filters.period === 'custom' ? 'selected' : ''}>${this.currentLang === 'ja' ? '期間指定 (カスタム)...' : 'Custom Date Range...'}</option>
                    </select>
                </div>

                <div id="custom-date-container" class="${isCustom ? 'flex' : 'hidden'} items-center gap-2">
                    <input type="date" id="filter-start-date" value="${this.filters.start_date || ''}" class="bg-slate-950 border border-slate-800 text-xs rounded-lg px-2.5 py-1 text-slate-200 focus:outline-none focus:border-cyan-500 font-sans" />
                    <span class="text-xs text-slate-500">${this.currentLang === 'ja' ? '〜' : 's/d'}</span>
                    <input type="date" id="filter-end-date" value="${this.filters.end_date || ''}" class="bg-slate-950 border border-slate-800 text-xs rounded-lg px-2.5 py-1 text-slate-200 focus:outline-none focus:border-cyan-500 font-sans" />
                </div>

                <button id="btn-apply-filters" class="bg-cyan-600 hover:bg-cyan-500 text-white text-xs px-3 py-1.5 rounded-lg font-medium transition-all shadow-md shadow-cyan-600/20 flex items-center gap-1.5 cursor-pointer">
                    <i data-lucide="filter" class="w-3.5 h-3.5"></i>
                    <span>${this.t('header.apply_filter', 'Apply Filter')}</span>
                </button>
            </div>
        `;
    }

    renderLayout() {
        const app = document.getElementById('app');
        const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');

        app.innerHTML = `
            <div class="flex h-screen bg-[#070D1E] text-slate-100 overflow-hidden font-sans" id="main-container">
                <!-- LEFT SIDEBAR (DYNAMIC PASTEL & NAVY INDUSTRIAL DESIGN) -->
                <aside id="sidebar" class="w-60 flex flex-col justify-between flex-shrink-0 transition-all duration-300 shadow-2xl z-20 select-none">
                    <div class="flex flex-col min-h-0 flex-1">
                        <!-- BRAND LOGO -->
                        <div id="sidebar-brand-header" class="h-14 px-4 flex items-center justify-between border-b border-[#152347] flex-shrink-0 box-border">
                            <div class="flex items-center gap-3 min-w-0">
                                <div id="sidebar-brand-logo-container" class="flex-shrink-0 flex items-center justify-center">
                                    ${this.companyProfile?.company_logo 
                                        ? `<img src="${this.escapeHtml(this.companyProfile.company_logo)}" class="w-8 h-8 object-contain drop-shadow-sm" alt="Logo" />` 
                                        : `<i data-lucide="activity" class="w-6 h-6 text-cyan-400"></i>`
                                    }
                                </div>
                                <div class="min-w-0 flex-1">
                                    <h1 id="sidebar-brand-name" class="font-extrabold text-white tracking-wide leading-tight truncate text-xs">${this.companyProfile?.plant_name || 'OEE Sys'}</h1>
                                    <span id="sidebar-brand-tagline" class="text-[9.5px] text-cyan-400 font-medium tracking-wider uppercase truncate block">${this.companyProfile?.plant_code ? `${this.companyProfile.plant_code} • MES Standard` : 'Enterprise MES'}</span>
                                </div>
                            </div>
                        </div>

                        <!-- NAVIGATION MENU WITH CATEGORIZED MODULE DIVIDERS -->
                        <nav class="p-2 space-y-0.5 overflow-y-auto flex-1 custom-scrollbar" id="nav-menu">
                            <!-- MODUL 1: OPERASIONAL & INPUT -->
                            ${(this.hasAccessToTab('dashboard') || this.hasAccessToTab('daily-report') || this.hasAccessToTab('ng-report') || this.hasAccessToTab('monitoring')) ? `
                                <div class="px-2.5 pt-1.5 pb-1 flex items-center justify-between">
                                    <span class="sidebar-section-title text-[9.5px] font-bold uppercase tracking-wider text-slate-400 font-mono flex items-center gap-1.5">
                                        <i data-lucide="layers" class="w-3 h-3 text-cyan-400"></i>
                                        <span>${this.t('nav.section_operations', 'Operasional & Input')}</span>
                                    </span>
                                </div>
                                ${this.navItem('dashboard', 'layout-dashboard', this.t('nav.dashboard', 'Dashboard'))}
                                ${this.navItem('daily-report', 'clipboard-edit', this.t('nav.daily_input', 'Input Laporan Harian'))}
                                ${this.navItem('ng-report', 'shield-alert', this.t('nav.ng_input', 'Input Laporan NG'), this.t('nav.new_badge', 'NEW'))}
                                ${this.navItem('monitoring', 'monitor', this.t('nav.monitoring', 'Production Monitoring'))}
                            ` : ''}

                            <!-- MODUL 2: ANALISIS & PERFORMANCE -->
                            ${(this.hasAccessToTab('machines') || this.hasAccessToTab('lines') || this.hasAccessToTab('shifts') || this.hasAccessToTab('teams') || this.hasAccessToTab('downtime') || this.hasAccessToTab('quality')) ? `
                                <div class="sidebar-section-divider"></div>
                                <div class="px-2.5 pt-1.5 pb-1 flex items-center justify-between">
                                    <span class="sidebar-section-title text-[9.5px] font-bold uppercase tracking-wider text-slate-400 font-mono flex items-center gap-1.5">
                                        <i data-lucide="bar-chart-2" class="w-3 h-3 text-sky-400"></i>
                                        <span>${this.t('nav.section_analytics', 'Analisis & SPC')}</span>
                                    </span>
                                </div>
                                ${this.navItem('machines', 'cpu', this.t('nav.machine_perf', 'Machine Performance'))}
                                ${this.navItem('lines', 'git-fork', this.t('nav.line_perf', 'Production Lines'))}
                                ${this.navItem('shifts', 'clock', this.t('nav.shift_perf', 'Shift Performance'))}
                                ${this.navItem('teams', 'users', this.t('nav.team_perf', 'Team Performance'))}
                                ${this.navItem('downtime', 'alert-triangle', this.t('nav.downtime_analysis', 'Downtime Analysis'))}
                                ${this.navItem('quality', 'pie-chart', this.t('nav.quality_perf', 'Quality Performance'), 'PARETO')}
                            ` : ''}

                            <!-- MODUL 3: REPORT & DATA MASTER -->
                            ${(this.hasAccessToTab('reports') || this.hasAccessToTab('master')) ? `
                                <div class="sidebar-section-divider"></div>
                                <div class="px-2.5 pt-1.5 pb-1 flex items-center justify-between">
                                    <span class="sidebar-section-title text-[9.5px] font-bold uppercase tracking-wider text-slate-400 font-mono flex items-center gap-1.5">
                                        <i data-lucide="file-text" class="w-3 h-3 text-emerald-400"></i>
                                        <span>${this.t('nav.section_reports', 'Master & Laporan')}</span>
                                    </span>
                                </div>
                                ${this.navItem('reports', 'file-bar-chart', this.t('nav.reports', 'Reports & Export'))}
                                ${this.navItem('master', 'database', this.t('nav.master_data', 'Master Data'))}
                            ` : ''}

                            <!-- MODUL 4: PENGATURAN SISTEM -->
                            ${(this.hasAccessToTab('users') || this.hasAccessToTab('settings') || this.hasAccessToTab('database')) ? `
                                <div class="sidebar-section-divider"></div>
                                <div class="px-2.5 pt-1.5 pb-1 flex items-center justify-between">
                                    <span class="sidebar-section-title text-[9.5px] font-bold uppercase tracking-wider text-slate-400 font-mono flex items-center gap-1.5">
                                        <i data-lucide="shield" class="w-3 h-3 text-indigo-400"></i>
                                        <span>${this.t('nav.section_system', 'Sistem & Akses')}</span>
                                    </span>
                                </div>
                                ${this.navItem('users', 'users', this.t('nav.users', 'User Management'))}
                                ${this.navItem('settings', 'settings', this.t('nav.settings', 'System Settings'))}
                                ${this.navItem('database', 'hard-drive', this.t('nav.database', 'Manajemen Database'), 'GO-LIVE')}
                            ` : ''}
                        </nav>
                    </div>

                    <!-- USER INFO & TV MODE TOGGLE -->
                    <div id="sidebar-bottom-container" class="p-2.5 flex-shrink-0">
                        <button id="btn-tv-mode" class="sidebar-tv-btn w-full mb-2 flex items-center justify-center gap-1.5 py-2 px-2.5 rounded-xl text-[11px] font-bold transition-all shadow-sm cursor-pointer" title="Fullscreen TV / Display Floor Mode">
                            <i data-lucide="tv" class="w-4 h-4"></i>
                            <span>${this.t('nav.tv_mode', 'TV / Floor Display Mode')}</span>
                        </button>
                        <div id="sidebar-profile-box" class="sidebar-profile-card flex items-center justify-between gap-2 px-2.5 py-1.5 rounded-xl transition-all">
                            <div class="flex items-center gap-2 min-w-0" title="${this.user.name} (${this.user.role})">
                                <div class="relative w-7 h-7 rounded-lg bg-gradient-to-br from-cyan-600 to-blue-700 border border-cyan-400/40 flex items-center justify-center font-bold text-xs text-white shadow-sm flex-shrink-0">
                                    ${(this.user.name || 'SA').slice(0, 2).toUpperCase()}
                                    <span class="absolute -bottom-0.5 -right-0.5 w-2 h-2 rounded-full bg-emerald-400 border border-slate-900 shadow-[0_0_4px_#34d399]"></span>
                                </div>
                                <div class="truncate min-w-0">
                                    <div class="text-[11px] font-bold text-slate-200 truncate leading-tight">${this.user.name}</div>
                                    <div class="text-[9px] text-cyan-400/90 font-mono truncate leading-tight">${this.user.role}</div>
                                </div>
                            </div>
                            <button id="btn-logout" class="p-1.5 rounded-lg text-slate-400 hover:text-rose-400 hover:bg-rose-950/40 transition-colors cursor-pointer flex-shrink-0" title="${this.t('nav.logout', 'Keluar / Logout')}: ${this.user.name}">
                                <i data-lucide="log-out" class="w-4 h-4"></i>
                            </button>
                        </div>
                    </div>
                </aside>

                <!-- MAIN CONTENT AREA (NAVY BG) -->
                <div class="flex-1 flex flex-col min-w-0 bg-[#070D1E] overflow-hidden">
                    <!-- TOPBAR -->
                    <header class="h-14 border-b border-[#152347] bg-[#091124]/90 backdrop-blur-md px-5 flex items-center justify-between z-10 flex-shrink-0">
                        <!-- FILTER BAR -->
                        ${this.renderFilterBar()}

                        <!-- ACTIONS & SYSTEM CLOCK -->
                        <div class="flex items-center gap-3">
                            <!-- REALTIME NOTIFICATION BELL -->
                            <div class="relative cursor-pointer p-1 text-slate-400 hover:text-cyan-400 transition-colors" id="btn-notification-bell" title="${this.t('header.notifications', 'Real-time System Notifications')}">
                                <i data-lucide="bell" class="w-4 h-4"></i>
                                <span id="notif-badge-count" class="hidden absolute top-0 right-0 bg-rose-500 text-white font-mono text-[9px] font-bold px-1 rounded-full animate-pulse">0</span>

                                <!-- NOTIFICATION POPUP DROPDOWN -->
                                <div id="notification-dropdown" class="hidden absolute right-0 mt-2 w-80 md:w-96 bg-[#0B142C] border border-[#1B2C56] rounded-xl shadow-2xl z-50 overflow-hidden font-sans">
                                    <div class="p-3 bg-[#070D1E] border-b border-[#152347] flex items-center justify-between">
                                        <div class="flex items-center gap-2">
                                            <i data-lucide="bell-ring" class="w-4 h-4 text-cyan-400"></i>
                                            <span class="font-bold text-xs text-slate-100">${this.t('header.notifications', 'System Notifications')}</span>
                                            <span id="notif-header-count" class="bg-cyan-950 text-cyan-400 border border-cyan-800 text-[10px] font-bold px-1.5 py-0.5 rounded-full font-mono">0 ${this.t('header.new_notif', 'new')}</span>
                                        </div>
                                        <button id="btn-mark-all-read" class="text-[10px] text-cyan-400 hover:text-cyan-300 font-semibold cursor-pointer">${this.t('header.mark_all_read', 'Mark all read')}</button>
                                    </div>
                                    <div id="notification-list" class="max-h-80 overflow-y-auto divide-y divide-[#152347]/60 text-xs">
                                        <!-- Dynamic Notifications Items -->
                                    </div>
                                    <div class="p-2 bg-[#070D1E] border-t border-[#152347] text-center">
                                        <button id="btn-clear-notifications" class="text-[11px] text-slate-400 hover:text-slate-200 cursor-pointer">${this.t('header.clear_all', 'Clear All Notifications')}</button>
                                    </div>
                                </div>
                            </div>

                            <!-- THEME TOGGLE -->
                            <button id="btn-theme-toggle" class="p-1 text-slate-400 hover:text-cyan-400 transition-colors cursor-pointer" title="${this.theme === 'dark' ? (this.currentLang === 'ja' ? 'ライトモードに切替' : 'Switch to Light Mode') : (this.currentLang === 'ja' ? 'ダークモードに切替' : 'Switch to Dark Mode')}">
                                <i data-lucide="${this.theme === 'dark' ? 'sun' : 'moon'}" class="w-4 h-4 ${this.theme === 'dark' ? 'text-amber-400 hover:text-amber-300' : 'text-cyan-400 hover:text-cyan-300'}"></i>
                            </button>

                            <!-- LANGUAGE SELECTOR TOGGLE (INTERNATIONAL STYLE) -->
                            <div class="relative" id="language-switcher-container">
                                <button id="btn-language-toggle" class="flex items-center gap-1.5 px-2.5 py-1 rounded-lg text-xs font-bold transition-all cursor-pointer ${isLight ? 'bg-slate-100 hover:bg-slate-200 text-slate-700 border border-slate-300' : 'bg-[#0E1A38] hover:bg-[#16254C] text-slate-200 border border-[#16254C]'}" title="${this.t('header.language', 'Language')}">
                                    <i data-lucide="globe" class="w-3.5 h-3.5 text-cyan-400"></i>
                                    <span id="current-lang-label" class="font-mono text-[11px]">${this.currentLang === 'ja' ? '🇯🇵 日本語' : '🇮🇩 ID'}</span>
                                    <i data-lucide="chevron-down" class="w-3 h-3 text-slate-400"></i>
                                </button>
                                <div id="language-dropdown" class="hidden absolute right-0 mt-2 w-48 rounded-xl shadow-2xl z-50 overflow-hidden font-sans border ${isLight ? 'bg-white border-slate-200 divide-slate-100' : 'bg-[#0B142C] border-[#1B2C56] divide-[#152347]/60'} divide-y">
                                    <div class="px-3 py-2 text-[10px] font-bold uppercase tracking-wider ${isLight ? 'bg-slate-50 text-slate-500' : 'bg-[#070D1E] text-slate-400'} flex items-center justify-between">
                                        <span>${this.t('header.language', 'Pilih Bahasa')}</span>
                                        <span class="font-mono text-[9px] px-1.5 py-0.5 rounded bg-cyan-950 text-cyan-400 border border-cyan-800 font-bold">i18n</span>
                                    </div>
                                    <button type="button" data-lang="id" class="lang-option-btn w-full px-3 py-2.5 text-left text-xs flex items-center justify-between transition-colors cursor-pointer ${this.currentLang === 'id' ? (isLight ? 'bg-cyan-50 font-bold text-cyan-800' : 'bg-cyan-950/60 font-bold text-cyan-400') : (isLight ? 'hover:bg-slate-50 text-slate-700' : 'hover:bg-[#0E1A38] text-slate-300')}">
                                        <span class="flex items-center gap-2">
                                            <span class="text-base leading-none">🇮🇩</span>
                                            <span>Bahasa Indonesia</span>
                                        </span>
                                        ${this.currentLang === 'id' ? '<i data-lucide="check" class="w-3.5 h-3.5 text-cyan-500"></i>' : ''}
                                    </button>
                                    <button type="button" data-lang="ja" class="lang-option-btn w-full px-3 py-2.5 text-left text-xs flex items-center justify-between transition-colors cursor-pointer ${this.currentLang === 'ja' ? (isLight ? 'bg-cyan-50 font-bold text-cyan-800' : 'bg-cyan-950/60 font-bold text-cyan-400') : (isLight ? 'hover:bg-slate-50 text-slate-700' : 'hover:bg-[#0E1A38] text-slate-300')}">
                                        <span class="flex items-center gap-2">
                                            <span class="text-base leading-none">🇯🇵</span>
                                            <span>日本語 (Japanese)</span>
                                        </span>
                                        ${this.currentLang === 'ja' ? '<i data-lucide="check" class="w-3.5 h-3.5 text-cyan-500"></i>' : ''}
                                    </button>
                                </div>
                            </div>

                            <!-- SYSTEM CLOCK -->
                            <div class="flex items-center gap-2 text-right font-mono px-1">
                                <i data-lucide="clock" class="w-3.5 h-3.5 text-cyan-400"></i>
                                <div class="leading-tight text-right">
                                    <div id="header-clock-time" class="text-xs font-bold text-cyan-400 tracking-wider">--:--:--</div>
                                    <div id="header-clock-date" class="text-[9.5px] font-medium text-slate-400">-- --- ----</div>
                                </div>
                            </div>

                            <button id="btn-add-record" class="flex items-center gap-1.5 bg-emerald-600 hover:bg-emerald-500 text-white text-xs px-3 py-1.5 rounded-lg font-medium transition-all shadow-md shadow-emerald-600/20 cursor-pointer">
                                <i data-lucide="plus" class="w-3.5 h-3.5"></i>
                                <span>${this.t('header.add_record', '+ Entry Record')}</span>
                            </button>
                        </div>
                    </header>

                    <!-- CONTENT PAGE BODY -->
                    <main id="content-body" class="flex-1 overflow-y-auto p-5 space-y-5 bg-[#070D1E]">
                        <!-- Dynamic page component inserted here -->
                    </main>
                </div>
            </div>

            <!-- MODALS CONTAINER -->
            <div id="modal-container"></div>
        `;

        this.bindGlobalEvents();
        this.loadNotifications();
        if (window.lucide) window.lucide.createIcons();
    }

    navItem(id, icon, label, badge = null) {
        const isActive = this.currentTab === id;
        const activeClass = isActive
            ? 'active'
            : 'text-slate-400';

        const iconColor = isActive 
            ? 'text-cyan-400' 
            : 'text-slate-400 group-hover:text-cyan-300';

        return `
            <a href="#" data-tab="${id}" class="nav-link sidebar-nav-item group flex items-center justify-between px-2.5 py-2 rounded-lg text-xs cursor-pointer ${activeClass}">
                <div class="flex items-center gap-2.5 min-w-0">
                    <i data-lucide="${icon}" class="w-4 h-4 nav-icon flex-shrink-0 ${iconColor} transition-all duration-200"></i>
                    <span class="truncate tracking-wide text-[11.5px] font-medium ${isActive ? 'text-white font-semibold' : 'text-slate-300 group-hover:text-white'}">${label}</span>
                </div>
                <div class="flex items-center gap-1 flex-shrink-0">
                    ${badge ? `<span class="text-[9px] font-mono font-bold px-1.5 py-0.2 rounded ${badge === 'PARETO' ? 'bg-rose-500/20 text-rose-300 border border-rose-500/30' : 'bg-cyan-500/20 text-cyan-300 border border-cyan-500/30'}">${badge}</span>` : ''}
                    ${isActive ? '<span class="w-1.5 h-1.5 rounded-full bg-cyan-400 shadow-[0_0_8px_#22d3ee] animate-pulse"></span>' : ''}
                </div>
            </a>
        `;
    }

    bindGlobalEvents() {
        document.querySelectorAll('.nav-link').forEach(el => {
            el.addEventListener('click', (e) => {
                e.preventDefault();
                const tab = el.getAttribute('data-tab');
                this.switchTab(tab);
            });
        });

        const filterPeriodSelect = document.getElementById('filter-period');
        const customDateContainer = document.getElementById('custom-date-container');

        if (filterPeriodSelect && customDateContainer) {
            filterPeriodSelect.addEventListener('change', (e) => {
                if (e.target.value === 'custom') {
                    customDateContainer.classList.remove('hidden');
                    customDateContainer.classList.add('flex');
                } else {
                    customDateContainer.classList.remove('flex');
                    customDateContainer.classList.add('hidden');
                }
            });
        }

        const autoApplyFilter = () => {
            this.filters.line_id = document.getElementById('filter-line')?.value || '';
            this.filters.machine_id = document.getElementById('filter-machine')?.value || '';
            this.filters.shift_id = document.getElementById('filter-shift')?.value || '';
            this.filters.period = document.getElementById('filter-period')?.value || '';

            if (this.filters.period === 'custom') {
                this.filters.start_date = document.getElementById('filter-start-date')?.value || '';
                this.filters.end_date = document.getElementById('filter-end-date')?.value || '';
            } else {
                this.filters.start_date = '';
                this.filters.end_date = '';
            }

            if (this.filters.period === 'today') {
                this.selectedDailyDate = new Date().toISOString().slice(0, 10);
            } else if (this.filters.period === 'yesterday') {
                const y = new Date();
                y.setDate(y.getDate() - 1);
                this.selectedDailyDate = y.toISOString().slice(0, 10);
            } else if (this.filters.period === 'custom' && this.filters.start_date) {
                this.selectedDailyDate = this.filters.start_date;
            }

            this.dashMachineFilters = { line: '', machine: '', product: '', status: '' };
            this.clearDashboardCache();
            this.loadCurrentTab(false);
        };

        const filterLine = document.getElementById('filter-line');
        const filterMachine = document.getElementById('filter-machine');
        const filterShift = document.getElementById('filter-shift');
        const filterPeriod = document.getElementById('filter-period');
        const filterStart = document.getElementById('filter-start-date');
        const filterEnd = document.getElementById('filter-end-date');

        if (filterLine) filterLine.addEventListener('change', autoApplyFilter);
        if (filterMachine) filterMachine.addEventListener('change', autoApplyFilter);
        if (filterShift) filterShift.addEventListener('change', autoApplyFilter);
        if (filterPeriod) filterPeriod.addEventListener('change', autoApplyFilter);
        if (filterStart) filterStart.addEventListener('change', autoApplyFilter);
        if (filterEnd) filterEnd.addEventListener('change', autoApplyFilter);

        const btnApply = document.getElementById('btn-apply-filters');
        if (btnApply) {
            btnApply.addEventListener('click', autoApplyFilter);
        }

        const btnLogout = document.getElementById('btn-logout');
        if (btnLogout) {
            btnLogout.addEventListener('click', (e) => {
                e.preventDefault();
                this.logout();
            });
        }

        const btnTv = document.getElementById('btn-tv-mode');
        if (btnTv) {
            btnTv.addEventListener('click', () => {
                this.toggleTvMode();
            });
        }

        const btnTheme = document.getElementById('btn-theme-toggle');
        if (btnTheme) {
            btnTheme.addEventListener('click', () => {
                this.theme = this.theme === 'dark' ? 'light' : 'dark';
                this.applyTheme();
                this.renderLayout();
                this.loadCurrentTab(false);
            });
        }

        // LANGUAGE SWITCHER EVENT LISTENERS
        const btnLang = document.getElementById('btn-language-toggle');
        const dropdownLang = document.getElementById('language-dropdown');
        if (btnLang && dropdownLang) {
            btnLang.addEventListener('click', (e) => {
                e.stopPropagation();
                // Close notification dropdown if open
                const notifDropdown = document.getElementById('notification-dropdown');
                if (notifDropdown) notifDropdown.classList.add('hidden');
                
                dropdownLang.classList.toggle('hidden');
            });

            document.querySelectorAll('.lang-option-btn').forEach(btn => {
                btn.addEventListener('click', (e) => {
                    e.stopPropagation();
                    const lang = btn.getAttribute('data-lang');
                    dropdownLang.classList.add('hidden');
                    if (lang && lang !== this.currentLang) {
                        this.setLanguage(lang);
                    }
                });
            });

            // Close dropdown when clicking outside
            document.addEventListener('click', (e) => {
                if (!btnLang.contains(e.target) && !dropdownLang.contains(e.target)) {
                    dropdownLang.classList.add('hidden');
                }
            });
        }

        const btnEntry = document.getElementById('btn-add-record');
        if (btnEntry) {
            btnEntry.addEventListener('click', () => {
                this.showProductionModal();
            });
        }

        // NOTIFICATION BELL EVENT LISTENERS
        const btnNotif = document.getElementById('btn-notification-bell');
        const dropdownNotif = document.getElementById('notification-dropdown');
        const btnMarkAll = document.getElementById('btn-mark-all-read');
        const btnClearNotifs = document.getElementById('btn-clear-notifications');

        if (btnNotif && dropdownNotif) {
            btnNotif.addEventListener('click', (e) => {
                e.stopPropagation();
                dropdownNotif.classList.toggle('hidden');
                this.loadNotifications();
            });

            document.addEventListener('click', (e) => {
                if (dropdownNotif && !btnNotif.contains(e.target) && !dropdownNotif.contains(e.target)) {
                    dropdownNotif.classList.add('hidden');
                }
            });
        }

        if (btnMarkAll) {
            btnMarkAll.addEventListener('click', (e) => {
                e.stopPropagation();
                if (this.notifications) {
                    this.notifications.forEach(n => n.read = true);
                    this.updateNotificationBadge();
                    this.renderNotificationList();
                }
            });
        }

        if (btnClearNotifs) {
            btnClearNotifs.addEventListener('click', (e) => {
                e.stopPropagation();
                this.notifications = [];
                this.updateNotificationBadge();
                this.renderNotificationList();
            });
        }

        if (window.lucide) window.lucide.createIcons();
    }

    async switchTab(tab) {
        if (!this.hasAccessToTab(tab)) {
            this.showNotification('Akses Dibatasi (RBAC)', `Role "${this.user.role}" tidak memiliki izin untuk membuka menu ini.`, 'status');
            return;
        }
        this.currentTab = tab;
        localStorage.setItem('oee_active_tab', tab);
        this.renderLayout();
        await this.loadCurrentTab();
    }

    saveNotifications() {
        try {
            localStorage.setItem('oee_notifications_v2', JSON.stringify(this.notifications || []));
        } catch (e) {
            console.warn('Failed to persist notifications:', e);
        }
    }

    pushNotification(title, message, type = 'info', isSystemAlert = false) {
        if (!this.notifications) {
            this.notifications = [];
        }

        const now = new Date();
        const timeStr = `${String(now.getHours()).padStart(2, '0')}:${String(now.getMinutes()).padStart(2, '0')}`;

        // Prevent duplicate alerts within 5 minutes
        const isDuplicate = this.notifications.some(n => 
            n.title === title && 
            n.message === message && 
            (Date.now() - (n.timestamp || 0)) < 5 * 60 * 1000
        );

        if (isDuplicate) return;

        const notifItem = {
            id: Date.now() + Math.floor(Math.random() * 1000),
            title: title,
            message: message,
            time: timeStr,
            timestamp: Date.now(),
            type: type, // 'danger', 'warning', 'success', 'info'
            read: false,
            isSystemAlert: isSystemAlert
        };

        this.notifications.unshift(notifItem);
        if (this.notifications.length > 25) {
            this.notifications = this.notifications.slice(0, 25);
        }

        this.saveNotifications();
        this.updateNotificationBadge();
        this.renderNotificationList();
    }

    evaluateRealtimeAlerts() {
        if (!this.masterData) return;

        // 1. Real Alert: Mesin yang berstatus STOP / BREAKDOWN di database aktual
        if (this.masterData.machines && this.masterData.machines.length > 0) {
            const stoppedMachines = this.masterData.machines.filter(m => 
                ['STOP', 'STOPPED', 'BREAKDOWN', 'MAINTENANCE'].includes((m.status || '').toUpperCase())
            );

            stoppedMachines.forEach(m => {
                const lineName = m.work_center?.production_line?.name || m.work_center?.production_line?.code || 'Line Operasional';
                this.pushNotification(
                    `🚨 Machine Breakdown Alert`,
                    `Mesin ${m.name || m.code} (${m.code}) terdeteksi STOP / Downtime di ${lineName}.`,
                    'danger',
                    true
                );
            });
        }

        // 2. Real Alert: OEE Drop di bawah ambang kritis (< 65%)
        if (this.kpi && this.kpi.oee != null && this.kpi.planned_time > 0 && Number(this.kpi.oee) < 65) {
            this.pushNotification(
                `⚠️ Critical Low OEE Alert`,
                `Rata-rata OEE aktual saat ini (${Number(this.kpi.oee).toFixed(1)}%) berada di bawah ambang batas minimum operasional (Target: 85.0%).`,
                'warning',
                true
            );
        }

        // 3. Real Alert: High Defect / NG Rate (> 10% reject)
        if (this.kpi && this.kpi.quality != null && Number(this.kpi.quality) < 90 && (this.kpi.total_reject_qty || this.kpi.reject_qty || 0) > 10) {
            const rejectQty = this.kpi.total_reject_qty || this.kpi.reject_qty || 0;
            const defectPct = (100 - Number(this.kpi.quality)).toFixed(1);
            this.pushNotification(
                `⚠️ High Defect Rate Alert`,
                `Tingkat reject produk terdata ${defectPct}% (${rejectQty.toLocaleString()} pcs reject).`,
                'warning',
                true
            );
        }
    }

    loadNotifications() {
        if (!this.notifications) {
            try {
                const saved = localStorage.getItem('oee_notifications_v2');
                this.notifications = saved ? JSON.parse(saved) : [];
            } catch (e) {
                this.notifications = [];
            }
        }

        this.evaluateRealtimeAlerts();
        this.updateNotificationBadge();
        this.renderNotificationList();
    }

    updateNotificationBadge() {
        const badge = document.getElementById('notif-badge-count');
        const headerCount = document.getElementById('notif-header-count');
        const unreadCount = (this.notifications || []).filter(n => !n.read).length;

        if (badge) {
            if (unreadCount > 0) {
                badge.textContent = unreadCount > 99 ? '99+' : unreadCount;
                badge.classList.remove('hidden');
            } else {
                badge.classList.add('hidden');
            }
        }
        if (headerCount) {
            headerCount.textContent = `${unreadCount} ${this.t('header.new_notif', 'new')}`;
        }
    }

    renderNotificationList() {
        const listEl = document.getElementById('notification-list');
        if (!listEl) return;

        if (!this.notifications || this.notifications.length === 0) {
            listEl.innerHTML = `
                <div class="p-6 text-center text-slate-400 font-sans">
                    <div class="w-10 h-10 rounded-full bg-emerald-500/10 border border-emerald-500/20 flex items-center justify-center mx-auto mb-2 text-emerald-400">
                        <i data-lucide="check-circle-2" class="w-5 h-5"></i>
                    </div>
                    <div class="font-bold text-xs text-slate-200">Tidak Ada Notifikasi Baru</div>
                    <p class="text-[11px] text-slate-500 mt-1 leading-normal">Semua sistem, mesin, dan target OEE berjalan normal.</p>
                </div>
            `;
            if (window.lucide) window.lucide.createIcons();
            return;
        }

        const iconMap = {
            danger: '<i data-lucide="alert-triangle" class="w-4 h-4 text-rose-400"></i>',
            warning: '<i data-lucide="alert-circle" class="w-4 h-4 text-amber-400"></i>',
            success: '<i data-lucide="check-circle-2" class="w-4 h-4 text-emerald-400"></i>',
            info: '<i data-lucide="info" class="w-4 h-4 text-cyan-400"></i>'
        };

        listEl.innerHTML = this.notifications.map((n, idx) => `
            <div data-notif-idx="${idx}" class="notif-item-row p-3 hover:bg-slate-800/60 transition-colors ${n.read ? 'opacity-60 bg-transparent' : 'bg-slate-900/50'} flex items-start gap-2.5 cursor-pointer group">
                <div class="mt-0.5 shrink-0">${iconMap[n.type] || iconMap.info}</div>
                <div class="flex-1 min-w-0">
                    <div class="flex items-center justify-between mb-0.5">
                        <span class="font-bold text-slate-200 text-xs truncate ${!n.read ? 'text-white' : ''}">${this.escapeHtml(n.title)}</span>
                        <span class="text-[10px] text-slate-500 font-mono shrink-0 ml-1">${n.time || ''}</span>
                    </div>
                    <p class="text-slate-400 text-[11px] leading-snug break-words">${this.escapeHtml(n.message)}</p>
                </div>
                ${!n.read ? '<span class="w-2 h-2 rounded-full bg-cyan-400 shrink-0 mt-1.5 shadow-[0_0_6px_#22d3ee]"></span>' : ''}
            </div>
        `).join('');

        // Bind single item click to mark as read
        listEl.querySelectorAll('.notif-item-row').forEach(row => {
            row.addEventListener('click', (e) => {
                const idx = parseInt(row.getAttribute('data-notif-idx'), 10);
                if (this.notifications && this.notifications[idx]) {
                    this.notifications[idx].read = true;
                    this.saveNotifications();
                    this.updateNotificationBadge();
                    this.renderNotificationList();
                }
            });
        });

        if (window.lucide) window.lucide.createIcons();
    }

    async loadCurrentTab(showSpinner = true) {
        const content = document.getElementById('content-body');
        if (!content) return;

        if (this.tvMode) {
            await this.renderTvDisplay();
            return;
        }

        if (showSpinner) {
            content.innerHTML = `
                <div class="h-96 flex items-center justify-center">
                    <div class="flex flex-col items-center gap-3">
                        <div class="w-10 h-10 border-4 border-cyan-500 border-t-transparent rounded-full animate-spin"></div>
                        <span class="text-xs text-slate-400 font-mono">Loading OEE System data...</span>
                    </div>
                </div>
            `;
        }

        try {
            switch (this.currentTab) {
                case 'dashboard':
                    await this.renderDashboard();
                    break;
                case 'daily-report':
                    await this.renderDailyReport();
                    break;
                case 'ng-report':
                    await this.renderNgReport();
                    break;
                case 'monitoring':
                    await this.renderMonitoring();
                    break;
                case 'machines':
                    await this.renderMachines();
                    break;
                case 'lines':
                    await this.renderLines();
                    break;
                case 'shifts':
                    await this.renderShifts();
                    break;
                case 'teams':
                    await this.renderTeams();
                    break;
                case 'downtime':
                    await this.renderDowntime();
                    break;
                case 'quality':
                    await this.renderQuality();
                    break;
                case 'reports':
                    await this.renderReports();
                    break;
                case 'master':
                    await this.renderMaster();
                    break;
                case 'users':
                    await this.renderUsers();
                    break;
                case 'settings':
                    await this.renderSettings();
                    break;
                case 'database':
                    await this.renderDatabaseManagement();
                    break;
                default:
                    await this.renderDashboard();
            }
        } catch (err) {
            console.error('Error rendering page:', err);
            content.innerHTML = `
                <div class="p-6 bg-rose-900/20 border border-rose-800/50 rounded-xl text-rose-300">
                    <h3 class="font-bold text-sm">Failed to load system data</h3>
                    <p class="text-xs mt-1">${err.message}</p>
                </div>
            `;
        }

        if (window.lucide) window.lucide.createIcons();
        this.initTableSorting(content);
        if (this.currentLang === 'ja') {
            i18n.localizeDom(content);
        }
    }

    /**
     * Universal Dynamic Table Column Sorting (A-Z, Z-A, 0-9, 9-0, Date/Time)
     * Automatically attaches sort triggers and visual indicators to all table headers.
     */
    initTableSorting(container = document) {
        const root = typeof container === 'string' ? document.querySelector(container) : (container || document);
        if (!root) return;

        const tables = root.querySelectorAll ? root.querySelectorAll('table') : [];
        tables.forEach(table => {
            const thead = table.querySelector('thead');
            const tbody = table.querySelector('tbody');
            if (!thead || !tbody) return;

            const headerRows = thead.querySelectorAll('tr');
            if (headerRows.length === 0) return;
            const thList = headerRows[headerRows.length - 1].querySelectorAll('th');

            thList.forEach((th, colIdx) => {
                const headerText = (th.textContent || '').trim();
                // Ignore Action columns or empty headers
                if (
                    th.classList.contains('no-sort') ||
                    th.getAttribute('data-no-sort') === 'true' ||
                    headerText.toLowerCase() === 'aksi' ||
                    headerText.toLowerCase() === 'action' ||
                    headerText === '操作' ||
                    headerText === ''
                ) {
                    return;
                }

                th.classList.add('cursor-pointer', 'select-none', 'group', 'transition-colors');
                th.setAttribute('title', 'Klik untuk mengurutkan A-Z / Z-A (Ascending / Descending)');

                let iconSpan = th.querySelector('.sort-indicator');
                if (!iconSpan) {
                    iconSpan = document.createElement('span');
                    iconSpan.className = 'sort-indicator inline-flex items-center ml-1 text-[10px] text-slate-500 group-hover:text-cyan-300 font-mono transition-colors';
                    iconSpan.innerHTML = '⇅';
                    th.appendChild(iconSpan);
                }

                if (th._sortHandler) {
                    th.removeEventListener('click', th._sortHandler);
                }

                th._sortHandler = () => {
                    const currentDir = th.getAttribute('data-sort-dir') || 'none';
                    const newDir = currentDir === 'asc' ? 'desc' : 'asc';

                    // Reset other headers in the same table
                    thList.forEach(otherTh => {
                        otherTh.removeAttribute('data-sort-dir');
                        const otherIcon = otherTh.querySelector('.sort-indicator');
                        if (otherIcon) {
                            otherIcon.innerHTML = '⇅';
                            otherIcon.className = 'sort-indicator inline-flex items-center ml-1 text-[10px] text-slate-500 group-hover:text-cyan-300 font-mono transition-colors';
                        }
                        otherTh.classList.remove('text-cyan-300', 'bg-slate-800/60');
                    });

                    th.setAttribute('data-sort-dir', newDir);
                    th.classList.add('text-cyan-300', 'bg-slate-800/60');
                    if (iconSpan) {
                        iconSpan.innerHTML = newDir === 'asc' ? '▲' : '▼';
                        iconSpan.className = `sort-indicator inline-flex items-center ml-1 text-[11px] font-bold font-mono transition-colors ${newDir === 'asc' ? 'text-emerald-400' : 'text-amber-400'}`;
                    }

                    const rows = Array.from(tbody.querySelectorAll('tr'));
                    const dataRows = rows.filter(r => !r.querySelector('td[colspan]'));
                    const emptyRows = rows.filter(r => r.querySelector('td[colspan]'));

                    if (dataRows.length <= 1) return;

                    dataRows.sort((rowA, rowB) => {
                        const cellA = rowA.children[colIdx];
                        const cellB = rowB.children[colIdx];
                        if (!cellA || !cellB) return 0;

                        let valA = (cellA.innerText || cellA.textContent || '').trim();
                        let valB = (cellB.innerText || cellB.textContent || '').trim();

                        if (valA === '-' || valA === '') return 1;
                        if (valB === '-' || valB === '') return -1;

                        // Check for time format (e.g., 07:30 or 07:30:00)
                        const isTimeA = /^([0-1]?[0-9]|2[0-3]):[0-5][0-9]/.test(valA);
                        const isTimeB = /^([0-1]?[0-9]|2[0-3]):[0-5][0-9]/.test(valB);

                        // Check for numbers (remove %, pcs, mins, min, Rp, comma)
                        const cleanNumA = valA.replace(/,/g, '').replace(/%/g, '').replace(/mins?/gi, '').replace(/pcs/gi, '').replace(/s$/i, '').trim();
                        const cleanNumB = valB.replace(/,/g, '').replace(/%/g, '').replace(/mins?/gi, '').replace(/pcs/gi, '').replace(/s$/i, '').trim();
                        
                        const isNumA = cleanNumA !== '' && !isNaN(cleanNumA) && !isTimeA;
                        const isNumB = cleanNumB !== '' && !isNaN(cleanNumB) && !isTimeB;

                        let result = 0;
                        if (isNumA && isNumB) {
                            result = parseFloat(cleanNumA) - parseFloat(cleanNumB);
                        } else if (isTimeA && isTimeB) {
                            result = valA.localeCompare(valB);
                        } else {
                            // Check if standard date string
                            const dateA = Date.parse(valA);
                            const dateB = Date.parse(valB);
                            if (!isNaN(dateA) && !isNaN(dateB) && valA.includes('-')) {
                                result = dateA - dateB;
                            } else {
                                result = valA.localeCompare(valB, 'id', { numeric: true, sensitivity: 'base' });
                            }
                        }

                        return newDir === 'asc' ? result : -result;
                    });

tbody.innerHTML = '';
                    dataRows.forEach(r => tbody.appendChild(r));
                    emptyRows.forEach(r => tbody.appendChild(r));
                };

                th.addEventListener('click', th._sortHandler);
            });
        });
    }

    // ==========================================
    // 1. DASHBOARD STATE & CACHE HELPERS
    // ==========================================
    clearDashboardCache() {
        this._dashOverviewCache = null;
        this._dashOverviewCacheKey = null;
        this._dashErrorCache = null;
        this._dashErrorCacheKey = null;
        this._dashSubAssetsCache = null;
        this._dashSubAssetsCacheKey = null;
        this._dashRealtimeCache = null;
        this._dashRealtimeCacheKey = null;
    }

    async getDashboardRealtimeMachines(force = false) {
        const currentKey = JSON.stringify(this.filters);
        if (!force && this._dashRealtimeCache && this._dashRealtimeCacheKey === currentKey) {
            return this._dashRealtimeCache;
        }
        const res = await api.getRealtimeStatus(this.filters);
        const machines = res.data?.data || [];
        this._dashRealtimeCache = machines;
        this._dashRealtimeCacheKey = currentKey;
        return machines;
    }

    switchDashboardSubTab(subtab) {
        if (!subtab || subtab === this.dashboardSubTab) return;
        this.dashboardSubTab = subtab;

        // Instant optimistic feedback for tab buttons in DOM
        document.querySelectorAll('.btn-dash-tab').forEach(b => {
            const isSelected = b.getAttribute('data-dtab') === subtab;
            if (isSelected) {
                b.className = 'btn-dash-tab px-3.5 py-1.5 rounded-md transition-all cursor-pointer bg-cyan-600 text-white shadow-md font-bold';
            } else {
                b.className = 'btn-dash-tab px-3.5 py-1.5 rounded-md transition-all cursor-pointer text-slate-400 hover:text-slate-200';
            }
        });

        // Fast render target subtab
        this.renderDashboard(false);
    }

    bindDashboardSubTabButtons() {
        document.querySelectorAll('.btn-dash-tab').forEach(btn => {
            btn.addEventListener('click', (e) => {
                const subtab = e.currentTarget.getAttribute('data-dtab');
                if (subtab) {
                    this.switchDashboardSubTab(subtab);
                }
            });
        });
    }

    // ==========================================
    // 1A. MAIN DASHBOARD ROUTER & OVERVIEW PAGE
    // ==========================================
    async renderDashboard(force = false) {
        if (!this.dashboardSubTab) this.dashboardSubTab = 'overview';

        if (this.dashboardSubTab === 'error') {
            return this.renderDashboardErrorAnalysis(force);
        }
        if (this.dashboardSubTab === 'subassets') {
            return this.renderDashboardSubAssets(force);
        }
        return this.renderDashboardOverview(force);
    }

    async renderDashboardOverview(force = false) {
        const content = document.getElementById('content-body');
        if (!content) return;

        const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');
        const currentKey = JSON.stringify(this.filters);

        let data = (!force && this._dashOverviewCache && this._dashOverviewCacheKey === currentKey)
            ? this._dashOverviewCache
            : null;

        if (!data) {
            // Lightweight inline loading if first visit or cache invalidated
            if (!content.querySelector('.btn-dash-tab')) {
                content.innerHTML = `
                    <div class="h-96 flex items-center justify-center">
                        <div class="flex flex-col items-center gap-3">
                            <div class="w-9 h-9 border-3 border-cyan-500 border-t-transparent rounded-full animate-spin"></div>
                            <span class="text-xs text-slate-400 font-mono">Memuat OEE Overview...</span>
                        </div>
                    </div>
                `;
            }

            const [kpiRes, trendRes, machines, sixLossRes, lineRankingRes] = await Promise.all([
                api.getOeeKpi(this.filters),
                api.getOeeTrend(this.filters),
                this.getDashboardRealtimeMachines(force),
                api.getSixBigLosses(this.filters),
                api.getLineRanking(this.filters),
            ]);

            data = {
                kpi: kpiRes.data?.data || {},
                trends: trendRes.data?.data || [],
                machines: machines,
                sixLosses: sixLossRes.data?.data || [],
                lines: lineRankingRes.data?.data || [],
            };
            this._dashOverviewCache = data;
            this._dashOverviewCacheKey = currentKey;
        }

        this.kpi = data.kpi;
        this.evaluateRealtimeAlerts();

        const kpi = data.kpi;
        const trends = data.trends;
        const lines = data.lines || [];
        const machines = data.machines || [];
        const sixLosses = data.sixLosses;

        // Dynamic Machine Status Counts from Database
        const runningCount = machines.filter(m => m.status === 'RUNNING').length;
        const idleCount = machines.filter(m => m.status === 'IDLE').length;
        const stopCount = machines.filter(m => ['STOP', 'STOPPED', 'BREAKDOWN', 'MAINTENANCE', 'OFFLINE'].includes((m.status || '').toUpperCase())).length;
        const totalMachines = machines.length;

        // 1. Available Lines for Dropdown Filter
        const availableLineNames = Array.from(new Set(machines.map(m => m.line_name).filter(Boolean)));

        // Filter Machine Status Overview List & Build Cascading Filter Options
        this.dashMachineFilters = this.dashMachineFilters || { line: '', machine: '', product: '', status: '' };

        // 2. Cascading Machines for Dropdown Filter
        let availableMachinesForSelect = machines;
        if (this.dashMachineFilters.line) {
            availableMachinesForSelect = availableMachinesForSelect.filter(m => 
                (m.line_name || '').toLowerCase() === this.dashMachineFilters.line.toLowerCase()
            );
        }

        // 3. Cascading Products for Dropdown Filter
        const availableProductNames = Array.from(new Set(availableMachinesForSelect.map(m => m.product_name).filter(p => p && !p.includes('Belum Ada Laporan') && p !== 'N/A')));

        // 4. Final Filtered Machines for Row List Rendering (All 4 filters applied strictly without exception)
        let filteredMachines = machines;

        if (this.dashMachineFilters.status) {
            const targetStatus = this.dashMachineFilters.status.toUpperCase();
            filteredMachines = filteredMachines.filter(m => {
                const s = (m.status || '').toUpperCase();
                if (targetStatus === 'STOP' || targetStatus === 'STOPPED') {
                    return s === 'STOP' || s === 'STOPPED' || s === 'BREAKDOWN' || s === 'MAINTENANCE' || s === 'OFFLINE';
                }
                return s === targetStatus;
            });
        }

        if (this.dashMachineFilters.line) {
            filteredMachines = filteredMachines.filter(m => 
                (m.line_name || '').toLowerCase() === this.dashMachineFilters.line.toLowerCase()
            );
        }

        if (this.dashMachineFilters.machine) {
            filteredMachines = filteredMachines.filter(m => 
                (m.machine_name || '').toLowerCase() === this.dashMachineFilters.machine.toLowerCase() ||
                (m.machine_code || '').toLowerCase() === this.dashMachineFilters.machine.toLowerCase() ||
                (m.machine_name || '').toLowerCase().includes(this.dashMachineFilters.machine.toLowerCase())
            );
        }

        if (this.dashMachineFilters.product) {
            filteredMachines = filteredMachines.filter(m => 
                (m.product_name || '').toLowerCase() === this.dashMachineFilters.product.toLowerCase() ||
                (m.product_name || '').toLowerCase().includes(this.dashMachineFilters.product.toLowerCase())
            );
        }

        // Helper calculations for 4 Macro KPI Cards
        const oeeVal = kpi.oee != null ? Number(kpi.oee) : 0;
        const availVal = kpi.availability != null ? Number(kpi.availability) : 0;
        const perfVal = kpi.performance != null ? Number(kpi.performance) : 0;
        const qualVal = kpi.quality != null ? Number(kpi.quality) : 0;

        const prevOee = kpi.previous_oee != null ? Number(kpi.previous_oee) : null;
        const prevAvail = kpi.previous_availability != null ? Number(kpi.previous_availability) : null;
        const prevPerf = kpi.previous_performance != null ? Number(kpi.previous_performance) : null;
        const prevQual = kpi.previous_quality != null ? Number(kpi.previous_quality) : null;

        const calcDelta = (curr, prev) => {
            if (prev == null) return { delta: 0, str: '±0.00%', isUp: false, isZero: true };
            const diff = Number((curr - prev).toFixed(2));
            if (diff > 0) return { delta: diff, str: `▲ +${diff.toFixed(2)}%`, isUp: true, isZero: false };
            if (diff < 0) return { delta: diff, str: `▼ ${diff.toFixed(2)}%`, isUp: false, isZero: false };
            return { delta: 0, str: '±0.00%', isUp: false, isZero: true };
        };

        const oeeDiff = calcDelta(oeeVal, prevOee);
        const availDiff = calcDelta(availVal, prevAvail);
        const perfDiff = calcDelta(perfVal, prevPerf);
        const qualDiff = calcDelta(qualVal, prevQual);

        const getDeltaBadge = (d) => {
            if (d.isZero) {
                return `<span class="inline-flex items-center gap-1 rounded-full bg-slate-800/80 text-slate-400 border border-slate-700 px-2 py-0.5 text-[11px] font-mono font-bold">${d.str}</span>`;
            }
            if (d.isUp) {
                return `<span class="inline-flex items-center gap-1 rounded-full bg-emerald-950/70 text-emerald-400 border border-emerald-800/50 px-2 py-0.5 text-[11px] font-mono font-bold">${d.str}</span>`;
            }
            return `<span class="inline-flex items-center gap-1 rounded-full bg-rose-950/70 text-rose-400 border border-rose-800/50 px-2 py-0.5 text-[11px] font-mono font-bold">${d.str}</span>`;
        };

        // Determine Status Visuals for each Metric
        // 1. Overall OEE: Dynamic (Bagus = Hijau, Kurang = Orange, Jelek = Merah)
        const oeeStatus = oeeVal >= 85 
            ? { label: 'Optimal / World Class', color: '#10b981', grad: 'from-emerald-600 to-emerald-400', tagClass: 'bg-emerald-500/10 text-emerald-400 border-emerald-500/30', benchBadge: 'text-emerald-400 bg-emerald-950/40 border-emerald-800/40' }
            : (oeeVal >= 65 
                ? { label: 'Warning / Minor Loss', color: '#f97316', grad: 'from-amber-600 to-orange-500', tagClass: 'bg-orange-500/10 text-orange-400 border-orange-500/30', benchBadge: 'text-orange-400 bg-orange-950/40 border-orange-800/40' }
                : { label: 'Critical / Breakdown', color: '#ef4444', grad: 'from-rose-600 to-red-500', tagClass: 'bg-rose-500/10 text-rose-400 border-rose-500/30', benchBadge: 'text-rose-400 bg-rose-950/40 border-rose-800/40' });

        // 2. Availability (A): Tema Biru (Sky/Blue)
        const availStatus = availVal >= 90
            ? { label: 'On Target (>=90%)', color: '#0ea5e9', grad: 'from-blue-600 to-sky-400', tagClass: 'bg-sky-500/10 text-sky-400 border-sky-500/30' }
            : (availVal >= 80
                ? { label: 'Warning (80-89%)', color: '#38bdf8', grad: 'from-blue-600 to-cyan-400', tagClass: 'bg-sky-500/10 text-sky-300 border-sky-500/30' }
                : { label: 'Needs Improvement', color: '#ef4444', grad: 'from-rose-600 to-rose-400', tagClass: 'bg-rose-500/10 text-rose-400 border-rose-500/30' });

        // 3. Performance (P): Tema Hijau (Emerald/Green)
        const perfStatus = perfVal >= 95
            ? { label: 'On Target (>=95%)', color: '#10b981', grad: 'from-emerald-600 to-teal-400', tagClass: 'bg-emerald-500/10 text-emerald-400 border-emerald-500/30' }
            : (perfVal >= 85
                ? { label: 'Warning (85-94%)', color: '#22c55e', grad: 'from-emerald-500 to-green-400', tagClass: 'bg-emerald-500/10 text-emerald-300 border-emerald-500/30' }
                : { label: 'Needs Improvement', color: '#ef4444', grad: 'from-rose-600 to-rose-400', tagClass: 'bg-rose-500/10 text-rose-400 border-rose-500/30' });

        // 4. Quality Rate (Q): Tema Merah / Jingga (Orange/Red)
        const qualStatus = qualVal >= 99
            ? { label: 'Zero Defect Target', color: '#f97316', grad: 'from-orange-600 to-amber-400', tagClass: 'bg-orange-500/10 text-orange-400 border-orange-500/30' }
            : (qualVal >= 95
                ? { label: 'Warning (95-98%)', color: '#ea580c', grad: 'from-orange-600 to-rose-400', tagClass: 'bg-amber-500/10 text-amber-400 border-amber-500/30' }
                : { label: 'High Rejects (<95%)', color: '#ef4444', grad: 'from-rose-600 to-red-500', tagClass: 'bg-rose-500/10 text-rose-400 border-rose-500/30' });

        content.innerHTML = `
            <!-- TOP SUB-HEADER BAR -->
            <div class="flex flex-wrap items-center justify-between ${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-xl px-5 py-3 shadow-xl mb-4 gap-4">
                <div class="flex items-center gap-6">
                    <h1 class="text-2xl font-black tracking-tight ${isLight ? 'text-slate-900' : 'text-white'} flex items-center gap-2">
                        <span class="text-cyan-400">OEE</span>
                    </h1>

                    <!-- SUB-NAV PILLS -->
                    <div class="flex items-center gap-1.5 ${isLight ? 'bg-slate-100 border-slate-300' : 'bg-slate-950 border-slate-800'} p-1 rounded-lg border text-xs font-semibold">
                        <button data-dtab="overview" class="btn-dash-tab px-3.5 py-1.5 rounded-md transition-all cursor-pointer ${this.dashboardSubTab === 'overview' ? 'bg-cyan-600 text-white shadow-md font-bold' : 'text-slate-400 hover:text-slate-200'}">OEE overview</button>
                        <button data-dtab="error" class="btn-dash-tab px-3.5 py-1.5 rounded-md transition-all cursor-pointer ${this.dashboardSubTab === 'error' ? 'bg-cyan-600 text-white shadow-md font-bold' : 'text-slate-400 hover:text-slate-200'}">Error analysis</button>
                        <button data-dtab="subassets" class="btn-dash-tab px-3.5 py-1.5 rounded-md transition-all cursor-pointer ${this.dashboardSubTab === 'subassets' ? 'bg-cyan-600 text-white shadow-md font-bold' : 'text-slate-400 hover:text-slate-200'}">Analysis of sub assets</button>
                    </div>
                </div>

                <div class="flex items-center gap-3 text-xs">
                    <!-- LIVE DATABASE COUNTERS (Colorblind-safe) -->
                    <div class="flex items-center gap-2">
                        <span class="px-2.5 py-1 rounded-full bg-emerald-500/10 text-emerald-400 border border-emerald-500/30 font-mono font-bold text-[11px] flex items-center gap-1.5">
                            <span class="w-2 h-2 rounded-full bg-emerald-500"></span> ${runningCount} RUNNING
                        </span>
                        <span class="px-2.5 py-1 rounded-full bg-amber-500/10 text-amber-400 border border-amber-500/30 font-mono font-bold text-[11px] flex items-center gap-1.5">
                            <span class="text-[9px]">▲</span> ${idleCount} IDLE
                        </span>
                        <span class="px-2.5 py-1 rounded-full bg-rose-500/15 text-rose-400 border border-rose-500/40 font-mono font-bold text-[11px] flex items-center gap-1.5 ${stopCount > 0 ? 'status-pulse-breakdown' : ''}">
                            <span class="text-[9px]">■</span> ${stopCount} STOP
                        </span>
                    </div>

                    <button class="p-1 text-slate-400 hover:text-slate-200 cursor-pointer transition-colors" title="Dashboard Options">
                        <i data-lucide="more-horizontal" class="w-5 h-5"></i>
                    </button>
                </div>
            </div>

            <!-- SECTION 1: MACRO KPI SUMMARY (4 MODERN INDUSTRIAL CARDS) -->
            <div class="mb-5">
                <div class="flex items-center justify-between mb-3">
                    <h3 class="text-xs font-bold ${isLight ? 'text-slate-800' : 'text-slate-300'} uppercase tracking-wider flex items-center gap-2">
                        <i data-lucide="gauge" class="w-4 h-4 text-cyan-400"></i>
                        Macro KPIs Overview
                    </h3>
                    <span class="text-xs text-cyan-400 cursor-pointer hover:underline flex items-center gap-1 font-semibold">
                        <i data-lucide="external-link" class="w-3.5 h-3.5"></i> Details
                    </span>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
                    <!-- CARD 1: OVERALL OEE (Dynamic: Green/Orange/Red) -->
                    <div class="relative overflow-hidden ${isLight ? 'bg-white border-slate-200 shadow-md' : 'bg-slate-900 border-slate-800 shadow-xl'} border rounded-xl p-4 flex flex-col justify-between transition-all duration-200 hover:border-slate-700">
                        <div class="flex items-center justify-between mb-1">
                            <div class="flex items-center gap-1.5">
                                <span class="w-2.5 h-2.5 rounded-full" style="background-color: ${oeeStatus.color}; box-shadow: 0 0 10px ${oeeStatus.color}"></span>
                                <span class="text-xs font-bold uppercase tracking-wider ${isLight ? 'text-slate-700' : 'text-slate-300'}">Overall OEE</span>
                            </div>
                            ${getDeltaBadge(oeeDiff)}
                        </div>

                        <div class="my-1.5 flex items-baseline justify-between">
                            <div class="flex items-baseline gap-1">
                                <span class="text-3xl font-black font-mono tracking-tight" style="color: ${oeeStatus.color}">${oeeVal.toFixed(2)}</span>
                                <span class="text-xs font-semibold text-slate-400">%</span>
                            </div>
                            <span class="text-[10px] font-bold px-2 py-0.5 rounded-full border ${oeeStatus.tagClass}">
                                ${oeeStatus.label}
                            </span>
                        </div>

                        <!-- Mini Speedometer Gauge (Overall OEE) -->
                        <div class="my-1 flex items-center justify-center">
                            <div id="dash-gauge-oee" class="flex items-center justify-center"></div>
                        </div>

                        <div class="pt-2 border-t ${isLight ? 'border-slate-100' : 'border-slate-800/80'} text-[11px] flex items-center justify-between">
                            <span class="text-slate-400 font-medium">World Class Std: 85%</span>
                            <span class="font-mono text-[10px] px-1.5 py-0.2 rounded border ${oeeStatus.benchBadge}">
                                Gap: ${(oeeVal - 85).toFixed(1)}%
                            </span>
                        </div>
                    </div>

                    <!-- CARD 2: AVAILABILITY (A) (Tema Biru: Sky/Blue) -->
                    <div class="relative overflow-hidden ${isLight ? 'bg-white border-slate-200 shadow-md' : 'bg-slate-900 border-slate-800 shadow-xl'} border rounded-xl p-4 flex flex-col justify-between transition-all duration-200 hover:border-slate-700">
                        <div class="flex items-center justify-between mb-1">
                            <div class="flex items-center gap-1.5">
                                <span class="w-2.5 h-2.5 rounded-full bg-sky-400 shadow-[0_0_10px_#38bdf8]"></span>
                                <span class="text-xs font-bold uppercase tracking-wider ${isLight ? 'text-slate-700' : 'text-slate-300'}">Availability (A)</span>
                            </div>
                            ${getDeltaBadge(availDiff)}
                        </div>

                        <div class="my-1.5 flex items-baseline justify-between">
                            <div class="flex items-baseline gap-1">
                                <span class="text-3xl font-black font-mono tracking-tight text-sky-400">${availVal.toFixed(2)}</span>
                                <span class="text-xs font-semibold text-slate-400">%</span>
                            </div>
                            <span class="text-[10px] font-bold px-2 py-0.5 rounded-full border ${availStatus.tagClass}">
                                Target: 90%
                            </span>
                        </div>

                        <!-- Mini Speedometer Gauge (Availability) -->
                        <div class="my-1 flex items-center justify-center">
                            <div id="dash-gauge-avail" class="flex items-center justify-center"></div>
                        </div>

                        <div class="pt-2 border-t ${isLight ? 'border-slate-100' : 'border-slate-800/80'} text-[11px] flex items-center justify-between">
                            <span class="text-slate-400 font-medium">Total Downtime Loss</span>
                            <span class="font-mono text-[10px] text-sky-400 font-bold bg-sky-950/40 px-1.5 py-0.2 rounded border border-sky-800/40">
                                ${kpi.total_downtime_minutes != null ? kpi.total_downtime_minutes : 0} min
                            </span>
                        </div>
                    </div>

                    <!-- CARD 3: PERFORMANCE (P) (Tema Hijau: Emerald/Green) -->
                    <div class="relative overflow-hidden ${isLight ? 'bg-white border-slate-200 shadow-md' : 'bg-slate-900 border-slate-800 shadow-xl'} border rounded-xl p-4 flex flex-col justify-between transition-all duration-200 hover:border-slate-700">
                        <div class="flex items-center justify-between mb-1">
                            <div class="flex items-center gap-1.5">
                                <span class="w-2.5 h-2.5 rounded-full bg-emerald-400 shadow-[0_0_10px_#34d399]"></span>
                                <span class="text-xs font-bold uppercase tracking-wider ${isLight ? 'text-slate-700' : 'text-slate-300'}">Performance (P)</span>
                            </div>
                            ${getDeltaBadge(perfDiff)}
                        </div>

                        <div class="my-1.5 flex items-baseline justify-between">
                            <div class="flex items-baseline gap-1">
                                <span class="text-3xl font-black font-mono tracking-tight text-emerald-400">${perfVal.toFixed(2)}</span>
                                <span class="text-xs font-semibold text-slate-400">%</span>
                            </div>
                            <span class="text-[10px] font-bold px-2 py-0.5 rounded-full border ${perfStatus.tagClass}">
                                Target: 95%
                            </span>
                        </div>

                        <!-- Mini Speedometer Gauge (Performance) -->
                        <div class="my-1 flex items-center justify-center">
                            <div id="dash-gauge-perf" class="flex items-center justify-center"></div>
                        </div>

                        <div class="pt-2 border-t ${isLight ? 'border-slate-100' : 'border-slate-800/80'} text-[11px] flex items-center justify-between">
                            <span class="text-slate-400 font-medium">Actual / Target</span>
                            <span class="font-mono text-[10px] text-emerald-400 font-bold bg-emerald-950/40 px-1.5 py-0.2 rounded border border-emerald-800/40">
                                ${(kpi.total_actual_qty || 0).toLocaleString()} / ${(kpi.total_target_qty || 0).toLocaleString()}
                            </span>
                        </div>
                    </div>

                    <!-- CARD 4: QUALITY RATE (Q) (Tema Merah / Jingga) -->
                    <div class="relative overflow-hidden ${isLight ? 'bg-white border-slate-200 shadow-md' : 'bg-slate-900 border-slate-800 shadow-xl'} border rounded-xl p-4 flex flex-col justify-between transition-all duration-200 hover:border-slate-700">
                        <div class="flex items-center justify-between mb-1">
                            <div class="flex items-center gap-1.5">
                                <span class="w-2.5 h-2.5 rounded-full bg-orange-400 shadow-[0_0_10px_#fb923c]"></span>
                                <span class="text-xs font-bold uppercase tracking-wider ${isLight ? 'text-slate-700' : 'text-slate-300'}">Quality Rate (Q)</span>
                            </div>
                            ${getDeltaBadge(qualDiff)}
                        </div>

                        <div class="my-1.5 flex items-baseline justify-between">
                            <div class="flex items-baseline gap-1">
                                <span class="text-3xl font-black font-mono tracking-tight text-orange-400">${qualVal.toFixed(2)}</span>
                                <span class="text-xs font-semibold text-slate-400">%</span>
                            </div>
                            <span class="text-[10px] font-bold px-2 py-0.5 rounded-full border ${qualStatus.tagClass}">
                                Target: 99%
                            </span>
                        </div>

                        <!-- Mini Speedometer Gauge (Quality) -->
                        <div class="my-1 flex items-center justify-center">
                            <div id="dash-gauge-qual" class="flex items-center justify-center"></div>
                        </div>

                        <div class="pt-2 border-t ${isLight ? 'border-slate-100' : 'border-slate-800/80'} text-[11px] flex items-center justify-between">
                            <span class="text-slate-400 font-medium">Reject / Scrap Qty</span>
                            <span class="font-mono text-[10px] text-orange-400 font-bold bg-orange-950/40 px-1.5 py-0.2 rounded border border-orange-800/40">
                                ${(kpi.total_reject_qty || 0).toLocaleString()} pcs
                            </span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- SECTION 2: RESUME SEMUA HASIL PRODUKSI (GLOBAL PRODUCTION SUMMARY BANNER) -->
            ${(() => {
                const targetQty = (kpi.total_target_qty || 0);
                const actualQty = (kpi.total_actual_qty || 0);
                const goodQty = (kpi.total_good_qty || 0);
                const rejectQty = (kpi.total_reject_qty || 0);
                const downtimeMins = (kpi.total_downtime_minutes || 0);
                const achievePct = targetQty > 0 ? ((actualQty / targetQty) * 100).toFixed(1) : '0.0';
                const yieldPct = actualQty > 0 ? ((goodQty / actualQty) * 100).toFixed(1) : '0.0';
                const defectRatePct = actualQty > 0 ? ((rejectQty / actualQty) * 100).toFixed(2) : '0.00';
                const dtHours = (downtimeMins / 60).toFixed(1);

                return `
                <div class="mb-5 ${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-xl p-4 shadow-xl">
                    <div class="flex flex-wrap items-center justify-between border-b ${isLight ? 'border-slate-100' : 'border-slate-800/80'} pb-3 mb-3 gap-2">
                        <div class="flex items-center gap-2.5">
                            <div class="w-8 h-8 rounded-lg bg-cyan-500/10 border border-cyan-500/30 flex items-center justify-center text-cyan-400">
                                <i data-lucide="boxes" class="w-4 h-4"></i>
                            </div>
                            <div>
                                <h3 class="text-sm font-bold ${isLight ? 'text-slate-800' : 'text-slate-100'} flex items-center gap-2">
                                    <span>Resume Hasil Produksi Global</span>
                                </h3>
                            </div>
                        </div>
                        <div class="flex items-center gap-2 text-xs">
                            <span class="text-slate-400 font-mono">Pencapaian Target: <strong class="${Number(achievePct) >= 100 ? 'text-emerald-400' : 'text-amber-400'} font-bold">${achievePct}%</strong></span>
                        </div>
                    </div>

                    <!-- 5 Production Result Metric Cards -->
                    <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-3">
                        <!-- 1. Plan Target -->
                        <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950/70 border-slate-800/80'} border rounded-lg p-3">
                            <div class="flex items-center justify-between text-slate-400 text-[11px] mb-1 font-medium">
                                <span class="flex items-center gap-1.5"><i data-lucide="target" class="w-3.5 h-3.5 text-sky-400"></i> Target Plan</span>
                                <span class="text-[10px] font-mono text-slate-500">PLAN</span>
                            </div>
                            <div class="text-xl font-black font-mono ${isLight ? 'text-slate-900' : 'text-slate-100'}">${targetQty.toLocaleString()} <span class="text-xs font-normal text-slate-400">pcs</span></div>
                            <div class="text-[10px] text-slate-400 mt-1 flex items-center gap-1 font-mono">
                                <span class="text-sky-400 font-semibold">100%</span> baseline target
                            </div>
                        </div>

                        <!-- 2. Actual Produced -->
                        <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950/70 border-slate-800/80'} border rounded-lg p-3">
                            <div class="flex items-center justify-between text-slate-400 text-[11px] mb-1 font-medium">
                                <span class="flex items-center gap-1.5"><i data-lucide="package-check" class="w-3.5 h-3.5 text-emerald-400"></i> Total Output</span>
                                <span class="text-[10px] font-mono font-bold px-1.5 py-0.2 rounded ${Number(achievePct) >= 100 ? 'bg-emerald-950 text-emerald-400 border border-emerald-800' : 'bg-amber-950 text-amber-400 border border-amber-800'}">${achievePct}%</span>
                            </div>
                            <div class="text-xl font-black font-mono text-emerald-400">${actualQty.toLocaleString()} <span class="text-xs font-normal text-slate-400">pcs</span></div>
                            <div class="text-[10px] text-slate-400 mt-1 flex items-center gap-1 font-mono">
                                <span class="${actualQty >= targetQty ? 'text-emerald-400' : 'text-amber-400'}">${actualQty >= targetQty ? '▲ +' + (actualQty - targetQty).toLocaleString() : '▼ -' + (targetQty - actualQty).toLocaleString()} pcs gap</span>
                            </div>
                        </div>

                        <!-- 3. Good Output (OK) -->
                        <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950/70 border-slate-800/80'} border rounded-lg p-3">
                            <div class="flex items-center justify-between text-slate-400 text-[11px] mb-1 font-medium">
                                <span class="flex items-center gap-1.5"><i data-lucide="check-circle-2" class="w-3.5 h-3.5 text-cyan-400"></i> Good Output (OK)</span>
                                <span class="text-[10px] font-mono font-bold px-1.5 py-0.2 rounded bg-cyan-950 text-cyan-400 border border-cyan-800">${yieldPct}% Yield</span>
                            </div>
                            <div class="text-xl font-black font-mono text-cyan-300">${goodQty.toLocaleString()} <span class="text-xs font-normal text-slate-400">pcs</span></div>
                            <div class="text-[10px] text-slate-400 mt-1 flex items-center gap-1 font-mono">
                                <span>Siap kirim / Lolos QC</span>
                            </div>
                        </div>

                        <!-- 4. Rejects / NG Defect -->
                        <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950/70 border-slate-800/80'} border rounded-lg p-3">
                            <div class="flex items-center justify-between text-slate-400 text-[11px] mb-1 font-medium">
                                <span class="flex items-center gap-1.5"><i data-lucide="alert-triangle" class="w-3.5 h-3.5 text-rose-400"></i> Defect (NG)</span>
                                <span class="text-[10px] font-mono font-bold px-1.5 py-0.2 rounded ${Number(defectRatePct) > 1.5 ? 'bg-rose-950 text-rose-400 border border-rose-800' : 'bg-emerald-950 text-emerald-400 border border-emerald-800'}">${defectRatePct}% Defect</span>
                            </div>
                            <div class="text-xl font-black font-mono text-rose-400">${rejectQty.toLocaleString()} <span class="text-xs font-normal text-slate-400">pcs</span></div>
                            <div class="text-[10px] text-slate-400 mt-1 flex items-center gap-1 font-mono">
                                <span>${rejectQty === 0 ? 'Zero Defect' : 'Perlu countermeasure'}</span>
                            </div>
                        </div>

                        <!-- 5. Downtime Loss -->
                        <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950/70 border-slate-800/80'} border rounded-lg p-3 col-span-2 md:col-span-1">
                            <div class="flex items-center justify-between text-slate-400 text-[11px] mb-1 font-medium">
                                <span class="flex items-center gap-1.5"><i data-lucide="clock" class="w-3.5 h-3.5 text-amber-400"></i> Total Downtime</span>
                                <span class="text-[10px] font-mono font-bold px-1.5 py-0.2 rounded bg-amber-950 text-amber-400 border border-amber-800">${dtHours}h</span>
                            </div>
                            <div class="text-xl font-black font-mono text-amber-400">${downtimeMins.toLocaleString()} <span class="text-xs font-normal text-slate-400">min</span></div>
                            <div class="text-[10px] text-slate-400 mt-1 flex items-center gap-1 font-mono">
                                <span>Waktu henti produksi</span>
                            </div>
                        </div>
                    </div>
                </div>
                `;
            })()}

            <!-- SECTION 3: GRAFIK TREN GLOBAL OEE & HASIL PRODUKSI (INTERACTIVE ANIMATED SPLINE LINE CHART) -->
            <div class="mb-5 ${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-xl p-5 shadow-xl">
                <div class="flex flex-wrap items-center justify-between border-b ${isLight ? 'border-slate-100' : 'border-slate-800/80'} pb-3 mb-4 gap-3">
                    <div>
                        <h3 class="text-base font-bold ${isLight ? 'text-slate-800' : 'text-slate-100'} flex items-center gap-2">
                            <i data-lucide="line-chart" class="w-5 h-5 text-cyan-400"></i>
                            <span>Grafik Performa OEE - Semua Line</span>
                        </h3>
                    </div>

                    <!-- MODE TOGGLE PILLS -->
                    <div class="flex flex-wrap items-center gap-1.5 ${isLight ? 'bg-slate-100 border-slate-300' : 'bg-slate-950 border-slate-800'} p-1 rounded-lg border text-xs font-semibold">
                        <button id="btn-trend-mode-lines-oee" data-mode="lines_oee" class="btn-trend-mode px-3 py-1.5 rounded-md transition-all cursor-pointer ${(this.dashTrendMode || 'lines_oee') === 'lines_oee' ? 'bg-cyan-600 text-white shadow-md font-bold' : 'text-slate-400 hover:text-slate-200'} flex items-center gap-1.5">
                            <i data-lucide="git-branch" class="w-3.5 h-3.5"></i>
                            <span>OEE Semua Line (FX-1 s/d FX-11)</span>
                        </button>
                        <button id="btn-trend-mode-lines-prod" data-mode="lines_prod" class="btn-trend-mode px-3 py-1.5 rounded-md transition-all cursor-pointer ${(this.dashTrendMode || 'lines_oee') === 'lines_prod' ? 'bg-cyan-600 text-white shadow-md font-bold' : 'text-slate-400 hover:text-slate-200'} flex items-center gap-1.5">
                            <i data-lucide="boxes" class="w-3.5 h-3.5"></i>
                            <span>Output Per Line (Pcs)</span>
                        </button>
                        <button id="btn-trend-mode-lines-dual" data-mode="lines_dual" class="btn-trend-mode px-3 py-1.5 rounded-md transition-all cursor-pointer ${(this.dashTrendMode || 'lines_oee') === 'lines_dual' ? 'bg-cyan-600 text-white shadow-md font-bold' : 'text-slate-400 hover:text-slate-200'} flex items-center gap-1.5">
                            <i data-lucide="git-merge" class="w-3.5 h-3.5"></i>
                            <span>Multi-Axis (OEE & Output)</span>
                        </button>
                        <button id="btn-trend-mode-date-trend" data-mode="date_trend" class="btn-trend-mode px-3 py-1.5 rounded-md transition-all cursor-pointer ${(this.dashTrendMode || 'lines_oee') === 'date_trend' ? 'bg-cyan-600 text-white shadow-md font-bold' : 'text-slate-400 hover:text-slate-200'} flex items-center gap-1.5">
                            <i data-lucide="calendar" class="w-3.5 h-3.5"></i>
                            <span>Tren Tanggal</span>
                        </button>
                    </div>
                </div>

                <!-- MAIN CHART CONTAINER -->
                <div id="chart-global-oee-trend" class="w-full h-80 min-h-[320px]"></div>

                <!-- CHART FOOTER METRIC HIGHLIGHTS -->
                <div class="grid grid-cols-2 sm:grid-cols-4 gap-3 pt-3 mt-2 border-t ${isLight ? 'border-slate-100' : 'border-slate-800/80'} text-xs">
                    <div class="flex items-center gap-2">
                        <span class="w-2.5 h-2.5 rounded-full bg-cyan-400 shadow-[0_0_8px_#22d3ee]"></span>
                        <span class="text-slate-400">Total Lini Terpantau:</span>
                        <strong class="font-mono text-cyan-300 font-bold">${lines.length || 11} Lines (FX-1 ~ FX-11)</strong>
                    </div>
                    <div class="flex items-center gap-2">
                        <span class="w-2.5 h-2.5 rounded-full bg-emerald-400 shadow-[0_0_8px_#34d399]"></span>
                        <span class="text-slate-400">Total Output:</span>
                        <strong class="font-mono text-emerald-400 font-bold">${(kpi.total_actual_qty || 0).toLocaleString()} pcs</strong>
                    </div>
                    <div class="flex items-center gap-2">
                        <span class="w-2.5 h-2.5 rounded-full bg-amber-400 shadow-[0_0_8px_#fbbf24]"></span>
                        <span class="text-slate-400">Target Benchmark:</span>
                        <strong class="font-mono text-amber-300 font-bold">85.0% World Class</strong>
                    </div>
                    <div class="flex items-center gap-2">
                        <span class="w-2.5 h-2.5 rounded-full bg-rose-400 shadow-[0_0_8px_#f43f5e]"></span>
                        <span class="text-slate-400">Defect Rate:</span>
                        <strong class="font-mono text-rose-400 font-bold">${(kpi.total_actual_qty ? ((kpi.total_reject_qty || 0) / kpi.total_actual_qty * 100).toFixed(2) : '0.00')}%</strong>
                    </div>
                </div>
            </div>

            <!-- SECTION 4: MACHINE STATUS & SIX BIG LOSSES GRID -->
            <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
                <!-- DYNAMIC MACHINE STATUS CARD (2 COLS) -->
                <div class="lg:col-span-2 bg-slate-900 border border-slate-800 rounded-xl p-5 shadow-xl flex flex-col justify-start">
                    <div class="border-b border-slate-800 pb-3 mb-3">
                        <div class="flex flex-wrap items-center justify-between gap-2">
                            <div>
                                <h3 class="text-base font-bold text-slate-100 flex items-center gap-2">
                                    <i data-lucide="activity" class="w-4 h-4 text-cyan-400"></i>
                                    Machine Status Overview
                                </h3>
                                <p class="text-xs text-slate-400 mt-0.5">
                                    Database Real-time Machine Status: 
                                    <button data-filter-status="RUNNING" class="btn-quick-status-filter text-emerald-400 font-bold hover:underline cursor-pointer transition-all">${runningCount} Running</button>, 
                                    <button data-filter-status="IDLE" class="btn-quick-status-filter text-amber-400 font-bold hover:underline cursor-pointer transition-all">${idleCount} Idle</button>, 
                                    <button data-filter-status="STOP" class="btn-quick-status-filter text-rose-400 font-bold hover:underline cursor-pointer transition-all">${stopCount} Stop</button>
                                </p>
                            </div>
                            <div class="flex items-center gap-2">
                                <span class="text-xs text-slate-400 font-mono">Showing <strong class="text-cyan-400 font-bold">${filteredMachines.length}</strong> of ${totalMachines} Machines</span>
                            </div>
                        </div>

                        <!-- QUICK FILTER DROPDOWNS TOOLBAR -->
                        <div class="flex flex-wrap items-center gap-2 mt-3 pt-2.5 border-t border-slate-800/80 text-xs">
                            <!-- STATUS FILTER -->
                            <div class="flex items-center gap-1.5 bg-slate-950 border border-slate-800 rounded-lg px-2.5 py-1">
                                <i data-lucide="activity" class="w-3.5 h-3.5 text-rose-400"></i>
                                <span class="text-slate-400 font-medium text-[11px]">Status:</span>
                                <select id="dash-filter-status" class="bg-transparent text-slate-200 text-xs focus:outline-none cursor-pointer font-sans font-bold">
                                    <option value="" class="bg-slate-900 text-slate-100">All Statuses</option>
                                    <option value="RUNNING" class="bg-slate-900 text-emerald-400 font-bold" ${this.dashMachineFilters?.status === 'RUNNING' ? 'selected' : ''}>🟢 RUNNING (${runningCount})</option>
                                    <option value="IDLE" class="bg-slate-900 text-amber-400 font-bold" ${this.dashMachineFilters?.status === 'IDLE' ? 'selected' : ''}>🟡 IDLE (${idleCount})</option>
                                    <option value="BREAKDOWN" class="bg-slate-900 text-rose-400 font-bold" ${this.dashMachineFilters?.status === 'BREAKDOWN' ? 'selected' : ''}>🔴 BREAKDOWN</option>
                                    <option value="STOP" class="bg-slate-900 text-slate-400 font-bold" ${this.dashMachineFilters?.status === 'STOP' || this.dashMachineFilters?.status === 'STOPPED' ? 'selected' : ''}>⏹️ STOP / STOPPED (${stopCount})</option>
                                </select>
                            </div>

                            <!-- LINE FILTER -->
                            <div class="flex items-center gap-1.5 bg-slate-950 border border-slate-800 rounded-lg px-2.5 py-1">
                                <i data-lucide="git-fork" class="w-3.5 h-3.5 text-cyan-400"></i>
                                <span class="text-slate-400 font-medium text-[11px]">Line:</span>
                                <select id="dash-filter-line" class="bg-transparent text-slate-200 text-xs focus:outline-none cursor-pointer font-sans">
                                    <option value="" class="bg-slate-900 text-slate-100">All Lines ${availableLineNames.length ? `(${availableLineNames.length})` : ''}</option>
                                    ${availableLineNames.map(l => `<option value="${l}" class="bg-slate-900 text-slate-100" ${this.dashMachineFilters?.line === l ? 'selected' : ''}>${l}</option>`).join('')}
                                </select>
                            </div>

                            <!-- MACHINE FILTER -->
                            <div class="flex items-center gap-1.5 bg-slate-950 border border-slate-800 rounded-lg px-2.5 py-1">
                                <i data-lucide="cpu" class="w-3.5 h-3.5 text-emerald-400"></i>
                                <span class="text-slate-400 font-medium text-[11px]">Machine:</span>
                                <select id="dash-filter-machine" class="bg-transparent text-slate-200 text-xs focus:outline-none cursor-pointer font-sans">
                                    <option value="" class="bg-slate-900 text-slate-100">All Machines ${availableMachinesForSelect.length ? `(${availableMachinesForSelect.length})` : ''}</option>
                                    ${availableMachinesForSelect.map(m => `<option value="${m.machine_code}" class="bg-slate-900 text-slate-100" ${this.dashMachineFilters?.machine === m.machine_code || this.dashMachineFilters?.machine === m.machine_name ? 'selected' : ''}>${m.machine_code} (${m.machine_name})</option>`).join('')}
                                </select>
                            </div>

                            <!-- PRODUCT FILTER -->
                            <div class="flex items-center gap-1.5 bg-slate-950 border border-slate-800 rounded-lg px-2.5 py-1">
                                <i data-lucide="package" class="w-3.5 h-3.5 text-amber-400"></i>
                                <span class="text-slate-400 font-medium text-[11px]">Product:</span>
                                <select id="dash-filter-product" class="bg-transparent text-slate-200 text-xs focus:outline-none cursor-pointer font-sans max-w-[180px]">
                                    <option value="" class="bg-slate-900 text-slate-100">All Products ${availableProductNames.length ? `(${availableProductNames.length})` : ''}</option>
                                    ${availableProductNames.map(p => `<option value="${p}" class="bg-slate-900 text-slate-100" ${this.dashMachineFilters?.product === p ? 'selected' : ''}>${p}</option>`).join('')}
                                </select>
                            </div>

                            ${(this.dashMachineFilters?.line || this.dashMachineFilters?.machine || this.dashMachineFilters?.product || this.dashMachineFilters?.status) ? `
                                <button id="btn-clear-dash-filters" class="text-[11px] text-rose-400 hover:text-rose-300 font-semibold px-2 py-0.5 rounded bg-rose-950/60 border border-rose-800/60 flex items-center gap-1 cursor-pointer">
                                    <i data-lucide="x" class="w-3 h-3"></i> Clear Filters
                                </button>
                            ` : ''}
                        </div>
                    </div>

                    <!-- MACHINE LIVE STATUS CARDS GRID -->
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-3 max-h-[380px] overflow-y-auto pr-1 auto-rows-max">
                        ${filteredMachines.length === 0 ? '<div class="col-span-2 text-center text-slate-500 py-8 text-xs font-sans">No machines found matching selected filters.</div>' : ''}
                        ${filteredMachines.map(m => `
                            <div class="bg-slate-950 border border-slate-800/80 rounded-lg p-3 flex items-center justify-between hover:border-slate-700 transition-all shadow-sm">
                                <div class="truncate pr-2">
                                    <div class="flex items-center gap-2 mb-1">
                                        <span class="font-mono font-bold text-xs text-cyan-400">${m.machine_code}</span>
                                        <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded text-[10px] font-bold border ${this.getMachineStatusBadge(m.status)}">
                                            ${this.getStatusIcon(m.status)}
                                            <span>${m.status}</span>
                                        </span>
                                    </div>
                                    <div class="text-xs font-semibold text-slate-200 truncate">${m.machine_name} • <span class="text-slate-400 font-normal">${m.line_name || 'N/A'}</span></div>
                                    <div class="text-[10px] text-amber-400 font-medium truncate flex items-center gap-1 mt-1 bg-amber-950/40 border border-amber-800/40 px-1.5 py-0.5 rounded w-fit max-w-full">
                                        <i data-lucide="package" class="w-3 h-3 text-amber-400 flex-shrink-0"></i>
                                        <span class="truncate font-mono">${m.product_name || 'No Active SKU'}</span>
                                    </div>
                                </div>
                                <div class="text-right font-mono flex-shrink-0">
                                    <div class="text-sm font-extrabold text-cyan-300">${m.oee}% <span class="text-[10px] font-normal text-slate-400">OEE</span></div>
                                    <div class="text-[10px] text-slate-400">${m.good_quantity || 0} / ${m.target_quantity || 0} pcs</div>
                                </div>
                            </div>
                        `).join('')}
                    </div>
                </div>

                <!-- SIX BIG LOSSES BREAKDOWN CARD (1 COL) -->
                <div id="card-dash-six-losses" class="bg-slate-900 border border-slate-800 rounded-xl p-5 shadow-xl h-fit self-start cursor-pointer group hover:border-cyan-500/60 hover:shadow-cyan-500/10 transition-all">
                    <div class="flex items-center justify-between border-b border-slate-800 pb-3 mb-3">
                        <div>
                            <h3 class="text-sm font-bold text-slate-100 flex items-center gap-2 group-hover:text-cyan-400 transition-colors">
                                <i data-lucide="pie-chart" class="w-4 h-4 text-rose-400 group-hover:scale-110 transition-transform"></i>
                                <span>Six Big Losses Breakdown</span>
                                <span class="text-[9.5px] font-mono font-bold bg-cyan-950 text-cyan-400 border border-cyan-800 px-2 py-0.5 rounded opacity-90 group-hover:opacity-100 flex items-center gap-1 shadow-sm"><i data-lucide="maximize-2" class="w-3 h-3"></i> Detail</span>
                            </h3>
                            <p class="text-xs text-slate-400 mt-0.5">Total downtime & speed loss across 6 TPM pillars &bull; <span class="text-cyan-400 underline group-hover:text-cyan-300">Klik untuk popup breakdown</span></p>
                        </div>
                        <div id="six-losses-total-badge"></div>
                    </div>
                    <div id="chart-six-losses" class="w-full"></div>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();

        // Render Gauges (strictly dynamic visual color mapping)
        this.renderSpeedometerGauge('#dash-gauge-oee', oeeVal, oeeStatus.color);
        this.renderSpeedometerGauge('#dash-gauge-avail', availVal, availStatus.color);
        this.renderSpeedometerGauge('#dash-gauge-perf', perfVal, perfStatus.color);
        this.renderSpeedometerGauge('#dash-gauge-qual', qualVal, qualStatus.color);

        // Render Global OEE & Production Spline Line Trend Chart (Default: Semua Line FX-1 s/d FX-11)
        this.renderGlobalOeeAndProductionTrendChart(data, kpi, this.dashTrendMode || 'lines_oee');

        // Bind Mode Switcher Buttons for Global Trend Chart
        document.querySelectorAll('.btn-trend-mode').forEach(btn => {
            btn.addEventListener('click', (e) => {
                const mode = e.currentTarget.getAttribute('data-mode') || 'lines_oee';
                this.dashTrendMode = mode;
                document.querySelectorAll('.btn-trend-mode').forEach(b => {
                    const active = b.getAttribute('data-mode') === mode;
                    b.className = `btn-trend-mode px-3 py-1.5 rounded-md transition-all cursor-pointer ${active ? 'bg-cyan-600 text-white shadow-md font-bold' : 'text-slate-400 hover:text-slate-200'} flex items-center gap-1.5`;
                });
                this.renderGlobalOeeAndProductionTrendChart(data, kpi, mode);
            });
        });

        // Render Six Big Losses Chart
        this.renderSixLossesChart(sixLosses);

        // Bind Six Big Losses Card Popup Click
        const dashSixLossesCard = document.getElementById('card-dash-six-losses');
        if (dashSixLossesCard) {
            dashSixLossesCard.addEventListener('click', () => {
                this.showSixBigLossesDetailModal(sixLosses, kpi);
            });
        }

        // Bind Subtab Buttons
        this.bindDashboardSubTabButtons();

        // Bind Dashboard Filter Dropdowns
        const statusSelect = document.getElementById('dash-filter-status');
        const lineSelect = document.getElementById('dash-filter-line');
        const machineSelect = document.getElementById('dash-filter-machine');
        const productSelect = document.getElementById('dash-filter-product');
        const clearBtn = document.getElementById('btn-clear-dash-filters');

        if (statusSelect) {
            statusSelect.addEventListener('change', (e) => {
                this.dashMachineFilters = this.dashMachineFilters || {};
                this.dashMachineFilters.status = e.target.value;
                this.renderDashboard();
            });
        }
        document.querySelectorAll('.btn-quick-status-filter').forEach(btn => {
            btn.addEventListener('click', (e) => {
                const st = e.currentTarget.getAttribute('data-filter-status');
                this.dashMachineFilters = this.dashMachineFilters || {};
                this.dashMachineFilters.status = (this.dashMachineFilters.status === st) ? '' : st;
                this.renderDashboard();
            });
        });

        if (lineSelect) {
            lineSelect.addEventListener('change', (e) => {
                this.dashMachineFilters = this.dashMachineFilters || {};
                this.dashMachineFilters.line = e.target.value;
                this.dashMachineFilters.machine = '';
                this.dashMachineFilters.product = '';
                this.renderDashboard();
            });
        }
        if (machineSelect) {
            machineSelect.addEventListener('change', (e) => {
                this.dashMachineFilters = this.dashMachineFilters || {};
                this.dashMachineFilters.machine = e.target.value;
                this.renderDashboard();
            });
        }
        if (productSelect) {
            productSelect.addEventListener('change', (e) => {
                this.dashMachineFilters = this.dashMachineFilters || {};
                this.dashMachineFilters.product = e.target.value;
                this.renderDashboard();
            });
        }
        if (clearBtn) {
            clearBtn.addEventListener('click', () => {
                this.dashMachineFilters = { line: '', machine: '', product: '', status: '' };
                this.renderDashboard();
            });
        }

        if (this.currentLang === 'ja') {
            i18n.localizeDom(document.getElementById('content-body') || document.body);
        }
    }

    // ==========================================
    // 1B. DASHBOARD SUB-TAB: ERROR ANALYSIS
    // ==========================================
    async renderDashboardErrorAnalysis(force = false) {
        const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');
        const content = document.getElementById('content-body');
        if (!content) return;

        const currentKey = JSON.stringify(this.filters);
        let errorData = (!force && this._dashErrorCache && this._dashErrorCacheKey === currentKey)
            ? this._dashErrorCache
            : null;

        if (!errorData) {
            if (!content.querySelector('.btn-dash-tab')) {
                content.innerHTML = `
                    <div class="h-96 flex items-center justify-center">
                        <div class="flex flex-col items-center gap-3">
                            <div class="w-9 h-9 border-3 border-amber-500 border-t-transparent rounded-full animate-spin"></div>
                            <span class="text-xs text-slate-400 font-mono">Memuat Error & Downtime Analysis...</span>
                        </div>
                    </div>
                `;
            }

            const [machines, dtRes, defRes] = await Promise.all([
                this.getDashboardRealtimeMachines(force),
                api.getParetoDowntime(this.filters),
                api.getParetoDefects(this.filters)
            ]);

            errorData = {
                machines: machines,
                dtData: dtRes.data?.data || [],
                top10Defects: defRes.data?.data?.top_10_defects || [],
                defectSummary: defRes.data?.data?.summary || {}
            };
            this._dashErrorCache = errorData;
            this._dashErrorCacheKey = currentKey;
        }

        const machines = errorData.machines || [];
        const dtData = errorData.dtData;
        const top10Defects = errorData.top10Defects;
        const defectSummary = errorData.defectSummary;

        const runningCount = machines.filter(m => m.status === 'RUNNING').length;
        const idleCount = machines.filter(m => m.status === 'IDLE').length;
        const stopCount = machines.filter(m => ['STOP', 'STOPPED', 'BREAKDOWN', 'MAINTENANCE', 'OFFLINE'].includes((m.status || '').toUpperCase())).length;
        const totalMachines = machines.length;

        // Calculate totals
        const totalDtMinutes = dtData.reduce((acc, d) => acc + (d.duration_minutes || d.total_minutes || 0), 0);
        const totalDtHours = (totalDtMinutes / 60).toFixed(1);
        const totalRejectQty = defectSummary.total_defect_volume || top10Defects.reduce((acc, d) => acc + (d.reject_quantity || 0), 0);
        const ppm = defectSummary.defect_ppm || (defectSummary.total_inspected_volume > 0 ? Math.round((totalRejectQty / defectSummary.total_inspected_volume) * 1000000) : 0);

        const subnavHtml = `
            <!-- TOP SUB-HEADER BAR -->
            <div class="flex flex-wrap items-center justify-between ${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-xl px-5 py-3 shadow-xl mb-4 gap-4">
                <div class="flex items-center gap-6">
                    <h1 class="text-2xl font-black tracking-tight ${isLight ? 'text-slate-900' : 'text-white'} flex items-center gap-2">
                        <span class="text-cyan-400">OEE</span>
                    </h1>

                    <!-- SUB-NAV PILLS -->
                    <div class="flex items-center gap-1.5 ${isLight ? 'bg-slate-100 border-slate-300' : 'bg-slate-950 border-slate-800'} p-1 rounded-lg border text-xs font-semibold">
                        <button data-dtab="overview" class="btn-dash-tab px-3.5 py-1.5 rounded-md transition-all cursor-pointer ${this.dashboardSubTab === 'overview' ? 'bg-cyan-600 text-white shadow-md font-bold' : 'text-slate-400 hover:text-slate-200'}">OEE overview</button>
                        <button data-dtab="error" class="btn-dash-tab px-3.5 py-1.5 rounded-md transition-all cursor-pointer ${this.dashboardSubTab === 'error' ? 'bg-cyan-600 text-white shadow-md font-bold' : 'text-slate-400 hover:text-slate-200'}">Error analysis</button>
                        <button data-dtab="subassets" class="btn-dash-tab px-3.5 py-1.5 rounded-md transition-all cursor-pointer ${this.dashboardSubTab === 'subassets' ? 'bg-cyan-600 text-white shadow-md font-bold' : 'text-slate-400 hover:text-slate-200'}">Analysis of sub assets</button>
                    </div>
                </div>

                <div class="flex items-center gap-3 text-xs">
                    <div class="flex items-center gap-2">
                        <span class="px-2.5 py-1 rounded-full bg-emerald-500/10 text-emerald-400 border border-emerald-500/30 font-mono font-bold text-[11px] flex items-center gap-1.5">
                            <span class="w-2 h-2 rounded-full bg-emerald-500"></span> ${runningCount} RUNNING
                        </span>
                        <span class="px-2.5 py-1 rounded-full bg-amber-500/10 text-amber-400 border border-amber-500/30 font-mono font-bold text-[11px] flex items-center gap-1.5">
                            <span class="text-[9px]">▲</span> ${idleCount} IDLE
                        </span>
                        <span class="px-2.5 py-1 rounded-full bg-rose-500/15 text-rose-400 border border-rose-500/40 font-mono font-bold text-[11px] flex items-center gap-1.5 ${stopCount > 0 ? 'status-pulse-breakdown' : ''}">
                            <span class="text-[9px]">■</span> ${stopCount} STOP
                        </span>
                    </div>
                </div>
            </div>
        `;

        content.innerHTML = `
            ${subnavHtml}

            <!-- ERROR ANALYSIS KPI SUMMARY ROW -->
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-5">
                <div class="${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-xl p-4 shadow-xl">
                    <div class="flex items-center justify-between text-xs text-slate-400 font-bold mb-1">
                        <span>TOTAL DOWNTIME LOSS</span>
                        <i data-lucide="clock" class="w-4 h-4 text-amber-400"></i>
                    </div>
                    <div class="flex items-baseline gap-2 font-mono">
                        <span class="text-2xl font-extrabold text-amber-400">${totalDtMinutes.toLocaleString()}</span>
                        <span class="text-xs text-slate-400">menit (${totalDtHours} jam)</span>
                    </div>
                    <div class="mt-2 pt-2 border-t ${isLight ? 'border-slate-100' : 'border-slate-800'} text-[11px] text-slate-400 flex justify-between">
                        <span>Loss Availability</span>
                        <span class="font-bold text-amber-400 font-mono">${dtData.length} Kategori</span>
                    </div>
                </div>

                <div class="${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-xl p-4 shadow-xl">
                    <div class="flex items-center justify-between text-xs text-slate-400 font-bold mb-1">
                        <span>TOTAL REJECT / DEFECT</span>
                        <i data-lucide="alert-circle" class="w-4 h-4 text-rose-400"></i>
                    </div>
                    <div class="flex items-baseline gap-2 font-mono">
                        <span class="text-2xl font-extrabold text-rose-500">${totalRejectQty.toLocaleString()}</span>
                        <span class="text-xs text-slate-400">Pcs NG</span>
                    </div>
                    <div class="mt-2 pt-2 border-t ${isLight ? 'border-slate-100' : 'border-slate-800'} text-[11px] text-slate-400 flex justify-between">
                        <span>Quality Rate Impact</span>
                        <span class="font-bold text-rose-400 font-mono">${ppm.toLocaleString()} PPM</span>
                    </div>
                </div>

                <div class="${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-xl p-4 shadow-xl">
                    <div class="flex items-center justify-between text-xs text-slate-400 font-bold mb-1">
                        <span>TOP DOWNTIME REASON</span>
                        <i data-lucide="wrench" class="w-4 h-4 text-cyan-400"></i>
                    </div>
                    <div class="text-sm font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'} truncate" title="${dtData[0]?.reason || ''}">
                        ${dtData[0]?.reason || dtData[0]?.reason_name || dtData[0]?.name || 'Tidak ada downtime'}
                    </div>
                    <div class="mt-2 pt-2 border-t ${isLight ? 'border-slate-100' : 'border-slate-800'} text-[11px] text-slate-400 flex justify-between font-mono">
                        <span>Durasi:</span>
                        <span class="font-bold text-cyan-400">${dtData[0]?.duration_minutes || dtData[0]?.total_minutes || 0} menit (${totalDtMinutes > 0 ? (((dtData[0]?.duration_minutes || 0) / totalDtMinutes) * 100).toFixed(1) : 0}%)</span>
                    </div>
                </div>

                <div class="${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-xl p-4 shadow-xl">
                    <div class="flex items-center justify-between text-xs text-slate-400 font-bold mb-1">
                        <span>TOP DEFECT / REJECT</span>
                        <i data-lucide="shield-alert" class="w-4 h-4 text-purple-400"></i>
                    </div>
                    <div class="text-sm font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'} truncate" title="${top10Defects[0]?.reason || ''}">
                        ${top10Defects[0]?.reason || 'Tidak ada defect'}
                    </div>
                    <div class="mt-2 pt-2 border-t ${isLight ? 'border-slate-100' : 'border-slate-800'} text-[11px] text-slate-400 flex justify-between font-mono">
                        <span>Volume:</span>
                        <span class="font-bold text-purple-400">${top10Defects[0]?.reject_quantity || 0} pcs (${top10Defects[0]?.percentage || 0}%)</span>
                    </div>
                </div>
            </div>

            <!-- 2 COLUMNS: DOWNTIME ERROR ANALYSIS & DEFECT ERROR ANALYSIS -->
            <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
                <!-- PANEL 1: DOWNTIME ERRORS -->
                <div class="${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-xl p-5 shadow-xl flex flex-col justify-between">
                    <div>
                        <div class="flex items-center justify-between pb-3 border-b ${isLight ? 'border-slate-200' : 'border-slate-800'} mb-4">
                            <div>
                                <h3 class="font-bold text-sm ${isLight ? 'text-slate-900' : 'text-slate-100'} flex items-center gap-2">
                                    <i data-lucide="alert-triangle" class="w-4 h-4 text-amber-400"></i>
                                    Analisis Gangguan & Downtime Mesin
                                </h3>
                                <p class="text-xs ${isLight ? 'text-slate-500' : 'text-slate-400'} mt-0.5">Top penyebab waktu henti mesin (Availability Losses)</p>
                            </div>
                            <button id="btn-jump-downtime" class="px-3 py-1 bg-amber-500/10 hover:bg-amber-500/20 text-amber-400 border border-amber-500/30 rounded-lg text-xs font-semibold flex items-center gap-1 cursor-pointer transition-all">
                                <span>Detail Module</span>
                                <i data-lucide="arrow-right" class="w-3.5 h-3.5"></i>
                            </button>
                        </div>

                        <div class="overflow-x-auto">
                            <table class="w-full text-left text-xs border-collapse font-sans">
                                <thead>
                                    <tr class="${isLight ? 'bg-slate-100 text-slate-700' : 'bg-slate-950/80 text-slate-400'} font-semibold border-b ${isLight ? 'border-slate-200' : 'border-slate-800'}">
                                        <th class="py-2.5 px-3">Penyebab Downtime</th>
                                        <th class="py-2.5 px-3">Kategori</th>
                                        <th class="py-2.5 px-3 text-right">Durasi</th>
                                        <th class="py-2.5 px-3 text-right">Kontribusi</th>
                                    </tr>
                                </thead>
                                <tbody class="divide-y ${isLight ? 'divide-slate-200' : 'divide-slate-800/60'}">
                                    ${dtData.length === 0 ? `
                                        <tr><td colspan="4" class="text-center py-8 text-slate-500 text-xs">Tidak ada data downtime untuk filter ini</td></tr>
                                    ` : dtData.slice(0, 7).map((d, idx) => {
                                        const dtMins = d.duration_minutes || d.total_minutes || 0;
                                        const dtPct = totalDtMinutes > 0 ? ((dtMins / totalDtMinutes) * 100).toFixed(1) : (d.percentage || 0);
                                        const reasonTitle = d.reason || d.reason_name || d.name || 'Unspecified Breakdown';
                                        const categoryLabel = d.category || (reasonTitle.toLowerCase().includes('change') || reasonTitle.toLowerCase().includes('mold') ? 'Setup & Adjustment' : 'Equipment Failure');
                                        return `
                                            <tr class="${isLight ? 'hover:bg-slate-50' : 'hover:bg-slate-800/40'} transition-colors">
                                                <td class="py-2.5 px-3 font-semibold ${isLight ? 'text-slate-900' : 'text-slate-100'}">
                                                    <div class="flex items-center gap-2">
                                                        <span class="w-5 h-5 rounded-md ${isLight ? 'bg-slate-200 text-slate-700' : 'bg-slate-800 text-slate-400'} font-mono font-bold text-[10px] flex items-center justify-center">#${idx + 1}</span>
                                                        <span class="truncate max-w-[200px]" title="${reasonTitle}">${reasonTitle}</span>
                                                    </div>
                                                </td>
                                                <td class="py-2.5 px-3">
                                                    <span class="px-2 py-0.5 rounded text-[10px] font-semibold ${isLight ? 'bg-slate-100 text-slate-700 border-slate-300' : 'bg-slate-800 text-slate-300 border-slate-700'} border">
                                                        ${categoryLabel}
                                                    </span>
                                                </td>
                                                <td class="py-2.5 px-3 text-right font-mono font-bold text-amber-400">
                                                    ${dtMins.toLocaleString()} m
                                                </td>
                                                <td class="py-2.5 px-3 text-right font-mono text-xs font-semibold ${isLight ? 'text-slate-800' : 'text-slate-200'}">
                                                    ${dtPct}%
                                                </td>
                                            </tr>
                                        `;
                                    }).join('')}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <!-- PANEL 2: DEFECT & REJECT ERRORS -->
                <div class="${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-xl p-5 shadow-xl flex flex-col justify-between">
                    <div>
                        <div class="flex items-center justify-between pb-3 border-b ${isLight ? 'border-slate-200' : 'border-slate-800'} mb-4">
                            <div>
                                <h3 class="font-bold text-sm ${isLight ? 'text-slate-900' : 'text-slate-100'} flex items-center gap-2">
                                    <i data-lucide="shield-alert" class="w-4 h-4 text-rose-400"></i>
                                    Analisis Cacat & Defect Produk (NG)
                                </h3>
                                <p class="text-xs ${isLight ? 'text-slate-500' : 'text-slate-400'} mt-0.5">Top jenis cacat & Rekomendasi Tindakan Korektif</p>
                            </div>
                            <button id="btn-jump-quality" class="px-3 py-1 bg-rose-500/10 hover:bg-rose-500/20 text-rose-400 border border-rose-500/30 rounded-lg text-xs font-semibold flex items-center gap-1 cursor-pointer transition-all">
                                <span>Detail Module</span>
                                <i data-lucide="arrow-right" class="w-3.5 h-3.5"></i>
                            </button>
                        </div>

                        <div class="overflow-x-auto">
                            <table class="w-full text-left text-xs border-collapse font-sans">
                                <thead>
                                    <tr class="${isLight ? 'bg-slate-100 text-slate-700' : 'bg-slate-950/80 text-slate-400'} font-semibold border-b ${isLight ? 'border-slate-200' : 'border-slate-800'}">
                                        <th class="py-2.5 px-3">Jenis Defect</th>
                                        <th class="py-2.5 px-3">Bagian</th>
                                        <th class="py-2.5 px-3 text-right">Volume</th>
                                        <th class="py-2.5 px-3">Rekomendasi Kaizen</th>
                                    </tr>
                                </thead>
                                <tbody class="divide-y ${isLight ? 'divide-slate-200' : 'divide-slate-800/60'}">
                                    ${top10Defects.length === 0 ? `
                                        <tr><td colspan="4" class="text-center py-8 text-slate-500 text-xs">Tidak ada data defect</td></tr>
                                    ` : top10Defects.slice(0, 6).map((d, idx) => `
                                        <tr class="${isLight ? 'hover:bg-slate-50' : 'hover:bg-slate-800/40'} transition-colors">
                                            <td class="py-2.5 px-3 font-semibold ${isLight ? 'text-slate-900' : 'text-slate-100'}">
                                                <div class="flex items-center gap-2">
                                                    <span class="w-5 h-5 rounded-md ${isLight ? 'bg-slate-200 text-slate-700' : 'bg-slate-800 text-slate-400'} font-mono font-bold text-[10px] flex items-center justify-center">#${idx + 1}</span>
                                                    <span class="truncate max-w-[130px]" title="${d.reason}">${d.reason}</span>
                                                </div>
                                            </td>
                                            <td class="py-2.5 px-3">
                                                <span class="px-2 py-0.5 rounded text-[10px] font-medium ${isLight ? 'bg-cyan-50 text-cyan-800 border-cyan-200' : 'bg-cyan-950/60 text-cyan-300 border-cyan-800/60'} border">
                                                    ${d.section || 'General'}
                                                </span>
                                            </td>
                                            <td class="py-2.5 px-3 text-right font-mono font-bold text-rose-400">
                                                ${(d.reject_quantity || 0).toLocaleString()} pcs
                                            </td>
                                            <td class="py-2.5 px-3 text-xs ${isLight ? 'text-slate-700' : 'text-slate-300'} max-w-[170px] truncate" title="${d.action_recommendation || ''}">
                                                ${d.action_recommendation || 'Analisis 5-Why problem solving'}
                                            </td>
                                        </tr>
                                    `).join('')}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();

        // Bind subtab buttons
        this.bindDashboardSubTabButtons();

        document.getElementById('btn-jump-downtime')?.addEventListener('click', () => this.switchTab('downtime'));
        document.getElementById('btn-jump-quality')?.addEventListener('click', () => this.switchTab('quality'));

        if (this.currentLang === 'ja') {
            i18n.localizeDom(document.getElementById('content-body') || document.body);
        }
    }

    // ==========================================
    // 1C. DASHBOARD SUB-TAB: ANALYSIS OF SUB ASSETS
    // ==========================================
    async renderDashboardSubAssets(force = false) {
        const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');
        const content = document.getElementById('content-body');
        if (!content) return;

        const currentKey = JSON.stringify(this.filters);
        let subData = (!force && this._dashSubAssetsCache && this._dashSubAssetsCacheKey === currentKey)
            ? this._dashSubAssetsCache
            : null;

        if (!subData) {
            if (!content.querySelector('.btn-dash-tab')) {
                content.innerHTML = `
                    <div class="h-96 flex items-center justify-center">
                        <div class="flex flex-col items-center gap-3">
                            <div class="w-9 h-9 border-3 border-emerald-500 border-t-transparent rounded-full animate-spin"></div>
                            <span class="text-xs text-slate-400 font-mono">Memuat Analysis of Sub Assets...</span>
                        </div>
                    </div>
                `;
            }

            const [machines, mRankRes, lRankRes] = await Promise.all([
                this.getDashboardRealtimeMachines(force),
                api.getMachineRanking(this.filters),
                api.getLineRanking(this.filters)
            ]);

            subData = {
                machines: machines,
                machineList: mRankRes.data?.data || [],
                lineList: lRankRes.data?.data || []
            };
            this._dashSubAssetsCache = subData;
            this._dashSubAssetsCacheKey = currentKey;
        }

        const machines = subData.machines || [];
        const machineList = subData.machineList;
        const lineList = subData.lineList;

        const runningCount = machines.filter(m => m.status === 'RUNNING').length;
        const idleCount = machines.filter(m => m.status === 'IDLE').length;
        const stopCount = machines.filter(m => ['STOP', 'STOPPED', 'BREAKDOWN', 'MAINTENANCE', 'OFFLINE'].includes((m.status || '').toUpperCase())).length;
        
        // Summary calculations
        const sortedByOee = [...machineList].filter(m => m.oee != null).sort((a, b) => b.oee - a.oee);
        const topMachine = sortedByOee[0] || null;
        const lowestMachine = sortedByOee[sortedByOee.length - 1] || null;
        const avgLineOee = lineList.length > 0 
            ? (lineList.reduce((acc, l) => acc + Number(l.oee || 0), 0) / lineList.length).toFixed(2)
            : '0.00';

        const subnavHtml = `
            <!-- TOP SUB-HEADER BAR -->
            <div class="flex flex-wrap items-center justify-between ${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-xl px-5 py-3 shadow-xl mb-4 gap-4">
                <div class="flex items-center gap-6">
                    <h1 class="text-2xl font-black tracking-tight ${isLight ? 'text-slate-900' : 'text-white'} flex items-center gap-2">
                        <span class="text-cyan-400">OEE</span>
                    </h1>

                    <!-- SUB-NAV PILLS -->
                    <div class="flex items-center gap-1.5 ${isLight ? 'bg-slate-100 border-slate-300' : 'bg-slate-950 border-slate-800'} p-1 rounded-lg border text-xs font-semibold">
                        <button data-dtab="overview" class="btn-dash-tab px-3.5 py-1.5 rounded-md transition-all cursor-pointer ${this.dashboardSubTab === 'overview' ? 'bg-cyan-600 text-white shadow-md font-bold' : 'text-slate-400 hover:text-slate-200'}">OEE overview</button>
                        <button data-dtab="error" class="btn-dash-tab px-3.5 py-1.5 rounded-md transition-all cursor-pointer ${this.dashboardSubTab === 'error' ? 'bg-cyan-600 text-white shadow-md font-bold' : 'text-slate-400 hover:text-slate-200'}">Error analysis</button>
                        <button data-dtab="subassets" class="btn-dash-tab px-3.5 py-1.5 rounded-md transition-all cursor-pointer ${this.dashboardSubTab === 'subassets' ? 'bg-cyan-600 text-white shadow-md font-bold' : 'text-slate-400 hover:text-slate-200'}">Analysis of sub assets</button>
                    </div>
                </div>

                <div class="flex items-center gap-3 text-xs">
                    <div class="flex items-center gap-2">
                        <span class="px-2.5 py-1 rounded-full bg-emerald-500/10 text-emerald-400 border border-emerald-500/30 font-mono font-bold text-[11px] flex items-center gap-1.5">
                            <span class="w-2 h-2 rounded-full bg-emerald-500"></span> ${runningCount} RUNNING
                        </span>
                        <span class="px-2.5 py-1 rounded-full bg-amber-500/10 text-amber-400 border border-amber-500/30 font-mono font-bold text-[11px] flex items-center gap-1.5">
                            <span class="text-[9px]">▲</span> ${idleCount} IDLE
                        </span>
                        <span class="px-2.5 py-1 rounded-full bg-rose-500/15 text-rose-400 border border-rose-500/40 font-mono font-bold text-[11px] flex items-center gap-1.5 ${stopCount > 0 ? 'status-pulse-breakdown' : ''}">
                            <span class="text-[9px]">■</span> ${stopCount} STOP
                        </span>
                    </div>
                </div>
            </div>
        `;

        content.innerHTML = `
            ${subnavHtml}

            <!-- SUB-ASSETS SUMMARY ROW -->
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-5">
                <div class="${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-xl p-4 shadow-xl">
                    <div class="flex items-center justify-between text-xs text-slate-400 font-bold mb-1">
                        <span>TOTAL PRODUCTION LINES</span>
                        <i data-lucide="git-fork" class="w-4 h-4 text-cyan-400"></i>
                    </div>
                    <div class="flex items-baseline gap-2 font-mono">
                        <span class="text-2xl font-extrabold text-cyan-400">${lineList.length}</span>
                        <span class="text-xs text-slate-400">Lini Produksi</span>
                    </div>
                    <div class="mt-2 pt-2 border-t ${isLight ? 'border-slate-100' : 'border-slate-800'} text-[11px] text-slate-400 flex justify-between font-mono">
                        <span>Rata-rata OEE:</span>
                        <span class="font-bold text-cyan-400">${avgLineOee}%</span>
                    </div>
                </div>

                <div class="${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-xl p-4 shadow-xl">
                    <div class="flex items-center justify-between text-xs text-slate-400 font-bold mb-1">
                        <span>TOTAL SUB-ASSETS (MESIN)</span>
                        <i data-lucide="cpu" class="w-4 h-4 text-emerald-400"></i>
                    </div>
                    <div class="flex items-baseline gap-2 font-mono">
                        <span class="text-2xl font-extrabold text-emerald-400">${machineList.length}</span>
                        <span class="text-xs text-slate-400">Unit Mesin Terdaftar</span>
                    </div>
                    <div class="mt-2 pt-2 border-t ${isLight ? 'border-slate-100' : 'border-slate-800'} text-[11px] text-slate-400 flex justify-between font-mono">
                        <span>Status Aktif:</span>
                        <span class="font-bold text-emerald-400">${runningCount} Operasional</span>
                    </div>
                </div>

                <div class="${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-xl p-4 shadow-xl">
                    <div class="flex items-center justify-between text-xs text-slate-400 font-bold mb-1">
                        <span>TOP PERFORMING ASSET</span>
                        <i data-lucide="trending-up" class="w-4 h-4 text-emerald-400"></i>
                    </div>
                    <div class="text-sm font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'} truncate">
                        ${topMachine ? `${topMachine.code} • ${topMachine.name}` : 'N/A'}
                    </div>
                    <div class="mt-2 pt-2 border-t ${isLight ? 'border-slate-100' : 'border-slate-800'} text-[11px] text-slate-400 flex justify-between font-mono">
                        <span>OEE Score:</span>
                        <span class="font-bold text-emerald-400">${topMachine ? topMachine.oee + '%' : '0%'}</span>
                    </div>
                </div>

                <div class="${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-xl p-4 shadow-xl">
                    <div class="flex items-center justify-between text-xs text-slate-400 font-bold mb-1">
                        <span>BOTTLENECK / LOWEST ASSET</span>
                        <i data-lucide="alert-octagon" class="w-4 h-4 text-rose-400"></i>
                    </div>
                    <div class="text-sm font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'} truncate">
                        ${lowestMachine ? `${lowestMachine.code} • ${lowestMachine.name}` : 'N/A'}
                    </div>
                    <div class="mt-2 pt-2 border-t ${isLight ? 'border-slate-100' : 'border-slate-800'} text-[11px] text-slate-400 flex justify-between font-mono">
                        <span>OEE Score:</span>
                        <span class="font-bold text-rose-400">${lowestMachine ? lowestMachine.oee + '%' : '0%'}</span>
                    </div>
                </div>
            </div>

            <!-- SECTION 1: PRODUCTION LINES HEALTH GRID -->
            <div class="${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-xl p-5 shadow-xl mb-6">
                <div class="flex items-center justify-between pb-3 border-b ${isLight ? 'border-slate-200' : 'border-slate-800'} mb-4">
                    <div>
                        <h3 class="font-bold text-sm ${isLight ? 'text-slate-900' : 'text-slate-100'} flex items-center gap-2">
                            <i data-lucide="git-fork" class="w-4 h-4 text-cyan-400"></i>
                            Performa Lini Produksi (Production Lines Matrix)
                        </h3>
                        <p class="text-xs ${isLight ? 'text-slate-500' : 'text-slate-400'} mt-0.5">Ringkasan efisiensi dan output per lini produksi</p>
                    </div>
                    <button id="btn-jump-lines" class="px-3 py-1 bg-cyan-500/10 hover:bg-cyan-500/20 text-cyan-400 border border-cyan-500/30 rounded-lg text-xs font-semibold flex items-center gap-1 cursor-pointer transition-all">
                        <span>Detail Production Lines</span>
                        <i data-lucide="arrow-right" class="w-3.5 h-3.5"></i>
                    </button>
                </div>

                <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4">
                    ${lineList.map(l => `
                        <div class="${isLight ? 'bg-slate-50 border-slate-200 hover:border-cyan-400' : 'bg-slate-950 border-slate-800 hover:border-cyan-500/60'} border rounded-xl p-3.5 transition-all shadow-xs">
                            <div class="flex items-center justify-between mb-1.5">
                                <div class="font-bold text-sm ${isLight ? 'text-slate-900' : 'text-slate-100'} flex items-center gap-1.5">
                                    <i data-lucide="git-fork" class="w-3.5 h-3.5 text-cyan-400"></i>
                                    <span>${l.name}</span>
                                </div>
                                <span class="font-mono font-extrabold text-sm ${Number(l.oee) >= 85 ? 'text-emerald-400' : (Number(l.oee) >= 65 ? 'text-amber-400' : (Number(l.oee) > 0 ? 'text-rose-400' : 'text-slate-500'))}">
                                    ${l.oee}% OEE
                                </span>
                            </div>
                            <!-- DYNAMIC PRODUCT INFO -->
                            <div class="flex items-center gap-1 text-[11px] mb-2.5 truncate font-sans" title="Type: ${l.product_summary || '-'}">
                                <i data-lucide="box" class="w-3 h-3 text-cyan-400 shrink-0"></i>
                                <span class="text-slate-400 text-[10px] shrink-0">Type:</span>
                                <span class="font-semibold ${isLight ? 'text-cyan-800' : 'text-cyan-300'} truncate">${l.product_summary || '-'}</span>
                            </div>
                            <div class="grid grid-cols-3 gap-1 text-[10px] font-mono text-center mb-2.5 pt-2 border-t ${isLight ? 'border-slate-200' : 'border-slate-800/80'}">
                                <div class="${isLight ? 'bg-white' : 'bg-slate-900'} p-1 rounded border ${isLight ? 'border-slate-200' : 'border-slate-800'}">
                                    <div class="text-slate-500 text-[9px]">Avail</div>
                                    <div class="font-bold text-cyan-400">${l.availability}%</div>
                                </div>
                                <div class="${isLight ? 'bg-white' : 'bg-slate-900'} p-1 rounded border ${isLight ? 'border-slate-200' : 'border-slate-800'}">
                                    <div class="text-slate-500 text-[9px]">Perf</div>
                                    <div class="font-bold text-amber-400">${l.performance}%</div>
                                </div>
                                <div class="${isLight ? 'bg-white' : 'bg-slate-900'} p-1 rounded border ${isLight ? 'border-slate-200' : 'border-slate-800'}">
                                    <div class="text-slate-500 text-[9px]">Qual</div>
                                    <div class="font-bold text-emerald-400">${l.quality}%</div>
                                </div>
                            </div>
                            <div class="flex items-center justify-between text-[11px] font-mono text-slate-400">
                                <span>Output:</span>
                                <span class="font-bold ${isLight ? 'text-slate-800' : 'text-slate-200'}">${(l.actual_quantity || 0).toLocaleString()} / ${(l.target_quantity || 0).toLocaleString()} pcs</span>
                            </div>
                        </div>
                    `).join('')}
                </div>
            </div>

            <!-- SECTION 2: SUB-ASSET / MACHINE RANKING TABLE -->
            <div class="${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-xl p-5 shadow-xl">
                <div class="flex items-center justify-between pb-3 border-b ${isLight ? 'border-slate-200' : 'border-slate-800'} mb-4">
                    <div>
                        <h3 class="font-bold text-sm ${isLight ? 'text-slate-900' : 'text-slate-100'} flex items-center gap-2">
                            <i data-lucide="cpu" class="w-4 h-4 text-emerald-400"></i>
                            Peringkat Efisiensi Sub-Asset Mesin (Machine Ranking)
                        </h3>
                        <p class="text-xs ${isLight ? 'text-slate-500' : 'text-slate-400'} mt-0.5">Daftar utilisasi seluruh unit mesin dan stasiun kerja</p>
                    </div>
                    <button id="btn-jump-machines" class="px-3 py-1 bg-emerald-500/10 hover:bg-emerald-500/20 text-emerald-400 border border-emerald-500/30 rounded-lg text-xs font-semibold flex items-center gap-1 cursor-pointer transition-all">
                        <span>Detail Machine Performance</span>
                        <i data-lucide="arrow-right" class="w-3.5 h-3.5"></i>
                    </button>
                </div>

                <div class="overflow-x-auto">
                    <table class="w-full text-left text-xs border-collapse font-sans">
                        <thead>
                            <tr class="${isLight ? 'bg-slate-100 text-slate-700' : 'bg-slate-950/80 text-slate-400'} font-semibold border-b ${isLight ? 'border-slate-200' : 'border-slate-800'}">
                                <th class="py-2.5 px-3">Kode & Nama Mesin</th>
                                <th class="py-2.5 px-3">Lini Produksi</th>
                                <th class="py-2.5 px-3">Status</th>
                                <th class="py-2.5 px-3 text-right">Availability</th>
                                <th class="py-2.5 px-3 text-right">Performance</th>
                                <th class="py-2.5 px-3 text-right">Quality</th>
                                <th class="py-2.5 px-3 text-right">Overall OEE</th>
                                <th class="py-2.5 px-3 text-right">Output Aktual</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y ${isLight ? 'divide-slate-200' : 'divide-slate-800/60'}">
                            ${machineList.length === 0 ? `
                                <tr><td colspan="8" class="text-center py-8 text-slate-500 text-xs">Tidak ada data mesin</td></tr>
                            ` : machineList.slice(0, 15).map(m => `
                                <tr class="${isLight ? 'hover:bg-slate-50' : 'hover:bg-slate-800/40'} transition-colors">
                                    <td class="py-2.5 px-3 font-semibold ${isLight ? 'text-slate-900' : 'text-slate-100'}">
                                        <div class="flex items-center gap-2">
                                            <span class="font-mono text-cyan-400 font-bold">${m.code}</span>
                                            <span>${m.name}</span>
                                        </div>
                                    </td>
                                    <td class="py-2.5 px-3">
                                        <span class="px-2 py-0.5 rounded text-[10px] font-semibold ${isLight ? 'bg-slate-100 text-slate-700 border-slate-300' : 'bg-slate-800 text-slate-300 border-slate-700'} border">
                                            ${m.line_name || 'N/A'}
                                        </span>
                                    </td>
                                    <td class="py-2.5 px-3">
                                        <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded text-[10px] font-bold border ${this.getMachineStatusBadge(m.status)}">
                                            ${this.getStatusIcon(m.status)}
                                            <span>${m.status}</span>
                                        </span>
                                    </td>
                                    <td class="py-2.5 px-3 text-right font-mono text-cyan-400 font-bold">${m.availability}%</td>
                                    <td class="py-2.5 px-3 text-right font-mono text-amber-400 font-bold">${m.performance}%</td>
                                    <td class="py-2.5 px-3 text-right font-mono text-emerald-400 font-bold">${m.quality}%</td>
                                    <td class="py-2.5 px-3 text-right font-mono font-extrabold text-sm ${Number(m.oee) >= 85 ? 'text-emerald-400' : (Number(m.oee) >= 65 ? 'text-amber-400' : 'text-rose-400')}">
                                        ${m.oee}%
                                    </td>
                                    <td class="py-2.5 px-3 text-right font-mono text-slate-300">
                                        ${(m.actual_quantity || 0).toLocaleString()} / ${(m.target_quantity || 0).toLocaleString()} pcs
                                    </td>
                                </tr>
                            `).join('')}
                        </tbody>
                    </table>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();

        // Bind subtab buttons
        document.querySelectorAll('.btn-dash-tab').forEach(btn => {
            btn.addEventListener('click', (e) => {
                const subtab = e.currentTarget.getAttribute('data-dtab');
                if (subtab) {
                    this.dashboardSubTab = subtab;
                    this.renderDashboard();
                }
            });
        });

        document.getElementById('btn-jump-lines')?.addEventListener('click', () => this.switchTab('lines'));
        document.getElementById('btn-jump-machines')?.addEventListener('click', () => this.switchTab('machines'));

        if (this.currentLang === 'ja') {
            i18n.localizeDom(document.getElementById('content-body') || document.body);
        }
    }

    // ==========================================
    // 2. PRODUCTION MONITORING PAGE (REALTIME GRID)
    // ==========================================
    async renderMonitoring() {
        if (!this.selectedMonitoringDate) {
            this.selectedMonitoringDate = new Date().toISOString().slice(0, 10);
        }
        if (!this.monitoringStatusFilter) {
            this.monitoringStatusFilter = 'ALL';
        }

        const res = await api.getRealtimeStatus({ date: this.selectedMonitoringDate });
        let machines = res.data.data || [];
        const isToday = res.data.is_today;
        const todayDateStr = new Date().toISOString().slice(0, 10);

        // 1. Filter by Line ID from topbar
        if (this.filters.line_id) {
            const selectedLine = (this.masterData.lines || []).find(l => l.id == this.filters.line_id);
            if (selectedLine) {
                machines = machines.filter(m => (m.line_name || '').toLowerCase() === selectedLine.name.toLowerCase());
            }
        }

        // 2. Filter by Machine ID from topbar
        if (this.filters.machine_id) {
            machines = machines.filter(m => m.machine_id == this.filters.machine_id);
        }

        // 3. Filter by Search Query
        const searchQuery = (this.monitoringSearchQuery || '').toLowerCase().trim();
        if (searchQuery) {
            machines = machines.filter(m => 
                (m.machine_code || '').toLowerCase().includes(searchQuery) ||
                (m.machine_name || '').toLowerCase().includes(searchQuery) ||
                (m.line_name || '').toLowerCase().includes(searchQuery) ||
                (m.product_name || '').toLowerCase().includes(searchQuery)
            );
        }

        // Calculate dynamic status counts
        const totalCount = machines.length;
        const runningCount = machines.filter(m => (m.status || '').toUpperCase() === 'RUNNING').length;
        const breakdownCount = machines.filter(m => (m.status || '').toUpperCase() === 'BREAKDOWN').length;
        const idleCount = machines.filter(m => (m.status || '').toUpperCase() === 'IDLE').length;
        const maintCount = machines.filter(m => (m.status || '').toUpperCase() === 'MAINTENANCE').length;
        const stopCount = machines.filter(m => ['STOP', 'STOPPED', 'OFFLINE'].includes((m.status || '').toUpperCase())).length;

        // Apply status filter
        let displayMachines = machines;
        if (this.monitoringStatusFilter && this.monitoringStatusFilter !== 'ALL') {
            if (this.monitoringStatusFilter === 'STOP') {
                displayMachines = machines.filter(m => ['STOP', 'STOPPED', 'OFFLINE'].includes((m.status || '').toUpperCase()));
            } else {
                displayMachines = machines.filter(m => (m.status || '').toUpperCase() === this.monitoringStatusFilter);
            }
        }

        const activeFilter = this.monitoringStatusFilter;

        const content = document.getElementById('content-body');
        content.innerHTML = `
            <div class="flex flex-wrap items-center justify-between gap-4 mb-4">
                <div>
                    <h2 class="text-xl font-bold text-slate-100 flex items-center gap-2">
                        <i data-lucide="activity" class="w-5 h-5 text-cyan-400"></i>
                        <span>Production Monitoring</span>
                        ${!isToday ? `<span class="text-xs font-semibold px-2.5 py-0.5 rounded-full bg-amber-950 text-amber-300 border border-amber-800">📅 ${this.selectedMonitoringDate}</span>` : ''}
                    </h2>
                </div>
                
                <div class="flex flex-wrap items-center gap-3">
                    <!-- DATE FILTER CONTROL -->
                    <div class="flex items-center gap-2 bg-slate-900 border border-slate-800 px-3 py-1.5 rounded-lg shadow-sm">
                        <i data-lucide="calendar" class="w-3.5 h-3.5 text-cyan-400"></i>
                        <span class="text-xs text-slate-400 font-medium font-sans">Filter Tanggal:</span>
                        <input type="date" id="input-monitoring-date" value="${this.selectedMonitoringDate}" class="bg-slate-950 border border-slate-800 rounded px-2 py-0.5 text-xs font-mono text-cyan-400 focus:outline-none focus:border-cyan-500 cursor-pointer" />
                        ${this.selectedMonitoringDate !== todayDateStr ? `
                            <button id="btn-reset-monitoring-date" title="Kembali ke Hari Ini" class="text-[10px] bg-cyan-950 hover:bg-cyan-900 border border-cyan-800 text-cyan-300 px-2 py-0.5 rounded font-medium cursor-pointer transition-colors flex items-center gap-1">
                                <i data-lucide="rotate-ccw" class="w-3 h-3"></i> Hari Ini
                            </button>
                        ` : ''}
                    </div>

                    <div class="relative w-56">
                        <i data-lucide="search" class="w-3.5 h-3.5 text-slate-500 absolute left-2.5 top-2.5"></i>
                        <input type="text" id="input-monitoring-search" placeholder="Search machine, line, SKU..." value="${this.monitoringSearchQuery || ''}" class="w-full bg-slate-950 border border-slate-800 rounded-lg pl-8 pr-3 py-1.5 text-xs text-slate-200 focus:outline-none focus:border-cyan-500 font-sans" />
                    </div>

                    <button id="btn-add-machine-monitoring" class="bg-cyan-600 hover:bg-cyan-500 text-white text-xs font-semibold px-3 py-1.5 rounded-lg flex items-center gap-1.5 cursor-pointer shadow-lg shadow-cyan-600/20">
                        <i data-lucide="plus" class="w-4 h-4"></i> + Add Machine
                    </button>

                    <div class="flex items-center gap-2 bg-slate-900 border border-slate-800 px-3 py-1.5 rounded-lg">
                        <span class="w-2.5 h-2.5 rounded-full ${isToday ? 'bg-emerald-500 animate-ping' : 'bg-amber-500'}"></span>
                        <span class="text-xs ${isToday ? 'text-emerald-400' : 'text-amber-400'} font-mono font-semibold">${isToday ? 'LIVE' : 'HISTORY'}</span>
                    </div>
                </div>
            </div>

            <!-- DYNAMIC STATUS FILTER TOOLBAR -->
            <div class="bg-slate-900/90 border border-slate-800/90 rounded-xl p-2.5 mb-5 shadow-sm backdrop-blur-sm flex flex-wrap items-center justify-between gap-3">
                <div class="flex items-center gap-1.5 flex-wrap">
                    <span class="text-xs font-semibold text-slate-400 mr-1 flex items-center gap-1.5">
                        <i data-lucide="filter" class="w-3.5 h-3.5 text-cyan-400"></i> Status:
                    </span>
                    
                    <!-- ALL -->
                    <button data-status="ALL" class="btn-monitoring-status-filter px-3 py-1.5 rounded-lg text-xs font-semibold cursor-pointer transition-all flex items-center gap-1.5 ${activeFilter === 'ALL' ? 'bg-cyan-600 text-white shadow-md shadow-cyan-600/30' : 'bg-slate-950 text-slate-400 hover:text-slate-200 hover:bg-slate-800 border border-slate-800'}">
                        <span>Semua Status</span>
                        <span class="text-[10px] px-1.5 py-0.5 rounded-full font-mono font-bold ${activeFilter === 'ALL' ? 'bg-white/20 text-white' : 'bg-slate-800 text-slate-400'}">${totalCount}</span>
                    </button>

                    <!-- RUNNING -->
                    <button data-status="RUNNING" class="btn-monitoring-status-filter px-3 py-1.5 rounded-lg text-xs font-semibold cursor-pointer transition-all flex items-center gap-1.5 ${activeFilter === 'RUNNING' ? 'bg-emerald-600 text-white shadow-md shadow-emerald-600/30' : 'bg-slate-950 text-emerald-400 hover:bg-emerald-950/40 border border-emerald-900/50'}">
                        <span class="w-2 h-2 rounded-full bg-emerald-400 ${runningCount > 0 ? 'animate-pulse' : ''}"></span>
                        <span>Running</span>
                        <span class="text-[10px] px-1.5 py-0.5 rounded-full font-mono font-bold ${activeFilter === 'RUNNING' ? 'bg-white/20 text-white' : 'bg-emerald-950 text-emerald-300 border border-emerald-800'}">${runningCount}</span>
                    </button>

                    <!-- BREAKDOWN -->
                    <button data-status="BREAKDOWN" class="btn-monitoring-status-filter px-3 py-1.5 rounded-lg text-xs font-semibold cursor-pointer transition-all flex items-center gap-1.5 ${activeFilter === 'BREAKDOWN' ? 'bg-rose-600 text-white shadow-md shadow-rose-600/30' : 'bg-slate-950 text-rose-400 hover:bg-rose-950/40 border border-rose-900/50'}">
                        <span class="w-2 h-2 rounded-full bg-rose-500 ${breakdownCount > 0 ? 'animate-ping' : ''}"></span>
                        <span>Breakdown</span>
                        <span class="text-[10px] px-1.5 py-0.5 rounded-full font-mono font-bold ${activeFilter === 'BREAKDOWN' ? 'bg-white/20 text-white' : 'bg-rose-950 text-rose-300 border border-rose-800'}">${breakdownCount}</span>
                    </button>

                    <!-- IDLE -->
                    <button data-status="IDLE" class="btn-monitoring-status-filter px-3 py-1.5 rounded-lg text-xs font-semibold cursor-pointer transition-all flex items-center gap-1.5 ${activeFilter === 'IDLE' ? 'bg-amber-600 text-white shadow-md shadow-amber-600/30' : 'bg-slate-950 text-amber-400 hover:bg-amber-950/40 border border-amber-900/50'}">
                        <span class="w-2 h-2 rounded-full bg-amber-400"></span>
                        <span>Idle</span>
                        <span class="text-[10px] px-1.5 py-0.5 rounded-full font-mono font-bold ${activeFilter === 'IDLE' ? 'bg-white/20 text-white' : 'bg-amber-950 text-amber-300 border border-amber-800'}">${idleCount}</span>
                    </button>

                    <!-- MAINTENANCE -->
                    <button data-status="MAINTENANCE" class="btn-monitoring-status-filter px-3 py-1.5 rounded-lg text-xs font-semibold cursor-pointer transition-all flex items-center gap-1.5 ${activeFilter === 'MAINTENANCE' ? 'bg-purple-600 text-white shadow-md shadow-purple-600/30' : 'bg-slate-950 text-purple-400 hover:bg-purple-950/40 border border-purple-900/50'}">
                        <span class="w-2 h-2 rounded-full bg-purple-400"></span>
                        <span>Maintenance</span>
                        <span class="text-[10px] px-1.5 py-0.5 rounded-full font-mono font-bold ${activeFilter === 'MAINTENANCE' ? 'bg-white/20 text-white' : 'bg-purple-950 text-purple-300 border border-purple-800'}">${maintCount}</span>
                    </button>

                    <!-- STOP -->
                    <button data-status="STOP" class="btn-monitoring-status-filter px-3 py-1.5 rounded-lg text-xs font-semibold cursor-pointer transition-all flex items-center gap-1.5 ${activeFilter === 'STOP' ? 'bg-slate-700 text-white shadow-md shadow-slate-700/30' : 'bg-slate-950 text-slate-400 hover:bg-slate-800 border border-slate-800'}">
                        <span class="w-2 h-2 rounded-full bg-slate-500"></span>
                        <span>Stop / Offline</span>
                        <span class="text-[10px] px-1.5 py-0.5 rounded-full font-mono font-bold ${activeFilter === 'STOP' ? 'bg-white/20 text-white' : 'bg-slate-800 text-slate-400'}">${stopCount}</span>
                    </button>
                </div>

                <div class="text-xs text-slate-400 font-mono flex items-center gap-1.5">
                    <span>Menampilkan:</span>
                    <span class="text-cyan-400 font-bold px-1.5 py-0.5 bg-slate-950 border border-slate-800 rounded">${displayMachines.length}</span>
                    <span>dari ${totalCount} mesin</span>
                </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-5">
                ${displayMachines.length === 0 ? '<div class="col-span-full bg-slate-900 border border-slate-800 rounded-xl p-8 text-center text-slate-400 text-sm">Tidak ada data mesin dengan status ' + (activeFilter === 'ALL' ? '' : activeFilter) + ' pada tanggal ' + this.selectedMonitoringDate + '.</div>' : ''}
                ${displayMachines.map(m => `
                    <div class="bg-slate-900 border ${m.status === 'RUNNING' ? 'border-emerald-500/40 shadow-emerald-950/20' : (m.status === 'BREAKDOWN' ? 'border-rose-500/40 shadow-rose-950/20' : 'border-slate-800')} rounded-xl p-4 flex flex-col justify-between shadow-lg hover:border-slate-700 transition-all relative group">
                        <div>
                            <!-- CARD HEADER & DYNAMIC STATUS BADGE -->
                            <div class="flex items-center justify-between mb-2">
                                <span class="text-xs font-mono font-bold text-cyan-400">${m.machine_code}</span>
                                
                                <div class="flex items-center gap-1.5">
                                    <span class="text-[10px] font-bold rounded px-2 py-0.5 border ${m.status_badge_color || 'bg-slate-800 text-slate-400 border-slate-700'}">
                                        ${m.status === 'RUNNING' ? '🟢 RUNNING' : (m.status === 'BREAKDOWN' ? '🔴 BREAKDOWN' : (m.status === 'IDLE' ? '🟡 IDLE' : (m.status === 'MAINTENANCE' ? '🔧 MAINTENANCE' : '⏹️ STOP')))}
                                    </span>
                                </div>
                            </div>

                            <h4 class="font-bold text-sm text-slate-100 truncate">${m.machine_name}</h4>
                            <div class="text-[11px] text-slate-400 truncate mb-3">${m.line_name || 'N/A'} • ${m.product_name || 'No Active SKU'}</div>
                        </div>

                        <div class="space-y-2 pt-3 border-t border-slate-800/80 font-mono text-xs">
                            <div class="flex justify-between text-slate-400">
                                <span>Output (Good / Target):</span>
                                <span class="text-slate-100 font-bold">${m.good_quantity || 0} / ${m.target_quantity || 0}</span>
                            </div>
                            <div class="flex justify-between text-slate-400">
                                <span>Rejects / Scrap:</span>
                                <span class="text-rose-400 font-bold">${m.reject_quantity || 0}</span>
                            </div>
                            <div class="pt-2 flex items-center justify-between border-t border-slate-800">
                                <span class="text-slate-400 font-sans text-xs">OEE Score:</span>
                                <span class="text-sm font-bold ${m.oee >= 85 ? 'text-emerald-400' : (m.oee >= 70 ? 'text-amber-400' : 'text-cyan-400')}">${m.oee}%</span>
                            </div>

                            <!-- CARD ACTION BUTTONS FOOTER -->
                            <div class="pt-3 mt-1 border-t border-slate-800/60 flex items-center justify-between gap-1.5 font-sans">
                                <button data-type="machines" data-id="${m.machine_id}" class="btn-edit-master flex-1 inline-flex items-center justify-center gap-1 py-1 rounded bg-cyan-950/70 hover:bg-cyan-900 text-cyan-400 border border-cyan-800/70 text-[11px] font-semibold transition-all cursor-pointer" title="Edit Machine Master Data">
                                    <i data-lucide="pencil" class="w-3 h-3"></i>
                                    <span>Edit</span>
                                </button>
                                <button data-id="${m.machine_id}" data-code="${m.machine_code}" data-name="${m.machine_name}" class="btn-quick-entry flex-1 inline-flex items-center justify-center gap-1 py-1 rounded bg-emerald-950/70 hover:bg-emerald-900 text-emerald-400 border border-emerald-800/70 text-[11px] font-semibold transition-all cursor-pointer" title="Log Production Entry">
                                    <i data-lucide="plus-circle" class="w-3 h-3"></i>
                                    <span>+ Entry</span>
                                </button>
                                <button data-type="machines" data-id="${m.machine_id}" data-name="${m.machine_name || m.machine_code}" class="btn-delete-master px-2 py-1 rounded bg-rose-950/70 hover:bg-rose-900 text-rose-400 border border-rose-800/70 text-[11px] font-semibold transition-all cursor-pointer" title="Delete Machine">
                                    <i data-lucide="trash-2" class="w-3 h-3"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                `).join('')}
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();

        // Status Filter Buttons Event Listeners
        document.querySelectorAll('.btn-monitoring-status-filter').forEach(btn => {
            btn.addEventListener('click', (e) => {
                const status = e.currentTarget.getAttribute('data-status');
                this.monitoringStatusFilter = status;
                this.renderMonitoring();
            });
        });

        // Date Filter Event Listener
        const dateInput = document.getElementById('input-monitoring-date');
        if (dateInput) {
            dateInput.addEventListener('change', (e) => {
                this.selectedMonitoringDate = e.target.value;
                this.renderMonitoring();
            });
        }

        const btnResetDate = document.getElementById('btn-reset-monitoring-date');
        if (btnResetDate) {
            btnResetDate.addEventListener('click', () => {
                this.selectedMonitoringDate = new Date().toISOString().slice(0, 10);
                this.renderMonitoring();
            });
        }

        // Search Event Listener
        const searchInput = document.getElementById('input-monitoring-search');
        if (searchInput) {
            searchInput.addEventListener('input', (e) => {
                this.monitoringSearchQuery = e.target.value;
                this.renderMonitoring();
            });
        }

        // Add Machine Button Event Listener
        const btnAddMachine = document.getElementById('btn-add-machine-monitoring');
        if (btnAddMachine) {
            btnAddMachine.addEventListener('click', () => {
                if (!this.masterData || !this.masterData.machines) {
                    this.loadMasterData().then(() => this.showAddMasterModal('machines'));
                } else {
                    this.showAddMasterModal('machines');
                }
            });
        }

        // Quick Entry Log Event Listener
        document.querySelectorAll('.btn-quick-entry').forEach(btn => {
            btn.addEventListener('click', (e) => {
                const id = e.currentTarget.getAttribute('data-id');
                const code = e.currentTarget.getAttribute('data-code');
                const name = e.currentTarget.getAttribute('data-name');
                this.showQuickEntryModal(id, code, name);
            });
        });

        // Master Edit & Delete Modal Trigger Bindings
        document.querySelectorAll('.btn-edit-master').forEach(btn => {
            btn.addEventListener('click', (e) => {
                const type = e.currentTarget.getAttribute('data-type');
                const id = e.currentTarget.getAttribute('data-id');
                if (!this.masterData || !this.masterData.machines || !this.masterData.machines.length) {
                    this.loadMasterData().then(() => this.showEditMasterModal(type, id));
                } else {
                    this.showEditMasterModal(type, id);
                }
            });
        });

        document.querySelectorAll('.btn-delete-master').forEach(btn => {
            btn.addEventListener('click', (e) => {
                const type = e.currentTarget.getAttribute('data-type');
                const id = e.currentTarget.getAttribute('data-id');
                const name = e.currentTarget.getAttribute('data-name');
                this.showDeleteMasterModal(type, id, name);
            });
        });

        if (this.currentLang === 'ja') {
            i18n.localizeDom(document.getElementById('content-body') || document.body);
        }
    }

    showQuickEntryModal(machineId, machineCode, machineName) {
        const modalContainer = document.getElementById('modal-container');
        modalContainer.innerHTML = `
            <div class="fixed inset-0 bg-slate-950/80 backdrop-blur-sm flex items-center justify-center p-4 z-50">
                <div class="bg-slate-900 border border-slate-800 rounded-xl p-6 max-w-md w-full shadow-2xl">
                    <div class="flex items-center justify-between mb-4 pb-3 border-b border-slate-800">
                        <h3 class="font-bold text-sm text-slate-100 flex items-center gap-2">
                            <i data-lucide="plus-circle" class="w-4 h-4 text-emerald-400"></i>
                            Log Entry Record: ${machineCode} (${machineName})
                        </h3>
                        <button id="btn-close-quick-entry" class="text-slate-400 hover:text-slate-200 cursor-pointer">
                            <i data-lucide="x" class="w-4 h-4"></i>
                        </button>
                    </div>

                    <form id="form-quick-entry" class="space-y-4 text-xs">
                        <input type="hidden" name="machine_id" value="${machineId}" />
                        
                        <div>
                            <label class="block text-slate-400 mb-1">Finish Good Quantity (Pcs)</label>
                            <input type="number" name="good_quantity" value="0" min="0" placeholder="0" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 font-mono" />
                        </div>
                        <div>
                            <label class="block text-slate-400 mb-1">Not Good (NG) Quantity (Pcs)</label>
                            <input type="number" name="reject_quantity" value="0" min="0" placeholder="0" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 font-mono" />
                        </div>
                        <div>
                            <label class="block text-slate-400 mb-1">Target Qty (Pcs)</label>
                            <input type="number" name="target_quantity" value="0" min="0" placeholder="0" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 font-mono" />
                        </div>

                        <div class="pt-4 flex justify-end gap-2 border-t border-slate-800">
                            <button type="button" id="btn-cancel-quick-entry" class="px-3 py-1.5 bg-slate-800 hover:bg-slate-700 text-slate-300 rounded font-medium cursor-pointer">Cancel</button>
                            <button type="submit" class="px-4 py-1.5 bg-emerald-600 hover:bg-emerald-500 text-white font-semibold rounded shadow-md shadow-emerald-600/20 cursor-pointer">Submit Log Entry</button>
                        </div>
                    </form>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();
        if (this.currentLang === 'ja') {
            i18n.localizeDom(modalContainer);
        }

        document.getElementById('btn-close-quick-entry').addEventListener('click', () => modalContainer.innerHTML = '');
        document.getElementById('btn-cancel-quick-entry').addEventListener('click', () => modalContainer.innerHTML = '');

        document.getElementById('form-quick-entry').addEventListener('submit', async (e) => {
            e.preventDefault();
            const formData = new FormData(e.target);
            const data = Object.fromEntries(formData.entries());

            const targetMachine = (this.masterData.machines || []).find(m => m.id == machineId);
            const lineId = targetMachine?.work_center?.production_line_id || targetMachine?.work_center_id || (this.masterData.lines?.[0]?.id || 17);
            const productId = (this.masterData.products?.[0]?.id || 1);
            const shiftId = (this.masterData.shifts?.[0]?.id || 1);

            const goodQty = parseInt(data.good_quantity || 0);
            const rejectQty = parseInt(data.reject_quantity || 0);
            const totalQty = goodQty + rejectQty;

            const payload = {
                production_line_id: lineId,
                machine_id: parseInt(machineId),
                product_id: productId,
                shift_id: shiftId,
                production_date: new Date().toISOString().slice(0, 10),
                planned_production_time: 480,
                planned_downtime: 60,
                run_time: 380,
                downtime: 40,
                idle_time: 0,
                ideal_cycle_time: 9.5,
                target_quantity: parseInt(data.target_quantity || 1000),
                total_quantity: totalQty,
                good_quantity: goodQty,
                reject_quantity: rejectQty,
                scrap_quantity: 0
            };

            try {
                await (api.createProductionEntry || api.createProductionRecord)(payload);
                this.playClingSound();
                this.showNotification('Production Entry Logged', `Logged ${goodQty} pcs for ${machineCode}`, 'create');
                modalContainer.innerHTML = '';
                this.renderMonitoring();
            } catch (err) {
                const errorMsg = err.response?.data?.message || err.message || 'Error submitting entry';
                this.showNotification('⚠️ Gagal Menyimpan', errorMsg, 'delete');
            }
        });
    }

    /**
     * Interactive Six Big Losses Breakdown & TPM Analysis Modal Popup
     * Displays complete mathematical breakdown, 3 OEE pillars, 6 loss cards with definitions,
     * formulas, categorized losstime causes, and live contributing Pareto downtime & defect data.
     */
    async showSixBigLossesDetailModal(initialLossData = null, initialKpi = null) {
        const modalContainer = document.getElementById('modal-container');
        if (!modalContainer) return;

        const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');
        const isJa = this.currentLang === 'ja';

        // Display smooth loading spinner
        modalContainer.innerHTML = `
            <div class="fixed inset-0 bg-slate-950/80 backdrop-blur-sm flex items-center justify-center p-3 sm:p-4 z-50 overflow-y-auto">
                <div class="${isLight ? 'bg-white border-slate-200 text-slate-900 shadow-2xl' : 'bg-slate-900 border-slate-800 text-slate-100 shadow-2xl'} border rounded-2xl max-w-2xl w-full my-auto p-12 flex flex-col items-center justify-center gap-3 font-sans">
                    <div class="w-10 h-10 border-4 border-rose-500 border-t-transparent rounded-full animate-spin"></div>
                    <span class="text-xs ${isLight ? 'text-slate-600' : 'text-slate-400'} font-mono font-bold">${isJa ? '6大ロス詳細データを計算中...' : 'Memuat Rincian Six Big Losses & TPM Breakdown...'}</span>
                </div>
            </div>
        `;

        try {
            // Concurrent fetch of Six Losses, KPI, Pareto Downtime, and Pareto Defects for deep drill-down
            const [sixLossRes, kpiRes, paretoDtRes, paretoDefRes] = await Promise.all([
                initialLossData ? Promise.resolve({ data: { data: initialLossData } }) : api.getSixBigLosses(this.filters),
                initialKpi ? Promise.resolve({ data: { data: initialKpi } }) : api.getOeeKpi(this.filters),
                api.getParetoDowntime(this.filters).catch(() => ({ data: { data: [] } })),
                api.getParetoDefects(this.filters).catch(() => ({ data: { data: [] } }))
            ]);

            const losses = sixLossRes.data?.data || [];
            const kpi = kpiRes.data?.data || {};
            const dtPareto = paretoDtRes.data?.data || [];
            const defPareto = paretoDefRes.data?.data || [];

            // Calculate Totals
            const totalMins = losses.reduce((acc, it) => acc + (Number(it.minutes) || 0), 0);
            const totalHours = (totalMins / 60).toFixed(1);

            // Group by 3 Pillars
            let availLossMins = 0;
            let perfLossMins = 0;
            let qualLossMins = 0;

            losses.forEach(it => {
                const m = Number(it.minutes) || 0;
                const name = it.loss || '';
                const cat = (it.category || '').toLowerCase();
                if (cat.includes('avail') || name.includes('Equipment') || name.includes('Setup')) {
                    availLossMins += m;
                } else if (cat.includes('perf') || name.includes('Idling') || name.includes('Speed')) {
                    perfLossMins += m;
                } else {
                    qualLossMins += m;
                }
            });

            const availPct = totalMins > 0 ? ((availLossMins / totalMins) * 100).toFixed(1) : '0.0';
            const perfPct = totalMins > 0 ? ((perfLossMins / totalMins) * 100).toFixed(1) : '0.0';
            const qualPct = totalMins > 0 ? ((qualLossMins / totalMins) * 100).toFixed(1) : '0.0';

            // Six Big Losses Knowledge Base Configuration (Definitions, Formulas, Shop Floor Causes, Kaizen/TPM Strategies)
            const sixLossMetadata = {
                'Equipment Failure': {
                    number: 1,
                    jaName: '設備故障 (Breakdown Loss)',
                    pillar: 'Availability Loss',
                    jaPillar: '時間稼働ロス',
                    color: '#f43f5e',
                    badgeBg: isLight ? 'bg-rose-100 text-rose-700 border-rose-300' : 'bg-rose-950/60 border-rose-800 text-rose-400',
                    gradient: 'from-rose-600 to-rose-400',
                    icon: 'alert-octagon',
                    definition: isJa
                        ? '予期せぬ突発的な設備故障・トラブルによる停止時間。'
                        : 'Kerusakan mesin atau komponen yang tidak terencana (unplanned downtime) sehingga proses produksi terhenti total.',
                    formula: isJa
                        ? '突発停止時間の合計 (分・時間)'
                        : 'Total Durasi Breakdown Unplanned Downtime (Menit / Jam)',
                    categories: isJa
                        ? ['機械的破損 (ベアリング、モーター、ギア等)', '電気・制御系トラブル (センサー、PLC、配線)', '油圧・空圧系の圧力低下・漏れ', '過熱 (Overheat) & 異常振動']
                        : ['Kerusakan mekanikal (bearing, motor, gear aus)', 'Trouble kelistrikan & kontrol (sensor, PLC, short circuit)', 'Kebocoran atau drop tekanan pneumatik / hidrolik', 'Overheat & vibrasi berlebih pada mesin'],
                    kaizen: isJa
                        ? '自主保全 (清掃・給油・増締め)、定期予防保全 (PM)、CBM (状態監視保全) の導入、なぜなぜ分析 (RCA) による再発防止。'
                        : 'Penerapan Autonomous Maintenance (Jishu Hozen 5S), Preventive Maintenance terjadwal, CBM (Condition-Based Monitoring), dan 5-Whys RCA untuk pencegahan terulang.'
                },
                'Setup & Adjustment': {
                    number: 2,
                    jaName: '段取り・調整 (Setup & Adjustment)',
                    pillar: 'Availability Loss',
                    jaPillar: '時間稼働ロス',
                    color: '#f97316',
                    badgeBg: isLight ? 'bg-orange-100 text-orange-700 border-orange-300' : 'bg-orange-950/60 border-orange-800 text-orange-400',
                    gradient: 'from-orange-600 to-orange-400',
                    icon: 'wrench',
                    definition: isJa
                        ? 'ロット切替、金型・刃具交換、仕様変更に伴う段取り替え及び初期微調整時間。'
                        : 'Waktu terhenti saat pergantian mold, dies, pergantian tipe produk (changeover SKU), serta fine-tuning posisi awal.',
                    formula: isJa
                        ? '段取り・金型交換・初期調整時間の合計 (分)'
                        : 'Total Durasi Changeover SKU + Mold Change + Alignment Tuning (Menit)',
                    categories: isJa
                        ? ['金型・治具・刃具の交換作業', '生産品目 (SKU) 切り替え作業', '初期寸法合わせ・試運転・センシング位置調整', '材料交換 & 供給ライン段取り']
                        : ['Pergantian mold, jig, dies, atau cutting tool', 'Changeover antar model / SKU produk', 'Fine-tuning setting awal, kalibrasi jarak, & trial clamping', 'Persiapan material awal lot baru'],
                    kaizen: isJa
                        ? 'SMED (シングル段取り) 手法の適用、外段取り化の推進、段取り手順標準化 (SOP)、ワンタッチ治具の導入。'
                        : 'Penerapan metode SMED (Single Minute Exchange of Dies), konversi setup internal ke eksternal, SOP standar baku, dan jig sistem quick-clamp.'
                },
                'Idling & Minor Stops': {
                    number: 3,
                    jaName: 'チョコ停・空転 (Idling & Minor Stops)',
                    pillar: 'Performance Loss',
                    jaPillar: '性能稼働ロス',
                    color: '#eab308',
                    badgeBg: isLight ? 'bg-amber-100 text-amber-700 border-amber-300' : 'bg-amber-950/60 border-amber-800 text-amber-400',
                    gradient: 'from-amber-600 to-amber-400',
                    icon: 'pause-circle',
                    definition: isJa
                        ? 'ワーク詰まり、センサー誤検知などによる一時的なライン停止 (通常5分未満) 及び設備の空転。'
                        : 'Pemberhentian mesin sesaat (< 5 menit) atau mesin beroperasi tanpa beban (idling) yang dapat di-reset cepat oleh operator tanpa tim maintenance.',
                    formula: isJa
                        ? 'チョコ停・空転累積時間 (分) または 稼働時間 - (実効稼働時間)'
                        : 'Total Durasi Chocotei & Idling Time (< 5 Menit per kejadian)',
                    categories: isJa
                        ? ['パーツフィーダー・シュート内でのワーク引っかかり・詰まり', '光電センサーの埃・切り粉付着による誤検知', 'コンベア満杯による待機 (Buffer Full)', 'オペレーターの材料補給・小休止による空転']
                        : ['Part macet / jamming di chute atau conveyor feeder', 'Sensor terhalang kotoran/gram yang memicu false stop', 'Mesin standby karena buffer line depan/belakang penuh', 'Jeda singkat operator saat memindahkan bin material'],
                    kaizen: isJa
                        ? 'センサー部へのエアブロー設置、シュート形状改善、チョコ停要因の見える化 (Andon)、日常清掃点検の徹底。'
                        : 'Pemasangan air-blow pembersih sensor otomatis, modifikasi sudut chute part, andon visual alarm, dan 5S area feeding.'
                },
                'Reduced Speed': {
                    number: 4,
                    jaName: '速度低下 (Reduced Speed Loss)',
                    pillar: 'Performance Loss',
                    jaPillar: '性能稼働ロス',
                    color: '#0284c7',
                    badgeBg: isLight ? 'bg-sky-100 text-sky-700 border-sky-300' : 'bg-sky-950/60 border-sky-800 text-cyan-400',
                    gradient: 'from-cyan-600 to-cyan-400',
                    icon: 'gauge',
                    definition: isJa
                        ? '設計上の基準サイクルタイム (Ideal Cycle Time) よりも遅い速度で運転されていることによる損失。'
                        : 'Kehilangan kapasitas karena mesin dioperasikan dengan cycle time lebih lambat dibanding kecepatan standar desain pabrik (Ideal Cycle Time).',
                    formula: isJa
                        ? '実稼働時間 - (基準サイクルタイム × 総生産数量)'
                        : 'Operating Run Time - (Ideal Cycle Time × Total Output Pcs) / 60',
                    categories: isJa
                        ? ['品質不良を懸念して意図的に速度を下げての運転', '機械の経年劣化・摩耗による定格速度低下', '作業者の不慣れによるタクトタイム延長', '材料硬度・寸法ばらつきによる加工速度ダウン']
                        : ['Operator menurunkan kecepatan karena khawatir timbul defect', 'Penurunan kapasitas dinamis mesin karena keausan mekanis', 'Cycle time manual operator melebihi standar takt time', 'Material keras atau tidak seragam sehingga feed rate diturunkan'],
                    kaizen: isJa
                        ? '設備の設計速度への復元保全、インバーター・モーターの最適化チューニング、標準作業組合せ票の見直し。'
                        : 'Restorasi kemampuan mesin ke rated speed, optimasi inverter/motor driver, review & audit berkala terhadap standar Ideal Cycle Time.'
                },
                'Process Defects': {
                    number: 5,
                    jaName: '工程不良・手直し (Process Defects)',
                    pillar: 'Quality Loss',
                    jaPillar: '品質ロス',
                    color: '#a855f7',
                    badgeBg: isLight ? 'bg-purple-100 text-purple-700 border-purple-300' : 'bg-purple-950/60 border-purple-800 text-purple-400',
                    gradient: 'from-purple-600 to-purple-400',
                    icon: 'shield-alert',
                    definition: isJa
                        ? '定常運転中に発生した不良品 (NG) 及び手直し (Rework) に要した時間損失。'
                        : 'Waktu produksi produktif yang terbuang sia-sia karena menghasilkan produk reject / NG atau memerlukan pengerjaan ulang (rework).',
                    formula: isJa
                        ? '(不良数量 × 基準サイクルタイム) / 60'
                        : '(Total Reject Pcs × Ideal Cycle Time) / 60 Menit',
                    categories: isJa
                        ? ['寸法公差外れ (Over / Under tolerance)', '外観不良 (キズ、打痕、バリ、巣穴、クラック)', '組付け不良・トルク不足・締め忘れ', '測定器 (Measuring) でのNG判定品']
                        : ['Dimensi out of tolerance (diameter, ketebalan, runout)', 'Cacat visual (scratch, dent, burr, crack, porosity)', 'Salah perakitan / bolt kurang torsi / missing part', 'Hasil inspeksi gauge / measuring machine reject'],
                    kaizen: isJa
                        ? 'ポカヨケ (ポカ除け治具) の設置、品質管理7つ道具による要因特定、自動インライン検査装置の導入、SPC管理図の運用。'
                        : 'Penerapan Poka-Yoke (Mistake-Proofing), Analisis QC 7 Tools & Pareto, kamera inspeksi visual otomatis, dan Statistical Process Control (SPC).'
                },
                'Reduced Yield (Scrap)': {
                    number: 6,
                    jaName: '立上り歩留ロス (Startup Scrap / Yield Loss)',
                    pillar: 'Quality Loss',
                    jaPillar: '品質ロス',
                    color: '#ec4899',
                    badgeBg: isLight ? 'bg-pink-100 text-pink-700 border-pink-300' : 'bg-pink-950/60 border-pink-800 text-pink-400',
                    gradient: 'from-pink-600 to-pink-400',
                    icon: 'trash-2',
                    definition: isJa
                        ? '始業時、金型交換直後、立ち上げ初期段階で発生する不良品及び歩留まり低下による損失。'
                        : 'Kehilangan waktu & material yang terjadi pada fase start-up awal saat mesin baru dinyalakan hingga proses mencapai kondisi stabil.',
                    formula: isJa
                        ? '(立上りスクラップ数量 × 基準サイクルタイム) / 60'
                        : '(Startup Scrap Pcs × Ideal Cycle Time) / 60 Menit',
                    categories: isJa
                        ? ['金型・炉の昇温待ち時の試打ちNG (Warming-up scrap)', '段取り後のファーストピース検査用サンプル不良', 'ライン立上げ時の材料パージ・押し出しロス', '初期ロットの寸法安定化待ち廃棄']
                        : ['Scrap saat pemanasan mold / furnace (warming-up test)', 'Sample piece inspeksi pertama setelah ganti mold/SKU', 'Purging material awal / sisa pembersihan jalur', 'Stabilisasi suhu pendingin dan tekanan di awal shift'],
                    kaizen: isJa
                        ? 'タイマー付き自動プレヒーティングの導入、初品検査手順の迅速化、スタートアップ標準パラメータの厳格化。'
                        : 'Pre-heating otomatis terjadwal sebelum shift dimulai, standardisasi checklist parameter start-up, dan prosedur first-piece release cepat.'
                }
            };

            // Active Filter Labels
            const filterPeriodLabel = this.filters.period === 'custom'
                ? `${this.filters.start_date || '-'} s/d ${this.filters.end_date || '-'}`
                : (this.filters.period || '30 Hari Terakhir');
            const filterLineLabel = this.filters.line_id
                ? (this.masterData.lines?.find(l => l.id == this.filters.line_id)?.name || `Line #${this.filters.line_id}`)
                : (isJa ? '全生産ライン (All Lines)' : 'Semua Lini Produksi');
            const filterMachineLabel = this.filters.machine_id === 'all_measuring'
                ? (isJa ? '全測定器 (All Measuring)' : 'Semua Mesin Measuring')
                : (this.filters.machine_id ? (this.masterData.machines?.find(m => m.id == this.filters.machine_id)?.name || `Machine #${this.filters.machine_id}`) : (isJa ? '全設備 (All Machines)' : 'Semua Mesin Operasional'));
            const filterShiftLabel = this.filters.shift_id
                ? (this.masterData.shifts?.find(s => s.id == this.filters.shift_id)?.name || `Shift #${this.filters.shift_id}`)
                : (isJa ? '全直 (All Shifts)' : 'Semua Shift Kerja');

            modalContainer.innerHTML = `
                <div class="fixed inset-0 bg-slate-950/85 backdrop-blur-md flex items-center justify-center p-2 sm:p-4 z-50 overflow-y-auto">
                    <div class="${isLight ? 'bg-white border-slate-200 text-slate-900 shadow-2xl' : 'bg-[#0B142C] border-[#1B2C56] text-slate-100 shadow-2xl'} border rounded-2xl max-w-5xl w-full my-auto overflow-hidden font-sans flex flex-col max-h-[92vh]">
                        
                        <!-- MODAL HEADER -->
                        <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-[#070D1E] border-[#152347]'} px-6 py-4 border-b flex items-center justify-between flex-shrink-0">
                            <div class="flex items-center gap-3">
                                <div class="w-11 h-11 rounded-xl bg-gradient-to-br from-rose-500/20 to-amber-500/20 border border-rose-500/40 text-rose-400 flex items-center justify-center shadow-inner flex-shrink-0">
                                    <i data-lucide="pie-chart" class="w-6 h-6"></i>
                                </div>
                                <div>
                                    <div class="flex items-center gap-2 flex-wrap">
                                        <h3 class="font-extrabold text-base sm:text-lg ${isLight ? 'text-slate-900' : 'text-slate-100'} flex items-center gap-2">
                                            <span>${isJa ? '6大ロス詳細分析 & TPMブレークダウン' : 'Six Big Losses Breakdown & TPM Analysis'}</span>
                                        </h3>
                                        <span class="px-2 py-0.5 rounded-full text-[10px] font-bold font-mono ${isLight ? 'bg-rose-100 text-rose-700 border border-rose-300' : 'bg-rose-950/80 text-rose-400 border border-rose-800/80'}">
                                            TPM 6 Pillars
                                        </span>
                                    </div>
                                    <p class="text-xs ${isLight ? 'text-slate-500' : 'text-slate-400'} mt-0.5 flex items-center gap-2 flex-wrap font-mono">
                                        <span>📅 ${filterPeriodLabel}</span> &bull;
                                        <span>🏭 ${filterLineLabel}</span> &bull;
                                        <span>⚙️ ${filterMachineLabel}</span> &bull;
                                        <span>⏰ ${filterShiftLabel}</span>
                                    </p>
                                </div>
                            </div>

                            <div class="flex items-center gap-2">
                                <div class="hidden sm:flex flex-col text-right font-mono pr-2">
                                    <span class="text-[10px] ${isLight ? 'text-slate-500' : 'text-slate-400'} uppercase font-sans">${isJa ? '総損失時間' : 'Total Losses'}</span>
                                    <span class="text-sm font-black text-rose-400">${totalHours}h <span class="text-[10px] font-normal text-slate-400">(${totalMins}m)</span></span>
                                </div>
                                <button id="btn-close-six-losses-modal" class="p-2 rounded-xl ${isLight ? 'text-slate-400 hover:text-slate-800 hover:bg-slate-200' : 'text-slate-400 hover:text-slate-100 hover:bg-slate-800'} cursor-pointer transition-colors" title="Tutup Modal (ESC)">
                                    <i data-lucide="x" class="w-5 h-5"></i>
                                </button>
                            </div>
                        </div>

                        <!-- MODAL BODY (SCROLLABLE) -->
                        <div class="p-4 sm:p-6 space-y-6 overflow-y-auto flex-1 text-xs custom-scrollbar">

                            <!-- 1. TOP SUMMARY: 3 PILLARS OF OEE LOSSES -->
                            <div>
                                <div class="flex items-center justify-between mb-2">
                                    <h4 class="font-bold text-xs uppercase tracking-wider ${isLight ? 'text-slate-700' : 'text-slate-300'} flex items-center gap-1.5">
                                        <i data-lucide="layers" class="w-3.5 h-3.5 text-cyan-400"></i>
                                        <span>${isJa ? 'OEE 3大要素別 損失サマリー' : 'Ringkasan Kerugian Berdasarkan 3 Pilar OEE'}</span>
                                    </h4>
                                    <span class="text-[11px] font-mono text-slate-400">${isJa ? '基準: TPM 世界基準フレームワーク' : 'Standar: World-Class TPM Framework'}</span>
                                </div>

                                <div class="grid grid-cols-1 sm:grid-cols-3 gap-3 font-mono">
                                    <!-- AVAILABILITY LOSS TILE -->
                                    <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-900/80 border-slate-800'} border rounded-xl p-3 relative overflow-hidden">
                                        <div class="flex items-center justify-between">
                                            <span class="text-[11px] font-bold font-sans ${isLight ? 'text-rose-700' : 'text-rose-400'} flex items-center gap-1.5">
                                                <span class="w-2 h-2 rounded-full bg-rose-500 shadow-[0_0_6px_#f43f5e]"></span>
                                                ${isJa ? '時間稼働ロス (Availability)' : 'Availability Loss'}
                                            </span>
                                            <span class="text-[10px] font-bold px-1.5 py-0.5 rounded ${isLight ? 'bg-rose-100 text-rose-700' : 'bg-rose-950 text-rose-400 border border-rose-800/60'}">${availPct}% of Loss</span>
                                        </div>
                                        <div class="text-lg font-black text-rose-400 mt-2">
                                            ${(availLossMins / 60).toFixed(1)}h <span class="text-xs font-normal text-slate-400">(${availLossMins}m)</span>
                                        </div>
                                        <p class="text-[10px] font-sans ${isLight ? 'text-slate-600' : 'text-slate-400'} mt-1">
                                            ${isJa ? '設備故障 + 段取り・調整ロス' : 'Equipment Failure + Setup & Adjustment'}
                                        </p>
                                        <div class="w-full h-1.5 rounded-full ${isLight ? 'bg-slate-200' : 'bg-slate-950'} mt-2 overflow-hidden">
                                            <div class="h-full rounded-full bg-rose-500" style="width: ${availPct}%;"></div>
                                        </div>
                                    </div>

                                    <!-- PERFORMANCE LOSS TILE -->
                                    <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-900/80 border-slate-800'} border rounded-xl p-3 relative overflow-hidden">
                                        <div class="flex items-center justify-between">
                                            <span class="text-[11px] font-bold font-sans ${isLight ? 'text-amber-700' : 'text-amber-400'} flex items-center gap-1.5">
                                                <span class="w-2 h-2 rounded-full bg-amber-500 shadow-[0_0_6px_#eab308]"></span>
                                                ${isJa ? '性能稼働ロス (Performance)' : 'Performance Loss'}
                                            </span>
                                            <span class="text-[10px] font-bold px-1.5 py-0.5 rounded ${isLight ? 'bg-amber-100 text-amber-700' : 'bg-amber-950 text-amber-400 border border-amber-800/60'}">${perfPct}% of Loss</span>
                                        </div>
                                        <div class="text-lg font-black text-amber-400 mt-2">
                                            ${(perfLossMins / 60).toFixed(1)}h <span class="text-xs font-normal text-slate-400">(${perfLossMins}m)</span>
                                        </div>
                                        <p class="text-[10px] font-sans ${isLight ? 'text-slate-600' : 'text-slate-400'} mt-1">
                                            ${isJa ? 'チョコ停・空転 + 速度低下ロス' : 'Idling & Minor Stops + Reduced Speed'}
                                        </p>
                                        <div class="w-full h-1.5 rounded-full ${isLight ? 'bg-slate-200' : 'bg-slate-950'} mt-2 overflow-hidden">
                                            <div class="h-full rounded-full bg-amber-500" style="width: ${perfPct}%;"></div>
                                        </div>
                                    </div>

                                    <!-- QUALITY LOSS TILE -->
                                    <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-900/80 border-slate-800'} border rounded-xl p-3 relative overflow-hidden">
                                        <div class="flex items-center justify-between">
                                            <span class="text-[11px] font-bold font-sans ${isLight ? 'text-purple-700' : 'text-purple-400'} flex items-center gap-1.5">
                                                <span class="w-2 h-2 rounded-full bg-purple-500 shadow-[0_0_6px_#a855f7]"></span>
                                                ${isJa ? '品質不良ロス (Quality)' : 'Quality Loss'}
                                            </span>
                                            <span class="text-[10px] font-bold px-1.5 py-0.5 rounded ${isLight ? 'bg-purple-100 text-purple-700' : 'bg-purple-950 text-purple-400 border border-purple-800/60'}">${qualPct}% of Loss</span>
                                        </div>
                                        <div class="text-lg font-black text-purple-400 mt-2">
                                            ${(qualLossMins / 60).toFixed(1)}h <span class="text-xs font-normal text-slate-400">(${qualLossMins}m)</span>
                                        </div>
                                        <p class="text-[10px] font-sans ${isLight ? 'text-slate-600' : 'text-slate-400'} mt-1">
                                            ${isJa ? '工程不良・手直し + 立上り歩留ロス' : 'Process Defects + Reduced Yield (Scrap)'}
                                        </p>
                                        <div class="w-full h-1.5 rounded-full ${isLight ? 'bg-slate-200' : 'bg-slate-950'} mt-2 overflow-hidden">
                                            <div class="h-full rounded-full bg-purple-500" style="width: ${qualPct}%;"></div>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- 2. THE 6 BIG LOSSES DETAILED BREAKDOWN GRID (6 CARDS) -->
                            <div>
                                <div class="flex items-center justify-between mb-3">
                                    <h4 class="font-bold text-xs uppercase tracking-wider ${isLight ? 'text-slate-700' : 'text-slate-300'} flex items-center gap-1.5">
                                        <i data-lucide="grid" class="w-3.5 h-3.5 text-cyan-400"></i>
                                        <span>${isJa ? '6大ロス詳細分類 & 計算内訳 (6 TPM Losses)' : 'Detail Perhitungan & Kategori 6 Big Losses'}</span>
                                    </h4>
                                    <span class="text-[11px] font-mono text-slate-400">${losses.length} ${isJa ? '項目' : 'Kategori Terhitung'}</span>
                                </div>

                                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                                    ${losses.map((item) => {
                                        const meta = sixLossMetadata[item.loss] || {
                                            number: 0,
                                            jaName: item.loss,
                                            pillar: item.category || 'Loss',
                                            jaPillar: '損失',
                                            color: '#3b82f6',
                                            badgeBg: isLight ? 'bg-blue-100 text-blue-700' : 'bg-blue-950 text-blue-400',
                                            gradient: 'from-blue-600 to-blue-400',
                                            icon: 'alert-circle',
                                            definition: 'Loss parameter dalam perhitungan OEE.',
                                            formula: 'Total Durasi (Menit)',
                                            categories: ['Downtime Operasional'],
                                            kaizen: 'Penerapan Kaizen & standardisasi proses.'
                                        };

                                        const mins = Number(item.minutes) || 0;
                                        const hrs = (mins / 60).toFixed(1);
                                        const pct = totalMins > 0 ? ((mins / totalMins) * 100).toFixed(1) : '0.0';

                                        return `
                                            <div class="${isLight ? 'bg-white border-slate-200 hover:border-slate-300' : 'bg-slate-900/90 border-slate-800 hover:border-slate-700'} border rounded-xl p-4.5 space-y-3 transition-all shadow-sm">
                                                <!-- CARD HEADER -->
                                                <div class="flex items-start justify-between gap-2 border-b ${isLight ? 'border-slate-100' : 'border-slate-800/80'} pb-2.5">
                                                    <div class="flex items-center gap-2.5 min-w-0">
                                                        <div class="w-8 h-8 rounded-lg flex items-center justify-center flex-shrink-0 font-bold font-mono text-white text-xs shadow-sm" style="background-color: ${meta.color};">
                                                            #${meta.number}
                                                        </div>
                                                        <div class="min-w-0">
                                                            <h5 class="font-extrabold text-sm ${isLight ? 'text-slate-900' : 'text-slate-100'} truncate" title="${item.loss}">
                                                                ${isJa ? meta.jaName : item.loss}
                                                            </h5>
                                                            <span class="text-[9.5px] px-1.5 py-0.2 rounded font-semibold border ${meta.badgeBg}">
                                                                ${isJa ? meta.jaPillar : meta.pillar}
                                                            </span>
                                                        </div>
                                                    </div>
                                                    <div class="text-right font-mono flex-shrink-0">
                                                        <div class="text-base font-black ${isLight ? 'text-slate-900' : 'text-slate-100'}">${hrs}h <span class="text-[10px] font-normal text-slate-400">(${mins}m)</span></div>
                                                        <div class="text-[10px] font-bold text-cyan-400">${pct}% of Total</div>
                                                    </div>
                                                </div>

                                                <!-- PROGRESS BAR -->
                                                <div class="w-full h-2 rounded-full ${isLight ? 'bg-slate-100' : 'bg-slate-950'} overflow-hidden border ${isLight ? 'border-slate-200' : 'border-slate-800'}">
                                                    <div class="h-full rounded-full bg-gradient-to-r ${meta.gradient}" style="width: ${pct}%;"></div>
                                                </div>

                                                <!-- DEFINITION & FORMULA -->
                                                <div class="space-y-1.5 text-[11px]">
                                                    <div>
                                                        <span class="font-bold ${isLight ? 'text-slate-700' : 'text-slate-300'}">${isJa ? '📖 定義:' : '📖 Definisi:'}</span>
                                                        <span class="${isLight ? 'text-slate-600' : 'text-slate-400'}">${meta.definition}</span>
                                                    </div>
                                                    <div class="${isLight ? 'bg-slate-50 text-slate-700 border-slate-200' : 'bg-slate-950/80 text-cyan-300 border-slate-800'} p-2 rounded-lg font-mono text-[10.5px] border">
                                                        <span class="font-bold text-slate-400 font-sans">${isJa ? '📐 計算式:' : '📐 Rumus:'}</span> ${meta.formula}
                                                    </div>
                                                </div>

                                                <!-- TYPICAL SHOP FLOOR CAUSES -->
                                                <div class="pt-2 border-t ${isLight ? 'border-slate-100' : 'border-slate-800/80'}">
                                                    <span class="font-bold text-[10.5px] ${isLight ? 'text-slate-800' : 'text-slate-300'} block mb-1">
                                                        ${isJa ? '⚠️ 含まれる停止・ロス要因 (Kategori Losstime):' : '⚠️ Kategori Losstime yang Termasuk:'}
                                                    </span>
                                                    <ul class="space-y-0.5 text-[10.5px] ${isLight ? 'text-slate-600' : 'text-slate-400'}">
                                                        ${meta.categories.map(catItem => `
                                                            <li class="flex items-start gap-1.5">
                                                                <span class="w-1 h-1 rounded-full bg-slate-400 mt-1.5 flex-shrink-0"></span>
                                                                <span>${catItem}</span>
                                                            </li>
                                                        `).join('')}
                                                    </ul>
                                                </div>

                                                <!-- KAIZEN / TPM STRATEGY -->
                                                <div class="${isLight ? 'bg-emerald-50/70 border-emerald-200 text-emerald-900' : 'bg-emerald-950/30 border-emerald-800/50 text-emerald-300'} p-2.5 rounded-lg border text-[10.5px]">
                                                    <span class="font-bold flex items-center gap-1 mb-0.5">
                                                        <i data-lucide="check-circle-2" class="w-3.5 h-3.5 text-emerald-400"></i>
                                                        <span>${isJa ? '💡 改善・TPM対策アプローチ:' : '💡 Strategi Solusi & Kaizen TPM:'}</span>
                                                    </span>
                                                    <p class="${isLight ? 'text-emerald-800' : 'text-emerald-300/90'} leading-relaxed">${meta.kaizen}</p>
                                                </div>
                                            </div>
                                        `;
                                    }).join('')}
                                </div>
                            </div>

                            <!-- 3. LIVE CONTRIBUTING PARETO DOWNTIME & DEFECT LOGS (DATA RIIL DARI SISTEM) -->
                            <div class="space-y-4 pt-2 border-t ${isLight ? 'border-slate-200' : 'border-slate-800'}">
                                <div class="flex items-center justify-between">
                                    <h4 class="font-bold text-xs uppercase tracking-wider ${isLight ? 'text-slate-700' : 'text-slate-300'} flex items-center gap-1.5">
                                        <i data-lucide="database" class="w-3.5 h-3.5 text-cyan-400"></i>
                                        <span>${isJa ? '実績データ内訳 (実測停止・不良データ)' : 'Data Riil Penyebab Downtime & Cacat Kualitas'}</span>
                                    </h4>
                                    <span class="text-[11px] font-mono text-slate-400">${isJa ? 'フィルター期間の実績集計' : 'Akumulasi Data Aktif'}</span>
                                </div>

                                <div class="grid grid-cols-1 lg:grid-cols-2 gap-4">
                                    <!-- DOWNTIME CAUSES TABLE -->
                                    <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-900/80 border-slate-800'} border rounded-xl p-4 space-y-2">
                                        <h5 class="font-bold text-xs ${isLight ? 'text-slate-800' : 'text-slate-200'} flex items-center justify-between">
                                            <span class="flex items-center gap-1.5">
                                                <i data-lucide="clock" class="w-3.5 h-3.5 text-rose-400"></i>
                                                <span>${isJa ? '主要停止要因 (Top Downtime Reasons)' : 'Alasan Downtime / Trouble Terbesar'}</span>
                                            </span>
                                            <span class="text-[10px] font-mono font-normal text-slate-400">${dtPareto.length} records</span>
                                        </h5>

                                        <div class="overflow-x-auto max-h-48 custom-scrollbar">
                                            <table class="w-full text-left text-[11px]">
                                                <thead>
                                                    <tr class="border-b ${isLight ? 'border-slate-200 text-slate-500' : 'border-slate-800 text-slate-400'} font-mono">
                                                        <th class="py-1 px-2">Alasan Downtime</th>
                                                        <th class="py-1 px-2 text-right">Durasi</th>
                                                        <th class="py-1 px-2 text-right">Freq</th>
                                                        <th class="py-1 px-2 text-right">%</th>
                                                    </tr>
                                                </thead>
                                                <tbody class="divide-y ${isLight ? 'divide-slate-200/60' : 'divide-slate-800/60'}">
                                                    ${dtPareto.length === 0 ? `
                                                        <tr>
                                                            <td colspan="4" class="text-center py-4 text-slate-500 font-mono">Tidak ada catatan downtime pada filter ini</td>
                                                        </tr>
                                                    ` : dtPareto.slice(0, 8).map(r => `
                                                        <tr class="${isLight ? 'hover:bg-slate-100' : 'hover:bg-slate-800/40'}">
                                                            <td class="py-1.5 px-2 font-medium ${isLight ? 'text-slate-800' : 'text-slate-200'} truncate max-w-[180px]">${r.reason || '-'}</td>
                                                            <td class="py-1.5 px-2 text-right font-mono font-bold text-rose-400">${r.duration_minutes || 0}m</td>
                                                            <td class="py-1.5 px-2 text-right font-mono text-slate-400">${r.stop_count || 0}x</td>
                                                            <td class="py-1.5 px-2 text-right font-mono text-cyan-400 font-semibold">${r.cumulative_percentage || 0}%</td>
                                                        </tr>
                                                    `).join('')}
                                                </tbody>
                                            </table>
                                        </div>
                                    </div>

                                    <!-- DEFECT CAUSES TABLE -->
                                    <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-900/80 border-slate-800'} border rounded-xl p-4 space-y-2">
                                        <h5 class="font-bold text-xs ${isLight ? 'text-slate-800' : 'text-slate-200'} flex items-center justify-between">
                                            <span class="flex items-center gap-1.5">
                                                <i data-lucide="shield-alert" class="w-3.5 h-3.5 text-purple-400"></i>
                                                <span>${isJa ? '主要不良項目 (Top Defect Categories)' : 'Kategori Defect / Cacat Terbesar'}</span>
                                            </span>
                                            <span class="text-[10px] font-mono font-normal text-slate-400">${defPareto.length} categories</span>
                                        </h5>

                                        <div class="overflow-x-auto max-h-48 custom-scrollbar">
                                            <table class="w-full text-left text-[11px]">
                                                <thead>
                                                    <tr class="border-b ${isLight ? 'border-slate-200 text-slate-500' : 'border-slate-800 text-slate-400'} font-mono">
                                                        <th class="py-1 px-2">Kategori Defect</th>
                                                        <th class="py-1 px-2 text-right">Reject Qty</th>
                                                        <th class="py-1 px-2 text-right">% Defect</th>
                                                    </tr>
                                                </thead>
                                                <tbody class="divide-y ${isLight ? 'divide-slate-200/60' : 'divide-slate-800/60'}">
                                                    ${defPareto.length === 0 ? `
                                                        <tr>
                                                            <td colspan="3" class="text-center py-4 text-slate-500 font-mono">Tidak ada catatan defect pada filter ini</td>
                                                        </tr>
                                                    ` : defPareto.slice(0, 8).map(d => `
                                                        <tr class="${isLight ? 'hover:bg-slate-100' : 'hover:bg-slate-800/40'}">
                                                            <td class="py-1.5 px-2 font-medium ${isLight ? 'text-slate-800' : 'text-slate-200'} truncate max-w-[200px]">${d.category || d.defect_name || '-'}</td>
                                                            <td class="py-1.5 px-2 text-right font-mono font-bold text-purple-400">${(d.count || d.quantity || 0).toLocaleString()} pcs</td>
                                                            <td class="py-1.5 px-2 text-right font-mono text-cyan-400 font-semibold">${d.percentage || d.cumulative_percentage || 0}%</td>
                                                        </tr>
                                                    `).join('')}
                                                </tbody>
                                            </table>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- 4. MATHEMATICAL REFERENCE BOX -->
                            <div class="${isLight ? 'bg-cyan-50/70 border-cyan-200 text-slate-800' : 'bg-cyan-950/20 border-cyan-800/40 text-slate-300'} p-3.5 rounded-xl border text-[11px] font-mono flex items-start gap-3">
                                <i data-lucide="info" class="w-4 h-4 text-cyan-400 flex-shrink-0 mt-0.5"></i>
                                <div>
                                    <div class="font-bold text-cyan-400 font-sans mb-1">${isJa ? '📐 OEE 総合設備効率 計算構造式' : '📐 Formula Standar OEE (Overall Equipment Effectiveness)'}</div>
                                    <div class="text-[10.5px] leading-relaxed">
                                        <strong>OEE (%) = Availability (A) &times; Performance (P) &times; Quality (Q)</strong><br>
                                        &bull; <strong>Availability (A)</strong> = (Planned Time - Loss #1 - Loss #2) / Planned Time<br>
                                        &bull; <strong>Performance (P)</strong> = (Run Time - Loss #3 - Loss #4) / Run Time<br>
                                        &bull; <strong>Quality Rate (Q)</strong> = (Total Output - Loss #5 - Loss #6) / Total Output
                                    </div>
                                </div>
                            </div>

                        </div>

                        <!-- MODAL FOOTER -->
                        <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-[#070D1E] border-[#152347]'} px-6 py-3.5 border-t flex items-center justify-between flex-shrink-0">
                            <div class="text-[11px] ${isLight ? 'text-slate-500' : 'text-slate-400'} font-mono hidden sm:block">
                                Total TPM Losses: <strong class="text-rose-400">${totalHours} Jam (${totalMins} Menit)</strong>
                            </div>

                            <div class="flex items-center gap-2 w-full sm:w-auto justify-end">
                                <button type="button" id="btn-print-six-losses-summary" class="px-4 py-2 rounded-xl text-xs font-semibold ${isLight ? 'bg-slate-200 hover:bg-slate-300 text-slate-800' : 'bg-slate-800 hover:bg-slate-700 text-slate-200'} flex items-center gap-1.5 cursor-pointer transition-colors">
                                    <i data-lucide="printer" class="w-3.5 h-3.5 text-cyan-400"></i>
                                    <span>${isJa ? '印刷 (Print)' : 'Cetak Ringkasan'}</span>
                                </button>
                                <button type="button" id="btn-close-six-losses-footer" class="px-5 py-2 rounded-xl text-xs font-bold bg-cyan-600 hover:bg-cyan-500 text-white shadow-lg shadow-cyan-600/30 flex items-center gap-1.5 cursor-pointer transition-colors">
                                    <span>${isJa ? '閉じる (Tutup)' : 'Tutup Preview'}</span>
                                </button>
                            </div>
                        </div>

                    </div>
                </div>
            `;

            if (window.lucide) window.lucide.createIcons();
            if (this.currentLang === 'ja') {
                i18n.localizeDom(modalContainer);
            }

            // Bind Event Listeners
            const closeBtn = document.getElementById('btn-close-six-losses-modal');
            const closeFooterBtn = document.getElementById('btn-close-six-losses-footer');
            const printBtn = document.getElementById('btn-print-six-losses-summary');

            const closeModal = () => {
                modalContainer.innerHTML = '';
            };

            if (closeBtn) closeBtn.addEventListener('click', closeModal);
            if (closeFooterBtn) closeFooterBtn.addEventListener('click', closeModal);

            if (printBtn) {
                printBtn.addEventListener('click', () => {
                    window.print();
                });
            }

            // Close on backdrop click
            modalContainer.querySelector('.fixed.inset-0')?.addEventListener('click', (e) => {
                if (e.target === e.currentTarget) closeModal();
            });

            // ESC Key listener
            const escHandler = (e) => {
                if (e.key === 'Escape') {
                    closeModal();
                    document.removeEventListener('keydown', escHandler);
                }
            };
            document.addEventListener('keydown', escHandler);

        } catch (err) {
            console.error('Error rendering Six Big Losses detail modal:', err);
            this.showNotification('⚠️ Gagal Membuka Detail', err.message, 'delete');
            modalContainer.innerHTML = '';
        }
    }

    // ==========================================
    // 3. MACHINE PERFORMANCE PAGE
    // ==========================================
    // 3. MACHINE PERFORMANCE PAGE (PAGINATED)
    // ==========================================
    async renderMachines() {
        const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');
        const res = await api.getMachineRanking(this.filters);
        const allMachines = res.data.data || [];

        // Pagination & Filter States
        if (!this.machinePerPage) this.machinePerPage = 25;
        if (!this.machinePage) this.machinePage = 1;
        this.machineSearchQuery = this.machineSearchQuery || '';
        this.machineLineFilter = this.machineLineFilter || '';

        // Extract available lines for filtering
        const availableLines = Array.from(new Set(allMachines.map(m => m.line_name).filter(Boolean))).sort();

        // Apply Local Search & Line Filters
        let filteredMachines = allMachines;
        if (this.machineLineFilter) {
            filteredMachines = filteredMachines.filter(m => (m.line_name || '').toLowerCase() === this.machineLineFilter.toLowerCase());
        }
        if (this.machineSearchQuery) {
            const q = this.machineSearchQuery.toLowerCase();
            filteredMachines = filteredMachines.filter(m => 
                (m.code || '').toLowerCase().includes(q) ||
                (m.name || '').toLowerCase().includes(q) ||
                (m.line_name || '').toLowerCase().includes(q) ||
                (m.oee_status || '').toLowerCase().includes(q)
            );
        }

        const totalFiltered = filteredMachines.length;
        const totalAll = allMachines.length;
        const perPage = this.machinePerPage === 'all' ? (totalFiltered || 1) : parseInt(this.machinePerPage);
        const totalPages = Math.ceil(totalFiltered / perPage) || 1;

        // Ensure current page is valid
        if (this.machinePage > totalPages) this.machinePage = totalPages;
        if (this.machinePage < 1) this.machinePage = 1;

        const startIndex = totalFiltered === 0 ? 0 : (this.machinePage - 1) * perPage;
        const endIndex = Math.min(startIndex + perPage, totalFiltered);
        const displayedMachines = totalFiltered === 0 ? [] : filteredMachines.slice(startIndex, endIndex);

        // Summary KPI calculations
        const avgOee = allMachines.length > 0 ? (allMachines.reduce((acc, m) => acc + Number(m.oee || 0), 0) / allMachines.length).toFixed(1) : '0.0';
        const activeMachinesCount = allMachines.filter(m => (m.actual_quantity || 0) > 0 || m.status === 'RUNNING').length;

        const content = document.getElementById('content-body');
        content.innerHTML = `
            <!-- HEADER & SEARCH/FILTER BAR -->
            <div class="${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-2xl p-5 shadow-xl mb-5">
                <div class="flex flex-col md:flex-row items-start md:items-center justify-between gap-4">
                    <div>
                        <div class="flex items-center gap-3">
                            <h2 class="text-xl font-black ${isLight ? 'text-slate-900' : 'text-slate-100'} flex items-center gap-2">
                                <i data-lucide="cpu" class="w-6 h-6 text-cyan-400"></i>
                                <span>Machine Performance</span>
                            </h2>
                            <span class="px-2.5 py-0.5 rounded-full ${isLight ? 'bg-cyan-50 text-cyan-700 border-cyan-200' : 'bg-cyan-950/80 text-cyan-300 border-cyan-800/80'} border font-mono font-bold text-xs">
                                Total ${totalAll} Mesin
                            </span>
                        </div>
                        <p class="text-xs ${isLight ? 'text-slate-500' : 'text-slate-400'} mt-1">
                            Peringkat efisiensi OEE, ketersediaan mesin, kualitas, dan downtime per unit sub-asset.
                        </p>
                    </div>

                    <!-- SEARCH & FILTER CONTROLS -->
                    <div class="flex flex-wrap items-center gap-2.5 w-full md:w-auto">
                        <!-- SEARCH INPUT -->
                        <div class="relative flex-1 md:w-64">
                            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none text-slate-400">
                                <i data-lucide="search" class="w-4 h-4"></i>
                            </div>
                            <input 
                                type="text" 
                                id="machine-search-input" 
                                placeholder="Cari kode/nama mesin..." 
                                value="${this.machineSearchQuery}"
                                class="w-full ${isLight ? 'bg-slate-50 border-slate-300 text-slate-900' : 'bg-slate-950 border-slate-800 text-slate-100'} border rounded-xl pl-9 pr-3 py-2 text-xs font-sans outline-none focus:border-cyan-500 transition-all"
                            />
                        </div>

                        <!-- LINE FILTER SELECT -->
                        <select id="machine-line-select" class="${isLight ? 'bg-slate-50 border-slate-300 text-slate-900' : 'bg-slate-950 border-slate-800 text-slate-100'} border rounded-xl px-3 py-2 text-xs font-sans outline-none focus:border-cyan-500 cursor-pointer">
                            <option value="">Semua Lini (${allMachines.length})</option>
                            ${availableLines.map(l => `<option value="${l}" ${this.machineLineFilter === l ? 'selected' : ''}>Lini: ${l}</option>`).join('')}
                        </select>

                        <!-- ROWS PER PAGE SELECT -->
                        <select id="machine-per-page-select" class="${isLight ? 'bg-slate-50 border-slate-300 text-slate-900' : 'bg-slate-950 border-slate-800 text-slate-100'} border rounded-xl px-3 py-2 text-xs font-sans font-mono outline-none focus:border-cyan-500 cursor-pointer">
                            <option value="25" ${this.machinePerPage == 25 ? 'selected' : ''}>25 / halaman</option>
                            <option value="50" ${this.machinePerPage == 50 ? 'selected' : ''}>50 / halaman</option>
                            <option value="100" ${this.machinePerPage == 100 ? 'selected' : ''}>100 / halaman</option>
                            <option value="all" ${this.machinePerPage === 'all' ? 'selected' : ''}>Tampilkan Semua</option>
                        </select>
                    </div>
                </div>
            </div>

            <!-- MAIN TABLE CARD -->
            <div class="${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-2xl shadow-xl overflow-hidden mb-4">
                <div class="overflow-x-auto">
                    <table class="w-full text-left text-xs border-collapse font-sans">
                        <thead class="${isLight ? 'bg-slate-100 text-slate-700' : 'bg-slate-950 text-slate-400'} uppercase font-semibold text-[10px] border-b ${isLight ? 'border-slate-200' : 'border-slate-800'}">
                            <tr>
                                <th class="py-3 px-3.5 text-center" style="width: 45px;">No</th>
                                <th class="py-3 px-3.5">Code</th>
                                <th class="py-3 px-3.5">Machine Name</th>
                                <th class="py-3 px-3.5">Line</th>
                                <th class="py-3 px-3.5 text-right">Target</th>
                                <th class="py-3 px-3.5 text-right">Actual</th>
                                <th class="py-3 px-3.5 text-right">Rejects</th>
                                <th class="py-3 px-3.5 text-right">Downtime</th>
                                <th class="py-3 px-3.5 text-right">Availability</th>
                                <th class="py-3 px-3.5 text-right">Performance</th>
                                <th class="py-3 px-3.5 text-right">Quality</th>
                                <th class="py-3 px-3.5 text-right">OEE</th>
                                <th class="py-3 px-3.5 text-center">Status</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y ${isLight ? 'divide-slate-200' : 'divide-slate-800/60'} font-mono">
                            ${displayedMachines.length === 0 ? `
                                <tr>
                                    <td colspan="13" class="text-center py-12 text-slate-500 font-sans">
                                        <div class="flex flex-col items-center gap-2">
                                            <i data-lucide="inbox" class="w-8 h-8 opacity-40"></i>
                                            <span>Tidak ada data mesin yang cocok dengan kriteria pencarian.</span>
                                        </div>
                                    </td>
                                </tr>
                            ` : displayedMachines.map((m, idx) => {
                                const rowNumber = startIndex + idx + 1;
                                const oeeNum = Number(m.oee || 0);
                                const oeeColor = oeeNum >= 85 ? 'text-emerald-400 font-extrabold' : (oeeNum >= 65 ? 'text-amber-400 font-bold' : (oeeNum > 0 ? 'text-rose-400 font-bold' : 'text-slate-500'));
                                return `
                                    <tr class="${isLight ? 'hover:bg-slate-50' : 'hover:bg-slate-800/40'} transition-colors">
                                        <td class="py-2.5 px-3.5 text-center text-slate-500 font-bold text-[11px]">${rowNumber}</td>
                                        <td class="py-2.5 px-3.5 font-bold text-cyan-400">${m.code}</td>
                                        <td class="py-2.5 px-3.5 ${isLight ? 'text-slate-900 font-semibold' : 'text-slate-200'} font-sans truncate max-w-[200px]" title="${m.name}">${m.name}</td>
                                        <td class="py-2.5 px-3.5 font-sans">
                                            <span class="px-2 py-0.5 rounded text-[10px] font-semibold ${isLight ? 'bg-slate-100 text-slate-700 border-slate-300' : 'bg-slate-800 text-slate-300 border-slate-700'} border">
                                                ${m.line_name || '-'}
                                            </span>
                                        </td>
                                        <td class="py-2.5 px-3.5 text-right text-slate-400">${(m.target_quantity || 0).toLocaleString()}</td>
                                        <td class="py-2.5 px-3.5 text-right font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'}">${(m.actual_quantity || 0).toLocaleString()}</td>
                                        <td class="py-2.5 px-3.5 text-right ${m.reject_quantity > 0 ? 'text-rose-400 font-bold' : 'text-slate-500'}">${(m.reject_quantity || 0).toLocaleString()}</td>
                                        <td class="py-2.5 px-3.5 text-right ${m.downtime_minutes > 0 ? 'text-amber-400 font-bold' : 'text-slate-500'}">${(m.downtime_minutes || 0)} m</td>
                                        <td class="py-2.5 px-3.5 text-right ${Number(m.availability) > 0 ? 'text-cyan-400' : 'text-slate-500'}">${m.availability}%</td>
                                        <td class="py-2.5 px-3.5 text-right ${Number(m.performance) > 0 ? 'text-amber-400' : 'text-slate-500'}">${m.performance}%</td>
                                        <td class="py-2.5 px-3.5 text-right ${Number(m.quality) > 0 ? 'text-emerald-400' : 'text-slate-500'}">${m.quality}%</td>
                                        <td class="py-2.5 px-3.5 text-right ${oeeColor} ${isLight ? 'bg-cyan-50/50' : 'bg-cyan-950/20'}">${m.oee}%</td>
                                        <td class="py-2.5 px-3.5 text-center font-sans">
                                            <span class="inline-block px-2.5 py-0.5 rounded text-[10px] font-bold border ${this.getStatusBadge(m.oee_status)}">
                                                ${m.oee_status || 'CRITICAL'}
                                            </span>
                                        </td>
                                    </tr>
                                `;
                            }).join('')}
                        </tbody>
                    </table>
                </div>

                <!-- PAGINATION FOOTER & ROW COUNT DETAILS -->
                <div class="flex flex-col sm:flex-row items-center justify-between gap-4 p-4 border-t ${isLight ? 'border-slate-200 bg-slate-50/80' : 'border-slate-800 bg-slate-950/70'}">
                    <!-- DETAIL ROW COUNT INFO -->
                    <div class="text-xs ${isLight ? 'text-slate-600' : 'text-slate-400'} flex items-center gap-1 font-sans">
                        <span>Menampilkan</span>
                        <span class="font-mono font-bold text-cyan-400">${totalFiltered === 0 ? 0 : startIndex + 1} - ${endIndex}</span>
                        <span>dari total</span>
                        <span class="font-mono font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'}">${totalFiltered}</span>
                        <span>baris data mesin</span>
                        ${totalFiltered !== totalAll ? `<span class="text-[11px] text-slate-500 ml-1">(difilter dari total ${totalAll} mesin)</span>` : ''}
                    </div>

                    <!-- PAGINATION BUTTON CONTROLS -->
                    <div class="flex items-center gap-1.5 text-xs font-sans">
                        <!-- FIRST PAGE -->
                        <button 
                            id="btn-mach-page-first" 
                            class="px-2.5 py-1.5 rounded-lg border ${this.machinePage === 1 ? 'opacity-40 cursor-not-allowed text-slate-500 border-slate-800' : 'cursor-pointer hover:bg-slate-800 text-slate-300 border-slate-700 transition-all'}"
                            title="Halaman Pertama"
                            ${this.machinePage === 1 ? 'disabled' : ''}
                        >
                            «
                        </button>

                        <!-- PREV PAGE -->
                        <button 
                            id="btn-mach-page-prev" 
                            class="px-3 py-1.5 rounded-lg border ${this.machinePage === 1 ? 'opacity-40 cursor-not-allowed text-slate-500 border-slate-800' : 'cursor-pointer hover:bg-slate-800 text-slate-300 border-slate-700 transition-all'}"
                            title="Halaman Sebelumnya"
                            ${this.machinePage === 1 ? 'disabled' : ''}
                        >
                            ‹ Prev
                        </button>

                        <!-- PAGE NUMBERS (SMART WINDOW) -->
                        <div class="flex items-center gap-1 font-mono">
                            ${(() => {
                                const pages = [];
                                const maxButtons = 5;
                                let start = Math.max(1, this.machinePage - Math.floor(maxButtons / 2));
                                let end = Math.min(totalPages, start + maxButtons - 1);
                                if (end - start + 1 < maxButtons) {
                                    start = Math.max(1, end - maxButtons + 1);
                                }

                                if (start > 1) {
                                    pages.push(`<button class="btn-mach-page-num w-8 h-8 rounded-lg border border-slate-800 hover:bg-slate-800 text-slate-400 cursor-pointer text-xs flex items-center justify-center transition-all" data-page="1">1</button>`);
                                    if (start > 2) pages.push(`<span class="text-slate-500 px-1">...</span>`);
                                }

                                for (let p = start; p <= end; p++) {
                                    const isActive = p === this.machinePage;
                                    pages.push(`
                                        <button 
                                            class="btn-mach-page-num w-8 h-8 rounded-lg border font-bold text-xs flex items-center justify-center transition-all cursor-pointer ${isActive ? 'bg-cyan-600 border-cyan-500 text-white shadow-md' : 'border-slate-800 hover:bg-slate-800 text-slate-300'}" 
                                            data-page="${p}"
                                        >
                                            ${p}
                                        </button>
                                    `);
                                }

                                if (end < totalPages) {
                                    if (end < totalPages - 1) pages.push(`<span class="text-slate-500 px-1">...</span>`);
                                    pages.push(`<button class="btn-mach-page-num w-8 h-8 rounded-lg border border-slate-800 hover:bg-slate-800 text-slate-400 cursor-pointer text-xs flex items-center justify-center transition-all" data-page="${totalPages}">${totalPages}</button>`);
                                }

                                return pages.join('');
                            })()}
                        </div>

                        <!-- NEXT PAGE -->
                        <button 
                            id="btn-mach-page-next" 
                            class="px-3 py-1.5 rounded-lg border ${this.machinePage === totalPages ? 'opacity-40 cursor-not-allowed text-slate-500 border-slate-800' : 'cursor-pointer hover:bg-slate-800 text-slate-300 border-slate-700 transition-all'}"
                            title="Halaman Berikutnya"
                            ${this.machinePage === totalPages ? 'disabled' : ''}
                        >
                            Next ›
                        </button>

                        <!-- LAST PAGE -->
                        <button 
                            id="btn-mach-page-last" 
                            class="px-2.5 py-1.5 rounded-lg border ${this.machinePage === totalPages ? 'opacity-40 cursor-not-allowed text-slate-500 border-slate-800' : 'cursor-pointer hover:bg-slate-800 text-slate-300 border-slate-700 transition-all'}"
                            title="Halaman Terakhir"
                            ${this.machinePage === totalPages ? 'disabled' : ''}
                        >
                            »
                        </button>
                    </div>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();

        // 1. Search Input Event (Debounced / on input)
        const searchInput = document.getElementById('machine-search-input');
        if (searchInput) {
            searchInput.addEventListener('input', (e) => {
                this.machineSearchQuery = e.target.value;
                this.machinePage = 1;
                this.renderMachines();
            });
        }

        // 2. Line Filter Select Event
        const lineSelect = document.getElementById('machine-line-select');
        if (lineSelect) {
            lineSelect.addEventListener('change', (e) => {
                this.machineLineFilter = e.target.value;
                this.machinePage = 1;
                this.renderMachines();
            });
        }

        // 3. Per-Page Select Event
        const perPageSelect = document.getElementById('machine-per-page-select');
        if (perPageSelect) {
            perPageSelect.addEventListener('change', (e) => {
                this.machinePerPage = e.target.value === 'all' ? 'all' : parseInt(e.target.value);
                this.machinePage = 1;
                this.renderMachines();
            });
        }

        // 4. Pagination Button Handlers
        document.getElementById('btn-mach-page-first')?.addEventListener('click', () => {
            if (this.machinePage > 1) {
                this.machinePage = 1;
                this.renderMachines();
            }
        });

        document.getElementById('btn-mach-page-prev')?.addEventListener('click', () => {
            if (this.machinePage > 1) {
                this.machinePage--;
                this.renderMachines();
            }
        });

        document.getElementById('btn-mach-page-next')?.addEventListener('click', () => {
            if (this.machinePage < totalPages) {
                this.machinePage++;
                this.renderMachines();
            }
        });

        document.getElementById('btn-mach-page-last')?.addEventListener('click', () => {
            if (this.machinePage < totalPages) {
                this.machinePage = totalPages;
                this.renderMachines();
            }
        });

        document.querySelectorAll('.btn-mach-page-num').forEach(btn => {
            btn.addEventListener('click', (e) => {
                const targetPage = parseInt(e.currentTarget.getAttribute('data-page'));
                if (targetPage && targetPage !== this.machinePage) {
                    this.machinePage = targetPage;
                    this.renderMachines();
                }
            });
        });

        if (this.currentLang === 'ja') {
            i18n.localizeDom(document.getElementById('content-body') || document.body);
        }
    }

    // ==========================================
    // 3B. DAILY PRODUCTION & TROUBLE REPORT PAGE
    // ==========================================
    async renderDailyReport() {
        const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');
        if (!this.selectedDailyDate) {
            this.selectedDailyDate = new Date().toISOString().slice(0, 10);
        }

        const queryParams = {
            date: this.selectedDailyDate,
            per_page: 100
        };
        if (this.filters.line_id) queryParams.line_id = this.filters.line_id;
        if (this.filters.machine_id) queryParams.machine_id = this.filters.machine_id;
        if (this.filters.shift_id) queryParams.shift_id = this.filters.shift_id;

        const [prodRes, dtRes] = await Promise.all([
            api.getProductionRecords(queryParams),
            api.getDowntimes(queryParams),
        ]);

        const records = prodRes.data.data || [];
        const downtimes = dtRes.data.data || [];
        this.currentDailyRecords = records;

        const activeLine = (this.masterData.lines || []).find(l => l.id == this.filters.line_id);
        const activeMachine = (this.masterData.machines || []).find(m => m.id == this.filters.machine_id);
        const activeShift = (this.masterData.shifts || []).find(s => s.id == this.filters.shift_id);
        const hasActiveFilters = !!(this.filters.line_id || this.filters.machine_id || this.filters.shift_id);

        // Daily Summary Metrics
        const totalTarget = records.reduce((s, r) => s + parseInt(r.target_quantity || 0), 0);
        const totalMeasuring = records.reduce((s, r) => s + parseInt(r.total_quantity || 0), 0);
        const totalGood = records.reduce((s, r) => s + parseInt(r.good_quantity || 0), 0);
        const totalReject = records.reduce((s, r) => s + parseInt(r.reject_quantity || 0), 0);
        const totalDowntimeMins = downtimes.reduce((s, d) => s + (d.calculated_duration_minutes || d.duration_minutes || 0), 0);

        const rejectRate = totalMeasuring > 0 ? ((totalReject / totalMeasuring) * 100).toFixed(1) : '0.0';
        const avgOee = records.length > 0
            ? (records.reduce((s, r) => s + (r.oee_record?.oee || 0), 0) / records.length).toFixed(1)
            : '0.0';

        const allMasterLines = (this.masterData.lines && this.masterData.lines.length > 0) ? this.masterData.lines : [];
        const totalMasterLineCount = allMasterLines.length || 11;
        
        // Count distinct production lines that have records today
        const submittedLineIds = new Set(records.map(r => r.production_line_id).filter(Boolean));
        const submittedLineCount = submittedLineIds.size;
        const missingLines = allMasterLines.filter(l => !submittedLineIds.has(l.id));
        const isAllLinesComplete = (totalMasterLineCount > 0 && submittedLineCount >= totalMasterLineCount);

        // Group records by production_line_id
        const lineGroupsMap = new Map();
        records.forEach(r => {
            const lineId = r.production_line_id || 0;
            const lineName = r.production_line?.name || (allMasterLines.find(l => l.id == lineId)?.name || `Line #${lineId}`);
            const lineCode = r.production_line?.code || (allMasterLines.find(l => l.id == lineId)?.code || '');
            if (!lineGroupsMap.has(lineId)) {
                lineGroupsMap.set(lineId, {
                    lineId,
                    lineName,
                    lineCode,
                    records: []
                });
            }
            lineGroupsMap.get(lineId).records.push(r);
        });
        const lineGroups = Array.from(lineGroupsMap.values());

        // Group downtimes by production_line_id
        const dtLineGroupsMap = new Map();
        downtimes.forEach(dt => {
            const lineId = dt.production_line_id || 0;
            const lineName = dt.production_line?.name || (allMasterLines.find(l => l.id == lineId)?.name || `Line #${lineId}`);
            const lineCode = dt.production_line?.code || (allMasterLines.find(l => l.id == lineId)?.code || '');
            if (!dtLineGroupsMap.has(lineId)) {
                dtLineGroupsMap.set(lineId, {
                    lineId,
                    lineName,
                    lineCode,
                    items: []
                });
            }
            dtLineGroupsMap.get(lineId).items.push(dt);
        });
        const dtLineGroups = Array.from(dtLineGroupsMap.values());

        const content = document.getElementById('content-body');
        content.innerHTML = `
            <!-- HEADER & DATE FILTER CONTROL -->
            <div class="flex flex-col gap-3 ${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-2xl p-5 shadow-xl">
                <div class="flex flex-col md:flex-row items-start md:items-center justify-between gap-4">
                    <div>
                        <h2 class="text-xl font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'} flex items-center gap-2">
                            <i data-lucide="clipboard-list" class="w-5 h-5 text-cyan-400"></i>
                            <span>Input Laporan Harian</span>
                        </h2>
                    </div>

                    <!-- DATE CONTROLLER -->
                    <div class="flex items-center gap-2 ${isLight ? 'bg-slate-100 border-slate-200' : 'bg-slate-950 border-slate-800'} border p-2 rounded-xl">
                        <button id="btn-daily-prev" class="px-2.5 py-1.5 ${isLight ? 'bg-white hover:bg-slate-200 text-slate-700 shadow-xs' : 'bg-slate-800 hover:bg-slate-700 text-slate-200'} rounded-lg text-xs font-semibold cursor-pointer transition-all flex items-center gap-1">
                            <i data-lucide="chevron-left" class="w-3.5 h-3.5"></i> Prev
                        </button>

                        <div class="flex items-center gap-1.5 px-2">
                            <i data-lucide="calendar" class="w-4 h-4 text-cyan-400"></i>
                            <input type="date" id="input-daily-date" value="${this.selectedDailyDate}" class="${isLight ? 'bg-white border-slate-300 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-1 text-xs font-mono font-bold focus:outline-none focus:border-cyan-500" />
                        </div>

                        <button id="btn-daily-today" class="px-3 py-1.5 bg-cyan-600/20 hover:bg-cyan-600/30 text-cyan-500 border border-cyan-500/30 rounded-lg text-xs font-bold cursor-pointer transition-all">
                            Hari Ini
                        </button>

                        <button id="btn-daily-next" class="px-2.5 py-1.5 ${isLight ? 'bg-white hover:bg-slate-200 text-slate-700 shadow-xs' : 'bg-slate-800 hover:bg-slate-700 text-slate-200'} rounded-lg text-xs font-semibold cursor-pointer transition-all flex items-center gap-1">
                            Next <i data-lucide="chevron-right" class="w-3.5 h-3.5"></i>
                        </button>
                    </div>
                </div>

                ${hasActiveFilters ? `
                    <div class="flex flex-wrap items-center gap-2 pt-2 border-t ${isLight ? 'border-slate-200' : 'border-slate-800/80'} text-xs">
                        <span class="text-[11px] text-slate-400 font-semibold flex items-center gap-1.5">
                            <i data-lucide="filter" class="w-3.5 h-3.5 text-cyan-400"></i> Filter Aktif:
                        </span>
                        ${activeLine ? `<span class="px-2 py-0.5 rounded-md ${isLight ? 'bg-cyan-50 text-cyan-700 border-cyan-200' : 'bg-cyan-950 text-cyan-300 border-cyan-800'} border font-mono text-[11px] flex items-center gap-1"><span>Lini:</span> <strong>${activeLine.name}</strong></span>` : ''}
                        ${activeMachine ? `<span class="px-2 py-0.5 rounded-md ${isLight ? 'bg-emerald-50 text-emerald-700 border-emerald-200' : 'bg-emerald-950 text-emerald-300 border-emerald-800'} border font-mono text-[11px] flex items-center gap-1"><span>Mesin:</span> <strong>${activeMachine.name}</strong></span>` : ''}
                        ${activeShift ? `<span class="px-2 py-0.5 rounded-md ${isLight ? 'bg-amber-50 text-amber-700 border-amber-200' : 'bg-amber-950 text-amber-300 border-amber-800'} border font-mono text-[11px] flex items-center gap-1"><span>Shift:</span> <strong>${activeShift.name}</strong></span>` : ''}
                        <button id="btn-clear-daily-filters" class="text-[11px] px-2 py-0.5 rounded bg-rose-950/60 text-rose-400 hover:bg-rose-900/80 border border-rose-800/60 font-medium cursor-pointer transition-all flex items-center gap-1 ml-1">
                            <i data-lucide="x" class="w-3 h-3"></i> Reset Filter
                        </button>
                    </div>
                ` : ''}
            </div>

            <!-- DYNAMIC LINE INPUT COMPLETENESS ALERT BANNER -->
            <div class="${isAllLinesComplete 
                ? (isLight ? 'bg-emerald-50 border-emerald-300 text-emerald-950' : 'bg-emerald-950/40 border-emerald-700/70 text-emerald-200') 
                : (isLight ? 'bg-amber-50 border-amber-300 text-amber-950' : 'bg-amber-950/40 border-amber-700/70 text-amber-200')} border-2 rounded-2xl p-4 shadow-lg flex flex-col md:flex-row items-start md:items-center justify-between gap-3 transition-all">
                <div class="flex items-center gap-3">
                    <div class="w-10 h-10 rounded-xl ${isAllLinesComplete 
                        ? (isLight ? 'bg-emerald-200 text-emerald-800' : 'bg-emerald-900/60 text-emerald-400 border border-emerald-700/60') 
                        : (isLight ? 'bg-amber-200 text-amber-800' : 'bg-amber-900/60 text-amber-400 border border-amber-700/60')} flex items-center justify-center shrink-0">
                        <i data-lucide="${isAllLinesComplete ? 'check-circle-2' : 'alert-triangle'}" class="w-5 h-5 ${isAllLinesComplete ? '' : 'animate-pulse'}"></i>
                    </div>
                    <div>
                        <div class="text-sm font-bold flex flex-wrap items-center gap-2">
                            <span>Data Harian Line Masuk:</span>
                            <span class="px-2.5 py-0.5 rounded-full font-mono text-xs font-black ${isAllLinesComplete 
                                ? (isLight ? 'bg-emerald-200 text-emerald-900' : 'bg-emerald-800 text-emerald-100') 
                                : (isLight ? 'bg-amber-200 text-amber-900' : 'bg-amber-800 text-amber-100')}">
                                ${submittedLineCount} dari ${totalMasterLineCount} Line
                            </span>
                            <span class="text-xs font-bold ${isAllLinesComplete ? 'text-emerald-400' : 'text-amber-400'}">
                                ${isAllLinesComplete ? '✓ (Lengkap 100%)' : `⚠️ (Kurang ${missingLines.length} Line)`}
                            </span>
                        </div>
                        <p class="text-xs ${isLight ? (isAllLinesComplete ? 'text-emerald-800' : 'text-amber-800') : (isAllLinesComplete ? 'text-emerald-300/90' : 'text-amber-300/90')} mt-0.5">
                            ${isAllLinesComplete 
                                ? `Seluruh <strong>${totalMasterLineCount} lini produksi</strong> telah berhasil menginput data hasil produksi harian.` 
                                : `Lini yang <strong>belum mengisi data (${missingLines.length} Line):</strong> <span class="font-bold ${isLight ? 'text-rose-700' : 'text-rose-400'}">${missingLines.map(l => l.name).join(', ') || 'Semua Line Belum Input'}</span>`}
                        </p>
                    </div>
                </div>
                <div class="flex items-center gap-2 self-end md:self-auto shrink-0">
                    ${!isAllLinesComplete ? `
                        <button id="btn-quick-fill-missing" class="px-3.5 py-1.5 ${isLight ? 'bg-amber-600 hover:bg-amber-700 text-white' : 'bg-amber-500 hover:bg-amber-400 text-slate-950'} font-bold rounded-xl text-xs flex items-center gap-1.5 cursor-pointer shadow-md transition-all">
                            <i data-lucide="plus-circle" class="w-3.5 h-3.5"></i>
                            <span>+ Input Laporan Line</span>
                        </button>
                    ` : `
                        <span class="px-3 py-1 rounded-xl bg-emerald-500/20 text-emerald-400 border border-emerald-500/40 text-xs font-bold flex items-center gap-1">
                            <i data-lucide="shield-check" class="w-3.5 h-3.5"></i> Terverifikasi Lengkap
                        </span>
                    `}
                </div>
            </div>

            <!-- DAILY KPI SUMMARY CARDS -->
            <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
                <div class="${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-xl p-4 shadow-xl">
                    <div class="flex items-center justify-between ${isLight ? 'text-slate-500' : 'text-slate-400'} text-xs mb-1">
                        <span>Finish Good (Units)</span>
                        <i data-lucide="check-circle" class="w-4 h-4 text-emerald-400"></i>
                    </div>
                    <div class="text-2xl font-bold font-mono text-emerald-400">${totalGood.toLocaleString()}</div>
                    <div class="text-[10px] ${isLight ? 'text-slate-500' : 'text-slate-400'} mt-1">Target: ${totalTarget.toLocaleString()} pcs</div>
                </div>

                <div class="${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-xl p-4 shadow-xl">
                    <div class="flex items-center justify-between ${isLight ? 'text-slate-500' : 'text-slate-400'} text-xs mb-1">
                        <span>Not Good (NG Qty)</span>
                        <i data-lucide="x-circle" class="w-4 h-4 text-rose-400"></i>
                    </div>
                    <div class="text-2xl font-bold font-mono text-rose-400">${totalReject.toLocaleString()}</div>
                    <div class="text-[10px] ${isLight ? 'text-slate-500' : 'text-slate-400'} mt-1">Reject Rate: <span class="text-rose-400 font-bold font-mono">${rejectRate}%</span></div>
                </div>

                <div class="${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-xl p-4 shadow-xl">
                    <div class="flex items-center justify-between ${isLight ? 'text-slate-500' : 'text-slate-400'} text-xs mb-1">
                        <span>OEE Rata-Rata Harian</span>
                        <i data-lucide="gauge" class="w-4 h-4 text-cyan-400"></i>
                    </div>
                    <div class="text-2xl font-bold font-mono text-cyan-400">${avgOee}%</div>
                    <div class="text-[10px] ${isLight ? 'text-slate-500' : 'text-slate-400'} mt-1">${records.length} entry record terfilter (${lineGroups.length} Line)</div>
                </div>

                <div class="${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-xl p-4 shadow-xl">
                    <div class="flex items-center justify-between ${isLight ? 'text-slate-500' : 'text-slate-400'} text-xs mb-1">
                        <span>Total Downtime Harian</span>
                        <i data-lucide="alert-triangle" class="w-4 h-4 text-amber-400"></i>
                    </div>
                    <div class="text-[10px] ${isLight ? 'text-slate-500' : 'text-slate-400'} mt-1">${downtimes.length} trouble stop event</div>
                </div>
            </div>

            <div class="space-y-6">
                <!-- TABEL DATA PRODUKSI HARIAN (GROUPED BY LINE & COLLAPSIBLE) -->
                <div class="${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-2xl p-5 shadow-xl">
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between gap-4 mb-4 pb-3 border-b ${isLight ? 'border-slate-100' : 'border-slate-800'}">
                        <div>
                            <h3 class="text-sm font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'} flex items-center gap-2">
                                <i data-lucide="layers" class="w-4 h-4 text-cyan-400"></i>
                                List Data Produksi — Tanggal ${this.selectedDailyDate}
                            </h3>
                        </div>
                        <div class="flex flex-wrap items-center gap-2">
                            <!-- EXPAND / COLLAPSE ALL BUTTONS -->
                            <div class="flex items-center gap-1 ${isLight ? 'bg-slate-100 border-slate-300' : 'bg-slate-950 border-slate-800'} border rounded-xl p-1">
                                <button id="btn-expand-all-lines" title="Buka seluruh rincian shift semua line" class="px-2.5 py-1 text-[11px] font-bold ${isLight ? 'text-slate-700 hover:bg-white' : 'text-slate-300 hover:bg-slate-800'} rounded-lg transition-all flex items-center gap-1 cursor-pointer">
                                    <i data-lucide="chevrons-down" class="w-3.5 h-3.5 text-cyan-400"></i> Expand All
                                </button>
                                <button id="btn-collapse-all-lines" title="Tutup rincian shift (hanya tampilkan ringkasan Line)" class="px-2.5 py-1 text-[11px] font-bold ${isLight ? 'text-slate-700 hover:bg-white' : 'text-slate-300 hover:bg-slate-800'} rounded-lg transition-all flex items-center gap-1 cursor-pointer">
                                    <i data-lucide="chevrons-up" class="w-3.5 h-3.5 text-slate-400"></i> Collapse All
                                </button>
                            </div>

                            <span class="px-3 py-1 rounded-full text-xs font-mono font-bold ${isLight ? 'bg-slate-100 text-cyan-700 border-slate-300' : 'bg-slate-950 text-cyan-400 border-slate-800'} border">
                                ${this.selectedDailyDate}
                            </span>
                            <!-- PRIMARY ACTION BUTTON TO OPEN MODAL -->
                            <button id="btn-open-create-prod-modal" class="px-4 py-2 bg-emerald-600 hover:bg-emerald-500 text-white font-bold rounded-xl shadow-lg shadow-emerald-600/30 flex items-center gap-2 cursor-pointer transition-all text-xs">
                                <i data-lucide="plus-circle" class="w-4 h-4"></i>
                                <span>+ Input Laporan Produksi</span>
                            </button>
                        </div>
                    </div>

                    <div class="overflow-x-auto">
                        <table class="w-full text-left text-xs">
                            <thead class="${isLight ? 'bg-slate-100 text-slate-700' : 'bg-slate-950 text-slate-400'} uppercase font-semibold text-[10px] border-b ${isLight ? 'border-slate-200' : 'border-slate-800'}">
                                <tr>
                                    <th class="p-3 text-center w-12">No</th>
                                    <th class="p-3">Production Line</th>
                                    <th class="p-3">Product Name</th>
                                    <th class="p-3">Shift</th>
                                    <th class="p-3">Target</th>
                                    <th class="p-3">Measuring</th>
                                    <th class="p-3">Finish Good</th>
                                    <th class="p-3">NG Qty</th>
                                    <th class="p-3">Reject Rate</th>
                                    <th class="p-3">OEE Score</th>
                                    <th class="p-3">Status</th>
                                    <th class="p-3 text-center">Aksi / Rincian</th>
                                </tr>
                            </thead>
                            <tbody class="divide-y ${isLight ? 'divide-slate-200' : 'divide-slate-800/60'} font-mono">
                                ${lineGroups.length === 0 ? `
                                    <tr>
                                        <td colspan="12" class="p-10 text-center text-slate-500 font-sans">
                                            <div class="flex flex-col items-center gap-2">
                                                <i data-lucide="inbox" class="w-8 h-8 opacity-40"></i>
                                                <span>Belum ada laporan produksi di-input pada tanggal <strong>${this.selectedDailyDate}</strong>.</span>
                                                <button id="btn-empty-open-create-prod" class="mt-2 px-4 py-2 bg-emerald-600/20 hover:bg-emerald-600/30 text-emerald-400 border border-emerald-500/40 rounded-xl text-xs font-bold flex items-center gap-1.5 cursor-pointer transition-all">
                                                    <i data-lucide="plus" class="w-3.5 h-3.5"></i>
                                                    <span>Klik untuk Input Laporan Produksi</span>
                                                </button>
                                            </div>
                                        </td>
                                    </tr>
                                ` : lineGroups.map((grp, gIdx) => {
                                    const grpTarget = grp.records.reduce((s, r) => s + parseInt(r.target_quantity || 0), 0);
                                    const grpMeasuring = grp.records.reduce((s, r) => s + parseInt(r.total_quantity || 0), 0);
                                    const grpGood = grp.records.reduce((s, r) => s + parseInt(r.good_quantity || 0), 0);
                                    const grpReject = grp.records.reduce((s, r) => s + parseInt(r.reject_quantity || 0), 0);
                                    const grpRejectRate = grpMeasuring > 0 ? ((grpReject / grpMeasuring) * 100).toFixed(1) : '0.0';
                                    const grpAvgOee = (grp.records.reduce((s, r) => s + (r.oee_record?.oee || 0), 0) / grp.records.length).toFixed(1);
                                    const uniqueShifts = [...new Set(grp.records.map(r => r.shift?.name || 'Shift 1'))].join(', ');
                                    const uniqueProducts = [...new Set(grp.records.map(r => r.product ? `${r.product.name}` : '-'))].join('; ');

                                    return `
                                        <!-- LINE GROUP SUMMARY HEADER ROW (CLICKABLE COLLAPSIBLE) -->
                                        <tr class="line-group-header cursor-pointer select-none ${isLight ? 'bg-slate-100 hover:bg-slate-200/90 border-slate-300 text-slate-900' : 'bg-slate-950 hover:bg-slate-800/90 border-slate-800 text-slate-100'} border-t-2 border-b transition-colors" data-toggle-line="${grp.lineId}">
                                            <td class="p-3 text-center">
                                                <button type="button" class="w-6 h-6 rounded-lg ${isLight ? 'bg-white text-slate-700 border-slate-300' : 'bg-slate-900 text-slate-300 border-slate-700'} border flex items-center justify-center transition-transform cursor-pointer shadow-xs">
                                                    <i data-lucide="chevron-down" class="w-4 h-4 chevron-icon-${grp.lineId} transition-transform duration-200"></i>
                                                </button>
                                            </td>
                                            <td class="p-3 font-sans">
                                                <div class="flex items-center gap-2">
                                                    <span class="px-2.5 py-1 rounded-md ${this.getLineBadgeStyle(grp.lineName || grp.lineId, isLight)} border font-bold text-xs flex items-center gap-1 font-mono shadow-xs">
                                                        🏭 ${grp.lineName}
                                                    </span>
                                                    <span class="px-2 py-0.5 rounded-full text-[10px] font-bold ${isLight ? 'bg-slate-200 text-slate-700' : 'bg-slate-800 text-slate-300'} font-sans">
                                                        ${grp.records.length} Shift / Batch
                                                    </span>
                                                </div>
                                                ${grp.lineCode ? `<div class="text-[10px] text-slate-400 font-mono mt-0.5">${grp.lineCode}</div>` : ''}
                                            </td>
                                            <td class="p-3 font-sans text-xs">
                                                <div class="font-bold ${isLight ? 'text-slate-800' : 'text-slate-200'} truncate max-w-[200px]" title="${uniqueProducts}">
                                                    ${uniqueProducts}
                                                </div>
                                                <div class="text-[10px] text-slate-400">Total ${grp.records.length} batch / shift</div>
                                            </td>
                                            <td class="p-3 font-sans">
                                                <span class="px-2 py-0.5 rounded text-[10px] font-bold ${isLight ? 'bg-amber-100 text-amber-800 border-amber-300' : 'bg-amber-950 text-amber-300 border-amber-800'} border">
                                                    ${uniqueShifts}
                                                </span>
                                            </td>
                                            <td class="p-3 font-mono font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'}">${grpTarget.toLocaleString()}</td>
                                            <td class="p-3 font-mono font-bold text-cyan-400">${grpMeasuring.toLocaleString()}</td>
                                            <td class="p-3 font-mono font-bold text-emerald-400">${grpGood.toLocaleString()}</td>
                                            <td class="p-3 font-mono font-bold text-rose-400">${grpReject.toLocaleString()}</td>
                                            <td class="p-3 font-sans font-bold ${grpMeasuring > 0 && (grpReject/grpMeasuring) > 0.05 ? 'text-rose-400' : (isLight ? 'text-slate-700' : 'text-slate-300')}">
                                                ${grpRejectRate}%
                                            </td>
                                            <td class="p-3 font-bold font-mono text-cyan-300 ${isLight ? 'bg-cyan-100/50' : 'bg-cyan-950/40'}">
                                                ${grpAvgOee}%
                                            </td>
                                            <td class="p-3 font-sans">
                                                <span class="px-2 py-0.5 rounded text-[10px] font-bold border ${this.getStatusBadge(parseFloat(grpAvgOee) >= 85 ? 'EXCELLENT' : (parseFloat(grpAvgOee) >= 70 ? 'GOOD' : 'CRITICAL'))}">
                                                    ${parseFloat(grpAvgOee) >= 85 ? 'EXCELLENT' : (parseFloat(grpAvgOee) >= 70 ? 'GOOD' : 'CRITICAL')}
                                                </span>
                                            </td>
                                            <td class="p-3 text-center font-sans">
                                                <span class="text-[10px] font-semibold text-slate-400 flex items-center justify-center gap-1">
                                                    <span>Toggle Shift</span>
                                                    <i data-lucide="chevron-right" class="w-3 h-3"></i>
                                                </span>
                                            </td>
                                        </tr>

                                        <!-- EXPANDABLE CHILD ROWS FOR THIS LINE -->
                                        ${grp.records.map((r, rIdx) => `
                                            <tr class="line-row-${grp.lineId} ${isLight ? 'bg-white hover:bg-slate-50' : 'bg-slate-900/70 hover:bg-slate-800/60'} transition-colors border-b ${isLight ? 'border-slate-200/60' : 'border-slate-800/50'}">
                                                <td class="p-3 text-center text-xs font-mono ${isLight ? 'text-slate-400' : 'text-slate-500'}">
                                                    <span class="inline-block pl-2 text-[11px] text-slate-400 font-mono">${gIdx + 1}.${rIdx + 1}</span>
                                                </td>
                                                <td class="p-3 font-semibold font-sans">
                                                    <div class="flex items-center gap-1.5">
                                                        <span class="px-2 py-0.5 rounded text-[11px] font-bold ${this.getLineBadgeStyle(r.production_line?.name || r.production_line_id, isLight)} border font-mono">
                                                            ${r.production_line?.name || 'Line'}
                                                        </span>
                                                    </div>
                                                    <div class="text-[10px] text-slate-400 font-mono mt-0.5">${r.machine ? r.machine.code : ''}</div>
                                                </td>
                                                <td class="p-3 font-sans ${isLight ? 'text-slate-800' : 'text-slate-200'}">
                                                    <div class="font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'}">${r.product ? `${r.product.name} - ${r.product.sku}` : 'Product #' + r.product_id}</div>
                                                    <div class="text-[10px] ${isLight ? 'text-slate-500' : 'text-slate-400'}">Target: ${r.target_quantity ? r.target_quantity.toLocaleString() : 0} pcs</div>
                                                </td>
                                                <td class="p-3 font-sans">
                                                    <span class="px-2 py-0.5 rounded text-[10px] font-semibold ${isLight ? 'bg-slate-100 text-slate-700 border-slate-300' : 'bg-slate-800 text-slate-200 border-slate-700'} border">
                                                        ${r.shift ? r.shift.name : 'Shift #' + r.shift_id}
                                                    </span>
                                                </td>
                                                <td class="p-3 ${isLight ? 'text-slate-700' : 'text-slate-300'} font-bold font-mono">${r.target_quantity.toLocaleString()}</td>
                                                <td class="p-3 text-cyan-400 font-bold font-mono">${r.total_quantity.toLocaleString()}</td>
                                                <td class="p-3 text-emerald-400 font-bold font-mono">${r.good_quantity.toLocaleString()}</td>
                                                <td class="p-3 text-rose-400 font-bold font-mono">${r.reject_quantity.toLocaleString()}</td>
                                                <td class="p-3 font-sans font-bold font-mono ${r.total_quantity > 0 && (r.reject_quantity/r.total_quantity) > 0.05 ? 'text-rose-400' : (isLight ? 'text-slate-700' : 'text-slate-300')}">
                                                    ${r.total_quantity > 0 ? ((r.reject_quantity / r.total_quantity) * 100).toFixed(1) : 0}%
                                                </td>
                                                <td class="p-3 font-bold font-mono text-cyan-300 ${isLight ? 'bg-cyan-50/60' : 'bg-cyan-950/30'}">
                                                    ${r.oee_record ? r.oee_record.oee : 0}%
                                                </td>
                                                <td class="p-3 font-sans">
                                                    <span class="px-2 py-0.5 rounded text-[10px] font-semibold border ${this.getStatusBadge(r.oee_record?.oee >= 85 ? 'EXCELLENT' : (r.oee_record?.oee >= 70 ? 'GOOD' : 'CRITICAL'))}">
                                                        ${r.oee_record?.oee >= 85 ? 'EXCELLENT' : (r.oee_record?.oee >= 70 ? 'GOOD' : 'CRITICAL')}
                                                    </span>
                                                </td>
                                                <td class="p-3 text-center font-sans">
                                                    <div class="flex items-center justify-center gap-1.5">
                                                        <button data-action="view-prod-record" data-id="${r.id}" title="Lihat Detail Laporan Produksi" class="w-7 h-7 rounded-lg ${isLight ? 'bg-blue-50 hover:bg-blue-100 border-blue-200 text-blue-700' : 'bg-blue-950/70 hover:bg-blue-900 border border-blue-800/80 text-blue-400 hover:text-blue-200'} border flex items-center justify-center cursor-pointer transition-all shadow-sm hover:scale-110 active:scale-95">
                                                            <i data-lucide="eye" class="w-3.5 h-3.5"></i>
                                                        </button>
                                                        <button data-action="edit-prod-record" data-id="${r.id}" title="Edit Data Produksi" class="w-7 h-7 rounded-lg ${isLight ? 'bg-cyan-50 hover:bg-cyan-100 border-cyan-200 text-cyan-700' : 'bg-cyan-950/70 hover:bg-cyan-900 border border-cyan-800/80 text-cyan-400 hover:text-cyan-200'} border flex items-center justify-center cursor-pointer transition-all shadow-sm hover:scale-110 active:scale-95">
                                                            <i data-lucide="pencil" class="w-3.5 h-3.5"></i>
                                                        </button>
                                                        <button data-action="delete-prod-record" data-id="${r.id}" title="Hapus Data Produksi" class="w-7 h-7 rounded-lg ${isLight ? 'bg-rose-50 hover:bg-rose-100 border-rose-200 text-rose-700' : 'bg-rose-950/70 hover:bg-rose-900 border border-rose-800/80 text-rose-400 hover:text-rose-200'} border flex items-center justify-center cursor-pointer transition-all shadow-sm hover:scale-110 active:scale-95">
                                                            <i data-lucide="trash-2" class="w-3.5 h-3.5"></i>
                                                        </button>
                                                    </div>
                                                </td>
                                            </tr>
                                        `).join('')}
                                    `;
                                }).join('')}
                            </tbody>
                        </table>
                    </div>
                </div>

                <!-- TABEL PROBLEM & DOWNTIME HARIAN (GROUPED BY LINE & COLLAPSIBLE) -->
                <div class="${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-2xl p-5 shadow-xl">
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between gap-4 mb-4 pb-3 border-b ${isLight ? 'border-slate-100' : 'border-slate-800'}">
                        <div>
                            <h3 class="text-sm font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'} flex items-center gap-2">
                                <i data-lucide="alert-triangle" class="w-4 h-4 text-amber-400"></i>
                                Log Problem & Downtime — Tanggal ${this.selectedDailyDate}
                            </h3>
                        </div>
                        <div class="flex flex-wrap items-center gap-2">
                            <!-- EXPAND / COLLAPSE ALL BUTTONS FOR PROBLEM TABLE -->
                            <div class="flex items-center gap-1 ${isLight ? 'bg-slate-100 border-slate-300' : 'bg-slate-950 border-slate-800'} border rounded-xl p-1">
                                <button id="btn-expand-all-dt-lines" title="Buka seluruh problem semua line" class="px-2.5 py-1 text-[11px] font-bold ${isLight ? 'text-slate-700 hover:bg-white' : 'text-slate-300 hover:bg-slate-800'} rounded-lg transition-all flex items-center gap-1 cursor-pointer">
                                    <i data-lucide="chevrons-down" class="w-3.5 h-3.5 text-amber-400"></i> Expand All
                                </button>
                                <button id="btn-collapse-all-dt-lines" title="Tutup rincian problem (hanya tampilkan ringkasan Line)" class="px-2.5 py-1 text-[11px] font-bold ${isLight ? 'text-slate-700 hover:bg-white' : 'text-slate-300 hover:bg-slate-800'} rounded-lg transition-all flex items-center gap-1 cursor-pointer">
                                    <i data-lucide="chevrons-up" class="w-3.5 h-3.5 text-slate-400"></i> Collapse All
                                </button>
                            </div>

                            <button id="btn-open-create-dt-modal" class="px-3.5 py-1.5 bg-amber-600 hover:bg-amber-500 text-white font-bold rounded-xl shadow-md flex items-center gap-1.5 cursor-pointer transition-all text-xs">
                                <i data-lucide="plus" class="w-3.5 h-3.5"></i>
                                <span>+ Tambah Problem Log</span>
                            </button>
                        </div>
                    </div>

                    <div class="overflow-x-auto">
                        <table class="w-full text-left text-xs">
                            <thead class="${isLight ? 'bg-slate-100 text-slate-700' : 'bg-slate-950 text-slate-400'} uppercase font-semibold text-[10px] border-b ${isLight ? 'border-slate-200' : 'border-slate-800'}">
                                <tr>
                                    <th class="p-3 text-center w-12">No</th>
                                    <th class="p-3">Tanggal & Jam</th>
                                    <th class="p-3">Line & OP Mesin</th>
                                    <th class="p-3">Shift & Team</th>
                                    <th class="p-3">Type Produk / SKU</th>
                                    <th class="p-3">Kategori Problem</th>
                                    <th class="p-3">Problem (Masalah)</th>
                                    <th class="p-3">Penyebab</th>
                                    <th class="p-3">Perbaikan (CAPA)</th>
                                    <th class="p-3 text-center">Losstime</th>
                                    <th class="p-3">PIC</th>
                                    <th class="p-3 text-center">Status</th>
                                    <th class="p-3 text-center w-28">Aksi</th>
                                </tr>
                            </thead>
                            <tbody class="divide-y ${isLight ? 'divide-slate-200' : 'divide-slate-800/60'} font-mono">
                                ${dtLineGroups.length === 0 ? `
                                    <tr>
                                        <td colspan="13" class="p-8 text-center text-slate-500 font-sans">
                                            <div class="flex flex-col items-center gap-2">
                                                <i data-lucide="check-circle" class="w-7 h-7 text-emerald-400 opacity-80"></i>
                                                <span class="font-medium text-slate-300 text-xs">Zero Breakdown! Tidak ada log problem & downtime tercatat pada tanggal <strong>${this.selectedDailyDate}</strong>.</span>
                                            </div>
                                        </td>
                                    </tr>
                                ` : dtLineGroups.map((grp, gIdx) => {
                                    const grpTotalMins = grp.items.reduce((s, dt) => s + (dt.calculated_duration_minutes || dt.duration_minutes || 0), 0);
                                    const openCount = grp.items.filter(dt => (dt.status || '').toUpperCase() === 'OPEN').length;
                                    const closedCount = grp.items.length - openCount;
                                    const uniqueShifts = [...new Set(grp.items.map(dt => dt.shift?.name || 'Shift 1'))].join(', ');
                                    const uniqueCategories = [...new Set(grp.items.map(dt => dt.problem_type || 'Problem Mesin Mekanik'))].join(', ');

                                    return `
                                        <!-- LINE GROUP HEADER ROW (CLICKABLE COLLAPSIBLE) -->
                                        <tr class="line-dt-group-header cursor-pointer select-none ${isLight ? 'bg-amber-50/90 hover:bg-amber-100/90 border-amber-200 text-slate-900' : 'bg-slate-950 hover:bg-slate-800/90 border-slate-800 text-slate-100'} border-t-2 border-b transition-colors" data-toggle-dt-line="${grp.lineId}">
                                            <td class="p-3 text-center">
                                                <button type="button" class="w-6 h-6 rounded-lg ${isLight ? 'bg-white text-slate-700 border-amber-300' : 'bg-slate-900 text-slate-300 border-slate-700'} border flex items-center justify-center transition-transform cursor-pointer shadow-xs">
                                                    <i data-lucide="chevron-down" class="w-4 h-4 chevron-icon-dt-${grp.lineId} transition-transform duration-200"></i>
                                                </button>
                                            </td>
                                            <td class="p-3 font-sans" colspan="2">
                                                <div class="flex items-center gap-2">
                                                    <span class="px-2.5 py-1 rounded-md ${this.getLineBadgeStyle(grp.lineName || grp.lineId, isLight)} border font-bold text-xs flex items-center gap-1 font-mono shadow-xs">
                                                        🏭 ${grp.lineName}
                                                    </span>
                                                    <span class="px-2 py-0.5 rounded-full text-[10px] font-bold ${isLight ? 'bg-rose-100 text-rose-800 border border-rose-200' : 'bg-rose-950 text-rose-300 border border-rose-800'} font-sans">
                                                        ${grp.items.length} Problem Event
                                                    </span>
                                                </div>
                                                ${grp.lineCode ? `<div class="text-[10px] text-slate-400 font-mono mt-0.5">${grp.lineCode}</div>` : ''}
                                            </td>
                                            <td class="p-3 font-sans">
                                                <span class="px-2 py-0.5 rounded text-[10px] font-bold ${isLight ? 'bg-slate-200 text-slate-800' : 'bg-slate-800 text-slate-300'} font-sans">
                                                    ${uniqueShifts}
                                                </span>
                                            </td>
                                            <td class="p-3 font-sans text-xs" colspan="2">
                                                <div class="text-[11px] font-semibold ${isLight ? 'text-slate-800' : 'text-slate-300'} truncate max-w-[200px]" title="${uniqueCategories}">
                                                    ${uniqueCategories}
                                                </div>
                                                <div class="text-[10px] text-slate-400">Subtotal ${grp.items.length} kejadian</div>
                                            </td>
                                            <td class="p-3 font-sans text-xs" colspan="3">
                                                <div class="flex items-center gap-1.5 font-sans">
                                                    ${openCount > 0 ? `<span class="px-2 py-0.5 rounded text-[10px] font-bold bg-rose-950 text-rose-300 border border-rose-800 animate-pulse">${openCount} OPEN</span>` : ''}
                                                    <span class="px-2 py-0.5 rounded text-[10px] font-bold bg-emerald-950 text-emerald-300 border border-emerald-800">${closedCount} CLOSED</span>
                                                </div>
                                            </td>
                                            <td class="p-3 text-center font-bold font-mono text-amber-400">
                                                ${grpTotalMins} m
                                            </td>
                                            <td class="p-3 text-center font-sans text-xs" colspan="2">
                                                <span class="text-[10px] ${isLight ? 'text-slate-500' : 'text-slate-400'} font-mono">Klik baris</span>
                                            </td>
                                        </tr>

                                        <!-- EXPANDABLE CHILD ROWS FOR PROBLEM TABLE -->
                                        ${grp.items.map((dt, dIdx) => `
                                            <tr class="dt-line-row-${grp.lineId} ${isLight ? 'bg-white hover:bg-slate-50' : 'bg-slate-900/70 hover:bg-slate-800/60'} transition-colors border-b ${isLight ? 'border-slate-200/60' : 'border-slate-800/50'}">
                                                <td class="p-3 text-center text-xs font-mono text-slate-400">
                                                    ${gIdx + 1}.${dIdx + 1}
                                                </td>
                                                <td class="p-3 font-mono text-xs ${isLight ? 'text-slate-800' : 'text-slate-300'}">
                                                    <div class="font-bold">${dt.formatted_start_time || dt.start_time?.slice(11, 16) || '-'}</div>
                                                    <div class="text-[10px] text-slate-500">${dt.start_time?.slice(0, 10) || ''}</div>
                                                </td>
                                                <td class="p-3 font-sans">
                                                    <div class="flex items-center gap-1.5">
                                                        <span class="px-2 py-0.5 rounded text-[11px] font-bold ${this.getLineBadgeStyle(dt.production_line?.name || dt.line_name || dt.production_line_id, isLight)} border font-mono">
                                                            ${dt.production_line?.name || dt.line_name || 'Line'}
                                                        </span>
                                                    </div>
                                                    <div class="text-[10px] text-slate-400 font-mono mt-0.5">${dt.machine ? `${dt.machine.code} (${dt.machine.name})` : '-'}</div>
                                                </td>
                                                <td class="p-3 font-sans">
                                                    <span class="px-2 py-0.5 rounded text-[10px] font-semibold ${isLight ? 'bg-slate-100 text-slate-700 border-slate-300' : 'bg-slate-800 text-slate-200 border-slate-700'} border">
                                                        ${dt.shift ? dt.shift.name : 'Shift #' + dt.shift_id}
                                                    </span>
                                                    ${dt.team ? `<div class="text-[10px] text-amber-400 font-bold mt-1">👥 ${dt.team}</div>` : ''}
                                                </td>
                                                <td class="p-3 text-slate-300 font-sans text-xs">
                                                    <div class="font-bold ${isLight ? 'text-slate-900' : 'text-slate-200'}">${dt.product ? dt.product.name : '-'}</div>
                                                    ${dt.product?.sku ? `<div class="text-[10px] text-slate-400 font-mono">SKU: ${dt.product.sku}</div>` : ''}
                                                </td>
                                                <td class="p-3 font-sans">
                                                    <span class="px-2 py-0.5 rounded text-[10px] font-bold ${
                                                        dt.problem_type === 'Problem Mesin Mekanik' ? 'bg-rose-950 text-rose-300 border border-rose-800' :
                                                        dt.problem_type === 'Problem Mesin Elektrik' ? 'bg-amber-950 text-amber-300 border border-amber-800' :
                                                        dt.problem_type === 'Problem Tool' ? 'bg-orange-950 text-orange-300 border border-orange-800' :
                                                        dt.problem_type === 'Planning Downtime' ? 'bg-blue-950 text-blue-300 border border-blue-800' :
                                                        dt.problem_type === 'Inventory' ? 'bg-purple-950 text-purple-300 border border-purple-800' :
                                                        'bg-slate-800 text-slate-300 border border-slate-700'
                                                    }">
                                                        ${dt.problem_type || 'Problem Mesin Mekanik'}
                                                    </span>
                                                </td>
                                                <td class="p-3 font-sans ${isLight ? 'text-slate-800' : 'text-slate-200'}">
                                                    <div class="font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'}">${dt.description || 'Problem Operasional'}</div>
                                                </td>
                                                <td class="p-3 font-sans text-xs">
                                                    <div class="${isLight ? 'text-slate-700' : 'text-slate-300'}">${dt.cause || '-'}</div>
                                                </td>
                                                <td class="p-3 font-sans text-xs">
                                                    <div class="text-emerald-400 font-medium">${dt.action_taken || '-'}</div>
                                                </td>
                                                <td class="p-3 font-bold text-center text-rose-400 font-mono">${dt.calculated_duration_minutes || dt.duration_minutes || 0} m</td>
                                                <td class="p-3 font-sans text-xs">
                                                    <div class="font-semibold text-cyan-300">${dt.pic || '-'}</div>
                                                </td>
                                                <td class="p-3 text-center font-sans">
                                                    <span class="px-2 py-0.5 rounded text-[10px] font-bold ${dt.status === 'OPEN' ? 'bg-rose-950 text-rose-300 border border-rose-800 animate-pulse' : 'bg-emerald-950 text-emerald-300 border border-emerald-800'}">
                                                        ${dt.status || 'CLOSED'}
                                                    </span>
                                                </td>
                                                <td class="p-3 text-center font-sans">
                                                    <div class="flex items-center justify-center gap-1.5">
                                                        <button data-action="view-dt-record" data-id="${dt.id}" title="Lihat Detail Log Problem" class="w-7 h-7 rounded-lg ${isLight ? 'bg-blue-50 hover:bg-blue-100 border-blue-200 text-blue-700' : 'bg-blue-950/70 hover:bg-blue-900 border border-blue-800/80 text-blue-400 hover:text-blue-200'} border flex items-center justify-center cursor-pointer transition-all shadow-sm hover:scale-110 active:scale-95">
                                                            <i data-lucide="eye" class="w-3.5 h-3.5"></i>
                                                        </button>
                                                        <button data-action="edit-dt-record" data-id="${dt.id}" title="Edit Log Problem" class="w-7 h-7 rounded-lg ${isLight ? 'bg-cyan-50 hover:bg-cyan-100 border-cyan-200 text-cyan-700' : 'bg-cyan-950/70 hover:bg-cyan-900 border border-cyan-800/80 text-cyan-400 hover:text-cyan-200'} border flex items-center justify-center cursor-pointer transition-all shadow-sm hover:scale-110 active:scale-95">
                                                            <i data-lucide="pencil" class="w-3.5 h-3.5"></i>
                                                        </button>
                                                        <button data-action="delete-dt-record" data-id="${dt.id}" title="Hapus Log Problem" class="w-7 h-7 rounded-lg ${isLight ? 'bg-rose-50 hover:bg-rose-100 border-rose-200 text-rose-700' : 'bg-rose-950/70 hover:bg-rose-900 border border-rose-800/80 text-rose-400 hover:text-rose-200'} border flex items-center justify-center cursor-pointer transition-all shadow-sm hover:scale-110 active:scale-95">
                                                            <i data-lucide="trash-2" class="w-3.5 h-3.5"></i>
                                                        </button>
                                                    </div>
                                                </td>
                                            </tr>
                                        `).join('')}
                                    `;
                                }).join('')}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();

        // 1. Button Triggers to Open Modals
        document.getElementById('btn-open-create-prod-modal')?.addEventListener('click', () => {
            this.showCreateDailyProductionModal();
        });
        document.getElementById('btn-empty-open-create-prod')?.addEventListener('click', () => {
            this.showCreateDailyProductionModal();
        });
        document.getElementById('btn-open-create-dt-modal')?.addEventListener('click', () => {
            this.showCreateDailyTroubleModal();
        });

        // 2. Date Navigation Controls
        document.getElementById('input-daily-date')?.addEventListener('change', (e) => {
            this.selectedDailyDate = e.target.value;
            this.renderDailyReport();
        });

        document.getElementById('btn-daily-today')?.addEventListener('click', () => {
            this.selectedDailyDate = new Date().toISOString().slice(0, 10);
            this.renderDailyReport();
        });

        document.getElementById('btn-daily-prev')?.addEventListener('click', () => {
            const d = new Date(this.selectedDailyDate);
            d.setDate(d.getDate() - 1);
            this.selectedDailyDate = d.toISOString().slice(0, 10);
            this.renderDailyReport();
        });

        document.getElementById('btn-daily-next')?.addEventListener('click', () => {
            const d = new Date(this.selectedDailyDate);
            d.setDate(d.getDate() + 1);
            this.selectedDailyDate = d.toISOString().slice(0, 10);
            this.renderDailyReport();
        });

        document.getElementById('btn-clear-daily-filters')?.addEventListener('click', () => {
            this.filters.line_id = '';
            this.filters.machine_id = '';
            this.filters.shift_id = '';
            this.populateFilterDropdowns();
            this.renderDailyReport();
        });

        // 3. View (Preview), Edit & Delete Action Buttons
        document.querySelectorAll('[data-action="view-prod-record"]').forEach(btn => {
            btn.addEventListener('click', (e) => {
                const id = e.currentTarget.getAttribute('data-id');
                this.showViewProductionModal(id, records);
            });
        });

        document.querySelectorAll('[data-action="edit-prod-record"]').forEach(btn => {
            btn.addEventListener('click', (e) => {
                const id = e.currentTarget.getAttribute('data-id');
                this.showEditProductionRecordModal(id, records);
            });
        });

        document.querySelectorAll('[data-action="delete-prod-record"]').forEach(btn => {
            btn.addEventListener('click', (e) => {
                const id = e.currentTarget.getAttribute('data-id');
                this.showDeleteProductionModal(id);
            });
        });

        document.querySelectorAll('[data-action="view-dt-record"]').forEach(btn => {
            btn.addEventListener('click', (e) => {
                const id = e.currentTarget.getAttribute('data-id');
                this.showViewDowntimeModal(id, downtimes);
            });
        });

        document.querySelectorAll('[data-action="edit-dt-record"]').forEach(btn => {
            btn.addEventListener('click', (e) => {
                const id = e.currentTarget.getAttribute('data-id');
                this.showEditDowntimeModal(id, downtimes);
            });
        });

        document.querySelectorAll('[data-action="delete-dt-record"]').forEach(btn => {
            btn.addEventListener('click', (e) => {
                const id = e.currentTarget.getAttribute('data-id');
                this.showDeleteDowntimeModal(id);
            });
        });

        // 4. Line Grouping Collapsible Toggles & Quick Actions
        document.getElementById('btn-quick-fill-missing')?.addEventListener('click', () => {
            this.showCreateDailyProductionModal();
        });

        document.querySelectorAll('[data-toggle-line]').forEach(headerRow => {
            headerRow.addEventListener('click', (e) => {
                // If user clicked a nested button/input, allow it
                const lineId = headerRow.getAttribute('data-toggle-line');
                const childRows = document.querySelectorAll(`.line-row-${lineId}`);
                const chevronIcon = document.querySelector(`.chevron-icon-${lineId}`);
                const isCurrentlyHidden = childRows[0]?.classList.contains('hidden');

                childRows.forEach(r => {
                    if (isCurrentlyHidden) {
                        r.classList.remove('hidden');
                    } else {
                        r.classList.add('hidden');
                    }
                });

                if (chevronIcon) {
                    if (isCurrentlyHidden) {
                        chevronIcon.classList.remove('-rotate-90');
                        chevronIcon.classList.add('rotate-0');
                    } else {
                        chevronIcon.classList.remove('rotate-0');
                        chevronIcon.classList.add('-rotate-90');
                    }
                }
            });
        });

        document.getElementById('btn-expand-all-lines')?.addEventListener('click', () => {
            document.querySelectorAll('[class*="line-row-"]').forEach(r => r.classList.remove('hidden'));
            document.querySelectorAll('[class*="chevron-icon-"]').forEach(i => {
                i.classList.remove('-rotate-90');
                i.classList.add('rotate-0');
            });
        });

        document.getElementById('btn-collapse-all-lines')?.addEventListener('click', () => {
            document.querySelectorAll('[class*="line-row-"]').forEach(r => r.classList.add('hidden'));
            document.querySelectorAll('[class*="chevron-icon-"]').forEach(i => {
                i.classList.remove('rotate-0');
                i.classList.add('-rotate-90');
            });
        });

        // 5. Problem & Downtime Table Line Grouping Collapsible Toggles
        document.querySelectorAll('[data-toggle-dt-line]').forEach(headerRow => {
            headerRow.addEventListener('click', (e) => {
                const lineId = headerRow.getAttribute('data-toggle-dt-line');
                const childRows = document.querySelectorAll(`.dt-line-row-${lineId}`);
                const chevronIcon = document.querySelector(`.chevron-icon-dt-${lineId}`);
                const isCurrentlyHidden = childRows[0]?.classList.contains('hidden');

                childRows.forEach(r => {
                    if (isCurrentlyHidden) {
                        r.classList.remove('hidden');
                    } else {
                        r.classList.add('hidden');
                    }
                });

                if (chevronIcon) {
                    if (isCurrentlyHidden) {
                        chevronIcon.classList.remove('-rotate-90');
                        chevronIcon.classList.add('rotate-0');
                    } else {
                        chevronIcon.classList.remove('rotate-0');
                        chevronIcon.classList.add('-rotate-90');
                    }
                }
            });
        });

        document.getElementById('btn-expand-all-dt-lines')?.addEventListener('click', () => {
            document.querySelectorAll('[class*="dt-line-row-"]').forEach(r => r.classList.remove('hidden'));
            document.querySelectorAll('[class*="chevron-icon-dt-"]').forEach(i => {
                i.classList.remove('-rotate-90');
                i.classList.add('rotate-0');
            });
        });

        document.getElementById('btn-collapse-all-dt-lines')?.addEventListener('click', () => {
            document.querySelectorAll('[class*="dt-line-row-"]').forEach(r => r.classList.add('hidden'));
            document.querySelectorAll('[class*="chevron-icon-dt-"]').forEach(i => {
                i.classList.remove('rotate-0');
                i.classList.add('-rotate-90');
            });
        });

        this.initTableSorting();
        if (this.currentLang === 'ja') {
            i18n.localizeDom(content);
        }
    }

    getMachinesForLine(lineId) {
        if (!lineId) return this.masterData.machines || [];
        const lineObj = (this.masterData.lines || []).find(l => String(l.id) === String(lineId));
        const lineCode = lineObj ? (lineObj.code || lineObj.name || '') : '';
        const lineNum = lineCode ? lineCode.replace(/^FX-?/i, '').trim() : '';

        const matched = (this.masterData.machines || []).filter(m => {
            const mLineId = m.work_center?.production_line_id || m.work_center?.production_line?.id || m.production_line_id || m.workCenter?.production_line_id;
            if (mLineId && String(mLineId) === String(lineId)) return true;

            if (lineNum) {
                const code = m.code || '';
                const name = m.name || '';
                if (lineNum === '1') {
                    if (/^MC-MEASURING-FX-?1$/i.test(code)) return true;
                    if (/^MC-FX-1[0-9]{2}(\.[0-9]+|,.*)?$/i.test(code)) return true;
                } else if (lineNum === '11') {
                    if (/^MC-MEASURING-FX-?11$/i.test(code)) return true;
                    if (/^MC-FX-11[0-9]{2,3}(\.[0-9]+|,.*)?$/i.test(code)) return true;
                } else {
                    const measuringRegex = new RegExp(`^MC-MEASURING-FX-?0*${lineNum}$`, 'i');
                    const opRegex = new RegExp(`^MC-FX-0*${lineNum}[0-9]+`, 'i');
                    if (measuringRegex.test(code)) return true;
                    if (opRegex.test(code)) return true;
                }
                if (new RegExp(`\\bFX-?0*${lineNum}\\b`, 'i').test(name)) return true;
            }
            return false;
        });

        const result = matched.length > 0 ? [...matched] : [];

        // Sort: Measuring Machine always at index 0 (top), then OP machines alphabetically/numerically
        result.sort((a, b) => {
            const isAMeasuring = (a.name && a.name.toLowerCase().includes('measuring')) || (a.code && a.code.toLowerCase().includes('measuring'));
            const isBMeasuring = (b.name && b.name.toLowerCase().includes('measuring')) || (b.code && b.code.toLowerCase().includes('measuring'));
            if (isAMeasuring && !isBMeasuring) return -1;
            if (!isAMeasuring && isBMeasuring) return 1;
            return (a.name || a.code || '').localeCompare(b.name || b.code || '', undefined, { numeric: true, sensitivity: 'base' });
        });

        return result;
    }

    // ==========================================
    // 3B-1. MODAL POPUP: CREATE DAILY PRODUCTION RECORD
    // ==========================================
    showCreateDailyProductionModal() {
        const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');
        let dailyTroubleLogsState = [];

        const defaultLineId = this.filters.line_id || this.masterData.lines?.[0]?.id || '';
        const defaultShiftId = this.filters.shift_id || this.masterData.shifts?.[0]?.id || '';
        const defaultMachineId = this.filters.machine_id || '';

        const modalHtml = `
            <div id="modal-create-daily-prod" class="fixed inset-0 z-50 flex items-center justify-center p-3 sm:p-5 bg-slate-950/75 backdrop-blur-md overflow-y-auto">
                <div class="relative w-full max-w-4xl max-h-[90vh] overflow-y-auto ${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-2xl shadow-2xl p-6 space-y-5 my-auto">
                    
                    <!-- MODAL HEADER -->
                    <div class="flex items-center justify-between pb-3 border-b ${isLight ? 'border-slate-200' : 'border-slate-800'}">
                        <div class="flex items-center gap-3">
                            <div class="w-9 h-9 rounded-xl bg-emerald-500/15 border border-emerald-500/30 flex items-center justify-center text-emerald-400">
                                <i data-lucide="clipboard-plus" class="w-5 h-5"></i>
                            </div>
                            <div>
                                <h3 class="font-bold text-base ${isLight ? 'text-slate-900' : 'text-slate-100'}">
                                    Form Input Laporan Produksi & Trouble Log
                                </h3>
                                <p class="text-xs ${isLight ? 'text-slate-500' : 'text-slate-400'}">Tanggal Produksi: <span class="font-mono font-bold text-cyan-400">${this.selectedDailyDate}</span></p>
                            </div>
                        </div>
                        <button type="button" id="btn-close-modal-create-daily" class="w-8 h-8 rounded-lg ${isLight ? 'bg-slate-100 hover:bg-slate-200 text-slate-600' : 'bg-slate-800 hover:bg-slate-700 text-slate-400 hover:text-slate-200'} flex items-center justify-center transition-colors cursor-pointer">
                            <i data-lucide="x" class="w-4 h-4"></i>
                        </button>
                    </div>

                    <!-- FORM -->
                    <form id="form-modal-daily-entry" class="space-y-4 text-xs">
                        <!-- LANGKAH 1: KONTEKS LINE & MESIN -->
                        <div class="form-step-card ${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950/60 border-slate-800'} border rounded-xl p-4 space-y-3">
                            <div class="flex items-center justify-between border-b ${isLight ? 'border-slate-200' : 'border-slate-800/80'} pb-2">
                                <div class="flex items-center gap-2">
                                    <span class="w-5 h-5 rounded-full bg-cyan-500/20 text-cyan-400 font-bold text-xs flex items-center justify-center border border-cyan-500/40">1</span>
                                    <h4 class="font-bold text-xs ${isLight ? 'text-slate-900' : 'text-slate-100'} uppercase tracking-wider">Tanggal Laporan, Line, Mesin, Produk & Shift</h4>
                                </div>
                                <span id="modal-cycle-time-badge" class="text-[11px] font-mono text-cyan-400 font-bold px-2 py-0.5 rounded ${isLight ? 'bg-cyan-50 border border-cyan-200' : 'bg-cyan-950 border border-cyan-800'}">
                                    Cycle Time: 9.5s
                                </span>
                            </div>
                            <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-5 gap-3">
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-400'} mb-1 font-medium flex items-center gap-1">
                                        <i data-lucide="calendar" class="w-3.5 h-3.5 text-cyan-400"></i>
                                        Tanggal Produksi
                                    </label>
                                    <input type="date" name="production_date" value="${this.selectedDailyDate}" required class="w-full ${isLight ? 'bg-white border-slate-300 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-mono font-bold outline-none focus:border-cyan-500" />
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-400'} mb-1 font-medium">Production Line</label>
                                    <select name="production_line_id" required class="w-full ${isLight ? 'bg-white border-slate-300 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 outline-none focus:border-cyan-500">
                                        ${this.masterData.lines.map(l => `<option value="${l.id}" ${(defaultLineId == l.id) ? 'selected' : ''}>${l.name}</option>`).join('')}
                                    </select>
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-400'} mb-1 font-medium">Mesin Input</label>
                                    <select name="machine_id" required class="w-full ${isLight ? 'bg-white border-slate-300 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-sans outline-none focus:border-cyan-500">
                                        <!-- Populated dynamically -->
                                    </select>
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-400'} mb-1 font-medium">Nama Produk</label>
                                    <select name="product_id" required class="w-full ${isLight ? 'bg-white border-slate-300 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-sans outline-none focus:border-cyan-500">
                                        <!-- Will be populated by filterProductsByLine() -->
                                    </select>
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-400'} mb-1 font-medium">Shift Kerja</label>
                                    <select name="shift_id" required class="w-full ${isLight ? 'bg-white border-slate-300 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 outline-none focus:border-cyan-500">
                                        ${this.masterData.shifts.map(s => `<option value="${s.id}" ${(defaultShiftId == s.id) ? 'selected' : ''}>${s.name}</option>`).join('')}
                                    </select>
                                </div>
                            </div>
                        </div>

                        <!-- LANGKAH 2: HASIL PRODUKSI & WAKTU -->
                        <div class="form-step-card ${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950/60 border-slate-800'} border rounded-xl p-4 space-y-3">
                            <div class="flex items-center justify-between border-b ${isLight ? 'border-slate-200' : 'border-slate-800/80'} pb-2">
                                <div class="flex items-center gap-2">
                                    <span class="w-5 h-5 rounded-full bg-emerald-500/20 text-emerald-400 font-bold text-xs flex items-center justify-center border border-emerald-500/40">2</span>
                                    <h4 class="font-bold text-xs ${isLight ? 'text-slate-900' : 'text-slate-100'} uppercase tracking-wider">Hasil Output & Jam Kerja</h4>
                                </div>
                                <span class="text-[10px] text-emerald-500 font-mono font-semibold">Formula: Target = (Planned Time × 60) ÷ Cycle Time</span>
                            </div>
                            <div class="grid grid-cols-2 md:grid-cols-5 gap-3">
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-400'} mb-1 font-medium flex items-center gap-1">
                                        Target Qty
                                        <span class="text-[10px] text-cyan-400 font-mono font-bold">(Auto)</span>
                                    </label>
                                    <input type="number" name="target_quantity" value="0" min="0" placeholder="0" required class="w-full ${isLight ? 'bg-white border-slate-300 text-cyan-600' : 'bg-slate-900 border-slate-800 text-cyan-400'} border rounded-lg px-2.5 py-2 font-mono font-bold outline-none focus:border-cyan-500 shadow-2xs" />
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-400'} mb-1 font-medium">Total Measuring</label>
                                    <input type="number" name="total_quantity" value="0" min="0" placeholder="0" required class="w-full ${isLight ? 'bg-white border-slate-300 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-mono font-bold outline-none focus:border-cyan-500" />
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-400'} mb-1 font-medium">Finish Good (OK)</label>
                                    <input type="number" name="good_quantity" value="0" min="0" placeholder="0" required class="w-full ${isLight ? 'bg-white border-slate-300 text-emerald-600' : 'bg-slate-900 border-slate-800 text-emerald-400'} border rounded-lg px-2.5 py-2 font-mono font-bold outline-none focus:border-cyan-500" />
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-400'} mb-1 font-medium">Not Good (NG)</label>
                                    <input type="number" name="reject_quantity" value="0" min="0" placeholder="0" required class="w-full ${isLight ? 'bg-white border-slate-300 text-rose-600' : 'bg-slate-900 border-slate-800 text-rose-400'} border rounded-lg px-2.5 py-2 font-mono font-bold outline-none focus:border-cyan-500" />
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-400'} mb-1 font-medium">Scrap / Afkir</label>
                                    <input type="number" name="scrap_quantity" value="0" min="0" placeholder="0" required class="w-full ${isLight ? 'bg-white border-slate-300 text-amber-600' : 'bg-slate-900 border-slate-800 text-amber-400'} border rounded-lg px-2.5 py-2 font-mono font-bold outline-none focus:border-cyan-500" />
                                </div>
                            </div>

                            <div class="grid grid-cols-3 gap-3 pt-1">
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-400'} mb-1 font-medium">Planned Time (min)</label>
                                    <input type="number" name="planned_production_time" value="480" required class="w-full ${isLight ? 'bg-white border-slate-300 text-slate-800' : 'bg-slate-900 border-slate-800 text-slate-200'} border rounded-lg px-2.5 py-1.5 font-mono outline-none focus:border-cyan-500" />
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-400'} mb-1 font-medium">Break / Istirahat (min)</label>
                                    <input type="number" name="planned_downtime" value="60" required class="w-full ${isLight ? 'bg-white border-slate-300 text-slate-800' : 'bg-slate-900 border-slate-800 text-slate-200'} border rounded-lg px-2.5 py-1.5 font-mono outline-none focus:border-cyan-500" />
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-400'} mb-1 font-medium">Run Time (min)</label>
                                    <input type="number" name="run_time" value="420" required class="w-full ${isLight ? 'bg-white border-slate-300 text-emerald-700' : 'bg-slate-900 border-slate-800 text-emerald-400'} border rounded-lg px-2.5 py-1.5 font-mono font-bold outline-none focus:border-cyan-500" />
                                </div>
                            </div>
                        </div>

                        <!-- LANGKAH 3: LOG PROBLEM & DOWNTIME DINAMIS (MULTI-ROW) -->
                        <div class="form-step-card ${isLight ? 'bg-amber-50/50 border-amber-200' : 'bg-slate-950/60 border-amber-500/30'} border rounded-xl p-4 space-y-3">
                            <div class="flex flex-wrap items-center justify-between border-b ${isLight ? 'border-amber-200' : 'border-slate-800/80'} pb-2 gap-2">
                                <div class="flex items-center gap-2">
                                    <span class="w-5 h-5 rounded-full bg-amber-500/20 text-amber-400 font-bold text-xs flex items-center justify-center border border-amber-500/40">3</span>
                                    <h4 class="font-bold text-xs ${isLight ? 'text-amber-800' : 'text-amber-300'} uppercase tracking-wider">Problem Log & Loss Time (Opsional)</h4>
                                </div>
                                <div class="flex items-center gap-2">
                                    <span id="modal-dt-total-badge" class="px-2.5 py-0.5 rounded-full text-[11px] font-mono font-bold ${isLight ? 'bg-amber-100 text-amber-800 border-amber-300' : 'bg-amber-950/80 text-amber-400 border border-amber-800'} border">
                                        Total Losstime: 0 Menit
                                    </span>
                                    <button type="button" id="btn-modal-add-trouble" class="px-3 py-1.5 rounded-lg text-xs font-bold bg-amber-600 hover:bg-amber-500 text-white shadow-md flex items-center gap-1.5 cursor-pointer transition-all">
                                        <i data-lucide="plus" class="w-3.5 h-3.5"></i>
                                        <span>+ Tambah Problem Log</span>
                                    </button>
                                </div>
                            </div>

                            <div id="modal-trouble-logs-container" class="space-y-3 pt-1">
                                <!-- Dynamic Problem Log Rows Container -->
                            </div>
                        </div>

                        <input type="hidden" name="downtime" value="0" />
                        <input type="hidden" name="idle_time" value="0" />
                        <input type="hidden" name="ideal_cycle_time" value="9.5" />

                        <!-- MODAL FOOTER BUTTONS -->
                        <div class="pt-3 border-t ${isLight ? 'border-slate-200' : 'border-slate-800'} flex items-center justify-end gap-3">
                            <button type="button" id="btn-cancel-modal-create-daily" class="px-5 py-2.5 rounded-xl border ${isLight ? 'border-slate-300 hover:bg-slate-100 text-slate-700' : 'border-slate-700 hover:bg-slate-800 text-slate-300'} text-xs font-semibold cursor-pointer transition-all">
                                Batal
                            </button>
                            <button type="submit" class="px-6 py-2.5 bg-emerald-600 hover:bg-emerald-500 text-white font-bold rounded-xl shadow-lg shadow-emerald-600/30 flex items-center gap-2 cursor-pointer transition-all text-xs">
                                <i data-lucide="save" class="w-4 h-4"></i>
                                <span>Simpan Laporan Produksi</span>
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        `;

        // Render modal
        let existingModal = document.getElementById('modal-create-daily-prod');
        if (existingModal) existingModal.remove();

        const modalWrapper = document.createElement('div');
        modalWrapper.innerHTML = modalHtml;
        document.body.appendChild(modalWrapper.firstElementChild);

        if (window.lucide) window.lucide.createIcons();

        const activeModal = document.getElementById('modal-create-daily-prod');
        if (this.currentLang === 'ja' && activeModal) {
            i18n.localizeDom(activeModal);
        }
        const closeModal = () => {
            if (activeModal) activeModal.remove();
        };

        document.getElementById('btn-close-modal-create-daily')?.addEventListener('click', closeModal);
        document.getElementById('btn-cancel-modal-create-daily')?.addEventListener('click', closeModal);

        // Elements inside modal
        const troubleContainer = document.getElementById('modal-trouble-logs-container');
        const totalDtBadge = document.getElementById('modal-dt-total-badge');
        const addTroubleBtn = document.getElementById('btn-modal-add-trouble');
        const cycleTimeBadge = document.getElementById('modal-cycle-time-badge');

        const lineSelect = document.querySelector('#form-modal-daily-entry select[name="production_line_id"]');
        const productSelect = document.querySelector('#form-modal-daily-entry select[name="product_id"]');
        const shiftSelect = document.querySelector('#form-modal-daily-entry select[name="shift_id"]');
        const machineSelect = document.querySelector('#form-modal-daily-entry select[name="machine_id"]');

        const plannedTimeInp = document.querySelector('#form-modal-daily-entry input[name="planned_production_time"]');
        const plannedDtInp = document.querySelector('#form-modal-daily-entry input[name="planned_downtime"]');
        const runTimeInp = document.querySelector('#form-modal-daily-entry input[name="run_time"]');
        const targetQtyInp = document.querySelector('#form-modal-daily-entry input[name="target_quantity"]');
        const idealCycleInp = document.querySelector('#form-modal-daily-entry input[name="ideal_cycle_time"]');
        const mainDowntimeInp = document.querySelector('#form-modal-daily-entry input[name="downtime"]');

        const totalQtyInp = document.querySelector('#form-modal-daily-entry input[name="total_quantity"]');
        const goodQtyInp = document.querySelector('#form-modal-daily-entry input[name="good_quantity"]');
        const rejectQtyInp = document.querySelector('#form-modal-daily-entry input[name="reject_quantity"]');
        const scrapQtyInp = document.querySelector('#form-modal-daily-entry input[name="scrap_quantity"]');

        const getLeaderName = (teamVal) => {
            if (!lineSelect) return 'Belum Didaftar';
            const lineId = lineSelect.value;
            const match = (this.masterData.groups || []).find(g => 
                g.production_line_id == lineId && 
                (g.team === teamVal || g.team.toLowerCase().includes((teamVal || '').toLowerCase()) || (teamVal || '').toLowerCase().includes(g.team.toLowerCase()))
            );
            return match ? match.leader_name : 'Belum Didaftar';
        };

        const updateRunTimeAndDowntime = () => {
            let totalDtMins = 0;
            dailyTroubleLogsState.forEach(r => {
                totalDtMins += parseFloat(r.duration_minutes || 0);
            });

            if (totalDtBadge) totalDtBadge.textContent = `Total Losstime: ${totalDtMins} Menit`;
            if (mainDowntimeInp) mainDowntimeInp.value = totalDtMins;
            if (runTimeInp && plannedTimeInp && plannedDtInp) {
                const plannedMins = parseFloat(plannedTimeInp.value || 0);
                const breakMins = parseFloat(plannedDtInp.value || 0);
                runTimeInp.value = Math.max(0, plannedMins - breakMins - totalDtMins);
            }
        };

        // 1. Target Qty Calculation based on Product Ideal Cycle Time & Planned Production Time
        const calcTargetQty = () => {
            const selectedProdId = productSelect ? productSelect.value : null;
            const selectedProd = (this.masterData.products || []).find(p => p.id == selectedProdId);
            const cycleTime = selectedProd && selectedProd.ideal_cycle_time ? parseFloat(selectedProd.ideal_cycle_time) : 9.5;

            if (idealCycleInp) idealCycleInp.value = cycleTime;
            if (cycleTimeBadge) cycleTimeBadge.textContent = `Cycle Time: ${cycleTime}s`;

            const plannedMins = plannedTimeInp ? parseFloat(plannedTimeInp.value || 0) : 480;
            if (targetQtyInp && cycleTime > 0 && plannedMins > 0) {
                const computedTarget = Math.floor((plannedMins * 60) / cycleTime);
                targetQtyInp.value = computedTarget;
            }
        };

        // 2. Cascading Line -> Products & Machines
        const filterProductsByLine = () => {
            if (!lineSelect || !productSelect) return;
            const selectedLineId = lineSelect.value;
            const lineProducts = this.getProductsForLine(selectedLineId);

            if (lineProducts.length === 0) {
                const allProds = this.masterData.products || [];
                productSelect.innerHTML = allProds.map(p => `
                    <option value="${p.id}">${p.name} — SKU: ${p.sku} (${p.ideal_cycle_time}s)</option>
                `).join('');
            } else {
                productSelect.innerHTML = lineProducts.map(p => `
                    <option value="${p.id}">${p.name} — SKU: ${p.sku} (${p.ideal_cycle_time}s)</option>
                `).join('');
            }

            // Update machine select strictly for the selected line & auto-select Measuring Machine
            if (machineSelect) {
                const lineMachines = this.getMachinesForLine(selectedLineId);
                machineSelect.innerHTML = lineMachines.map(m => 
                    `<option value="${m.id}">${m.name} (${m.code})</option>`
                ).join('');

                const measuringMC = lineMachines.find(m => 
                    (m.name && m.name.toLowerCase().includes('measuring')) || 
                    (m.code && m.code.toLowerCase().includes('measuring'))
                );
                if (measuringMC) {
                    machineSelect.value = measuringMC.id;
                } else if (lineMachines.length > 0) {
                    machineSelect.value = lineMachines[0].id;
                }
            }

            calcTargetQty();
        };

        // 3. Shift Time Calculation
        const updateShiftCalc = () => {
            if (!shiftSelect) return;
            const shiftId = shiftSelect.value;
            const shiftObj = (this.masterData.shifts || []).find(s => s.id == shiftId);
            if (!shiftObj) return;

            const startStr = shiftObj.start_time || '07:30';
            const endStr = shiftObj.end_time || '16:30';
            const breakMins = parseInt(shiftObj.break_duration_minutes || 60);

            const startParts = startStr.split(':');
            const endParts = endStr.split(':');
            let startMins = parseInt(startParts[0]) * 60 + parseInt(startParts[1] || 0);
            let endMins = parseInt(endParts[0]) * 60 + parseInt(endParts[1] || 0);
            if (endMins <= startMins) endMins += 24 * 60;

            const totalDurationMins = endMins - startMins;
            const plannedTimeMins = Math.max(0, totalDurationMins - breakMins);

            if (plannedTimeInp) plannedTimeInp.value = plannedTimeMins;
            if (plannedDtInp) plannedDtInp.value = breakMins;
            
            calcTargetQty();
            updateRunTimeAndDowntime();
        };

        // 5. Problem Log Rows Renderer
        const renderTroubleRows = () => {
            if (!troubleContainer) return;

            if (dailyTroubleLogsState.length === 0) {
                troubleContainer.innerHTML = `
                    <div class="text-center py-4 px-4 ${isLight ? 'bg-slate-100/70 border-slate-300' : 'bg-slate-900/40 border-slate-800'} border border-dashed rounded-xl text-slate-400 text-xs">
                        <i data-lucide="check-circle" class="w-4 h-4 text-emerald-400 inline mr-1"></i>
                        <span>Produksi lancar tanpa kendala. Klik <strong class="text-amber-400 cursor-pointer" id="btn-modal-empty-add-trouble">+ Tambah Problem Log</strong> jika ada stoppage/problem hari ini.</span>
                    </div>
                `;
                document.getElementById('btn-modal-empty-add-trouble')?.addEventListener('click', () => {
                    addTroubleBtn?.click();
                });
            } else {
                troubleContainer.innerHTML = dailyTroubleLogsState.map((row, idx) => `
                    <div class="trouble-row-card ${isLight ? 'bg-white border-slate-300' : 'bg-slate-900 border-slate-800'} border rounded-xl p-3 space-y-3 hover:border-amber-500/40 transition-colors">
                        <div class="flex items-center justify-between border-b ${isLight ? 'border-slate-200' : 'border-slate-800/80'} pb-2">
                            <div class="flex items-center gap-2">
                                <span class="px-2 py-0.5 rounded text-[10px] font-mono font-bold bg-amber-950 text-amber-300 border border-amber-800">
                                    Problem #${idx + 1}
                                </span>
                                <span class="text-[11px] ${isLight ? 'text-slate-800' : 'text-slate-300'} font-sans font-medium">${row.problem_type || 'Mesin'} (${row.duration_minutes || 0} Menit Losstime)</span>
                            </div>
                            <div class="flex items-center gap-2">
                                <span class="px-2 py-0.5 rounded text-[10px] font-bold ${row.status === 'OPEN' ? 'bg-rose-950 text-rose-300 border border-rose-800' : 'bg-emerald-950 text-emerald-300 border border-emerald-800'}">
                                    ${row.status || 'CLOSED'}
                                </span>
                                <button type="button" data-del-modal-dt-idx="${idx}" class="p-1 rounded-lg text-slate-400 hover:text-rose-400 hover:bg-rose-950/40 border border-transparent hover:border-rose-800 transition-colors cursor-pointer" title="Hapus Baris Problem Ini">
                                    <i data-lucide="trash-2" class="w-3.5 h-3.5"></i>
                                </button>
                            </div>
                        </div>

                        <div class="grid grid-cols-1 md:grid-cols-3 gap-3">
                            <div>
                                <label class="block text-slate-400 text-[10px] mb-1 font-semibold">Team & Leader</label>
                                <div class="grid grid-cols-2 gap-1.5">
                                    <select data-trouble-field="team" data-row="${idx}" class="${isLight ? 'bg-white border-slate-300 text-slate-900' : 'bg-slate-950 border-slate-800 text-slate-100'} border rounded px-2 py-1 text-xs font-bold text-amber-500">
                                        <option value="Team A" ${row.team === 'Team A' ? 'selected' : ''}>Team A</option>
                                        <option value="Team B" ${row.team === 'Team B' ? 'selected' : ''}>Team B</option>
                                        <option value="Team C" ${row.team === 'Team C' ? 'selected' : ''}>Team C</option>
                                        <option value="Team D" ${row.team === 'Team D' ? 'selected' : ''}>Team D</option>
                                    </select>
                                    <input type="text" readonly value="${row.leader_name || getLeaderName(row.team)}" class="${isLight ? 'bg-slate-100 border-slate-300 text-amber-700' : 'bg-slate-950/80 border-slate-800 text-amber-300'} border rounded px-2 py-1 font-bold text-[10px] truncate" title="${row.leader_name || ''}" />
                                </div>
                            </div>
                            <div>
                                <label class="block text-slate-400 text-[10px] mb-1 font-semibold">Kategori Problem*</label>
                                <select data-trouble-field="problem_type" data-row="${idx}" class="w-full ${isLight ? 'bg-white border-slate-300 text-slate-900' : 'bg-slate-950 border-slate-800 text-slate-100'} border rounded px-2 py-1 text-xs font-bold">
                                    <option value="Inventory" ${row.problem_type === 'Inventory' ? 'selected' : ''}>Inventory</option>
                                    <option value="Others" ${row.problem_type === 'Others' || row.problem_type === 'Other' ? 'selected' : ''}>Others</option>
                                    <option value="Planning Downtime" ${row.problem_type === 'Planning Downtime' ? 'selected' : ''}>Planning Downtime</option>
                                    <option value="Problem Mesin Elektrik" ${row.problem_type === 'Problem Mesin Elektrik' ? 'selected' : ''}>Problem Mesin Elektrik</option>
                                    <option value="Problem Mesin Mekanik" ${row.problem_type === 'Problem Mesin Mekanik' || (!row.problem_type && idx === 0) || row.problem_type === 'Mesin' ? 'selected' : ''}>Problem Mesin Mekanik</option>
                                    <option value="Problem Tool" ${row.problem_type === 'Problem Tool' || row.problem_type === 'Tool' || row.problem_type === 'Dies / Tooling' ? 'selected' : ''}>Problem Tool</option>
                                </select>
                            </div>
                            <div>
                                <label class="block text-slate-400 text-[10px] mb-1 font-semibold">Jam Mulai, Selesai & Losstime (Menit)*</label>
                                <div class="grid grid-cols-3 gap-1.5">
                                    <input type="time" data-trouble-field="start_time" data-row="${idx}" value="${row.start_time || '08:00'}" class="${isLight ? 'bg-white border-slate-300 text-slate-900' : 'bg-slate-950 border-slate-800 text-slate-100'} border rounded px-1.5 py-1 text-xs font-mono" />
                                    <input type="time" data-trouble-field="end_time" data-row="${idx}" value="${row.end_time || ''}" class="${isLight ? 'bg-white border-slate-300 text-slate-900' : 'bg-slate-950 border-slate-800 text-slate-100'} border rounded px-1.5 py-1 text-xs font-mono" />
                                    <input type="number" data-trouble-field="duration_minutes" data-row="${idx}" value="${row.duration_minutes || 0}" min="0" placeholder="Mins" class="${isLight ? 'bg-white border-slate-300 text-rose-600' : 'bg-slate-950 border-slate-800 text-rose-400'} border rounded px-1.5 py-1 font-bold text-xs font-mono" />
                                </div>
                            </div>
                        </div>

                        <div class="grid grid-cols-1 md:grid-cols-2 gap-3 pt-1">
                            <div>
                                <label class="block text-slate-400 text-[10px] mb-1 font-semibold">Problem (Deskripsi Masalah / Kerusakan)*</label>
                                <input type="text" data-trouble-field="description" data-row="${idx}" placeholder="Deskripsi kendala kerusakan / masalah..." value="${row.description || ''}" class="w-full ${isLight ? 'bg-slate-50 border-slate-300 text-slate-900' : 'bg-slate-950 border-slate-800 text-slate-100'} border rounded px-2 py-1 text-xs outline-none focus:border-amber-500 font-sans" />
                            </div>
                            <div>
                                <label class="block text-slate-400 text-[10px] mb-1 font-semibold">Penyebab (Akar Masalah)*</label>
                                <input type="text" data-trouble-field="cause" data-row="${idx}" placeholder="Akar masalah terjadinya kerusakan..." value="${row.cause || ''}" class="w-full ${isLight ? 'bg-slate-50 border-slate-300 text-slate-900' : 'bg-slate-950 border-slate-800 text-slate-100'} border rounded px-2 py-1 text-xs outline-none focus:border-amber-500 font-sans" />
                            </div>
                        </div>

                        <div class="grid grid-cols-1 md:grid-cols-3 gap-3 pt-1">
                            <div class="md:col-span-1">
                                <label class="block text-slate-400 text-[10px] mb-1 font-semibold">Perbaikan (Tindakan CAPA)*</label>
                                <input type="text" data-trouble-field="action_taken" data-row="${idx}" placeholder="Tindakan perbaikan yang dilakukan..." value="${row.action_taken || ''}" class="w-full ${isLight ? 'bg-slate-50 border-slate-300 text-emerald-800' : 'bg-slate-950 border-slate-800 text-emerald-300'} border rounded px-2 py-1 text-xs outline-none focus:border-emerald-500 font-sans" />
                            </div>
                            <div>
                                <label class="block text-slate-400 text-[10px] mb-1 font-semibold">PIC Departemen*</label>
                                <input type="text" data-trouble-field="pic" data-row="${idx}" placeholder="Nama PIC..." value="${row.pic || ''}" class="w-full ${isLight ? 'bg-slate-50 border-slate-300 text-cyan-800' : 'bg-slate-950 border-slate-800 text-cyan-300'} border rounded px-2 py-1 text-xs outline-none focus:border-cyan-500 font-sans font-medium" />
                            </div>
                            <div>
                                <label class="block text-slate-400 text-[10px] mb-1 font-semibold">Status*</label>
                                <select data-trouble-field="status" data-row="${idx}" class="w-full ${isLight ? 'bg-white border-slate-300 text-slate-900' : 'bg-slate-950 border-slate-800 text-slate-100'} border rounded px-2 py-1 text-xs font-bold outline-none focus:border-amber-500">
                                    <option value="CLOSED" ${row.status === 'CLOSED' ? 'selected' : ''}>CLOSED</option>
                                    <option value="OPEN" ${row.status === 'OPEN' ? 'selected' : ''}>OPEN</option>
                                </select>
                            </div>
                        </div>
                    </div>
                `).join('');

                // Problem field change bindings
                troubleContainer.querySelectorAll('[data-trouble-field]').forEach(input => {
                    input.addEventListener('input', (e) => {
                        const field = e.target.getAttribute('data-trouble-field');
                        const rowIdx = parseInt(e.target.getAttribute('data-row'));
                        if (dailyTroubleLogsState[rowIdx]) {
                            dailyTroubleLogsState[rowIdx][field] = e.target.value;

                            if (field === 'team') {
                                dailyTroubleLogsState[rowIdx].leader_name = getLeaderName(e.target.value);
                                if (!dailyTroubleLogsState[rowIdx].pic) {
                                    dailyTroubleLogsState[rowIdx].pic = `${dailyTroubleLogsState[rowIdx].leader_name} (${e.target.value})`;
                                }
                                renderTroubleRows();
                            }

                            if (field === 'start_time' || field === 'end_time') {
                                const st = dailyTroubleLogsState[rowIdx].start_time;
                                const et = dailyTroubleLogsState[rowIdx].end_time;
                                if (st && et) {
                                    const stParts = st.split(':');
                                    const etParts = et.split(':');
                                    let stMin = parseInt(stParts[0]) * 60 + parseInt(stParts[1]);
                                    let etMin = parseInt(etParts[0]) * 60 + parseInt(etParts[1]);
                                    if (etMin < stMin) etMin += 24 * 60;
                                    const diff = Math.max(0, etMin - stMin);
                                    dailyTroubleLogsState[rowIdx].duration_minutes = diff;
                                    renderTroubleRows();
                                }
                            }

                            updateRunTimeAndDowntime();
                        }
                    });
                });

                // Problem delete row bindings
                troubleContainer.querySelectorAll('[data-del-modal-dt-idx]').forEach(btn => {
                    btn.addEventListener('click', (e) => {
                        const idx = parseInt(e.currentTarget.getAttribute('data-del-modal-dt-idx'));
                        dailyTroubleLogsState.splice(idx, 1);
                        renderTroubleRows();
                        updateRunTimeAndDowntime();
                    });
                });
            }

            if (window.lucide) window.lucide.createIcons();
            if (this.currentLang === 'ja') {
                i18n.localizeDom(troubleContainer);
            }
            updateRunTimeAndDowntime();
        };

        if (addTroubleBtn) {
            addTroubleBtn.addEventListener('click', () => {
                const defaultTeam = 'Team A';
                const defaultLeader = getLeaderName(defaultTeam);
                dailyTroubleLogsState.push({
                    team: defaultTeam,
                    leader_name: defaultLeader,
                    problem_type: 'Problem Mesin Mekanik',
                    downtime_reason_id: '',
                    start_time: '08:00',
                    end_time: '',
                    duration_minutes: 0,
                    description: '',
                    cause: '',
                    action_taken: '',
                    pic: `${defaultLeader} (${defaultTeam})`,
                    status: 'CLOSED',
                    is_planned: false
                });
                renderTroubleRows();
            });
        }

        // Change Event Listeners
        if (lineSelect) {
            lineSelect.addEventListener('change', () => {
                filterProductsByLine();
                dailyTroubleLogsState.forEach(r => {
                    r.leader_name = getLeaderName(r.team);
                });
                renderTroubleRows();
            });
        }

        if (productSelect) {
            productSelect.addEventListener('change', calcTargetQty);
        }

        if (shiftSelect) {
            shiftSelect.addEventListener('change', updateShiftCalc);
        }

        if (plannedTimeInp) {
            plannedTimeInp.addEventListener('input', () => {
                calcTargetQty();
                updateRunTimeAndDowntime();
            });
        }

        if (plannedDtInp) {
            plannedDtInp.addEventListener('input', updateRunTimeAndDowntime);
        }

        // 6. INITIAL TRIGGER ON MODAL OPEN
        filterProductsByLine();
        updateShiftCalc();
        calcTargetQty();
        updateRunTimeAndDowntime();
        renderTroubleRows();

        // 7. Form Submit Inside Modal
        const modalForm = document.getElementById('form-modal-daily-entry');
        if (modalForm) {
            modalForm.addEventListener('submit', async (e) => {
                e.preventDefault();
                const formData = new FormData(e.target);
                const data = Object.fromEntries(formData.entries());

                const prodDate = data.production_date || this.selectedDailyDate;
                const validTroubleLogs = dailyTroubleLogsState.filter(r => 
                    (r.duration_minutes > 0) || (r.description && r.description.trim() !== '') || (r.start_time && r.start_time.trim() !== '')
                ).map(r => ({
                    team: r.team || null,
                    problem_type: r.problem_type || 'Problem Mesin Mekanik',
                    downtime_reason_id: r.downtime_reason_id || null,
                    start_time: r.start_time ? `${prodDate} ${r.start_time}:00` : `${prodDate} 08:00:00`,
                    end_time: r.end_time ? `${prodDate} ${r.end_time}:00` : null,
                    duration_minutes: parseFloat(r.duration_minutes || 0),
                    description: r.description || null,
                    cause: r.cause || null,
                    action_taken: r.action_taken || null,
                    pic: r.pic || null,
                    status: (r.status || 'CLOSED').toUpperCase(),
                    is_planned: false
                }));

                data.downtime_logs = validTroubleLogs;

                if (!this.validateProductionForm(data, modalForm)) return;

                try {
                    if (data.production_date) {
                        this.selectedDailyDate = data.production_date;
                    }
                    await api.createProductionRecord(data);
                    this.showNotification('Laporan Produksi Tersimpan', `Data laporan produksi tanggal ${this.selectedDailyDate} beserta ${validTroubleLogs.length} Log Problem berhasil disimpan!`, 'create');
                    closeModal();
                    await this.renderDailyReport();
                } catch (err) {
                    const errorMsg = err.response?.data?.message || err.message || 'Gagal menyimpan laporan produksi';
                    const errorDetail = err.response?.data?.error_detail || '';
                    const params = err.response?.data?.parameters || null;

                    if (errorMsg.includes('tidak boleh sama') || err.response?.status === 422) {
                        let paramListHtml = '';
                        if (params) {
                            paramListHtml = `
                                <div class="mt-3 p-3 bg-slate-950/90 border border-slate-800 rounded-xl space-y-1.5 font-mono text-[11px] text-left">
                                    <div class="text-amber-400 font-bold mb-1 flex items-center gap-1.5">
                                        <i data-lucide="alert-triangle" class="w-3.5 h-3.5 text-amber-400"></i>
                                        Rincian Parameter yang Sudah Terdaftar:
                                    </div>
                                    <div class="grid grid-cols-2 gap-1 text-slate-300">
                                        <div>🏭 Line: <strong class="text-cyan-400 font-sans">${params.line}</strong></div>
                                        <div>⏰ Shift: <strong class="text-cyan-400 font-sans">${params.shift}</strong></div>
                                        <div>📅 Tanggal: <strong class="text-slate-200">${params.tanggal}</strong></div>
                                        <div>⚙️ Mesin: <strong class="text-slate-200">${params.mesin}</strong></div>
                                    </div>
                                    <div class="text-slate-300 truncate">📦 Produk: <strong class="text-emerald-400 font-sans">${params.produk}</strong></div>
                                </div>
                            `;
                        }

                        this.showAlertModal({
                            title: 'Data sudah ditambahkan tidak boleh sama !',
                            subtitle: (errorDetail || 'Kombinasi Line dan Shift pada tanggal tersebut sudah pernah ditambahkan ke sistem.') + paramListHtml,
                            buttonText: 'Mengerti, Ubah Shift / Line'
                        });
                    } else {
                        this.showNotification('⚠️ Gagal Menyimpan', errorMsg, 'delete');
                    }
                }
            });
        }
    }

    // ==========================================
    // 3B-2. MODAL POPUP: CREATE STANDALONE TROUBLE LOG
    // ==========================================
    showCreateDailyTroubleModal() {
        const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');
        const defaultLineId = this.filters.line_id || this.masterData.lines?.[0]?.id || '';
        const defaultShiftId = this.filters.shift_id || this.masterData.shifts?.[0]?.id || '';
        const curDate = this.selectedDailyDate || new Date().toISOString().slice(0, 10);

        const picOptions = [
            'Leader Team A (Alim Utama)',
            'Leader Team B (Dadi)',
            'Leader Team C (Karyanto)',
            'Leader Team D (Kurniawan)',
            'Maintenance Mekanik',
            'Maintenance Elektrik',
            'Tooling & Dies Team',
            'Quality Control (QC Inspector)',
            'Operator Mesin (OP)',
            'Supervisor Produksi',
            ...(this.masterData.groups || []).map(g => `${g.leader_name} (${g.team})`),
        ];
        const uniquePicOptions = [...new Set(picOptions)];

        const modalHtml = `
            <div id="modal-create-standalone-trouble" class="fixed inset-0 z-50 flex items-center justify-center p-3 sm:p-5 bg-slate-950/75 backdrop-blur-md overflow-y-auto">
                <div class="relative w-full max-w-2xl max-h-[92vh] overflow-y-auto ${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-2xl shadow-2xl p-6 space-y-4 my-auto font-sans">
                    
                    <div class="flex items-center justify-between pb-3 border-b ${isLight ? 'border-slate-200' : 'border-slate-800'}">
                        <div class="flex items-center gap-3">
                            <div class="w-9 h-9 rounded-xl bg-amber-500/15 border border-amber-500/30 flex items-center justify-center text-amber-400">
                                <i data-lucide="alert-triangle" class="w-5 h-5"></i>
                            </div>
                            <div>
                                <h3 class="font-bold text-base ${isLight ? 'text-slate-900' : 'text-slate-100'}">Tambah Problem Log & Downtime</h3>
                                <p class="text-xs ${isLight ? 'text-slate-500' : 'text-slate-400'}">Pencatatan Masalah/Problem Mesin & Downtime Lini Produksi</p>
                            </div>
                        </div>
                        <button type="button" id="btn-close-modal-standalone-dt" class="w-8 h-8 rounded-lg ${isLight ? 'bg-slate-100 hover:bg-slate-200 text-slate-600' : 'bg-slate-800 hover:bg-slate-700 text-slate-400 hover:text-slate-200'} flex items-center justify-center transition-colors cursor-pointer">
                            <i data-lucide="x" class="w-4 h-4"></i>
                        </button>
                    </div>

                    <form id="form-standalone-trouble" class="space-y-3.5 text-xs">
                        <datalist id="standalone-problem-pic-list">
                            ${uniquePicOptions.map(p => `<option value="${p}"></option>`).join('')}
                        </datalist>

                        <!-- ROW 1: Tanggal*, Line*, Shift*, Team* -->
                        <div class="grid grid-cols-1 sm:grid-cols-4 gap-2.5">
                            <div>
                                <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold">Tanggal*</label>
                                <input type="date" name="production_date" value="${curDate}" required class="w-full ${isLight ? 'bg-white border-slate-300 text-slate-900' : 'bg-slate-950 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-mono outline-none focus:border-amber-500" />
                            </div>
                            <div>
                                <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold">Line*</label>
                                <select name="production_line_id" required class="w-full ${isLight ? 'bg-white border-slate-300 text-slate-900' : 'bg-slate-950 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-bold text-cyan-600 dark:text-cyan-400 outline-none focus:border-amber-500">
                                    ${this.masterData.lines.map(l => `<option value="${l.id}" ${l.id == defaultLineId ? 'selected' : ''}>${l.name}</option>`).join('')}
                                </select>
                            </div>
                            <div>
                                <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold">Shift*</label>
                                <select name="shift_id" required class="w-full ${isLight ? 'bg-white border-slate-300 text-slate-900' : 'bg-slate-950 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-semibold outline-none focus:border-amber-500">
                                    ${this.masterData.shifts.map(s => `<option value="${s.id}" ${s.id == defaultShiftId ? 'selected' : ''}>${s.name}</option>`).join('')}
                                </select>
                            </div>
                            <div>
                                <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold">Team*</label>
                                <select name="team" required class="w-full ${isLight ? 'bg-white border-slate-300 text-slate-900' : 'bg-slate-950 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-bold text-amber-500 outline-none focus:border-amber-500">
                                    <option value="Team A">Team A</option>
                                    <option value="Team B">Team B</option>
                                    <option value="Team C">Team C</option>
                                    <option value="Team D">Team D</option>
                                </select>
                            </div>
                        </div>

                        <!-- ROW 2: Type Produk / SKU*, OP Mesin*, Kategori Problem* -->
                        <div class="grid grid-cols-1 sm:grid-cols-3 gap-2.5">
                            <div>
                                <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold">Type Produk / SKU*</label>
                                <select name="product_id" required class="w-full ${isLight ? 'bg-white border-slate-300 text-slate-900' : 'bg-slate-950 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-medium outline-none focus:border-amber-500">
                                    <!-- Populated dynamically per line -->
                                </select>
                            </div>
                            <div>
                                <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold">OP Mesin*</label>
                                <select name="machine_id" required class="w-full ${isLight ? 'bg-white border-slate-300 text-slate-900' : 'bg-slate-950 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-mono outline-none focus:border-amber-500">
                                    <!-- Populated dynamically per line (Measuring MC first) -->
                                </select>
                            </div>
                            <div>
                                <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold">Kategori Problem*</label>
                                <select name="problem_type" required class="w-full ${isLight ? 'bg-white border-slate-300 text-slate-900' : 'bg-slate-950 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-bold outline-none focus:border-amber-500">
                                    <option value="Inventory">Inventory</option>
                                    <option value="Others">Others</option>
                                    <option value="Planning Downtime">Planning Downtime</option>
                                    <option value="Problem Mesin Elektrik">Problem Mesin Elektrik</option>
                                    <option value="Problem Mesin Mekanik" selected>Problem Mesin Mekanik</option>
                                    <option value="Problem Tool">Problem Tool</option>
                                </select>
                            </div>
                        </div>

                        <!-- ROW 3: Problem* -->
                        <div>
                            <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold">Problem* (Deskripsi Masalah / Kerusakan)</label>
                            <input type="text" name="description" placeholder="Jelaskan kendala mesin / tooling / kerusakan yang terjadi..." required class="w-full ${isLight ? 'bg-white border-slate-300 text-slate-900' : 'bg-slate-950 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 outline-none focus:border-amber-500 font-sans" />
                        </div>

                        <!-- ROW 4: Penyebab* -->
                        <div>
                            <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold">Penyebab* (Akar Masalah / Root Cause)</label>
                            <input type="text" name="cause" placeholder="Jelaskan akar penyebab terjadinya problem..." required class="w-full ${isLight ? 'bg-white border-slate-300 text-slate-900' : 'bg-slate-950 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 outline-none focus:border-amber-500 font-sans" />
                        </div>

                        <!-- ROW 5: Perbaikan* -->
                        <div>
                            <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold">Perbaikan* (Tindakan Penanganan / CAPA)</label>
                            <input type="text" name="action_taken" placeholder="Tindakan perbaikan dan countermeasure yang dilakukan..." required class="w-full ${isLight ? 'bg-white border-slate-300 text-emerald-700' : 'bg-slate-950 border-slate-800 text-emerald-300'} border rounded-lg px-2.5 py-2 outline-none focus:border-emerald-500 font-sans" />
                        </div>

                        <!-- ROW 6: Jam Mulai, Jam Selesai & Losstime (Wajib Angka)* -->
                        <div class="grid grid-cols-1 sm:grid-cols-3 gap-2.5">
                            <div>
                                <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-medium">Jam Mulai</label>
                                <input type="time" name="start_time" value="08:00" required class="w-full ${isLight ? 'bg-white border-slate-300 text-slate-900' : 'bg-slate-950 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-mono outline-none focus:border-amber-500" />
                            </div>
                            <div>
                                <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-medium">Jam Selesai</label>
                                <input type="time" name="end_time" class="w-full ${isLight ? 'bg-white border-slate-300 text-slate-900' : 'bg-slate-950 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-mono outline-none focus:border-amber-500" />
                            </div>
                            <div>
                                <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold flex items-center justify-between">
                                    <span>Losstime (Menit)*</span>
                                    <span class="text-[10px] text-amber-400 font-mono font-bold">(Wajib Angka)</span>
                                </label>
                                <input type="number" name="duration_minutes" min="1" step="1" placeholder="0" required class="w-full ${isLight ? 'bg-white border-slate-300 text-rose-600' : 'bg-slate-950 border-slate-800 text-rose-400'} border rounded-lg px-2.5 py-2 font-mono font-bold outline-none focus:border-amber-500" />
                            </div>
                        </div>

                        <!-- ROW 7: PIC (Daftar Nama PIC Departemen)* & Status (Closed / Open)* -->
                        <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
                            <div>
                                <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold">PIC (Daftar Nama PIC Departemen)*</label>
                                <input type="text" name="pic" list="standalone-problem-pic-list" placeholder="Pilih atau ketik PIC Departemen..." required class="w-full ${isLight ? 'bg-white border-slate-300 text-cyan-800' : 'bg-slate-950 border-slate-800 text-cyan-300'} border rounded-lg px-2.5 py-2 font-medium outline-none focus:border-cyan-500" />
                            </div>
                            <div>
                                <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold">Status (Closed / Open)*</label>
                                <select name="status" required class="w-full ${isLight ? 'bg-white border-slate-300 text-slate-900' : 'bg-slate-950 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-bold outline-none focus:border-amber-500">
                                    <option value="CLOSED">CLOSED</option>
                                    <option value="OPEN">OPEN</option>
                                </select>
                            </div>
                        </div>

                        <div class="pt-3 border-t ${isLight ? 'border-slate-200' : 'border-slate-800'} flex items-center justify-end gap-2.5">
                            <button type="button" id="btn-cancel-modal-standalone-dt" class="px-4 py-2 rounded-xl border ${isLight ? 'border-slate-300 hover:bg-slate-100 text-slate-700' : 'border-slate-700 hover:bg-slate-800 text-slate-300'} text-xs font-semibold cursor-pointer">
                                Batal
                            </button>
                            <button type="submit" class="px-5 py-2.5 bg-amber-600 hover:bg-amber-500 text-white font-bold rounded-xl shadow-lg shadow-amber-600/30 text-xs cursor-pointer flex items-center gap-1.5 transition-all">
                                <i data-lucide="save" class="w-4 h-4"></i>
                                <span>Simpan Problem Log</span>
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        `;

        let existingModal = document.getElementById('modal-create-standalone-trouble');
        if (existingModal) existingModal.remove();

        const modalWrapper = document.createElement('div');
        modalWrapper.innerHTML = modalHtml;
        document.body.appendChild(modalWrapper.firstElementChild);

        if (window.lucide) window.lucide.createIcons();

        const activeModal = document.getElementById('modal-create-standalone-trouble');
        if (this.currentLang === 'ja' && activeModal) {
            i18n.localizeDom(activeModal);
        }
        const closeModal = () => {
            if (activeModal) activeModal.remove();
        };

        document.getElementById('btn-close-modal-standalone-dt')?.addEventListener('click', closeModal);
        document.getElementById('btn-cancel-modal-standalone-dt')?.addEventListener('click', closeModal);

        const formDt = document.getElementById('form-standalone-trouble');
        const lineSelect = formDt.querySelector('select[name="production_line_id"]');
        const machineSelect = formDt.querySelector('select[name="machine_id"]');
        const productSelect = formDt.querySelector('select[name="product_id"]');
        const teamSelect = formDt.querySelector('select[name="team"]');
        const picInput = formDt.querySelector('input[name="pic"]');
        const startTimeInp = formDt.querySelector('input[name="start_time"]');
        const endTimeInp = formDt.querySelector('input[name="end_time"]');
        const durationInp = formDt.querySelector('input[name="duration_minutes"]');

        const getLeaderName = (lineId, teamVal) => {
            const match = (this.masterData.groups || []).find(g => 
                g.production_line_id == lineId && 
                (g.team === teamVal || g.team.toLowerCase().includes((teamVal || '').toLowerCase()) || (teamVal || '').toLowerCase().includes(g.team.toLowerCase()))
            );
            return match ? match.leader_name : '';
        };

        const updatePicSuggestion = () => {
            if (lineSelect && teamSelect && picInput && !picInput.value) {
                const leader = getLeaderName(lineSelect.value, teamSelect.value);
                if (leader) {
                    picInput.value = `${leader} (${teamSelect.value})`;
                }
            }
        };

        const updateMachinesAndProducts = () => {
            if (!lineSelect) return;
            const lineId = lineSelect.value;
            
            // Machines
            const lineMachines = this.getMachinesForLine(lineId);
            if (machineSelect) {
                machineSelect.innerHTML = lineMachines.map(m => 
                    `<option value="${m.id}">${m.name} (${m.code})</option>`
                ).join('');

                const measuringMC = lineMachines.find(m => 
                    (m.name && m.name.toLowerCase().includes('measuring')) || 
                    (m.code && m.code.toLowerCase().includes('measuring'))
                );
                if (measuringMC) {
                    machineSelect.value = measuringMC.id;
                } else if (lineMachines.length > 0) {
                    machineSelect.value = lineMachines[0].id;
                }
            }

            // Products
            const prods = this.getProductsForLine(lineId);
            const allProds = prods.length > 0 ? prods : (this.masterData.products || []);
            if (productSelect) {
                productSelect.innerHTML = allProds.map(p => `<option value="${p.id}">${p.name} — SKU: ${p.sku}</option>`).join('');
            }

            updatePicSuggestion();
        };

        const updateAutoDuration = () => {
            if (startTimeInp && endTimeInp && durationInp) {
                const s = startTimeInp.value;
                const e = endTimeInp.value;
                if (s && e) {
                    const sParts = s.split(':');
                    const eParts = e.split(':');
                    let sMin = parseInt(sParts[0]) * 60 + parseInt(sParts[1]);
                    let eMin = parseInt(eParts[0]) * 60 + parseInt(eParts[1]);
                    if (eMin < sMin) eMin += 24 * 60;
                    durationInp.value = Math.max(1, eMin - sMin);
                }
            }
        };

        lineSelect?.addEventListener('change', updateMachinesAndProducts);
        teamSelect?.addEventListener('change', updatePicSuggestion);
        startTimeInp?.addEventListener('input', updateAutoDuration);
        endTimeInp?.addEventListener('input', updateAutoDuration);

        // Initial setup
        updateMachinesAndProducts();
        updatePicSuggestion();

        if (formDt) {
            formDt.addEventListener('submit', async (e) => {
                e.preventDefault();
                const formData = new FormData(e.target);
                const data = Object.fromEntries(formData.entries());

                const durationMins = parseFloat(data.duration_minutes || 0);
                if (isNaN(durationMins) || durationMins <= 0) {
                    this.showNotification('⚠️ Losstime Wajib Diisi', 'Durasi losstime harus berupa angka menit yang lebih besar dari 0.', 'delete');
                    return;
                }

                const targetDate = data.production_date || this.selectedDailyDate || new Date().toISOString().slice(0, 10);
                const startTimeVal = data.start_time || '08:00';

                const payload = {
                    production_line_id: data.production_line_id,
                    machine_id: data.machine_id,
                    shift_id: data.shift_id,
                    product_id: data.product_id || null,
                    team: data.team || null,
                    problem_type: data.problem_type || 'Problem Mesin Mekanik',
                    downtime_reason_id: data.downtime_reason_id || null,
                    start_time: `${targetDate} ${startTimeVal}:00`,
                    end_time: data.end_time ? `${targetDate} ${data.end_time}:00` : null,
                    duration_minutes: durationMins,
                    description: data.description,
                    cause: data.cause || null,
                    action_taken: data.action_taken || null,
                    pic: data.pic || null,
                    status: (data.status || 'CLOSED').toUpperCase(),
                    is_planned: false,
                };

                try {
                    await api.createDowntime(payload);
                    this.showNotification('Problem Log Tersimpan', `Log problem sebesar ${durationMins} menit berhasil dicatat pada tanggal ${targetDate}!`, 'create');
                    closeModal();
                    this.selectedDailyDate = targetDate;
                    await this.renderDailyReport();
                } catch (err) {
                    const errorMsg = err.response?.data?.message || err.message || 'Gagal menyimpan data problem log.';
                    this.showNotification('⚠️ Gagal Menyimpan', errorMsg, 'delete');
                }
            });
        }
    }

    showCreateDailyProblemModal() {
        return this.showCreateDailyTroubleModal();
    }

    // ==========================================
    // 3C. INPUT LAPORAN NG (DEFECT DETAIL ENTRY)
    // ==========================================
    async renderNgReport() {
        this.ngFilters = this.ngFilters || { date: '', line_id: '', machine_id: '', shift_id: '', status: 'all', search: '' };

        const res = await api.getNgQueue(this.ngFilters);
        const summary = res.data.summary || {
            total_reports_with_ng: 0,
            pending_count: 0,
            completed_count: 0,
            total_ng_target_pcs: 0,
            total_ng_oee_pcs: 0,
            total_ng_non_oee_pcs: 0,
            breakdown_oee: { assy: 0, rod: 0, cap: 0 },
            breakdown_non_oee: { bolt: 0, bush: 0, nut: 0, pin: 0 },
        };
        let items = res.data.data || [];

        // Client-side search filtering (if search text is provided)
        if (this.ngFilters.search && this.ngFilters.search.trim() !== '') {
            const query = this.ngFilters.search.toLowerCase().trim();
            items = items.filter(item => 
                (item.line_name || '').toLowerCase().includes(query) ||
                (item.line_code || '').toLowerCase().includes(query) ||
                (item.machine_name || '').toLowerCase().includes(query) ||
                (item.machine_code || '').toLowerCase().includes(query) ||
                (item.product_name || '').toLowerCase().includes(query) ||
                (item.product_sku || '').toLowerCase().includes(query) ||
                (item.operator_name || '').toLowerCase().includes(query)
            );
        }

        // Group items by Line (production_line_id / line_name) for multi-shift/batch collapsible view
        const ngLineGroupsMap = new Map();
        items.forEach(item => {
            const lineId = item.production_line_id || item.line_id || item.line_code || 0;
            const lineName = item.line_name || `Line #${lineId}`;
            const lineCode = item.line_code || '';
            if (!ngLineGroupsMap.has(lineId)) {
                ngLineGroupsMap.set(lineId, {
                    lineId,
                    lineName,
                    lineCode,
                    items: []
                });
            }
            ngLineGroupsMap.get(lineId).items.push(item);
        });
        const ngLineGroups = Array.from(ngLineGroupsMap.values());

        const linesOptions = (this.masterData.lines || []).map(l => 
            `<option value="${l.id}" ${this.ngFilters.line_id == l.id ? 'selected' : ''}>${l.code} - ${l.name}</option>`
        ).join('');

        const shiftsOptions = (this.masterData.shifts || []).map(s => 
            `<option value="${s.id}" ${this.ngFilters.shift_id == s.id ? 'selected' : ''}>${s.name}</option>`
        ).join('');

        const content = document.getElementById('content-body');
        const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');

        content.innerHTML = `
            <!-- HEADER CONTEXT & FILTERS -->
            <div class="flex flex-wrap items-center justify-between gap-4 mb-6">
                <div>
                    <h2 class="text-xl font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'} flex items-center gap-2">
                        <i data-lucide="shield-alert" class="w-5 h-5 text-rose-500"></i>
                        <span>Input Laporan NG</span>
                    </h2>
                </div>

                <!-- FILTER CONTROLS -->
                <div class="flex flex-wrap items-center gap-2.5">
                    <!-- DATE FILTER -->
                    <div class="flex items-center gap-1.5 ${isLight ? 'bg-white border-slate-200 shadow-sm' : 'bg-slate-900 border-slate-800'} border rounded-lg px-2.5 py-1.5 text-slate-500">
                        <i data-lucide="calendar" class="w-3.5 h-3.5 text-cyan-500"></i>
                        <input type="date" id="filter-ng-date" value="${this.ngFilters.date || ''}" class="bg-transparent ${isLight ? 'text-slate-800' : 'text-slate-100'} text-xs focus:outline-none cursor-pointer font-sans" />
                    </div>

                    <!-- LINE -->
                    <div class="flex items-center gap-1.5 ${isLight ? 'bg-white border-slate-200 shadow-sm' : 'bg-slate-900 border-slate-800'} border rounded-lg px-2.5 py-1.5 text-slate-500">
                        <i data-lucide="git-branch" class="w-3.5 h-3.5 text-emerald-500"></i>
                        <select id="filter-ng-line" class="bg-transparent ${isLight ? 'text-slate-800' : 'text-slate-100'} text-xs focus:outline-none cursor-pointer font-sans">
                            <option value="">Semua Lini Produksi</option>
                            ${linesOptions}
                        </select>
                    </div>

                    <!-- SHIFT -->
                    <div class="flex items-center gap-1.5 ${isLight ? 'bg-white border-slate-200 shadow-sm' : 'bg-slate-900 border-slate-800'} border rounded-lg px-2.5 py-1.5 text-slate-500">
                        <i data-lucide="clock" class="w-3.5 h-3.5 text-amber-500"></i>
                        <select id="filter-ng-shift" class="bg-transparent ${isLight ? 'text-slate-800' : 'text-slate-100'} text-xs focus:outline-none cursor-pointer font-sans">
                            <option value="">Semua Shift</option>
                            ${shiftsOptions}
                        </select>
                    </div>

                    <!-- STATUS -->
                    <div class="flex items-center gap-1.5 ${isLight ? 'bg-white border-slate-200 shadow-sm' : 'bg-slate-900 border-slate-800'} border rounded-lg px-2.5 py-1.5 text-slate-500">
                        <i data-lucide="check-circle" class="w-3.5 h-3.5 text-purple-500"></i>
                        <select id="filter-ng-status" class="bg-transparent ${isLight ? 'text-slate-800' : 'text-slate-100'} text-xs focus:outline-none cursor-pointer font-sans font-medium">
                            <option value="all" ${this.ngFilters.status === 'all' ? 'selected' : ''}>Semua Status (${summary.total_reports_with_ng})</option>
                            <option value="pending" class="text-amber-600 font-bold" ${this.ngFilters.status === 'pending' ? 'selected' : ''}>🟡 Belum Diisi (${summary.pending_count})</option>
                            <option value="completed" class="text-emerald-600 font-bold" ${this.ngFilters.status === 'completed' ? 'selected' : ''}>🟢 Selesai Terisi (${summary.completed_count})</option>
                        </select>
                    </div>

                    <!-- RESET / REFRESH -->
                    <button id="btn-reset-ng-filters" class="p-2 ${isLight ? 'bg-white hover:bg-slate-100 border-slate-200 text-slate-700 shadow-sm' : 'bg-slate-800 hover:bg-slate-700 text-slate-300'} border rounded-lg transition-colors cursor-pointer" title="Reset Filter">
                        <i data-lucide="rotate-ccw" class="w-3.5 h-3.5"></i>
                    </button>
                </div>
            </div>

            <!-- KPI SUMMARY CARDS (STATISTIK ANTRIAN NG & BREAKDOWN) -->
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
                <!-- CARD 1: TOTAL LAPORAN BER-NG -->
                <div class="${isLight ? 'bg-gradient-to-br from-rose-50/90 via-white to-slate-50/90 border-rose-200 shadow-lg shadow-rose-100/50' : 'bg-gradient-to-br from-rose-950/40 via-slate-900 to-slate-900 border-rose-800/40 shadow-xl'} border rounded-2xl p-4 flex flex-col justify-between transition-all">
                    <div class="flex items-center justify-between">
                        <div>
                            <span class="text-[11px] font-bold ${isLight ? 'text-rose-800' : 'text-rose-300'} uppercase tracking-wider">Antrean Laporan NG</span>
                            <div class="text-2xl font-black ${isLight ? 'text-slate-900' : 'text-white'} font-mono mt-0.5">${summary.total_reports_with_ng} <span class="text-xs font-sans ${isLight ? 'text-slate-500 font-medium' : 'text-slate-400 font-normal'}">Laporan</span></div>
                        </div>
                        <div class="w-10 h-10 rounded-xl ${isLight ? 'bg-rose-100 border-rose-200 text-rose-600 shadow-sm' : 'bg-rose-900/40 border-rose-700/50 text-rose-400'} border flex items-center justify-center">
                            <i data-lucide="alert-octagon" class="w-5 h-5"></i>
                        </div>
                    </div>
                    <div class="mt-3 pt-2.5 border-t ${isLight ? 'border-rose-100 text-slate-600' : 'border-slate-800 text-slate-400'} flex items-center justify-between text-[11px] font-mono">
                        <span>Total Target NG:</span>
                        <strong class="${isLight ? 'text-rose-700' : 'text-rose-400'} font-bold">${summary.total_ng_target_pcs.toLocaleString()} Pcs</strong>
                    </div>
                </div>

                <!-- CARD 2: STATUS PENGISIAN (PENDING VS COMPLETED) -->
                <div class="${isLight ? 'bg-gradient-to-br from-amber-50/90 via-white to-slate-50/90 border-amber-200 shadow-lg shadow-amber-100/50' : 'bg-slate-900 border-slate-800 shadow-xl'} border rounded-2xl p-4 flex flex-col justify-between transition-all">
                    <div class="flex items-center justify-between">
                        <div>
                            <span class="text-[11px] font-bold ${isLight ? 'text-amber-800' : 'text-amber-400'} uppercase tracking-wider">Status Input Detail</span>
                            <div class="text-2xl font-black ${isLight ? 'text-slate-900' : 'text-white'} font-mono mt-0.5">${summary.pending_count} <span class="text-xs font-sans ${isLight ? 'text-amber-700' : 'text-amber-400'} font-bold">Pending</span></div>
                        </div>
                        <div class="w-10 h-10 rounded-xl ${isLight ? 'bg-amber-100 border-amber-200 text-amber-700 shadow-sm' : 'bg-amber-950/60 border-amber-800/60 text-amber-400'} border flex items-center justify-center">
                            <i data-lucide="clock" class="w-5 h-5"></i>
                        </div>
                    </div>
                    <div class="mt-3 pt-2.5 border-t ${isLight ? 'border-amber-100 text-slate-600' : 'border-slate-800 text-slate-400'} flex items-center justify-between text-[11px]">
                        <span>Sudah Lengkap:</span>
                        <span class="${isLight ? 'text-emerald-700' : 'text-emerald-400'} font-mono font-bold">🟢 ${summary.completed_count} Laporan</span>
                    </div>
                </div>

                <!-- CARD 3: BREAKDOWN KOMPONEN WAJIB (OEE) -->
                <div class="${isLight ? 'bg-gradient-to-br from-cyan-50/90 via-white to-slate-50/90 border-cyan-200 shadow-lg shadow-cyan-100/50' : 'bg-slate-900 border-cyan-800/40 shadow-xl'} border rounded-2xl p-4 flex flex-col justify-between transition-all">
                    <div class="flex items-center justify-between">
                        <div>
                            <span class="text-[11px] font-bold ${isLight ? 'text-cyan-800' : 'text-cyan-400'} uppercase tracking-wider">Komponen Wajib (OEE)</span>
                            <div class="text-xl font-black ${isLight ? 'text-slate-900' : 'text-white'} font-mono mt-0.5">${summary.total_ng_oee_pcs.toLocaleString()} <span class="text-xs font-sans ${isLight ? 'text-slate-500 font-medium' : 'text-slate-400 font-normal'}">Pcs</span></div>
                        </div>
                        <div class="w-9 h-9 rounded-xl ${isLight ? 'bg-cyan-100 border-cyan-200 text-cyan-700 shadow-sm' : 'bg-cyan-950/60 border-cyan-800/60 text-cyan-400'} border flex items-center justify-center">
                            <i data-lucide="cpu" class="w-4 h-4"></i>
                        </div>
                    </div>
                    <div class="mt-3 pt-2.5 border-t ${isLight ? 'border-cyan-100' : 'border-slate-800'} grid grid-cols-3 gap-1 text-[10px] ${isLight ? 'text-slate-600' : 'text-slate-400'} font-mono text-center">
                        <div class="${isLight ? 'bg-white border-cyan-200/80 shadow-xs' : 'bg-slate-950 border-slate-800'} p-1 rounded border">
                            <span class="${isLight ? 'text-slate-500' : 'text-slate-500'} block">Assy</span>
                            <strong class="${isLight ? 'text-cyan-800 font-bold' : 'text-cyan-300 font-bold'}">${summary.breakdown_oee.assy}</strong>
                        </div>
                        <div class="${isLight ? 'bg-white border-cyan-200/80 shadow-xs' : 'bg-slate-950 border-slate-800'} p-1 rounded border">
                            <span class="${isLight ? 'text-slate-500' : 'text-slate-500'} block">Rod</span>
                            <strong class="${isLight ? 'text-cyan-800 font-bold' : 'text-cyan-300 font-bold'}">${summary.breakdown_oee.rod}</strong>
                        </div>
                        <div class="${isLight ? 'bg-white border-cyan-200/80 shadow-xs' : 'bg-slate-950 border-slate-800'} p-1 rounded border">
                            <span class="${isLight ? 'text-slate-500' : 'text-slate-500'} block">Cap</span>
                            <strong class="${isLight ? 'text-cyan-800 font-bold' : 'text-cyan-300 font-bold'}">${summary.breakdown_oee.cap}</strong>
                        </div>
                    </div>
                </div>

                <!-- CARD 4: BREAKDOWN PELENGKAP (OPSIONAL NON-OEE) -->
                <div class="${isLight ? 'bg-gradient-to-br from-purple-50/90 via-white to-slate-50/90 border-purple-200 shadow-lg shadow-purple-100/50' : 'bg-slate-900 border-slate-800 shadow-xl'} border rounded-2xl p-4 flex flex-col justify-between transition-all">
                    <div class="flex items-center justify-between">
                        <div>
                            <span class="text-[11px] font-bold ${isLight ? 'text-purple-800' : 'text-purple-400'} uppercase tracking-wider">Pelengkap (Non-OEE)</span>
                            <div class="text-xl font-black ${isLight ? 'text-slate-900' : 'text-white'} font-mono mt-0.5">${summary.total_ng_non_oee_pcs.toLocaleString()} <span class="text-xs font-sans ${isLight ? 'text-slate-500 font-medium' : 'text-slate-400 font-normal'}">Pcs</span></div>
                        </div>
                        <div class="w-9 h-9 rounded-xl ${isLight ? 'bg-purple-100 border-purple-200 text-purple-700 shadow-sm' : 'bg-purple-950/60 border-purple-800/60 text-purple-400'} border flex items-center justify-center">
                            <i data-lucide="layers" class="w-4 h-4"></i>
                        </div>
                    </div>
                    <div class="mt-3 pt-2.5 border-t ${isLight ? 'border-purple-100' : 'border-slate-800'} grid grid-cols-4 gap-1 text-[9px] ${isLight ? 'text-slate-600' : 'text-slate-400'} font-mono text-center">
                        <div class="${isLight ? 'bg-white border-purple-200/80 shadow-xs' : 'bg-slate-950 border-slate-800'} p-1 rounded border">
                            <span class="${isLight ? 'text-slate-500' : 'text-slate-500'} block">Bolt</span>
                            <strong class="${isLight ? 'text-purple-800 font-bold' : 'text-purple-300'}">${summary.breakdown_non_oee.bolt}</strong>
                        </div>
                        <div class="${isLight ? 'bg-white border-purple-200/80 shadow-xs' : 'bg-slate-950 border-slate-800'} p-1 rounded border">
                            <span class="${isLight ? 'text-slate-500' : 'text-slate-500'} block">Bush</span>
                            <strong class="${isLight ? 'text-purple-800 font-bold' : 'text-purple-300'}">${summary.breakdown_non_oee.bush}</strong>
                        </div>
                        <div class="${isLight ? 'bg-white border-purple-200/80 shadow-xs' : 'bg-slate-950 border-slate-800'} p-1 rounded border">
                            <span class="${isLight ? 'text-slate-500' : 'text-slate-500'} block">Nut</span>
                            <strong class="${isLight ? 'text-purple-800 font-bold' : 'text-purple-300'}">${summary.breakdown_non_oee.nut}</strong>
                        </div>
                        <div class="${isLight ? 'bg-white border-purple-200/80 shadow-xs' : 'bg-slate-950 border-slate-800'} p-1 rounded border">
                            <span class="${isLight ? 'text-slate-500' : 'text-slate-500'} block">Pin</span>
                            <strong class="${isLight ? 'text-purple-800 font-bold' : 'text-purple-300'}">${summary.breakdown_non_oee.pin}</strong>
                        </div>
                    </div>
                </div>
            </div>

            <!-- WORK QUEUE TABLE (DAFTAR LAPORAN HARIAN BER-NG - GROUPED BY LINE & COLLAPSIBLE) -->
            <div class="${isLight ? 'bg-white border-slate-200 shadow-xl' : 'bg-slate-900 border-slate-800 shadow-xl'} border rounded-2xl p-5 space-y-4">
                <div class="flex flex-wrap items-center justify-between gap-3 border-b ${isLight ? 'border-slate-200' : 'border-slate-800'} pb-3">
                    <div>
                        <h3 class="text-sm font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'} flex items-center gap-2">
                            <i data-lucide="list-checks" class="w-4 h-4 text-cyan-500"></i>
                            Daftar Antrean Laporan Harian yang Memiliki Reject / Defect
                        </h3>
                    </div>

                    <div class="flex flex-wrap items-center gap-2">
                        <!-- EXPAND / COLLAPSE ALL BUTTONS FOR NG TABLE -->
                        <div class="flex items-center gap-1 ${isLight ? 'bg-slate-100 border-slate-300' : 'bg-slate-950 border-slate-800'} border rounded-xl p-1 shrink-0">
                            <button id="btn-expand-all-ng-lines" title="Buka seluruh laporan NG semua line" class="px-2.5 py-1 text-[11px] font-bold ${isLight ? 'text-slate-700 hover:bg-white' : 'text-slate-300 hover:bg-slate-800'} rounded-lg transition-all flex items-center gap-1 cursor-pointer">
                                <i data-lucide="chevrons-down" class="w-3.5 h-3.5 text-rose-500"></i> Expand All
                            </button>
                            <button id="btn-collapse-all-ng-lines" title="Tutup rincian shift (hanya tampilkan ringkasan Line)" class="px-2.5 py-1 text-[11px] font-bold ${isLight ? 'text-slate-700 hover:bg-white' : 'text-slate-300 hover:bg-slate-800'} rounded-lg transition-all flex items-center gap-1 cursor-pointer">
                                <i data-lucide="chevrons-up" class="w-3.5 h-3.5 text-slate-400"></i> Collapse All
                            </button>
                        </div>

                        <!-- SEARCH BAR -->
                        <div class="relative w-64 sm:w-72">
                            <i data-lucide="search" class="w-3.5 h-3.5 absolute left-3 top-1/2 transform -translate-y-1/2 text-slate-400"></i>
                            <input type="text" id="input-search-ng" value="${this.ngFilters.search || ''}" placeholder="Cari Lini, Mesin, SKU, Produk..." class="w-full ${isLight ? 'bg-slate-50 border-slate-200 text-slate-800 focus:border-cyan-600' : 'bg-slate-950 border-slate-800 text-slate-200 focus:border-cyan-500'} border rounded-lg pl-9 pr-3 py-1.5 text-xs focus:outline-none font-sans" />
                        </div>
                    </div>
                </div>

                <div class="overflow-x-auto">
                    <table class="w-full text-left text-xs">
                        <thead class="${isLight ? 'bg-slate-100 border-slate-200 text-slate-700' : 'bg-slate-950 border-slate-800 text-slate-400'} uppercase font-semibold text-[10px] border-b">
                            <tr>
                                <th class="p-3 text-center w-10">No</th>
                                <th class="p-3">Tanggal & Shift</th>
                                <th class="p-3">Lini & Mesin</th>
                                <th class="p-3">Produk - SKU</th>
                                <th class="p-3 text-right">Total Output</th>
                                <th class="p-3 text-right">Target NG</th>
                                <th class="p-3">Rincian Komponen Wajib (OEE)</th>
                                <th class="p-3">Pelengkap (Non-OEE)</th>
                                <th class="p-3 text-center">Status Input</th>
                                <th class="p-3 text-center w-28">Aksi</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y ${isLight ? 'divide-slate-200 text-slate-800' : 'divide-slate-800/60 text-slate-200'} font-mono">
                            ${ngLineGroups.length === 0 ? `
                                <tr>
                                    <td colspan="10" class="p-8 text-center text-slate-500 font-sans">
                                        <div class="w-12 h-12 rounded-full ${isLight ? 'bg-slate-100 text-slate-500' : 'bg-slate-950 text-slate-600'} flex items-center justify-center mx-auto mb-2">
                                            <i data-lucide="check-circle-2" class="w-6 h-6 text-emerald-500"></i>
                                        </div>
                                        <div class="font-bold ${isLight ? 'text-slate-800' : 'text-slate-300'}">Tidak ada antrean laporan NG</div>
                                        <div class="text-[11px] text-slate-500 mt-0.5">Semua laporan harian pada filter ini memiliki 0 reject atau detail NG telah selesai diinput.</div>
                                    </td>
                                </tr>
                            ` : ngLineGroups.map((grp, gIdx) => {
                                const grpTotalOutput = grp.items.reduce((s, it) => s + (it.total_output || 0), 0);
                                const grpGoodQty = grp.items.reduce((s, it) => s + (it.good_quantity || 0), 0);
                                const grpTargetNg = grp.items.reduce((s, it) => s + (it.total_ng_target || 0), 0);
                                const grpOeeNg = grp.items.reduce((s, it) => s + (it.ng_detail?.total_ng_oee || 0), 0);
                                const grpAssyNg = grp.items.reduce((s, it) => s + (it.ng_detail?.ng_assy || 0), 0);
                                const grpRodNg = grp.items.reduce((s, it) => s + (it.ng_detail?.ng_rod || 0), 0);
                                const grpCapNg = grp.items.reduce((s, it) => s + (it.ng_detail?.ng_cap || 0), 0);
                                const grpNonOeeNg = grp.items.reduce((s, it) => s + (it.ng_detail?.total_ng_non_oee || 0), 0);
                                const grpPendingCount = grp.items.filter(it => !it.has_ng_detail || !it.is_balanced).length;
                                const grpCompletedCount = grp.items.length - grpPendingCount;
                                const uniqueShifts = [...new Set(grp.items.map(it => it.shift_name || 'Shift 1'))].join(', ');
                                const uniqueProducts = [...new Set(grp.items.map(it => it.product_sku || it.product_name))].join(', ');
                                const uniqueProductsCount = new Set(grp.items.map(it => it.product_sku || it.product_name)).size;

                                return `
                                    <!-- LINE GROUP HEADER ROW (CLICKABLE COLLAPSIBLE) -->
                                    <tr class="line-ng-group-header cursor-pointer select-none ${isLight ? 'bg-rose-50/70 hover:bg-rose-100/80 border-rose-200 text-slate-900' : 'bg-slate-950 hover:bg-slate-800/90 border-slate-800 text-slate-100'} border-t-2 border-b transition-colors" data-toggle-ng-line="${grp.lineId}">
                                        <td class="p-3 text-center">
                                            <button type="button" class="w-6 h-6 rounded-lg ${isLight ? 'bg-white text-slate-700 border-rose-200' : 'bg-slate-900 text-slate-300 border-slate-700'} border flex items-center justify-center transition-transform cursor-pointer shadow-xs">
                                                <i data-lucide="chevron-down" class="w-4 h-4 chevron-icon-ng-${grp.lineId} transition-transform duration-200"></i>
                                            </button>
                                        </td>
                                        <td class="p-3 font-sans">
                                            <div class="text-xs font-bold ${isLight ? 'text-slate-800' : 'text-slate-300'}">${uniqueShifts}</div>
                                            <div class="text-[10px] text-slate-500 font-mono">${grp.items.length} Batch / Shift</div>
                                        </td>
                                        <td class="p-3 font-sans">
                                            <div class="flex items-center gap-2">
                                                <span class="px-2.5 py-1 rounded-md ${this.getLineBadgeStyle(grp.lineName || grp.lineId, isLight)} border font-bold text-xs flex items-center gap-1 font-mono shadow-xs">
                                                    🏭 ${grp.lineName}
                                                </span>
                                                <span class="px-2 py-0.5 rounded-full text-[10px] font-bold ${isLight ? 'bg-slate-200 text-slate-800' : 'bg-slate-800 text-slate-300'} font-sans">
                                                    ${grp.items.length} Laporan
                                                </span>
                                            </div>
                                            ${grp.lineCode ? `<div class="text-[10px] text-slate-400 font-mono mt-0.5">${grp.lineCode}</div>` : ''}
                                        </td>
                                        <td class="p-3 font-sans text-xs">
                                            <div class="font-semibold ${isLight ? 'text-slate-800' : 'text-slate-300'} truncate max-w-xs" title="${uniqueProducts}">${uniqueProducts}</div>
                                            <div class="text-[10px] text-slate-500 font-mono">${uniqueProductsCount} Jenis Part</div>
                                        </td>
                                        <td class="p-3 text-right">
                                            <div class="font-bold ${isLight ? 'text-slate-900' : 'text-slate-200'}">${grpTotalOutput.toLocaleString()}</div>
                                            <div class="text-[10px] ${isLight ? 'text-emerald-700 font-semibold' : 'text-emerald-400'}">${grpGoodQty.toLocaleString()} OK</div>
                                        </td>
                                        <td class="p-3 text-right">
                                            <span class="px-2 py-0.5 rounded-lg text-xs font-black ${isLight ? 'bg-rose-100 text-rose-800 border border-rose-300' : 'bg-rose-950 text-rose-300 border border-rose-800'}">
                                                ${grpTargetNg.toLocaleString()} Pcs
                                            </span>
                                        </td>
                                        <td class="p-3 font-sans">
                                            <div class="text-xs font-mono font-bold ${isLight ? 'text-cyan-800' : 'text-cyan-300'}">
                                                Total OEE: ${grpOeeNg.toLocaleString()} Pcs
                                            </div>
                                            <div class="text-[10px] ${isLight ? 'text-slate-600' : 'text-slate-400'} font-mono">
                                                (Assy: ${grpAssyNg} | Rod: ${grpRodNg} | Cap: ${grpCapNg})
                                            </div>
                                        </td>
                                        <td class="p-3 font-sans">
                                            <div class="text-[10px] font-mono ${isLight ? 'text-purple-800 font-semibold' : 'text-purple-300'}">
                                                ${grpNonOeeNg > 0 ? `${grpNonOeeNg} Pcs` : '-'}
                                            </div>
                                        </td>
                                        <td class="p-3 text-center font-sans">
                                            <div class="flex items-center justify-center gap-1 flex-wrap">
                                                ${grpPendingCount > 0 ? `<span class="px-2 py-0.5 rounded-full text-[10px] font-bold ${isLight ? 'bg-amber-100 text-amber-800 border border-amber-300' : 'bg-amber-950 text-amber-300 border border-amber-800'}">${grpPendingCount} Pending</span>` : ''}
                                                ${grpCompletedCount > 0 ? `<span class="px-2 py-0.5 rounded-full text-[10px] font-bold ${isLight ? 'bg-emerald-100 text-emerald-800 border border-emerald-300' : 'bg-emerald-950 text-emerald-300 border border-emerald-800'}">${grpCompletedCount} Lengkap</span>` : ''}
                                            </div>
                                        </td>
                                        <td class="p-3 text-center">
                                            <span class="text-[10px] ${isLight ? 'text-slate-500' : 'text-slate-400'} font-mono">Klik baris</span>
                                        </td>
                                    </tr>

                                    <!-- CHILD ROWS FOR THIS LINE -->
                                    ${grp.items.map((item, idx) => {
                                        const ng = item.ng_detail;
                                        return `
                                            <tr class="ng-line-row-${grp.lineId} ${isLight ? 'hover:bg-slate-50/80' : 'hover:bg-slate-800/40'} transition-colors ${item.status === 'PENDING' ? (isLight ? 'bg-amber-50/40' : 'bg-amber-950/10') : ''}">
                                                <td class="p-3 text-center font-mono ${isLight ? 'text-slate-500' : 'text-slate-500'} text-[11px]">${idx + 1}</td>
                                                <td class="p-3 font-sans">
                                                    <div class="font-bold ${isLight ? 'text-slate-900' : 'text-slate-200'}">${item.formatted_date}</div>
                                                    <div class="text-[10px] ${isLight ? 'text-cyan-700' : 'text-cyan-400'} font-mono">${item.shift_name}</div>
                                                </td>
                                                <td class="p-3 font-sans">
                                                    <div class="flex items-center gap-1.5">
                                                        <span class="px-2 py-0.5 rounded text-[11px] font-bold ${this.getLineBadgeStyle(item.line_name || item.line_id, isLight)} border font-mono">
                                                            ${item.line_name || item.line_code}
                                                        </span>
                                                    </div>
                                                    <div class="text-[10px] ${isLight ? 'text-slate-500' : 'text-slate-400'} font-mono mt-0.5">${item.machine_name}</div>
                                                </td>
                                                <td class="p-3 font-sans">
                                                    <div class="font-bold ${isLight ? 'text-slate-900' : 'text-slate-200'} truncate max-w-xs">${item.product_name} - <span class="${isLight ? 'text-amber-700 font-bold' : 'text-amber-400'} font-mono">${item.product_sku}</span></div>
                                                </td>
                                                <td class="p-3 text-right">
                                                    <div class="${isLight ? 'text-slate-900' : 'text-slate-200'} font-bold">${item.total_output.toLocaleString()}</div>
                                                    <div class="text-[10px] ${isLight ? 'text-emerald-700 font-semibold' : 'text-emerald-400'}">${item.good_quantity.toLocaleString()} OK</div>
                                                </td>
                                                <td class="p-3 text-right">
                                                    <!-- BLOK TARGET NG (SOFT & PROPER) -->
                                                    <span class="px-2.5 py-1 rounded-lg text-xs font-black ${isLight ? 'bg-rose-50 text-rose-700 border border-rose-200 shadow-xs' : 'bg-rose-950/80 text-rose-400 border border-rose-800'}">
                                                        ${item.total_ng_target.toLocaleString()} Pcs
                                                    </span>
                                                    <div class="text-[9px] ${isLight ? 'text-slate-500' : 'text-slate-500'} mt-0.5">(${item.reject_quantity} rej + ${item.scrap_quantity} scp)</div>
                                                </td>
                                                <td class="p-3 font-sans">
                                                    ${ng ? `
                                                        <div class="space-y-1.5">
                                                            <!-- BLOK RINCIAN KOMPONEN WAJIB OEE (SOFT PASTEL) -->
                                                            <div class="flex items-center gap-1.5 flex-wrap">
                                                                <span class="px-2 py-0.5 rounded text-[10px] font-mono font-bold ${isLight ? 'bg-sky-50 text-sky-800 border border-sky-200' : 'bg-cyan-950/80 text-cyan-300 border border-cyan-800/80'}">
                                                                    Assy: ${ng.ng_assy} Pcs
                                                                </span>
                                                                <span class="px-2 py-0.5 rounded text-[10px] font-mono font-bold ${isLight ? 'bg-sky-50 text-sky-800 border border-sky-200' : 'bg-cyan-950/80 text-cyan-300 border border-cyan-800/80'}">
                                                                    Rod: ${ng.ng_rod} Pcs
                                                                </span>
                                                                <span class="px-2 py-0.5 rounded text-[10px] font-mono font-bold ${isLight ? 'bg-sky-50 text-sky-800 border border-sky-200' : 'bg-cyan-950/80 text-cyan-300 border border-cyan-800/80'}">
                                                                    Cap: ${ng.ng_cap} Pcs
                                                                </span>
                                                            </div>

                                                            <!-- RINCIAN BARIS MULTI-ITEM -->
                                                            ${((ng.items && ng.items.length > 0) || (ng.items_breakdown && ng.items_breakdown.length > 0)) ? `
                                                                <div class="space-y-1 max-h-24 overflow-y-auto pr-1">
                                                                    ${((ng.items && ng.items.length > 0) ? ng.items : (ng.items_breakdown || [])).map(it => `
                                                                        <div class="text-[10px] ${isLight ? 'bg-slate-50 border-slate-200 text-slate-800' : 'bg-slate-950/60 border-slate-800/80 text-slate-300'} border px-1.5 py-0.5 rounded flex items-center gap-1.5 truncate">
                                                                            <span class="font-mono font-bold ${isLight ? 'text-sky-700' : 'text-cyan-400'} shrink-0">${it.component_type || 'ASSY'}: ${it.quantity}x</span>
                                                                            <span class="${isLight ? 'text-slate-700' : 'text-slate-400'} truncate">${it.section || '-'}</span>
                                                                            <span class="text-slate-400 shrink-0">•</span>
                                                                            <span class="${isLight ? 'text-amber-800 font-semibold' : 'text-amber-400'} truncate">${it.reason || '-'}</span>
                                                                        </div>
                                                                    `).join('')}
                                                                </div>
                                                            ` : ''}

                                                            <div class="text-[10px] ${isLight ? 'text-slate-600' : 'text-slate-400'} font-mono">
                                                                Total Terdata: <strong class="${item.is_balanced ? (isLight ? 'text-emerald-700 font-bold' : 'text-emerald-400') : (isLight ? 'text-rose-700 font-bold' : 'text-rose-400')}">${ng.total_ng_oee} Pcs</strong>
                                                                ${item.is_balanced ? '✓' : `<span class="${isLight ? 'text-rose-700 font-bold' : 'text-rose-400'}">(${item.variance > 0 ? '+' : ''}${item.variance})</span>`}
                                                            </div>
                                                        </div>
                                                    ` : `
                                                        <span class="${isLight ? 'text-slate-500' : 'text-slate-500'} italic text-[11px] font-sans">Belum dirinci</span>
                                                    `}
                                                </td>
                                                <td class="p-3 font-sans">
                                                    ${ng && ng.total_ng_non_oee > 0 ? `
                                                        <div class="text-[10px] font-mono ${isLight ? 'text-purple-800 font-semibold' : 'text-purple-300'} space-x-1">
                                                            <span>B:${ng.ng_bolt}</span>
                                                            <span>Bs:${ng.ng_bush}</span>
                                                            <span>N:${ng.ng_nut}</span>
                                                            <span>P:${ng.ng_pin}</span>
                                                        </div>
                                                        <div class="text-[9px] ${isLight ? 'text-slate-500' : 'text-slate-500'} font-mono">(${ng.total_ng_non_oee} Pcs non-OEE)</div>
                                                    ` : `
                                                        <span class="${isLight ? 'text-slate-400' : 'text-slate-600'} font-mono text-[10px]">-</span>
                                                    `}
                                                </td>
                                                <td class="p-3 text-center font-sans">
                                                    <!-- STATUS INPUT BADGE (SOFT & CLEAR) -->
                                                    ${item.has_ng_detail ? `
                                                        ${item.is_balanced ? `
                                                            <span class="inline-flex items-center gap-1 px-2.5 py-1 rounded-full text-[10px] font-bold ${isLight ? 'bg-emerald-50 text-emerald-700 border border-emerald-200' : 'bg-emerald-950/80 text-emerald-400 border border-emerald-800'}">
                                                                <i data-lucide="check" class="w-3 h-3"></i> Lengkap
                                                            </span>
                                                        ` : `
                                                            <span class="inline-flex items-center gap-1 px-2.5 py-1 rounded-full text-[10px] font-bold ${isLight ? 'bg-rose-50 text-rose-700 border border-rose-200' : 'bg-rose-950/80 text-rose-400 border border-rose-800'}">
                                                                <i data-lucide="alert-circle" class="w-3 h-3"></i> Selisih ${item.variance}
                                                            </span>
                                                        `}
                                                    ` : `
                                                        <span class="inline-flex items-center gap-1 px-2.5 py-1 rounded-full text-[10px] font-bold ${isLight ? 'bg-amber-50 text-amber-800 border border-amber-200' : 'bg-amber-950/80 text-amber-400 border border-amber-800'}">
                                                            <i data-lucide="clock" class="w-3 h-3"></i> Perlu Input
                                                        </span>
                                                    `}
                                                </td>
                                                <td class="p-3 text-center font-sans">
                                                    <!-- TOMBOL AKSI ICON-ONLY (EDIT & HAPUS DETAIL NG) -->
                                                    <div class="flex items-center justify-center gap-1.5">
                                                        <button type="button" class="btn-open-ng-modal w-7 h-7 rounded-lg ${item.has_ng_detail ? (isLight ? 'bg-cyan-50 hover:bg-cyan-100 text-cyan-700 border border-cyan-200' : 'bg-cyan-950/70 hover:bg-cyan-900 border border-cyan-800/80 text-cyan-400 hover:text-cyan-200') : (isLight ? 'bg-amber-50 hover:bg-amber-100 text-amber-700 border border-amber-300' : 'bg-amber-950/70 hover:bg-amber-900 border border-amber-800/80 text-amber-400 hover:text-amber-200')} border flex items-center justify-center cursor-pointer transition-all shadow-sm hover:scale-110 active:scale-95" data-record-id="${item.production_record_id}" title="${item.has_ng_detail ? 'Edit Detail NG' : 'Input Detail NG'}">
                                                            <i data-lucide="${item.has_ng_detail ? 'pencil' : 'plus'}" class="w-3.5 h-3.5"></i>
                                                        </button>
                                                        ${item.has_ng_detail ? `
                                                            <button type="button" class="btn-delete-ng-row w-7 h-7 rounded-lg ${isLight ? 'bg-rose-50 hover:bg-rose-100 text-rose-700 border border-rose-200' : 'bg-rose-950/70 hover:bg-rose-900 border border-rose-800/80 text-rose-400 hover:text-rose-200'} border flex items-center justify-center cursor-pointer transition-all shadow-sm hover:scale-110 active:scale-95" data-record-id="${item.production_record_id}" data-ng-id="${item.ng_detail?.id || ''}" title="Hapus / Reset Detail NG">
                                                                <i data-lucide="trash-2" class="w-3.5 h-3.5"></i>
                                                            </button>
                                                        ` : ''}
                                                    </div>
                                                </td>
                                            </tr>
                                        `;
                                    }).join('')}
                                `;
                            }).join('')}
                        </tbody>
                    </table>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();

        // 1. Bind Filter Handlers
        const dateInput = document.getElementById('filter-ng-date');
        const lineSelect = document.getElementById('filter-ng-line');
        const shiftSelect = document.getElementById('filter-ng-shift');
        const statusSelect = document.getElementById('filter-ng-status');
        const searchInput = document.getElementById('input-search-ng');
        const resetBtn = document.getElementById('btn-reset-ng-filters');

        if (dateInput) {
            dateInput.addEventListener('change', (e) => {
                this.ngFilters.date = e.target.value;
                this.renderNgReport();
            });
        }
        if (lineSelect) {
            lineSelect.addEventListener('change', (e) => {
                this.ngFilters.line_id = e.target.value;
                this.renderNgReport();
            });
        }
        if (shiftSelect) {
            shiftSelect.addEventListener('change', (e) => {
                this.ngFilters.shift_id = e.target.value;
                this.renderNgReport();
            });
        }
        if (statusSelect) {
            statusSelect.addEventListener('change', (e) => {
                this.ngFilters.status = e.target.value;
                this.renderNgReport();
            });
        }
        if (searchInput) {
            searchInput.addEventListener('input', (e) => {
                this.ngFilters.search = e.target.value;
                this.renderNgReport();
            });
        }
        if (resetBtn) {
            resetBtn.addEventListener('click', () => {
                this.ngFilters = { date: '', line_id: '', machine_id: '', shift_id: '', status: 'all', search: '' };
                this.renderNgReport();
            });
        }

        // 2. Bind Open NG Detail Modal Buttons
        document.querySelectorAll('.btn-open-ng-modal').forEach(btn => {
            btn.addEventListener('click', (e) => {
                const recordId = e.currentTarget.getAttribute('data-record-id');
                if (recordId) {
                    this.showNgDetailModal(recordId);
                }
            });
        });

        // 3. Bind Delete NG Detail Buttons
        document.querySelectorAll('.btn-delete-ng-row').forEach(btn => {
            btn.addEventListener('click', async (e) => {
                const ngId = e.currentTarget.getAttribute('data-ng-id');
                if (!ngId) {
                    this.showNotification('Info', 'Belum ada rincian NG tersimpan untuk dihapus.', 'info');
                    return;
                }
                const confirmed = await this.showConfirmModal({
                    title: 'Reset & Hapus Detail NG',
                    subtitle: 'Apakah Anda yakin ingin menghapus / mereset rincian detail NG untuk shift ini?',
                    confirmText: 'Ya, Hapus Data',
                    cancelText: 'Batal',
                    type: 'danger'
                });
                if (confirmed) {
                    try {
                        await api.deleteNgDetail(ngId);
                        this.showNotification('Data Dihapus', 'Detail NG berhasil direset.', 'delete');
                        this.renderNgReport();
                    } catch (err) {
                        this.showNotification('Gagal Menghapus', err.response?.data?.message || err.message, 'delete');
                    }
                }
            });
        });

        // 3. Line Grouping Collapsible Toggles & Expand/Collapse All for NG Table
        document.querySelectorAll('[data-toggle-ng-line]').forEach(headerRow => {
            headerRow.addEventListener('click', (e) => {
                const lineId = headerRow.getAttribute('data-toggle-ng-line');
                const childRows = document.querySelectorAll(`.ng-line-row-${lineId}`);
                const chevronIcon = document.querySelector(`.chevron-icon-ng-${lineId}`);
                const isCurrentlyHidden = childRows[0]?.classList.contains('hidden');

                childRows.forEach(r => {
                    if (isCurrentlyHidden) {
                        r.classList.remove('hidden');
                    } else {
                        r.classList.add('hidden');
                    }
                });

                if (chevronIcon) {
                    if (isCurrentlyHidden) {
                        chevronIcon.classList.remove('-rotate-90');
                        chevronIcon.classList.add('rotate-0');
                    } else {
                        chevronIcon.classList.remove('rotate-0');
                        chevronIcon.classList.add('-rotate-90');
                    }
                }
            });
        });

        document.getElementById('btn-expand-all-ng-lines')?.addEventListener('click', () => {
            document.querySelectorAll('[class*="ng-line-row-"]').forEach(r => r.classList.remove('hidden'));
            document.querySelectorAll('[class*="chevron-icon-ng-"]').forEach(i => {
                i.classList.remove('-rotate-90');
                i.classList.add('rotate-0');
            });
        });

        document.getElementById('btn-collapse-all-ng-lines')?.addEventListener('click', () => {
            document.querySelectorAll('[class*="ng-line-row-"]').forEach(r => r.classList.add('hidden'));
            document.querySelectorAll('[class*="chevron-icon-ng-"]').forEach(i => {
                i.classList.remove('rotate-0');
                i.classList.add('-rotate-90');
            });
        });

        this.initTableSorting();
        if (this.currentLang === 'ja') {
            i18n.localizeDom(content);
        }
    }

    /**
     * Interactive Modal for Entering & Balancing NG Component Breakdown
     * Supports Dynamic Multi-Row Input with '+' Button per NG Breakdown Instance
     */
    async showNgDetailModal(productionRecordId) {
        const modalContainer = document.getElementById('modal-container');
        if (!modalContainer) return;

        modalContainer.innerHTML = `
            <div class="fixed inset-0 bg-slate-950/80 backdrop-blur-sm flex items-center justify-center p-4 z-50">
                <div class="bg-slate-900 border border-slate-800 rounded-2xl p-6 max-w-2xl w-full shadow-2xl text-center">
                    <i data-lucide="loader-2" class="w-8 h-8 mx-auto text-cyan-400 animate-spin mb-3"></i>
                    <p class="text-xs text-slate-400 font-mono">Memuat data Laporan Harian & Bank Data NG...</p>
                </div>
            </div>
        `;
        if (window.lucide) window.lucide.createIcons();

        try {
            const detailRes = await api.getNgDetail(productionRecordId);
            const item = detailRes.data.data;
            const ng = item.ng_record || {};
            const targetNg = item.total_ng_target || 0;

            const sectionsBank = detailRes.data.bank_data?.sections || [
                'Small End (Pin Bore)',
                'Big End (Crank Bore)',
                'Rod Body (I-Beam)',
                'Cap Body',
                'Joint Face / Serration',
                'Side Face / Thrust Width',
                'Bolt Hole & Thread',
                'Oil Hole (Lubrication)',
                'Bushing Press Fit Area',
                'Assembly Fitment / Rakitan',
            ];

            const reasonsBank = detailRes.data.bank_data?.reasons || [];

            // Initialize Dynamic NG Breakdown Items State
            let ngItemsState = [];
            if (ng.items && Array.isArray(ng.items) && ng.items.length > 0) {
                ngItemsState = ng.items.map(it => ({
                    component_type: it.component_type || 'ASSY',
                    quantity: parseInt(it.quantity || 1),
                    section: it.section || '',
                    reason: it.reason || ''
                }));
            } else if (ng.items_breakdown && Array.isArray(ng.items_breakdown) && ng.items_breakdown.length > 0) {
                ngItemsState = ng.items_breakdown.map(it => ({
                    component_type: it.component_type || 'ASSY',
                    quantity: parseInt(it.quantity || 1),
                    section: it.section || '',
                    reason: it.reason || ''
                }));
            } else if (ng.ng_assy > 0 || ng.ng_rod > 0 || ng.ng_cap > 0) {
                if (ng.ng_assy > 0) {
                    ngItemsState.push({
                        component_type: 'ASSY',
                        quantity: parseInt(ng.ng_assy),
                        section: ng.assy_section || '',
                        reason: ng.assy_reason || ''
                    });
                }
                if (ng.ng_rod > 0) {
                    ngItemsState.push({
                        component_type: 'ROD',
                        quantity: parseInt(ng.ng_rod),
                        section: ng.rod_section || '',
                        reason: ng.rod_reason || ''
                    });
                }
                if (ng.ng_cap > 0) {
                    ngItemsState.push({
                        component_type: 'CAP',
                        quantity: parseInt(ng.ng_cap),
                        section: ng.cap_section || '',
                        reason: ng.cap_reason || ''
                    });
                }
            }

            // If completely empty, seed with 1 default row
            if (ngItemsState.length === 0) {
                ngItemsState.push({
                    component_type: 'ASSY',
                    quantity: targetNg > 0 ? targetNg : 1,
                    section: '',
                    reason: ''
                });
            }

            const existingBolt = ng.ng_bolt || 0;
            const existingBush = ng.ng_bush || 0;
            const existingNut = ng.ng_nut || 0;
            const existingPin = ng.ng_pin || 0;
            const isLightModal = this.theme === 'light' || document.documentElement.classList.contains('light');

            modalContainer.innerHTML = `
                <div class="fixed inset-0 bg-slate-950/80 backdrop-blur-sm flex items-center justify-center p-2 sm:p-4 z-50 overflow-hidden">
                    <div class="${isLightModal ? 'bg-white border-slate-200 text-slate-900 shadow-2xl' : 'bg-slate-900 border-slate-800 text-slate-100 shadow-2xl'} border rounded-2xl max-w-5xl w-full overflow-hidden font-sans flex flex-col max-h-[92vh] my-auto">
                        <!-- MODAL HEADER (STICKY AT TOP) -->
                        <div class="${isLightModal ? 'bg-slate-50 border-slate-200' : 'bg-slate-950 border-slate-800'} px-5 sm:px-6 py-3.5 sm:py-4 border-b flex items-center justify-between shrink-0 z-20">
                            <div class="flex items-center gap-3">
                                <div class="w-10 h-10 rounded-xl ${isLightModal ? 'bg-rose-100 border-rose-200 text-rose-600' : 'bg-rose-950/60 border-rose-800/80 text-rose-400'} border flex items-center justify-center shrink-0">
                                    <i data-lucide="shield-alert" class="w-5 h-5"></i>
                                </div>
                                <div>
                                    <h3 class="text-base font-bold ${isLightModal ? 'text-slate-900' : 'text-white'} flex flex-wrap items-center gap-2">
                                        <span>Input Detail Komponen NG</span>
                                        <span class="text-[10px] font-mono px-2 py-0.5 rounded ${isLightModal ? 'bg-rose-50 text-rose-700 border-rose-200' : 'bg-rose-950 text-rose-300 border-rose-800'} border font-bold">Target: ${targetNg} Pcs</span>
                                    </h3>
                                    <p class="text-xs ${isLightModal ? 'text-slate-500' : 'text-slate-400'}">${item.formatted_date} • ${item.shift_name} • ${item.line_name} (${item.machine_name})</p>
                                </div>
                            </div>
                            <button type="button" id="btn-close-ng-modal" class="p-1.5 rounded-lg ${isLightModal ? 'text-slate-500 hover:text-slate-900 hover:bg-slate-200' : 'text-slate-400 hover:text-slate-100 hover:bg-slate-800'} transition-colors cursor-pointer" title="Tutup">
                                <i data-lucide="x" class="w-5 h-5"></i>
                            </button>
                        </div>

                        <!-- MODAL BODY FORM WRAPPER -->
                        <form id="form-ng-detail" class="flex flex-col flex-1 overflow-hidden min-h-0">
                            <input type="hidden" name="production_record_id" value="${item.production_record_id}" />

                            <!-- SCROLLABLE BODY CONTENT -->
                            <div class="p-4 sm:p-6 space-y-4 sm:space-y-5 overflow-y-auto flex-1 custom-scrollbar">
                                <!-- METADATA BADGE BAR -->
                                <div class="grid grid-cols-2 sm:grid-cols-4 gap-2 ${isLightModal ? 'bg-slate-50 border-slate-200' : 'bg-slate-950 border-slate-800'} p-3 rounded-xl border text-xs font-mono">
                                    <div>
                                        <span class="${isLightModal ? 'text-slate-500' : 'text-slate-500'} block text-[10px] font-sans">Produk - SKU:</span>
                                        <strong class="${isLightModal ? 'text-slate-900' : 'text-slate-200'} truncate block" title="${item.product_name} - ${item.product_sku}">${item.product_name} - <span class="${isLightModal ? 'text-amber-700 font-bold' : 'text-amber-400'} font-mono">${item.product_sku}</span></strong>
                                    </div>
                                    <div>
                                        <span class="${isLightModal ? 'text-slate-500' : 'text-slate-500'} block text-[10px] font-sans">Total Measuring:</span>
                                        <strong class="${isLightModal ? 'text-cyan-700 font-bold' : 'text-cyan-400'}">${(item.total_output || 0).toLocaleString()} Pcs</strong>
                                    </div>
                                    <div>
                                        <span class="${isLightModal ? 'text-slate-500' : 'text-slate-500'} block text-[10px] font-sans">Finish Good:</span>
                                        <strong class="${isLightModal ? 'text-emerald-700 font-bold' : 'text-emerald-400'}">${(item.good_quantity || 0).toLocaleString()} Pcs</strong>
                                    </div>
                                    <div>
                                        <span class="${isLightModal ? 'text-slate-500' : 'text-slate-500'} block text-[10px] font-sans">Total Reject / Defect:</span>
                                        <strong class="${isLightModal ? 'text-rose-700 font-bold' : 'text-rose-400 font-bold'}">${targetNg.toLocaleString()} Pcs</strong>
                                    </div>
                                </div>

                                <!-- SEKSI 1: KOMPONEN WAJIB (DIHITUNG SEBAGAI OEE) — DYNAMIC MULTI-ROW BUILDER -->
                                <div class="${isLightModal ? 'bg-cyan-50/30 border-cyan-200' : 'bg-slate-950/60 border-cyan-500/30'} border rounded-xl p-4 space-y-3.5">
                                    <div class="flex flex-wrap items-center justify-between gap-2 border-b ${isLightModal ? 'border-cyan-100' : 'border-slate-800/80'} pb-2">
                                        <div class="flex items-center gap-2">
                                            <span class="w-5 h-5 rounded-full ${isLightModal ? 'bg-cyan-100 text-cyan-800 border-cyan-300' : 'bg-cyan-500/20 text-cyan-400 border-cyan-500/40'} font-bold text-xs flex items-center justify-center border">1</span>
                                            <h4 class="font-bold text-xs ${isLightModal ? 'text-cyan-900' : 'text-cyan-300'} uppercase tracking-wider">Komponen Utama (Wajib OEE) — Rincian Multi-Baris</h4>
                                        </div>
                                        
                                        <!-- LIVE BALANCE INDICATOR -->
                                        <div id="ng-balance-indicator" class="flex items-center gap-1.5 px-2.5 py-0.5 rounded-full text-[11px] font-mono font-bold">
                                            <!-- Dynamic balance badge -->
                                        </div>
                                    </div>

                                    <div class="flex flex-wrap items-center justify-between gap-2">
                                        <p class="text-[11px] ${isLightModal ? 'text-slate-600' : 'text-slate-400'}">Tambahkan baris rincian defect per jenis penyebab & bagian part. Total wajib seimbang dengan Target (${targetNg} Pcs).</p>
                                        
                                        <!-- BUTTON TAMBAH BARIS DATA -->
                                        <button type="button" id="btn-add-ng-row" class="px-3 py-1.5 rounded-lg text-xs font-bold ${isLightModal ? 'bg-cyan-600 hover:bg-cyan-700' : 'bg-cyan-600 hover:bg-cyan-500'} text-white shadow-md flex items-center gap-1.5 cursor-pointer transition-all">
                                            <i data-lucide="plus" class="w-3.5 h-3.5"></i>
                                            <span>+ Tambah Baris NG</span>
                                        </button>
                                    </div>

                                    <!-- SUB-TOTAL SUMMARY BADGES -->
                                    <div class="grid grid-cols-3 gap-2 ${isLightModal ? 'bg-white border-cyan-200' : 'bg-slate-900/90 border-slate-800'} border p-2.5 rounded-xl text-center text-xs font-mono">
                                        <div class="${isLightModal ? 'bg-cyan-50/80 border-cyan-200/80' : 'bg-slate-950 border-slate-800/80'} py-1.5 px-2 rounded-lg border">
                                            <span class="text-[10px] ${isLightModal ? 'text-slate-600' : 'text-slate-400'} block font-sans">Total Assy NG:</span>
                                            <strong id="badge-subtotal-assy" class="${isLightModal ? 'text-cyan-800' : 'text-cyan-400'} font-bold text-sm">0 Pcs</strong>
                                        </div>
                                        <div class="${isLightModal ? 'bg-cyan-50/80 border-cyan-200/80' : 'bg-slate-950 border-slate-800/80'} py-1.5 px-2 rounded-lg border">
                                            <span class="text-[10px] ${isLightModal ? 'text-slate-600' : 'text-slate-400'} block font-sans">Total Rod NG:</span>
                                            <strong id="badge-subtotal-rod" class="${isLightModal ? 'text-cyan-800' : 'text-cyan-400'} font-bold text-sm">0 Pcs</strong>
                                        </div>
                                        <div class="${isLightModal ? 'bg-cyan-50/80 border-cyan-200/80' : 'bg-slate-950 border-slate-800/80'} py-1.5 px-2 rounded-lg border">
                                            <span class="text-[10px] ${isLightModal ? 'text-slate-600' : 'text-slate-400'} block font-sans">Total Cap NG:</span>
                                            <strong id="badge-subtotal-cap" class="${isLightModal ? 'text-cyan-800' : 'text-cyan-400'} font-bold text-sm">0 Pcs</strong>
                                        </div>
                                    </div>

                                    <!-- DATALISTS FOR INSTANT SEARCH-AS-YOU-TYPE AUTOCOMPLETE -->
                                    <datalist id="ng-sections-datalist">
                                        ${sectionsBank.map(s => `<option value="${s}">${s}</option>`).join('')}
                                    </datalist>
                                    <datalist id="ng-reasons-datalist">
                                        ${reasonsBank.map(r => `<option value="${r.name}">${r.category ? `[${r.category}] ` : ''}${r.name}</option>`).join('')}
                                    </datalist>

                                    <!-- DYNAMIC TABLE CONTAINER WITH SCROLLING & STICKY HEADER -->
                                    <div class="relative max-h-64 sm:max-h-72 overflow-y-auto overflow-x-auto rounded-xl ${isLightModal ? 'border-slate-200 bg-white' : 'border-slate-800/80 bg-slate-950/60'} border custom-scrollbar shadow-inner">
                                        <table class="w-full text-left text-xs border-collapse">
                                            <thead class="sticky top-0 z-10 ${isLightModal ? 'bg-slate-100 text-slate-700 border-slate-200' : 'bg-slate-900 text-slate-300 border-slate-700'} uppercase font-semibold text-[10px] border-b shadow-xs">
                                                <tr>
                                                    <th class="p-2.5 text-center w-8 ${isLightModal ? 'bg-slate-100' : 'bg-slate-900'}">#</th>
                                                    <th class="p-2.5 w-32 ${isLightModal ? 'bg-slate-100' : 'bg-slate-900'}">Komponen OEE <span class="text-rose-500">*</span></th>
                                                    <th class="p-2.5 w-28 ${isLightModal ? 'bg-slate-100' : 'bg-slate-900'}">Jumlah (Pcs) <span class="text-rose-500">*</span></th>
                                                    <th class="p-2.5 min-w-[160px] ${isLightModal ? 'bg-slate-100' : 'bg-slate-900'}">Bagian NG (Ketik / Cari)</th>
                                                    <th class="p-2.5 min-w-[240px] ${isLightModal ? 'bg-slate-100' : 'bg-slate-900'}">Penyebab / Remark (Ketik / Cari)</th>
                                                    <th class="p-2.5 text-center w-10 ${isLightModal ? 'bg-slate-100' : 'bg-slate-900'}">Aksi</th>
                                                </tr>
                                            </thead>
                                            <tbody id="ng-dynamic-rows-tbody" class="divide-y ${isLightModal ? 'divide-slate-200 text-slate-800' : 'divide-slate-800/60 text-slate-200'} font-mono">
                                                <!-- Dynamically generated rows -->
                                            </tbody>
                                        </table>
                                    </div>
                                </div>

                                <!-- SEKSI 2: KOMPONEN PELENGKAP (OPSIONAL - TIDAK DIHITUNG OEE) -->
                                <div class="${isLightModal ? 'bg-purple-50/30 border-purple-200' : 'bg-slate-950/60 border-purple-500/30'} border rounded-xl p-4 space-y-3">
                                    <div class="flex items-center justify-between border-b ${isLightModal ? 'border-purple-100' : 'border-slate-800/80'} pb-2">
                                        <div class="flex items-center gap-2">
                                            <span class="w-5 h-5 rounded-full ${isLightModal ? 'bg-purple-100 text-purple-800 border-purple-300' : 'bg-purple-500/20 text-purple-400 border-purple-500/40'} font-bold text-xs flex items-center justify-center border">2</span>
                                            <h4 class="font-bold text-xs ${isLightModal ? 'text-purple-900' : 'text-purple-300'} uppercase tracking-wider">Komponen Pelengkap & Fasteners (Opsional)</h4>
                                        </div>
                                        <span class="px-2 py-0.5 rounded text-[10px] font-mono font-semibold ${isLightModal ? 'bg-purple-100 text-purple-800 border-purple-200' : 'bg-purple-950 text-purple-300 border border-purple-800'} border">
                                            Non-OEE Tracking
                                        </span>
                                    </div>

                                    <p class="text-[11px] ${isLightModal ? 'text-slate-600' : 'text-slate-400'}">Dicatat untuk ketertelusuran material QC tanpa mempengaruhi Quality Rate kalkulasi OEE.</p>

                                    <div class="grid grid-cols-2 sm:grid-cols-4 gap-3">
                                        <div>
                                            <label class="block text-[11px] ${isLightModal ? 'text-slate-700' : 'text-slate-300'} mb-1 font-medium">Bolt (Baut)</label>
                                            <input type="number" name="ng_bolt" value="${existingBolt}" min="0" class="w-full ${isLightModal ? 'bg-white border-slate-200 text-purple-900 focus:border-purple-600' : 'bg-slate-900 border-slate-800 text-purple-300 focus:border-purple-500'} border rounded-lg px-2.5 py-1.5 text-xs font-mono font-bold focus:outline-none" />
                                        </div>
                                        <div>
                                            <label class="block text-[11px] ${isLightModal ? 'text-slate-700' : 'text-slate-300'} mb-1 font-medium">Bush (Bushing)</label>
                                            <input type="number" name="ng_bush" value="${existingBush}" min="0" class="w-full ${isLightModal ? 'bg-white border-slate-200 text-purple-900 focus:border-purple-600' : 'bg-slate-900 border-slate-800 text-purple-300 focus:border-purple-500'} border rounded-lg px-2.5 py-1.5 text-xs font-mono font-bold focus:outline-none" />
                                        </div>
                                        <div>
                                            <label class="block text-[11px] ${isLightModal ? 'text-slate-700' : 'text-slate-300'} mb-1 font-medium">Nut (Mur)</label>
                                            <input type="number" name="ng_nut" value="${existingNut}" min="0" class="w-full ${isLightModal ? 'bg-white border-slate-200 text-purple-900 focus:border-purple-600' : 'bg-slate-900 border-slate-800 text-purple-300 focus:border-purple-500'} border rounded-lg px-2.5 py-1.5 text-xs font-mono font-bold focus:outline-none" />
                                        </div>
                                        <div>
                                            <label class="block text-[11px] ${isLightModal ? 'text-slate-700' : 'text-slate-300'} mb-1 font-medium">Pin (Dowel Pin)</label>
                                            <input type="number" name="ng_pin" value="${existingPin}" min="0" class="w-full ${isLightModal ? 'bg-white border-slate-200 text-purple-900 focus:border-purple-600' : 'bg-slate-900 border-slate-800 text-purple-300 focus:border-purple-500'} border rounded-lg px-2.5 py-1.5 text-xs font-mono font-bold focus:outline-none" />
                                        </div>
                                    </div>
                                </div>

                                <!-- SEKSI 3: QUALITY ANALYSIS & DISPOSISI -->
                                <div class="${isLightModal ? 'bg-slate-50 border-slate-200' : 'bg-slate-950/60 border-slate-800'} border rounded-xl p-4 space-y-3">
                                    <div class="flex items-center gap-2 border-b ${isLightModal ? 'border-slate-200' : 'border-slate-800/80'} pb-2">
                                        <span class="w-5 h-5 rounded-full ${isLightModal ? 'bg-slate-200 text-slate-800 border-slate-300' : 'bg-slate-800 text-slate-300 border-slate-700'} font-bold text-xs flex items-center justify-center border">3</span>
                                        <h4 class="font-bold text-xs ${isLightModal ? 'text-slate-900' : 'text-slate-200'} uppercase tracking-wider">Analisa Defect & Disposisi QC</h4>
                                    </div>

                                    <div class="grid grid-cols-1 sm:grid-cols-3 gap-3">
                                        <div>
                                            <label class="block text-xs ${isLightModal ? 'text-slate-700 font-medium' : 'text-slate-400 font-medium'} mb-1">Gejala Defect (Symptom)</label>
                                            <input type="text" name="defect_symptom" value="${ng.defect_symptom || ''}" placeholder="Cth: Scratch, Dimension Out, Dent, Porosity" class="w-full ${isLightModal ? 'bg-white border-slate-200 text-slate-900 focus:border-cyan-600' : 'bg-slate-900 border-slate-800 text-slate-100 focus:border-cyan-500'} border rounded-lg px-2.5 py-2 text-xs focus:outline-none font-sans" />
                                        </div>
                                        <div>
                                            <label class="block text-xs ${isLightModal ? 'text-slate-700 font-medium' : 'text-slate-400 font-medium'} mb-1">Disposisi Part</label>
                                            <select name="action_taken" class="w-full ${isLightModal ? 'bg-white border-slate-200 text-slate-900 focus:border-cyan-600' : 'bg-slate-900 border-slate-800 text-slate-100 focus:border-cyan-500'} border rounded-lg px-2.5 py-2 text-xs focus:outline-none font-sans">
                                                <option value="SCRAP" ${ng.action_taken === 'SCRAP' ? 'selected' : ''}>SCRAP (Afkir / Buang)</option>
                                                <option value="REWORK" ${ng.action_taken === 'REWORK' ? 'selected' : ''}>REWORK (Perbaikan Ulang)</option>
                                                <option value="HOLD" ${ng.action_taken === 'HOLD' ? 'selected' : ''}>HOLD (Karantina / Analisa QC)</option>
                                            </select>
                                        </div>
                                        <div>
                                            <label class="block text-xs ${isLightModal ? 'text-slate-700 font-medium' : 'text-slate-400 font-medium'} mb-1">Nama Inspector QC</label>
                                            <input type="text" name="inspector_name" value="${ng.inspector_name || (this.user?.name || 'QC Inspector')}" class="w-full ${isLightModal ? 'bg-white border-slate-200 text-cyan-800 focus:border-cyan-600' : 'bg-slate-900 border-slate-800 text-cyan-300 focus:border-cyan-500'} border rounded-lg px-2.5 py-2 text-xs font-sans focus:outline-none font-bold" />
                                        </div>
                                    </div>

                                    <div>
                                        <label class="block text-xs ${isLightModal ? 'text-slate-700 font-medium' : 'text-slate-400 font-medium'} mb-1">Catatan / Keterangan Tambahan</label>
                                        <textarea name="notes" rows="2" placeholder="Catatan analisa akar masalah atau tindakan penanganan..." class="w-full ${isLightModal ? 'bg-white border-slate-200 text-slate-900 focus:border-cyan-600' : 'bg-slate-900 border-slate-800 text-slate-100 focus:border-cyan-500'} border rounded-lg px-2.5 py-2 text-xs focus:outline-none font-sans">${ng.notes || ''}</textarea>
                                    </div>
                                </div>
                            </div>

                            <!-- MODAL FOOTER (STICKY AT BOTTOM) -->
                            <div class="${isLightModal ? 'bg-slate-50 border-slate-200' : 'bg-slate-950 border-slate-800'} px-5 sm:px-6 py-3.5 border-t flex items-center justify-between shrink-0 z-20">
                                ${ng.id ? `
                                    <button type="button" id="btn-delete-ng-detail" class="px-4 py-2 rounded-xl text-xs font-bold text-rose-600 hover:bg-rose-50 border border-rose-200 transition-colors flex items-center gap-1.5 cursor-pointer">
                                        <i data-lucide="trash-2" class="w-3.5 h-3.5"></i>
                                        <span>Reset / Hapus Detail</span>
                                    </button>
                                ` : `<div></div>`}

                                <div class="flex items-center gap-2">
                                    <button type="button" id="btn-cancel-ng-modal" class="px-4 py-2 rounded-xl text-xs font-medium ${isLightModal ? 'text-slate-600 hover:text-slate-900 hover:bg-slate-200' : 'text-slate-400 hover:text-slate-200 hover:bg-slate-800'} transition-colors cursor-pointer">
                                        Batal
                                    </button>
                                    <button type="submit" id="btn-submit-ng-detail" class="px-6 py-2 rounded-xl text-xs font-bold bg-emerald-600 hover:bg-emerald-500 text-white shadow-lg shadow-emerald-600/30 flex items-center gap-2 transition-all cursor-pointer">
                                        <i data-lucide="save" class="w-4 h-4"></i>
                                        <span>Simpan Detail Laporan NG</span>
                                    </button>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            `;

            if (window.lucide) window.lucide.createIcons();
            if (this.currentLang === 'ja') {
                i18n.localizeDom(modalContainer);
            }

            // 1. Dynamic Multi-Row Table Renderer & Calculator
            const tbody = document.getElementById('ng-dynamic-rows-tbody');
            const balanceIndicator = document.getElementById('ng-balance-indicator');
            const badgeSubAssy = document.getElementById('badge-subtotal-assy');
            const badgeSubRod = document.getElementById('badge-subtotal-rod');
            const badgeSubCap = document.getElementById('badge-subtotal-cap');

            const renderDynamicRows = () => {
                if (!tbody) return;

                tbody.innerHTML = ngItemsState.map((row, idx) => {
                    return `
                        <tr class="${isLightModal ? 'hover:bg-slate-50' : 'hover:bg-slate-800/30'} transition-colors">
                            <td class="p-2 text-center text-slate-500 font-mono text-[11px]">${idx + 1}</td>
                            <td class="p-2 font-sans">
                                <select data-row-idx="${idx}" data-field="component_type" class="w-full ${isLightModal ? 'bg-slate-50 border-slate-200 text-cyan-800' : 'bg-slate-950 border-slate-800 text-cyan-300'} border rounded-lg px-2 py-1.5 text-xs font-bold focus:outline-none focus:border-cyan-500">
                                    <option value="ASSY" ${row.component_type === 'ASSY' ? 'selected' : ''}>ASSY</option>
                                    <option value="ROD" ${row.component_type === 'ROD' ? 'selected' : ''}>ROD</option>
                                    <option value="CAP" ${row.component_type === 'CAP' ? 'selected' : ''}>CAP</option>
                                </select>
                            </td>
                            <td class="p-2">
                                <input type="number" min="1" value="${row.quantity}" data-row-idx="${idx}" data-field="quantity" required class="w-full ${isLightModal ? 'bg-slate-50 border-slate-200 text-cyan-900' : 'bg-slate-950 border-slate-800 text-cyan-400'} border rounded-lg px-2.5 py-1.5 text-xs font-mono font-bold focus:outline-none focus:border-cyan-500" />
                            </td>
                            <td class="p-2 font-sans">
                                <div class="relative flex items-center">
                                    <input 
                                        type="text" 
                                        list="ng-sections-datalist" 
                                        data-row-idx="${idx}" 
                                        data-field="section" 
                                        value="${row.section || ''}" 
                                        placeholder="Ketik / pilih Bagian..." 
                                        autocomplete="off"
                                        class="w-full ${isLightModal ? 'bg-white border-slate-300 text-slate-900 placeholder:text-slate-400 focus:bg-cyan-50/20' : 'bg-slate-950 border-slate-800 text-slate-100 placeholder:text-slate-500 focus:bg-slate-900'} border rounded-lg pl-2.5 pr-6 py-1.5 text-xs font-sans focus:outline-none focus:border-cyan-500 shadow-xs transition-colors" 
                                    />
                                    <span class="absolute right-2 text-slate-400 pointer-events-none">
                                        <i data-lucide="chevron-down" class="w-3 h-3"></i>
                                    </span>
                                </div>
                            </td>
                            <td class="p-2 font-sans">
                                <div class="relative flex items-center">
                                    <input 
                                        type="text" 
                                        list="ng-reasons-datalist" 
                                        data-row-idx="${idx}" 
                                        data-field="reason" 
                                        value="${row.reason || ''}" 
                                        placeholder="Ketik / cari Penyebab Defect..." 
                                        autocomplete="off"
                                        class="w-full ${isLightModal ? 'bg-white border-slate-300 text-slate-900 placeholder:text-slate-400 focus:bg-cyan-50/20' : 'bg-slate-950 border-slate-800 text-slate-100 placeholder:text-slate-500 focus:bg-slate-900'} border rounded-lg pl-2.5 pr-6 py-1.5 text-xs font-sans focus:outline-none focus:border-cyan-500 shadow-xs transition-colors" 
                                    />
                                    <span class="absolute right-2 text-slate-400 pointer-events-none">
                                        <i data-lucide="chevron-down" class="w-3 h-3"></i>
                                    </span>
                                </div>
                            </td>
                            <td class="p-2 text-center">
                                <button type="button" data-delete-row-idx="${idx}" class="p-1.5 rounded-lg text-slate-400 hover:text-rose-600 hover:bg-rose-50 transition-colors cursor-pointer" title="Hapus Baris">
                                    <i data-lucide="trash-2" class="w-3.5 h-3.5"></i>
                                </button>
                            </td>
                        </tr>
                    `;
                }).join('');

                if (window.lucide) window.lucide.createIcons();
                if (this.currentLang === 'ja') {
                    i18n.localizeDom(tbody);
                }

                // Bind Row Inputs Event Listeners (support both typing and datalist picking)
                tbody.querySelectorAll('select, input').forEach(el => {
                    const handleFieldUpdate = (e) => {
                        const idx = parseInt(e.target.getAttribute('data-row-idx'));
                        const field = e.target.getAttribute('data-field');
                        if (ngItemsState[idx]) {
                            if (field === 'quantity') {
                                ngItemsState[idx][field] = parseInt(e.target.value || 0);
                            } else {
                                ngItemsState[idx][field] = e.target.value;
                            }
                            updateLiveBalances();
                        }
                    };

                    el.addEventListener('input', handleFieldUpdate);
                    el.addEventListener('change', handleFieldUpdate);
                });

                // Bind Delete Row Buttons
                tbody.querySelectorAll('[data-delete-row-idx]').forEach(btn => {
                    btn.addEventListener('click', (e) => {
                        const idx = parseInt(e.currentTarget.getAttribute('data-delete-row-idx'));
                        if (ngItemsState.length <= 1) {
                            alert('Minimal harus memiliki 1 baris rincian NG.');
                            return;
                        }
                        ngItemsState.splice(idx, 1);
                        renderDynamicRows();
                    });
                });

                updateLiveBalances();
            };

            const updateLiveBalances = () => {
                let sumAssy = 0;
                let sumRod = 0;
                let sumCap = 0;

                ngItemsState.forEach(it => {
                    const q = parseInt(it.quantity || 0);
                    const type = (it.component_type || 'ASSY').toUpperCase();
                    if (type === 'ASSY') sumAssy += q;
                    else if (type === 'ROD') sumRod += q;
                    else if (type === 'CAP') sumCap += q;
                });

                const totalOeeSum = sumAssy + sumRod + sumCap;
                const variance = totalOeeSum - targetNg;

                if (badgeSubAssy) badgeSubAssy.textContent = `${sumAssy} Pcs`;
                if (badgeSubRod) badgeSubRod.textContent = `${sumRod} Pcs`;
                if (badgeSubCap) badgeSubCap.textContent = `${sumCap} Pcs`;

                if (balanceIndicator) {
                    if (totalOeeSum === targetNg) {
                        balanceIndicator.className = 'flex items-center gap-1.5 px-2.5 py-0.5 rounded-full text-[11px] font-mono font-bold bg-emerald-950 text-emerald-400 border border-emerald-800';
                        balanceIndicator.innerHTML = `
                            <i data-lucide="check" class="w-3.5 h-3.5"></i>
                            <span>Total: ${totalOeeSum} / ${targetNg} Pcs (SEIMBANG ✓)</span>
                        `;
                    } else if (totalOeeSum < targetNg) {
                        balanceIndicator.className = 'flex items-center gap-1.5 px-2.5 py-0.5 rounded-full text-[11px] font-mono font-bold bg-amber-950 text-amber-400 border border-amber-800';
                        balanceIndicator.innerHTML = `
                            <i data-lucide="alert-triangle" class="w-3.5 h-3.5"></i>
                            <span>Total: ${totalOeeSum} / ${targetNg} Pcs (Kurang ${Math.abs(variance)} Pcs)</span>
                        `;
                    } else {
                        balanceIndicator.className = 'flex items-center gap-1.5 px-2.5 py-0.5 rounded-full text-[11px] font-mono font-bold bg-rose-950 text-rose-400 border border-rose-800';
                        balanceIndicator.innerHTML = `
                            <i data-lucide="alert-circle" class="w-3.5 h-3.5"></i>
                            <span>Total: ${totalOeeSum} / ${targetNg} Pcs (Lebih +${variance} Pcs)</span>
                        `;
                    }
                    if (window.lucide) window.lucide.createIcons();
                    if (this.currentLang === 'ja') {
                        i18n.localizeDom(balanceIndicator);
                    }
                }
            };

            // Bind Add Row Button
            const addRowBtn = document.getElementById('btn-add-ng-row');
            if (addRowBtn) {
                addRowBtn.addEventListener('click', () => {
                    ngItemsState.push({
                        component_type: 'ASSY',
                        quantity: 1,
                        section: '',
                        reason: ''
                    });
                    renderDynamicRows();
                    setTimeout(() => {
                        const tableScrollContainer = tbody?.closest('.overflow-y-auto');
                        if (tableScrollContainer) {
                            tableScrollContainer.scrollTo({
                                top: tableScrollContainer.scrollHeight,
                                behavior: 'smooth'
                            });
                        }
                    }, 50);
                });
            }

            renderDynamicRows();

            // 2. Close Modal Handlers
            const closeModal = () => {
                modalContainer.innerHTML = '';
            };
            document.getElementById('btn-close-ng-modal')?.addEventListener('click', closeModal);
            document.getElementById('btn-cancel-ng-modal')?.addEventListener('click', closeModal);

            // 3. Delete / Reset Detail Handler
            const deleteBtn = document.getElementById('btn-delete-ng-detail');
            if (deleteBtn && ng.id) {
                deleteBtn.addEventListener('click', async () => {
                    const confirmed = await this.showConfirmModal({
                        title: 'Reset & Hapus Detail NG',
                        subtitle: 'Apakah Anda yakin ingin menghapus / mereset rincian detail NG untuk laporan ini?',
                        confirmText: 'Ya, Hapus Data',
                        cancelText: 'Batal',
                        type: 'danger'
                    });

                    if (confirmed) {
                        try {
                            await api.deleteNgDetail(ng.id);
                            this.showNotification('Data Dihapus', 'Detail NG berhasil direset.', 'delete');
                            closeModal();
                            this.renderNgReport();
                        } catch (err) {
                            this.showNotification('Gagal Menghapus', err.response?.data?.message || err.message, 'delete');
                        }
                    }
                });
            }

            // 4. Submit Form Handler
            const form = document.getElementById('form-ng-detail');
            if (form) {
                form.addEventListener('submit', async (e) => {
                    e.preventDefault();
                    const formData = new FormData(e.target);
                    const payload = Object.fromEntries(formData.entries());

                    // Reset previous validation borders
                    form.querySelectorAll('select, input').forEach(el => {
                        el.classList.remove('border-rose-500', 'ring-2', 'ring-rose-500/50', 'bg-rose-500/10');
                    });

                    // 1. STRICT VALIDATION: Check each row for missing required fields
                    const errors = [];
                    let firstInvalidEl = null;

                    const markRowInvalid = (selector, msg) => {
                        errors.push(msg);
                        const el = form.querySelector(selector);
                        if (el) {
                            el.classList.add('border-rose-500', 'ring-2', 'ring-rose-500/50', 'bg-rose-500/10');
                            if (!firstInvalidEl) firstInvalidEl = el;

                            const clearErr = () => {
                                el.classList.remove('border-rose-500', 'ring-2', 'ring-rose-500/50', 'bg-rose-500/10');
                                el.removeEventListener('input', clearErr);
                                el.removeEventListener('change', clearErr);
                            };
                            el.addEventListener('input', clearErr);
                            el.addEventListener('change', clearErr);
                        }
                    };

                    if (!ngItemsState || ngItemsState.length === 0) {
                        await this.showAlertModal({
                            title: 'Data Tidak Dapat Disimpan',
                            subtitle: 'Minimal harus mengisi 1 baris rincian komponen NG.',
                            buttonText: 'Saya Mengerti'
                        });
                        return;
                    }

                    ngItemsState.forEach((it, idx) => {
                        const rowNum = idx + 1;
                        const qty = parseInt(it.quantity || 0);

                        if (!it.component_type || String(it.component_type).trim() === '') {
                            markRowInvalid(`select[data-row-idx="${idx}"][data-field="component_type"]`, `<strong>Baris #${rowNum}:</strong> Komponen OEE wajib dipilih.`);
                        }

                        if (isNaN(qty) || qty <= 0) {
                            markRowInvalid(`input[data-row-idx="${idx}"][data-field="quantity"]`, `<strong>Baris #${rowNum}:</strong> Jumlah Qty NG tidak boleh 0 atau kosong.`);
                        }

                        if (!it.section || String(it.section).trim() === '') {
                            markRowInvalid(`select[data-row-idx="${idx}"][data-field="section"]`, `<strong>Baris #${rowNum}:</strong> Bagian NG wajib dipilih.`);
                        }

                        if (!it.reason || String(it.reason).trim() === '') {
                            markRowInvalid(`select[data-row-idx="${idx}"][data-field="reason"]`, `<strong>Baris #${rowNum}:</strong> Penyebab / Remark defect wajib dipilih.`);
                        }
                    });

                    if (errors.length > 0) {
                        this.showNotification('Data Tidak Dapat Disimpan', 'Isi data detail yang masih kosong! (Komponen, Jumlah, Bagian NG, Penyebab tidak boleh kosong)', 'delete');
                        
                        await this.showAlertModal({
                            title: 'Data Tidak Dapat Disimpan',
                            subtitle: 'Isi data detail rincian NG yang masih kosong (Komponen, Jumlah, Bagian NG, Penyebab tidak boleh kosong):',
                            errors: errors,
                            buttonText: 'Perbaiki Isian Sekarang'
                        });

                        if (firstInvalidEl) {
                            firstInvalidEl.focus();
                            firstInvalidEl.scrollIntoView({ behavior: 'smooth', block: 'center' });
                        }
                        return;
                    }

                    // Calculate total from dynamic items
                    let totalSum = 0;
                    ngItemsState.forEach(it => {
                        totalSum += parseInt(it.quantity || 0);
                    });

                    if (totalSum !== targetNg) {
                        const selisih = totalSum - targetNg;
                        const msg = selisih > 0 
                            ? `Total rincian komponen OEE (${totalSum} Pcs) melebihi target (${targetNg} Pcs) sebanyak +${selisih} Pcs.`
                            : `Total rincian komponen OEE (${totalSum} Pcs) masih kurang ${Math.abs(selisih)} Pcs dari target (${targetNg} Pcs).`;
                        
                        const proceed = await this.showConfirmModal({
                            title: 'Konfirmasi Keseimbangan Target NG',
                            subtitle: `${msg}\nApakah Anda tetap ingin melanjutkan penyimpanan data ini?`,
                            confirmText: 'Tetap Simpan',
                            cancelText: 'Periksa Ulang'
                        });

                        if (!proceed) {
                            return;
                        }
                    }

                    payload.items = ngItemsState;

                    try {
                        await api.saveNgDetail(payload);
                        this.playClingSound();
                        this.showNotification('Detail NG Tersimpan', `Rincian ${ngItemsState.length} baris NG (${totalSum} Pcs OEE) berhasil disimpan.`, 'create');
                        closeModal();
                        this.renderNgReport();
                    } catch (err) {
                        const errMsg = err.response?.data?.message || err.message || 'Gagal menyimpan detail NG.';
                        this.showNotification('⚠️ Gagal Menyimpan', errMsg, 'delete');
                    }
                });
            }
        } catch (err) {
            this.showNotification('Gagal Memuat Data', err.response?.data?.message || err.message, 'delete');
            modalContainer.innerHTML = '';
        }
    }

    // ==========================================
    // 4. PRODUCTION LINES PAGE
    // ==========================================
    async renderLines() {
        const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');
        const res = await api.getLineRanking(this.filters);
        const lines = res.data.data || [];

        const content = document.getElementById('content-body');
        content.innerHTML = `
            <!-- HEADER -->
            <div class="${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-2xl p-5 shadow-xl mb-5 flex flex-col sm:flex-row sm:items-center justify-between gap-4">
                <div>
                    <div class="flex items-center gap-3">
                        <h2 class="text-xl font-black ${isLight ? 'text-slate-900' : 'text-slate-100'} flex items-center gap-2">
                            <i data-lucide="git-fork" class="w-6 h-6 text-cyan-400"></i>
                            <span>Production Lines</span>
                        </h2>
                        <span class="px-2.5 py-0.5 rounded-full ${isLight ? 'bg-cyan-50 text-cyan-700 border-cyan-200' : 'bg-cyan-950/80 text-cyan-300 border-cyan-800/80'} border font-mono font-bold text-xs">
                            Total ${lines.length} Lini
                        </span>
                    </div>
                    <p class="text-xs ${isLight ? 'text-slate-500' : 'text-slate-400'} mt-1">
                        Efisiensi OEE, ketersediaan, performa, mutu kualitas, dan produk yang diproduksi per lini produksi.
                    </p>
                </div>
            </div>

            <!-- LINES GRID -->
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-5">
                ${lines.map(line => {
                    const oeeNum = Number(line.oee || 0);
                    const oeeColor = oeeNum >= 85 ? 'text-emerald-400 font-extrabold' : (oeeNum >= 65 ? 'text-amber-400 font-bold' : (oeeNum > 0 ? 'text-rose-400 font-bold' : 'text-slate-500'));
                    const prodList = (line.products_produced && line.products_produced.length > 0) 
                        ? line.products_produced 
                        : (line.products_assigned || []);
                    
                    return `
                        <div class="${isLight ? 'bg-white border-slate-200 hover:border-cyan-400 shadow-lg' : 'bg-slate-900 border-slate-800 hover:border-cyan-500/60 shadow-xl'} border rounded-2xl p-5 transition-all flex flex-col justify-between">
                            <div>
                                <!-- LINE HEADER -->
                                <div class="flex items-center justify-between mb-2">
                                    <div class="flex items-center gap-2">
                                        <span class="text-xs font-mono font-bold text-cyan-400">${line.code}</span>
                                        <h3 class="font-bold text-base ${isLight ? 'text-slate-900' : 'text-slate-100'}">${line.name}</h3>
                                    </div>
                                    <span class="px-2.5 py-0.5 rounded text-[10px] font-bold border ${this.getStatusBadge(line.oee_status)}">
                                        ${line.oee_status || 'CRITICAL'}
                                    </span>
                                </div>

                                <!-- DYNAMIC PRODUCT DISPLAY (REPLACES STATIC AREA LABEL) -->
                                <div class="mb-4 p-2.5 rounded-xl ${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950/70 border-slate-800/80'} border">
                                    <div class="flex items-center gap-1.5 text-[11px] font-semibold text-slate-400 mb-1.5">
                                        <i data-lucide="package" class="w-3.5 h-3.5 text-cyan-400"></i>
                                        <span>Type yang diproduksi:</span>
                                    </div>
                                    <div class="flex flex-wrap gap-1.5">
                                        ${prodList.length === 0 ? `
                                            <span class="text-[11px] text-slate-500 italic">Belum ada produk</span>
                                        ` : prodList.map(p => `
                                            <span class="px-2 py-0.5 rounded-md text-[11px] font-bold font-mono ${isLight ? 'bg-cyan-100/70 text-cyan-800 border-cyan-300/80' : 'bg-cyan-950/80 text-cyan-300 border-cyan-800/80'} border shadow-2xs">
                                                ${p}
                                            </span>
                                        `).join('')}
                                    </div>
                                </div>

                                <!-- OEE PILLARS METRICS -->
                                <div class="space-y-2 font-mono text-xs border-t ${isLight ? 'border-slate-200' : 'border-slate-800'} pt-3">
                                    <div class="flex justify-between items-center">
                                        <span class="${isLight ? 'text-slate-600' : 'text-slate-400'} font-sans font-medium">Overall OEE:</span>
                                        <span class="${oeeColor} text-sm">${line.oee}%</span>
                                    </div>
                                    <div class="flex justify-between items-center">
                                        <span class="${isLight ? 'text-slate-600' : 'text-slate-400'} font-sans font-medium">Availability:</span>
                                        <span class="text-cyan-400 font-bold">${line.availability}%</span>
                                    </div>
                                    <div class="flex justify-between items-center">
                                        <span class="${isLight ? 'text-slate-600' : 'text-slate-400'} font-sans font-medium">Performance:</span>
                                        <span class="text-amber-400 font-bold">${line.performance}%</span>
                                    </div>
                                    <div class="flex justify-between items-center">
                                        <span class="${isLight ? 'text-slate-600' : 'text-slate-400'} font-sans font-medium">Quality:</span>
                                        <span class="text-emerald-400 font-bold">${line.quality}%</span>
                                    </div>
                                </div>
                            </div>

                            <!-- BOTTOM OUTPUT ACHIEVEMENT PROGRESS -->
                            <div class="mt-4 pt-3 border-t ${isLight ? 'border-slate-200' : 'border-slate-800'}">
                                <div class="flex justify-between text-xs font-mono mb-1.5 ${isLight ? 'text-slate-600' : 'text-slate-400'}">
                                    <span class="font-sans">Output vs Target:</span>
                                    <span class="font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'}">${(line.actual_quantity || 0).toLocaleString()} / ${(line.target_quantity || 0).toLocaleString()} pcs (${line.achievement}%)</span>
                                </div>
                                <div class="w-full ${isLight ? 'bg-slate-100 border-slate-200' : 'bg-slate-950 border-slate-800'} rounded-full h-2 overflow-hidden border">
                                    <div class="bg-cyan-500 h-full rounded-full transition-all duration-500" style="width: ${Math.min(100, line.achievement)}%"></div>
                                </div>
                            </div>
                        </div>
                    `;
                }).join('')}
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();
        if (this.currentLang === 'ja') {
            i18n.localizeDom(document.getElementById('content-body') || document.body);
        }
    }

    // ==========================================
    // 5. SHIFT PERFORMANCE PAGE
    // ==========================================
    async renderShifts() {
        this.shiftFilters = this.shiftFilters || { date: '', production_line_id: '', machine_id: '' };
        const res = await api.getShiftComparison(this.shiftFilters);
        const shifts = res.data.data || [];
        const dailyBreakdown = res.data.daily_breakdown || [];
        const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');

        const shiftColors = isLight ? {
            1: { 
                cardGradient: 'bg-gradient-to-br from-sky-50/90 via-white to-slate-50/90', 
                border: 'border-sky-200 shadow-sm', 
                badge: 'bg-sky-100 text-sky-800 border-sky-300 font-bold', 
                text: 'text-sky-700', 
                bar: 'bg-sky-500',
                subCardBg: 'bg-white/90 border-slate-200/90 shadow-2xs',
                bottomBoxBg: 'bg-slate-50/90 border-slate-200/80 shadow-inner'
            },
            2: { 
                cardGradient: 'bg-gradient-to-br from-amber-50/90 via-white to-slate-50/90', 
                border: 'border-amber-200 shadow-sm', 
                badge: 'bg-amber-100 text-amber-900 border-amber-300 font-bold', 
                text: 'text-amber-700', 
                bar: 'bg-amber-500',
                subCardBg: 'bg-white/90 border-slate-200/90 shadow-2xs',
                bottomBoxBg: 'bg-slate-50/90 border-slate-200/80 shadow-inner'
            },
            3: { 
                cardGradient: 'bg-gradient-to-br from-emerald-50/90 via-white to-slate-50/90', 
                border: 'border-emerald-200 shadow-sm', 
                badge: 'bg-emerald-100 text-emerald-800 border-emerald-300 font-bold', 
                text: 'text-emerald-700', 
                bar: 'bg-emerald-500',
                subCardBg: 'bg-white/90 border-slate-200/90 shadow-2xs',
                bottomBoxBg: 'bg-slate-50/90 border-slate-200/80 shadow-inner'
            },
            4: { 
                cardGradient: 'bg-gradient-to-br from-purple-50/90 via-white to-slate-50/90', 
                border: 'border-purple-200 shadow-sm', 
                badge: 'bg-purple-100 text-purple-800 border-purple-300 font-bold', 
                text: 'text-purple-700', 
                bar: 'bg-purple-500',
                subCardBg: 'bg-white/90 border-slate-200/90 shadow-2xs',
                bottomBoxBg: 'bg-slate-50/90 border-slate-200/80 shadow-inner'
            },
            5: { 
                cardGradient: 'bg-gradient-to-br from-rose-50/90 via-white to-slate-50/90', 
                border: 'border-rose-200 shadow-sm', 
                badge: 'bg-rose-100 text-rose-800 border-rose-300 font-bold', 
                text: 'text-rose-700', 
                bar: 'bg-rose-500',
                subCardBg: 'bg-white/90 border-slate-200/90 shadow-2xs',
                bottomBoxBg: 'bg-slate-50/90 border-slate-200/80 shadow-inner'
            },
        } : {
            1: { 
                cardGradient: 'bg-gradient-to-br from-sky-500/10 via-slate-900 to-slate-900', 
                border: 'border-sky-500/40', 
                badge: 'bg-sky-950/80 text-sky-400 border-sky-800', 
                text: 'text-sky-400', 
                bar: 'bg-sky-400',
                subCardBg: 'bg-slate-950/70 border-slate-800',
                bottomBoxBg: 'bg-slate-950/50 border-slate-800/60'
            },
            2: { 
                cardGradient: 'bg-gradient-to-br from-amber-500/10 via-slate-900 to-slate-900', 
                border: 'border-amber-500/40', 
                badge: 'bg-amber-950/80 text-amber-400 border-amber-800', 
                text: 'text-amber-400', 
                bar: 'bg-amber-400',
                subCardBg: 'bg-slate-950/70 border-slate-800',
                bottomBoxBg: 'bg-slate-950/50 border-slate-800/60'
            },
            3: { 
                cardGradient: 'bg-gradient-to-br from-emerald-500/10 via-slate-900 to-slate-900', 
                border: 'border-emerald-500/40', 
                badge: 'bg-emerald-950/80 text-emerald-400 border-emerald-800', 
                text: 'text-emerald-400', 
                bar: 'bg-emerald-400',
                subCardBg: 'bg-slate-950/70 border-slate-800',
                bottomBoxBg: 'bg-slate-950/50 border-slate-800/60'
            },
            4: { 
                cardGradient: 'bg-gradient-to-br from-purple-500/10 via-slate-900 to-slate-900', 
                border: 'border-purple-500/40', 
                badge: 'bg-purple-950/80 text-purple-400 border-purple-800', 
                text: 'text-purple-400', 
                bar: 'bg-purple-400',
                subCardBg: 'bg-slate-950/70 border-slate-800',
                bottomBoxBg: 'bg-slate-950/50 border-slate-800/60'
            },
            5: { 
                cardGradient: 'bg-gradient-to-br from-rose-500/10 via-slate-900 to-slate-900', 
                border: 'border-rose-500/40', 
                badge: 'bg-rose-950/80 text-rose-400 border-rose-800', 
                text: 'text-rose-400', 
                bar: 'bg-rose-400',
                subCardBg: 'bg-slate-950/70 border-slate-800',
                bottomBoxBg: 'bg-slate-950/50 border-slate-800/60'
            },
        };

        const lineOptions = (this.masterData?.lines || []).map(l => 
            `<option value="${l.id}" ${(this.shiftFilters?.production_line_id == l.id) ? 'selected' : ''}>${l.name} (${l.code})</option>`
        ).join('');

        const machineOptions = (this.masterData?.machines || []).map(m => 
            `<option value="${m.id}" ${(this.shiftFilters?.machine_id == m.id) ? 'selected' : ''}>${m.name} (${m.code})</option>`
        ).join('');

        const content = document.getElementById('content-body');
        content.innerHTML = `
            <!-- HEADER CONTEXT & TAB SWITCHER (SHIFT VS TEAM) -->
            <div class="flex flex-wrap items-center justify-between gap-4">
                <div class="flex flex-wrap items-center gap-3">
                    <h2 class="text-xl font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'} flex items-center gap-2">
                        <i data-lucide="clock" class="w-5 h-5 text-cyan-500"></i>
                        <span>Shift Performance</span>
                    </h2>

                    <!-- 2-TAB SEGMENTED CONTROLLER (SHIFT & TEAM PERFORMANCES) -->
                    <div class="flex items-center gap-1 p-1 ${isLight ? 'bg-slate-200/80 border-slate-300' : 'bg-slate-950 border-slate-800'} border rounded-xl shadow-xs">
                        <button type="button" data-perf-tab="shifts" class="px-3 py-1 text-xs font-bold rounded-lg transition-all flex items-center gap-1.5 cursor-pointer ${isLight ? 'bg-white text-cyan-800 shadow-sm' : 'bg-cyan-950 text-cyan-300 border border-cyan-800/80'}">
                            <i data-lucide="clock" class="w-3.5 h-3.5 text-cyan-500"></i> Shift Performance
                        </button>
                        <button type="button" data-perf-tab="teams" class="px-3 py-1 text-xs font-bold rounded-lg transition-all flex items-center gap-1.5 cursor-pointer ${isLight ? 'text-slate-600 hover:text-slate-900' : 'text-slate-400 hover:text-slate-200'}">
                            <i data-lucide="users" class="w-3.5 h-3.5 text-indigo-400"></i> Team Performance
                        </button>
                    </div>
                </div>

                <!-- REALTIME FILTER CONTROLS -->
                <div class="flex flex-wrap items-center gap-2 bg-transparent text-xs">
                    <div class="flex items-center gap-1.5 ${isLight ? 'bg-white border-slate-200 shadow-sm' : 'bg-slate-900 border-slate-800'} border rounded-lg px-2.5 py-1.5 text-slate-400">
                        <i data-lucide="calendar" class="w-3.5 h-3.5 text-cyan-500"></i>
                        <input type="date" id="shift-filter-date" value="${this.shiftFilters?.date || ''}" class="bg-transparent ${isLight ? 'text-slate-800' : 'text-slate-200'} text-xs focus:outline-none cursor-pointer font-sans" />
                    </div>
                    <div class="flex items-center gap-1.5 ${isLight ? 'bg-white border-slate-200 shadow-sm' : 'bg-slate-900 border-slate-800'} border rounded-lg px-2.5 py-1.5 text-slate-400">
                        <i data-lucide="git-branch" class="w-3.5 h-3.5 text-emerald-500"></i>
                        <select id="shift-filter-line" class="bg-transparent ${isLight ? 'text-slate-800' : 'text-slate-200'} text-xs focus:outline-none cursor-pointer font-sans">
                            <option value="">Semua Lini</option>
                            ${lineOptions}
                        </select>
                    </div>
                    <div class="flex items-center gap-1.5 ${isLight ? 'bg-white border-slate-200 shadow-sm' : 'bg-slate-900 border-slate-800'} border rounded-lg px-2.5 py-1.5 text-slate-400">
                        <i data-lucide="cpu" class="w-3.5 h-3.5 text-amber-500"></i>
                        <select id="shift-filter-machine" class="bg-transparent ${isLight ? 'text-slate-800' : 'text-slate-200'} text-xs focus:outline-none cursor-pointer font-sans">
                            <option value="">Semua Mesin</option>
                            ${machineOptions}
                        </select>
                    </div>
                    <button id="btn-reset-shift-filters" class="p-2 ${isLight ? 'bg-white hover:bg-slate-100 border-slate-200 text-slate-700 shadow-sm' : 'bg-slate-800 hover:bg-slate-700 text-slate-300'} border rounded-lg transition-colors cursor-pointer" title="Reset Filter">
                        <i data-lucide="rotate-ccw" class="w-3.5 h-3.5"></i>
                    </button>
                </div>
            </div>

            <!-- COMPACT, SLEEK & INFORMATIVE SHIFT CARDS -->
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4.5">
                ${shifts.map((shift, sIdx) => {
                    const theme = shiftColors[shift.shift_id] || shiftColors[(sIdx % 5) + 1];
                    const bestBadge = shift.is_best_performer ? `
                        <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full ${isLight ? 'bg-amber-100 text-amber-900 border-amber-300' : 'bg-amber-950 text-amber-300 border-amber-800'} border text-[10px] font-bold shadow-xs animate-pulse">
                            🏆 Best Performer
                        </span>
                    ` : '';

                    return `
                        <div class="${theme.cardGradient} border ${shift.is_best_performer ? (isLight ? 'border-amber-400 ring-2 ring-amber-400/30' : 'border-amber-500/60 ring-2 ring-amber-500/20') : theme.border} rounded-2xl p-4.5 shadow-lg relative overflow-hidden transition-all duration-300 hover:-translate-y-1 hover:shadow-xl group">
                            <!-- TOP BAR: BADGE, WORKING HOURS & STATUS -->
                            <div class="flex items-start justify-between gap-2 mb-3">
                                <div>
                                    <div class="flex items-center gap-1.5">
                                        <span class="px-2.5 py-0.5 rounded-md text-[11px] font-bold border ${theme.badge} font-mono shadow-xs">
                                            🕒 ${shift.shift_name}
                                        </span>
                                    </div>
                                    <span class="text-[10.5px] ${isLight ? 'text-slate-600 font-medium' : 'text-slate-400'} font-mono block mt-1">
                                        ${shift.working_hours}
                                    </span>
                                </div>
                                <div class="flex flex-col items-end gap-1">
                                    <span class="px-2 py-0.5 rounded text-[10px] font-bold border ${this.getStatusBadge(shift.oee_status)}">
                                        ${shift.oee_status}
                                    </span>
                                    ${bestBadge}
                                </div>
                            </div>

                            <!-- OEE HERO ROW & PROGRESS BAR -->
                            <div class="mb-3.5 pb-2.5 border-b ${isLight ? 'border-slate-200' : 'border-slate-800'}">
                                <div class="flex items-baseline justify-between">
                                    <span class="text-3xl font-black ${theme.text} font-mono tracking-tight">${shift.oee}%</span>
                                    <span class="text-[11px] font-bold ${isLight ? 'text-slate-600' : 'text-slate-400'} uppercase font-sans">Overall OEE</span>
                                </div>
                                <div class="w-full ${isLight ? 'bg-slate-200' : 'bg-slate-950'} rounded-full h-1.5 mt-1.5 overflow-hidden">
                                    <div class="${theme.bar} h-full rounded-full transition-all duration-700" style="width: ${Math.min(100, shift.oee)}%"></div>
                                </div>
                            </div>

                            <!-- 3 PILLARS (AVAILABILITY, PERFORMANCE, QUALITY) -->
                            <div class="grid grid-cols-3 gap-1.5 mb-3 text-center font-mono">
                                <div class="${theme.subCardBg} border rounded-xl p-2">
                                    <div class="text-[9.5px] ${isLight ? 'text-slate-600 font-semibold' : 'text-slate-400'} font-sans">Avail</div>
                                    <div class="${isLight ? 'text-emerald-700' : 'text-emerald-400'} font-bold text-xs mt-0.5">${shift.availability}%</div>
                                </div>
                                <div class="${theme.subCardBg} border rounded-xl p-2">
                                    <div class="text-[9.5px] ${isLight ? 'text-slate-600 font-semibold' : 'text-slate-400'} font-sans">Perf</div>
                                    <div class="${isLight ? 'text-amber-700' : 'text-amber-400'} font-bold text-xs mt-0.5">${shift.performance}%</div>
                                </div>
                                <div class="${theme.subCardBg} border rounded-xl p-2">
                                    <div class="text-[9.5px] ${isLight ? 'text-slate-600 font-semibold' : 'text-slate-400'} font-sans">Qual</div>
                                    <div class="${isLight ? 'text-purple-700' : 'text-purple-400'} font-bold text-xs mt-0.5">${shift.quality}%</div>
                                </div>
                            </div>

                            <!-- COMPACT PRODUCTION QUANTITIES -->
                            <div class="space-y-1.5 text-[11px] font-mono ${theme.bottomBoxBg} rounded-xl p-3 border">
                                <div class="flex justify-between items-center ${isLight ? 'text-slate-700' : 'text-slate-300'}">
                                    <span class="font-sans text-[10.5px]">Target / Actual:</span>
                                    <strong class="${isLight ? 'text-slate-900' : 'text-white'} font-bold">${shift.target_quantity.toLocaleString()} / <span class="text-cyan-500">${shift.total_quantity.toLocaleString()}</span></strong>
                                </div>
                                <div class="flex justify-between items-center ${isLight ? 'text-slate-700' : 'text-slate-300'}">
                                    <span class="font-sans text-[10.5px]">Good / NG:</span>
                                    <span>
                                        <strong class="${isLight ? 'text-emerald-700' : 'text-emerald-400'}">${shift.good_quantity.toLocaleString()} OK</strong>
                                        <span class="text-slate-400 mx-0.5">•</span>
                                        <strong class="${isLight ? 'text-rose-700' : 'text-rose-400'}">${shift.reject_quantity.toLocaleString()} NG</strong>
                                    </span>
                                </div>
                                <div class="pt-1.5 border-t ${isLight ? 'border-slate-200' : 'border-slate-800'} flex justify-between items-center text-[10.5px]">
                                    <span class="font-sans text-slate-500">Yield / Reject:</span>
                                    <span>
                                        <strong class="${isLight ? 'text-emerald-700' : 'text-emerald-400'}">${shift.yield_rate}%</strong>
                                        <span class="text-slate-400 mx-0.5">/</span>
                                        <strong class="${isLight ? 'text-rose-700' : 'text-rose-400'}">${shift.rejection_rate}%</strong>
                                    </span>
                                </div>
                            </div>
                        </div>
                    `;
                }).join('')}
            </div>

            <!-- ANIMATED SHIFT COMPARISON CHART -->
            <div class="${isLight ? 'bg-white border-slate-200 shadow-lg' : 'bg-slate-900 border-slate-800 shadow-xl'} border rounded-2xl p-5">
                <div class="flex items-center justify-between mb-3 border-b ${isLight ? 'border-slate-200' : 'border-slate-800'} pb-3">
                    <div>
                        <h3 class="text-sm font-bold ${isLight ? 'text-slate-800' : 'text-slate-100'} flex items-center gap-2">
                            <i data-lucide="bar-chart-2" class="w-4 h-4 text-cyan-500"></i>
                            Shift OEE & Pillars Comparative Metrics
                        </h3>
                    </div>
                </div>
                <div id="chart-shift-comparison" class="w-full h-72"></div>
            </div>

            <!-- DAILY BREAKDOWN PER PRODUCTION DATE TABLE -->
            <div class="${isLight ? 'bg-white border-slate-200 shadow-lg' : 'bg-slate-900 border-slate-800 shadow-xl'} border rounded-2xl p-5">
                <div class="flex items-center justify-between mb-4 border-b ${isLight ? 'border-slate-200' : 'border-slate-800'} pb-3">
                    <div>
                        <h3 class="text-sm font-bold ${isLight ? 'text-slate-800' : 'text-slate-100'} flex items-center gap-2">
                            <i data-lucide="calendar-range" class="w-4 h-4 text-amber-500"></i>
                            Daily Production Shift Breakdown
                        </h3>
                    </div>
                </div>

                <div class="overflow-x-auto">
                    <table class="w-full text-left text-xs">
                        <thead class="${isLight ? 'bg-slate-100 text-slate-700 border-slate-200' : 'bg-slate-950 text-slate-400 border-slate-800'} uppercase font-semibold text-[10px] border-b">
                            <tr>
                                <th class="p-3">Tanggal Produksi</th>
                                <th class="p-3">Shift Name</th>
                                <th class="p-3 text-center">OEE %</th>
                                <th class="p-3 text-center">Availability</th>
                                <th class="p-3 text-center">Performance</th>
                                <th class="p-3 text-center">Quality</th>
                                <th class="p-3 text-right">Total Measuring</th>
                                <th class="p-3 text-right">Finish Good</th>
                                <th class="p-3 text-right">Not Good (NG)</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y ${isLight ? 'divide-slate-200 text-slate-800' : 'divide-slate-800/60 text-slate-200'} font-mono">
                            ${dailyBreakdown.length === 0 ? `
                                <tr><td colspan="9" class="p-6 text-center text-slate-500 font-sans">Belum ada data log produksi untuk filter terpilih</td></tr>
                            ` : dailyBreakdown.map(day => {
                                return day.shifts.map((s, idx) => `
                                    <tr class="${isLight ? 'hover:bg-slate-50/80' : 'hover:bg-slate-800/40'} transition-colors">
                                        ${idx === 0 ? `<td rowspan="${day.shifts.length}" class="p-3 ${isLight ? 'text-cyan-800 bg-slate-50 border-slate-200 font-bold' : 'text-cyan-400 bg-slate-950/40 border-slate-800 font-bold'} font-sans align-middle border-r">${day.date}</td>` : ''}
                                        <td class="p-3 font-sans font-bold ${isLight ? 'text-slate-800' : 'text-slate-200'}">${s.shift_name}</td>
                                        <td class="p-3 text-center ${isLight ? 'text-cyan-800' : 'text-cyan-400'} font-bold">${s.oee}%</td>
                                        <td class="p-3 text-center ${isLight ? 'text-emerald-800' : 'text-emerald-400'} font-semibold">${s.availability}%</td>
                                        <td class="p-3 text-center ${isLight ? 'text-amber-800' : 'text-amber-400'} font-semibold">${s.performance}%</td>
                                        <td class="p-3 text-center ${isLight ? 'text-purple-800' : 'text-purple-400'} font-semibold">${s.quality}%</td>
                                        <td class="p-3 text-right ${isLight ? 'text-slate-800' : 'text-slate-200'} font-semibold">${(s.total_quantity ?? 0).toLocaleString()} pcs</td>
                                        <td class="p-3 text-right ${isLight ? 'text-emerald-700 font-bold' : 'text-emerald-400 font-bold'}">${(s.good_quantity ?? 0).toLocaleString()} pcs</td>
                                        <td class="p-3 text-right ${isLight ? 'text-rose-700 font-bold' : 'text-rose-400 font-bold'}">${(s.reject_quantity ?? 0).toLocaleString()} pcs</td>
                                    </tr>
                                `).join('');
                            }).join('')}
                        </tbody>
                    </table>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();

        // Render Shift Comparison ApexChart
        this.renderShiftComparisonChart(shifts);

        // Tab switcher event listeners
        content.querySelectorAll('[data-perf-tab]').forEach(btn => {
            btn.addEventListener('click', (e) => {
                const tab = btn.getAttribute('data-perf-tab');
                this.switchTab(tab);
            });
        });

        // Filter event listeners
        const dateInput = document.getElementById('shift-filter-date');
        const lineSelect = document.getElementById('shift-filter-line');
        const machineSelect = document.getElementById('shift-filter-machine');
        const resetBtn = document.getElementById('btn-reset-shift-filters');

        const updateFilters = () => {
            this.shiftFilters = {
                date: dateInput ? dateInput.value : '',
                production_line_id: lineSelect ? lineSelect.value : '',
                machine_id: machineSelect ? machineSelect.value : ''
            };
            this.renderShifts();
        };

        if (dateInput) dateInput.addEventListener('change', updateFilters);
        if (lineSelect) lineSelect.addEventListener('change', updateFilters);
        if (machineSelect) machineSelect.addEventListener('change', updateFilters);
        if (resetBtn) {
            resetBtn.addEventListener('click', () => {
                this.shiftFilters = {};
                this.renderShifts();
            });
        }

        if (this.currentLang === 'ja') {
            i18n.localizeDom(content);
        }
    }

    renderShiftComparisonChart(shifts) {
        const el = document.getElementById('chart-shift-comparison');
        if (!el || !window.ApexCharts) return;

        const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');
        const categories = ['OEE Score', 'Availability', 'Performance', 'Quality Rate'];

        const series = shifts.map(s => {
            return {
                name: s.shift_name,
                data: [s.oee, s.availability, s.performance, s.quality]
            };
        });

        const options = {
            chart: {
                type: 'bar',
                height: 280,
                background: 'transparent',
                toolbar: { show: false },
                animations: {
                    enabled: true,
                    easing: 'easeinout',
                    speed: 600,
                }
            },
            theme: { mode: isLight ? 'light' : 'dark' },
            colors: ['#0284c7', '#f59e0b', '#10b981', '#8b5cf6', '#f43f5e'],
            plotOptions: {
                bar: {
                    horizontal: false,
                    columnWidth: '40%',
                    borderRadius: 6,
                    dataLabels: { position: 'top' }
                }
            },
            dataLabels: {
                enabled: true,
                style: { colors: [isLight ? '#0f172a' : '#ffffff'], fontSize: '10px', fontWeight: 'bold' },
                formatter: val => val + '%',
                offsetY: -18
            },
            stroke: { show: true, width: 2, colors: ['transparent'] },
            series: series,
            xaxis: {
                categories: categories,
                labels: { style: { colors: isLight ? '#334155' : '#94a3b8', fontSize: '11px', fontWeight: '600' } },
                axisBorder: { color: isLight ? '#cbd5e1' : '#334155' }
            },
            yaxis: {
                max: 100,
                labels: { style: { colors: isLight ? '#475569' : '#64748b', fontSize: '10px' }, formatter: val => val + '%' }
            },
            grid: { borderColor: isLight ? '#e2e8f0' : '#1e293b', strokeDashArray: 4, padding: { top: 10, right: 10, bottom: 0, left: 10 } },
            legend: {
                position: 'top',
                horizontalAlign: 'right',
                labels: { colors: isLight ? '#1e293b' : '#cbd5e1' }
            },
            tooltip: {
                theme: isLight ? 'light' : 'dark',
                y: { formatter: val => val + '%' }
            }
        };

        if (this.charts['shiftComp']) this.charts['shiftComp'].destroy();
        this.charts['shiftComp'] = new window.ApexCharts(el, options);
        this.charts['shiftComp'].render();
    }

    // ==========================================
    // 5B. TEAM PERFORMANCE PAGE
    // ==========================================
    async renderTeams() {
        this.teamFilters = this.teamFilters || { date: '', production_line_id: '', machine_id: '' };
        const res = await api.getTeamComparison(this.teamFilters);
        const teams = res.data.data || [];
        const dailyBreakdown = res.data.daily_breakdown || [];
        const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');

        const teamColors = isLight ? {
            1: { 
                cardGradient: 'bg-gradient-to-br from-indigo-50/90 via-white to-slate-50/90', 
                border: 'border-indigo-200 shadow-sm', 
                badge: 'bg-indigo-100 text-indigo-800 border-indigo-300 font-bold', 
                text: 'text-indigo-700', 
                bar: 'bg-indigo-500',
                subCardBg: 'bg-white/90 border-slate-200/90 shadow-2xs',
                bottomBoxBg: 'bg-slate-50/90 border-slate-200/80 shadow-inner'
            },
            2: { 
                cardGradient: 'bg-gradient-to-br from-emerald-50/90 via-white to-slate-50/90', 
                border: 'border-emerald-200 shadow-sm', 
                badge: 'bg-emerald-100 text-emerald-800 border-emerald-300 font-bold', 
                text: 'text-emerald-700', 
                bar: 'bg-emerald-500',
                subCardBg: 'bg-white/90 border-slate-200/90 shadow-2xs',
                bottomBoxBg: 'bg-slate-50/90 border-slate-200/80 shadow-inner'
            },
            3: { 
                cardGradient: 'bg-gradient-to-br from-amber-50/90 via-white to-slate-50/90', 
                border: 'border-amber-200 shadow-sm', 
                badge: 'bg-amber-100 text-amber-900 border-amber-300 font-bold', 
                text: 'text-amber-700', 
                bar: 'bg-amber-500',
                subCardBg: 'bg-white/90 border-slate-200/90 shadow-2xs',
                bottomBoxBg: 'bg-slate-50/90 border-slate-200/80 shadow-inner'
            },
            4: { 
                cardGradient: 'bg-gradient-to-br from-purple-50/90 via-white to-slate-50/90', 
                border: 'border-purple-200 shadow-sm', 
                badge: 'bg-purple-100 text-purple-800 border-purple-300 font-bold', 
                text: 'text-purple-700', 
                bar: 'bg-purple-500',
                subCardBg: 'bg-white/90 border-slate-200/90 shadow-2xs',
                bottomBoxBg: 'bg-slate-50/90 border-slate-200/80 shadow-inner'
            },
        } : {
            1: { 
                cardGradient: 'bg-gradient-to-br from-indigo-500/10 via-slate-900 to-slate-900', 
                border: 'border-indigo-500/40', 
                badge: 'bg-indigo-950/80 text-indigo-300 border-indigo-800', 
                text: 'text-indigo-400', 
                bar: 'bg-indigo-400',
                subCardBg: 'bg-slate-950/70 border-slate-800',
                bottomBoxBg: 'bg-slate-950/50 border-slate-800/60'
            },
            2: { 
                cardGradient: 'bg-gradient-to-br from-emerald-500/10 via-slate-900 to-slate-900', 
                border: 'border-emerald-500/40', 
                badge: 'bg-emerald-950/80 text-emerald-300 border-emerald-800', 
                text: 'text-emerald-400', 
                bar: 'bg-emerald-400',
                subCardBg: 'bg-slate-950/70 border-slate-800',
                bottomBoxBg: 'bg-slate-950/50 border-slate-800/60'
            },
            3: { 
                cardGradient: 'bg-gradient-to-br from-amber-500/10 via-slate-900 to-slate-900', 
                border: 'border-amber-500/40', 
                badge: 'bg-amber-950/80 text-amber-300 border-amber-800', 
                text: 'text-amber-400', 
                bar: 'bg-amber-400',
                subCardBg: 'bg-slate-950/70 border-slate-800',
                bottomBoxBg: 'bg-slate-950/50 border-slate-800/60'
            },
            4: { 
                cardGradient: 'bg-gradient-to-br from-purple-500/10 via-slate-900 to-slate-900', 
                border: 'border-purple-500/40', 
                badge: 'bg-purple-950/80 text-purple-300 border-purple-800', 
                text: 'text-purple-400', 
                bar: 'bg-purple-400',
                subCardBg: 'bg-slate-950/70 border-slate-800',
                bottomBoxBg: 'bg-slate-950/50 border-slate-800/60'
            },
        };

        const lineOptions = (this.masterData?.lines || []).map(l => 
            `<option value="${l.id}" ${(this.teamFilters?.production_line_id == l.id) ? 'selected' : ''}>${l.name} (${l.code})</option>`
        ).join('');

        const machineOptions = (this.masterData?.machines || []).map(m => 
            `<option value="${m.id}" ${(this.teamFilters?.machine_id == m.id) ? 'selected' : ''}>${m.name} (${m.code})</option>`
        ).join('');

        const content = document.getElementById('content-body');
        content.innerHTML = `
            <!-- HEADER CONTEXT & TAB SWITCHER (SHIFT VS TEAM) -->
            <div class="flex flex-wrap items-center justify-between gap-4">
                <div class="flex flex-wrap items-center gap-3">
                    <h2 class="text-xl font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'} flex items-center gap-2">
                        <i data-lucide="users" class="w-5 h-5 text-indigo-400"></i>
                        <span>Team Performance</span>
                    </h2>

                    <!-- 2-TAB SEGMENTED CONTROLLER (SHIFT & TEAM PERFORMANCES) -->
                    <div class="flex items-center gap-1 p-1 ${isLight ? 'bg-slate-200/80 border-slate-300' : 'bg-slate-950 border-slate-800'} border rounded-xl shadow-xs">
                        <button type="button" data-perf-tab="shifts" class="px-3 py-1 text-xs font-bold rounded-lg transition-all flex items-center gap-1.5 cursor-pointer ${isLight ? 'text-slate-600 hover:text-slate-900' : 'text-slate-400 hover:text-slate-200'}">
                            <i data-lucide="clock" class="w-3.5 h-3.5 text-cyan-500"></i> Shift Performance
                        </button>
                        <button type="button" data-perf-tab="teams" class="px-3 py-1 text-xs font-bold rounded-lg transition-all flex items-center gap-1.5 cursor-pointer ${isLight ? 'bg-white text-indigo-800 shadow-sm' : 'bg-indigo-950 text-indigo-300 border border-indigo-800/80'}">
                            <i data-lucide="users" class="w-3.5 h-3.5 text-indigo-400"></i> Team Performance
                        </button>
                    </div>
                </div>

                <!-- REALTIME FILTER CONTROLS -->
                <div class="flex flex-wrap items-center gap-2 bg-transparent text-xs">
                    <div class="flex items-center gap-1.5 ${isLight ? 'bg-white border-slate-200 shadow-sm' : 'bg-slate-900 border-slate-800'} border rounded-lg px-2.5 py-1.5 text-slate-400">
                        <i data-lucide="calendar" class="w-3.5 h-3.5 text-indigo-500"></i>
                        <input type="date" id="team-filter-date" value="${this.teamFilters?.date || ''}" class="bg-transparent ${isLight ? 'text-slate-800' : 'text-slate-200'} text-xs focus:outline-none cursor-pointer font-sans" />
                    </div>
                    <div class="flex items-center gap-1.5 ${isLight ? 'bg-white border-slate-200 shadow-sm' : 'bg-slate-900 border-slate-800'} border rounded-lg px-2.5 py-1.5 text-slate-400">
                        <i data-lucide="git-branch" class="w-3.5 h-3.5 text-emerald-500"></i>
                        <select id="team-filter-line" class="bg-transparent ${isLight ? 'text-slate-800' : 'text-slate-200'} text-xs focus:outline-none cursor-pointer font-sans">
                            <option value="">Semua Lini</option>
                            ${lineOptions}
                        </select>
                    </div>
                    <div class="flex items-center gap-1.5 ${isLight ? 'bg-white border-slate-200 shadow-sm' : 'bg-slate-900 border-slate-800'} border rounded-lg px-2.5 py-1.5 text-slate-400">
                        <i data-lucide="cpu" class="w-3.5 h-3.5 text-amber-500"></i>
                        <select id="team-filter-machine" class="bg-transparent ${isLight ? 'text-slate-800' : 'text-slate-200'} text-xs focus:outline-none cursor-pointer font-sans">
                            <option value="">Semua Mesin</option>
                            ${machineOptions}
                        </select>
                    </div>
                    <button id="btn-reset-team-filters" class="p-2 ${isLight ? 'bg-white hover:bg-slate-100 border-slate-200 text-slate-700 shadow-sm' : 'bg-slate-800 hover:bg-slate-700 text-slate-300'} border rounded-lg transition-colors cursor-pointer" title="Reset Filter">
                        <i data-lucide="rotate-ccw" class="w-3.5 h-3.5"></i>
                    </button>
                </div>
            </div>

            <!-- COMPACT, SLEEK & INFORMATIVE TEAM CARDS -->
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4.5">
                ${teams.map((team, tIdx) => {
                    const theme = teamColors[team.team_id] || teamColors[(tIdx % 4) + 1];
                    const bestBadge = team.is_best_performer ? `
                        <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full ${isLight ? 'bg-amber-100 text-amber-900 border-amber-300' : 'bg-amber-950 text-amber-300 border-amber-800'} border text-[10px] font-bold shadow-xs animate-pulse">
                            🏆 Best Team
                        </span>
                    ` : '';

                    return `
                        <div class="${theme.cardGradient} border ${team.is_best_performer ? (isLight ? 'border-amber-400 ring-2 ring-amber-400/30' : 'border-amber-500/60 ring-2 ring-amber-500/20') : theme.border} rounded-2xl p-4.5 shadow-lg relative overflow-hidden transition-all duration-300 hover:-translate-y-1 hover:shadow-xl group">
                            <!-- TOP BAR: BADGE, LEADER/SPV & STATUS -->
                            <div class="flex items-start justify-between gap-2 mb-3">
                                <div>
                                    <div class="flex items-center gap-1.5">
                                        <span class="px-2.5 py-0.5 rounded-md text-[11px] font-bold border ${theme.badge} font-mono shadow-xs">
                                            👥 ${team.team_name}
                                        </span>
                                    </div>
                                    <span class="text-[10.5px] ${isLight ? 'text-slate-600 font-medium' : 'text-slate-400'} font-sans block mt-1 truncate max-w-[200px]" title="Leader: ${team.leader_name} • SPV: ${team.supervisor_name}">
                                        👤 ${team.leader_name}
                                    </span>
                                </div>
                                <div class="flex flex-col items-end gap-1">
                                    <span class="px-2 py-0.5 rounded text-[10px] font-bold border ${this.getStatusBadge(team.oee_status)}">
                                        ${team.oee_status}
                                    </span>
                                    ${bestBadge}
                                </div>
                            </div>

                            <!-- OEE HERO ROW & PROGRESS BAR -->
                            <div class="mb-3.5 pb-2.5 border-b ${isLight ? 'border-slate-200' : 'border-slate-800'}">
                                <div class="flex items-baseline justify-between">
                                    <span class="text-3xl font-black ${theme.text} font-mono tracking-tight">${team.oee}%</span>
                                    <span class="text-[11px] font-bold ${isLight ? 'text-slate-600' : 'text-slate-400'} uppercase font-sans">Team OEE</span>
                                </div>
                                <div class="w-full ${isLight ? 'bg-slate-200' : 'bg-slate-950'} rounded-full h-1.5 mt-1.5 overflow-hidden">
                                    <div class="${theme.bar} h-full rounded-full transition-all duration-700" style="width: ${Math.min(100, team.oee)}%"></div>
                                </div>
                            </div>

                            <!-- 3 PILLARS (AVAILABILITY, PERFORMANCE, QUALITY) -->
                            <div class="grid grid-cols-3 gap-1.5 mb-3 text-center font-mono">
                                <div class="${theme.subCardBg} border rounded-xl p-2">
                                    <div class="text-[9.5px] ${isLight ? 'text-slate-600 font-semibold' : 'text-slate-400'} font-sans">Avail</div>
                                    <div class="${isLight ? 'text-emerald-700' : 'text-emerald-400'} font-bold text-xs mt-0.5">${team.availability}%</div>
                                </div>
                                <div class="${theme.subCardBg} border rounded-xl p-2">
                                    <div class="text-[9.5px] ${isLight ? 'text-slate-600 font-semibold' : 'text-slate-400'} font-sans">Perf</div>
                                    <div class="${isLight ? 'text-amber-700' : 'text-amber-400'} font-bold text-xs mt-0.5">${team.performance}%</div>
                                </div>
                                <div class="${theme.subCardBg} border rounded-xl p-2">
                                    <div class="text-[9.5px] ${isLight ? 'text-slate-600 font-semibold' : 'text-slate-400'} font-sans">Qual</div>
                                    <div class="${isLight ? 'text-purple-700' : 'text-purple-400'} font-bold text-xs mt-0.5">${team.quality}%</div>
                                </div>
                            </div>

                            <!-- COMPACT PRODUCTION QUANTITIES -->
                            <div class="space-y-1.5 text-[11px] font-mono ${theme.bottomBoxBg} rounded-xl p-3 border">
                                <div class="flex justify-between items-center ${isLight ? 'text-slate-700' : 'text-slate-300'}">
                                    <span class="font-sans text-[10.5px]">Target / Actual:</span>
                                    <strong class="${isLight ? 'text-slate-900' : 'text-white'} font-bold">${team.target_quantity.toLocaleString()} / <span class="text-indigo-500">${team.total_quantity.toLocaleString()}</span></strong>
                                </div>
                                <div class="flex justify-between items-center ${isLight ? 'text-slate-700' : 'text-slate-300'}">
                                    <span class="font-sans text-[10.5px]">Good / NG:</span>
                                    <span>
                                        <strong class="${isLight ? 'text-emerald-700' : 'text-emerald-400'}">${team.good_quantity.toLocaleString()} OK</strong>
                                        <span class="text-slate-400 mx-0.5">•</span>
                                        <strong class="${isLight ? 'text-rose-700' : 'text-rose-400'}">${team.reject_quantity.toLocaleString()} NG</strong>
                                    </span>
                                </div>
                                <div class="pt-1.5 border-t ${isLight ? 'border-slate-200' : 'border-slate-800'} flex justify-between items-center text-[10.5px]">
                                    <span class="font-sans text-slate-500">Yield / Reject:</span>
                                    <span>
                                        <strong class="${isLight ? 'text-emerald-700' : 'text-emerald-400'}">${team.yield_rate}%</strong>
                                        <span class="text-slate-400 mx-0.5">/</span>
                                        <strong class="${isLight ? 'text-rose-700' : 'text-rose-400'}">${team.rejection_rate}%</strong>
                                    </span>
                                </div>
                            </div>
                        </div>
                    `;
                }).join('')}
            </div>

            <!-- ANIMATED TEAM COMPARISON CHART -->
            <div class="${isLight ? 'bg-white border-slate-200 shadow-lg' : 'bg-slate-900 border-slate-800 shadow-xl'} border rounded-2xl p-5">
                <div class="flex items-center justify-between mb-3 border-b ${isLight ? 'border-slate-200' : 'border-slate-800'} pb-3">
                    <div>
                        <h3 class="text-sm font-bold ${isLight ? 'text-slate-800' : 'text-slate-100'} flex items-center gap-2">
                            <i data-lucide="bar-chart-2" class="w-4 h-4 text-indigo-400"></i>
                            Team OEE & Pillars Comparative Metrics
                        </h3>
                    </div>
                </div>
                <div id="chart-team-comparison" class="w-full h-72"></div>
            </div>

            <!-- DAILY BREAKDOWN PER PRODUCTION DATE TABLE FOR TEAMS -->
            <div class="${isLight ? 'bg-white border-slate-200 shadow-lg' : 'bg-slate-900 border-slate-800 shadow-xl'} border rounded-2xl p-5">
                <div class="flex items-center justify-between mb-4 border-b ${isLight ? 'border-slate-200' : 'border-slate-800'} pb-3">
                    <div>
                        <h3 class="text-sm font-bold ${isLight ? 'text-slate-800' : 'text-slate-100'} flex items-center gap-2">
                            <i data-lucide="calendar-range" class="w-4 h-4 text-amber-500"></i>
                            Daily Production Team Breakdown
                        </h3>
                    </div>
                </div>

                <div class="overflow-x-auto">
                    <table class="w-full text-left text-xs">
                        <thead class="${isLight ? 'bg-slate-100 text-slate-700 border-slate-200' : 'bg-slate-950 text-slate-400 border-slate-800'} uppercase font-semibold text-[10px] border-b">
                            <tr>
                                <th class="p-3">Tanggal Produksi</th>
                                <th class="p-3">Team Name</th>
                                <th class="p-3 text-center">OEE %</th>
                                <th class="p-3 text-center">Availability</th>
                                <th class="p-3 text-center">Performance</th>
                                <th class="p-3 text-center">Quality</th>
                                <th class="p-3 text-right">Total Measuring</th>
                                <th class="p-3 text-right">Finish Good</th>
                                <th class="p-3 text-right">Not Good (NG)</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y ${isLight ? 'divide-slate-200 text-slate-800' : 'divide-slate-800/60 text-slate-200'} font-mono">
                            ${dailyBreakdown.length === 0 ? `
                                <tr><td colspan="9" class="p-6 text-center text-slate-500 font-sans">Belum ada data log produksi untuk filter terpilih</td></tr>
                            ` : dailyBreakdown.map(day => {
                                return day.teams.map((t, idx) => `
                                    <tr class="${isLight ? 'hover:bg-slate-50/80' : 'hover:bg-slate-800/40'} transition-colors">
                                        ${idx === 0 ? `<td rowspan="${day.teams.length}" class="p-3 ${isLight ? 'text-indigo-800 bg-slate-50 border-slate-200 font-bold' : 'text-indigo-400 bg-slate-950/40 border-slate-800 font-bold'} font-sans align-middle border-r">${day.date}</td>` : ''}
                                        <td class="p-3 font-sans font-bold ${isLight ? 'text-slate-800' : 'text-slate-200'}">${t.team_name}</td>
                                        <td class="p-3 text-center ${isLight ? 'text-indigo-800' : 'text-indigo-400'} font-bold">${t.oee}%</td>
                                        <td class="p-3 text-center ${isLight ? 'text-emerald-800' : 'text-emerald-400'} font-semibold">${t.availability}%</td>
                                        <td class="p-3 text-center ${isLight ? 'text-amber-800' : 'text-amber-400'} font-semibold">${t.performance}%</td>
                                        <td class="p-3 text-center ${isLight ? 'text-purple-800' : 'text-purple-400'} font-semibold">${t.quality}%</td>
                                        <td class="p-3 text-right ${isLight ? 'text-slate-800' : 'text-slate-200'} font-semibold">${(t.total_quantity ?? 0).toLocaleString()} pcs</td>
                                        <td class="p-3 text-right ${isLight ? 'text-emerald-700 font-bold' : 'text-emerald-400 font-bold'}">${(t.good_quantity ?? 0).toLocaleString()} pcs</td>
                                        <td class="p-3 text-right ${isLight ? 'text-rose-700 font-bold' : 'text-rose-400 font-bold'}">${(t.reject_quantity ?? 0).toLocaleString()} pcs</td>
                                    </tr>
                                `).join('');
                            }).join('')}
                        </tbody>
                    </table>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();

        // Render Team Comparison ApexChart
        this.renderTeamComparisonChart(teams);

        // Tab switcher event listeners
        content.querySelectorAll('[data-perf-tab]').forEach(btn => {
            btn.addEventListener('click', (e) => {
                const tab = btn.getAttribute('data-perf-tab');
                this.switchTab(tab);
            });
        });

        // Filter event listeners
        const dateInput = document.getElementById('team-filter-date');
        const lineSelect = document.getElementById('team-filter-line');
        const machineSelect = document.getElementById('team-filter-machine');
        const resetBtn = document.getElementById('btn-reset-team-filters');

        const updateFilters = () => {
            this.teamFilters = {
                date: dateInput ? dateInput.value : '',
                production_line_id: lineSelect ? lineSelect.value : '',
                machine_id: machineSelect ? machineSelect.value : ''
            };
            this.renderTeams();
        };

        if (dateInput) dateInput.addEventListener('change', updateFilters);
        if (lineSelect) lineSelect.addEventListener('change', updateFilters);
        if (machineSelect) machineSelect.addEventListener('change', updateFilters);
        if (resetBtn) {
            resetBtn.addEventListener('click', () => {
                this.teamFilters = {};
                this.renderTeams();
            });
        }

        if (this.currentLang === 'ja') {
            i18n.localizeDom(content);
        }
    }

    renderTeamComparisonChart(teams) {
        const el = document.getElementById('chart-team-comparison');
        if (!el || !window.ApexCharts) return;

        const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');
        const categories = ['OEE Score', 'Availability', 'Performance', 'Quality Rate'];

        const series = teams.map(t => {
            return {
                name: t.team_name,
                data: [t.oee, t.availability, t.performance, t.quality]
            };
        });

        const options = {
            chart: {
                type: 'bar',
                height: 280,
                background: 'transparent',
                toolbar: { show: false },
                animations: {
                    enabled: true,
                    easing: 'easeinout',
                    speed: 600,
                }
            },
            theme: { mode: isLight ? 'light' : 'dark' },
            colors: ['#6366f1', '#10b981', '#f59e0b', '#ec4899'],
            plotOptions: {
                bar: {
                    horizontal: false,
                    columnWidth: '40%',
                    borderRadius: 6,
                    dataLabels: { position: 'top' }
                }
            },
            dataLabels: {
                enabled: true,
                style: { colors: [isLight ? '#0f172a' : '#ffffff'], fontSize: '10px', fontWeight: 'bold' },
                formatter: val => val + '%',
                offsetY: -18
            },
            stroke: { show: true, width: 2, colors: ['transparent'] },
            series: series,
            xaxis: {
                categories: categories,
                labels: { style: { colors: isLight ? '#334155' : '#94a3b8', fontSize: '11px', fontWeight: '600' } },
                axisBorder: { color: isLight ? '#cbd5e1' : '#334155' }
            },
            yaxis: {
                max: 100,
                labels: { style: { colors: isLight ? '#475569' : '#64748b', fontSize: '10px' }, formatter: val => val + '%' }
            },
            grid: { borderColor: isLight ? '#e2e8f0' : '#1e293b', strokeDashArray: 4, padding: { top: 10, right: 10, bottom: 0, left: 10 } },
            legend: {
                position: 'top',
                horizontalAlign: 'right',
                labels: { colors: isLight ? '#1e293b' : '#cbd5e1' }
            },
            tooltip: {
                theme: isLight ? 'light' : 'dark',
                y: { formatter: val => val + '%' }
            }
        };

        if (this.charts['teamComp']) this.charts['teamComp'].destroy();
        this.charts['teamComp'] = new window.ApexCharts(el, options);
        this.charts['teamComp'].render();
    }

    // ==========================================
    // 6. DOWNTIME ANALYSIS PAGE
    // ==========================================
    async renderDowntime() {
        const [dtListRes, dtParetoRes] = await Promise.all([
            api.getDowntimes(this.filters),
            api.getParetoDowntime(this.filters),
        ]);

        const downtimes = dtListRes.data.data;
        const pareto = dtParetoRes.data.data;

        const totalDowntime = pareto.reduce((sum, i) => sum + i.duration_minutes, 0);
        const totalStops = pareto.reduce((sum, i) => sum + i.stop_count, 0);
        const mttr = totalStops > 0 ? (totalDowntime / totalStops).toFixed(1) : 0;

        const content = document.getElementById('content-body');
        content.innerHTML = `
            <div class="mb-4">
                <h2 class="text-xl font-bold text-slate-100 flex items-center gap-2">
                    <i data-lucide="clock-alert" class="w-5 h-5 text-amber-400"></i>
                    <span>Downtime Analysis</span>
                </h2>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-4 gap-4">
                ${this.kpiCard('Total Downtime', totalDowntime.toFixed(1) + ' mins', 'Accumulated loss duration', 'alert-triangle', 'text-rose-400', 'from-rose-500/10 to-transparent border-rose-500/30')}
                ${this.kpiCard('Number of Stops', totalStops + ' stops', 'Total stop events', 'pause-circle', 'text-amber-400', 'from-amber-500/10 to-transparent border-amber-500/30')}
                ${this.kpiCard('MTTR (Mean Time To Repair)', mttr + ' mins', 'Average repair time per stop', 'wrench', 'text-cyan-400', 'from-cyan-500/10 to-transparent border-cyan-500/30')}
                ${this.kpiCard('MTBF (Mean Time Between Failures)', '320 mins', 'Average operating interval', 'shield-check', 'text-emerald-400', 'from-emerald-500/10 to-transparent border-emerald-500/30')}
            </div>

            <div class="bg-slate-900 border border-slate-800 rounded-xl p-5 shadow-xl">
                <h3 class="text-sm font-bold text-slate-100 mb-4">Downtime Reasons Pareto Chart</h3>
                <div id="chart-pareto-downtime" class="h-64"></div>
            </div>

            <div class="bg-slate-900 border border-slate-800 rounded-xl p-5 shadow-xl">
                <div class="flex items-center justify-between mb-4">
                    <div>
                        <h3 class="text-sm font-bold text-slate-100 flex items-center gap-2">
                            <i data-lucide="history" class="w-4 h-4 text-cyan-400"></i>
                            Downtime & Loss Trouble History Logs
                        </h3>
                        <p class="text-xs text-slate-400">Catatan log problem trouble mesin, tool, jig, material & tindakan perbaikan</p>
                    </div>
                </div>
                <div class="overflow-x-auto">
                    <table class="w-full text-left text-xs">
                        <thead class="bg-slate-950 text-slate-400 uppercase font-semibold text-[10px] border-b border-slate-800">
                            <tr>
                                <th class="p-3 text-center w-12">No</th>
                                <th class="p-3">Waktu Trouble</th>
                                <th class="p-3">Line & Mesin</th>
                                <th class="p-3">Shift & Team</th>
                                <th class="p-3">Product</th>
                                <th class="p-3">Jenis Problem</th>
                                <th class="p-3">Masalah / Kendala (Trouble)</th>
                                <th class="p-3">Durasi</th>
                                <th class="p-3">Tindakan Perbaikan (CAPA)</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y divide-slate-800/60 font-mono">
                            ${downtimes.length === 0 ? `
                                <tr><td colspan="9" class="p-6 text-center text-slate-500 font-sans">Belum ada riwayat log trouble downtime</td></tr>
                            ` : downtimes.map((dt, idx) => `
                                <tr class="hover:bg-slate-800/40 transition-colors">
                                    <td class="p-3 text-center font-bold text-slate-500 font-mono">${idx + 1}</td>
                                    <td class="p-3 text-slate-300 text-[11px]">
                                        <div>${dt.start_time ? dt.start_time.slice(0,16).replace('T', ' ') : '-'}</div>
                                        <div class="text-slate-500 text-[10px]">s/d ${dt.end_time ? dt.end_time.slice(11,16) : '<span class="text-rose-400 font-bold">ONGOING</span>'}</div>
                                    </td>
                                    <td class="p-3 font-sans">
                                        <div class="font-bold text-cyan-400">${dt.production_line ? dt.production_line.name : (dt.production_line_id ? 'Line #' + dt.production_line_id : 'FX Line')}</div>
                                        <div class="text-[10px] text-slate-400 font-mono">${dt.machine ? dt.machine.name : 'MC-MEASURING'}</div>
                                    </td>
                                    <td class="p-3 font-sans">
                                        <span class="px-2 py-0.5 rounded text-[10px] font-semibold bg-slate-800 text-slate-200 border border-slate-700">
                                            ${dt.shift ? dt.shift.name : 'Shift 1'}
                                        </span>
                                        ${dt.team ? `<div class="text-[10px] text-amber-400 font-bold mt-1">👥 ${dt.team}</div>` : ''}
                                    </td>
                                    <td class="p-3 text-slate-300 font-sans text-xs">
                                        ${dt.product ? dt.product.name : '-'}
                                    </td>
                                    <td class="p-3 font-sans">
                                        <span class="px-2 py-0.5 rounded text-[10px] font-bold ${dt.problem_type === 'Mesin' ? 'bg-rose-950 text-rose-300 border border-rose-800' : 'bg-amber-950 text-amber-300 border border-amber-800'}">
                                            ${dt.problem_type || 'Mesin'}
                                        </span>
                                    </td>
                                    <td class="p-3 font-sans text-slate-200">
                                        <div class="font-bold text-slate-100">${dt.description || (dt.downtime_reason ? dt.downtime_reason.name : 'Trouble Operasional')}</div>
                                        ${dt.description && dt.downtime_reason && dt.downtime_reason.name !== dt.description && dt.downtime_reason.name !== 'Mechanical Jam & Motor Overheat' ? `<div class="text-[10px] text-slate-400 truncate max-w-xs">${dt.downtime_reason.name}</div>` : ''}
                                    </td>
                                    <td class="p-3 font-bold text-rose-400">${dt.calculated_duration_minutes || dt.duration_minutes} mins</td>
                                    <td class="p-3 font-sans text-xs">
                                        <div class="text-emerald-400 font-medium">${dt.action_taken || '-'}</div>
                                    </td>
                                </tr>
                            `).join('')}
                        </tbody>
                    </table>
                </div>
            </div>
        `;

        this.renderParetoDowntimeChart(pareto);
        if (this.currentLang === 'ja') {
            i18n.localizeDom(document.getElementById('content-body') || document.body);
        }
    }

    // ==========================================
    // 7. QUALITY PERFORMANCE PAGE
    // ==========================================
    // ==========================================
    // 7. QUALITY PERFORMANCE & PARETO DEFECT PAGE
    // ==========================================
    async renderQuality() {
        this.currentView = 'quality';
        const content = document.getElementById('content-body');
        content.innerHTML = `
            <div class="flex items-center justify-center p-12 text-slate-400">
                <div class="w-8 h-8 border-4 border-cyan-500/30 border-t-cyan-500 rounded-full animate-spin"></div>
            </div>
        `;

        try {
            const defParetoRes = await api.getParetoDefects(this.filters);
            const paretoData = defParetoRes.data;
            const top10 = paretoData.data || [];
            const allDefects = paretoData.top_defects || top10;
            const topSections = paretoData.top_sections || [];
            const summary = paretoData.summary || {};
            const compDist = summary.component_distribution || [];
            const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');

            content.innerHTML = `
                <!-- TOP HEADER & CONTEXT STRIP -->
                <div class="flex flex-wrap items-center justify-between gap-4 mb-5">
                    <div>
                        <h2 class="text-xl font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'} flex items-center gap-2">
                            <i data-lucide="pie-chart" class="w-5 h-5 text-rose-500"></i>
                            <span>Quality Performance</span>
                        </h2>
                    </div>

                    <!-- ACTION BUTTONS & PERIOD QUICK SELECT -->
                    <div class="flex flex-wrap items-center gap-2">
                        <div class="inline-flex ${isLight ? 'bg-white border-slate-200 shadow-sm' : 'bg-slate-900 border-slate-800'} border p-0.5 rounded-xl text-xs font-medium">
                            <button id="btn-period-all" class="px-3 py-1.5 rounded-lg transition-all cursor-pointer ${!this.filters.period ? 'bg-cyan-600 text-white font-semibold shadow-md shadow-cyan-600/30' : (isLight ? 'text-slate-600 hover:text-slate-900' : 'text-slate-400 hover:text-slate-200')}">Semua</button>
                            <button id="btn-period-7d" class="px-3 py-1.5 rounded-lg transition-all cursor-pointer ${this.filters.period === '7d' ? 'bg-cyan-600 text-white font-semibold shadow-md shadow-cyan-600/30' : (isLight ? 'text-slate-600 hover:text-slate-900' : 'text-slate-400 hover:text-slate-200')}">7 Hari</button>
                            <button id="btn-period-30d" class="px-3 py-1.5 rounded-lg transition-all cursor-pointer ${this.filters.period === '30d' ? 'bg-cyan-600 text-white font-semibold shadow-md shadow-cyan-600/30' : (isLight ? 'text-slate-600 hover:text-slate-900' : 'text-slate-400 hover:text-slate-200')}">30 Hari</button>
                        </div>
                        <button id="btn-export-pareto-excel" class="flex items-center gap-1.5 px-3.5 py-2 rounded-xl bg-emerald-600 hover:bg-emerald-500 text-white text-xs font-semibold shadow-lg shadow-emerald-600/20 transition-all cursor-pointer">
                            <i data-lucide="file-spreadsheet" class="w-4 h-4"></i>
                            <span>Export Excel</span>
                        </button>
                        <button id="btn-refresh-quality" class="p-2 rounded-xl ${isLight ? 'bg-white hover:bg-slate-100 border-slate-200 text-slate-700 shadow-sm' : 'bg-slate-900 hover:bg-slate-800 border-slate-800 text-slate-300'} border transition-all cursor-pointer" title="Refresh Data">
                            <i data-lucide="refresh-cw" class="w-4 h-4"></i>
                        </button>
                    </div>
                </div>

                <!-- 4 EXECUTIVE KPI CARDS -->
                <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-6">
                    <!-- KPI 1: TOTAL DEFECT -->
                    <div class="${isLight ? 'bg-gradient-to-br from-rose-50/90 via-white to-slate-50/90 border-rose-200/90 shadow-lg shadow-rose-100/50' : 'bg-gradient-to-br from-rose-950/40 via-slate-900 to-slate-900 border-rose-900/40 shadow-xl'} border rounded-2xl p-4 relative overflow-hidden group hover:border-rose-500/60 transition-all">
                        <div class="absolute -right-4 -bottom-4 w-24 h-24 bg-rose-500/10 rounded-full blur-xl group-hover:scale-125 transition-all"></div>
                        <div class="flex items-center justify-between mb-2">
                            <span class="text-[11px] font-bold ${isLight ? 'text-rose-700' : 'text-rose-400'} uppercase tracking-wider">Total Cacat / Reject</span>
                            <div class="w-8 h-8 rounded-xl ${isLight ? 'bg-rose-100 border-rose-200 text-rose-600 shadow-sm' : 'bg-rose-950/80 border-rose-800 text-rose-400'} border flex items-center justify-center">
                                <i data-lucide="shield-alert" class="w-4 h-4"></i>
                            </div>
                        </div>
                        <div class="text-3xl font-extrabold ${isLight ? 'text-slate-900' : 'text-white'} font-mono tracking-tight">${(summary.total_reject_pcs || 0).toLocaleString()} <span class="text-sm font-sans font-medium ${isLight ? 'text-slate-500' : 'text-slate-400'}">Pcs</span></div>
                        <div class="text-[11px] ${isLight ? 'text-slate-600 border-slate-200' : 'text-slate-400 border-slate-800/80'} mt-2 flex items-center justify-between pt-2 border-t">
                            <span>Defect Rate:</span>
                            <span class="font-mono font-bold ${isLight ? 'text-rose-700' : 'text-rose-400'}">${(summary.ppm_defect_rate || 0).toLocaleString()} PPM</span>
                        </div>
                    </div>

                    <!-- KPI 2: #1 PRIMARY DEFECT DRIVER -->
                    <div class="${isLight ? 'bg-gradient-to-br from-amber-50/90 via-white to-slate-50/90 border-amber-200/90 shadow-lg shadow-amber-100/50' : 'bg-gradient-to-br from-amber-950/40 via-slate-900 to-slate-900 border-amber-900/40 shadow-xl'} border rounded-2xl p-4 relative overflow-hidden group hover:border-amber-500/60 transition-all">
                        <div class="absolute -right-4 -bottom-4 w-24 h-24 bg-amber-500/10 rounded-full blur-xl group-hover:scale-125 transition-all"></div>
                        <div class="flex items-center justify-between mb-2">
                            <span class="text-[11px] font-bold ${isLight ? 'text-amber-800' : 'text-amber-400'} uppercase tracking-wider">Top #1 Defect Reason</span>
                            <div class="w-8 h-8 rounded-xl ${isLight ? 'bg-amber-100 border-amber-200 text-amber-700 shadow-sm' : 'bg-amber-950/80 border-amber-800 text-amber-400'} border flex items-center justify-center">
                                <i data-lucide="flame" class="w-4 h-4"></i>
                            </div>
                        </div>
                        <div class="text-lg font-extrabold ${isLight ? 'text-slate-900' : 'text-amber-300'} truncate" title="${summary.top_defect_name || '-'}">${summary.top_defect_name || 'Tidak Ada Defect'}</div>
                        <div class="text-[11px] ${isLight ? 'text-slate-600 border-slate-200' : 'text-slate-400 border-slate-800/80'} mt-2 flex items-center justify-between pt-2 border-t">
                            <span>Kontribusi:</span>
                            <span class="font-mono font-bold ${isLight ? 'text-amber-800' : 'text-amber-400'}">${summary.top_defect_qty || 0} pcs (${summary.top_defect_pct || 0}%)</span>
                        </div>
                    </div>

                    <!-- KPI 3: CRITICAL NG SECTION -->
                    <div class="${isLight ? 'bg-gradient-to-br from-cyan-50/90 via-white to-slate-50/90 border-cyan-200/90 shadow-lg shadow-cyan-100/50' : 'bg-gradient-to-br from-cyan-950/40 via-slate-900 to-slate-900 border-cyan-900/40 shadow-xl'} border rounded-2xl p-4 relative overflow-hidden group hover:border-cyan-500/60 transition-all">
                        <div class="absolute -right-4 -bottom-4 w-24 h-24 bg-cyan-500/10 rounded-full blur-xl group-hover:scale-125 transition-all"></div>
                        <div class="flex items-center justify-between mb-2">
                            <span class="text-[11px] font-bold ${isLight ? 'text-cyan-800' : 'text-cyan-400'} uppercase tracking-wider">Bagian NG Kritis</span>
                            <div class="w-8 h-8 rounded-xl ${isLight ? 'bg-cyan-100 border-cyan-200 text-cyan-700 shadow-sm' : 'bg-cyan-950/80 border-cyan-800 text-cyan-400'} border flex items-center justify-center">
                                <i data-lucide="crosshair" class="w-4 h-4"></i>
                            </div>
                        </div>
                        <div class="text-lg font-extrabold ${isLight ? 'text-slate-900' : 'text-cyan-300'} truncate" title="${summary.top_section_name || '-'}">${summary.top_section_name || 'N/A'}</div>
                        <div class="text-[11px] ${isLight ? 'text-slate-600 border-slate-200' : 'text-slate-400 border-slate-800/80'} mt-2 flex items-center justify-between pt-2 border-t">
                            <span>Volume Terkena:</span>
                            <span class="font-mono font-bold ${isLight ? 'text-cyan-800' : 'text-cyan-400'}">${summary.top_section_qty || 0} pcs</span>
                        </div>
                    </div>

                    <!-- KPI 4: 80/20 VITAL FEW RATIO -->
                    <div class="${isLight ? 'bg-gradient-to-br from-purple-50/90 via-white to-slate-50/90 border-purple-200/90 shadow-lg shadow-purple-100/50' : 'bg-gradient-to-br from-purple-950/40 via-slate-900 to-slate-900 border-purple-900/40 shadow-xl'} border rounded-2xl p-4 relative overflow-hidden group hover:border-purple-500/60 transition-all">
                        <div class="absolute -right-4 -bottom-4 w-24 h-24 bg-purple-500/10 rounded-full blur-xl group-hover:scale-125 transition-all"></div>
                        <div class="flex items-center justify-between mb-2">
                            <span class="text-[11px] font-bold ${isLight ? 'text-purple-800' : 'text-purple-400'} uppercase tracking-wider">Pareto 80/20 Vital Few</span>
                            <div class="w-8 h-8 rounded-xl ${isLight ? 'bg-purple-100 border-purple-200 text-purple-700 shadow-sm' : 'bg-purple-950/80 border-purple-800 text-purple-400'} border flex items-center justify-center">
                                <i data-lucide="target" class="w-4 h-4"></i>
                            </div>
                        </div>
                        <div class="text-3xl font-extrabold ${isLight ? 'text-slate-900' : 'text-white'} font-mono tracking-tight">${summary.vital_few_count || 0} <span class="text-sm font-sans font-medium ${isLight ? 'text-slate-500' : 'text-slate-400'}">Penyebab</span></div>
                        <div class="text-[11px] ${isLight ? 'text-slate-600 border-slate-200' : 'text-slate-400 border-slate-800/80'} mt-2 flex items-center justify-between pt-2 border-t">
                            <span>Mencakup Masalah:</span>
                            <span class="font-mono font-bold ${isLight ? 'text-emerald-700' : 'text-emerald-400'}">${summary.vital_few_percentage || 0}% Total Reject</span>
                        </div>
                    </div>
                </div>

                <!-- MAIN DUAL-AXIS PARETO CHART SECTION -->
                <div class="${isLight ? 'bg-white border-slate-200 shadow-xl' : 'bg-slate-900/90 border-slate-800 shadow-2xl backdrop-blur-md'} border rounded-2xl p-5 mb-6">
                    <div class="flex flex-wrap items-center justify-between gap-3 mb-4 pb-3 border-b ${isLight ? 'border-slate-200' : 'border-slate-800'}">
                        <div>
                            <h3 class="text-sm font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'} flex items-center gap-2">
                                <i data-lucide="bar-chart-3" class="w-4 h-4 text-cyan-500"></i>
                                <span>Diagram Pareto Penyebab Reject (Vital Few vs Useful Many)</span>
                            </h3>
                            <p class="text-xs ${isLight ? 'text-slate-600' : 'text-slate-400'} mt-0.5">Distribusi frekuensi defect aktual (Batang Rose) & Kurva Kumulatif 80/20 (Garis Neon Cyan)</p>
                        </div>
                        <div class="flex items-center gap-4 text-xs">
                            <div class="flex items-center gap-1.5">
                                <span class="w-3 h-3 rounded bg-rose-500 inline-block"></span>
                                <span class="${isLight ? 'text-slate-700' : 'text-slate-300'} font-medium">Reject Qty (Pcs)</span>
                            </div>
                            <div class="flex items-center gap-1.5">
                                <span class="w-3 h-1 bg-cyan-500 inline-block rounded-full"></span>
                                <span class="${isLight ? 'text-slate-700' : 'text-slate-300'} font-medium">Kumulatif (%)</span>
                            </div>
                            <div class="flex items-center gap-1.5">
                                <span class="w-3 h-0.5 border-b-2 border-dashed border-emerald-500 inline-block"></span>
                                <span class="text-emerald-600 font-semibold">Garis Batas 80%</span>
                            </div>
                        </div>
                    </div>

                    <div id="chart-pareto-defect" class="w-full min-h-[340px]"></div>
                </div>

                <!-- INFOGRAPHICS ROW: 2 SUB-PANELS -->
                <div class="grid grid-cols-1 lg:grid-cols-3 gap-6 mb-6">
                    <!-- PANEL 1: DONUT DISTRIBUSI KOMPONEN -->
                    <div class="${isLight ? 'bg-white border-slate-200 shadow-xl' : 'bg-slate-900 border-slate-800 shadow-xl'} border rounded-2xl p-5 flex flex-col justify-between">
                        <div>
                            <h3 class="text-sm font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'} mb-1 flex items-center gap-2">
                                <i data-lucide="layers" class="w-4 h-4 text-amber-500"></i>
                                <span>Proporsi Reject per Komponen</span>
                            </h3>
                            <p class="text-xs ${isLight ? 'text-slate-600' : 'text-slate-400'} mb-3">Distribusi produk cacat antara Assy, Rod, Cap & Fasteners</p>
                            <div id="chart-defect-component" class="w-full min-h-[220px]"></div>
                        </div>
                        <div class="grid grid-cols-2 gap-2 pt-3 border-t ${isLight ? 'border-slate-200' : 'border-slate-800'} mt-2">
                            ${compDist.map(c => `
                                <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950/60 border-slate-800/80'} border rounded-xl p-2 flex items-center justify-between">
                                    <div class="flex items-center gap-1.5">
                                        <span class="w-2 h-2 rounded-full ${c.name === 'ASSY' ? 'bg-rose-500' : c.name === 'ROD' ? 'bg-amber-500' : c.name === 'CAP' ? 'bg-cyan-500' : 'bg-purple-500'}"></span>
                                        <span class="text-[11px] font-semibold ${isLight ? 'text-slate-800' : 'text-slate-300'}">${c.name}</span>
                                    </div>
                                    <span class="text-xs font-mono font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'}">${c.quantity} <span class="text-[10px] ${isLight ? 'text-slate-500' : 'text-slate-400'}">(${c.percentage}%)</span></span>
                                </div>
                            `).join('')}
                        </div>
                    </div>

                    <!-- PANEL 2: TOP BAGIAN NG (SECTION PARETO) -->
                    <div class="lg:col-span-2 ${isLight ? 'bg-white border-slate-200 shadow-xl' : 'bg-slate-900 border-slate-800 shadow-xl'} border rounded-2xl p-5">
                        <div class="flex items-center justify-between mb-3 pb-3 border-b ${isLight ? 'border-slate-200' : 'border-slate-800'}">
                            <div>
                                <h3 class="text-sm font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'} flex items-center gap-2">
                                    <i data-lucide="crosshair" class="w-4 h-4 text-cyan-500"></i>
                                    <span>Ranking Bagian NG Terbanyak (Defect Section Breakdown)</span>
                                </h3>
                                <p class="text-xs ${isLight ? 'text-slate-600' : 'text-slate-400'} mt-0.5">Area anatomi komponen manufaktur yang paling sering mengalami defect</p>
                            </div>
                            <span class="text-xs font-mono ${isLight ? 'text-slate-600' : 'text-slate-400'}">${topSections.length} Area Terdeteksi</span>
                        </div>

                        <div class="space-y-3 pt-1">
                            ${topSections.length === 0 ? `
                                <div class="text-center py-8 ${isLight ? 'text-slate-500' : 'text-slate-500'} text-xs">Belum ada data bagian NG.</div>
                            ` : topSections.slice(0, 5).map((sec, idx) => `
                                <div class="${isLight ? 'bg-slate-50/90 border-slate-200 hover:border-slate-300' : 'bg-slate-950/70 border-slate-800/80 hover:border-slate-700'} border rounded-xl p-3 transition-all">
                                    <div class="flex items-center justify-between mb-1.5">
                                        <div class="flex items-center gap-2">
                                            <span class="w-5 h-5 rounded-lg flex items-center justify-center text-[10px] font-mono font-bold ${idx === 0 ? (isLight ? 'bg-amber-100 text-amber-800 border border-amber-300' : 'bg-amber-500/20 text-amber-300 border border-amber-500/40') : idx === 1 ? (isLight ? 'bg-slate-200 text-slate-800 border border-slate-300' : 'bg-slate-300/20 text-slate-200 border border-slate-400/40') : idx === 2 ? (isLight ? 'bg-orange-100 text-orange-800 border border-orange-300' : 'bg-amber-700/20 text-amber-500 border border-amber-700/40') : (isLight ? 'bg-slate-100 text-slate-600' : 'bg-slate-800 text-slate-400')}">#${sec.rank}</span>
                                            <span class="text-xs font-bold ${isLight ? 'text-slate-900' : 'text-slate-200'}">${sec.section}</span>
                                        </div>
                                        <div class="flex items-center gap-3">
                                            <span class="text-xs font-mono font-bold text-rose-500">${sec.reject_quantity} Pcs</span>
                                            <span class="text-[11px] font-mono font-semibold ${isLight ? 'text-slate-700 bg-white border-slate-200' : 'text-slate-400 bg-slate-900 border-slate-800'} px-2 py-0.5 rounded border">${sec.percentage}%</span>
                                        </div>
                                    </div>
                                    <div class="w-full ${isLight ? 'bg-slate-200' : 'bg-slate-900'} rounded-full h-2 overflow-hidden">
                                        <div class="bg-gradient-to-r ${idx === 0 ? 'from-amber-500 to-rose-500' : 'from-cyan-500 to-blue-500'} h-2 rounded-full transition-all duration-500" style="width: ${sec.percentage}%"></div>
                                    </div>
                                </div>
                            `).join('')}
                        </div>
                    </div>
                </div>

                <!-- TOP 10 DEFECT REASON MATRIX & SMART COUNTERMEASURES TABLE -->
                <div class="${isLight ? 'bg-white border-slate-200 shadow-xl' : 'bg-slate-900 border-slate-800 shadow-xl'} border rounded-2xl overflow-hidden mb-6">
                    <div class="p-5 border-b ${isLight ? 'bg-slate-50/80 border-slate-200' : 'bg-slate-900/60 border-slate-800'} flex flex-wrap items-center justify-between gap-3">
                        <div>
                            <h3 class="text-sm font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'} flex items-center gap-2">
                                <i data-lucide="list-ordered" class="w-4 h-4 text-emerald-500"></i>
                                <span>Matriks Top 10 Defect Reason & Rekomendasi Solusi Teknis</span>
                            </h3>
                            <p class="text-xs ${isLight ? 'text-slate-600' : 'text-slate-400'} mt-0.5">Analisis prioritas perbaikan continuous improvement berbasis prinsip Kaizen & Six Sigma</p>
                        </div>
                        <div class="flex items-center gap-2">
                            <span class="inline-flex items-center gap-1 px-2.5 py-1 rounded-lg text-[10px] font-bold ${isLight ? 'bg-rose-50 text-rose-700 border-rose-200' : 'bg-rose-500/10 text-rose-400 border-rose-500/30'} border">
                                <i data-lucide="alert-circle" class="w-3 h-3"></i>
                                <span>Vital Few (80% Prioritas)</span>
                            </span>
                            <span class="inline-flex items-center gap-1 px-2.5 py-1 rounded-lg text-[10px] font-bold ${isLight ? 'bg-emerald-50 text-emerald-700 border-emerald-200' : 'bg-emerald-500/10 text-emerald-400 border-emerald-500/30'} border">
                                <i data-lucide="check-circle" class="w-3 h-3"></i>
                                <span>Useful Many</span>
                            </span>
                        </div>
                    </div>

                    <div class="overflow-x-auto">
                        <table class="w-full text-left text-xs border-collapse">
                            <thead>
                                <tr class="${isLight ? 'bg-slate-100 border-slate-200 text-slate-700' : 'bg-slate-950/80 border-slate-800 text-slate-400'} border-b text-[11px] uppercase tracking-wider font-semibold">
                                    <th class="py-3 px-4 w-16 text-center">Rank</th>
                                    <th class="py-3 px-4 min-w-[200px]">Penyebab Defect / Remark</th>
                                    <th class="py-3 px-4 min-w-[150px]">Bagian NG (Section)</th>
                                    <th class="py-3 px-4 text-right min-w-[120px]">Reject Qty</th>
                                    <th class="py-3 px-4 text-right min-w-[90px]">Kontribusi</th>
                                    <th class="py-3 px-4 min-w-[130px]">Kumulatif (%)</th>
                                    <th class="py-3 px-4 min-w-[130px]">Klasifikasi</th>
                                    <th class="py-3 px-4 min-w-[280px]">Rekomendasi Tindakan Korektif (Countermeasure)</th>
                                </tr>
                            </thead>
                            <tbody class="divide-y ${isLight ? 'divide-slate-200 text-slate-800' : 'divide-slate-800/70 text-slate-200'}">
                                ${top10.length === 0 ? `
                                    <tr>
                                        <td colspan="8" class="text-center py-10 ${isLight ? 'text-slate-500' : 'text-slate-500'} text-xs">Tidak ada data defect untuk filter ini.</td>
                                    </tr>
                                ` : top10.map((d, i) => `
                                    <tr class="${isLight ? 'hover:bg-slate-50/80' : 'hover:bg-slate-800/40'} transition-colors ${d.is_vital_few ? (isLight ? 'bg-rose-50/60' : 'bg-rose-950/10') : ''}">
                                        <td class="py-3.5 px-4 text-center">
                                            ${d.rank === 1 ? '<span class="inline-flex items-center justify-center w-7 h-7 rounded-xl bg-amber-500/20 text-amber-600 font-bold border border-amber-500/40 shadow-sm">🥇</span>' :
                                              d.rank === 2 ? '<span class="inline-flex items-center justify-center w-7 h-7 rounded-xl bg-slate-300/30 text-slate-700 font-bold border border-slate-400/40 shadow-sm">🥈</span>' :
                                              d.rank === 3 ? '<span class="inline-flex items-center justify-center w-7 h-7 rounded-xl bg-amber-700/20 text-amber-700 font-bold border border-amber-700/40 shadow-sm">🥉</span>' :
                                              `<span class="inline-flex items-center justify-center w-6 h-6 rounded-lg ${isLight ? 'bg-slate-100 text-slate-700' : 'bg-slate-800 text-slate-400'} font-mono font-bold text-[11px]">#${d.rank}</span>`
                                            }
                                        </td>
                                        <td class="py-3.5 px-4">
                                            <div class="font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'} flex items-center gap-1.5">
                                                <span>${d.reason}</span>
                                            </div>
                                            <div class="flex items-center gap-1.5 mt-1">
                                                <span class="px-1.5 py-0.5 rounded text-[10px] font-mono font-semibold ${isLight ? 'bg-slate-100 text-slate-700 border-slate-300' : 'bg-slate-800 text-slate-300 border-slate-700'} border">${d.component_type || 'ASSY'}</span>
                                            </div>
                                        </td>
                                        <td class="py-3.5 px-4">
                                            <span class="inline-flex items-center gap-1 px-2.5 py-1 rounded-lg text-[11px] font-medium ${isLight ? 'bg-cyan-50 text-cyan-800 border-cyan-200' : 'bg-cyan-950/60 text-cyan-300 border-cyan-800/60'} border">
                                                <i data-lucide="tag" class="w-3 h-3 text-cyan-500"></i>
                                                <span>${d.section}</span>
                                            </span>
                                        </td>
                                        <td class="py-3.5 px-4 text-right font-mono">
                                            <div class="font-extrabold text-sm text-rose-500">${d.reject_quantity.toLocaleString()}</div>
                                            <div class="text-[10px] ${isLight ? 'text-slate-500' : 'text-slate-500'}">Pcs</div>
                                        </td>
                                        <td class="py-3.5 px-4 text-right font-mono">
                                            <span class="inline-block font-bold ${isLight ? 'text-slate-800 bg-slate-100 border-slate-300' : 'text-slate-100 bg-slate-800 border-slate-700'} px-2 py-0.5 rounded border text-xs">${d.percentage}%</span>
                                        </td>
                                        <td class="py-3.5 px-4">
                                            <div class="flex items-center justify-between text-[11px] font-mono font-bold mb-1 ${d.cumulative_percentage <= 80 ? (isLight ? 'text-rose-700' : 'text-rose-400') : (isLight ? 'text-emerald-700' : 'text-emerald-400')}">
                                                <span>${d.cumulative_percentage}%</span>
                                            </div>
                                            <div class="w-full ${isLight ? 'bg-slate-200' : 'bg-slate-950'} rounded-full h-1.5 overflow-hidden">
                                                <div class="h-1.5 rounded-full ${d.cumulative_percentage <= 80 ? 'bg-gradient-to-r from-rose-500 to-amber-500' : 'bg-gradient-to-r from-emerald-500 to-cyan-500'}" style="width: ${d.cumulative_percentage}%"></div>
                                            </div>
                                        </td>
                                        <td class="py-3.5 px-4">
                                            ${d.is_vital_few ? `
                                                <span class="inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full text-[10px] font-bold ${isLight ? 'bg-rose-100 text-rose-700 border-rose-300' : 'bg-rose-500/15 text-rose-400 border-rose-500/40'} border">
                                                    <span class="w-1.5 h-1.5 rounded-full bg-rose-500 animate-ping"></span>
                                                    <span>VITAL FEW (80%)</span>
                                                </span>
                                            ` : `
                                                <span class="inline-flex items-center gap-1 px-2.5 py-1 rounded-full text-[10px] font-semibold ${isLight ? 'bg-slate-100 text-slate-600 border-slate-300' : 'bg-slate-800 text-slate-400 border-slate-700'} border">
                                                    <span>Useful Many</span>
                                                </span>
                                            `}
                                        </td>
                                        <td class="py-3.5 px-4">
                                            <div class="text-xs ${isLight ? 'text-slate-800 bg-slate-50 border-slate-200' : 'text-slate-300 bg-slate-950/60 border-slate-800/80'} p-2.5 rounded-xl border font-sans leading-relaxed relative group hover:border-cyan-500/50 transition-all">
                                                <div class="flex items-center justify-between gap-1 mb-1">
                                                    <div class="font-bold ${isLight ? 'text-emerald-700' : 'text-emerald-400'} flex items-center gap-1">
                                                        <i data-lucide="wrench" class="w-3.5 h-3.5"></i>
                                                        <span>Rekomendasi Kaizen:</span>
                                                        ${d.is_custom_countermeasure ? `<span class="text-[9px] font-mono px-1.5 py-0.2 rounded ${isLight ? 'bg-cyan-100 text-cyan-800 border-cyan-200' : 'bg-cyan-950 text-cyan-300 border-cyan-800'} border font-semibold">Customized</span>` : ''}
                                                    </div>
                                                    <button 
                                                        type="button" 
                                                        class="btn-edit-countermeasure inline-flex items-center gap-1 px-2 py-0.5 rounded-md text-[10px] font-bold ${isLight ? 'bg-white hover:bg-cyan-50 text-cyan-700 border-slate-200 hover:border-cyan-300' : 'bg-slate-900 hover:bg-cyan-950 text-cyan-400 border-slate-700 hover:border-cyan-500/50'} border shadow-xs cursor-pointer transition-all" 
                                                        data-reason-id="${d.defect_reason_id || ''}" 
                                                        data-reason-name="${d.reason.replace(/"/g, '&quot;')}" 
                                                        data-countermeasure="${encodeURIComponent(d.action_recommendation || '')}" 
                                                        title="Edit / Sesuaikan Rekomendasi Tindakan Korektif"
                                                    >
                                                        <i data-lucide="edit-3" class="w-3 h-3 text-cyan-500"></i>
                                                        <span>Edit</span>
                                                    </button>
                                                </div>
                                                <p class="text-[11px] ${isLight ? 'text-slate-700' : 'text-slate-300'} leading-relaxed font-sans">${d.action_recommendation || 'Lakukan analisis 5-Why problem solving bersama tim Maintenance & QC Line untuk menentukan akar penyebab.'}</p>
                                            </div>
                                        </td>
                                    </tr>
                                `).join('')}
                            </tbody>
                        </table>
                    </div>
                </div>
            `;

            if (window.lucide) window.lucide.createIcons();

            // Render Charts
            this.renderParetoDefectChart(top10, summary);
            this.renderDefectComponentChart(compDist);

            // Hook Event Listeners
            const btnAll = document.getElementById('btn-period-all');
            const btn7d = document.getElementById('btn-period-7d');
            const btn30d = document.getElementById('btn-period-30d');
            const btnRefresh = document.getElementById('btn-refresh-quality');
            const btnExport = document.getElementById('btn-export-pareto-excel');

            if (btnAll) btnAll.addEventListener('click', () => { delete this.filters.period; this.renderQuality(); });
            if (btn7d) btn7d.addEventListener('click', () => { this.filters.period = '7d'; this.renderQuality(); });
            if (btn30d) btn30d.addEventListener('click', () => { this.filters.period = '30d'; this.renderQuality(); });
            if (btnRefresh) btnRefresh.addEventListener('click', () => this.renderQuality());

            // Bind Edit Countermeasure buttons
            document.querySelectorAll('.btn-edit-countermeasure').forEach(btn => {
                btn.addEventListener('click', (e) => {
                    const reasonId = e.currentTarget.getAttribute('data-reason-id');
                    const reasonName = e.currentTarget.getAttribute('data-reason-name');
                    const currentCm = decodeURIComponent(e.currentTarget.getAttribute('data-countermeasure') || '');
                    this.showEditCountermeasureModal(reasonId, reasonName, currentCm);
                });
            });
            if (btnExport) btnExport.addEventListener('click', () => this.exportQualityPareto(top10, topSections, summary));

            if (this.currentLang === 'ja') {
                i18n.localizeDom(content);
            }
        } catch (err) {
            console.error('Error loading quality pareto:', err);
            content.innerHTML = `
                <div class="p-8 text-center bg-slate-900 border border-slate-800 rounded-2xl">
                    <i data-lucide="alert-triangle" class="w-12 h-12 text-rose-500 mx-auto mb-3"></i>
                    <h3 class="text-base font-bold text-slate-100">Gagal Memuat Analisis Pareto</h3>
                    <button id="btn-retry-quality" class="mt-4 px-4 py-2 bg-cyan-600 hover:bg-cyan-500 text-white rounded-lg text-xs font-semibold cursor-pointer">Coba Lagi</button>
                </div>
            `;
            if (window.lucide) window.lucide.createIcons();
            document.getElementById('btn-retry-quality')?.addEventListener('click', () => this.renderQuality());
        }
    }

    showEditCountermeasureModal(reasonId, reasonName, currentText) {
        const modalContainer = document.getElementById('modal-container');
        const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');

        modalContainer.innerHTML = `
            <div class="fixed inset-0 app-modal-backdrop flex items-center justify-center p-4 z-50">
                <div class="${isLight ? 'bg-white border-slate-200 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-2xl p-5 sm:p-6 max-w-lg w-full shadow-2xl font-sans app-modal-dialog">
                    <div class="flex items-center justify-between mb-4 pb-3 border-b ${isLight ? 'border-slate-200' : 'border-slate-800'}">
                        <div class="flex items-center gap-2.5">
                            <div class="w-9 h-9 rounded-xl ${isLight ? 'bg-cyan-50 text-cyan-600 border-cyan-200' : 'bg-cyan-950/80 text-cyan-400 border-cyan-800/80'} border flex items-center justify-center">
                                <i data-lucide="wrench" class="w-5 h-5"></i>
                            </div>
                            <div>
                                <h3 class="font-bold text-sm ${isLight ? 'text-slate-900' : 'text-slate-100'}">Edit Rekomendasi Tindakan Korektif</h3>
                                <p class="text-xs ${isLight ? 'text-slate-500' : 'text-slate-400'} font-medium">${reasonName}</p>
                            </div>
                        </div>
                        <button type="button" id="btn-close-cm-modal" class="p-1 rounded-lg ${isLight ? 'text-slate-400 hover:text-slate-900 hover:bg-slate-100' : 'text-slate-400 hover:text-slate-100 hover:bg-slate-800'} transition-colors cursor-pointer">
                            <i data-lucide="x" class="w-4 h-4"></i>
                        </button>
                    </div>

                    <form id="form-edit-countermeasure" class="space-y-4 text-xs">
                        <input type="hidden" name="id" value="${reasonId || ''}" />
                        <input type="hidden" name="name" value="${reasonName.replace(/"/g, '&quot;')}" />

                        <div class="${isLight ? 'bg-cyan-50/50 border-cyan-200/80' : 'bg-cyan-950/30 border-cyan-900/50'} border p-3 rounded-xl">
                            <p class="text-[11px] ${isLight ? 'text-cyan-900' : 'text-cyan-300'} leading-relaxed">
                                💡 <strong>Catatan Kaizen:</strong> Rekomendasi ini dapat disesuaikan oleh tim Engineering / Maintenance / QC / Kaizen sesuai SOP aktual penanganan cacat di lini produksi.
                            </p>
                        </div>

                        <div>
                            <label class="block font-semibold ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1.5">
                                Rekomendasi Tindakan Korektif (Countermeasure / Kaizen SOP) <span class="text-rose-500">*</span>
                            </label>
                            <textarea 
                                name="countermeasure" 
                                rows="4" 
                                required 
                                placeholder="Contoh: Periksa keausan insert CNC, bersihkan clamping jig dari gram chip halus, dan kalibrasi air micrometer gauge..." 
                                class="w-full ${isLight ? 'bg-white border-slate-300 text-slate-900 focus:border-cyan-600 focus:bg-cyan-50/10' : 'bg-slate-950 border-slate-800 text-slate-100 focus:border-cyan-500'} border rounded-xl px-3 py-2.5 text-xs font-sans focus:outline-none shadow-xs leading-relaxed"
                            >${currentText || ''}</textarea>
                        </div>

                        <div class="flex justify-end gap-2 pt-2 border-t ${isLight ? 'border-slate-100' : 'border-slate-800/80'}">
                            <button type="button" id="btn-cancel-cm-modal" class="px-4 py-2 rounded-xl text-xs font-medium ${isLight ? 'bg-slate-100 hover:bg-slate-200 text-slate-700' : 'bg-slate-800 hover:bg-slate-700 text-slate-300'} transition-colors cursor-pointer">
                                Batal
                            </button>
                            <button type="submit" id="btn-save-cm-modal" class="px-5 py-2 bg-cyan-600 hover:bg-cyan-500 text-white rounded-xl text-xs font-bold shadow-lg shadow-cyan-600/30 flex items-center gap-1.5 cursor-pointer transition-all">
                                <i data-lucide="save" class="w-3.5 h-3.5"></i>
                                <span>Simpan Rekomendasi</span>
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();
        if (this.currentLang === 'ja') {
            i18n.localizeDom(modalContainer);
        }

        document.getElementById('btn-close-cm-modal').addEventListener('click', () => modalContainer.innerHTML = '');
        document.getElementById('btn-cancel-cm-modal').addEventListener('click', () => modalContainer.innerHTML = '');

        document.getElementById('form-edit-countermeasure').addEventListener('submit', async (e) => {
            e.preventDefault();
            const submitBtn = document.getElementById('btn-save-cm-modal');
            submitBtn.disabled = true;
            submitBtn.innerHTML = `<i data-lucide="loader-2" class="w-3.5 h-3.5 animate-spin"></i> Menyimpan...`;
            if (window.lucide) window.lucide.createIcons();

            const formData = new FormData(e.target);
            const payload = Object.fromEntries(formData.entries());

            try {
                const res = await api.updateCountermeasure(payload);
                modalContainer.innerHTML = '';
                this.showNotification('Rekomendasi Kaizen Diperbarui', res.data?.message || 'Tindakan korektif berhasil diperbarui untuk jenis defect ini.', 'success');
                await this.loadMasterData();
                this.renderQuality();
            } catch (err) {
                console.error(err);
                alert('Gagal menyimpan rekomendasi: ' + (err.response?.data?.message || err.message));
                submitBtn.disabled = false;
                submitBtn.innerHTML = `<i data-lucide="save" class="w-3.5 h-3.5"></i> <span>Simpan Rekomendasi</span>`;
                if (window.lucide) window.lucide.createIcons();
            }
        });
    }

    // ==========================================
    // 8. REPORTS & EXPORT MODULE (2-TAB PRODUCTION & NG REPORTS)
    // ==========================================
    async renderReports() {
        await this.loadMasterData();

        this.activeReportTab = this.activeReportTab || 'production';
        this.reportFilters = this.reportFilters || {
            start_date: this.selectedMonitoringDate || new Date().toISOString().slice(0, 10),
            end_date: this.selectedMonitoringDate || new Date().toISOString().slice(0, 10),
            shift_id: this.filters.shift_id || '',
            line_id: this.filters.line_id || '',
            product_id: '',
            supervisor: '',
        };

        if (this.reportFilters.date && !this.reportFilters.start_date) {
            this.reportFilters.start_date = this.reportFilters.date;
            this.reportFilters.end_date = this.reportFilters.date;
        }

        const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');

        // Load Shift & Production Summary Data, NG Data, AND Trouble & Downtime Data concurrently in PARALLEL
        const [resProd, resNg, resTrouble] = await Promise.all([
            api.getShiftSummary(this.reportFilters),
            api.getNgQueue(this.reportFilters).catch(err => {
                console.error('Failed to load NG report data:', err);
                return { data: { data: [], summary: {} } };
            }),
            api.getTroubleSummary(this.reportFilters).catch(err => {
                console.error('Failed to load Trouble report data:', err);
                return { data: { data: { header: {}, summary: {}, items: [], category_breakdown: [], top_machines: [] } } };
            })
        ]);

        const report = resProd.data?.data || {};
        const header = report.header || {};
        const summary = report.executive_summary || {};
        const rows = report.matrix_rows || [];
        const sixLosses = report.six_big_losses || [];
        const incidentLogs = report.incident_logs || [];
        const totalIncidentMins = incidentLogs.reduce((acc, inc) => acc + (Number(inc.duration_minutes) || 0), 0);
        const avgIncidentMins = incidentLogs.length > 0 ? Math.round(totalIncidentMins / incidentLogs.length) : 0;
        const handoverNotes = report.handover_notes || [];

        const ngData = {
            items: resNg.data?.data || [],
            stats: resNg.data?.summary || {},
        };
        const ngItems = ngData.items || [];
        const ngStats = ngData.stats || {};
        const totalNgReports = ngItems.length;

        const troubleData = resTrouble.data?.data || {};
        const troubleHeader = troubleData.header || header;
        const troubleSummary = troubleData.summary || {};
        const troubleItems = troubleData.items || [];
        const troubleCategories = troubleData.category_breakdown || [];
        const troubleTopMachines = troubleData.top_machines || [];
        const totalTroubleReports = troubleItems.length;

        // Group rows by production_date for Tab 1
        const groupedProdRows = rows.reduce((acc, row) => {
            const d = row.production_date || row.formatted_date || 'Unknown';
            if (!acc[d]) {
                acc[d] = {
                    date: d,
                    formatted_date: row.formatted_date || d,
                    items: [],
                };
            }
            acc[d].items.push(row);
            return acc;
        }, {});
        const prodDateGroups = Object.values(groupedProdRows);

        // Group items by production_date for Tab 2
        const groupedNgItems = ngItems.reduce((acc, it) => {
            const d = it.production_date || it.formatted_date || 'Unknown';
            if (!acc[d]) {
                acc[d] = {
                    date: d,
                    formatted_date: it.formatted_date || d,
                    items: [],
                };
            }
            acc[d].items.push(it);
            return acc;
        }, {});
        const ngDateGroups = Object.values(groupedNgItems);

        // Group items by production_date for Tab 3 (Laporan Trouble & Downtime)
        const groupedTroubleItems = troubleItems.reduce((acc, it) => {
            const d = it.production_date || it.formatted_date || 'Unknown';
            if (!acc[d]) {
                acc[d] = {
                    date: d,
                    formatted_date: it.formatted_date || d,
                    items: [],
                };
            }
            acc[d].items.push(it);
            return acc;
        }, {});
        const troubleDateGroups = Object.values(groupedTroubleItems);

        // Aggregate Quality & NG KPIs
        const totalOutputAll = ngItems.reduce((acc, it) => acc + (it.total_output || 0), 0);
        const totalGoodAll = ngItems.reduce((acc, it) => acc + (it.good_quantity || 0), 0);
        const totalNgPcsAll = ngItems.reduce((acc, it) => acc + (it.total_ng_target || 0), 0);
        const totalOeeNgAll = ngItems.reduce((acc, it) => acc + (it.ng_detail?.total_ng_oee || 0), 0);
        const totalNonOeeNgAll = ngItems.reduce((acc, it) => acc + (it.ng_detail?.total_ng_non_oee || 0), 0);

        const totalAssyAll = ngItems.reduce((acc, it) => acc + (it.ng_detail?.ng_assy || 0), 0);
        const totalRodAll = ngItems.reduce((acc, it) => acc + (it.ng_detail?.ng_rod || 0), 0);
        const totalCapAll = ngItems.reduce((acc, it) => acc + (it.ng_detail?.ng_cap || 0), 0);

        const totalBoltAll = ngItems.reduce((acc, it) => acc + (it.ng_detail?.ng_bolt || 0), 0);
        const totalBushAll = ngItems.reduce((acc, it) => acc + (it.ng_detail?.ng_bush || 0), 0);
        const totalNutAll = ngItems.reduce((acc, it) => acc + (it.ng_detail?.ng_nut || 0), 0);
        const totalPinAll = ngItems.reduce((acc, it) => acc + (it.ng_detail?.ng_pin || 0), 0);

        const overallDefectRate = totalOutputAll > 0 ? ((totalNgPcsAll / totalOutputAll) * 100).toFixed(2) : '0.00';
        const overallYieldRate = totalOutputAll > 0 ? ((totalGoodAll / totalOutputAll) * 100).toFixed(2) : '100.00';
        const overallDefectPpm = totalOutputAll > 0 ? Math.round((totalNgPcsAll / totalOutputAll) * 1000000) : 0;

        // Helper for deviation badges
        const renderDevBadge = (val, target, isGreaterGood = true) => {
            const diff = Number((val - target).toFixed(2));
            if (diff === 0) return `<span class="text-[10px] font-mono font-bold text-slate-400">±0.00% (ON TARGET)</span>`;
            const isOk = isGreaterGood ? diff >= 0 : diff <= 0;
            const sign = diff > 0 ? `▲ +${diff.toFixed(2)}%` : `▼ ${diff.toFixed(2)}%`;
            return isOk 
                ? `<span class="text-[10px] font-mono font-bold text-emerald-400">${sign} (OK)</span>`
                : `<span class="text-[10px] font-mono font-bold text-rose-400">${sign} (NG)</span>`;
        };

        const content = document.getElementById('content-body');
        content.innerHTML = `
            <!-- MODULE TOPBAR & TAB SWITCHER -->
            <div class="flex flex-wrap items-center justify-between gap-3 mb-4 no-print">
                <div class="flex items-center gap-3">
                    <div class="flex items-center gap-2">
                        <i data-lucide="file-text" class="w-6 h-6 text-cyan-400"></i>
                        <h2 class="text-xl font-bold tracking-tight text-white">Reports & Export</h2>
                    </div>

                    <!-- 3-TAB TOGGLE BUTTONS -->
                    <div class="flex items-center bg-slate-900 border border-slate-800 rounded-xl p-1 shadow-inner gap-1">
                        <button id="tab-btn-report-prod" class="px-3.5 py-1.5 rounded-lg text-xs font-bold transition-all flex items-center gap-1.5 cursor-pointer ${this.activeReportTab === 'production' ? 'bg-gradient-to-r from-cyan-600 to-blue-600 text-white shadow-md shadow-cyan-600/30' : 'text-slate-400 hover:text-slate-200 hover:bg-slate-800/60'}">
                            <i data-lucide="activity" class="w-3.5 h-3.5"></i>
                            <span>${this.t('reports.tab_prod', '1. Laporan Produksi & OEE')}</span>
                            <span class="px-1.5 py-0.2 rounded-full text-[10px] ${this.activeReportTab === 'production' ? 'bg-white/20 text-white' : 'bg-slate-800 text-slate-400'} font-mono">${rows.length}</span>
                        </button>
                        <button id="tab-btn-report-ng" class="px-3.5 py-1.5 rounded-lg text-xs font-bold transition-all flex items-center gap-1.5 cursor-pointer ${this.activeReportTab === 'ng' ? 'bg-gradient-to-r from-amber-600 to-rose-600 text-white shadow-md shadow-amber-600/30' : 'text-slate-400 hover:text-slate-200 hover:bg-slate-800/60'}">
                            <i data-lucide="shield-alert" class="w-3.5 h-3.5"></i>
                            <span>${this.t('reports.tab_ng', '2. Laporan Data NG')}</span>
                            <span class="px-1.5 py-0.2 rounded-full text-[10px] ${this.activeReportTab === 'ng' ? 'bg-white/20 text-white' : 'bg-slate-800 text-amber-400'} font-mono">${totalNgReports}</span>
                        </button>
                        <button id="tab-btn-report-trouble" class="px-3.5 py-1.5 rounded-lg text-xs font-bold transition-all flex items-center gap-1.5 cursor-pointer ${this.activeReportTab === 'trouble' ? 'bg-gradient-to-r from-rose-600 to-amber-600 text-white shadow-md shadow-rose-600/30' : 'text-slate-400 hover:text-slate-200 hover:bg-slate-800/60'}">
                            <i data-lucide="wrench" class="w-3.5 h-3.5"></i>
                            <span>${this.t('reports.tab_trouble', '3. Laporan Trouble & Downtime')}</span>
                            <span class="px-1.5 py-0.2 rounded-full text-[10px] ${this.activeReportTab === 'trouble' ? 'bg-white/20 text-white' : 'bg-slate-800 text-rose-400'} font-mono">${totalTroubleReports}</span>
                        </button>
                    </div>
                </div>

                <!-- STANDARDIZED FILTER STRIP -->
                <div class="flex flex-wrap items-center gap-2">
                    <!-- DATE RANGE FILTERS -->
                    <div class="flex items-center gap-1.5 bg-slate-900 border border-slate-800 rounded-lg px-2.5 py-1" title="Pilih Tanggal Mulai Laporan">
                        <i data-lucide="calendar" class="w-3.5 h-3.5 text-emerald-400"></i>
                        <span class="text-slate-400 text-[11px] font-medium">${this.currentLang === 'ja' ? '開始日:' : 'Dari:'}</span>
                        <input type="date" id="report-filter-start-date" value="${this.reportFilters.start_date}" class="bg-transparent text-slate-100 text-xs focus:outline-none cursor-pointer font-sans" />
                    </div>

                    <div class="flex items-center gap-1.5 bg-slate-900 border border-slate-800 rounded-lg px-2.5 py-1" title="Pilih Tanggal Selesai Laporan">
                        <i data-lucide="calendar-days" class="w-3.5 h-3.5 text-cyan-400"></i>
                        <span class="text-slate-400 text-[11px] font-medium">${this.currentLang === 'ja' ? '終了日:' : 'Sampai:'}</span>
                        <input type="date" id="report-filter-end-date" value="${this.reportFilters.end_date}" class="bg-transparent text-slate-100 text-xs focus:outline-none cursor-pointer font-sans" />
                    </div>

                    <!-- LINE FILTER -->
                    <div class="flex items-center gap-1.5 bg-slate-900 border border-slate-800 rounded-lg px-2.5 py-1">
                        <i data-lucide="git-fork" class="w-3.5 h-3.5 text-cyan-400"></i>
                        <span class="text-slate-400 text-[11px] font-medium">Line:</span>
                        <select id="report-filter-line" class="bg-transparent text-slate-100 text-xs focus:outline-none cursor-pointer font-sans max-w-[140px]">
                            <option value="" class="bg-slate-900 text-slate-100">${this.t('header.all_lines', 'Semua Line')}</option>
                            ${(this.masterData.lines || []).map(l => `<option value="${l.id}" class="bg-slate-900 text-slate-100" ${this.reportFilters.line_id == l.id ? 'selected' : ''}>${l.name}</option>`).join('')}
                        </select>
                    </div>

                    <!-- TYPE / PRODUCT FILTER -->
                    <div class="flex items-center gap-1.5 bg-slate-900 border border-slate-800 rounded-lg px-2.5 py-1">
                        <i data-lucide="layers" class="w-3.5 h-3.5 text-purple-400"></i>
                        <span class="text-slate-400 text-[11px] font-medium">Type:</span>
                        <select id="report-filter-type" class="bg-transparent text-slate-100 text-xs focus:outline-none cursor-pointer font-sans max-w-[150px]">
                            <option value="" class="bg-slate-900 text-slate-100">${this.currentLang === 'ja' ? '全製品・型番' : 'Semua Type Produk'}</option>
                            ${(this.masterData.products || []).map(p => `<option value="${p.id}" class="bg-slate-900 text-slate-100" ${this.reportFilters.product_id == p.id ? 'selected' : ''}>${p.name} - ${p.sku}</option>`).join('')}
                        </select>
                    </div>

                    <!-- SHIFT FILTER -->
                    <div class="flex items-center gap-1.5 bg-slate-900 border border-slate-800 rounded-lg px-2.5 py-1">
                        <i data-lucide="clock" class="w-3.5 h-3.5 text-amber-400"></i>
                        <span class="text-slate-400 text-[11px] font-medium">Shift:</span>
                        <select id="report-filter-shift" class="bg-transparent text-slate-100 text-xs focus:outline-none cursor-pointer font-sans">
                            <option value="" class="bg-slate-900 text-slate-100" ${!this.reportFilters.shift_id ? 'selected' : ''}>${this.t('header.all_shifts', 'Semua Shift')}</option>
                            ${(this.masterData.shifts || []).map(s => `<option value="${s.id}" class="bg-slate-900 text-slate-100" ${this.reportFilters.shift_id == s.id ? 'selected' : ''}>${s.name}</option>`).join('')}
                        </select>
                    </div>

                    <!-- SUPERVISOR / PIC SELECTOR -->
                    <div class="flex items-center gap-1.5 bg-slate-900 border border-slate-800 rounded-lg px-2.5 py-1" title="Pilih atau ketik nama Supervisor / QC In-Charge">
                        <i data-lucide="user-check" class="w-3.5 h-3.5 text-emerald-400"></i>
                        <span class="text-slate-400 text-[11px] font-medium">PIC:</span>
                        <input type="text" id="report-filter-supervisor" list="supervisors-datalist" value="${this.reportFilters.supervisor || header.supervisor_in_charge || ''}" placeholder="${this.currentLang === 'ja' ? '担当者名を入力...' : 'Ketik PIC...'}" class="bg-transparent text-slate-100 text-xs focus:outline-none font-sans w-28 border-b border-slate-700/60 focus:border-cyan-400 transition-colors" />
                        <datalist id="supervisors-datalist">
                            ${(header.available_supervisors || ['Alim Utama', 'Hendra Wijaya', 'Surya Pratama', 'Budi Santoso']).map(s => `<option value="${this.escapeHtml(s)}"></option>`).join('')}
                        </datalist>
                    </div>

                    <!-- CONDITIONAL ACTION BUTTONS BASED ON ACTIVE TAB -->
                    <div id="report-actions-prod" class="${this.activeReportTab === 'production' ? 'flex' : 'hidden'} items-center gap-2">
                        <button id="btn-print-report" class="bg-blue-600 hover:bg-blue-500 text-white text-xs px-3 py-1.5 rounded-lg font-semibold transition-all shadow-md shadow-blue-600/20 flex items-center gap-1.5 cursor-pointer">
                            <i data-lucide="printer" class="w-3.5 h-3.5"></i>
                            <span>${this.t('reports.btn_print_oee', 'Print Handover (A4)')}</span>
                        </button>
                        <button id="btn-export-excel-report" class="bg-emerald-600 hover:bg-emerald-500 text-white text-xs px-3 py-1.5 rounded-lg font-semibold transition-all shadow-md shadow-emerald-600/20 flex items-center gap-1.5 cursor-pointer">
                            <i data-lucide="file-spreadsheet" class="w-3.5 h-3.5"></i>
                            <span>${this.t('reports.btn_export_excel', 'Export Excel / CSV')}</span>
                        </button>
                    </div>
                    <div id="report-actions-ng" class="${this.activeReportTab === 'ng' ? 'flex' : 'hidden'} items-center gap-2">
                        <button id="btn-print-ng-report" class="bg-amber-600 hover:bg-amber-500 text-white text-xs px-3 py-1.5 rounded-lg font-semibold transition-all shadow-md shadow-amber-600/20 flex items-center gap-1.5 cursor-pointer">
                            <i data-lucide="printer" class="w-3.5 h-3.5"></i>
                            <span>${this.t('reports.btn_print_ng', 'Cetak Laporan NG (A4)')}</span>
                        </button>
                        <button id="btn-export-excel-ng" class="bg-emerald-600 hover:bg-emerald-500 text-white text-xs px-3 py-1.5 rounded-lg font-semibold transition-all shadow-md shadow-emerald-600/20 flex items-center gap-1.5 cursor-pointer">
                            <i data-lucide="file-spreadsheet" class="w-3.5 h-3.5"></i>
                            <span>${this.t('reports.btn_export_excel_ng', 'Export Excel (Data NG)')}</span>
                        </button>
                    </div>
                    <div id="report-actions-trouble" class="${this.activeReportTab === 'trouble' ? 'flex' : 'hidden'} items-center gap-2">
                        <button id="btn-print-trouble-report" class="bg-rose-600 hover:bg-rose-500 text-white text-xs px-3 py-1.5 rounded-lg font-semibold transition-all shadow-md shadow-rose-600/20 flex items-center gap-1.5 cursor-pointer">
                            <i data-lucide="printer" class="w-3.5 h-3.5"></i>
                            <span>${this.t('reports.btn_print_trouble', 'Cetak Laporan Trouble (A4)')}</span>
                        </button>
                        <button id="btn-export-excel-trouble" class="bg-emerald-600 hover:bg-emerald-500 text-white text-xs px-3 py-1.5 rounded-lg font-semibold transition-all shadow-md shadow-emerald-600/20 flex items-center gap-1.5 cursor-pointer">
                            <i data-lucide="file-spreadsheet" class="w-3.5 h-3.5"></i>
                            <span>${this.t('reports.btn_export_excel_trouble', 'Export Excel (Data Trouble)')}</span>
                        </button>
                    </div>
                </div>
            </div>

            <!-- ========================================== -->
            <!-- TAB 1 CONTENT: LAPORAN PRODUKSI & HANDOVER -->
            <!-- ========================================== -->
            <div id="report-pane-production" class="${this.activeReportTab === 'production' ? '' : 'hidden'}">
                <!-- METADATA HEADER BANNER -->
                <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl mb-4 print-card">
                    <div class="flex flex-wrap items-center justify-between gap-4 border-b border-slate-800 pb-3 mb-3">
                        <div class="flex items-center gap-3 min-w-0">
                            <div class="w-12 h-12 rounded-lg bg-slate-950 border border-slate-800 flex items-center justify-center flex-shrink-0 text-cyan-400 p-1">
                                <img src="${this.escapeHtml(this.getCompanyLogo(header))}" class="w-full h-full object-contain" alt="Logo" onerror="this.onerror=null; this.src='/images/yasunaga-logo.png';" />
                            </div>
                            <div class="min-w-0">
                                <div class="text-xs font-bold text-cyan-400 uppercase tracking-wider">${header.company_name || 'PT. YASUNAGA INDONESIA'}</div>
                                <div class="text-sm font-black tracking-wide text-white uppercase truncate">${header.plant_name || 'ENGINE PARTS & AIR PUMP MFG'}</div>
                                <div class="text-[10px] text-slate-400 font-medium truncate">${header.address || header.plant_location || ''} • <strong class="text-slate-200 font-mono">${header.plant_id || 'PLT-01'}</strong></div>
                            </div>
                        </div>

                        <div class="flex flex-wrap items-center gap-4 text-xs font-mono">
                            <div class="bg-slate-950 border border-slate-800 px-3 py-1 rounded-lg">
                                <span class="text-slate-400 font-sans">Shift:</span> 
                                <strong class="text-cyan-400">${header.shift_name}</strong> 
                                <span class="text-slate-500 text-[10px]">(${header.shift_time_window})</span>
                            </div>
                            <div class="bg-slate-950 border border-slate-800 px-3 py-1 rounded-lg">
                                <span class="text-slate-400 font-sans">Date:</span> 
                                <strong class="text-amber-400">${header.formatted_date}</strong>
                            </div>
                            <div class="bg-slate-950 border border-slate-800 px-3 py-1 rounded-lg flex items-center gap-1.5 cursor-pointer hover:border-cyan-500 transition-colors group" id="btn-quick-edit-supervisor" title="Klik untuk mengganti Supervisor">
                                <span class="text-slate-400 font-sans">Supervisor:</span> 
                                <strong class="text-emerald-400 group-hover:text-emerald-300">${header.supervisor_in_charge}</strong>
                                <i data-lucide="edit-2" class="w-3 h-3 text-slate-500 group-hover:text-cyan-400"></i>
                            </div>
                            <span class="px-2.5 py-1 rounded-lg bg-emerald-950/80 text-emerald-400 border border-emerald-800 font-bold text-[11px] flex items-center gap-1.5">
                                <span class="w-2 h-2 rounded-full bg-emerald-400"></span> SHIFT COMPLETE
                            </span>
                        </div>
                    </div>

                    <div class="grid grid-cols-2 sm:grid-cols-4 gap-3 text-xs font-mono">
                        <div>
                            <span class="text-slate-400 text-[10px] block uppercase">Scope Target:</span>
                            <span class="font-bold text-slate-200">${header.line_name}</span>
                        </div>
                        <div>
                            <span class="text-slate-400 text-[10px] block uppercase">Baseline Target OEE:</span>
                            <span class="font-bold text-cyan-400">85.00% (World Class)</span>
                        </div>
                        <div>
                            <span class="text-slate-400 text-[10px] block uppercase">Standard Target Speed:</span>
                            <span class="font-bold text-amber-400">Ideal Cycle Standard</span>
                        </div>
                        <div>
                            <span class="text-slate-400 text-[10px] block uppercase">Target Quality Rate:</span>
                            <span class="font-bold text-purple-400">99.00% (Zero Defect)</span>
                        </div>
                    </div>
                </div>

                <!-- 5 MACRO KPI CARDS -->
                <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-5 gap-3 mb-4">
                    <!-- CARD 1: OVERALL PLANT OEE -->
                    <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl flex flex-col justify-between print-card">
                        <div class="flex items-center justify-between mb-1">
                            <span class="text-[11px] font-bold uppercase tracking-wider text-slate-400">Overall Plant OEE</span>
                            <span class="w-2 h-2 rounded-full ${summary.overall_oee >= 85 ? 'bg-emerald-400 shadow-[0_0_8px_#10b981]' : (summary.overall_oee >= 65 ? 'bg-amber-400' : 'bg-rose-500')}"></span>
                        </div>
                        <div class="my-1.5 flex items-baseline justify-between">
                            <div class="font-mono text-3xl font-black text-white">${summary.overall_oee.toFixed(2)}<span class="text-sm font-semibold text-slate-400 ml-0.5">%</span></div>
                            <div class="text-right text-[10px] font-mono text-slate-400">Target: <strong>85.00%</strong></div>
                        </div>
                        <div class="pt-2 border-t border-slate-800 flex items-center justify-between">
                            ${renderDevBadge(summary.overall_oee, 85.0)}
                            <span class="text-[9px] font-bold px-1.5 py-0.2 rounded border ${summary.overall_oee >= 85 ? 'bg-emerald-950/60 text-emerald-400 border-emerald-800' : (summary.overall_oee >= 65 ? 'bg-amber-950/60 text-amber-400 border-amber-800' : 'bg-rose-950/60 text-rose-400 border-rose-800')}">
                                ${summary.overall_oee >= 85 ? 'OPTIMAL' : (summary.overall_oee >= 65 ? 'WARNING' : 'CRITICAL')}
                            </span>
                        </div>
                    </div>

                    <!-- CARD 2: AVAILABILITY -->
                    <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl flex flex-col justify-between print-card">
                        <div class="flex items-center justify-between mb-1">
                            <span class="text-[11px] font-bold uppercase tracking-wider text-slate-400">Availability (A)</span>
                            <span class="w-2 h-2 rounded-full ${summary.availability >= 90 ? 'bg-emerald-400' : 'bg-amber-400'}"></span>
                        </div>
                        <div class="my-1.5 flex items-baseline justify-between">
                            <div class="font-mono text-3xl font-black text-white">${summary.availability.toFixed(2)}<span class="text-sm font-semibold text-slate-400 ml-0.5">%</span></div>
                            <div class="text-right text-[10px] font-mono text-slate-400">Target: <strong>90.00%</strong></div>
                        </div>
                        <div class="pt-2 border-t border-slate-800 flex items-center justify-between">
                            ${renderDevBadge(summary.availability, 90.0)}
                            <span class="text-[10px] font-mono text-slate-400">Down: <strong class="text-rose-400">${summary.total_downtime_minutes}m</strong></span>
                        </div>
                    </div>

                    <!-- CARD 3: PERFORMANCE -->
                    <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl flex flex-col justify-between print-card">
                        <div class="flex items-center justify-between mb-1">
                            <span class="text-[11px] font-bold uppercase tracking-wider text-slate-400">Performance (P)</span>
                            <span class="w-2 h-2 rounded-full ${summary.performance >= 95 ? 'bg-emerald-400' : 'bg-amber-400'}"></span>
                        </div>
                        <div class="my-1.5 flex items-baseline justify-between">
                            <div class="font-mono text-3xl font-black text-white">${summary.performance.toFixed(2)}<span class="text-sm font-semibold text-slate-400 ml-0.5">%</span></div>
                            <div class="text-right text-[10px] font-mono text-slate-400">Target: <strong>95.00%</strong></div>
                        </div>
                        <div class="pt-2 border-t border-slate-800 flex items-center justify-between">
                            ${renderDevBadge(summary.performance, 95.0)}
                            <span class="text-[10px] font-mono text-slate-400">Run: <strong class="text-cyan-400">${summary.total_run_time_minutes}m</strong></span>
                        </div>
                    </div>

                    <!-- CARD 4: QUALITY -->
                    <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl flex flex-col justify-between print-card">
                        <div class="flex items-center justify-between mb-1">
                            <span class="text-[11px] font-bold uppercase tracking-wider text-slate-400">Quality Rate (Q)</span>
                            <span class="w-2 h-2 rounded-full ${summary.quality >= 99 ? 'bg-emerald-400' : (summary.quality >= 95 ? 'bg-amber-400' : 'bg-rose-500')}"></span>
                        </div>
                        <div class="my-1.5 flex items-baseline justify-between">
                            <div class="font-mono text-3xl font-black text-white">${summary.quality.toFixed(2)}<span class="text-sm font-semibold text-slate-400 ml-0.5">%</span></div>
                            <div class="text-right text-[10px] font-mono text-slate-400">Target: <strong>99.00%</strong></div>
                        </div>
                        <div class="pt-2 border-t border-slate-800 flex items-center justify-between">
                            ${renderDevBadge(summary.quality, 99.0)}
                            <span class="text-[10px] font-mono text-slate-400">Yield: <strong class="text-purple-400">${summary.yield_rate_pct}%</strong></span>
                        </div>
                    </div>

                    <!-- CARD 5: OUTPUT & REJECT SUMMARY -->
                    <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl flex flex-col justify-between print-card">
                        <div class="flex items-center justify-between mb-1">
                            <span class="text-[11px] font-bold uppercase tracking-wider text-slate-400">Output vs Reject</span>
                            <i data-lucide="package" class="w-4 h-4 text-emerald-400"></i>
                        </div>
                        <div class="my-1 flex items-baseline justify-between">
                            <div class="font-mono text-2xl font-black text-white">${summary.total_output_pcs.toLocaleString()}<span class="text-xs font-normal text-slate-400 ml-0.5">pcs</span></div>
                            <div class="text-right text-[10px] font-mono text-emerald-400 font-bold">${summary.good_output_pcs.toLocaleString()} OK</div>
                        </div>
                        <div class="pt-2 border-t border-slate-800 flex items-center justify-between text-[10px] font-mono">
                            <span class="text-rose-400 font-bold">Reject: ${summary.reject_output_pcs}</span>
                            <span class="text-amber-400 font-bold">Scrap: ${summary.scrap_output_pcs}</span>
                        </div>
                    </div>
                </div>

                <!-- CORE PRODUCTION MATRIX TABLE -->
                <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl mb-4 print-card">
                    <div class="flex items-center justify-between border-b border-slate-800 pb-3 mb-3">
                        <div>
                            <h3 class="text-sm font-bold text-slate-100 flex items-center gap-2">
                                <i data-lucide="table" class="w-4 h-4 text-cyan-400"></i>
                                Production Lines Core Performance Matrix
                            </h3>
                            <p class="text-[11px] text-slate-400 mt-0.5">Comprehensive OEE factors, actual output, cycle time deviations & operational status per line</p>
                        </div>
                        <div class="font-mono text-xs text-slate-400">
                            Total Lines: <strong class="text-slate-200">${rows.length}</strong>
                        </div>
                    </div>

                    <div class="overflow-x-auto">
                        <table class="w-full text-xs text-left font-sans border-collapse print-table">
                            <thead class="bg-slate-950/80 text-slate-400 uppercase text-[10px] font-mono tracking-wider border-b border-slate-800">
                                <tr>
                                    <th class="py-2.5 px-3 text-center w-10">No</th>
                                    <th class="py-2.5 px-3">Line ID & Name</th>
                                    <th class="py-2.5 px-3">Machine & Produk - SKU</th>
                                    <th class="py-2.5 px-2 text-right">Planned (Min)</th>
                                    <th class="py-2.5 px-2 text-right">Down (Min)</th>
                                    <th class="py-2.5 px-2 text-right font-bold text-cyan-400">Avail (%)</th>
                                    <th class="py-2.5 px-2 text-right">Target Speed</th>
                                    <th class="py-2.5 px-2 text-right">Output (Pcs)</th>
                                    <th class="py-2.5 px-2 text-right font-bold text-amber-400">Perf (%)</th>
                                    <th class="py-2.5 px-2 text-right">Defect (Rej+Scrap)</th>
                                    <th class="py-2.5 px-2 text-right font-bold text-purple-400">Quality (%)</th>
                                    <th class="py-2.5 px-3 text-right font-bold text-emerald-400">OEE Score</th>
                                    <th class="py-2.5 px-3 text-center">Status Classification</th>
                                    <th class="py-2.5 px-2 text-center no-print">Action</th>
                                </tr>
                            </thead>
                            <tbody class="divide-y divide-slate-800/60 font-mono">
                                ${rows.length === 0 ? `<tr><td colspan="14" class="py-8 text-center text-slate-500 font-sans">No production records found for selected date range, line, or shift.</td></tr>` : ''}
                                ${prodDateGroups.map((group) => {
                                    const groupOutput = group.items.reduce((s, r) => s + (r.total_output_pcs || 0), 0);
                                    const groupDefect = group.items.reduce((s, r) => s + (r.total_defect_pcs || 0), 0);
                                    const groupAvgOee = group.items.length > 0 ? (group.items.reduce((s, r) => s + (r.oee_pct || 0), 0) / group.items.length).toFixed(2) : '0.00';

                                    return `
                                        <!-- DATE GROUP SEPARATOR -->
                                        <tr class="bg-slate-950/90 border-y-2 border-cyan-700/60 font-mono">
                                            <td colspan="14" class="py-2.5 px-3">
                                                <div class="flex flex-wrap items-center justify-between gap-2">
                                                    <div class="flex items-center gap-2">
                                                        <span class="px-2 py-0.5 rounded bg-cyan-950 text-cyan-300 border border-cyan-800 text-[10px] font-bold uppercase font-mono flex items-center gap-1.5">
                                                            <i data-lucide="calendar" class="w-3.5 h-3.5 text-cyan-400"></i>
                                                            TANGGAL PRODUKSI: ${group.formatted_date}
                                                        </span>
                                                        <span class="text-[11px] text-slate-300 font-sans font-bold">
                                                            Rekap Produksi (${group.items.length} Mesin / Lini Operasional)
                                                        </span>
                                                    </div>
                                                    <div class="flex items-center gap-3 text-[11px] text-slate-400 font-mono">
                                                        <span>Output: <strong class="text-white">${groupOutput.toLocaleString()} pcs</strong></span>
                                                        <span>Defect: <strong class="text-rose-400">${groupDefect.toLocaleString()} pcs</strong></span>
                                                        <span>Avg OEE: <strong class="${Number(groupAvgOee) >= 85 ? 'text-emerald-400' : 'text-amber-400'}">${groupAvgOee}%</strong></span>
                                                    </div>
                                                </div>
                                            </td>
                                        </tr>
                                        ${group.items.map((row, idx) => {
                                            const oeeClass = row.oee_pct >= 85 
                                                ? 'bg-emerald-950/20 text-emerald-400 font-black' 
                                                : (row.oee_pct >= 65 ? 'bg-amber-950/20 text-amber-400 font-bold' : 'bg-rose-950/30 text-rose-400 font-black');
                                            
                                            const availClass = row.availability_pct >= 90 ? 'text-emerald-400' : 'text-amber-400';
                                            const perfClass = row.performance_pct >= 95 ? 'text-emerald-400' : 'text-amber-400';
                                            const qualClass = row.quality_pct >= 99 ? 'text-emerald-400' : (row.quality_pct >= 95 ? 'text-amber-400' : 'text-rose-400');

                                            return `
                                                <tr class="hover:bg-slate-800/40 transition-colors cursor-pointer row-line-drilldown" data-row-index="${rows.indexOf(row)}">
                                                    <td class="py-2.5 px-3 text-center font-bold text-slate-500 font-mono text-[11px]">${idx + 1}</td>
                                                    <td class="py-2.5 px-3">
                                                        <div class="font-bold text-slate-200">${row.line_name}</div>
                                                        <div class="text-[10px] text-slate-400 font-mono">${row.shift_name ? `${row.shift_name} • ` : ''}${row.line_id}</div>
                                                    </td>
                                                    <td class="py-2.5 px-3 font-sans">
                                                        <div class="font-mono text-cyan-400 font-bold text-[11px]">${row.machine_code}</div>
                                                        <div class="text-[10px] text-amber-400 truncate max-w-[150px]">${row.product_sku}</div>
                                                    </td>
                                                    <td class="py-2.5 px-2 text-right text-slate-300">${row.planned_time_minutes.toFixed(1)}</td>
                                                    <td class="py-2.5 px-2 text-right ${row.downtime_minutes > 45 ? 'text-rose-400 font-bold' : 'text-slate-300'}">${row.downtime_minutes.toFixed(1)}</td>
                                                    <td class="py-2.5 px-2 text-right font-bold ${availClass}">${row.availability_pct.toFixed(2)}%</td>
                                                    <td class="py-2.5 px-2 text-right text-slate-300 font-sans">${row.target_speed > 0 ? row.target_speed + ' pcs/m' : '-'}</td>
                                                    <td class="py-2.5 px-2 text-right text-slate-200 font-bold">${row.total_output_pcs.toLocaleString()}</td>
                                                    <td class="py-2.5 px-2 text-right font-bold ${perfClass}">${row.performance_pct.toFixed(2)}%</td>
                                                    <td class="py-2.5 px-2 text-right ${row.total_defect_pcs > 100 ? 'text-rose-400 font-bold' : 'text-slate-300'}">
                                                        <div>${row.total_defect_pcs.toLocaleString()}</div>
                                                        <div class="text-[9px] text-slate-500 font-normal font-sans">(${row.reject_pcs || 0} rej + ${row.scrap_pcs || 0} scp)</div>
                                                    </td>
                                                    <td class="py-2.5 px-2 text-right font-bold ${qualClass}">${row.quality_pct.toFixed(2)}%</td>
                                                    <td class="py-2.5 px-3 text-right text-sm ${oeeClass}">${row.oee_pct.toFixed(2)}%</td>
                                                    <td class="py-2.5 px-3 text-center">
                                                        <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded text-[10px] font-bold border ${row.oee_pct >= 85 ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/30' : (row.oee_pct >= 65 ? 'bg-amber-500/10 text-amber-400 border-amber-500/30' : 'bg-rose-500/10 text-rose-400 border-rose-500/30')}">
                                                            ${row.status_badge}
                                                        </span>
                                                    </td>
                                                    <td class="py-2.5 px-2 text-center no-print">
                                                        <button class="text-[11px] text-cyan-400 hover:text-cyan-300 font-semibold px-2 py-1 rounded bg-cyan-950/60 border border-cyan-800 flex items-center gap-1 mx-auto cursor-pointer">
                                                            <i data-lucide="eye" class="w-3 h-3"></i> Details
                                                        </button>
                                                    </td>
                                                </tr>
                                            `;
                                        }).join('')}
                                    `;
                                }).join('')}
                            </tbody>
                            <tfoot class="bg-slate-950/90 font-mono font-bold border-t-2 border-slate-700 text-slate-200 text-xs">
                                <tr>
                                    <td colspan="3" class="py-3 px-3 uppercase tracking-wider font-sans font-black text-cyan-400">PLANT AVERAGE / TOTALS:</td>
                                    <td class="py-3 px-2 text-right">${summary.total_planned_minutes.toFixed(1)}</td>
                                    <td class="py-3 px-2 text-right text-rose-400">${summary.total_downtime_minutes.toFixed(1)}</td>
                                    <td class="py-3 px-2 text-right text-cyan-400">${summary.availability.toFixed(2)}%</td>
                                    <td class="py-3 px-2 text-right text-slate-500 font-sans">—</td>
                                    <td class="py-3 px-2 text-right text-white">${summary.total_output_pcs.toLocaleString()}</td>
                                    <td class="py-3 px-2 text-right text-amber-400">${summary.performance.toFixed(2)}%</td>
                                    <td class="py-3 px-2 text-right text-rose-400">
                                        <div>${summary.total_defect_pcs.toLocaleString()}</div>
                                        <div class="text-[9px] text-slate-400 font-normal font-sans">(${summary.reject_output_pcs} rej + ${summary.scrap_output_pcs} scp)</div>
                                    </td>
                                    <td class="py-3 px-2 text-right text-purple-400">${summary.quality.toFixed(2)}%</td>
                                    <td class="py-3 px-3 text-right text-sm font-black ${summary.overall_oee >= 85 ? 'text-emerald-400' : 'text-amber-400'}">${summary.overall_oee.toFixed(2)}%</td>
                                    <td colspan="2" class="py-3 px-3 text-center">
                                        <span class="text-[11px] font-bold text-slate-300">${summary.oee_badge}</span>
                                    </td>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                </div>

                <!-- SIX BIG LOSSES & INCIDENTS -->
                <div class="grid grid-cols-1 lg:grid-cols-2 gap-4 mb-4">
                    <!-- SIX BIG LOSSES PARETO -->
                    <div id="card-report-six-losses" class="${isLight ? 'bg-white border-slate-200 shadow-sm' : 'bg-slate-900 border-slate-800 shadow-xl'} border rounded-xl p-4 flex flex-col justify-between print-card cursor-pointer group hover:border-cyan-500/60 transition-all">
                        <div class="flex items-center justify-between border-b ${isLight ? 'border-slate-200' : 'border-slate-800'} pb-3 mb-3">
                            <div>
                                <h3 class="text-sm font-bold ${isLight ? 'text-slate-800 group-hover:text-cyan-600' : 'text-slate-100 group-hover:text-cyan-400'} flex items-center gap-2 transition-colors">
                                    <i data-lucide="pie-chart" class="w-4 h-4 text-rose-400 group-hover:scale-110 transition-transform"></i>
                                    <span>Six Big Losses Pareto Breakdown</span>
                                    <span class="text-[9px] font-mono font-bold px-1.5 py-0.5 rounded bg-cyan-950 text-cyan-400 border border-cyan-800 flex items-center gap-1 opacity-80 group-hover:opacity-100"><i data-lucide="maximize-2" class="w-3 h-3"></i> Detail</span>
                                </h3>
                                <p class="text-[11px] text-slate-400 mt-0.5">Analisis 6 pilar TPM &bull; <span class="text-cyan-400 underline">Klik untuk popup breakdown</span></p>
                            </div>
                            <span class="text-[10px] font-mono px-2 py-0.5 rounded ${isLight ? 'bg-rose-100 text-rose-700 border border-rose-300' : 'bg-rose-950 text-rose-400 border border-rose-800'} font-bold" id="report-six-losses-total-badge">
                                Total Loss
                            </span>
                        </div>
                        <div id="report-chart-six-losses" class="space-y-2.5"></div>
                    </div>

                    <!-- MAJOR INCIDENTS LOG -->
                    <div class="${isLight ? 'bg-white border-slate-200 shadow-sm' : 'bg-slate-900 border-slate-800 shadow-xl'} border rounded-xl p-4 flex flex-col justify-between print-card">
                        <div class="flex-1 flex flex-col">
                            <!-- Card Header with Dynamic Count & Lost Time Badges -->
                            <div class="flex items-center justify-between border-b ${isLight ? 'border-slate-200' : 'border-slate-800'} pb-3 mb-3">
                                <div class="flex items-center gap-2 min-w-0">
                                    <div class="w-7 h-7 rounded-lg ${isLight ? 'bg-amber-100 text-amber-600 border border-amber-200' : 'bg-amber-500/10 text-amber-400 border border-amber-500/20'} flex items-center justify-center flex-shrink-0">
                                        <i data-lucide="alert-octagon" class="w-4 h-4"></i>
                                    </div>
                                    <div class="min-w-0">
                                        <h3 class="text-sm font-bold ${isLight ? 'text-slate-800' : 'text-slate-100'} flex items-center gap-1.5 truncate">
                                            Major Incident Log Table (Downtime & Trouble)
                                        </h3>
                                        <p class="text-[10px] ${isLight ? 'text-slate-500' : 'text-slate-400'} font-mono">Downtime signifikan dengan threshold &gt; 5 menit</p>
                                    </div>
                                </div>
                                <div class="flex items-center gap-1.5 flex-shrink-0">
                                    ${incidentLogs.length > 0 ? `
                                        <span class="text-[10px] font-mono font-bold px-2 py-0.5 rounded-md ${isLight ? 'bg-rose-100 text-rose-700 border border-rose-300' : 'bg-rose-950/60 text-rose-400 border border-rose-800/60'} flex items-center gap-1 shadow-sm">
                                            <span class="w-1.5 h-1.5 rounded-full bg-rose-500 animate-pulse"></span>
                                            ${incidentLogs.length} Insiden
                                        </span>
                                        <span class="text-[10px] font-mono font-bold px-2 py-0.5 rounded-md ${isLight ? 'bg-amber-100 text-amber-800 border border-amber-300' : 'bg-amber-950/60 text-amber-400 border border-amber-800/60'}">
                                            ${totalIncidentMins}m Lost
                                        </span>
                                    ` : `
                                        <span class="text-[10px] font-mono font-bold px-2 py-0.5 rounded-md ${isLight ? 'bg-emerald-100 text-emerald-700 border border-emerald-300' : 'bg-emerald-950/60 text-emerald-400 border border-emerald-800/60'} flex items-center gap-1">
                                            <span class="w-1.5 h-1.5 rounded-full bg-emerald-500"></span>
                                            0 Insiden
                                        </span>
                                    `}
                                </div>
                            </div>

                            <!-- List of Incidents (Clean Top Alignment & Responsive Scroll) -->
                            <div class="space-y-2.5 overflow-y-auto max-h-[290px] pr-1 flex-1">
                                ${incidentLogs.length === 0 ? `
                                    <div class="p-8 text-center rounded-xl ${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950/40 border-slate-800/60'} border my-auto flex flex-col items-center justify-center">
                                        <div class="w-12 h-12 rounded-full ${isLight ? 'bg-emerald-100 text-emerald-600 border border-emerald-200' : 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'} mx-auto flex items-center justify-center mb-2 shadow-sm">
                                            <i data-lucide="shield-check" class="w-6 h-6"></i>
                                        </div>
                                        <div class="text-xs font-bold ${isLight ? 'text-slate-800' : 'text-slate-200'}">Zero Major Breakdown</div>
                                        <p class="text-[11px] ${isLight ? 'text-slate-500' : 'text-slate-400'} mt-1 max-w-xs mx-auto">Tidak ada catatan kendala mesin melebihi 5 menit pada rentang waktu ini. Lini beroperasi optimal.</p>
                                    </div>
                                ` : incidentLogs.map((inc) => `
                                    <div class="${isLight ? 'bg-slate-50/80 hover:bg-slate-100/90 border-slate-200 hover:border-slate-300' : 'bg-slate-950/70 hover:bg-slate-950 border-slate-800 hover:border-slate-700/80'} border rounded-xl p-3 text-xs transition-all shadow-sm">
                                        <div class="flex items-center justify-between gap-2 mb-2 pb-1.5 border-b ${isLight ? 'border-slate-200' : 'border-slate-800/70'}">
                                            <div class="flex items-center gap-2 min-w-0 font-mono">
                                                <span class="px-1.5 py-0.5 rounded ${isLight ? 'bg-sky-100 text-sky-800 border-sky-300' : 'bg-cyan-950/80 text-cyan-400 border-cyan-800/60'} border text-[11px] font-bold tracking-tight flex-shrink-0">
                                                    ${inc.incident_id}
                                                </span>
                                                <span class="text-[11px] ${isLight ? 'text-slate-600 font-medium' : 'text-slate-400'} flex items-center gap-1 flex-shrink-0">
                                                    <i data-lucide="clock" class="w-3 h-3 ${isLight ? 'text-slate-500' : 'text-slate-500'}"></i>
                                                    ${inc.time_window}
                                                </span>
                                                <span class="${isLight ? 'text-slate-300' : 'text-slate-600'}">•</span>
                                                <span class="${isLight ? 'text-slate-800' : 'text-slate-200'} font-bold truncate">
                                                    ${inc.machine_code} <span class="${isLight ? 'text-slate-500' : 'text-slate-400'} font-normal">(${inc.line_name})</span>
                                                </span>
                                            </div>
                                            <div class="flex items-center gap-1.5 flex-shrink-0">
                                                ${inc.category ? `
                                                    <span class="hidden sm:inline-block px-1.5 py-0.5 rounded text-[10px] font-sans font-semibold ${isLight ? 'bg-slate-200 text-slate-700 border-slate-300' : 'bg-slate-800/80 text-slate-300 border-slate-700/60'} border">
                                                        ${inc.category}
                                                    </span>
                                                ` : ''}
                                                <span class="px-2 py-0.5 rounded-md text-[11px] font-mono font-bold ${isLight ? 'bg-rose-100 text-rose-700 border-rose-300' : 'bg-rose-500/15 text-rose-400 border-rose-500/30'} border flex items-center gap-1">
                                                    <i data-lucide="timer" class="w-3 h-3"></i>
                                                    ${inc.duration_minutes}m
                                                </span>
                                            </div>
                                        </div>

                                        <div class="grid grid-cols-1 sm:grid-cols-2 gap-2 text-xs">
                                            <!-- ROOT CAUSE (RCA) -->
                                            <div class="${isLight ? 'bg-white border-slate-200 shadow-xs' : 'bg-slate-900/50 border-slate-800/50'} rounded-lg p-2.5 border">
                                                <span class="text-[10px] ${isLight ? 'text-rose-700 font-bold' : 'text-slate-400 font-semibold'} font-mono uppercase tracking-wider flex items-center gap-1 mb-1">
                                                    <i data-lucide="help-circle" class="w-3 h-3 ${isLight ? 'text-rose-600' : 'text-rose-400'}"></i>
                                                    ${this.t('dashboard.root_cause', 'Root Cause (RCA)')}
                                                </span>
                                                <span class="${isLight ? 'text-slate-800 font-medium' : 'text-slate-200 font-medium'} leading-relaxed block text-[11.5px]">
                                                    ${inc.root_cause || '-'}
                                                </span>
                                            </div>

                                            <!-- CORRECTIVE ACTION (CAPA) -->
                                            <div class="${isLight ? 'bg-white border-slate-200 shadow-xs' : 'bg-slate-900/50 border-slate-800/50'} rounded-lg p-2.5 border">
                                                <span class="text-[10px] ${isLight ? 'text-emerald-700 font-bold' : 'text-slate-400 font-semibold'} font-mono uppercase tracking-wider flex items-center gap-1 mb-1">
                                                    <i data-lucide="check-circle-2" class="w-3 h-3 ${isLight ? 'text-emerald-600' : 'text-emerald-400'}"></i>
                                                    ${this.t('dashboard.action_plan', 'Action Plan (CAPA)')}
                                                </span>
                                                <div class="flex items-start justify-between gap-1.5">
                                                    <span class="${isLight ? 'text-emerald-900 font-medium' : 'text-emerald-300 font-medium'} leading-relaxed text-[11.5px]">
                                                        ${inc.action_plan || '-'}
                                                    </span>
                                                    ${inc.pic ? `
                                                        <span class="inline-flex items-center gap-1 text-[9.5px] font-mono ${isLight ? 'text-emerald-800 bg-emerald-50 border-emerald-200 font-semibold' : 'text-emerald-400 bg-emerald-950/60 border-emerald-800/50'} px-1.5 py-0.5 rounded border flex-shrink-0 mt-0.5">
                                                            <i data-lucide="user" class="w-2.5 h-2.5"></i>
                                                            ${inc.pic}
                                                        </span>
                                                    ` : ''}
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                `).join('')}
                            </div>
                        </div>

                        <!-- INCIDENT FOOTER KPI SUMMARY (Mirrors Left Card Pillar Footer) -->
                        <div class="grid grid-cols-3 gap-2.5 pt-3 mt-3 border-t ${isLight ? 'border-slate-200' : 'border-slate-800'} font-mono text-center">
                            <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950/70 border-slate-800/80'} border rounded-lg p-2">
                                <div class="text-[10px] ${isLight ? 'text-slate-600' : 'text-slate-400'} uppercase font-sans font-bold flex items-center justify-center gap-1">
                                    <span class="w-1.5 h-1.5 rounded-full bg-amber-500"></span> Total Insiden
                                </div>
                                <div class="text-xs font-bold ${isLight ? 'text-amber-700' : 'text-amber-400'} mt-0.5">
                                    ${incidentLogs.length} Kasus
                                </div>
                            </div>
                            <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950/70 border-slate-800/80'} border rounded-lg p-2">
                                <div class="text-[10px] ${isLight ? 'text-slate-600' : 'text-slate-400'} uppercase font-sans font-bold flex items-center justify-center gap-1">
                                    <span class="w-1.5 h-1.5 rounded-full bg-rose-500"></span> Total Waktu Henti
                                </div>
                                <div class="text-xs font-bold ${isLight ? 'text-rose-700' : 'text-rose-400'} mt-0.5">
                                    ${totalIncidentMins}m <span class="text-[10px] font-normal ${isLight ? 'text-slate-500' : 'text-slate-400'}">(${(totalIncidentMins / 60).toFixed(1)}h)</span>
                                </div>
                            </div>
                            <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950/70 border-slate-800/80'} border rounded-lg p-2">
                                <div class="text-[10px] ${isLight ? 'text-slate-600' : 'text-slate-400'} uppercase font-sans font-bold flex items-center justify-center gap-1">
                                    <span class="w-1.5 h-1.5 rounded-full bg-cyan-500"></span> Rata-rata Durasi
                                </div>
                                <div class="text-xs font-bold ${isLight ? 'text-sky-700' : 'text-cyan-400'} mt-0.5">
                                    ${avgIncidentMins} Menit
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- HANDOVER INSTRUCTIONS & SIGNATURE BLOCKS -->
                <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl mb-4 print-card">
                    <div class="pb-3 mb-3 border-b border-slate-800 flex items-center justify-between">
                        <h3 class="text-sm font-bold text-slate-100 flex items-center gap-2">
                            <i data-lucide="clipboard-check" class="w-4 h-4 text-emerald-400"></i>
                            Shift Handover Instructions & Next-Shift Action Items
                        </h3>
                        <span class="text-[10px] font-mono text-slate-400">Official MES Handover Protocol</span>
                    </div>

                    <div class="grid grid-cols-1 md:grid-cols-3 gap-3 mb-4">
                        ${handoverNotes.map((note, i) => `
                            <div class="bg-slate-950 border border-slate-800 rounded-lg p-3 flex items-start gap-2.5">
                                <span class="w-5 h-5 rounded-full bg-cyan-950 text-cyan-400 border border-cyan-800 text-xs font-mono font-bold flex items-center justify-center flex-shrink-0 mt-0.5">${i + 1}</span>
                                <p class="text-xs text-slate-300 leading-relaxed">${note}</p>
                            </div>
                        `).join('')}
                    </div>

                    <!-- SIGNATURE BLOCKS -->
                    <div class="pt-4 border-t border-slate-800">
                        <div class="text-right text-xs text-slate-400 font-mono mb-3">
                            Tanggal: <strong class="text-slate-200">${header.formatted_date || ''}</strong>
                        </div>
                        <div class="grid grid-cols-2 gap-8 text-center font-sans text-xs">
                            <div>
                                <div class="text-slate-400 font-medium mb-12">Diserahkan Oleh (Shift Selesai):</div>
                                <div class="font-bold text-slate-200 border-b border-slate-700 pb-1 w-64 mx-auto">${header.supervisor_in_charge}</div>
                                <div class="text-[10px] text-slate-400 font-mono mt-1">Supervisor Shift In-Charge</div>
                            </div>
                            <div>
                                <div class="text-slate-400 font-medium mb-12">Diterima Oleh (Shift Berikutnya):</div>
                                <div class="font-bold text-slate-200 border-b border-slate-700 pb-1 w-64 mx-auto">( .................................................... )</div>
                                <div class="text-[10px] text-slate-400 font-mono mt-1">Supervisor Incoming Shift</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- ========================================== -->
            <!-- TAB 2 CONTENT: LAPORAN DATA NG & DEFECT    -->
            <!-- ========================================== -->
            <div id="report-pane-ng" class="${this.activeReportTab === 'ng' ? '' : 'hidden'}">
                <!-- METADATA HEADER BANNER FOR NG REPORT -->
                <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl mb-4 print-card">
                    <div class="flex flex-wrap items-center justify-between gap-4 border-b border-slate-800 pb-3 mb-3">
                        <div class="flex items-center gap-3 min-w-0">
                            <div class="w-12 h-12 rounded-lg bg-slate-950 border border-slate-800 flex items-center justify-center flex-shrink-0 text-amber-400 p-1">
                                <img src="${this.escapeHtml(this.getCompanyLogo(header))}" class="w-full h-full object-contain" alt="Logo" onerror="this.onerror=null; this.src='/images/yasunaga-logo.png';" />
                            </div>
                            <div class="min-w-0">
                                <div class="text-xs font-bold text-amber-400 uppercase tracking-wider">${header.company_name || 'PT. YASUNAGA INDONESIA'}</div>
                                <div class="text-sm font-black tracking-wide text-white uppercase truncate">LAPORAN DATA NG & KUALITAS DEFECT PRODUKSI</div>
                                <div class="text-[10px] text-slate-400 font-medium truncate">${header.address || header.plant_location || ''} • <strong class="text-slate-200 font-mono">${header.plant_id || 'PLT-01'}</strong></div>
                            </div>
                        </div>

                        <div class="flex flex-wrap items-center gap-4 text-xs font-mono">
                            <div class="bg-slate-950 border border-slate-800 px-3 py-1 rounded-lg">
                                <span class="text-slate-400 font-sans">Shift:</span> 
                                <strong class="text-cyan-400">${header.shift_name}</strong>
                            </div>
                            <div class="bg-slate-950 border border-slate-800 px-3 py-1 rounded-lg">
                                <span class="text-slate-400 font-sans">Tanggal:</span> 
                                <strong class="text-amber-400">${header.formatted_date}</strong>
                            </div>
                            <div class="bg-slate-950 border border-slate-800 px-3 py-1 rounded-lg">
                                <span class="text-slate-400 font-sans">Line:</span> 
                                <strong class="text-emerald-400">${header.line_name}</strong>
                            </div>
                            <span class="px-2.5 py-1 rounded-lg bg-amber-950/80 text-amber-400 border border-amber-800 font-bold text-[11px] flex items-center gap-1.5">
                                <i data-lucide="shield" class="w-3.5 h-3.5"></i> QC AUDIT REPORT
                            </span>
                        </div>
                    </div>

                    <div class="grid grid-cols-2 sm:grid-cols-4 gap-3 text-xs font-mono">
                        <div>
                            <span class="text-slate-400 text-[10px] block uppercase">Defect Benchmark:</span>
                            <span class="font-bold text-cyan-400">&lt; 1.00% (Zero Defect Target)</span>
                        </div>
                        <div>
                            <span class="text-slate-400 text-[10px] block uppercase">Pencapaian Defect Rate:</span>
                            <span class="font-bold ${Number(overallDefectRate) <= 1.0 ? 'text-emerald-400' : 'text-rose-400'}">${overallDefectRate}% (${overallDefectPpm} PPM)</span>
                        </div>
                        <div>
                            <span class="text-slate-400 text-[10px] block uppercase">Total Laporan Terkena NG:</span>
                            <span class="font-bold text-amber-400">${totalNgReports} Laporan Produksi</span>
                        </div>
                        <div>
                            <span class="text-slate-400 text-[10px] block uppercase">Status Verifikasi QC:</span>
                            <span class="font-bold text-purple-400">${ngStats.completed_count || 0} Terverifikasi / ${ngStats.pending_count || 0} Pending</span>
                        </div>
                    </div>
                </div>

                <!-- 5 QC MACRO KPI CARDS -->
                <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-5 gap-3 mb-4">
                    <!-- CARD 1: TOTAL NG vs OUTPUT -->
                    <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl flex flex-col justify-between print-card">
                        <div class="flex items-center justify-between mb-1">
                            <span class="text-[11px] font-bold uppercase tracking-wider text-slate-400">Total NG (Reject+Scrap)</span>
                            <span class="w-2 h-2 rounded-full ${totalNgPcsAll === 0 ? 'bg-emerald-400 shadow-[0_0_8px_#10b981]' : (Number(overallDefectRate) <= 2.0 ? 'bg-amber-400' : 'bg-rose-500')}"></span>
                        </div>
                        <div class="my-1.5 flex items-baseline justify-between">
                            <div class="font-mono text-3xl font-black text-rose-400">${totalNgPcsAll.toLocaleString()}<span class="text-xs font-semibold text-slate-400 ml-0.5">pcs</span></div>
                            <div class="text-right text-[10px] font-mono text-slate-400">Rate: <strong class="${Number(overallDefectRate) <= 1.0 ? 'text-emerald-400' : 'text-rose-400'}">${overallDefectRate}%</strong></div>
                        </div>
                        <div class="pt-2 border-t border-slate-800 flex items-center justify-between text-[10px] font-mono">
                            <span class="text-slate-400">Output: <strong class="text-slate-200">${totalOutputAll.toLocaleString()} pcs</strong></span>
                            <span class="text-emerald-400 font-bold">OK: ${totalGoodAll.toLocaleString()}</span>
                        </div>
                    </div>

                    <!-- CARD 2: WAJIB OEE (ASSY, ROD, CAP) -->
                    <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl flex flex-col justify-between print-card">
                        <div class="flex items-center justify-between mb-1">
                            <span class="text-[11px] font-bold uppercase tracking-wider text-slate-400">Wajib OEE (Komponen Utama)</span>
                            <i data-lucide="layers" class="w-4 h-4 text-cyan-400"></i>
                        </div>
                        <div class="my-1.5 flex items-baseline justify-between">
                            <div class="font-mono text-3xl font-black text-cyan-400">${totalOeeNgAll.toLocaleString()}<span class="text-xs font-semibold text-slate-400 ml-0.5">pcs</span></div>
                            <div class="text-right text-[10px] font-mono text-slate-400">${totalNgPcsAll > 0 ? ((totalOeeNgAll/totalNgPcsAll)*100).toFixed(0) : 0}% Defect</div>
                        </div>
                        <div class="pt-2 border-t border-slate-800 flex items-center justify-between text-[10px] font-mono text-slate-400">
                            <span>Assy: <strong class="text-cyan-400">${totalAssyAll}</strong></span>
                            <span>Rod: <strong class="text-cyan-400">${totalRodAll}</strong></span>
                            <span>Cap: <strong class="text-cyan-400">${totalCapAll}</strong></span>
                        </div>
                    </div>

                    <!-- CARD 3: PELENGKAP NON-OEE (BOLT, BUSH, NUT, PIN) -->
                    <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl flex flex-col justify-between print-card">
                        <div class="flex items-center justify-between mb-1">
                            <span class="text-[11px] font-bold uppercase tracking-wider text-slate-400">Pelengkap Non-OEE</span>
                            <i data-lucide="wrench" class="w-4 h-4 text-amber-400"></i>
                        </div>
                        <div class="my-1.5 flex items-baseline justify-between">
                            <div class="font-mono text-3xl font-black text-amber-400">${totalNonOeeNgAll.toLocaleString()}<span class="text-xs font-semibold text-slate-400 ml-0.5">pcs</span></div>
                            <div class="text-right text-[10px] font-mono text-slate-400">Spare Parts</div>
                        </div>
                        <div class="pt-2 border-t border-slate-800 grid grid-cols-4 gap-1 text-[9px] font-mono text-slate-400 text-center">
                            <span>Bolt: <strong class="text-amber-400">${totalBoltAll}</strong></span>
                            <span>Bush: <strong class="text-amber-400">${totalBushAll}</strong></span>
                            <span>Nut: <strong class="text-amber-400">${totalNutAll}</strong></span>
                            <span>Pin: <strong class="text-amber-400">${totalPinAll}</strong></span>
                        </div>
                    </div>

                    <!-- CARD 4: TOP DEFECT COMPONENT -->
                    <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl flex flex-col justify-between print-card">
                        <div class="flex items-center justify-between mb-1">
                            <span class="text-[11px] font-bold uppercase tracking-wider text-slate-400">Dominant Component</span>
                            <i data-lucide="target" class="w-4 h-4 text-purple-400"></i>
                        </div>
                        <div class="my-1 flex flex-col justify-center">
                            <div class="font-sans text-lg font-bold text-white truncate">
                                ${totalRodAll >= totalAssyAll && totalRodAll >= totalCapAll && totalRodAll > 0 ? 'Connecting Rod' : (totalCapAll >= totalAssyAll && totalCapAll > 0 ? 'Cap Body' : (totalAssyAll > 0 ? 'Assy Rakitan' : 'Zero Defect'))}
                            </div>
                            <div class="text-[10px] font-mono text-purple-400 font-semibold mt-0.5">
                                ${totalOeeNgAll > 0 ? `Kontribusi: ${Math.max(totalAssyAll, totalRodAll, totalCapAll)} pcs (${((Math.max(totalAssyAll, totalRodAll, totalCapAll)/totalOeeNgAll)*100).toFixed(0)}%)` : 'Kualitas Terkendali'}
                            </div>
                        </div>
                        <div class="pt-2 border-t border-slate-800 flex items-center justify-between text-[10px] font-mono text-slate-400">
                            <span>Symptom: <strong class="text-slate-200 truncate max-w-[120px]">${ngItems[0]?.ng_detail?.defect_symptom || 'Dimension Out'}</strong></span>
                        </div>
                    </div>

                    <!-- CARD 5: QUALITY YIELD & PPM -->
                    <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl flex flex-col justify-between print-card">
                        <div class="flex items-center justify-between mb-1">
                            <span class="text-[11px] font-bold uppercase tracking-wider text-slate-400">Quality Yield & PPM</span>
                            <i data-lucide="check-circle" class="w-4 h-4 text-emerald-400"></i>
                        </div>
                        <div class="my-1.5 flex items-baseline justify-between">
                            <div class="font-mono text-3xl font-black text-emerald-400">${overallYieldRate}<span class="text-sm font-semibold text-slate-400 ml-0.5">%</span></div>
                            <div class="text-right text-[10px] font-mono text-slate-400">PPM: <strong class="text-amber-400">${overallDefectPpm}</strong></div>
                        </div>
                        <div class="pt-2 border-t border-slate-800 flex items-center justify-between text-[10px] font-mono">
                            <span class="text-slate-400">Target Yield: <strong>99.00%</strong></span>
                            <span class="px-1.5 py-0.2 rounded text-[9px] font-bold ${Number(overallYieldRate) >= 99.0 ? 'bg-emerald-950 text-emerald-400 border border-emerald-800' : 'bg-amber-950 text-amber-400 border border-amber-800'}">
                                ${Number(overallYieldRate) >= 99.0 ? 'PASS' : 'SCRAP RISK'}
                            </span>
                        </div>
                    </div>
                </div>

                <!-- TABEL REKAPITULASI DATA NG & DEFECT -->
                <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl mb-4 print-card">
                    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between gap-2 border-b border-slate-800 pb-3 mb-3">
                        <div>
                            <h3 class="text-sm font-bold text-slate-100 flex items-center gap-2">
                                <i data-lucide="list" class="w-4 h-4 text-amber-400"></i>
                                Tabel Rekapitulasi Rincian Data Defect & NG
                            </h3>
                            <p class="text-[11px] text-slate-400 mt-0.5">Daftar item NG per lini, mesin, jenis defect wajib OEE & pelengkap non-OEE beserta tindakan CAPA</p>
                        </div>
                        <div class="font-mono text-xs text-slate-400">
                            Total Records: <strong class="text-amber-400">${ngItems.length}</strong> Entri
                        </div>
                    </div>

                    <div class="overflow-x-auto">
                        <table class="w-full text-xs text-left font-sans border-collapse print-table">
                            <thead class="bg-slate-950/80 text-slate-400 uppercase text-[10px] font-mono tracking-wider border-b border-slate-800">
                                <tr>
                                    <th class="py-2.5 px-3 text-center w-10">No</th>
                                    <th class="py-2.5 px-3">Tanggal & Shift</th>
                                    <th class="py-2.5 px-3">Line & Mesin</th>
                                    <th class="py-2.5 px-3">Type Produk (SKU)</th>
                                    <th class="py-2.5 px-2 text-right">Output</th>
                                    <th class="py-2.5 px-2 text-right font-bold text-emerald-400">OK (Pcs)</th>
                                    <th class="py-2.5 px-2 text-right font-bold text-rose-400">Total NG</th>
                                    <th class="py-2.5 px-2 text-right">Defect %</th>
                                    <th class="py-2.5 px-3 text-center text-cyan-400">Wajib OEE (Assy/Rod/Cap)</th>
                                    <th class="py-2.5 px-3 text-center text-amber-400">Pelengkap (Bolt/Bush/Nut/Pin)</th>
                                    <th class="py-2.5 px-3">Kategori & Masalah Defect</th>
                                    <th class="py-2.5 px-3">Tindakan Perbaikan (CAPA)</th>
                                    <th class="py-2.5 px-2 text-center">Status / PIC</th>
                                </tr>
                            </thead>
                            <tbody class="divide-y divide-slate-800/60 font-mono text-[11px]">
                                ${ngItems.length === 0 ? `
                                    <tr>
                                        <td colspan="13" class="py-8 text-center text-slate-500 font-sans">
                                            <div class="flex flex-col items-center gap-2">
                                                <i data-lucide="check-circle" class="w-8 h-8 text-emerald-400/40"></i>
                                                <span>Tidak ada data defect/NG yang ditemukan sesuai kriteria filter tanggal, line, type, atau shift yang dipilih.</span>
                                            </div>
                                        </td>
                                    </tr>
                                ` : ngDateGroups.map((group) => {
                                    const groupTotalOutput = group.items.reduce((s, it) => s + (it.total_output || 0), 0);
                                    const groupTotalGood = group.items.reduce((s, it) => s + (it.good_quantity || 0), 0);
                                    const groupTotalNg = group.items.reduce((s, it) => s + (it.total_ng_target || 0), 0);
                                    const groupDefectRate = groupTotalOutput > 0 ? ((groupTotalNg / groupTotalOutput) * 100).toFixed(2) : '0.00';

                                    return `
                                        <!-- DATE GROUP SEPARATOR -->
                                        <tr class="bg-slate-950/90 border-y-2 border-amber-700/60 font-mono">
                                            <td colspan="13" class="py-2.5 px-3">
                                                <div class="flex flex-wrap items-center justify-between gap-2">
                                                    <div class="flex items-center gap-2">
                                                        <span class="px-2 py-0.5 rounded bg-amber-950 text-amber-300 border border-amber-800 text-[10px] font-bold uppercase font-mono flex items-center gap-1.5">
                                                            <i data-lucide="calendar" class="w-3.5 h-3.5 text-amber-400"></i>
                                                            TANGGAL AUDIT: ${group.formatted_date}
                                                        </span>
                                                        <span class="text-[11px] text-slate-300 font-sans font-bold">
                                                            Rekap Data Defect (${group.items.length} Entri Produksi)
                                                        </span>
                                                    </div>
                                                    <div class="flex items-center gap-3 text-[11px] text-slate-400 font-mono">
                                                        <span>Output: <strong class="text-white">${groupTotalOutput.toLocaleString()} pcs</strong></span>
                                                        <span>OK: <strong class="text-emerald-400">${groupTotalGood.toLocaleString()} pcs</strong></span>
                                                        <span>Total NG: <strong class="text-rose-400">${groupTotalNg.toLocaleString()} pcs</strong></span>
                                                        <span>Rate: <strong class="${Number(groupDefectRate) <= 1.0 ? 'text-emerald-400' : 'text-rose-400'}">${groupDefectRate}%</strong></span>
                                                    </div>
                                                </div>
                                            </td>
                                        </tr>
                                        ${group.items.map((it, idx) => {
                                            const dRate = it.total_output > 0 ? ((it.total_ng_target / it.total_output) * 100).toFixed(2) : '0.00';
                                            const ng = it.ng_detail || {};
                                            return `
                                                <tr class="hover:bg-slate-800/40 transition-colors">
                                                    <td class="py-2.5 px-3 text-center font-bold text-slate-500 font-mono">${idx + 1}</td>
                                                    <td class="py-2.5 px-3">
                                                        <div class="text-slate-200 font-bold">${it.production_date}</div>
                                                        <span class="px-1.5 py-0.2 rounded text-[9px] font-semibold bg-slate-800 text-slate-300 border border-slate-700 font-sans">${it.shift_name}</span>
                                                    </td>
                                                    <td class="py-2.5 px-3 font-sans">
                                                        <div class="font-bold text-cyan-400">${it.line_name}</div>
                                                        <div class="text-[10px] text-slate-400 font-mono">${it.machine_code} - ${it.machine_name}</div>
                                                    </td>
                                                    <td class="py-2.5 px-3 font-sans">
                                                        <div class="font-bold text-slate-100">${it.product_name}</div>
                                                        <div class="text-[10px] text-amber-400 font-mono">${it.product_sku}</div>
                                                    </td>
                                                    <td class="py-2.5 px-2 text-right text-slate-300 font-bold">${(it.total_output || 0).toLocaleString()}</td>
                                                    <td class="py-2.5 px-2 text-right text-emerald-400 font-bold">${(it.good_quantity || 0).toLocaleString()}</td>
                                                    <td class="py-2.5 px-2 text-right text-rose-400 font-black">${(it.total_ng_target || 0).toLocaleString()}</td>
                                                    <td class="py-2.5 px-2 text-right font-bold ${Number(dRate) > 1.0 ? 'text-rose-400' : 'text-slate-300'}">${dRate}%</td>
                                                    <td class="py-2.5 px-3 text-center">
                                                        <span class="px-2 py-0.5 rounded bg-cyan-950/80 text-cyan-300 border border-cyan-800 font-bold text-[10px]">
                                                            A: ${ng.ng_assy || 0} | R: ${ng.ng_rod || 0} | C: ${ng.ng_cap || 0}
                                                        </span>
                                                    </td>
                                                    <td class="py-2.5 px-3 text-center">
                                                        <span class="px-2 py-0.5 rounded bg-amber-950/80 text-amber-300 border border-amber-800 font-bold text-[10px]">
                                                            B: ${ng.ng_bolt || 0} | Bs: ${ng.ng_bush || 0} | N: ${ng.ng_nut || 0} | P: ${ng.ng_pin || 0}
                                                        </span>
                                                    </td>
                                                    <td class="py-2.5 px-3 font-sans">
                                                        <div class="font-bold text-slate-200">${ng.defect_category_name || (ng.defect_symptom ? ng.defect_symptom : 'Dimensional Defect')}</div>
                                                        <div class="text-[10px] text-slate-400">${ng.defect_reason_name || ng.notes || 'Penanganan proses standar'}</div>
                                                    </td>
                                                    <td class="py-2.5 px-3 font-sans">
                                                        <div class="text-emerald-400 font-medium text-[11px]">${ng.action_taken || 'Tool adjustment & monitoring'}</div>
                                                    </td>
                                                    <td class="py-2.5 px-2 text-center font-sans">
                                                        <span class="px-2 py-0.5 rounded text-[9px] font-bold ${it.has_ng_detail ? 'bg-emerald-950 text-emerald-400 border border-emerald-800' : 'bg-amber-950 text-amber-400 border border-amber-800'}">
                                                            ${it.has_ng_detail ? 'VERIFIED' : 'PENDING'}
                                                        </span>
                                                        <div class="text-[9px] text-slate-400 font-mono mt-0.5">${ng.inspector_name || it.operator_name || 'QC'}</div>
                                                    </td>
                                                </tr>
                                            `;
                                        }).join('')}
                                    `;
                                }).join('')}
                            </tbody>
                            <tfoot class="bg-slate-950/90 font-mono font-bold border-t-2 border-slate-700 text-slate-200 text-xs">
                                <tr>
                                    <td colspan="4" class="py-3 px-3 uppercase tracking-wider font-sans font-black text-amber-400">TOTAL KESELURUHAN DATA NG:</td>
                                    <td class="py-3 px-2 text-right text-slate-300">${totalOutputAll.toLocaleString()}</td>
                                    <td class="py-3 px-2 text-right text-emerald-400">${totalGoodAll.toLocaleString()}</td>
                                    <td class="py-3 px-2 text-right text-rose-400">${totalNgPcsAll.toLocaleString()}</td>
                                    <td class="py-3 px-2 text-right text-amber-400">${overallDefectRate}%</td>
                                    <td class="py-3 px-3 text-center text-cyan-400 font-bold">Assy: ${totalAssyAll} | Rod: ${totalRodAll} | Cap: ${totalCapAll}</td>
                                    <td class="py-3 px-3 text-center text-amber-400 font-bold">Bolt: ${totalBoltAll} | Bush: ${totalBushAll} | Nut: ${totalNutAll} | Pin: ${totalPinAll}</td>
                                    <td colspan="3" class="py-3 px-3 text-center text-slate-400 font-sans text-[11px]">
                                        Total Wajib OEE: <strong class="text-cyan-400">${totalOeeNgAll} pcs</strong> • Total Non-OEE: <strong class="text-amber-400">${totalNonOeeNgAll} pcs</strong>
                                    </td>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                </div>

                <!-- KOMPONEN DEFECT BREAKDOWN & CAPA LOG -->
                <div class="grid grid-cols-1 lg:grid-cols-2 gap-4 mb-4">
                    <!-- KOMPONEN BREAKDOWN BAR -->
                    <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl flex flex-col justify-between print-card">
                        <div class="flex items-center justify-between border-b border-slate-800 pb-3 mb-3">
                            <h3 class="text-sm font-bold text-slate-100 flex items-center gap-2">
                                <i data-lucide="bar-chart-2" class="w-4 h-4 text-amber-400"></i>
                                Distribusi Defect per Bagian Komponen
                            </h3>
                            <span class="text-[10px] font-mono text-slate-400">Proporsi (%)</span>
                        </div>
                        <div class="space-y-3">
                            <div>
                                <div class="flex justify-between text-xs font-mono mb-1">
                                    <span class="text-slate-300 font-sans">Connecting Rod Body (Wajib OEE)</span>
                                    <span class="text-cyan-400 font-bold">${totalRodAll} pcs (${totalNgPcsAll > 0 ? ((totalRodAll/totalNgPcsAll)*100).toFixed(1) : 0}%)</span>
                                </div>
                                <div class="w-full bg-slate-950 rounded-full h-2 overflow-hidden border border-slate-800">
                                    <div class="bg-gradient-to-r from-cyan-600 to-blue-500 h-full rounded-full transition-all" style="width: ${totalNgPcsAll > 0 ? ((totalRodAll/totalNgPcsAll)*100) : 0}%"></div>
                                </div>
                            </div>

                            <div>
                                <div class="flex justify-between text-xs font-mono mb-1">
                                    <span class="text-slate-300 font-sans">Cap Body (Wajib OEE)</span>
                                    <span class="text-cyan-400 font-bold">${totalCapAll} pcs (${totalNgPcsAll > 0 ? ((totalCapAll/totalNgPcsAll)*100).toFixed(1) : 0}%)</span>
                                </div>
                                <div class="w-full bg-slate-950 rounded-full h-2 overflow-hidden border border-slate-800">
                                    <div class="bg-gradient-to-r from-cyan-600 to-blue-500 h-full rounded-full transition-all" style="width: ${totalNgPcsAll > 0 ? ((totalCapAll/totalNgPcsAll)*100) : 0}%"></div>
                                </div>
                            </div>

                            <div>
                                <div class="flex justify-between text-xs font-mono mb-1">
                                    <span class="text-slate-300 font-sans">Assembly Fitment / Rakitan (Wajib OEE)</span>
                                    <span class="text-cyan-400 font-bold">${totalAssyAll} pcs (${totalNgPcsAll > 0 ? ((totalAssyAll/totalNgPcsAll)*100).toFixed(1) : 0}%)</span>
                                </div>
                                <div class="w-full bg-slate-950 rounded-full h-2 overflow-hidden border border-slate-800">
                                    <div class="bg-gradient-to-r from-cyan-600 to-blue-500 h-full rounded-full transition-all" style="width: ${totalNgPcsAll > 0 ? ((totalAssyAll/totalNgPcsAll)*100) : 0}%"></div>
                                </div>
                            </div>

                            <div>
                                <div class="flex justify-between text-xs font-mono mb-1">
                                    <span class="text-slate-300 font-sans">Komponen Pelengkap (Bolt, Bush, Nut, Pin)</span>
                                    <span class="text-amber-400 font-bold">${totalNonOeeNgAll} pcs (${totalNgPcsAll > 0 ? ((totalNonOeeNgAll/totalNgPcsAll)*100).toFixed(1) : 0}%)</span>
                                </div>
                                <div class="w-full bg-slate-950 rounded-full h-2 overflow-hidden border border-slate-800">
                                    <div class="bg-gradient-to-r from-amber-600 to-orange-500 h-full rounded-full transition-all" style="width: ${totalNgPcsAll > 0 ? ((totalNonOeeNgAll/totalNgPcsAll)*100) : 0}%"></div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- RINGKASAN TINDAKAN PERBAIKAN CAPA -->
                    <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl flex flex-col justify-between print-card">
                        <div class="flex items-center justify-between border-b border-slate-800 pb-3 mb-3">
                            <h3 class="text-sm font-bold text-slate-100 flex items-center gap-2">
                                <i data-lucide="check-square" class="w-4 h-4 text-emerald-400"></i>
                                Log Tindakan Korektif & Rekomendasi Kaizen (CAPA)
                            </h3>
                            <span class="text-[10px] font-mono text-slate-400">Quality Action Items</span>
                        </div>
                        <div class="space-y-2 max-h-[220px] overflow-y-auto pr-1">
                            ${ngItems.filter(it => it.ng_detail && it.ng_detail.action_taken).length === 0 ? `
                                <div class="p-6 text-center text-slate-500 font-sans text-xs">
                                    <i data-lucide="shield-check" class="w-6 h-6 mx-auto mb-1 text-emerald-400 opacity-60"></i>
                                    <span>Semua parameter proses mutu berada dalam batas toleransi standar (In-Control).</span>
                                </div>
                            ` : ngItems.filter(it => it.ng_detail && it.ng_detail.action_taken).map(it => `
                                <div class="bg-slate-950 border border-slate-800 rounded-lg p-3 text-xs">
                                    <div class="flex items-center justify-between font-mono mb-1">
                                        <span class="text-cyan-400 font-bold">${it.line_name} • ${it.product_name} (${it.product_sku})</span>
                                        <span class="text-rose-400 font-bold">${it.total_ng_target} NG</span>
                                    </div>
                                    <div class="text-slate-300 mt-1">
                                        <span class="text-slate-400">Penyebab:</span> <strong>${it.ng_detail.defect_symptom || it.ng_detail.defect_reason_name || 'Gejala Defect'}</strong>
                                    </div>
                                    <div class="text-emerald-400 mt-1 font-medium">
                                        <span class="text-slate-400">Tindakan CAPA:</span> ${it.ng_detail.action_taken}
                                    </div>
                                </div>
                            `).join('')}
                        </div>
                    </div>
                </div>

                <!-- OFFICIAL QC SIGNATURE SECTION (3 APPROVAL ROLES) -->
                <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl mb-4 print-card">
                    <div class="pb-3 mb-3 border-b border-slate-800 flex items-center justify-between">
                        <h3 class="text-sm font-bold text-slate-100 flex items-center gap-2">
                            <i data-lucide="award" class="w-4 h-4 text-purple-400"></i>
                            Pengesahan Laporan Data NG & Kontrol Kualitas (QC Inspection Approval)
                        </h3>
                        <span class="text-[10px] font-mono text-slate-400">Standard QC ISO/IATF Form</span>
                    </div>

                    <div class="pt-2">
                        <div class="text-right text-xs text-slate-400 font-mono mb-3">
                            Tanggal Audit: <strong class="text-slate-200">${header.formatted_date || ''}</strong>
                        </div>
                        <div class="grid grid-cols-1 sm:grid-cols-3 gap-6 text-center font-sans text-xs">
                            <div>
                                <div class="text-slate-400 font-medium mb-12">Dibuat Oleh (Operator Produksi):</div>
                                <div class="font-bold text-slate-200 border-b border-slate-700 pb-1 w-48 mx-auto">${ngItems[0]?.operator_name || 'Operator Mesin'}</div>
                                <div class="text-[10px] text-slate-400 font-mono mt-1">Operator Produksi</div>
                            </div>
                            <div>
                                <div class="text-slate-400 font-medium mb-12">Diperiksa Oleh (QC Inspector / Leader):</div>
                                <div class="font-bold text-slate-200 border-b border-slate-700 pb-1 w-48 mx-auto">${ngItems[0]?.ng_detail?.inspector_name || 'Leader QC'}</div>
                                <div class="text-[10px] text-slate-400 font-mono mt-1">Quality Control Inspector</div>
                            </div>
                            <div>
                                <div class="text-slate-400 font-medium mb-12">Disetujui Oleh (Supervisor Produksi / QC):</div>
                                <div class="font-bold text-slate-200 border-b border-slate-700 pb-1 w-48 mx-auto">${header.supervisor_in_charge || 'Alim Utama'}</div>
                                <div class="text-[10px] text-slate-400 font-mono mt-1">Supervisor Produksi & QC</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- ========================================== -->
            <!-- TAB 3 CONTENT: LAPORAN TROUBLE & DOWNTIME  -->
            <!-- ========================================== -->
            <div id="report-pane-trouble" class="${this.activeReportTab === 'trouble' ? '' : 'hidden'}">
                <!-- METADATA HEADER BANNER FOR TROUBLE REPORT -->
                <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl mb-4 print-card">
                    <div class="flex flex-wrap items-center justify-between gap-4 border-b border-slate-800 pb-3 mb-3">
                        <div class="flex items-center gap-3 min-w-0">
                            <div class="w-12 h-12 rounded-lg bg-slate-950 border border-slate-800 flex items-center justify-center flex-shrink-0 text-rose-400 p-1">
                                <img src="${this.escapeHtml(this.getCompanyLogo(troubleHeader))}" class="w-full h-full object-contain" alt="Logo" onerror="this.onerror=null; this.src='/images/yasunaga-logo.png';" />
                            </div>
                            <div class="min-w-0">
                                <div class="text-xs font-bold text-rose-400 uppercase tracking-wider">${troubleHeader.company_name || this.companyProfile?.company_name || 'PT. YASUNAGA INDONESIA'}</div>
                                <div class="text-sm font-black tracking-wide text-white uppercase truncate">LAPORAN RIWAYAT TROUBLE & DOWNTIME MESIN PRODUKSI</div>
                                <div class="text-[10px] text-slate-400 font-medium truncate">${troubleHeader.address || this.companyProfile?.address || troubleHeader.plant_location || ''} • <strong class="text-slate-200 font-mono">${troubleHeader.plant_id || this.companyProfile?.plant_code || 'PLT-01'}</strong></div>
                            </div>
                        </div>

                        <div class="flex flex-wrap items-center gap-4 text-xs font-mono">
                            <div class="bg-slate-950 border border-slate-800 px-3 py-1 rounded-lg">
                                <span class="text-slate-400 font-sans">Shift:</span> 
                                <strong class="text-cyan-400">${troubleHeader.shift_name}</strong>
                            </div>
                            <div class="bg-slate-950 border border-slate-800 px-3 py-1 rounded-lg">
                                <span class="text-slate-400 font-sans">Tanggal:</span> 
                                <strong class="text-amber-400">${troubleHeader.formatted_date}</strong>
                            </div>
                            <div class="bg-slate-950 border border-slate-800 px-3 py-1 rounded-lg">
                                <span class="text-slate-400 font-sans">Line:</span> 
                                <strong class="text-emerald-400">${troubleHeader.line_name}</strong>
                            </div>
                            <span class="px-2.5 py-1 rounded-lg bg-rose-950/80 text-rose-400 border border-rose-800 font-bold text-[11px] flex items-center gap-1.5">
                                <i data-lucide="wrench" class="w-3.5 h-3.5"></i> MAINTENANCE & RELIABILITY AUDIT
                            </span>
                        </div>
                    </div>

                    <div class="grid grid-cols-2 sm:grid-cols-4 gap-3 text-xs font-mono">
                        <div>
                            <span class="text-slate-400 text-[10px] block uppercase">Target Unplanned Breakdown:</span>
                            <span class="font-bold text-cyan-400">&lt; 30 Menit / Shift</span>
                        </div>
                        <div>
                            <span class="text-slate-400 text-[10px] block uppercase">Total Waktu Henti (Downtime):</span>
                            <span class="font-bold ${(troubleSummary.total_downtime_minutes || 0) <= 60 ? 'text-emerald-400' : 'text-rose-400'}">${troubleSummary.total_downtime_minutes || 0} Menit (${troubleSummary.total_downtime_hours || 0} Jam)</span>
                        </div>
                        <div>
                            <span class="text-slate-400 text-[10px] block uppercase">Total Kejadian Trouble:</span>
                            <span class="font-bold text-amber-400">${totalTroubleReports} Kali Kejadian</span>
                        </div>
                        <div>
                            <span class="text-slate-400 text-[10px] block uppercase">Rata-rata Penanganan (MTTR):</span>
                            <span class="font-bold text-purple-400">${troubleSummary.mttr_minutes || 0} Menit / Trouble</span>
                        </div>
                    </div>
                </div>

                <!-- 5 MAINTENANCE MACRO KPI CARDS -->
                <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-5 gap-3 mb-4">
                    <!-- CARD 1: TOTAL TROUBLE CALLS -->
                    <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl flex flex-col justify-between print-card">
                        <div class="flex items-center justify-between mb-1">
                            <span class="text-[11px] font-bold uppercase tracking-wider text-slate-400">Total Kejadian Trouble</span>
                            <span class="w-2 h-2 rounded-full ${totalTroubleReports === 0 ? 'bg-emerald-400 shadow-[0_0_8px_#10b981]' : (totalTroubleReports <= 3 ? 'bg-amber-400' : 'bg-rose-500')}"></span>
                        </div>
                        <div class="my-1.5 flex items-baseline justify-between">
                            <div class="font-mono text-3xl font-black text-rose-400">${totalTroubleReports}<span class="text-xs font-semibold text-slate-400 ml-0.5">calls</span></div>
                            <div class="text-right text-[10px] font-mono text-slate-400">Unplanned: <strong class="text-rose-400">${troubleSummary.unplanned_count || 0}</strong></div>
                        </div>
                        <div class="pt-2 border-t border-slate-800 flex items-center justify-between text-[10px] font-mono">
                            <span class="text-slate-400">Planned Maint: <strong class="text-slate-200">${troubleSummary.planned_count || 0}</strong></span>
                            <span class="${totalTroubleReports === 0 ? 'text-emerald-400' : 'text-rose-400'} font-bold">${totalTroubleReports === 0 ? 'ZERO TROUBLE' : 'NEED MONITOR'}</span>
                        </div>
                    </div>

                    <!-- CARD 2: TOTAL DOWNTIME DURATION -->
                    <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl flex flex-col justify-between print-card">
                        <div class="flex items-center justify-between mb-1">
                            <span class="text-[11px] font-bold uppercase tracking-wider text-slate-400">Total Durasi Waktu Henti</span>
                            <i data-lucide="clock" class="w-4 h-4 text-amber-400"></i>
                        </div>
                        <div class="my-1.5 flex items-baseline justify-between">
                            <div class="font-mono text-3xl font-black text-amber-400">${troubleSummary.total_downtime_minutes || 0}<span class="text-xs font-semibold text-slate-400 ml-0.5">menit</span></div>
                            <div class="text-right text-[10px] font-mono text-slate-400">${troubleSummary.total_downtime_hours || 0} Jam</div>
                        </div>
                        <div class="pt-2 border-t border-slate-800 flex items-center justify-between text-[10px] font-mono text-slate-400">
                            <span>Unplanned: <strong class="text-rose-400">${troubleSummary.unplanned_minutes || 0}m</strong></span>
                            <span>Planned: <strong class="text-cyan-400">${troubleSummary.planned_minutes || 0}m</strong></span>
                        </div>
                    </div>

                    <!-- CARD 3: MEAN TIME TO REPAIR (MTTR) -->
                    <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl flex flex-col justify-between print-card">
                        <div class="flex items-center justify-between mb-1">
                            <span class="text-[11px] font-bold uppercase tracking-wider text-slate-400">Rata-Rata Penanganan (MTTR)</span>
                            <i data-lucide="timer" class="w-4 h-4 text-cyan-400"></i>
                        </div>
                        <div class="my-1.5 flex items-baseline justify-between">
                            <div class="font-mono text-3xl font-black text-cyan-400">${troubleSummary.mttr_minutes || 0}<span class="text-xs font-semibold text-slate-400 ml-0.5">m/trb</span></div>
                            <div class="text-right text-[10px] font-mono text-slate-400">Standard: &lt; 20m</div>
                        </div>
                        <div class="pt-2 border-t border-slate-800 flex items-center justify-between text-[10px] font-mono text-slate-400">
                            <span>Durasi Max: <strong class="text-rose-400">${troubleSummary.max_downtime_minutes || 0} menit</strong></span>
                            <span class="${(troubleSummary.mttr_minutes || 0) <= 20 ? 'text-emerald-400' : 'text-amber-400'} font-bold">${(troubleSummary.mttr_minutes || 0) <= 20 ? 'GOOD SPEED' : 'LONG REPAIR'}</span>
                        </div>
                    </div>

                    <!-- CARD 4: TOP BOTTLENECK MACHINE -->
                    <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl flex flex-col justify-between print-card">
                        <div class="flex items-center justify-between mb-1">
                            <span class="text-[11px] font-bold uppercase tracking-wider text-slate-400">Mesin Paling Terkendala</span>
                            <i data-lucide="alert-octagon" class="w-4 h-4 text-purple-400"></i>
                        </div>
                        <div class="my-1.5">
                            <div class="text-base font-black text-purple-400 truncate">${troubleSummary.top_machine ? troubleSummary.top_machine.machine_code : 'Semua Mesin Aman'}</div>
                            <div class="text-[10px] text-slate-400 truncate">${troubleSummary.top_machine ? troubleSummary.top_machine.machine_name : 'Zero Downtime Tercapai'}</div>
                        </div>
                        <div class="pt-2 border-t border-slate-800 flex items-center justify-between text-[10px] font-mono text-slate-400">
                            <span>Durasi: <strong class="text-rose-400">${troubleSummary.top_machine ? troubleSummary.top_machine.minutes + 'm (' + troubleSummary.top_machine.count + 'x)' : '0m'}</strong></span>
                            <span class="text-slate-300 truncate max-w-[90px]">${troubleSummary.top_machine ? troubleSummary.top_machine.line_name : 'Line Normal'}</span>
                        </div>
                    </div>

                    <!-- CARD 5: DOMINANT PROBLEM CATEGORY -->
                    <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl flex flex-col justify-between print-card">
                        <div class="flex items-center justify-between mb-1">
                            <span class="text-[11px] font-bold uppercase tracking-wider text-slate-400">Kategori Masalah Dominan</span>
                            <i data-lucide="pie-chart" class="w-4 h-4 text-emerald-400"></i>
                        </div>
                        <div class="my-1.5">
                            <div class="text-base font-black text-emerald-400 truncate">${troubleSummary.dominant_category ? troubleSummary.dominant_category.category : 'Zero Incident'}</div>
                            <div class="text-[10px] text-slate-400 truncate">${troubleSummary.dominant_category ? troubleSummary.dominant_category.percentage + '% Total Waktu Henti' : 'Operasional 100% Lancar'}</div>
                        </div>
                        <div class="pt-2 border-t border-slate-800 flex items-center justify-between text-[10px] font-mono text-slate-400">
                            <span>Total: <strong class="text-emerald-400">${troubleSummary.dominant_category ? troubleSummary.dominant_category.minutes + ' Menit' : '0m'}</strong></span>
                            <span class="text-emerald-400 font-bold">${troubleSummary.dominant_category ? troubleSummary.dominant_category.count + ' Kejadian' : 'OPTIMAL'}</span>
                        </div>
                    </div>
                </div>

                <!-- TABEL REKAPITULASI RINCIAN TROUBLE & DOWNTIME -->
                <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl mb-4 print-card">
                    <div class="flex flex-wrap items-center justify-between gap-2 pb-3 mb-3 border-b border-slate-800">
                        <div>
                            <h3 class="text-sm font-bold text-slate-100 flex items-center gap-2">
                                <i data-lucide="list-collapse" class="w-4 h-4 text-rose-400"></i>
                                Tabel Rekapitulasi Rincian Data Trouble & Downtime Mesin
                            </h3>
                            <p class="text-[11px] text-slate-400">Daftar seluruh kendala teknis, waktu henti, analisa root cause, dan tindakan perbaikan (CAPA) tersinkron otomatis dari data harian produksi.</p>
                        </div>
                        <div class="text-right">
                            <span class="text-xs font-mono font-bold text-slate-300">Total Records: <strong class="text-rose-400">${totalTroubleReports} Entri</strong></span>
                        </div>
                    </div>

                    <div class="overflow-x-auto">
                        <table class="w-full text-left text-xs border-collapse font-sans">
                            <thead>
                                <tr class="border-b border-slate-800 bg-slate-950/60 text-slate-400 text-[10px] uppercase font-bold tracking-wider">
                                    <th class="py-2.5 px-3 text-center w-10">No</th>
                                    <th class="py-2.5 px-3">Tanggal & Waktu</th>
                                    <th class="py-2.5 px-3">Line & Mesin</th>
                                    <th class="py-2.5 px-3">Type Produk (SKU)</th>
                                    <th class="py-2.5 px-3">Shift</th>
                                    <th class="py-2.5 px-3">Kategori Masalah</th>
                                    <th class="py-2.5 px-3">Gejala & Root Cause Trouble</th>
                                    <th class="py-2.5 px-3 text-right">Durasi</th>
                                    <th class="py-2.5 px-3">Tindakan Perbaikan (CAPA)</th>
                                    <th class="py-2.5 px-3">PIC / Tim</th>
                                    <th class="py-2.5 px-3 text-center">Status</th>
                                </tr>
                            </thead>
                            <tbody class="divide-y divide-slate-800/60 font-sans text-xs">
                                ${troubleItems.length === 0 ? `
                                    <tr>
                                        <td colspan="11" class="py-12 text-center text-slate-400">
                                            <div class="flex flex-col items-center justify-center gap-2">
                                                <i data-lucide="check-circle-2" class="w-8 h-8 text-emerald-400"></i>
                                                <div class="text-sm font-semibold text-slate-300">Zero Breakdown! Tidak Ada Riwayat Trouble Mesin</div>
                                                <div class="text-[11px] text-slate-500">Semua mesin beroperasi lancar tanpa catatan downtime pada filter periode tanggal, line, atau shift yang dipilih.</div>
                                            </div>
                                        </td>
                                    </tr>
                                ` : troubleDateGroups.map((group) => {
                                    const groupMins = group.items.reduce((sum, it) => sum + (it.duration_minutes || 0), 0);
                                    return `
                                        <!-- GROUP HEADER BY DATE -->
                                        <tr class="bg-rose-950/30 border-y border-rose-900/40">
                                            <td colspan="11" class="py-2 px-3 text-xs font-bold text-rose-300">
                                                <div class="flex flex-wrap items-center justify-between gap-2">
                                                    <div class="flex items-center gap-2">
                                                        <i data-lucide="calendar" class="w-3.5 h-3.5 text-rose-400"></i>
                                                        <span>Tanggal Produksi: ${group.formatted_date}</span>
                                                        <span class="px-2 py-0.5 rounded-full text-[10px] bg-rose-900/60 text-rose-200 font-mono">${group.items.length} Kejadian Trouble</span>
                                                    </div>
                                                    <div class="font-mono text-[11px] text-slate-300">
                                                        Subtotal Waktu Henti: <strong class="text-amber-400">${groupMins} Menit</strong> (${(groupMins/60).toFixed(1)} Jam)
                                                    </div>
                                                </div>
                                            </td>
                                        </tr>
                                        ${group.items.map((it, idx) => {
                                            const catBadgeClass = 
                                                it.problem_type === 'Problem Mesin Mekanik' ? 'bg-rose-950 text-rose-300 border-rose-800' :
                                                it.problem_type === 'Problem Mesin Elektrik' ? 'bg-amber-950 text-amber-300 border-amber-800' :
                                                it.problem_type === 'Problem Tool' ? 'bg-orange-950 text-orange-300 border-orange-800' :
                                                it.problem_type === 'Planning Downtime' ? 'bg-blue-950 text-blue-300 border-blue-800' :
                                                it.problem_type === 'Inventory' ? 'bg-purple-950 text-purple-300 border-purple-800' :
                                                'bg-slate-800 text-slate-300 border border-slate-700';

                                            const isClosed = String(it.status || '').toUpperCase() === 'CLOSED' || String(it.status || '').toUpperCase() === 'RESOLVED';
                                            return `
                                                <tr class="hover:bg-slate-800/40 transition-colors">
                                                    <td class="py-2.5 px-3 text-center font-mono text-[11px] text-slate-400">${idx + 1}</td>
                                                    <td class="py-2.5 px-3 font-mono text-[11px] whitespace-nowrap">
                                                        <div class="font-bold text-slate-200">${it.formatted_date}</div>
                                                        <div class="text-slate-400 text-[10px] flex items-center gap-1"><i data-lucide="clock" class="w-2.5 h-2.5"></i> ${it.time_window}</div>
                                                    </td>
                                                    <td class="py-2.5 px-3">
                                                        <div class="font-bold text-white">${it.machine_code}</div>
                                                        <div class="text-[10px] text-slate-400 truncate max-w-[130px]">${it.line_name} • ${it.machine_name}</div>
                                                    </td>
                                                    <td class="py-2.5 px-3">
                                                        <div class="font-medium text-slate-200 text-xs truncate max-w-[140px]" title="${this.escapeHtml(it.product_name)}">${this.escapeHtml(it.product_name)}</div>
                                                    </td>
                                                    <td class="py-2.5 px-3 whitespace-nowrap">
                                                        <span class="px-2 py-0.5 rounded text-[10px] font-bold bg-slate-800 text-slate-300 font-mono">${it.shift_name}</span>
                                                        ${it.team ? `<div class="text-[10px] text-amber-400 font-bold mt-1">👥 ${it.team}</div>` : ''}
                                                    </td>
                                                    <td class="py-2.5 px-3 whitespace-nowrap">
                                                        <span class="px-2 py-0.5 rounded text-[10px] font-bold border ${catBadgeClass}">
                                                            ${it.problem_type || 'Problem Mesin Mekanik'}
                                                        </span>
                                                    </td>
                                                    <td class="py-2.5 px-3 max-w-[240px]">
                                                        <div class="text-rose-300 font-bold text-xs leading-snug">${this.escapeHtml(it.description || it.root_cause || '-')}</div>
                                                        ${it.cause && it.cause !== '-' ? `<div class="text-[11px] text-amber-300 font-medium mt-1 flex items-start gap-1"><span class="text-slate-400 text-[9px] uppercase font-mono px-1 py-0.2 rounded bg-slate-800 border border-slate-700">Penyebab</span> <span>${this.escapeHtml(it.cause)}</span></div>` : ''}
                                                    </td>
                                                    <td class="py-2.5 px-3 text-right font-mono whitespace-nowrap">
                                                        <div class="font-bold text-amber-400 text-sm">${it.duration_minutes}<span class="text-[10px] font-normal text-slate-400 ml-0.5">m</span></div>
                                                        <div class="text-[10px] text-slate-400">${it.duration_hours} jam</div>
                                                    </td>
                                                    <td class="py-2.5 px-3 max-w-[240px]">
                                                        <div class="text-emerald-300 text-xs leading-snug">${this.escapeHtml(it.action_plan || 'Penyetelan & perbaikan oleh teknisi.')}</div>
                                                    </td>
                                                    <td class="py-2.5 px-3 whitespace-nowrap">
                                                        <div class="text-xs text-cyan-300 font-medium">${this.escapeHtml(it.pic || 'Maintenance')}</div>
                                                    </td>
                                                    <td class="py-2.5 px-3 text-center whitespace-nowrap">
                                                        <span class="px-2 py-0.5 rounded-full text-[10px] font-bold border ${isClosed ? 'bg-emerald-950 text-emerald-400 border-emerald-800' : 'bg-rose-950 text-rose-400 border-rose-800 animate-pulse'}">
                                                            ${isClosed ? '✓ CLOSED' : '⏳ OPEN'}
                                                        </span>
                                                    </td>
                                                </tr>
                                            `;
                                        }).join('')}
                                    `;
                                }).join('')}
                            </tbody>
                            ${troubleItems.length > 0 ? `
                                <tfoot>
                                    <tr class="bg-slate-950/90 border-t-2 border-slate-700 font-mono font-bold text-xs text-slate-200">
                                        <td colspan="7" class="py-3 px-3 uppercase text-slate-300">
                                            TOTAL KESELURUHAN DOWNTIME PRODUKSI:
                                        </td>
                                        <td class="py-3 px-3 text-right text-amber-400 text-sm">
                                            ${troubleSummary.total_downtime_minutes || 0}m
                                            <div class="text-[10px] font-normal text-slate-400">${troubleSummary.total_downtime_hours || 0} jam</div>
                                        </td>
                                        <td colspan="3" class="py-3 px-3 text-[11px] text-slate-400 font-sans">
                                            Unplanned: <strong class="text-rose-400 font-mono">${troubleSummary.unplanned_minutes || 0}m</strong> (${troubleSummary.unplanned_count || 0}x) &nbsp;|&nbsp;
                                            Planned: <strong class="text-cyan-400 font-mono">${troubleSummary.planned_minutes || 0}m</strong> (${troubleSummary.planned_count || 0}x)
                                        </td>
                                    </tr>
                                </tfoot>
                            ` : ''}
                        </table>
                    </div>
                </div>

                <!-- 2-COLUMN ANALYTICAL BREAKDOWN (CATEGORY & MACHINE PARETO) -->
                <div class="grid grid-cols-1 lg:grid-cols-2 gap-4 mb-4">
                    <!-- COLUMN 1: CATEGORY BREAKDOWN -->
                    <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl print-card">
                        <div class="pb-3 mb-3 border-b border-slate-800 flex items-center justify-between">
                            <h3 class="text-sm font-bold text-slate-100 flex items-center gap-2">
                                <i data-lucide="pie-chart" class="w-4 h-4 text-amber-400"></i>
                                Distribusi Trouble per Kategori Masalah
                            </h3>
                            <span class="text-[10px] font-mono text-slate-400">Total: ${troubleSummary.total_downtime_minutes || 0} Menit</span>
                        </div>
                        <div class="space-y-3">
                            ${troubleCategories.length === 0 ? `
                                <div class="py-8 text-center text-slate-500 text-xs">Tidak ada data kategori trouble untuk periode ini.</div>
                            ` : troubleCategories.map((cat) => `
                                <div>
                                    <div class="flex items-center justify-between text-xs mb-1">
                                        <span class="font-bold text-slate-200">${cat.category}</span>
                                        <span class="font-mono text-slate-400"><strong class="text-amber-400">${cat.minutes}m</strong> (${cat.count} kejadian - ${cat.percentage}%)</span>
                                    </div>
                                    <div class="w-full bg-slate-950 rounded-full h-2 overflow-hidden border border-slate-800">
                                        <div class="bg-gradient-to-r from-amber-500 to-rose-500 h-2 rounded-full" style="width: ${Math.min(100, Math.max(5, cat.percentage))}%"></div>
                                    </div>
                                </div>
                            `).join('')}
                        </div>
                    </div>

                    <!-- COLUMN 2: TOP 5 MACHINES PARETO & CAPA EVALUATION -->
                    <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl print-card">
                        <div class="pb-3 mb-3 border-b border-slate-800 flex items-center justify-between">
                            <h3 class="text-sm font-bold text-slate-100 flex items-center gap-2">
                                <i data-lucide="wrench" class="w-4 h-4 text-cyan-400"></i>
                                Top Mesin Terkendala & Rekomendasi CAPA
                            </h3>
                            <span class="text-[10px] font-mono text-slate-400">Pareto Loss Availability</span>
                        </div>
                        <div class="space-y-3">
                            ${troubleTopMachines.length === 0 ? `
                                <div class="py-8 text-center text-slate-500 text-xs">Semua mesin berada dalam performa optimal (Zero Trouble).</div>
                            ` : troubleTopMachines.map((m, mIdx) => `
                                <div class="p-2.5 rounded-lg bg-slate-950/60 border border-slate-800/80">
                                    <div class="flex items-center justify-between text-xs mb-1.5">
                                        <div class="flex items-center gap-2">
                                            <span class="w-5 h-5 rounded bg-slate-800 text-slate-300 font-mono font-bold text-[10px] flex items-center justify-center">${mIdx + 1}</span>
                                            <span class="font-bold text-white">${m.machine_code}</span>
                                            <span class="text-[10px] text-slate-400 truncate max-w-[150px]">${m.line_name}</span>
                                        </div>
                                        <span class="font-mono text-xs font-bold text-rose-400">${m.minutes}m <span class="text-slate-400 font-normal">(${m.count}x)</span></span>
                                    </div>
                                    <div class="w-full bg-slate-900 rounded-full h-1.5 overflow-hidden mb-1.5">
                                        <div class="bg-rose-500 h-1.5 rounded-full" style="width: ${Math.min(100, Math.max(5, m.percentage))}%"></div>
                                    </div>
                                    <div class="text-[10px] text-emerald-400 flex items-center gap-1">
                                        <i data-lucide="check" class="w-2.5 h-2.5"></i>
                                        <span>Rekomendasi: Lakukan inspeksi preventif berkala dan standarisasi SOP penanganan mesin.</span>
                                    </div>
                                </div>
                            `).join('')}
                        </div>
                    </div>
                </div>

                <!-- OFFICIAL MAINTENANCE & RELIABILITY SIGNATURE SECTION (3 APPROVAL ROLES) -->
                <div class="bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl mb-4 print-card">
                    <div class="pb-3 mb-3 border-b border-slate-800 flex items-center justify-between">
                        <h3 class="text-sm font-bold text-slate-100 flex items-center gap-2">
                            <i data-lucide="award" class="w-4 h-4 text-rose-400"></i>
                            Pengesahan Riwayat Trouble & Kontrol Reliabilitas Mesin (Maintenance Approval)
                        </h3>
                        <span class="text-[10px] font-mono text-slate-400">Standard Maintenance TPM & ISO Form</span>
                    </div>

                    <div class="pt-2">
                        <div class="text-right text-xs text-slate-400 font-mono mb-3">
                            Tanggal Cetak / Verifikasi: <strong class="text-slate-200">${troubleHeader.formatted_date || ''}</strong>
                        </div>
                        <div class="grid grid-cols-1 sm:grid-cols-3 gap-6 text-center font-sans text-xs">
                            <div>
                                <div class="text-slate-400 font-medium mb-12">Dibuat Oleh (Operator / Leader Shift):</div>
                                <div class="font-bold text-slate-200 border-b border-slate-700 pb-1 w-48 mx-auto">${troubleItems[0]?.pic || 'Leader Shift Produksi'}</div>
                                <div class="text-[10px] text-slate-400 font-mono mt-1">Leader / Operator Produksi</div>
                            </div>
                            <div>
                                <div class="text-slate-400 font-medium mb-12">Ditangani Oleh (Teknisi / Maintenance):</div>
                                <div class="font-bold text-slate-200 border-b border-slate-700 pb-1 w-48 mx-auto">${troubleItems[0]?.team ? troubleItems[0].team + ' Team' : 'Teknisi Maintenance'}</div>
                                <div class="text-[10px] text-slate-400 font-mono mt-1">Maintenance & Engineering</div>
                            </div>
                            <div>
                                <div class="text-slate-400 font-medium mb-12">Mengetahui / Disetujui (Supervisor Produksi):</div>
                                <div class="font-bold text-slate-200 border-b border-slate-700 pb-1 w-48 mx-auto">${troubleHeader.supervisor_in_charge || 'Alim Utama'}</div>
                                <div class="text-[10px] text-slate-400 font-mono mt-1">Supervisor Produksi & Maintenance</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- SLIDE-IN DRILL-DOWN DRAWER MODAL -->
            <div id="drawer-drilldown-backdrop" class="fixed inset-0 bg-slate-950/70 backdrop-blur-xs z-50 hidden transition-opacity"></div>
            <div id="drawer-drilldown-panel" class="fixed top-0 right-0 bottom-0 w-full max-w-lg bg-slate-900 border-l border-slate-800 shadow-2xl z-50 transform translate-x-full transition-transform duration-300 ease-in-out overflow-y-auto p-5">
                <div id="drawer-drilldown-content"></div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();

        // Render Six Big Losses Pareto
        this.renderSixLossesChart(sixLosses, 'report-chart-six-losses', 'report-six-losses-total-badge');

        // Bind Six Big Losses Card Popup Click
        const reportSixLossesCard = document.getElementById('card-report-six-losses');
        if (reportSixLossesCard) {
            reportSixLossesCard.addEventListener('click', () => {
                this.showSixBigLossesDetailModal(sixLosses, reportKpi);
            });
        }

        // Tab Switching Event Handlers (INSTANT 0ms CLIENT-SIDE TOGGLE)
        const btnProd = document.getElementById('tab-btn-report-prod');
        const btnNg = document.getElementById('tab-btn-report-ng');
        const btnTrouble = document.getElementById('tab-btn-report-trouble');
        const paneProd = document.getElementById('report-pane-production');
        const paneNg = document.getElementById('report-pane-ng');
        const paneTrouble = document.getElementById('report-pane-trouble');
        const actionsProd = document.getElementById('report-actions-prod');
        const actionsNg = document.getElementById('report-actions-ng');
        const actionsTrouble = document.getElementById('report-actions-trouble');

        const setReportTab = (tab) => {
            this.activeReportTab = tab;
            if (btnProd) {
                btnProd.className = tab === 'production' 
                    ? 'px-3.5 py-1.5 rounded-lg text-xs font-bold transition-all flex items-center gap-1.5 cursor-pointer bg-gradient-to-r from-cyan-600 to-blue-600 text-white shadow-md shadow-cyan-600/30'
                    : 'px-3.5 py-1.5 rounded-lg text-xs font-bold transition-all flex items-center gap-1.5 cursor-pointer text-slate-400 hover:text-slate-200 hover:bg-slate-800/60';
            }
            if (btnNg) {
                btnNg.className = tab === 'ng' 
                    ? 'px-3.5 py-1.5 rounded-lg text-xs font-bold transition-all flex items-center gap-1.5 cursor-pointer bg-gradient-to-r from-amber-600 to-rose-600 text-white shadow-md shadow-amber-600/30'
                    : 'px-3.5 py-1.5 rounded-lg text-xs font-bold transition-all flex items-center gap-1.5 cursor-pointer text-slate-400 hover:text-slate-200 hover:bg-slate-800/60';
            }
            if (btnTrouble) {
                btnTrouble.className = tab === 'trouble' 
                    ? 'px-3.5 py-1.5 rounded-lg text-xs font-bold transition-all flex items-center gap-1.5 cursor-pointer bg-gradient-to-r from-rose-600 to-amber-600 text-white shadow-md shadow-rose-600/30'
                    : 'px-3.5 py-1.5 rounded-lg text-xs font-bold transition-all flex items-center gap-1.5 cursor-pointer text-slate-400 hover:text-slate-200 hover:bg-slate-800/60';
            }

            paneProd?.classList.toggle('hidden', tab !== 'production');
            paneNg?.classList.toggle('hidden', tab !== 'ng');
            paneTrouble?.classList.toggle('hidden', tab !== 'trouble');

            if (actionsProd) {
                actionsProd.classList.toggle('hidden', tab !== 'production');
                actionsProd.classList.toggle('flex', tab === 'production');
            }
            if (actionsNg) {
                actionsNg.classList.toggle('hidden', tab !== 'ng');
                actionsNg.classList.toggle('flex', tab === 'ng');
            }
            if (actionsTrouble) {
                actionsTrouble.classList.toggle('hidden', tab !== 'trouble');
                actionsTrouble.classList.toggle('flex', tab === 'trouble');
            }
        };

        btnProd?.addEventListener('click', () => setReportTab('production'));
        btnNg?.addEventListener('click', () => setReportTab('ng'));
        btnTrouble?.addEventListener('click', () => setReportTab('trouble'));

        // Filter Event Listeners
        const reportStartDateInput = document.getElementById('report-filter-start-date');
        const reportEndDateInput = document.getElementById('report-filter-end-date');
        const reportShiftSelect = document.getElementById('report-filter-shift');
        const reportLineSelect = document.getElementById('report-filter-line');
        const reportTypeSelect = document.getElementById('report-filter-type');
        const reportSupervisorInput = document.getElementById('report-filter-supervisor');

        if (reportStartDateInput) {
            reportStartDateInput.addEventListener('change', (e) => {
                this.reportFilters.start_date = e.target.value;
                if (!this.reportFilters.end_date || this.reportFilters.end_date < e.target.value) {
                    this.reportFilters.end_date = e.target.value;
                }
                this.renderReports();
            });
        }
        if (reportEndDateInput) {
            reportEndDateInput.addEventListener('change', (e) => {
                this.reportFilters.end_date = e.target.value;
                if (!this.reportFilters.start_date || this.reportFilters.start_date > e.target.value) {
                    this.reportFilters.start_date = e.target.value;
                }
                this.renderReports();
            });
        }
        if (reportShiftSelect) {
            reportShiftSelect.addEventListener('change', (e) => {
                this.reportFilters.shift_id = e.target.value;
                this.renderReports();
            });
        }
        if (reportLineSelect) {
            reportLineSelect.addEventListener('change', (e) => {
                this.reportFilters.line_id = e.target.value;
                this.renderReports();
            });
        }
        if (reportTypeSelect) {
            reportTypeSelect.addEventListener('change', (e) => {
                this.reportFilters.product_id = e.target.value;
                this.renderReports();
            });
        }

        if (reportSupervisorInput) {
            reportSupervisorInput.addEventListener('change', (e) => {
                this.reportFilters.supervisor = e.target.value.trim();
                this.renderReports();
            });
        }

        const btnQuickEditSup = document.getElementById('btn-quick-edit-supervisor');
        if (btnQuickEditSup) {
            btnQuickEditSup.addEventListener('click', () => {
                const supInput = document.getElementById('report-filter-supervisor');
                if (supInput) {
                    supInput.focus();
                    supInput.select();
                }
            });
        }

        // Bind Print Buttons
        const btnPrint = document.getElementById('btn-print-report');
        if (btnPrint) {
            btnPrint.addEventListener('click', () => {
                this.prepareAndPrintReport(report);
            });
        }

        const btnPrintNg = document.getElementById('btn-print-ng-report');
        if (btnPrintNg) {
            btnPrintNg.addEventListener('click', () => {
                this.prepareAndPrintNgReport(ngData, header, this.reportFilters);
            });
        }

        const btnPrintTrouble = document.getElementById('btn-print-trouble-report');
        if (btnPrintTrouble) {
            btnPrintTrouble.addEventListener('click', () => {
                this.prepareAndPrintTroubleReport(troubleData, troubleHeader, this.reportFilters);
            });
        }

        // Bind Export Buttons
        const btnExport = document.getElementById('btn-export-excel-report');
        if (btnExport) {
            btnExport.addEventListener('click', () => {
                this.exportExcelSpreadsheet(report);
            });
        }

        const btnExportNg = document.getElementById('btn-export-excel-ng');
        if (btnExportNg) {
            btnExportNg.addEventListener('click', () => {
                this.exportExcelNgSpreadsheet(ngData, header, this.reportFilters);
            });
        }

        const btnExportTrouble = document.getElementById('btn-export-excel-trouble');
        if (btnExportTrouble) {
            btnExportTrouble.addEventListener('click', () => {
                this.exportTroubleReportExcel(troubleData, troubleHeader, this.reportFilters);
            });
        }

        // Bind Row Drill-Down Click Listeners (for Production Tab)
        document.querySelectorAll('.row-line-drilldown').forEach(row => {
            row.addEventListener('click', (e) => {
                const idx = e.currentTarget.getAttribute('data-row-index');
                const rowData = rows[idx];
                if (rowData) {
                    this.openLineDrillDownDrawer(rowData, header);
                }
            });
        });

        if (this.currentLang === 'ja') {
            i18n.localizeDom(document.getElementById('content-body') || document.body);
        }
    }

    /**
     * Sanitize filename for safe Windows and browser Save As behavior
     */
    sanitizeReportFilename(filename) {
        return String(filename || '')
            .replace(/[\\/:*?"<>|]/g, '-')
            .replace(/\s+/g, ' ')
            .trim();
    }

    /**
     * Export Full Structured Excel Spreadsheet matching official format exactly
     */
    exportExcelSpreadsheet(report) {
        const header = report.header || {};
        const summary = report.executive_summary || {};
        const rows = report.matrix_rows || [];
        const sixLosses = report.six_big_losses || [];
        const incidentLogs = report.incident_logs || [];
        const handoverNotes = report.handover_notes || [];

        // Group rows by production_date
        const groupedProdRows = rows.reduce((acc, row) => {
            const d = row.production_date || row.formatted_date || 'Unknown';
            if (!acc[d]) acc[d] = { date: d, formatted_date: row.formatted_date || d, items: [] };
            acc[d].items.push(row);
            return acc;
        }, {});

        const excelContent = `
<html xmlns:o="urn:schemas-microsoft-com:office:office" xmlns:x="urn:schemas-microsoft-com:office:excel" xmlns="http://www.w3.org/TR/REC-html40">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <!--[if gte mso 9]>
    <xml>
        <x:ExcelWorkbook>
            <x:ExcelWorksheets>
                <x:ExcelWorksheet>
                    <x:Name>Shift Handover Report</x:Name>
                    <x:WorksheetOptions>
                        <x:DisplayGridlines/>
                        <x:Print>
                            <x:ValidPrinterInfo/>
                            <x:PaperSizeIndex>9</x:PaperSizeIndex>
                            <x:Scale>95</x:Scale>
                        </x:Print>
                    </x:WorksheetOptions>
                </x:ExcelWorksheet>
            </x:ExcelWorksheets>
        </x:ExcelWorkbook>
    </xml>
    <![endif]-->
    <style>
        body { font-family: Arial, Calibri, sans-serif; font-size: 8.5pt; color: #000000; }
        table { border-collapse: collapse; margin-bottom: 10px; width: 100%; }
        .doc-header-1 { font-family: Arial, sans-serif; font-size: 11pt; font-weight: bold; color: #000000; text-align: left; }
        .doc-header-2 { font-family: Arial, sans-serif; font-size: 8.5pt; font-weight: bold; color: #000000; text-align: left; }
        .doc-header-3 { font-family: Arial, sans-serif; font-size: 7.5pt; color: #444444; text-align: left; }
        .doc-title { font-family: Arial, sans-serif; font-size: 10.5pt; font-weight: bold; color: #0070C0; text-transform: uppercase; padding: 4px 0 6px 0; text-align: left; }
        .sec-title { font-family: Arial, sans-serif; font-size: 9pt; font-weight: bold; background-color: #0070C0; color: #FFFFFF; padding: 3px 6px; border: 0.5pt solid #005090; }
        .th-cell { background-color: #FFFFFF; font-weight: bold; border: 0.5pt solid #000000; text-align: left; padding: 3px 4px; font-size: 8pt; }
        .th-cell-num { background-color: #FFFFFF; font-weight: bold; border: 0.5pt solid #000000; text-align: right; padding: 3px 4px; font-size: 8pt; }
        .th-cell-center { background-color: #FFFFFF; font-weight: bold; border: 0.5pt solid #000000; text-align: center; padding: 3px 4px; font-size: 8pt; }
        .td-cell { border: 0.5pt solid #000000; padding: 3px 4px; vertical-align: middle; font-size: 8pt; }
        .td-cell-bold { border: 0.5pt solid #000000; padding: 3px 4px; font-weight: bold; font-size: 8pt; }
        .td-cell-num { border: 0.5pt solid #000000; padding: 3px 4px; text-align: right; font-size: 8pt; mso-number-format: "#,##0"; }
        .td-cell-dec { border: 0.5pt solid #000000; padding: 3px 4px; text-align: right; font-size: 8pt; mso-number-format: "0.0"; }
        .td-cell-pct { border: 0.5pt solid #000000; padding: 3px 4px; text-align: right; font-size: 8pt; }
        .td-cell-center { border: 0.5pt solid #000000; padding: 3px 4px; text-align: center; font-size: 8pt; }
        .meta-label { font-weight: bold; border: 0.5pt solid #000000; padding: 3px 4px; font-size: 8pt; text-align: right; background-color: #FFFFFF; }
        .meta-val { border: 0.5pt solid #000000; padding: 3px 4px; font-size: 8pt; }
        .total-row { font-weight: bold; background-color: #E2E8F0; border: 0.5pt solid #000000; }
        .sig-box { text-align: center; border: none; padding: 6px; font-size: 8pt; vertical-align: top; }
    </style>
</head>
<body>
    <!-- KOP DOKUMEN PT. YASUNAGA INDONESIA -->
    <table>
        <tr>
            <td colspan="15" class="doc-header-1">PT. YASUNAGA INDONESIA</td>
        </tr>
        <tr>
            <td colspan="15" class="doc-header-2">Engine Parts & Air Pump Manufacturing • PLT-01</td>
        </tr>
        <tr>
            <td colspan="15" class="doc-header-3">Jl. Modern Industri Raya Kav. 24 Kawasan Industri Modern Cikande, Nambo Ilir Kibin Serang Banten | Telp: (0254) 400306 | Email: prodcr@yasunaga.co.id</td>
        </tr>
        <tr><td colspan="15" style="border: none; height: 4px;"></td></tr>
        <tr>
            <td colspan="15" class="doc-title">LAPORAN KINERJA PRODUKSI & PERHITUNGAN OEE (SHIFT HANDOVER REPORT)</td>
        </tr>
    </table>

    <!-- METADATA INFORMATION TABLE -->
    <table>
        <tr>
            <td colspan="3" class="meta-label">Periode Tanggal:</td>
            <td colspan="4" class="meta-val"><strong>${header.formatted_date || header.date_range_label || ''}</strong></td>
            <td colspan="3" class="meta-label">Lini Produksi:</td>
            <td colspan="5" class="meta-val"><strong>${header.line_name || 'All Production Lines'}</strong></td>
        </tr>
        <tr>
            <td colspan="3" class="meta-label">Shift Kerja:</td>
            <td colspan="4" class="meta-val"><strong>${header.shift_name || ''}</strong> (${header.shift_time_window || ''})</td>
            <td colspan="3" class="meta-label">Supervisor In-Charge:</td>
            <td colspan="5" class="meta-val"><strong>${header.supervisor_in_charge || ''}</strong></td>
        </tr>
    </table>

    <!-- SECTION I: EXECUTIVE SUMMARY KPI TABLE -->
    <table>
        <tr>
            <td colspan="15" class="sec-title">I. RINGKASAN EKSEKUTIF PENCAPAIAN OEE & OUTPUT PRODUKSI</td>
        </tr>
        <tr>
            <th colspan="4" class="th-cell">Parameter Metrik</th>
            <th colspan="3" class="th-cell-num">Target Standar</th>
            <th colspan="3" class="th-cell-num">Pencapaian Aktual</th>
            <th colspan="2" class="th-cell-num">Deviasi</th>
            <th colspan="3" class="th-cell-center">Status Evaluasi</th>
        </tr>
        <tr>
            <td colspan="4" class="td-cell-bold">Availability (A) - Ketersediaan Mesin</td>
            <td colspan="3" class="td-cell-num">90.00%</td>
            <td colspan="3" class="td-cell-num" style="font-weight: bold;">${(summary.availability || 0).toFixed(2)}%</td>
            <td colspan="2" class="td-cell-num">${(summary.deviations?.availability || 0) >= 0 ? '+' : ''}${(summary.deviations?.availability || 0).toFixed(2)}%</td>
            <td colspan="3" class="td-cell-center" style="font-weight: bold; color: ${(summary.availability || 0) >= 90 ? '#008000' : '#DC2626'};">${(summary.availability || 0) >= 90 ? 'ON TARGET (BAIK)' : 'WARNING (DOWN)'}</td>
        </tr>
        <tr>
            <td colspan="4" class="td-cell-bold">Performance (P) - Efisiensi Kecepatan</td>
            <td colspan="3" class="td-cell-num">95.00%</td>
            <td colspan="3" class="td-cell-num" style="font-weight: bold;">${(summary.performance || 0).toFixed(2)}%</td>
            <td colspan="2" class="td-cell-num">${(summary.deviations?.performance || 0) >= 0 ? '+' : ''}${(summary.deviations?.performance || 0).toFixed(2)}%</td>
            <td colspan="3" class="td-cell-center" style="font-weight: bold; color: ${(summary.performance || 0) >= 95 ? '#008000' : '#DC2626'};">${(summary.performance || 0) >= 95 ? 'ON TARGET (BAIK)' : 'WARNING (SPEED LOSS)'}</td>
        </tr>
        <tr>
            <td colspan="4" class="td-cell-bold">Quality Rate (Q) - Kualitas Mutu</td>
            <td colspan="3" class="td-cell-num">99.00%</td>
            <td colspan="3" class="td-cell-num" style="font-weight: bold;">${(summary.quality || 0).toFixed(2)}%</td>
            <td colspan="2" class="td-cell-num">${(summary.deviations?.quality || 0) >= 0 ? '+' : ''}${(summary.deviations?.quality || 0).toFixed(2)}%</td>
            <td colspan="3" class="td-cell-center" style="font-weight: bold; color: ${(summary.quality || 0) >= 99 ? '#008000' : '#DC2626'};">${(summary.quality || 0) >= 99 ? 'ON TARGET (BAIK)' : 'WARNING (HIGH DEFECT)'}</td>
        </tr>
        <tr class="total-row">
            <td colspan="4" class="td-cell-bold" style="background-color: #E2E8F0;">OVERALL EQUIPMENT EFFECTIVENESS (OEE)</td>
            <td colspan="3" class="td-cell-num" style="background-color: #E2E8F0; font-weight: bold;">85.00%</td>
            <td colspan="3" class="td-cell-num" style="background-color: #E2E8F0; font-weight: bold; font-size: 9.5pt;">${(summary.overall_oee || 0).toFixed(2)}%</td>
            <td colspan="2" class="td-cell-num" style="background-color: #E2E8F0; font-weight: bold;">${(summary.deviations?.oee || 0) >= 0 ? '+' : ''}${(summary.deviations?.oee || 0).toFixed(2)}%</td>
            <td colspan="3" class="td-cell-center" style="background-color: #E2E8F0; font-weight: bold; color: ${(summary.overall_oee || 0) >= 85 ? '#008000' : '#DC2626'};">${summary.oee_badge || '[● OPTIMAL]'}</td>
        </tr>
        <tr>
            <td colspan="3" class="td-cell-bold" style="background-color: #FFFF00; border: 0.5pt solid #000000;">Rincian Output Produksi:</td>
            <td colspan="2" class="td-cell-center" style="background-color: #FFFF00; font-weight: bold; border: 0.5pt solid #000000;">Total Output: ${(summary.total_output_pcs || 0).toLocaleString()} Pcs</td>
            <td colspan="3" class="td-cell-center" style="background-color: #FFFF00; font-weight: bold; border: 0.5pt solid #000000;">Good Output: ${(summary.good_output_pcs || 0).toLocaleString()} Pcs</td>
            <td colspan="3" class="td-cell-center" style="background-color: #FFFF00; font-weight: bold; border: 0.5pt solid #000000;">Reject (NG): ${(summary.reject_output_pcs || 0).toLocaleString()} Pcs</td>
            <td colspan="2" class="td-cell-center" style="background-color: #FFFF00; font-weight: bold; border: 0.5pt solid #000000;">Scrap: ${(summary.scrap_output_pcs || 0).toLocaleString()} Pcs</td>
            <td colspan="2" class="td-cell-center" style="background-color: #FFFF00; font-weight: bold; border: 0.5pt solid #000000;">Yield: ${summary.yield_rate_pct || 100}%</td>
        </tr>
    </table>

    <!-- SECTION II: CORE PRODUCTION MATRIX TABLE -->
    <table>
        <tr>
            <td colspan="15" class="sec-title">II. TABEL RINCIAN PERHITUNGAN OEE PER LINI PRODUKSI & MESIN</td>
        </tr>
        <tr>
            <th class="th-cell-center" style="width: 3%;">No</th>
            <th class="th-cell" style="width: 6%;">Line ID</th>
            <th class="th-cell" style="width: 10%;">Line Name</th>
            <th class="th-cell" style="width: 12%;">Machine</th>
            <th class="th-cell" style="width: 14%;">Product SKU</th>
            <th class="th-cell-num" style="width: 6%;">Plan (m)</th>
            <th class="th-cell-num" style="width: 6%;">Down (m)</th>
            <th class="th-cell-num" style="width: 6%;">Avail (%)</th>
            <th class="th-cell-num" style="width: 6%;">Speed (p/m)</th>
            <th class="th-cell-num" style="width: 7%;">Output (Pcs)</th>
            <th class="th-cell-num" style="width: 6%;">Perf (%)</th>
            <th class="th-cell-num" style="width: 6%;">Defect (Pcs)</th>
            <th class="th-cell-num" style="width: 6%;">Qual (%)</th>
            <th class="th-cell-num" style="width: 6%;">OEE (%)</th>
            <th class="th-cell-center" style="width: 8%;">Status</th>
        </tr>
        ${Object.values(groupedProdRows).map((group) => `
            <tr style="background-color: #0070C0; color: #FFFFFF; font-weight: bold;">
                <td colspan="15" style="padding: 4px 6px; font-size: 8.5pt; border: 0.5pt solid #000000; color: #FFFFFF; background-color: #0070C0;">
                    📅 TANGGAL: ${group.formatted_date} (${group.items.length} Entri Produksi)
                </td>
            </tr>
            ${group.items.map((r, i) => `
                <tr>
                    <td class="td-cell-center">${i + 1}</td>
                    <td class="td-cell-bold">${r.line_id || ''}</td>
                    <td class="td-cell">${r.line_name || ''}</td>
                    <td class="td-cell-bold">${r.machine_code || ''}</td>
                    <td class="td-cell">${r.product_sku || ''}</td>
                    <td class="td-cell-num">${(r.planned_time_minutes || 0).toFixed(0)}</td>
                    <td class="td-cell-num">${(r.downtime_minutes || 0).toFixed(0)}</td>
                    <td class="td-cell-pct">${(r.availability_pct || 0).toFixed(2)}%</td>
                    <td class="td-cell-dec">${r.target_speed > 0 ? Number(r.target_speed).toFixed(1) : '-'}</td>
                    <td class="td-cell-num" style="font-weight: bold;">${(r.total_output_pcs || 0).toLocaleString()}</td>
                    <td class="td-cell-pct">${(r.performance_pct || 0).toFixed(2)}%</td>
                    <td class="td-cell-num">${(r.total_defect_pcs || 0).toLocaleString()}</td>
                    <td class="td-cell-pct">${(r.quality_pct || 0).toFixed(2)}%</td>
                    <td class="td-cell-pct" style="font-weight: bold; background-color: ${(r.oee_pct || 0) >= 85 ? '#D1FAE5' : ((r.oee_pct || 0) >= 65 ? '#FEF3C7' : '#FEE2E2')};">${(r.oee_pct || 0).toFixed(2)}%</td>
                    <td class="td-cell-center">${r.status_badge || ''}</td>
                </tr>
            `).join('')}
        `).join('')}
        <tr class="total-row">
            <td colspan="5" class="td-cell-center font-bold" style="background-color: #E2E8F0;">TOTAL / RATA-RATA PABRIK:</td>
            <td class="td-cell-num" style="background-color: #E2E8F0;">${(summary.total_planned_minutes || 0).toFixed(0)}</td>
            <td class="td-cell-num" style="background-color: #E2E8F0;">${(summary.total_downtime_minutes || 0).toFixed(0)}</td>
            <td class="td-cell-pct" style="background-color: #E2E8F0;">${(summary.availability || 0).toFixed(2)}%</td>
            <td class="td-cell-center" style="background-color: #E2E8F0;">—</td>
            <td class="td-cell-num" style="background-color: #E2E8F0; font-weight: bold;">${(summary.total_output_pcs || 0).toLocaleString()}</td>
            <td class="td-cell-pct" style="background-color: #E2E8F0;">${(summary.performance || 0).toFixed(2)}%</td>
            <td class="td-cell-num" style="background-color: #E2E8F0;">${(summary.total_defect_pcs || 0).toLocaleString()}</td>
            <td class="td-cell-pct" style="background-color: #E2E8F0;">${(summary.quality || 0).toFixed(2)}%</td>
            <td class="td-cell-pct" style="background-color: #E2E8F0; font-weight: bold;">${(summary.overall_oee || 0).toFixed(2)}%</td>
            <td class="td-cell-center" style="background-color: #E2E8F0;">${summary.oee_badge || ''}</td>
        </tr>
    </table>

    <!-- SECTION III: SIX BIG LOSSES -->
    <table>
        <tr>
            <td colspan="15" class="sec-title">III. ANALISIS SIX BIG LOSSES (TPM LOSS BREAKDOWN)</td>
        </tr>
        <tr>
            <th class="th-cell-center" style="width: 4%;">No</th>
            <th colspan="4" class="th-cell" style="width: 28%;">Kategori Six Big Loss</th>
            <th colspan="3" class="th-cell" style="width: 22%;">Pilar OEE Terkait</th>
            <th colspan="2" class="th-cell-num" style="width: 15%;">Durasi (Menit)</th>
            <th colspan="2" class="th-cell-num" style="width: 15%;">Durasi (Jam)</th>
            <th colspan="3" class="th-cell-num" style="width: 16%;">% Kontribusi Loss</th>
        </tr>
        ${sixLosses.map((loss, idx) => `
            <tr>
                <td class="td-cell-center">${idx + 1}</td>
                <td colspan="4" class="td-cell-bold">${loss.loss || ''}</td>
                <td colspan="3" class="td-cell">${loss.category || ''} Loss</td>
                <td colspan="2" class="td-cell-dec">${(loss.minutes || 0).toFixed(1)}</td>
                <td colspan="2" class="td-cell-dec">${(loss.hours || 0).toFixed(2)}</td>
                <td colspan="3" class="td-cell-pct" style="font-weight: bold;">${(loss.percentage || 0).toFixed(1)}%</td>
            </tr>
        `).join('')}
    </table>

    <!-- SECTION IV: MAJOR INCIDENTS LOG -->
    <table>
        <tr>
            <td colspan="15" class="sec-title">IV. LOG GANGGUAN MESIN & AKAR MASALAH (MAJOR INCIDENTS & RCA/CAPA)</td>
        </tr>
        <tr>
            <th colspan="2" class="th-cell" style="width: 11%;">ID Incident</th>
            <th colspan="2" class="th-cell" style="width: 12%;">Jam Trouble</th>
            <th colspan="3" class="th-cell" style="width: 17%;">Mesin & Line</th>
            <th class="th-cell-num" style="width: 8%;">Durasi (m)</th>
            <th colspan="3" class="th-cell" style="width: 26%;">Akar Masalah (5 Whys RCA)</th>
            <th colspan="4" class="th-cell" style="width: 26%;">Tindakan Perbaikan (CAPA) & PIC</th>
        </tr>
        ${incidentLogs.length === 0 ? `
            <tr>
                <td colspan="15" class="td-cell-center" style="padding: 8px; color: #555555;">Tidak ada catatan gangguan breakdown selama shift ini (Zero Breakdown).</td>
            </tr>
        ` : incidentLogs.map(inc => `
            <tr>
                <td colspan="2" class="td-cell-bold">${inc.incident_id || ''}</td>
                <td colspan="2" class="td-cell">${inc.time_window || ''}</td>
                <td colspan="3" class="td-cell">${inc.machine_code || ''} (${inc.line_name || ''})</td>
                <td class="td-cell-num" style="font-weight: bold;">${(inc.duration_minutes || 0).toFixed(0)}</td>
                <td colspan="3" class="td-cell">${inc.root_cause || ''}</td>
                <td colspan="4" class="td-cell">${inc.action_plan || ''} (PIC: ${inc.pic || ''})</td>
            </tr>
        `).join('')}
    </table>

    <!-- SECTION V: HANDOVER & SIGNATURES -->
    <table>
        <tr>
            <td colspan="15" class="sec-title">V. CATATAN SERAH TERIMA & PENGESAHAN LAPORAN</td>
        </tr>
        <tr>
            <td colspan="15" class="td-cell" style="background-color: #F8FAFC; padding: 6px;">
                <strong>Instruksi Prioritas Shift Handover:</strong>
                <div style="margin-top: 3px;">
                    ${handoverNotes.map(n => `<div style="padding: 1px 0;">• ${n}</div>`).join('')}
                </div>
            </td>
        </tr>
        <tr>
            <td colspan="15" style="text-align: right; border: none; font-size: 8pt; padding: 10px 10px 4px 0;">
                Tanggal: <strong>${header.formatted_date || ''}</strong>
            </td>
        </tr>
        <tr>
            <td colspan="5" class="sig-box" style="width: 33.3%;">
                Diserahkan Oleh,<br><br><br><br>
                ( <strong>${header.supervisor_in_charge || '........................'}</strong> )<br>
                <span style="font-size: 7.5pt; color: #555555;">Supervisor Shift In-Charge</span>
            </td>
            <td colspan="5" class="sig-box" style="width: 33.3%;">
                Diterima Oleh,<br><br><br><br>
                ( ............................................ )<br>
                <span style="font-size: 7.5pt; color: #555555;">Supervisor Incoming Shift</span>
            </td>
            <td colspan="5" class="sig-box" style="width: 33.3%;">
                Mengetahui / Diverifikasi:<br><br><br><br>
                ( ............................................ )<br>
                <span style="font-size: 7.5pt; color: #555555;">Production Manager</span>
            </td>
        </tr>
    </table>
</body>
</html>
        `;

        const isJa = this.currentLang === 'ja';
        let finalExcelContent = excelContent;
        if (isJa) {
            finalExcelContent = i18n.translateHtml(excelContent);
        }

        const blob = new Blob([finalExcelContent], { type: 'application/vnd.ms-excel;charset=utf-8' });
        const url = URL.createObjectURL(blob);
        const link = document.createElement('a');
        const dateStr = header.formatted_date || header.production_date || new Date().toISOString().slice(0, 10);
        const lineStr = (header.line_name && header.line_name !== 'All Production Lines' && header.line_name !== 'All Lines') ? ` - ${header.line_name}` : '';
        const shiftStr = (header.shift_name && !header.shift_name.toLowerCase().includes('all')) ? ` - ${header.shift_name}` : '';
        let excelFilename = this.sanitizeReportFilename(`Laporan Produksi dan OEE Tgl ${dateStr}${lineStr}${shiftStr}`);
        if (isJa) excelFilename = i18n.translateText(excelFilename);
        link.href = url;
        link.download = excelFilename + '.xls';
        document.body.appendChild(link);
        link.click();
        document.body.removeChild(link);
        URL.revokeObjectURL(url);
    }

    /**
     * Generate & Trigger Clean Excel-Style A4 Portrait Report Print (Isolated Iframe Engine)
     */
    prepareAndPrintReport(report) {
        const header = report.header || {};
        const summary = report.executive_summary || {};
        const rows = report.matrix_rows || [];
        const sixLosses = report.six_big_losses || [];
        const incidentLogs = report.incident_logs || [];
        const handoverNotes = report.handover_notes || [];
        const isJa = this.currentLang === 'ja';

        const dateStr = header.formatted_date || header.production_date || new Date().toISOString().slice(0, 10);
        const lineStr = (header.line_name && header.line_name !== 'All Production Lines' && header.line_name !== 'All Lines') ? ` - ${header.line_name}` : '';
        const shiftStr = (header.shift_name && !header.shift_name.toLowerCase().includes('all')) ? ` - ${header.shift_name}` : '';
        let reportFilename = this.sanitizeReportFilename(`Laporan Produksi dan OEE Tgl ${dateStr}${lineStr}${shiftStr}`);
        if (isJa) reportFilename = i18n.translateText(reportFilename);

        // Group rows by production_date
        const groupedProdRows = rows.reduce((acc, row) => {
            const d = row.production_date || row.formatted_date || 'Unknown';
            if (!acc[d]) acc[d] = { date: d, formatted_date: row.formatted_date || d, items: [] };
            acc[d].items.push(row);
            return acc;
        }, {});

        let printHtml = `<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>${reportFilename}</title>
    <style>
        @page {
            size: A4 portrait;
            margin: 8mm 8mm 8mm 8mm;
        }
        * {
            box-sizing: border-box;
            -webkit-print-color-adjust: exact !important;
            print-color-adjust: exact !important;
        }
        body {
            font-family: Arial, Helvetica, sans-serif;
            font-size: 9px;
            color: #000000;
            background: #ffffff;
            margin: 0;
            padding: 0;
            line-height: 1.3;
        }
        .header-container {
            border-bottom: 2px solid #000000;
            padding-bottom: 4px;
            margin-bottom: 6px;
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
        }
        .company-name {
            font-size: 13px;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        .report-title {
            font-size: 11.5px;
            font-weight: bold;
            text-align: right;
            text-transform: uppercase;
        }
        .meta-sub {
            font-size: 8px;
            color: #333333;
        }
        .section-header {
            font-size: 9px;
            font-weight: bold;
            text-transform: uppercase;
            background-color: #E2E8F0;
            padding: 2.5px 5px;
            border-left: 3px solid #0284C7;
            margin: 6px 0 2.5px 0;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 5px;
            font-size: 9px;
        }
        th, td {
            border: 1px solid #475569;
            padding: 3px 4px;
            vertical-align: middle;
        }
        th {
            background-color: #F1F5F9;
            font-weight: bold;
            color: #000000;
            text-align: left;
        }
        .text-right { text-align: right; }
        .text-center { text-align: center; }
        .font-bold { font-weight: bold; }
        .bg-light { background-color: #F8FAFC; }
        .bg-highlight { background-color: #F1F5F9; font-weight: bold; }
        .signatures {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
            font-size: 8.5px;
            page-break-inside: avoid;
        }
        .signatures td {
            border: none;
            text-align: center;
            padding: 3px 5px;
            vertical-align: top;
        }
        .sig-line {
            border-bottom: 1px solid #000000;
            width: 75%;
            margin: 28px auto 3px auto;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <!-- OFFICIAL COMPANY LETTERHEAD (KOP SURAT RESMI DENGAN LOGO) -->
    <div style="border-bottom: 2.5px double #000000; padding-bottom: 5px; margin-bottom: 6px;">
        <table style="width: 100%; border: none; margin-bottom: 0;">
            <tr>
                <td style="width: 65px; border: none; padding: 0 8px 0 0; vertical-align: middle;">
                    <img src="${this.escapeHtml(this.getCompanyLogo(header))}" style="max-height: 48px; max-width: 65px; object-fit: contain;" alt="Logo" onerror="this.onerror=null; this.src='/images/yasunaga-logo.png';" />
                </td>
                <td style="border: none; padding: 0; vertical-align: middle;">
                    <div style="font-size: 12px; font-weight: 900; text-transform: uppercase; letter-spacing: 0.5px; line-height: 1.15;">${header.company_name || this.companyProfile?.company_name || 'PT. YASUNAGA INDONESIA'}</div>
                    <div style="font-size: 8.5px; font-weight: bold; color: #1e293b; margin-top: 1px;">${header.plant_name || this.companyProfile?.plant_name || 'ENGINE PARTS & AIR PUMP MFG'} • <strong>${header.plant_id || this.companyProfile?.plant_code || 'PLT-01'}</strong></div>
                    <div style="font-size: 7.5px; color: #475569; margin-top: 1px;">${header.address || this.companyProfile?.address || header.plant_location || ''}</div>
                    <div style="font-size: 7.5px; color: #64748b;">${(header.phone || this.companyProfile?.phone) ? 'Telp: ' + (header.phone || this.companyProfile?.phone) : ''} ${(header.email || this.companyProfile?.email) ? '• Email: ' + (header.email || this.companyProfile?.email) : ''} ${(header.website || this.companyProfile?.website) ? '• Web: ' + (header.website || this.companyProfile?.website) : ''}</div>
                </td>
                <td style="width: 220px; border: none; padding: 0; text-align: right; vertical-align: top;">
                    <div style="font-size: 10.5px; font-weight: 800; text-transform: uppercase; line-height: 1.2;">LAPORAN KINERJA PRODUKSI & OEE</div>
                    <div style="font-size: 8px; color: #333333; font-family: monospace; margin-top: 2px;">Doc ID: ${header.doc_prefix || 'YSN-OEE'}-${header.production_date || ''}</div>
                    <div style="font-size: 7.5px; color: #64748b; font-family: monospace;">Form: A4-Portrait Shift Handover</div>
                </td>
            </tr>
        </table>
    </div>

    <!-- METADATA INFORMATION TABLE (COMPACT EXCEL BOX) -->
    <table>
        <tr>
            <td style="width: 15%; font-weight: bold; background-color: #f1f5f9;">Tanggal Produksi:</td>
            <td style="width: 35%;">${header.formatted_date || ''}</td>
            <td style="width: 15%; font-weight: bold; background-color: #f1f5f9;">Lini Produksi:</td>
            <td style="width: 35%;"><strong>${header.line_name || 'All Lines'}</strong></td>
        </tr>
        <tr>
            <td style="font-weight: bold; background-color: #f1f5f9;">Shift Kerja:</td>
            <td><strong>${header.shift_name || ''}</strong> (${header.shift_time_window || ''})</td>
            <td style="font-weight: bold; background-color: #f1f5f9;">Supervisor In-Charge:</td>
            <td><strong>${header.supervisor_in_charge || ''}</strong></td>
        </tr>
    </table>

    <!-- SECTION I: RINGKASAN PENCAPAIAN OEE & OUTPUT PRODUKSI (EXECUTIVE SUMMARY) -->
    <div class="section-header">
        I. RINGKASAN PENCAPAIAN OEE & OUTPUT PRODUKSI (EXECUTIVE SUMMARY)
    </div>
    <table>
        <thead>
            <tr>
                <th style="width: 32%;">Parameter Metrik</th>
                <th style="width: 16%; text-align: right;">Target Standar</th>
                <th style="width: 18%; text-align: right;">Pencapaian Aktual</th>
                <th style="width: 16%; text-align: right;">Deviasi</th>
                <th style="width: 18%; text-align: center;">Status Evaluasi</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td><strong>Availability (A) - Ketersediaan Mesin</strong></td>
                <td class="text-right">90.00%</td>
                <td class="text-right font-bold">${(summary.availability || 0).toFixed(2)}%</td>
                <td class="text-right">${(summary.deviations?.availability || 0) >= 0 ? '+' : ''}${(summary.deviations?.availability || 0).toFixed(2)}%</td>
                <td class="text-center font-bold">${(summary.availability || 0) >= 90 ? 'ON TARGET (BAIK)' : 'WARNING (DOWN)'}</td>
            </tr>
            <tr>
                <td><strong>Performance (P) - Efisiensi Kecepatan</strong></td>
                <td class="text-right">95.00%</td>
                <td class="text-right font-bold">${(summary.performance || 0).toFixed(2)}%</td>
                <td class="text-right">${(summary.deviations?.performance || 0) >= 0 ? '+' : ''}${(summary.deviations?.performance || 0).toFixed(2)}%</td>
                <td class="text-center font-bold">${(summary.performance || 0) >= 95 ? 'ON TARGET (BAIK)' : 'WARNING (SPEED LOSS)'}</td>
            </tr>
            <tr>
                <td><strong>Quality Rate (Q) - Kualitas Mutu</strong></td>
                <td class="text-right">99.00%</td>
                <td class="text-right font-bold">${(summary.quality || 0).toFixed(2)}%</td>
                <td class="text-right">${(summary.deviations?.quality || 0) >= 0 ? '+' : ''}${(summary.deviations?.quality || 0).toFixed(2)}%</td>
                <td class="text-center font-bold">${(summary.quality || 0) >= 99 ? 'ON TARGET (BAIK)' : 'WARNING (HIGH DEFECT)'}</td>
            </tr>
            <tr class="bg-highlight">
                <td><strong>OVERALL EQUIPMENT EFFECTIVENESS (OEE)</strong></td>
                <td class="text-right">85.00%</td>
                <td class="text-right" style="font-size: 10px;">${(summary.overall_oee || 0).toFixed(2)}%</td>
                <td class="text-right">${(summary.deviations?.oee || 0) >= 0 ? '+' : ''}${(summary.deviations?.oee || 0).toFixed(2)}%</td>
                <td class="text-center">${summary.oee_badge || '[● OPTIMAL]'}</td>
            </tr>
            <tr>
                <td><strong>Total Output Produksi:</strong></td>
                <td colspan="4">
                    Total Output: <strong>${(summary.total_output_pcs || 0).toLocaleString()} Pcs</strong> &nbsp;|&nbsp; 
                    Good Output: <strong>${(summary.good_output_pcs || 0).toLocaleString()} Pcs</strong> &nbsp;|&nbsp; 
                    Total Reject/Scrap: <strong>${(summary.total_defect_pcs || 0).toLocaleString()} Pcs</strong> &nbsp;|&nbsp; 
                    Yield: <strong>${summary.yield_rate_pct || 100}%</strong>
                </td>
            </tr>
        </tbody>
    </table>

    <!-- SECTION II: TABEL RINCIAN PERHITUNGAN OEE PER LINI PRODUKSI & MESIN -->
    <div class="section-header">
        II. TABEL RINCIAN PERHITUNGAN OEE PER LINI PRODUKSI & MESIN
    </div>
    <table>
        <thead>
            <tr>
                <th style="width: 3%; text-align: center;">No</th>
                <th style="width: 14%;">Lini Produksi</th>
                <th style="width: 10%;">Mesin</th>
                <th style="width: 17%;">Produk - SKU</th>
                <th style="width: 7%; text-align: right;">Plan (m)</th>
                <th style="width: 7%; text-align: right;">Down (m)</th>
                <th style="width: 7%; text-align: right;">Avail</th>
                <th style="width: 8%; text-align: right;">Output</th>
                <th style="width: 7%; text-align: right;">Perf</th>
                <th style="width: 7%; text-align: right;">Defect</th>
                <th style="width: 7%; text-align: right;">Qual</th>
                <th style="width: 8%; text-align: right;">OEE</th>
            </tr>
        </thead>
        <tbody>
            ${rows.length === 0 ? `<tr><td colspan="12" class="text-center" style="padding: 10px;">Tidak ada data produksi pada periode ini.</td></tr>` : ''}
            ${Object.values(groupedProdRows).map((group) => `
                <tr style="background-color: #0f172a; color: #38bdf8; font-weight: bold;">
                    <td colspan="12" style="padding: 3px 6px; font-size: 8px; border-top: 1.5px solid #0284c7; background-color: #0f172a; color: #38bdf8;">
                        📅 TANGGAL PRODUKSI: ${group.formatted_date} (${group.items.length} Mesin / Lini Operasional)
                    </td>
                </tr>
                ${group.items.map((r, i) => `
                    <tr>
                        <td class="text-center">${i + 1}</td>
                        <td><strong>${r.line_name || ''}</strong><br><span style="font-size: 7.5px; color: #555;">${r.shift_name ? `${r.shift_name} • ` : ''}${r.line_id || ''}</span></td>
                        <td><strong>${r.machine_code || ''}</strong></td>
                        <td>${r.product_sku || ''}</td>
                        <td class="text-right">${(r.planned_time_minutes || 0).toFixed(0)}</td>
                        <td class="text-right">${(r.downtime_minutes || 0).toFixed(0)}</td>
                        <td class="text-right">${(r.availability_pct || 0).toFixed(1)}%</td>
                        <td class="text-right font-bold">${(r.total_output_pcs || 0).toLocaleString()}</td>
                        <td class="text-right">${(r.performance_pct || 0).toFixed(1)}%</td>
                        <td class="text-right">${(r.reject_output_pcs || 0).toLocaleString()}</td>
                        <td class="text-right">${(r.quality_pct || 0).toFixed(1)}%</td>
                        <td class="text-right font-bold">${(r.oee_pct || 0).toFixed(2)}%</td>
                    </tr>
                `).join('')}
            `).join('')}
        </tbody>
        <tfoot>
            <tr class="bg-highlight">
                <td colspan="4" class="text-center font-bold">RATA-RATA / TOTAL PABRIK:</td>
                <td class="text-right">${(summary.total_planned_minutes || 0).toFixed(0)}</td>
                <td class="text-right">${(summary.total_downtime_minutes || 0).toFixed(0)}</td>
                <td class="text-right">${(summary.availability || 0).toFixed(1)}%</td>
                <td class="text-right">${(summary.total_output_pcs || 0).toLocaleString()}</td>
                <td class="text-right">${(summary.performance || 0).toFixed(1)}%</td>
                <td class="text-right">${(summary.total_defect_pcs || 0).toLocaleString()}</td>
                <td class="text-right">${(summary.quality || 0).toFixed(1)}%</td>
                <td class="text-right font-bold" style="font-size: 10px;">${(summary.overall_oee || 0).toFixed(2)}%</td>
            </tr>
        </tfoot>
    </table>

    <!-- SECTION III: ANALISIS SIX BIG LOSSES (TPM LOSS BREAKDOWN) -->
    <div class="section-header">
        III. ANALISIS SIX BIG LOSSES (TPM LOSS BREAKDOWN)
    </div>
    <table>
        <thead>
            <tr>
                <th style="width: 4%; text-align: center;">No</th>
                <th style="width: 25%;">Kategori Six Big Loss</th>
                <th style="width: 18%;">Pilar OEE Terkait</th>
                <th style="width: 15%; text-align: right;">Durasi (Menit)</th>
                <th style="width: 15%; text-align: right;">Durasi (Jam)</th>
                <th style="width: 23%;">% Kontribusi Loss</th>
            </tr>
        </thead>
        <tbody>
            ${sixLosses.map((loss, idx) => `
                <tr>
                    <td class="text-center">${idx + 1}</td>
                    <td><strong>${loss.loss || ''}</strong></td>
                    <td>${loss.category || ''} Loss</td>
                    <td class="text-right">${(loss.minutes || 0).toFixed(1)} m</td>
                    <td class="text-right">${(loss.hours || 0).toFixed(2)} hrs</td>
                    <td class="text-right font-bold">${(loss.percentage || 0).toFixed(1)}%</td>
                </tr>
            `).join('')}
        </tbody>
    </table>

    ${incidentLogs.length > 0 ? `
        <!-- SECTION IV: LOG GANGGUAN MESIN & AKAR MASALAH (MAJOR INCIDENTS & RCA/CAPA) -->
        <div class="section-header">
            IV. LOG GANGGUAN MESIN & AKAR MASALAH (MAJOR INCIDENTS & RCA/CAPA)
        </div>
        <table>
            <thead>
                <tr>
                    <th style="width: 10%;">ID Incident</th>
                    <th style="width: 11%;">Jam</th>
                    <th style="width: 14%;">Mesin & Line</th>
                    <th style="width: 8%; text-align: right;">Durasi</th>
                    <th style="width: 29%;">Akar Masalah (5 Whys RCA)</th>
                    <th style="width: 28%;">Tindakan Perbaikan (CAPA) & PIC</th>
                </tr>
            </thead>
            <tbody>
                ${incidentLogs.map(inc => `
                    <tr>
                        <td><strong>${inc.incident_id || ''}</strong></td>
                        <td>${inc.time_window || ''}</td>
                        <td><strong>${inc.machine_code || ''}</strong><br><span style="font-size: 7.5px;">${inc.line_name || ''}</span></td>
                        <td class="text-right font-bold">${(inc.duration_minutes || 0).toFixed(0)} m</td>
                        <td>${inc.root_cause || ''}</td>
                        <td>${inc.action_plan || ''}<br><span style="font-size: 7.5px; font-weight: bold;">PIC: ${inc.pic || ''}</span></td>
                    </tr>
                `).join('')}
            </tbody>
        </table>
    ` : ''}

    <!-- SECTION V: INSTRUKSI SHIFT BERIKUTNYA & PENGESAHAN SERAH TERIMA -->
    <div class="section-header">
        V. INSTRUKSI SHIFT BERIKUTNYA & PENGESAHAN SERAH TERIMA
    </div>
    <table>
        <tr>
            <td style="vertical-align: top; padding: 3px 5px;">
                <strong>Catatan Prioritas Handover:</strong>
                <ol style="margin: 2px 0 0 12px; padding: 0;">
                    ${handoverNotes.map(n => `<li>${n}</li>`).join('')}
                </ol>
            </td>
        </tr>
    </table>

    <!-- TANDA TANGAN (3 KOLOM RESMI) -->
    <div style="text-align: right; font-size: 8.5px; margin-top: 8px; margin-bottom: 2px; color: #1e293b;">
        Tanggal: <strong>${header.formatted_date || ''}</strong>
    </div>
    <table class="signatures">
        <tr>
            <td style="width: 33.3%;">
                Diserahkan Oleh (Shift Selesai):
                <div class="sig-line">${header.supervisor_in_charge || ''}</div>
                <div style="font-size: 7.5px; color: #444;">Supervisor Shift In-Charge</div>
            </td>
            <td style="width: 33.3%;">
                Diterima Oleh (Shift Berikutnya):
                <div class="sig-line">( ............................................ )</div>
                <div style="font-size: 7.5px; color: #444;">Supervisor Incoming Shift</div>
            </td>
            <td style="width: 33.3%;">
                Mengetahui / Diverifikasi:
                <div class="sig-line">( ............................................ )</div>
                <div style="font-size: 7.5px; color: #444;">Production Manager</div>
            </td>
        </tr>
    </table>
</body>
</html>`;

        if (isJa) {
            printHtml = i18n.translateHtml(printHtml);
        }

        let frame = document.getElementById('print-iframe-target');
        if (!frame) {
            frame = document.createElement('iframe');
            frame.id = 'print-iframe-target';
            frame.style.position = 'fixed';
            frame.style.right = '0';
            frame.style.bottom = '0';
            frame.style.width = '0';
            frame.style.height = '0';
            frame.style.border = '0';
            frame.style.visibility = 'hidden';
            document.body.appendChild(frame);
        }

        const frameDoc = frame.contentWindow.document;
        frameDoc.open();
        frameDoc.write(printHtml);
        frameDoc.close();

        if (isJa) {
            i18n.localizeDom(frameDoc.body);
        }

        const originalDocTitle = document.title;
        this.triggerFramePrint(frame, frameDoc, originalDocTitle, reportFilename);
    }

    /**
     * Print Dedicated Official A4 NG & Quality Defect Report
     */
    prepareAndPrintNgReport(ngData, header, filters) {
        const items = ngData.items || [];
        const summary = ngData.summary || {};
        const totalOutput = summary.total_output || 0;
        const totalGood = summary.total_good || 0;
        const totalNg = summary.total_ng || 0;
        const defectRate = summary.defect_rate || '0.00';
        const yieldRate = summary.yield_rate || '100.00';
        const totalAssy = summary.total_assy || 0;
        const totalRod = summary.total_rod || 0;
        const totalCap = summary.total_cap || 0;
        const totalBolt = summary.total_bolt || 0;
        const totalBush = summary.total_bush || 0;
        const totalNut = summary.total_nut || 0;
        const totalPin = summary.total_pin || 0;
        const totalOeeNg = summary.total_oee_ng || 0;
        const totalNonOeeNg = summary.total_non_oee_ng || 0;

        // Group items by production_date
        const groupedNgItems = items.reduce((acc, it) => {
            const d = it.production_date || it.formatted_date || 'Unknown';
            if (!acc[d]) acc[d] = { date: d, formatted_date: it.formatted_date || d, items: [] };
            acc[d].items.push(it);
            return acc;
        }, {});

        const isJa = this.currentLang === 'ja';
        const dateStr = header.formatted_date || header.production_date || new Date().toISOString().slice(0, 10);
        const lineStr = (header.line_name && header.line_name !== 'All Production Lines' && header.line_name !== 'All Lines') ? ` - ${header.line_name}` : '';
        const shiftStr = (header.shift_name && !header.shift_name.toLowerCase().includes('all')) ? ` - ${header.shift_name}` : '';
        let ngFilename = this.sanitizeReportFilename(`Laporan Data NG dan Defect Mutu Tgl ${dateStr}${lineStr}${shiftStr}`);
        if (isJa) ngFilename = i18n.translateText(ngFilename);

        let printHtml = `<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>${ngFilename}</title>
    <style>
        @page {
            size: A4 landscape;
            margin: 8mm 8mm 8mm 8mm;
        }
        * {
            box-sizing: border-box;
            -webkit-print-color-adjust: exact !important;
            print-color-adjust: exact !important;
        }
        body {
            font-family: Arial, Helvetica, sans-serif;
            font-size: 8.5px;
            color: #000000;
            background: #ffffff;
            margin: 0;
            padding: 0;
            line-height: 1.25;
        }
        .section-header {
            font-size: 9px;
            font-weight: bold;
            text-transform: uppercase;
            background-color: #E2E8F0;
            padding: 2.5px 5px;
            border-left: 3px solid #D97706;
            margin: 6px 0 2.5px 0;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 5px;
            font-size: 8.5px;
        }
        th, td {
            border: 1px solid #475569;
            padding: 3px 4px;
            vertical-align: middle;
        }
        th {
            background-color: #F1F5F9;
            font-weight: bold;
            text-transform: uppercase;
            font-size: 8px;
            color: #000000;
            text-align: left;
        }
        .text-right { text-align: right; }
        .text-center { text-align: center; }
        .font-bold { font-weight: bold; }
        .bg-light { background-color: #F8FAFC; }
        .bg-highlight { background-color: #FEF3C7; font-weight: bold; }
        .signatures {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
            font-size: 8.5px;
            page-break-inside: avoid;
        }
        .signatures td {
            border: none;
            text-align: center;
            padding: 3px 5px;
            vertical-align: top;
        }
        .sig-line {
            border-bottom: 1px solid #000000;
            width: 75%;
            margin: 28px auto 3px auto;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <!-- KOP SURAT RESMI PT YASUNAGA INDONESIA -->
    <div style="border-bottom: 2.5px double #000000; padding-bottom: 5px; margin-bottom: 6px;">
        <table style="width: 100%; border: none; margin-bottom: 0;">
            <tr>
                <td style="width: 65px; border: none; padding: 0 8px 0 0; vertical-align: middle;">
                    <img src="${this.escapeHtml(this.getCompanyLogo(header))}" style="max-height: 48px; max-width: 65px; object-fit: contain;" alt="Logo" onerror="this.onerror=null; this.src='/images/yasunaga-logo.png';" />
                </td>
                <td style="border: none; padding: 0; vertical-align: middle;">
                    <div style="font-size: 12px; font-weight: 900; text-transform: uppercase; letter-spacing: 0.5px; line-height: 1.15;">${header.company_name || this.companyProfile?.company_name || 'PT. YASUNAGA INDONESIA'}</div>
                    <div style="font-size: 8.5px; font-weight: bold; color: #1e293b; margin-top: 1px;">${header.plant_name || this.companyProfile?.plant_name || 'ENGINE PARTS & AIR PUMP MFG'} • <strong>${header.plant_id || this.companyProfile?.plant_code || 'PLT-01'}</strong></div>
                    <div style="font-size: 7.5px; color: #475569; margin-top: 1px;">${header.address || this.companyProfile?.address || header.plant_location || ''}</div>
                    <div style="font-size: 7.5px; color: #64748b;">${(header.phone || this.companyProfile?.phone) ? 'Telp: ' + (header.phone || this.companyProfile?.phone) : ''} ${(header.email || this.companyProfile?.email) ? '• Email: ' + (header.email || this.companyProfile?.email) : ''}</div>
                </td>
                <td style="width: 250px; border: none; padding: 0; text-align: right; vertical-align: top;">
                    <div style="font-size: 11px; font-weight: 800; text-transform: uppercase; line-height: 1.2; color: #B45309;">LAPORAN DATA NG & DEFECT MUTU</div>
                    <div style="font-size: 8px; color: #333333; font-family: monospace; margin-top: 2px;">Doc ID: QC-NG-${header.production_date || new Date().toISOString().slice(0,10)}</div>
                    <div style="font-size: 7.5px; color: #64748b; font-family: monospace;">Form: A4-Landscape QC Defect Inspection Report</div>
                </td>
            </tr>
        </table>
    </div>

    <!-- METADATA INFORMATION TABLE -->
    <table>
        <tr>
            <td style="width: 15%; font-weight: bold; background-color: #f1f5f9;">Periode Tanggal:</td>
            <td style="width: 35%;"><strong>${header.formatted_date || header.date_range_label || header.production_date || ''}</strong></td>
            <td style="width: 15%; font-weight: bold; background-color: #f1f5f9;">Lini Produksi:</td>
            <td style="width: 35%;"><strong>${header.line_name || 'All Lines'}</strong></td>
        </tr>
        <tr>
            <td style="font-weight: bold; background-color: #f1f5f9;">Shift Kerja:</td>
            <td><strong>${header.shift_name || 'All Shifts'}</strong></td>
            <td style="font-weight: bold; background-color: #f1f5f9;">Supervisor / QC PIC:</td>
            <td><strong>${header.supervisor_in_charge || 'Alim Utama'}</strong></td>
        </tr>
    </table>

    <!-- SECTION I: RINGKASAN REKAPITULASI KUALITAS -->
    <div class="section-header">
        I. RINGKASAN PENCAPAIAN KUALITAS & DEFECT (QC EXECUTIVE SUMMARY)
    </div>
    <table>
        <thead>
            <tr>
                <th style="width: 20%;">Total Output Produksi</th>
                <th style="width: 20%; text-align: right;">Good Output (OK)</th>
                <th style="width: 20%; text-align: right;">Total Reject / NG</th>
                <th style="width: 20%; text-align: right;">Defect Rate (%)</th>
                <th style="width: 20%; text-align: right;">Quality Yield Rate (%)</th>
            </tr>
        </thead>
        <tbody>
            <tr class="bg-highlight">
                <td><strong>${totalOutput.toLocaleString()} Pcs</strong></td>
                <td class="text-right" style="color: #047857; font-weight: bold;">${totalGood.toLocaleString()} Pcs</td>
                <td class="text-right" style="color: #B91C1C; font-weight: bold;">${totalNg.toLocaleString()} Pcs</td>
                <td class="text-right font-bold">${defectRate}%</td>
                <td class="text-right font-bold" style="color: #047857;">${yieldRate}%</td>
            </tr>
            <tr>
                <td colspan="5" style="background-color: #f8fafc;">
                    <strong>Rincian Komponen NG:</strong> &nbsp;
                    Wajib OEE: <strong>${totalOeeNg} pcs</strong> (Assy: ${totalAssy} | Rod: ${totalRod} | Cap: ${totalCap}) &nbsp;|&nbsp;
                    Pelengkap Non-OEE: <strong>${totalNonOeeNg} pcs</strong> (Bolt: ${totalBolt} | Bush: ${totalBush} | Nut: ${totalNut} | Pin: ${totalPin})
                </td>
            </tr>
        </tbody>
    </table>

    <!-- SECTION II: TABEL REKAPITULASI DATA NG -->
    <div class="section-header">
        II. TABEL REKAPITULASI RINCIAN DATA DEFECT PER LINE & PRODUK
    </div>
    <table>
        <thead>
            <tr>
                <th style="width: 3%; text-align: center;">No</th>
                <th style="width: 8%;">Tanggal & Shift</th>
                <th style="width: 10%;">Line & Mesin</th>
                <th style="width: 12%;">Type Produk (SKU)</th>
                <th style="width: 6%; text-align: right;">Output</th>
                <th style="width: 6%; text-align: right;">OK</th>
                <th style="width: 6%; text-align: right;">Total NG</th>
                <th style="width: 5%; text-align: right;">Rate (%)</th>
                <th style="width: 11%; text-align: center;">Wajib OEE (A/R/C)</th>
                <th style="width: 11%; text-align: center;">Pelengkap (B/Bs/N/P)</th>
                <th style="width: 12%;">Masalah / Kategori Defect</th>
                <th style="width: 10%;">Tindakan CAPA</th>
            </tr>
        </thead>
        <tbody>
            ${items.length === 0 ? `
                <tr>
                    <td colspan="12" class="text-center" style="padding: 10px; color: #555;">Tidak ada catatan data defect / NG untuk kriteria filter yang dipilih.</td>
                </tr>
            ` : Object.values(groupedNgItems).map((group) => `
                <tr style="background-color: #0f172a; color: #fbbf24; font-weight: bold;">
                    <td colspan="12" style="padding: 3px 6px; font-size: 8px; border-top: 1.5px solid #d97706; background-color: #0f172a; color: #fbbf24;">
                        📅 TANGGAL AUDIT DEFECT: ${group.formatted_date} (${group.items.length} Entri Produksi)
                    </td>
                </tr>
                ${group.items.map((it, i) => {
                    const dRate = it.total_output > 0 ? ((it.total_ng_target / it.total_output) * 100).toFixed(2) : '0.00';
                    const ng = it.ng_detail || {};
                    return `
                        <tr>
                            <td class="text-center">${i + 1}</td>
                            <td>${it.production_date}<br><span style="font-size: 7.5px; color: #555;">${it.shift_name}</span></td>
                            <td><strong>${it.line_name}</strong><br><span style="font-size: 7.5px;">${it.machine_code}</span></td>
                            <td><strong>${it.product_name}</strong><br><span style="font-size: 7.5px;">${it.product_sku}</span></td>
                            <td class="text-right">${(it.total_output || 0).toLocaleString()}</td>
                            <td class="text-right" style="color: #047857; font-weight: bold;">${(it.good_quantity || 0).toLocaleString()}</td>
                            <td class="text-right font-bold" style="color: #B91C1C;">${(it.total_ng_target || 0).toLocaleString()}</td>
                            <td class="text-right">${dRate}%</td>
                            <td class="text-center font-bold">A:${ng.ng_assy || 0} | R:${ng.ng_rod || 0} | C:${ng.ng_cap || 0}</td>
                            <td class="text-center">B:${ng.ng_bolt || 0} | Bs:${ng.ng_bush || 0} | N:${ng.ng_nut || 0} | P:${ng.ng_pin || 0}</td>
                            <td><strong>${ng.defect_category_name || (ng.defect_symptom ? ng.defect_symptom : 'Defect Proses')}</strong><br><span style="font-size: 7.5px;">${ng.defect_reason_name || ng.notes || '-'}</span></td>
                            <td>${ng.action_taken || '-'}</td>
                        </tr>
                    `;
                }).join('')}
            `).join('')}
        </tbody>
        <tfoot>
            <tr style="background-color: #E2E8F0; font-weight: bold;">
                <td colspan="4" class="text-center font-bold">TOTAL KESELURUHAN:</td>
                <td class="text-right">${totalOutput.toLocaleString()}</td>
                <td class="text-right" style="color: #047857;">${totalGood.toLocaleString()}</td>
                <td class="text-right" style="color: #B91C1C;">${totalNg.toLocaleString()}</td>
                <td class="text-right">${defectRate}%</td>
                <td class="text-center">A: ${totalAssy} | R: ${totalRod} | C: ${totalCap}</td>
                <td class="text-center">B: ${totalBolt} | Bs: ${totalBush} | N: ${totalNut} | P: ${totalPin}</td>
                <td colspan="2" class="text-center" style="font-size: 7.5px;">Total Wajib: ${totalOeeNg} pcs | Non-OEE: ${totalNonOeeNg} pcs</td>
            </tr>
        </tfoot>
    </table>

    <!-- TANDA TANGAN 3 KOLOM PENGESAHAN QC -->
    <div style="text-align: right; font-size: 8.5px; margin-top: 10px; margin-bottom: 2px; color: #1e293b;">
        Tanggal Cetak: <strong>${header.formatted_date || ''}</strong>
    </div>
    <table class="signatures">
        <tr>
            <td style="width: 33.3%;">
                Dibuat Oleh (Operator Produksi):
                <div class="sig-line">${items[0]?.operator_name || 'Operator Mesin'}</div>
                <div style="font-size: 7.5px; color: #444;">Operator Produksi</div>
            </td>
            <td style="width: 33.3%;">
                Diperiksa Oleh (QC Inspector / Leader):
                <div class="sig-line">${items[0]?.ng_detail?.inspector_name || 'Leader QC'}</div>
                <div style="font-size: 7.5px; color: #444;">Quality Control Inspector</div>
            </td>
            <td style="width: 33.3%;">
                Disetujui Oleh (Supervisor Produksi / QC):
                <div class="sig-line">${header.supervisor_in_charge || 'Alim Utama'}</div>
                <div style="font-size: 7.5px; color: #444;">Supervisor Produksi & QC</div>
            </td>
        </tr>
    </table>
</body>
</html>`;

        if (isJa) {
            printHtml = i18n.translateHtml(printHtml);
        }

        let frame = document.getElementById('print-iframe-target');
        if (!frame) {
            frame = document.createElement('iframe');
            frame.id = 'print-iframe-target';
            frame.style.position = 'fixed';
            frame.style.right = '0';
            frame.style.bottom = '0';
            frame.style.width = '0';
            frame.style.height = '0';
            frame.style.border = '0';
            frame.style.visibility = 'hidden';
            document.body.appendChild(frame);
        }

        const frameDoc = frame.contentWindow.document;
        frameDoc.open();
        frameDoc.write(printHtml);
        frameDoc.close();

        if (isJa) {
            i18n.localizeDom(frameDoc.body);
        }

        const originalDocTitle = document.title;
        this.triggerFramePrint(frame, frameDoc, originalDocTitle, ngFilename);
    }

    /**
     * Export Full Quality NG & Defect Report to Structured Excel Spreadsheet (.xls)
     */
    exportExcelNgSpreadsheet(ngData, header, filters) {
        const items = ngData.items || [];
        const totalOutput = items.reduce((acc, it) => acc + (it.total_output || 0), 0);
        const totalGood = items.reduce((acc, it) => acc + (it.good_quantity || 0), 0);
        const totalNg = items.reduce((acc, it) => acc + (it.total_ng_target || 0), 0);
        const totalOeeNg = items.reduce((acc, it) => acc + (it.ng_detail?.total_ng_oee || 0), 0);
        const totalNonOeeNg = items.reduce((acc, it) => acc + (it.ng_detail?.total_ng_non_oee || 0), 0);

        const totalAssy = items.reduce((acc, it) => acc + (it.ng_detail?.ng_assy || 0), 0);
        const totalRod = items.reduce((acc, it) => acc + (it.ng_detail?.ng_rod || 0), 0);
        const totalCap = items.reduce((acc, it) => acc + (it.ng_detail?.ng_cap || 0), 0);

        const totalBolt = items.reduce((acc, it) => acc + (it.ng_detail?.ng_bolt || 0), 0);
        const totalBush = items.reduce((acc, it) => acc + (it.ng_detail?.ng_bush || 0), 0);
        const totalNut = items.reduce((acc, it) => acc + (it.ng_detail?.ng_nut || 0), 0);
        const totalPin = items.reduce((acc, it) => acc + (it.ng_detail?.ng_pin || 0), 0);

        const defectRate = totalOutput > 0 ? ((totalNg / totalOutput) * 100).toFixed(2) : '0.00';
        const yieldRate = totalOutput > 0 ? ((totalGood / totalOutput) * 100).toFixed(2) : '100.00';

        // Group items by production_date
        const groupedNgItems = items.reduce((acc, it) => {
            const d = it.production_date || it.formatted_date || 'Unknown';
            if (!acc[d]) acc[d] = { date: d, formatted_date: it.formatted_date || d, items: [] };
            acc[d].items.push(it);
            return acc;
        }, {});

        const excelContent = `
<html xmlns:o="urn:schemas-microsoft-com:office:office" xmlns:x="urn:schemas-microsoft-com:office:excel" xmlns="http://www.w3.org/TR/REC-html40">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <style>
        body { font-family: Arial, Calibri, sans-serif; font-size: 8.5pt; color: #000000; }
        table { border-collapse: collapse; margin-bottom: 10px; width: 100%; }
        .doc-header-1 { font-family: Arial, sans-serif; font-size: 11pt; font-weight: bold; color: #000000; text-align: left; }
        .doc-header-2 { font-family: Arial, sans-serif; font-size: 8.5pt; font-weight: bold; color: #000000; text-align: left; }
        .doc-title { font-family: Arial, sans-serif; font-size: 10.5pt; font-weight: bold; color: #B45309; text-transform: uppercase; padding: 4px 0 6px 0; text-align: left; }
        .sec-title { font-family: Arial, sans-serif; font-size: 9pt; font-weight: bold; background-color: #D97706; color: #FFFFFF; padding: 3px 6px; border: 0.5pt solid #B45309; }
        .th-cell { background-color: #F1F5F9; font-weight: bold; border: 0.5pt solid #000000; text-align: left; padding: 3px 4px; font-size: 8pt; }
        .th-cell-num { background-color: #F1F5F9; font-weight: bold; border: 0.5pt solid #000000; text-align: right; padding: 3px 4px; font-size: 8pt; }
        .th-cell-center { background-color: #F1F5F9; font-weight: bold; border: 0.5pt solid #000000; text-align: center; padding: 3px 4px; font-size: 8pt; }
        .td-cell { border: 0.5pt solid #000000; padding: 3px 4px; vertical-align: middle; font-size: 8pt; }
        .td-cell-bold { border: 0.5pt solid #000000; padding: 3px 4px; font-weight: bold; font-size: 8pt; }
        .td-cell-num { border: 0.5pt solid #000000; padding: 3px 4px; text-align: right; font-size: 8pt; mso-number-format: "#,##0"; }
        .td-cell-pct { border: 0.5pt solid #000000; padding: 3px 4px; text-align: right; font-size: 8pt; }
        .td-cell-center { border: 0.5pt solid #000000; padding: 3px 4px; text-align: center; font-size: 8pt; }
        .meta-label { font-weight: bold; border: 0.5pt solid #000000; padding: 3px 4px; font-size: 8pt; text-align: right; background-color: #F1F5F9; }
        .meta-val { border: 0.5pt solid #000000; padding: 3px 4px; font-size: 8pt; }
        .total-row { font-weight: bold; background-color: #E2E8F0; border: 0.5pt solid #000000; }
    </style>
</head>
<body>
    <table>
        <tr>
            <td colspan="15" class="doc-header-1">${header.company_name || 'PT. YASUNAGA INDONESIA'}</td>
        </tr>
        <tr>
            <td colspan="15" class="doc-header-2">${header.plant_name || 'ENGINE PARTS & AIR PUMP MFG'} • ${header.plant_id || 'PLT-01'}</td>
        </tr>
        <tr><td colspan="15" style="border: none; height: 4px;"></td></tr>
        <tr>
            <td colspan="15" class="doc-title">LAPORAN DATA NG & DEFECT KUALITAS PRODUKSI</td>
        </tr>
    </table>

    <table>
        <tr>
            <td colspan="3" class="meta-label">Periode Tanggal:</td>
            <td colspan="4" class="meta-val"><strong>${header.formatted_date || header.date_range_label || header.production_date || ''}</strong></td>
            <td colspan="3" class="meta-label">Lini Produksi:</td>
            <td colspan="5" class="meta-val"><strong>${header.line_name || 'All Lines'}</strong></td>
        </tr>
        <tr>
            <td colspan="3" class="meta-label">Shift Kerja:</td>
            <td colspan="4" class="meta-val"><strong>${header.shift_name || 'All Shifts'}</strong></td>
            <td colspan="3" class="meta-label">Supervisor / QC PIC:</td>
            <td colspan="5" class="meta-val"><strong>${header.supervisor_in_charge || 'Alim Utama'}</strong></td>
        </tr>
    </table>

    <table>
        <tr>
            <td colspan="15" class="sec-title">I. RINGKASAN REKAPITULASI DEFECT (QC EXECUTIVE SUMMARY)</td>
        </tr>
        <tr>
            <th colspan="3" class="th-cell">Total Output Produksi</th>
            <th colspan="3" class="th-cell-num">Good Output (OK)</th>
            <th colspan="3" class="th-cell-num">Total Reject / NG</th>
            <th colspan="3" class="th-cell-num">Defect Rate (%)</th>
            <th colspan="3" class="th-cell-num">Quality Yield (%)</th>
        </tr>
        <tr>
            <td colspan="3" class="td-cell-bold">${totalOutput.toLocaleString()} Pcs</td>
            <td colspan="3" class="td-cell-num" style="color: #047857; font-weight: bold;">${totalGood.toLocaleString()} Pcs</td>
            <td colspan="3" class="td-cell-num" style="color: #B91C1C; font-weight: bold;">${totalNg.toLocaleString()} Pcs</td>
            <td colspan="3" class="td-cell-pct" style="font-weight: bold;">${defectRate}%</td>
            <td colspan="3" class="td-cell-pct" style="color: #047857; font-weight: bold;">${yieldRate}%</td>
        </tr>
        <tr class="total-row">
            <td colspan="15" class="td-cell">
                <strong>Rincian Komponen:</strong> &nbsp;
                Wajib OEE: <strong>${totalOeeNg} pcs</strong> (Assy: ${totalAssy}, Rod: ${totalRod}, Cap: ${totalCap}) &nbsp;|&nbsp;
                Pelengkap Non-OEE: <strong>${totalNonOeeNg} pcs</strong> (Bolt: ${totalBolt}, Bush: ${totalBush}, Nut: ${totalNut}, Pin: ${totalPin})
            </td>
        </tr>
    </table>

    <table>
        <tr>
            <td colspan="15" class="sec-title">II. TABEL RINCIAN DEFECT & KONTROL KUALITAS PER LINE</td>
        </tr>
        <tr>
            <th class="th-cell-center" style="width: 3%;">No</th>
            <th class="th-cell" style="width: 7%;">Tanggal</th>
            <th class="th-cell" style="width: 6%;">Shift</th>
            <th class="th-cell" style="width: 8%;">Line</th>
            <th class="th-cell" style="width: 8%;">Mesin</th>
            <th class="th-cell" style="width: 12%;">Type Produk (SKU)</th>
            <th class="th-cell-num" style="width: 6%;">Output</th>
            <th class="th-cell-num" style="width: 6%;">OK (Pcs)</th>
            <th class="th-cell-num" style="width: 6%;">NG (Pcs)</th>
            <th class="th-cell-num" style="width: 5%;">Rate (%)</th>
            <th class="th-cell-center" style="width: 9%;">Wajib OEE (A/R/C)</th>
            <th class="th-cell-center" style="width: 9%;">Non-OEE (B/Bs/N/P)</th>
            <th class="th-cell" style="width: 10%;">Gejala Defect</th>
            <th class="th-cell" style="width: 10%;">Tindakan CAPA</th>
            <th class="th-cell" style="width: 5%;">Inspector</th>
        </tr>
        ${Object.values(groupedNgItems).map((group) => `
            <tr style="background-color: #D97706; color: #FFFFFF; font-weight: bold;">
                <td colspan="15" style="padding: 4px 6px; font-size: 8.5pt; border: 0.5pt solid #000000; color: #FFFFFF; background-color: #D97706;">
                    📅 TANGGAL: ${group.formatted_date} (${group.items.length} Entri Defect)
                </td>
            </tr>
            ${group.items.map((it, idx) => {
                const dRate = it.total_output > 0 ? ((it.total_ng_target / it.total_output) * 100).toFixed(2) : '0.00';
                const ng = it.ng_detail || {};
                return `
                    <tr>
                        <td class="td-cell-center">${idx + 1}</td>
                        <td class="td-cell">${it.production_date}</td>
                        <td class="td-cell">${it.shift_name}</td>
                        <td class="td-cell-bold">${it.line_name}</td>
                        <td class="td-cell">${it.machine_code}</td>
                        <td class="td-cell">${it.product_name} - ${it.product_sku}</td>
                        <td class="td-cell-num">${(it.total_output || 0).toLocaleString()}</td>
                        <td class="td-cell-num" style="color: #047857;">${(it.good_quantity || 0).toLocaleString()}</td>
                        <td class="td-cell-num" style="color: #B91C1C; font-weight: bold;">${(it.total_ng_target || 0).toLocaleString()}</td>
                        <td class="td-cell-pct">${dRate}%</td>
                        <td class="td-cell-center">A:${ng.ng_assy || 0} | R:${ng.ng_rod || 0} | C:${ng.ng_cap || 0}</td>
                        <td class="td-cell-center">B:${ng.ng_bolt || 0} | Bs:${ng.ng_bush || 0} | N:${ng.ng_nut || 0} | P:${ng.ng_pin || 0}</td>
                        <td class="td-cell">${ng.defect_category_name || ng.defect_symptom || '-'}</td>
                        <td class="td-cell">${ng.action_taken || '-'}</td>
                        <td class="td-cell">${ng.inspector_name || it.operator_name || 'QC'}</td>
                    </tr>
                `;
            }).join('')}
        `).join('')}
        <tr class="total-row">
            <td colspan="6" class="td-cell-center font-bold">TOTAL KESELURUHAN:</td>
            <td class="td-cell-num">${totalOutput.toLocaleString()}</td>
            <td class="td-cell-num" style="color: #047857;">${totalGood.toLocaleString()}</td>
            <td class="td-cell-num" style="color: #B91C1C;">${totalNg.toLocaleString()}</td>
            <td class="td-cell-pct">${defectRate}%</td>
            <td class="td-cell-center">A:${totalAssy} | R:${totalRod} | C:${totalCap}</td>
            <td class="td-cell-center">B:${totalBolt} | Bs:${totalBush} | N:${totalNut} | P:${totalPin}</td>
            <td colspan="3" class="td-cell-center">Total Wajib: ${totalOeeNg} pcs | Non-OEE: ${totalNonOeeNg} pcs</td>
        </tr>
    </table>

    <table style="margin-top: 15px;">
        <tr>
            <td colspan="5" class="doc-header-2" style="text-align: center; border: none;">Dibuat Oleh (Operator)</td>
            <td colspan="5" class="doc-header-2" style="text-align: center; border: none;">Diperiksa Oleh (QC Leader)</td>
            <td colspan="5" class="doc-header-2" style="text-align: center; border: none;">Disetujui Oleh (Supervisor)</td>
        </tr>
        <tr><td colspan="15" style="height: 35px; border: none;"></td></tr>
        <tr>
            <td colspan="5" style="text-align: center; border: none; font-weight: bold;">( ${items[0]?.operator_name || 'Operator Mesin'} )</td>
            <td colspan="5" style="text-align: center; border: none; font-weight: bold;">( ${items[0]?.ng_detail?.inspector_name || 'Leader QC'} )</td>
            <td colspan="5" style="text-align: center; border: none; font-weight: bold;">( ${header.supervisor_in_charge || 'Alim Utama'} )</td>
        </tr>
    </table>
</body>
</html>`;

        const isJa = this.currentLang === 'ja';
        let finalExcelContent = excelContent;
        if (isJa) {
            finalExcelContent = i18n.translateHtml(excelContent);
        }

        const blob = new Blob([finalExcelContent], { type: 'application/vnd.ms-excel;charset=utf-8' });
        const url = URL.createObjectURL(blob);
        const a = document.createElement('a');
        const dateStr = header.formatted_date || header.production_date || new Date().toISOString().slice(0, 10);
        const lineStr = (header.line_name && header.line_name !== 'All Production Lines' && header.line_name !== 'All Lines') ? ` - ${header.line_name}` : '';
        const shiftStr = (header.shift_name && !header.shift_name.toLowerCase().includes('all')) ? ` - ${header.shift_name}` : '';
        let filename = this.sanitizeReportFilename(`Laporan Data NG dan Defect Mutu Tgl ${dateStr}${lineStr}${shiftStr}`);
        if (isJa) filename = i18n.translateText(filename);
        filename += '.xls';
        a.href = url;
        a.download = filename;
        document.body.appendChild(a);
        a.click();
        document.body.removeChild(a);
        URL.revokeObjectURL(url);
    }

    /**
     * Print Dedicated Official A4 Trouble & Downtime History Report
     */
    prepareAndPrintTroubleReport(troubleData, header, filters) {
        const items = troubleData.items || [];
        const summary = troubleData.summary || {};
        const categories = troubleData.category_breakdown || [];
        const topMachines = troubleData.top_machines || [];

        const isJa = this.currentLang === 'ja';
        const dateStr = header.formatted_date || header.production_date || new Date().toISOString().slice(0, 10);
        const lineStr = (header.line_name && header.line_name !== 'All Production Lines' && header.line_name !== 'All Lines') ? ` - ${header.line_name}` : '';
        const shiftStr = (header.shift_name && !header.shift_name.toLowerCase().includes('all')) ? ` - ${header.shift_name}` : '';
        let troubleFilename = this.sanitizeReportFilename(`Laporan Trouble dan Downtime Mesin Tgl ${dateStr}${lineStr}${shiftStr}`);
        if (isJa) troubleFilename = i18n.translateText(troubleFilename);

        // Group items by production_date
        const groupedTrouble = items.reduce((acc, it) => {
            const d = it.production_date || it.formatted_date || 'Unknown';
            if (!acc[d]) acc[d] = { date: d, formatted_date: it.formatted_date || d, items: [] };
            acc[d].items.push(it);
            return acc;
        }, {});

        let printHtml = `<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>${troubleFilename}</title>
    <style>
        @page {
            size: A4 landscape;
            margin: 8mm 8mm 8mm 8mm;
        }
        * {
            box-sizing: border-box;
            -webkit-print-color-adjust: exact !important;
            print-color-adjust: exact !important;
        }
        body {
            font-family: Arial, Helvetica, sans-serif;
            font-size: 8.5px;
            color: #000000;
            background: #ffffff;
            margin: 0;
            padding: 0;
            line-height: 1.25;
        }
        .section-header {
            font-size: 9px;
            font-weight: bold;
            text-transform: uppercase;
            background-color: #E2E8F0;
            padding: 2.5px 5px;
            border-left: 3px solid #BE123C;
            margin: 6px 0 2.5px 0;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 5px;
            font-size: 8.5px;
        }
        th, td {
            border: 1px solid #475569;
            padding: 3px 4px;
            vertical-align: middle;
        }
        th {
            background-color: #F1F5F9;
            font-weight: bold;
            text-transform: uppercase;
            font-size: 8px;
            color: #000000;
            text-align: left;
        }
        .text-right { text-align: right; }
        .text-center { text-align: center; }
        .font-bold { font-weight: bold; }
        .bg-light { background-color: #F8FAFC; }
        .bg-highlight { background-color: #FFE4E6; font-weight: bold; }
        .signatures {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
            font-size: 8.5px;
            page-break-inside: avoid;
        }
        .signatures td {
            border: none;
            text-align: center;
            padding: 3px 5px;
            vertical-align: top;
        }
        .sig-line {
            border-bottom: 1px solid #000000;
            width: 75%;
            margin: 28px auto 3px auto;
        }
    </style>
</head>
<body>
    <!-- KOP SURAT RESMI PT YASUNAGA INDONESIA (SERAGAM DENGAN LAPORAN LAINNYA) -->
    <div style="border-bottom: 2.5px double #000000; padding-bottom: 5px; margin-bottom: 6px;">
        <table style="width: 100%; border: none; margin-bottom: 0;">
            <tr>
                <td style="width: 65px; border: none; padding: 0 8px 0 0; vertical-align: middle;">
                    <img src="${this.escapeHtml(this.getCompanyLogo(header))}" style="max-height: 48px; max-width: 65px; object-fit: contain;" alt="Logo" onerror="this.onerror=null; this.src='/images/yasunaga-logo.png';" />
                </td>
                <td style="border: none; padding: 0; vertical-align: middle;">
                    <div style="font-size: 12px; font-weight: 900; text-transform: uppercase; letter-spacing: 0.5px; line-height: 1.15;">${header.company_name || this.companyProfile?.company_name || 'PT. YASUNAGA INDONESIA'}</div>
                    <div style="font-size: 8.5px; font-weight: bold; color: #1e293b; margin-top: 1px;">${header.plant_name || this.companyProfile?.plant_name || 'ENGINE PARTS & AIR PUMP MFG'} • <strong>${header.plant_id || this.companyProfile?.plant_code || 'PLT-01'}</strong></div>
                    <div style="font-size: 7.5px; color: #475569; margin-top: 1px;">${header.address || this.companyProfile?.address || header.plant_location || ''}</div>
                    <div style="font-size: 7.5px; color: #64748b;">${(header.phone || this.companyProfile?.phone) ? 'Telp: ' + (header.phone || this.companyProfile?.phone) : ''} ${(header.email || this.companyProfile?.email) ? '• Email: ' + (header.email || this.companyProfile?.email) : ''}</div>
                </td>
                <td style="width: 260px; border: none; padding: 0; text-align: right; vertical-align: top;">
                    <div style="font-size: 11px; font-weight: 800; text-transform: uppercase; line-height: 1.2; color: #BE123C;">LAPORAN DATA TROUBLE & DOWNTIME</div>
                    <div style="font-size: 8px; color: #333333; font-family: monospace; margin-top: 2px;">Doc ID: TRB-${header.production_date || new Date().toISOString().slice(0,10)}</div>
                    <div style="font-size: 7.5px; color: #64748b; font-family: monospace;">Form: A4-Landscape Trouble & Downtime Report</div>
                </td>
            </tr>
        </table>
    </div>

    <!-- METADATA LAPORAN -->
    <table style="margin-bottom: 6px; font-size: 8px;">
        <tr class="bg-light">
            <td style="width: 14%; font-weight: bold;">Periode Tanggal</td>
            <td style="width: 20%; font-weight: bold; color: #BE123C;">${header.formatted_date || header.date_range_label || '-'}</td>
            <td style="width: 14%; font-weight: bold;">Lini Produksi</td>
            <td style="width: 20%;">${header.line_name || 'All Production Lines'}</td>
            <td style="width: 14%; font-weight: bold;">Shift Kerja</td>
            <td style="width: 18%;">${header.shift_name || 'All Shifts'}</td>
        </tr>
        <tr>
            <td style="font-weight: bold;">Supervisor PIC</td>
            <td><strong>${header.supervisor_in_charge || 'Alim Utama'}</strong></td>
            <td style="font-weight: bold;">Target Max Breakdown</td>
            <td>&lt; 30 Menit / Shift</td>
            <td style="font-weight: bold;">Total Entri Trouble</td>
            <td><strong>${items.length} Kejadian Trouble</strong></td>
        </tr>
    </table>

    <!-- I. RINGKASAN EKSEKUTIF (EXECUTIVE SUMMARY) -->
    <div class="section-header">I. RINGKASAN EKSEKUTIF WAKTU HENTI (DOWNTIME & RELIABILITY KPI)</div>
    <table style="text-align: center; margin-bottom: 6px;">
        <tr class="bg-light">
            <th style="width: 20%; text-align: center;">Total Kejadian (Calls)</th>
            <th style="width: 20%; text-align: center;">Total Waktu Henti</th>
            <th style="width: 20%; text-align: center;">Rata-rata Penanganan (MTTR)</th>
            <th style="width: 20%; text-align: center;">Unplanned Breakdown</th>
            <th style="width: 20%; text-align: center;">Planned Maintenance</th>
        </tr>
        <tr>
            <td class="font-bold" style="font-size: 9.5px; color: #BE123C;">${items.length} Kali Kejadian</td>
            <td class="font-bold" style="font-size: 9.5px; color: #B45309;">${summary.total_downtime_minutes || 0} Menit (${summary.total_downtime_hours || 0} Jam)</td>
            <td class="font-bold" style="font-size: 9.5px; color: #0284C7;">${summary.mttr_minutes || 0} Menit / Trouble</td>
            <td class="font-bold" style="color: #BE123C;">${summary.unplanned_minutes || 0} m (${summary.unplanned_count || 0}x)</td>
            <td class="font-bold" style="color: #047857;">${summary.planned_minutes || 0} m (${summary.planned_count || 0}x)</td>
        </tr>
    </table>

    <!-- II. TABEL RINCIAN DATA TROUBLE & TINDAKAN PERBAIKAN -->
    <div class="section-header">II. TABEL RINCIAN DATA TROUBLE MESIN & TINDAKAN PERBAIKAN (CAPA)</div>
    <table>
        <thead>
            <tr>
                <th style="width: 3%; text-align: center;">No</th>
                <th style="width: 8%;">Tanggal & Jam</th>
                <th style="width: 10%;">Line & Mesin</th>
                <th style="width: 11%;">Part / SKU</th>
                <th style="width: 6%;">Shift</th>
                <th style="width: 8%;">Kategori</th>
                <th style="width: 20%;">Gejala & Root Cause Masalah</th>
                <th style="width: 7%; text-align: right;">Durasi</th>
                <th style="width: 18%;">Tindakan Perbaikan (CAPA)</th>
                <th style="width: 5%;">PIC / Tim</th>
                <th style="width: 4%; text-align: center;">Status</th>
            </tr>
        </thead>
        <tbody>
            ${items.length === 0 ? `
                <tr>
                    <td colspan="11" style="text-align: center; padding: 15px; color: #047857; font-weight: bold;">
                        ✓ Zero Breakdown! Semua mesin beroperasi normal tanpa catatan waktu henti (downtime) pada periode ini.
                    </td>
                </tr>
            ` : Object.values(groupedTrouble).map((grp) => {
                const subMins = grp.items.reduce((s, it) => s + (it.duration_minutes || 0), 0);
                return `
                    <tr style="background-color: #FCE7F3; font-weight: bold; border-top: 1.5px solid #BE123C;">
                        <td colspan="11" style="padding: 2.5px 5px; color: #881337;">
                            📅 Tanggal Produksi: ${grp.formatted_date} (${grp.items.length} Kejadian Trouble — Subtotal: ${subMins} Menit / ${(subMins/60).toFixed(1)} Jam)
                        </td>
                    </tr>
                    ${grp.items.map((it, idx) => `
                        <tr>
                            <td class="text-center">${idx + 1}</td>
                            <td>${it.formatted_date}<br><span style="color: #64748b; font-size: 7.5px;">${it.time_window}</span></td>
                            <td><strong>${it.machine_code}</strong><br><span style="color: #64748b; font-size: 7.5px;">${it.line_name}</span></td>
                            <td>${it.product_name}</td>
                            <td>${it.shift_name}</td>
                            <td><strong>${it.problem_type}</strong></td>
                            <td style="color: #991B1B; font-weight: 600;">${it.description || it.root_cause || '-'}${it.cause && it.cause !== '-' ? `<br><span style="color: #475569; font-size: 7.5px;">Penyebab: ${it.cause}</span>` : ''}</td>
                            <td class="text-right font-bold" style="color: #B45309;">${it.duration_minutes}m<br><span style="color: #64748b; font-size: 7px; font-weight: normal;">${it.duration_hours}h</span></td>
                            <td style="color: #065F46;">${it.action_plan || 'Penyetelan dan perbaikan teknisi.'}</td>
                            <td>${it.pic || 'Maintenance'}</td>
                            <td class="text-center font-bold" style="color: ${String(it.status).toUpperCase() === 'CLOSED' || it.status === 'RESOLVED' ? '#047857' : '#D97706'};">${String(it.status).toUpperCase() === 'CLOSED' || it.status === 'RESOLVED' ? 'CLOSED' : 'OPEN'}</td>
                        </tr>
                    `).join('')}
                `;
            }).join('')}
            ${items.length > 0 ? `
                <tr class="bg-highlight">
                    <td colspan="7" style="text-align: right; font-weight: bold; text-transform: uppercase;">Total Keseluruhan Waktu Henti:</td>
                    <td class="text-right font-bold" style="color: #B45309; font-size: 9px;">${summary.total_downtime_minutes || 0}m</td>
                    <td colspan="3" style="font-size: 8px;">Unplanned: <strong>${summary.unplanned_minutes || 0}m</strong> | Planned: <strong>${summary.planned_minutes || 0}m</strong></td>
                </tr>
            ` : ''}
        </tbody>
    </table>

    <!-- III. RANGKUMAN ANALISIS KATEGORI & MESIN TERKENDALA -->
    <table style="width: 100%; border: none; margin-top: 4px;">
        <tr style="border: none;">
            <td style="width: 50%; border: none; vertical-align: top; padding-right: 4px;">
                <div class="section-header">III. DISTRIBUSI KATEGORI MASALAH</div>
                <table style="font-size: 8px;">
                    <tr class="bg-light">
                        <th style="width: 50%;">Kategori Masalah</th>
                        <th style="width: 25%; text-align: center;">Frekuensi</th>
                        <th style="width: 25%; text-align: right;">Durasi (Menit)</th>
                    </tr>
                    ${categories.length === 0 ? `<tr><td colspan="3" class="text-center">-</td></tr>` : categories.map(c => `
                        <tr>
                            <td><strong>${c.category}</strong></td>
                            <td class="text-center">${c.count}x (${c.percentage}%)</td>
                            <td class="text-right font-bold" style="color: #B45309;">${c.minutes}m</td>
                        </tr>
                    `).join('')}
                </table>
            </td>
            <td style="width: 50%; border: none; vertical-align: top; padding-left: 4px;">
                <div class="section-header">IV. TOP 5 MESIN DENGAN DOWNTIME TERTINGGI</div>
                <table style="font-size: 8px;">
                    <tr class="bg-light">
                        <th style="width: 45%;">Kode Mesin</th>
                        <th style="width: 30%;">Lini Produksi</th>
                        <th style="width: 25%; text-align: right;">Waktu Henti</th>
                    </tr>
                    ${topMachines.length === 0 ? `<tr><td colspan="3" class="text-center">Zero Downtime</td></tr>` : topMachines.map(m => `
                        <tr>
                            <td><strong>${m.machine_code}</strong></td>
                            <td>${m.line_name}</td>
                            <td class="text-right font-bold" style="color: #BE123C;">${m.minutes}m (${m.count}x)</td>
                        </tr>
                    `).join('')}
                </table>
            </td>
        </tr>
    </table>

    <!-- LEMBAR PENGESAHAN TANDA TANGAN (3 KOLOM RESMI) -->
    <div style="text-align: right; font-size: 8px; margin-top: 6px; margin-bottom: 2px; color: #1e293b;">
        Tanggal Verifikasi: <strong>${header.formatted_date || ''}</strong>
    </div>
    <table class="signatures">
        <tr>
            <td style="width: 33.3%;">
                Dibuat Oleh (Shift Leader):
                <div class="sig-line">${items[0]?.pic || 'Leader Shift'}</div>
                <div style="font-size: 7.5px; color: #444;">Leader Produksi</div>
            </td>
            <td style="width: 33.3%;">
                Ditangani Oleh (Teknisi / Maint):
                <div class="sig-line">${items[0]?.team ? items[0].team + ' Team' : 'Teknisi Maintenance'}</div>
                <div style="font-size: 7.5px; color: #444;">Maintenance & Engineering</div>
            </td>
            <td style="width: 33.3%;">
                Mengetahui / Disetujui:
                <div class="sig-line">${header.supervisor_in_charge || 'Alim Utama'}</div>
                <div style="font-size: 7.5px; color: #444;">Supervisor Produksi / Dept Head</div>
            </td>
        </tr>
    </table>
</body>
</html>`;

        if (isJa) {
            printHtml = i18n.translateHtml(printHtml);
        }

        let frame = document.getElementById('print-iframe-target');
        if (!frame) {
            frame = document.createElement('iframe');
            frame.id = 'print-iframe-target';
            frame.style.position = 'fixed';
            frame.style.right = '0';
            frame.style.bottom = '0';
            frame.style.width = '0';
            frame.style.height = '0';
            frame.style.border = '0';
            frame.style.visibility = 'hidden';
            document.body.appendChild(frame);
        }

        const frameDoc = frame.contentWindow.document;
        frameDoc.open();
        frameDoc.write(printHtml);
        frameDoc.close();

        if (isJa) {
            i18n.localizeDom(frameDoc.body);
        }

        const originalDocTitle = document.title;
        this.triggerFramePrint(frame, frameDoc, originalDocTitle, troubleFilename);
    }

    /**
     * Export Full Trouble & Downtime History Report to Structured Excel Spreadsheet (.xls)
     */
    exportTroubleReportExcel(troubleData, header, filters) {
        const items = troubleData.items || [];
        const summary = troubleData.summary || {};
        const categories = troubleData.category_breakdown || [];
        const topMachines = troubleData.top_machines || [];

        // Group items by production_date
        const groupedTrouble = items.reduce((acc, it) => {
            const d = it.production_date || it.formatted_date || 'Unknown';
            if (!acc[d]) acc[d] = { date: d, formatted_date: it.formatted_date || d, items: [] };
            acc[d].items.push(it);
            return acc;
        }, {});

        const excelContent = `
<html xmlns:o="urn:schemas-microsoft-com:office:office" xmlns:x="urn:schemas-microsoft-com:office:excel" xmlns="http://www.w3.org/TR/REC-html40">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <style>
        body { font-family: Arial, Calibri, sans-serif; font-size: 8.5pt; color: #000000; }
        table { border-collapse: collapse; margin-bottom: 10px; width: 100%; }
        .doc-header-1 { font-family: Arial, sans-serif; font-size: 11pt; font-weight: bold; color: #000000; text-align: left; }
        .doc-header-2 { font-family: Arial, sans-serif; font-size: 8.5pt; font-weight: bold; color: #000000; text-align: left; }
        .doc-title { font-family: Arial, sans-serif; font-size: 10.5pt; font-weight: bold; color: #BE123C; text-transform: uppercase; padding: 4px 0 6px 0; text-align: left; }
        .sec-title { font-family: Arial, sans-serif; font-size: 9pt; font-weight: bold; background-color: #BE123C; color: #FFFFFF; padding: 3px 6px; border: 0.5pt solid #881337; }
        .th-cell { background-color: #F1F5F9; font-weight: bold; border: 0.5pt solid #000000; text-align: left; padding: 3px 4px; font-size: 8pt; }
        .th-cell-num { background-color: #F1F5F9; font-weight: bold; border: 0.5pt solid #000000; text-align: right; padding: 3px 4px; font-size: 8pt; }
        .th-cell-center { background-color: #F1F5F9; font-weight: bold; border: 0.5pt solid #000000; text-align: center; padding: 3px 4px; font-size: 8pt; }
        .td-cell { border: 0.5pt solid #000000; padding: 3px 4px; vertical-align: middle; font-size: 8pt; }
        .td-cell-bold { border: 0.5pt solid #000000; padding: 3px 4px; font-weight: bold; font-size: 8pt; }
        .td-cell-num { border: 0.5pt solid #000000; padding: 3px 4px; text-align: right; font-size: 8pt; mso-number-format: "#,##0"; }
        .td-cell-pct { border: 0.5pt solid #000000; padding: 3px 4px; text-align: right; font-size: 8pt; }
        .td-cell-center { border: 0.5pt solid #000000; padding: 3px 4px; text-align: center; font-size: 8pt; }
        .meta-label { font-weight: bold; border: 0.5pt solid #000000; padding: 3px 4px; font-size: 8pt; text-align: right; background-color: #F1F5F9; }
        .meta-val { border: 0.5pt solid #000000; padding: 3px 4px; font-size: 8pt; }
        .total-row { font-weight: bold; background-color: #E2E8F0; border: 0.5pt solid #000000; }
    </style>
</head>
<body>
    <table>
        <tr>
            <td colspan="11" class="doc-header-1">${header.company_name || 'PT. YASUNAGA INDONESIA'}</td>
        </tr>
        <tr>
            <td colspan="11" class="doc-header-2">${header.plant_name || 'ENGINE PARTS & AIR PUMP MFG'} • ${header.plant_id || 'PLT-01'}</td>
        </tr>
        <tr><td colspan="11" style="border: none; height: 4px;"></td></tr>
        <tr>
            <td colspan="11" class="doc-title">LAPORAN DATA RIWAYAT TROUBLE & DOWNTIME MESIN PRODUKSI</td>
        </tr>
    </table>

    <table>
        <tr>
            <td colspan="2" class="meta-label">Periode Tanggal:</td>
            <td colspan="4" class="meta-val"><strong>${header.formatted_date || header.date_range_label || header.production_date || ''}</strong></td>
            <td colspan="2" class="meta-label">Lini Produksi:</td>
            <td colspan="3" class="meta-val"><strong>${header.line_name || 'All Lines'}</strong></td>
        </tr>
        <tr>
            <td colspan="2" class="meta-label">Shift Kerja:</td>
            <td colspan="4" class="meta-val"><strong>${header.shift_name || 'All Shifts'}</strong></td>
            <td colspan="2" class="meta-label">Supervisor / Maintenance:</td>
            <td colspan="3" class="meta-val"><strong>${header.supervisor_in_charge || 'Alim Utama'}</strong></td>
        </tr>
    </table>

    <table>
        <tr>
            <td colspan="11" class="sec-title">I. RINGKASAN EKSEKUTIF WAKTU HENTI (DOWNTIME & RELIABILITY KPI)</td>
        </tr>
        <tr>
            <th colspan="2" class="th-cell-center">Total Kejadian</th>
            <th colspan="2" class="th-cell-num">Total Downtime (Menit)</th>
            <th colspan="2" class="th-cell-num">Total Downtime (Jam)</th>
            <th colspan="2" class="th-cell-num">Rata-rata MTTR (Menit)</th>
            <th colspan="3" class="th-cell-center">Unplanned vs Planned</th>
        </tr>
        <tr>
            <td colspan="2" class="td-cell-center font-bold" style="color: #BE123C; font-size: 9.5pt;">${items.length} Kali Trouble</td>
            <td colspan="2" class="td-cell-num" style="color: #BE123C; font-weight: bold; font-size: 9.5pt;">${summary.total_downtime_minutes || 0} m</td>
            <td colspan="2" class="td-cell-num font-bold">${summary.total_downtime_hours || 0} Jam</td>
            <td colspan="2" class="td-cell-num font-bold" style="color: #0284C7;">${summary.mttr_minutes || 0} Menit</td>
            <td colspan="3" class="td-cell-center font-bold">Unplanned: ${summary.unplanned_minutes || 0}m (${summary.unplanned_count || 0}x) | Planned: ${summary.planned_minutes || 0}m</td>
        </tr>
    </table>

    <table>
        <tr>
            <td colspan="11" class="sec-title">II. TABEL RINCIAN RIWAYAT TROUBLE MESIN & TINDAKAN PERBAIKAN (CAPA)</td>
        </tr>
        <tr>
            <th class="th-cell-center" style="width: 3%;">No</th>
            <th class="th-cell" style="width: 8%;">Tanggal</th>
            <th class="th-cell" style="width: 7%;">Jam Kejadian</th>
            <th class="th-cell" style="width: 7%;">Shift</th>
            <th class="th-cell" style="width: 8%;">Line Produksi</th>
            <th class="th-cell" style="width: 10%;">Mesin Terkendala</th>
            <th class="th-cell" style="width: 12%;">Type Produk (SKU)</th>
            <th class="th-cell" style="width: 10%;">Kategori Masalah</th>
            <th class="th-cell" style="width: 17%;">Gejala & Root Cause Trouble</th>
            <th class="th-cell-num" style="width: 6%;">Durasi (m)</th>
            <th class="th-cell" style="width: 12%;">Tindakan Perbaikan (CAPA)</th>
        </tr>
        ${Object.values(groupedTrouble).map((group) => `
            <tr style="background-color: #BE123C; color: #FFFFFF; font-weight: bold;">
                <td colspan="11" style="padding: 4px 6px; font-size: 8.5pt; border: 0.5pt solid #000000; color: #FFFFFF; background-color: #BE123C;">
                    📅 TANGGAL: ${group.formatted_date} (${group.items.length} Kejadian Trouble - Subtotal: ${group.items.reduce((acc, it) => acc + (it.duration_minutes || 0), 0)} Menit)
                </td>
            </tr>
            ${group.items.map((it, idx) => `
                <tr>
                    <td class="td-cell-center">${idx + 1}</td>
                    <td class="td-cell">${it.production_date}</td>
                    <td class="td-cell">${it.time_window || `${it.start_time} - ${it.end_time}`}</td>
                    <td class="td-cell">${it.shift_name}</td>
                    <td class="td-cell-bold">${it.line_name}</td>
                    <td class="td-cell">${it.machine_code}</td>
                    <td class="td-cell">${it.product_name}</td>
                    <td class="td-cell">${it.problem_type}</td>
                    <td class="td-cell">${it.root_cause || it.description || '-'}</td>
                    <td class="td-cell-num" style="color: #BE123C; font-weight: bold;">${it.duration_minutes || 0}</td>
                    <td class="td-cell" style="color: #047857;">${it.action_plan || it.action_taken || '-'}</td>
                </tr>
            `).join('')}
        `).join('')}
        <tr class="total-row">
            <td colspan="9" class="td-cell-center font-bold">TOTAL KESELURUHAN WAKTU HENTI DOWNTIME:</td>
            <td class="td-cell-num" style="color: #BE123C; font-size: 9pt;">${summary.total_downtime_minutes || 0} m</td>
            <td class="td-cell">Unplanned: ${summary.unplanned_minutes || 0}m | Planned: ${summary.planned_minutes || 0}m</td>
        </tr>
    </table>

    <table style="margin-top: 15px;">
        <tr>
            <td colspan="4" class="doc-header-2" style="text-align: center; border: none;">Dibuat Oleh (Leader Shift)</td>
            <td colspan="3" class="doc-header-2" style="text-align: center; border: none;">Ditangani Oleh (Maintenance)</td>
            <td colspan="4" class="doc-header-2" style="text-align: center; border: none;">Disetujui Oleh (Supervisor)</td>
        </tr>
        <tr><td colspan="11" style="height: 35px; border: none;"></td></tr>
        <tr>
            <td colspan="4" style="text-align: center; border: none; font-weight: bold;">( ${items[0]?.pic || 'Leader Shift'} )</td>
            <td colspan="3" style="text-align: center; border: none; font-weight: bold;">( ${items[0]?.team ? items[0].team + ' Team' : 'Teknisi Maintenance'} )</td>
            <td colspan="4" style="text-align: center; border: none; font-weight: bold;">( ${header.supervisor_in_charge || 'Alim Utama'} )</td>
        </tr>
    </table>
</body>
</html>`;

        const isJa = this.currentLang === 'ja';
        let finalExcelContent = excelContent;
        if (isJa) {
            finalExcelContent = i18n.translateHtml(excelContent);
        }

        const blob = new Blob([finalExcelContent], { type: 'application/vnd.ms-excel;charset=utf-8' });
        const url = URL.createObjectURL(blob);
        const a = document.createElement('a');
        const dateStr = header.formatted_date || header.production_date || new Date().toISOString().slice(0, 10);
        const lineStr = (header.line_name && header.line_name !== 'All Production Lines' && header.line_name !== 'All Lines') ? ` - ${header.line_name}` : '';
        const shiftStr = (header.shift_name && !header.shift_name.toLowerCase().includes('all')) ? ` - ${header.shift_name}` : '';
        let filename = this.sanitizeReportFilename(`Laporan Trouble dan Downtime Mesin Tgl ${dateStr}${lineStr}${shiftStr}`);
        if (isJa) filename = i18n.translateText(filename);
        filename += '.xls';
        a.href = url;
        a.download = filename;
        document.body.appendChild(a);
        a.click();
        document.body.removeChild(a);
        URL.revokeObjectURL(url);
    }

    /**
     * Open Interactive Slide-In Drilldown Drawer for Line Details
     */
    openLineDrillDownDrawer(row, header) {
        const backdrop = document.getElementById('drawer-drilldown-backdrop');
        const panel = document.getElementById('drawer-drilldown-panel');
        const container = document.getElementById('drawer-drilldown-content');
        if (!backdrop || !panel || !container) return;

        const cycleVariance = (row.target_speed > 0 && row.planned_time_minutes > 0) 
            ? ((row.run_time_minutes * 60) / Math.max(1, row.total_output_pcs)).toFixed(2)
            : row.ideal_cycle_time.toFixed(2);

        container.innerHTML = `
            <div class="flex items-center justify-between pb-3 mb-4 border-b border-slate-800">
                <div class="flex items-center gap-2.5">
                    <div class="w-8 h-8 rounded-lg bg-cyan-950 border border-cyan-800 text-cyan-400 flex items-center justify-center">
                        <i data-lucide="cpu" class="w-4 h-4"></i>
                    </div>
                    <div>
                        <h3 class="text-base font-bold text-white">${row.line_name}</h3>
                        <p class="text-[11px] text-slate-400 font-mono">${row.machine_code} (${row.machine_name})</p>
                    </div>
                </div>
                <button id="btn-close-drawer" class="text-slate-400 hover:text-slate-200 p-1 rounded-lg hover:bg-slate-800 cursor-pointer">
                    <i data-lucide="x" class="w-5 h-5"></i>
                </button>
            </div>

            <!-- OEE HERO SCORES -->
            <div class="bg-slate-950 border border-slate-800 rounded-xl p-4 mb-4">
                <div class="flex items-center justify-between mb-3 border-b border-slate-800 pb-2">
                    <span class="text-xs font-bold text-slate-400">Line OEE Score:</span>
                    <span class="text-sm font-black font-mono ${row.oee_pct >= 85 ? 'text-emerald-400' : 'text-amber-400'}">${row.oee_pct.toFixed(2)}%</span>
                </div>
                <div class="grid grid-cols-3 gap-2 font-mono text-center text-xs">
                    <div class="bg-slate-900/80 p-2 rounded-lg border border-slate-800">
                        <div class="text-[10px] text-cyan-400 uppercase font-sans font-bold">Availability</div>
                        <div class="text-sm font-bold text-white mt-0.5">${row.availability_pct.toFixed(2)}%</div>
                    </div>
                    <div class="bg-slate-900/80 p-2 rounded-lg border border-slate-800">
                        <div class="text-[10px] text-amber-400 uppercase font-sans font-bold">Performance</div>
                        <div class="text-sm font-bold text-white mt-0.5">${row.performance_pct.toFixed(2)}%</div>
                    </div>
                    <div class="bg-slate-900/80 p-2 rounded-lg border border-slate-800">
                        <div class="text-[10px] text-purple-400 uppercase font-sans font-bold">Quality</div>
                        <div class="text-sm font-bold text-white mt-0.5">${row.quality_pct.toFixed(2)}%</div>
                    </div>
                </div>
            </div>

            <!-- OPERATIONAL TELEMETRY & SPEEDS -->
            <div class="bg-slate-950 border border-slate-800 rounded-xl p-4 mb-4 space-y-2.5 text-xs font-mono">
                <h4 class="font-sans font-bold text-slate-200 text-xs uppercase tracking-wider mb-2 pb-1 border-b border-slate-800 flex items-center gap-1.5">
                    <i data-lucide="gauge" class="w-3.5 h-3.5 text-emerald-400"></i> Speed & Output Telemetry
                </h4>
                <div class="flex justify-between">
                    <span class="text-slate-400">Active Product SKU:</span>
                    <span class="text-amber-400 font-bold font-sans">${row.product_sku} (${row.product_name})</span>
                </div>
                <div class="flex justify-between">
                    <span class="text-slate-400">Ideal Standard Cycle:</span>
                    <span class="text-slate-200">${row.ideal_cycle_time.toFixed(2)} sec / pcs</span>
                </div>
                <div class="flex justify-between">
                    <span class="text-slate-400">Actual Average Cycle:</span>
                    <span class="text-cyan-400 font-bold">${cycleVariance} sec / pcs</span>
                </div>
                <div class="flex justify-between border-t border-slate-900 pt-2">
                    <span class="text-slate-400">Total Output Units:</span>
                    <span class="text-white font-bold">${row.total_output_pcs.toLocaleString()} pcs</span>
                </div>
                <div class="flex justify-between">
                    <span class="text-emerald-400">Good Products:</span>
                    <span class="text-emerald-400 font-bold">${row.good_output_pcs.toLocaleString()} pcs</span>
                </div>
                <div class="flex justify-between">
                    <span class="text-rose-400">Reject & Scrap:</span>
                    <span class="text-rose-400 font-bold">${row.reject_output_pcs.toLocaleString()} pcs</span>
                </div>
            </div>

            <!-- DOWNTIME & PROBLEM LOGS FOR THIS LINE -->
            <div class="bg-slate-950 border border-slate-800 rounded-xl p-4 mb-4">
                <h4 class="font-sans font-bold text-slate-200 text-xs uppercase tracking-wider mb-2.5 pb-1 border-b border-slate-800 flex items-center gap-1.5">
                    <i data-lucide="alert-circle" class="w-3.5 h-3.5 text-rose-400"></i> Recorded Stoppages & Downtimes (${(row.downtime_logs || []).length})
                </h4>
                <div class="space-y-2 max-h-48 overflow-y-auto pr-1">
                    ${(row.downtime_logs || []).length === 0 ? `<div class="text-slate-500 text-xs py-4 text-center">No individual breakdown events recorded for this machine.</div>` : ''}
                    ${(row.downtime_logs || []).map(dt => `
                        <div class="bg-slate-900 border border-slate-800 rounded-lg p-2.5 text-xs flex items-center justify-between">
                            <div>
                                <div class="font-bold text-slate-200">${dt.reason}</div>
                                <div class="text-[10px] text-slate-400 font-mono">${dt.start_time} - ${dt.end_time} • ${dt.is_planned ? 'Planned' : 'Unplanned'}</div>
                            </div>
                            <span class="font-mono font-bold text-rose-400 text-xs">${dt.duration.toFixed(1)}m</span>
                        </div>
                    `).join('')}
                </div>
            </div>

            <div class="flex gap-2">
                <button id="btn-drawer-close-action" class="w-full bg-slate-800 hover:bg-slate-700 text-slate-200 text-xs font-semibold py-2 rounded-lg transition-all cursor-pointer">
                    Close Details
                </button>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();

        // Show drawer
        backdrop.classList.remove('hidden');
        panel.classList.remove('translate-x-full');

        const closeDrawer = () => {
            panel.classList.add('translate-x-full');
            setTimeout(() => {
                backdrop.classList.add('hidden');
            }, 300);
        };

        const btnClose = document.getElementById('btn-close-drawer');
        const btnCloseAction = document.getElementById('btn-drawer-close-action');
        if (btnClose) btnClose.addEventListener('click', closeDrawer);
        if (btnCloseAction) btnCloseAction.addEventListener('click', closeDrawer);
        backdrop.addEventListener('click', closeDrawer);
    }

    // ==========================================
    // 7. MASTER DATA MANAGEMENT PAGE
    // ==========================================
    async renderMaster() {
        await this.loadMasterData();
        if (!this.activeMasterTab) this.activeMasterTab = 'machines';
        if (!this.masterPerPage) this.masterPerPage = 15;
        if (!this.masterPage) this.masterPage = 1;

        const content = document.getElementById('content-body');

        const activeClass = "pb-2 text-cyan-400 border-b-2 border-cyan-500 font-semibold cursor-pointer";
        const inactiveClass = "pb-2 text-slate-400 hover:text-slate-200 cursor-pointer transition-colors";

        const tabs = [
            { id: 'machines', label: `Machines (${(this.masterData.machines || []).length})` },
            { id: 'lines', label: `Lines (${(this.masterData.lines || []).length})` },
            { id: 'products', label: `Products (${(this.masterData.products || []).length})` },
            { id: 'shifts', label: `Shift Management (${(this.masterData.shifts || []).length})` },
            { id: 'groups', label: `Group Leader (${(this.masterData.groups || []).length})` },
            { id: 'plants', label: `Plants (${(this.masterData.plants || []).length})` },
            { id: 'downtime', label: `Downtime Reasons (${(this.masterData.downtimeReasons || []).length})` },
            { id: 'ng_sections', label: `Bagian NG (${(this.masterData.ngSections || []).length})` },
            { id: 'defect_reasons', label: `Penyebab / Remark (${(this.masterData.defectReasons || []).length})` },
        ];

        let tableHeaderHtml = '';
        let filteredItems = [];

        const actionButtons = (type, id, name) => `
            <button data-type="${type}" data-id="${id}" class="btn-edit-master inline-flex items-center gap-1 px-2.5 py-1 rounded bg-cyan-950/60 hover:bg-cyan-900/80 text-cyan-400 border border-cyan-800/60 text-xs font-semibold transition-all mr-1.5 cursor-pointer shadow-sm" title="Edit Record">
                <i data-lucide="pencil" class="w-3.5 h-3.5"></i>
                <span>Edit</span>
            </button>
            <button data-type="${type}" data-id="${id}" data-name="${name}" class="btn-delete-master inline-flex items-center gap-1 px-2.5 py-1 rounded bg-rose-950/60 hover:bg-rose-900/80 text-rose-400 border border-rose-800/60 text-xs font-semibold transition-all cursor-pointer shadow-sm" title="Delete Record">
                <i data-lucide="trash-2" class="w-3.5 h-3.5"></i>
                <span>Delete</span>
            </button>
        `;

        const searchQuery = (this.masterSearchQuery || '').toLowerCase().trim();

        if (this.activeMasterTab === 'machines') {
            tableHeaderHtml = `
                <tr>
                    <th class="p-3 text-center w-12">No</th>
                    <th class="p-3">IDMC / Code</th>
                    <th class="p-3">Machine Name</th>
                    <th class="p-3">Line / Location</th>
                    <th class="p-3">Status</th>
                    <th class="p-3 text-right">Actions</th>
                </tr>
            `;
            filteredItems = (this.masterData.machines || []).sort((a,b) => (a.code || '').localeCompare(b.code || '')).filter(m => {
                if (!searchQuery) return true;
                const code = (m.code || '').toLowerCase();
                const name = (m.name || '').toLowerCase();
                const line = (m.work_center?.production_line?.name || m.work_center?.name || '').toLowerCase();
                return code.includes(searchQuery) || name.includes(searchQuery) || line.includes(searchQuery);
            });
        } else if (this.activeMasterTab === 'lines') {
            tableHeaderHtml = `
                <tr>
                    <th class="p-3 text-center w-12">No</th>
                    <th class="p-3">Code</th>
                    <th class="p-3">Line Name</th>
                    <th class="p-3">Plant / Location</th>
                    <th class="p-3">Target OEE</th>
                    <th class="p-3 text-right">Actions</th>
                </tr>
            `;
            filteredItems = (this.masterData.lines || []).filter(l => {
                if (!searchQuery) return true;
                return (l.code || '').toLowerCase().includes(searchQuery) || (l.name || '').toLowerCase().includes(searchQuery);
            });
        } else if (this.activeMasterTab === 'products') {
            tableHeaderHtml = `
                <tr>
                    <th class="p-3 text-center w-12">No</th>
                    <th class="p-3">Line Produksi</th>
                    <th class="p-3">Product Name (Nama Produk)</th>
                    <th class="p-3">SKU</th>
                    <th class="p-3">Ideal Cycle Time</th>
                    <th class="p-3">Standard Speed</th>
                    <th class="p-3 text-right">Actions</th>
                </tr>
            `;
            filteredItems = (this.masterData.products || []).filter(p => {
                if (!searchQuery) return true;
                const lineObj = (this.masterData.lines || []).find(l => l.id == p.production_line_id);
                const lineName = (lineObj?.name || p.production_line?.name || '').toLowerCase();
                return (p.sku || '').toLowerCase().includes(searchQuery) ||
                       (p.name || '').toLowerCase().includes(searchQuery) ||
                       lineName.includes(searchQuery);
            });
        } else if (this.activeMasterTab === 'shifts') {
            tableHeaderHtml = `
                <tr>
                    <th class="p-3 text-center w-12">No</th>
                    <th class="p-3">Shift Name</th>
                    <th class="p-3">Jam Kerja (Working Hours)</th>
                    <th class="p-3">Waktu Total (Menit)</th>
                    <th class="p-3">Jam Istirahat (Break)</th>
                    <th class="p-3">Planned Production Time (Waktu Efektif)</th>
                    <th class="p-3 text-right">Actions</th>
                </tr>
            `;
            filteredItems = (this.masterData.shifts || []).filter(s => {
                if (!searchQuery) return true;
                return (s.name || '').toLowerCase().includes(searchQuery) || (s.start_time || '').toLowerCase().includes(searchQuery) || (s.end_time || '').toLowerCase().includes(searchQuery);
            });
        } else if (this.activeMasterTab === 'groups') {
            tableHeaderHtml = `
                <tr>
                    <th class="p-3 text-center w-12">No</th>
                    <th class="p-3">Team</th>
                    <th class="p-3">Leader Name / PIC</th>
                    <th class="p-3">Supervisor Name</th>
                    <th class="p-3">Production Line</th>
                    <th class="p-3 text-right">Actions</th>
                </tr>
            `;
            filteredItems = (this.masterData.groups || []).filter(g => {
                if (!searchQuery) return true;
                const team = (g.team || '').toLowerCase();
                const leader = (g.leader_name || '').toLowerCase();
                const supervisor = (g.supervisor_name || '').toLowerCase();
                const line = (g.production_line?.name || '').toLowerCase();
                return team.includes(searchQuery) || leader.includes(searchQuery) || supervisor.includes(searchQuery) || line.includes(searchQuery);
            });
        } else if (this.activeMasterTab === 'plants') {
            tableHeaderHtml = `
                <tr>
                    <th class="p-3 text-center w-12">No</th>
                    <th class="p-3">Code</th>
                    <th class="p-3">Plant Name</th>
                    <th class="p-3">Location</th>
                    <th class="p-3">Status</th>
                    <th class="p-3 text-right">Actions</th>
                </tr>
            `;
            filteredItems = (this.masterData.plants || []).filter(pl => {
                if (!searchQuery) return true;
                return (pl.code || '').toLowerCase().includes(searchQuery) || (pl.name || '').toLowerCase().includes(searchQuery);
            });
        } else if (this.activeMasterTab === 'downtime') {
            tableHeaderHtml = `
                <tr>
                    <th class="p-3 text-center w-12">No</th>
                    <th class="p-3">Code</th>
                    <th class="p-3">Reason Name</th>
                    <th class="p-3">Category</th>
                    <th class="p-3">Type</th>
                    <th class="p-3 text-right">Actions</th>
                </tr>
            `;
            filteredItems = (this.masterData.downtimeReasons || []).filter(dt => {
                if (!searchQuery) return true;
                return (dt.code || '').toLowerCase().includes(searchQuery) || (dt.name || '').toLowerCase().includes(searchQuery);
            });
        } else if (this.activeMasterTab === 'ng_sections') {
            tableHeaderHtml = `
                <tr>
                    <th class="p-3 text-center w-12">No</th>
                    <th class="p-3">Kode Bagian NG</th>
                    <th class="p-3">Nama Bagian NG</th>
                    <th class="p-3">Status Aktif</th>
                    <th class="p-3 text-right">Actions</th>
                </tr>
            `;
            filteredItems = (this.masterData.ngSections || []).filter(sec => {
                if (!searchQuery) return true;
                return (sec.code || '').toLowerCase().includes(searchQuery) || (sec.name || '').toLowerCase().includes(searchQuery);
            });
        } else if (this.activeMasterTab === 'defect_reasons') {
            tableHeaderHtml = `
                <tr>
                    <th class="p-3 text-center w-12">No</th>
                    <th class="p-3">Kategori Defect</th>
                    <th class="p-3">Nama Penyebab / Remark</th>
                    <th class="p-3">Rekomendasi Kaizen / Countermeasure</th>
                    <th class="p-3 text-right">Actions</th>
                </tr>
            `;
            filteredItems = (this.masterData.defectReasons || []).filter(dr => {
                if (!searchQuery) return true;
                const catName = (dr.defect_category?.name || dr.defectCategory?.name || '').toLowerCase();
                const cm = (dr.countermeasure || '').toLowerCase();
                return (dr.code || '').toLowerCase().includes(searchQuery) ||
                       (dr.name || '').toLowerCase().includes(searchQuery) ||
                       catName.includes(searchQuery) ||
                       cm.includes(searchQuery);
            });
        }

        // PAGINATION MATH
        const totalItems = filteredItems.length;
        const perPage = this.masterPerPage || 15;
        const totalPages = Math.max(1, Math.ceil(totalItems / perPage));

        if (this.masterPage > totalPages) this.masterPage = totalPages;
        if (this.masterPage < 1) this.masterPage = 1;

        const startIndex = (this.masterPage - 1) * perPage;
        const endIndex = Math.min(startIndex + perPage, totalItems);
        const paginatedItems = filteredItems.slice(startIndex, endIndex);

        let tableRowsHtml = '';
        if (paginatedItems.length === 0) {
            tableRowsHtml = '<tr><td colspan="7" class="p-6 text-center text-slate-500 font-sans">No records found</td></tr>';
        } else if (this.activeMasterTab === 'machines') {
            tableRowsHtml = paginatedItems.map((m, idx) => `
                <tr class="hover:bg-slate-800/40 transition-colors">
                    <td class="p-3 text-center font-bold text-slate-500 font-mono text-[11px]">${startIndex + idx + 1}</td>
                    <td class="p-3 text-cyan-400 font-bold font-mono">${m.code}</td>
                    <td class="p-3 text-slate-200 font-sans font-semibold">${m.name}</td>
                    <td class="p-3 text-slate-400 font-sans">${m.work_center?.production_line?.name || m.work_center?.name || 'N/A'}</td>
                    <td class="p-3 font-sans">
                        <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded text-[10px] font-bold border ${this.getMachineStatusBadge(m.status)}">
                            ${this.getStatusIcon(m.status)}
                            <span>${m.status}</span>
                        </span>
                    </td>
                    <td class="p-3 text-right font-sans">
                        ${actionButtons('machines', m.id, m.name || m.code)}
                    </td>
                </tr>
            `).join('');
        } else if (this.activeMasterTab === 'lines') {
            tableRowsHtml = paginatedItems.map((l, idx) => `
                <tr class="hover:bg-slate-800/40 transition-colors">
                    <td class="p-3 text-center font-bold text-slate-500 font-mono text-[11px]">${startIndex + idx + 1}</td>
                    <td class="p-3 text-cyan-400 font-bold font-mono">${l.code}</td>
                    <td class="p-3 text-slate-200 font-sans font-semibold">${l.name}</td>
                    <td class="p-3 text-slate-400 font-sans">${l.plant?.name || l.area?.plant?.name || 'N/A'}</td>
                    <td class="p-3 text-emerald-400 font-sans font-bold">${l.target_oee || 85}%</td>
                    <td class="p-3 text-right font-sans">
                        ${actionButtons('lines', l.id, l.name || l.code)}
                    </td>
                </tr>
            `).join('');
        } else if (this.activeMasterTab === 'products') {
            tableRowsHtml = paginatedItems.map((p, idx) => {
                const lineObj = (this.masterData.lines || []).find(l => l.id == p.production_line_id);
                const lineName = lineObj ? lineObj.name : (p.production_line?.name || 'Unassigned');
                const isUnassigned = !lineObj && !p.production_line;
                return `
                    <tr class="hover:bg-slate-800/40 transition-colors">
                        <td class="p-3 text-center font-bold text-slate-500 font-mono text-[11px]">${startIndex + idx + 1}</td>
                        <td class="p-3 font-sans">
                            <span class="inline-flex items-center gap-1.5 px-2.5 py-1 rounded-md text-[11px] font-bold ${isUnassigned ? 'bg-slate-800 text-slate-400 border border-slate-700' : 'bg-cyan-950/80 text-cyan-300 border border-cyan-800/80 shadow-sm'}">
                                <i data-lucide="git-branch" class="w-3 h-3 ${isUnassigned ? 'text-slate-500' : 'text-cyan-400'}"></i>
                                <span>${lineName}</span>
                            </span>
                        </td>
                        <td class="p-3 text-cyan-400 font-bold font-sans">${p.name}</td>
                        <td class="p-3 text-slate-300 font-mono">${p.sku}</td>
                        <td class="p-3 text-amber-400 font-sans font-semibold">${p.ideal_cycle_time}s</td>
                        <td class="p-3 text-slate-300 font-sans">${p.ideal_cycle_time > 0 ? (60 / p.ideal_cycle_time).toFixed(1) : 'N/A'} Pcs/Min</td>
                        <td class="p-3 text-right font-sans">
                            ${actionButtons('products', p.id, p.name || p.sku)}
                        </td>
                    </tr>
                `;
            }).join('');
        } else if (this.activeMasterTab === 'shifts') {
            tableRowsHtml = paginatedItems.map((s, idx) => {
                const startStr = (s.start_time || '07:30').slice(0,5);
                const endStr = (s.end_time || '16:30').slice(0,5);

                const startParts = startStr.split(':');
                const endParts = endStr.split(':');
                let startMins = parseInt(startParts[0]) * 60 + parseInt(startParts[1] || 0);
                let endMins = parseInt(endParts[0]) * 60 + parseInt(endParts[1] || 0);

                if (endMins <= startMins) {
                    endMins += 24 * 60; // Overnight shift
                }

                const totalMins = endMins - startMins;
                const totalHours = (totalMins / 60).toFixed(1);
                const breakMins = parseInt(s.break_duration_minutes || 60);
                const plannedMins = Math.max(0, totalMins - breakMins);
                const plannedHours = (plannedMins / 60).toFixed(1);

                return `
                    <tr class="hover:bg-slate-800/40 transition-colors">
                        <td class="p-3 text-center font-bold text-slate-500 font-mono text-[11px]">${startIndex + idx + 1}</td>
                        <td class="p-3 text-cyan-400 font-bold font-sans">${s.name}</td>
                        <td class="p-3 text-slate-200 font-mono font-semibold">${startStr} - ${endStr}</td>
                        <td class="p-3 text-amber-400 font-mono font-bold">${totalMins} mins (${totalHours} hrs)</td>
                        <td class="p-3 text-rose-400 font-mono font-bold">${breakMins} mins (${(breakMins/60).toFixed(1)} hrs)</td>
                        <td class="p-3 text-emerald-400 font-mono font-bold">${plannedMins} mins (${plannedHours} hrs)</td>
                        <td class="p-3 text-right font-sans">
                            ${actionButtons('shifts', s.id, s.name)}
                        </td>
                    </tr>
                `;
            }).join('');
        } else if (this.activeMasterTab === 'groups') {
            tableRowsHtml = paginatedItems.map((g, idx) => `
                <tr class="hover:bg-slate-800/40 transition-colors">
                    <td class="p-3 text-center font-bold text-slate-500 font-mono text-[11px]">${startIndex + idx + 1}</td>
                    <td class="p-3 text-cyan-400 font-bold font-sans">${g.team}</td>
                    <td class="p-3 text-slate-100 font-sans font-bold">${g.leader_name}</td>
                    <td class="p-3 text-emerald-400 font-sans font-semibold">${g.supervisor_name || 'Alim Utama'}</td>
                    <td class="p-3 text-slate-300 font-sans">${g.production_line?.name || 'N/A'}</td>
                    <td class="p-3 text-right font-sans">
                        ${actionButtons('groups', g.id, `${g.team} — ${g.leader_name}`)}
                    </td>
                </tr>
            `).join('');
        } else if (this.activeMasterTab === 'plants') {
            tableRowsHtml = paginatedItems.map((pl, idx) => `
                <tr class="hover:bg-slate-800/40 transition-colors">
                    <td class="p-3 text-center font-bold text-slate-500 font-mono text-[11px]">${startIndex + idx + 1}</td>
                    <td class="p-3 text-cyan-400 font-bold font-mono">${pl.code}</td>
                    <td class="p-3 text-slate-200 font-sans font-semibold">${pl.name}</td>
                    <td class="p-3 text-slate-400 font-sans">${pl.location || pl.address || 'N/A'}</td>
                    <td class="p-3 font-sans">
                        <span class="px-2 py-0.5 rounded text-[10px] font-semibold border bg-emerald-950/50 text-emerald-400 border-emerald-800">
                            ACTIVE
                        </span>
                    </td>
                    <td class="p-3 text-right font-sans">
                        ${actionButtons('plants', pl.id, pl.name || pl.code)}
                    </td>
                </tr>
            `).join('');
        } else if (this.activeMasterTab === 'downtime') {
            tableRowsHtml = paginatedItems.map((dt, idx) => `
                <tr class="hover:bg-slate-800/40 transition-colors">
                    <td class="p-3 text-center font-bold text-slate-500 font-mono text-[11px]">${startIndex + idx + 1}</td>
                    <td class="p-3 text-cyan-400 font-bold font-mono">${dt.code}</td>
                    <td class="p-3 text-slate-200 font-sans font-semibold">${dt.name}</td>
                    <td class="p-3 text-slate-400 font-sans">${dt.category || 'General'}</td>
                    <td class="p-3 font-sans">
                        <span class="px-2 py-0.5 rounded text-[10px] font-semibold border ${dt.is_planned ? 'bg-blue-950/50 text-blue-400 border-blue-800' : 'bg-rose-950/50 text-rose-400 border-rose-800'}">
                            ${dt.is_planned ? 'PLANNED' : 'UNPLANNED'}
                        </span>
                    </td>
                    <td class="p-3 text-right font-sans">
                        ${actionButtons('downtime', dt.id, dt.name || dt.code)}
                    </td>
                </tr>
            `).join('');
        } else if (this.activeMasterTab === 'ng_sections') {
            tableRowsHtml = paginatedItems.map((sec, idx) => `
                <tr class="hover:bg-slate-800/40 transition-colors">
                    <td class="p-3 text-center font-bold text-slate-500 font-mono text-[11px]">${startIndex + idx + 1}</td>
                    <td class="p-3 text-cyan-400 font-bold font-mono">${sec.code}</td>
                    <td class="p-3 text-slate-100 font-sans font-semibold">${sec.name}</td>
                    <td class="p-3 font-sans">
                        <span class="px-2 py-0.5 rounded text-[10px] font-semibold border ${sec.is_active ? 'bg-emerald-950/50 text-emerald-400 border-emerald-800' : 'bg-slate-800 text-slate-400 border-slate-700'}">
                            ${sec.is_active ? 'AKTIF' : 'NONAKTIF'}
                        </span>
                    </td>
                    <td class="p-3 text-right font-sans">
                        ${actionButtons('ng_sections', sec.id, sec.name || sec.code)}
                    </td>
                </tr>
            `).join('');
        } else if (this.activeMasterTab === 'defect_reasons') {
            tableRowsHtml = paginatedItems.map((dr, idx) => `
                <tr class="hover:bg-slate-800/40 transition-colors">
                    <td class="p-3 text-center font-bold text-slate-500 font-mono text-[11px]">${startIndex + idx + 1}</td>
                    <td class="p-3 font-sans">
                        <span class="px-2.5 py-1 rounded text-xs font-semibold bg-purple-950 text-purple-300 border border-purple-800">
                            ${dr.defect_category?.name || dr.defectCategory?.name || 'General'}
                        </span>
                    </td>
                    <td class="p-3 text-slate-100 font-sans font-semibold">${dr.name}</td>
                    <td class="p-3 font-sans text-xs text-slate-300 max-w-xs truncate" title="${dr.countermeasure || 'Otomatis (Smart Rule Engine)'}">
                        ${dr.countermeasure ? `<span class="text-cyan-400 font-medium">${dr.countermeasure}</span>` : `<span class="text-slate-500 italic text-[11px]">Otomatis (Smart QC Rule Engine)</span>`}
                    </td>
                    <td class="p-3 text-right font-sans">
                        ${actionButtons('defect_reasons', dr.id, dr.name || dr.code)}
                    </td>
                </tr>
            `).join('');
        }

        // PAGE BUTTONS BUILDER
        let pageButtonsHtml = '';
        const maxVisible = 5;
        let startPage = Math.max(1, this.masterPage - 2);
        let endPage = Math.min(totalPages, startPage + maxVisible - 1);
        if (endPage - startPage < maxVisible - 1) {
            startPage = Math.max(1, endPage - maxVisible + 1);
        }

        if (startPage > 1) {
            pageButtonsHtml += `<button class="btn-master-page px-2.5 py-1 rounded bg-slate-950 hover:bg-slate-800 border border-slate-800 text-slate-300 cursor-pointer text-xs" data-page="1">1</button>`;
            if (startPage > 2) pageButtonsHtml += `<span class="px-1 text-slate-600 text-xs">...</span>`;
        }

        for (let p = startPage; p <= endPage; p++) {
            const activeStyle = p === this.masterPage
                ? 'bg-cyan-600 text-white font-bold border-cyan-500'
                : 'bg-slate-950 hover:bg-slate-800 text-slate-300 border-slate-800';
            pageButtonsHtml += `<button class="btn-master-page px-2.5 py-1 rounded border ${activeStyle} cursor-pointer text-xs" data-page="${p}">${p}</button>`;
        }

        if (endPage < totalPages) {
            if (endPage < totalPages - 1) pageButtonsHtml += `<span class="px-1 text-slate-600 text-xs">...</span>`;
            pageButtonsHtml += `<button class="btn-master-page px-2.5 py-1 rounded bg-slate-950 hover:bg-slate-800 border border-slate-800 text-slate-300 cursor-pointer text-xs" data-page="${totalPages}">${totalPages}</button>`;
        }

        content.innerHTML = `
            <div class="flex flex-wrap items-center justify-between gap-4">
                <div>
                    <h2 class="text-xl font-bold text-slate-100 flex items-center gap-2">
                        <i data-lucide="database" class="w-5 h-5 text-cyan-400"></i>
                        <span>Master Data</span>
                    </h2>
                </div>
                <div class="flex flex-wrap items-center gap-2">
                    <button id="btn-template-master" class="bg-slate-800 hover:bg-slate-700 text-slate-200 border border-slate-700 text-xs font-semibold px-3 py-1.5 rounded-lg flex items-center gap-1.5 cursor-pointer shadow-sm transition-all" title="Unduh Template Format Excel (.xlsx)">
                        <i data-lucide="file-spreadsheet" class="w-4 h-4 text-emerald-400"></i>
                        <span>Unduh Template Excel (.xlsx)</span>
                    </button>
                    <button id="btn-import-master" class="bg-emerald-600 hover:bg-emerald-500 text-white text-xs font-semibold px-3 py-1.5 rounded-lg flex items-center gap-1.5 cursor-pointer shadow-lg shadow-emerald-600/20 transition-all" title="Import Data Sekaligus (Bulk)">
                        <i data-lucide="upload-cloud" class="w-4 h-4 text-emerald-100"></i>
                        <span>Import Excel / Data (Bulk)</span>
                    </button>
                    <button id="btn-add-master" class="bg-cyan-600 hover:bg-cyan-500 text-white text-xs font-semibold px-3 py-1.5 rounded-lg flex items-center gap-1.5 cursor-pointer shadow-lg shadow-cyan-600/20 transition-all">
                        <i data-lucide="plus" class="w-4 h-4"></i>
                        <span>+ Tambah Data Master</span>
                    </button>
                </div>
            </div>

            <!-- MASTER DATA TABS -->
            <div class="bg-slate-900 border border-slate-800 rounded-xl p-5 shadow-xl">
                <div class="border-b border-slate-800 mb-4 flex flex-wrap items-center justify-between gap-4">
                    <div class="flex gap-4 text-xs font-medium overflow-x-auto pb-1 max-w-full">
                        ${tabs.map(t => `
                            <button data-tab="${t.id}" class="btn-master-tab whitespace-nowrap ${this.activeMasterTab === t.id ? activeClass : inactiveClass}">${t.label}</button>
                        `).join('')}
                    </div>

                    <div class="relative w-64">
                        <i data-lucide="search" class="w-3.5 h-3.5 text-slate-500 absolute left-2.5 top-2"></i>
                        <input type="text" id="input-master-search" placeholder="Search records..." value="${this.masterSearchQuery || ''}" class="w-full bg-slate-950 border border-slate-800 rounded-lg pl-8 pr-3 py-1 text-xs text-slate-200 focus:outline-none focus:border-cyan-500 font-sans" />
                    </div>
                </div>

                <div class="overflow-x-auto">
                    <table class="w-full text-left text-xs">
                        <thead class="bg-slate-950 text-slate-400 uppercase font-semibold text-[10px] border-b border-slate-800">
                            ${tableHeaderHtml}
                        </thead>
                        <tbody class="divide-y divide-slate-800/60 font-mono">
                            ${tableRowsHtml}
                        </tbody>
                    </table>
                </div>

                <!-- PAGINATION CONTROLS FOOTER -->
                <div class="mt-4 pt-3 border-t border-slate-800 flex flex-wrap items-center justify-between gap-4 text-xs font-sans">
                    <div class="flex items-center gap-3 text-slate-400">
                        <span>Showing <strong class="text-cyan-400 font-mono">${totalItems > 0 ? startIndex + 1 : 0}</strong> to <strong class="text-cyan-400 font-mono">${endIndex}</strong> of <strong class="text-slate-200 font-mono">${totalItems}</strong> records</span>
                        <div class="flex items-center gap-1.5 ml-2">
                            <span class="text-slate-500 text-[11px]">Items per page:</span>
                            <select id="select-master-per-page" class="bg-slate-950 border border-slate-800 text-slate-300 rounded px-2 py-0.5 text-xs focus:outline-none focus:border-cyan-500 cursor-pointer">
                                <option value="15" ${perPage === 15 ? 'selected' : ''}>15</option>
                                <option value="20" ${perPage === 20 ? 'selected' : ''}>20</option>
                                <option value="35" ${perPage === 35 ? 'selected' : ''}>35</option>
                                <option value="50" ${perPage === 50 ? 'selected' : ''}>50</option>
                            </select>
                        </div>
                    </div>

                    <div class="flex items-center gap-1">
                        <button class="btn-master-page px-2.5 py-1 rounded bg-slate-950 hover:bg-slate-800 border border-slate-800 text-slate-300 disabled:opacity-40 disabled:cursor-not-allowed cursor-pointer text-xs font-medium" data-page="${this.masterPage - 1}" ${this.masterPage <= 1 ? 'disabled' : ''}>
                            ‹ Prev
                        </button>
                        ${pageButtonsHtml}
                        <button class="btn-master-page px-2.5 py-1 rounded bg-slate-950 hover:bg-slate-800 border border-slate-800 text-slate-300 disabled:opacity-40 disabled:cursor-not-allowed cursor-pointer text-xs font-medium" data-page="${this.masterPage + 1}" ${this.masterPage >= totalPages ? 'disabled' : ''}>
                            Next ›
                        </button>
                    </div>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();

        const btnAddMaster = document.getElementById('btn-add-master');
        if (btnAddMaster) {
            btnAddMaster.addEventListener('click', () => {
                const activeTab = this.activeMasterTab || 'machines';
                this.showAddMasterModal(activeTab);
            });
        }

        const btnTemplateMaster = document.getElementById('btn-template-master');
        if (btnTemplateMaster) {
            btnTemplateMaster.addEventListener('click', () => {
                const activeTab = this.activeMasterTab || 'ng_sections';
                this.downloadMasterTemplate(activeTab);
            });
        }

        const btnImportMaster = document.getElementById('btn-import-master');
        if (btnImportMaster) {
            btnImportMaster.addEventListener('click', () => {
                const activeTab = this.activeMasterTab || 'ng_sections';
                this.showImportMasterModal(activeTab);
            });
        }

        const searchInput = document.getElementById('input-master-search');
        if (searchInput) {
            searchInput.addEventListener('input', (e) => {
                this.masterSearchQuery = e.target.value;
                this.masterPage = 1;
                this.renderMaster();
            });
        }

        document.querySelectorAll('.btn-master-tab').forEach(btn => {
            btn.addEventListener('click', (e) => {
                const targetTab = e.currentTarget.getAttribute('data-tab');
                if (targetTab) {
                    this.activeMasterTab = targetTab;
                    this.masterPage = 1;
                    this.renderMaster();
                }
            });
        });

        document.querySelectorAll('.btn-master-page').forEach(btn => {
            btn.addEventListener('click', (e) => {
                const p = parseInt(e.currentTarget.getAttribute('data-page'));
                if (p && p !== this.masterPage && p >= 1 && p <= totalPages) {
                    this.masterPage = p;
                    this.renderMaster();
                }
            });
        });

        const perPageSelect = document.getElementById('select-master-per-page');
        if (perPageSelect) {
            perPageSelect.addEventListener('change', (e) => {
                this.masterPerPage = parseInt(e.target.value) || 15;
                this.masterPage = 1;
                this.renderMaster();
            });
        }

        document.querySelectorAll('.btn-edit-master').forEach(btn => {
            btn.addEventListener('click', (e) => {
                const type = e.currentTarget.getAttribute('data-type');
                const id = e.currentTarget.getAttribute('data-id');
                this.showEditMasterModal(type, id);
            });
        });

        document.querySelectorAll('.btn-delete-master').forEach(btn => {
            btn.addEventListener('click', (e) => {
                const type = e.currentTarget.getAttribute('data-type');
                const id = e.currentTarget.getAttribute('data-id');
                const name = e.currentTarget.getAttribute('data-name');
                this.showDeleteMasterModal(type, id, name);
            });
        });

        if (this.currentLang === 'ja') {
            i18n.localizeDom(document.getElementById('content-body') || document.body);
        }
    }

    downloadMasterTemplate(type) {
        let filename = 'Template_Master_Data.xlsx';
        let dataRows = [];
        let colWidths = [];

        if (type === 'ng_sections') {
            filename = 'Template_Master_Bagian_NG.xlsx';
            dataRows = [
                {
                    "Kode Bagian NG": "SEC-PIN-BORE",
                    "Nama Bagian NG": "Small End (Pin Bore)",
                    "Status Aktif (1=Aktif, 0=Nonaktif)": 1
                },
                {
                    "Kode Bagian NG": "SEC-CRANK-BORE",
                    "Nama Bagian NG": "Big End (Crank Bore)",
                    "Status Aktif (1=Aktif, 0=Nonaktif)": 1
                },
                {
                    "Kode Bagian NG": "SEC-ROD-BODY",
                    "Nama Bagian NG": "Rod Body / Shank",
                    "Status Aktif (1=Aktif, 0=Nonaktif)": 1
                },
                {
                    "Kode Bagian NG": "SEC-BOLT-HOLE",
                    "Nama Bagian NG": "Bolt Hole Threads",
                    "Status Aktif (1=Aktif, 0=Nonaktif)": 1
                },
                {
                    "Kode Bagian NG": "SEC-CAP-BODY",
                    "Nama Bagian NG": "Cap Body",
                    "Status Aktif (1=Aktif, 0=Nonaktif)": 1
                },
                {
                    "Kode Bagian NG": "SEC-JOINT-FACE",
                    "Nama Bagian NG": "Joint / Serration Face",
                    "Status Aktif (1=Aktif, 0=Nonaktif)": 1
                },
                {
                    "Kode Bagian NG": "SEC-SIDE-FACE",
                    "Nama Bagian NG": "Thrust / Side Face",
                    "Status Aktif (1=Aktif, 0=Nonaktif)": 1
                },
                {
                    "Kode Bagian NG": "SEC-OIL-HOLE",
                    "Nama Bagian NG": "Oil Lubrication Hole",
                    "Status Aktif (1=Aktif, 0=Nonaktif)": 1
                },
                {
                    "Kode Bagian NG": "SEC-BUSHING",
                    "Nama Bagian NG": "Small End Bushing",
                    "Status Aktif (1=Aktif, 0=Nonaktif)": 1
                },
                {
                    "Kode Bagian NG": "SEC-PIN-NOTCH",
                    "Nama Bagian NG": "Bearing Lock Notch",
                    "Status Aktif (1=Aktif, 0=Nonaktif)": 1
                }
            ];
            colWidths = [{ wch: 22 }, { wch: 35 }, { wch: 32 }];
        } else if (type === 'defect_reasons') {
            filename = 'Template_Master_Penyebab_Defect.xlsx';
            dataRows = [
                {
                    "Kategori Defect": "Dimensi & Geometri",
                    "Nama Penyebab / Remark": "Diameter Out of Spec"
                },
                {
                    "Kategori Defect": "Dimensi & Geometri",
                    "Nama Penyebab / Remark": "Bending & Twist"
                },
                {
                    "Kategori Defect": "Dimensi & Geometri",
                    "Nama Penyebab / Remark": "Thickness Out of Spec"
                },
                {
                    "Kategori Defect": "Dimensi & Geometri",
                    "Nama Penyebab / Remark": "Bore Ovality / Roundness"
                },
                {
                    "Kategori Defect": "Kualitas Permukaan & Visual",
                    "Nama Penyebab / Remark": "Surface Scratch & Dent"
                },
                {
                    "Kategori Defect": "Kualitas Permukaan & Visual",
                    "Nama Penyebab / Remark": "Rough Surface Finish"
                },
                {
                    "Kategori Defect": "Kualitas Permukaan & Visual",
                    "Nama Penyebab / Remark": "Rust / Oxidation Stain"
                },
                {
                    "Kategori Defect": "Kualitas Permukaan & Visual",
                    "Nama Penyebab / Remark": "Step Line & Uneven Surface"
                },
                {
                    "Kategori Defect": "Cacat Material & Cor (Foundry)",
                    "Nama Penyebab / Remark": "Blow Hole / Porosity"
                },
                {
                    "Kategori Defect": "Cacat Material & Cor (Foundry)",
                    "Nama Penyebab / Remark": "Material Crack"
                },
                {
                    "Kategori Defect": "Cacat Material & Cor (Foundry)",
                    "Nama Penyebab / Remark": "Slag Inclusion / Sand Mark"
                },
                {
                    "Kategori Defect": "Proses Machining & Assembly",
                    "Nama Penyebab / Remark": "Heavy Burr & Flash"
                },
                {
                    "Kategori Defect": "Proses Machining & Assembly",
                    "Nama Penyebab / Remark": "Damaged Thread"
                },
                {
                    "Kategori Defect": "Proses Machining & Assembly",
                    "Nama Penyebab / Remark": "Tool Chipping Mark"
                }
            ];
            colWidths = [{ wch: 35 }, { wch: 45 }];
        } else {
            filename = `Template_Master_${type}.xlsx`;
            dataRows = [
                { "Code": "CODE-01", "Name": "Contoh Nama 1", "Description": "Deskripsi contoh 1" },
                { "Code": "CODE-02", "Name": "Contoh Nama 2", "Description": "Deskripsi contoh 2" }
            ];
            colWidths = [{ wch: 18 }, { wch: 30 }, { wch: 40 }];
        }

        try {
            const worksheet = XLSX.utils.json_to_sheet(dataRows);
            worksheet['!cols'] = colWidths;
            const workbook = XLSX.utils.book_new();
            XLSX.utils.book_append_sheet(workbook, worksheet, 'Master Data');
            XLSX.writeFile(workbook, filename);
            this.showNotification('Template Excel Siap', `File Excel (${filename}) berhasil diunduh. Silakan isi datanya lalu gunakan tombol Import Data.`, 'info');
        } catch (err) {
            console.error('Error exporting Excel template:', err);
            alert('Gagal membuat file Excel: ' + err.message);
        }
    }

    showImportMasterModal(defaultType) {
        const modalContainer = document.getElementById('modal-container');
        let currentType = (defaultType === 'defect_reasons' || defaultType === 'ng_sections') ? defaultType : 'ng_sections';

        const renderModalContent = (type) => {
            const sampleRowsExample = type === 'ng_sections'
                ? `SEC-PIN-BORE\tSmall End (Pin Bore)\t1\nSEC-CRANK-BORE\tBig End (Crank Bore)\t1\nSEC-ROD-BODY\tRod Body / Shank\t1`
                : `Dimensi & Geometri\tDiameter Out of Spec\nKualitas Permukaan & Visual\tSurface Scratch & Dent\nCacat Material & Cor\tBlow Hole / Porosity`;

            const columnsHelper = type === 'ng_sections'
                ? `<strong>Kolom Excel (.xlsx) [3 Kolom]:</strong> <code>Kode Bagian NG</code>, <code>Nama Bagian NG</code>, <code>Status Aktif</code> (1 = Aktif, 0 = Nonaktif)`
                : `<strong>Kolom Excel (.xlsx) [2 Kolom]:</strong> <code>Kategori Defect</code>, <code>Nama Penyebab / Remark</code>`;

            return `
                <div class="fixed inset-0 bg-slate-950/85 backdrop-blur-md flex items-center justify-center p-4 z-50 overflow-y-auto">
                    <div class="bg-slate-900 border border-slate-800 rounded-2xl p-6 max-w-3xl w-full shadow-2xl space-y-4 my-8">
                        <!-- HEADER -->
                        <div class="flex items-center justify-between pb-3 border-b border-slate-800">
                            <div>
                                <h3 class="font-bold text-base text-slate-100 flex items-center gap-2">
                                    <i data-lucide="file-spreadsheet" class="w-5 h-5 text-emerald-400"></i>
                                    Import File Excel (.xlsx / .xls) Data Master
                                </h3>
                                <p class="text-xs text-slate-400 mt-0.5">Upload file Excel atau paste data langsung dari spreadsheet untuk impor data massal.</p>
                            </div>
                            <button id="btn-close-import-modal" class="text-slate-400 hover:text-slate-200 p-1 rounded-lg hover:bg-slate-800 cursor-pointer">
                                <i data-lucide="x" class="w-5 h-5"></i>
                            </button>
                        </div>

                        <!-- TARGET MASTER TYPE SELECTOR -->
                        <div class="bg-slate-950 border border-slate-800 rounded-xl p-3.5 flex flex-wrap items-center justify-between gap-3">
                            <div class="flex items-center gap-2">
                                <label class="text-xs font-semibold text-slate-300">Pilih Data Master Target:</label>
                                <select id="select-import-target-type" class="bg-slate-900 border border-cyan-800/80 text-cyan-300 font-bold rounded-lg px-3 py-1.5 text-xs focus:outline-none focus:border-cyan-400 cursor-pointer">
                                    <option value="ng_sections" ${type === 'ng_sections' ? 'selected' : ''}>Bagian NG (3 Kolom: Kode, Nama, Status)</option>
                                    <option value="defect_reasons" ${type === 'defect_reasons' ? 'selected' : ''}>Penyebab / Remark (2 Kolom: Kategori, Nama)</option>
                                </select>
                            </div>
                            <button id="btn-modal-download-template" class="inline-flex items-center gap-1.5 px-3.5 py-1.5 rounded-lg bg-emerald-950/60 hover:bg-emerald-900/80 text-emerald-300 border border-emerald-800 text-xs font-bold cursor-pointer transition-all shadow-sm">
                                <i data-lucide="file-spreadsheet" class="w-4 h-4 text-emerald-400"></i>
                                Unduh Template Excel (.xlsx)
                            </button>
                        </div>

                        <!-- HELPER NOTE -->
                        <div class="p-3 bg-cyan-950/30 border border-cyan-900/40 rounded-lg text-xs text-cyan-200/90 leading-relaxed">
                            <div class="flex items-start gap-2">
                                <i data-lucide="info" class="w-4 h-4 text-cyan-400 flex-shrink-0 mt-0.5"></i>
                                <div>
                                    <p class="mb-1">${columnsHelper}</p>
                                    <p class="text-[11px] text-cyan-300/70">* Data yang sudah ada akan diperbarui otomatis (*Update*). Data baru akan langsung ditambahkan (*Insert*).</p>
                                </div>
                            </div>
                        </div>

                        <!-- INPUT TABS: FILE UPLOAD vs PASTE TEXT -->
                        <div class="space-y-3">
                            <!-- Drag & Drop / File Input Box -->
                            <div>
                                <label class="block text-xs font-semibold text-slate-300 mb-1.5">Opsi 1: Upload File Excel (.xlsx, .xls) atau (.csv)</label>
                                <div id="drop-zone-csv" class="border-2 border-dashed border-emerald-700/60 hover:border-emerald-400 bg-slate-950/70 hover:bg-slate-950 rounded-xl p-5 text-center cursor-pointer transition-all">
                                    <i data-lucide="file-spreadsheet" class="w-8 h-8 text-emerald-400 mx-auto mb-1.5"></i>
                                    <p class="text-xs text-slate-200 font-semibold">Klik untuk memilih file Excel (.xlsx / .xls) atau drag & drop ke sini</p>
                                    <p class="text-[10px] text-slate-400 mt-1">Sistem langsung membaca sheet Excel dan memetakan kolom secara otomatis</p>
                                    <input type="file" id="input-csv-file" accept=".xlsx,.xls,.csv,.txt" class="hidden" />
                                </div>
                            </div>

                            <!-- Paste Textarea Box -->
                            <div>
                                <div class="flex items-center justify-between mb-1.5">
                                    <label class="text-xs font-semibold text-slate-300">Opsi 2: Atau Salin (Copy-Paste) Sel Data Langsung dari Excel</label>
                                    <button type="button" id="btn-load-sample-csv" class="text-[11px] text-cyan-400 hover:underline cursor-pointer">
                                        Muat Contoh Teks
                                    </button>
                                </div>
                                <textarea id="textarea-csv-raw" rows="4" placeholder="Salin sel tabel langsung dari Excel lalu paste di sini...&#10;Contoh:&#10;${sampleRowsExample}" class="w-full bg-slate-950 border border-slate-800 rounded-xl p-3 text-xs font-mono text-slate-200 placeholder:text-slate-600 focus:outline-none focus:border-cyan-500"></textarea>
                            </div>
                        </div>

                        <!-- LIVE PREVIEW ACCORDION / BOX -->
                        <div id="import-preview-container" class="hidden space-y-2">
                            <div class="flex items-center justify-between">
                                <span class="text-xs font-bold text-slate-300 flex items-center gap-1.5">
                                    <i data-lucide="check-circle-2" class="w-4 h-4 text-emerald-400"></i>
                                    Pratinjau Data Terdeteksi (<span id="preview-row-count" class="text-cyan-400 font-mono">0</span> Baris)
                                </span>
                                <span class="text-[10px] text-slate-500">Menampilkan maksimal 5 baris pertama</span>
                            </div>
                            <div class="overflow-x-auto max-h-48 border border-slate-800 rounded-lg bg-slate-950">
                                <table class="w-full text-left text-[11px]">
                                    <thead id="preview-table-head" class="bg-slate-900 text-slate-400 uppercase font-semibold text-[9px] border-b border-slate-800"></thead>
                                    <tbody id="preview-table-body" class="divide-y divide-slate-800/60 font-mono text-slate-300"></tbody>
                                </table>
                            </div>
                        </div>

                        <!-- FOOTER ACTIONS -->
                        <div class="pt-4 flex items-center justify-between border-t border-slate-800">
                            <button type="button" id="btn-cancel-import-modal" class="px-4 py-2 bg-slate-800 hover:bg-slate-700 text-slate-300 text-xs font-medium rounded-lg cursor-pointer transition-all">
                                Batal
                            </button>
                            <button type="button" id="btn-execute-import" class="px-5 py-2 bg-emerald-600 hover:bg-emerald-500 text-white text-xs font-bold rounded-lg shadow-lg shadow-emerald-600/30 flex items-center gap-2 cursor-pointer transition-all">
                                <i data-lucide="upload-cloud" class="w-4 h-4"></i>
                                <span>Mulai Import Data</span>
                            </button>
                        </div>
                    </div>
                </div>
            `;
        };

        modalContainer.innerHTML = renderModalContent(currentType);
        if (window.lucide) window.lucide.createIcons();
        if (this.currentLang === 'ja') {
            i18n.localizeDom(modalContainer);
        }

        // Helper to bind all modal events
        const bindModalEvents = () => {
            const selectType = document.getElementById('select-import-target-type');
            const dropZone = document.getElementById('drop-zone-csv');
            const fileInput = document.getElementById('input-csv-file');
            const textarea = document.getElementById('textarea-csv-raw');
            const previewContainer = document.getElementById('import-preview-container');
            const previewRowCount = document.getElementById('preview-row-count');
            const previewHead = document.getElementById('preview-table-head');
            const previewBody = document.getElementById('preview-table-body');
            const btnExecute = document.getElementById('btn-execute-import');
            const btnDownloadTpl = document.getElementById('btn-modal-download-template');
            const btnLoadSample = document.getElementById('btn-load-sample-csv');

            let parsedRowsCache = [];

            // Switch target master type
            if (selectType) {
                selectType.addEventListener('change', (e) => {
                    currentType = e.target.value;
                    modalContainer.innerHTML = renderModalContent(currentType);
                    if (window.lucide) window.lucide.createIcons();
                    if (this.currentLang === 'ja') {
                        i18n.localizeDom(modalContainer);
                    }
                    bindModalEvents();
                });
            }

            // Download Template button in modal
            if (btnDownloadTpl) {
                btnDownloadTpl.addEventListener('click', () => {
                    this.downloadMasterTemplate(currentType);
                });
            }

            // Load Sample data
            if (btnLoadSample) {
                btnLoadSample.addEventListener('click', () => {
                    if (currentType === 'ng_sections') {
                        textarea.value = `code\tname\tis_active\n` +
                            `SEC-PIN-BORE\tSmall End (Pin Bore)\t1\n` +
                            `SEC-CRANK-BORE\tBig End (Crank Bore)\t1\n` +
                            `SEC-ROD-BODY\tRod Body / Shank\t1\n` +
                            `SEC-BOLT-HOLE\tBolt Hole Threads\t1\n` +
                            `SEC-CAP-BODY\tCap Body\t1\n` +
                            `SEC-JOINT-FACE\tJoint / Serration Face\t1\n` +
                            `SEC-SIDE-FACE\tThrust / Side Face\t1`;
                    } else {
                        textarea.value = `category\tname\n` +
                            `Dimensi & Geometri\tDiameter Out of Spec\n` +
                            `Dimensi & Geometri\tBending & Twist\n` +
                            `Dimensi & Geometri\tThickness Out of Spec\n` +
                            `Kualitas Permukaan & Visual\tSurface Scratch & Dent\n` +
                            `Kualitas Permukaan & Visual\tRough Surface Finish\n` +
                            `Cacat Material & Cor (Foundry)\tBlow Hole / Porosity\n` +
                            `Proses Machining & Assembly\tHeavy Burr & Flash`;
                    }
                    parseAndPreviewText(textarea.value);
                });
            }

            // Function to render preview table from JSON rows array (from Excel or text)
            const renderPreviewFromRows = (rows) => {
                if (!rows || rows.length === 0) {
                    previewContainer.classList.add('hidden');
                    parsedRowsCache = [];
                    return;
                }

                parsedRowsCache = rows;
                previewRowCount.textContent = rows.length;

                const headers = Object.keys(rows[0]);
                previewHead.innerHTML = `<tr>${headers.map(h => `<th class="p-2.5 whitespace-nowrap">${this.escapeHtml(h)}</th>`).join('')}</tr>`;

                const displayRows = rows.slice(0, 5);
                previewBody.innerHTML = displayRows.map(r => `
                    <tr class="hover:bg-slate-900/60">
                        ${headers.map(h => `<td class="p-2 text-slate-300 font-mono whitespace-nowrap">${this.escapeHtml(String(r[h] ?? '-'))}</td>`).join('')}
                    </tr>
                `).join('');

                previewContainer.classList.remove('hidden');
            };

            // Function to parse CSV/TSV text and render preview
            const parseAndPreviewText = (text) => {
                if (!text || !text.trim()) {
                    previewContainer.classList.add('hidden');
                    parsedRowsCache = [];
                    return;
                }

                text = text.replace(/^\uFEFF/, '');
                const rawLines = text.split(/\r?\n/).filter(l => l.trim().length > 0);
                if (rawLines.length === 0) {
                    previewContainer.classList.add('hidden');
                    return;
                }

                const firstLine = rawLines[0];
                let separator = '\t';
                if (firstLine.includes('\t')) separator = '\t';
                else if (firstLine.split(';').length > firstLine.split(',').length) separator = ';';
                else if (firstLine.includes(',')) separator = ',';

                const headers = firstLine.split(separator).map(h => h.trim().replace(/^["']|["']$/g, ''));
                const rows = [];

                for (let i = 1; i < rawLines.length; i++) {
                    const cols = rawLines[i].split(separator).map(c => c.trim().replace(/^["']|["']$/g, ''));
                    if (cols.length > 0 && cols.some(c => c.length > 0)) {
                        const obj = {};
                        headers.forEach((h, idx) => { obj[h] = cols[idx] || ''; });
                        rows.push(obj);
                    }
                }

                renderPreviewFromRows(rows);
            };

            // Textarea live input preview
            if (textarea) {
                textarea.addEventListener('input', (e) => {
                    parseAndPreviewText(e.target.value);
                });
            }

            // File selection & Dropzone for Excel / CSV
            if (dropZone && fileInput) {
                dropZone.addEventListener('click', () => fileInput.click());
                dropZone.addEventListener('dragover', (e) => {
                    e.preventDefault();
                    dropZone.classList.add('border-emerald-400', 'bg-slate-950');
                });
                dropZone.addEventListener('dragleave', () => {
                    dropZone.classList.remove('border-emerald-400', 'bg-slate-950');
                });
                dropZone.addEventListener('drop', (e) => {
                    e.preventDefault();
                    dropZone.classList.remove('border-emerald-400', 'bg-slate-950');
                    if (e.dataTransfer.files && e.dataTransfer.files[0]) {
                        handleUploadedExcelFile(e.dataTransfer.files[0]);
                    }
                });

                fileInput.addEventListener('change', (e) => {
                    if (e.target.files && e.target.files[0]) {
                        handleUploadedExcelFile(e.target.files[0]);
                    }
                });
            }

            const handleUploadedExcelFile = (file) => {
                const fileName = file.name.toLowerCase();
                if (fileName.endsWith('.xlsx') || fileName.endsWith('.xls')) {
                    const reader = new FileReader();
                    reader.onload = (e) => {
                        try {
                            const data = new Uint8Array(e.target.result);
                            const workbook = XLSX.read(data, { type: 'array' });
                            const firstSheetName = workbook.SheetNames[0];
                            const worksheet = workbook.Sheets[firstSheetName];
                            const jsonRows = XLSX.utils.sheet_to_json(worksheet, { defval: '' });
                            renderPreviewFromRows(jsonRows);
                        } catch (err) {
                            alert('Gagal membaca file Excel: ' + err.message);
                        }
                    };
                    reader.readAsArrayBuffer(file);
                } else {
                    // CSV or Text file
                    const reader = new FileReader();
                    reader.onload = (e) => {
                        const content = e.target.result;
                        textarea.value = content;
                        parseAndPreviewText(content);
                    };
                    reader.readAsText(file);
                }
            };

            // Close actions
            const closeBtn = document.getElementById('btn-close-import-modal');
            const cancelBtn = document.getElementById('btn-cancel-import-modal');
            if (closeBtn) closeBtn.addEventListener('click', () => modalContainer.innerHTML = '');
            if (cancelBtn) cancelBtn.addEventListener('click', () => modalContainer.innerHTML = '');

            // Execute Import
            if (btnExecute) {
                btnExecute.addEventListener('click', async () => {
                    if (!parsedRowsCache || parsedRowsCache.length === 0) {
                        alert('Silakan pilih file Excel (.xlsx / .xls) atau tempel (paste) data terlebih dahulu!');
                        return;
                    }

                    btnExecute.disabled = true;
                    btnExecute.innerHTML = `<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Mengimpor ${parsedRowsCache.length} Data...`;
                    if (window.lucide) window.lucide.createIcons();

                    try {
                        let res;
                        if (currentType === 'ng_sections') {
                            res = await api.importNgSections({ rows: parsedRowsCache });
                        } else {
                            res = await api.importDefectReasons({ rows: parsedRowsCache });
                        }

                        if (res.data?.success) {
                            this.showNotification(
                                'Import Excel Berhasil!',
                                res.data.message || `${parsedRowsCache.length} data berhasil diimport ke master ${currentType === 'ng_sections' ? 'Bagian NG' : 'Penyebab Defect'}.`,
                                'success'
                            );
                            modalContainer.innerHTML = '';
                            await this.loadMasterData();
                            this.renderMaster();
                        } else {
                            alert('Gagal mengimpor data: ' + (res.data?.message || 'Terjadi kesalahan'));
                            btnExecute.disabled = false;
                            btnExecute.innerHTML = `<i data-lucide="upload-cloud" class="w-4 h-4"></i> Mulai Import Data`;
                            if (window.lucide) window.lucide.createIcons();
                        }
                    } catch (err) {
                        console.error('Import error:', err);
                        alert('Error saat import: ' + (err.response?.data?.message || err.message));
                        btnExecute.disabled = false;
                        btnExecute.innerHTML = `<i data-lucide="upload-cloud" class="w-4 h-4"></i> Mulai Import Data`;
                        if (window.lucide) window.lucide.createIcons();
                    }
                });
            }
        };

        bindModalEvents();
    }

    showAddMasterModal(type) {
        const modalContainer = document.getElementById('modal-container');
        let fieldsHtml = '';
        let title = `Tambah Master ${type.toUpperCase()}`;

        if (type === 'machines') {
            const lineOptions = (this.masterData.lines || []).map(l => `<option value="${l.id}">${l.name} (${l.code})</option>`).join('');
            fieldsHtml = `
                <div>
                    <label class="block text-slate-400 mb-1">Machine Code (IDMC)</label>
                    <input type="text" name="code" placeholder="e.g. MC-FX-120" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200" />
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Machine Name</label>
                    <input type="text" name="name" placeholder="e.g. OP-20" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200" />
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Production Line / Location</label>
                    <select name="work_center_id" class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200">
                        ${lineOptions || '<option value="1">Default Work Center</option>'}
                    </select>
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Initial Status</label>
                    <select name="status" class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200">
                        <option value="RUNNING">RUNNING</option>
                        <option value="IDLE">IDLE</option>
                        <option value="BREAKDOWN">BREAKDOWN</option>
                        <option value="MAINTENANCE">MAINTENANCE</option>
                    </select>
                </div>
            `;
        } else if (type === 'lines') {
            const plantOptions = (this.masterData.plants || []).map(p => `<option value="${p.id}">${p.name} (${p.code})</option>`).join('');
            fieldsHtml = `
                <div>
                    <label class="block text-slate-400 mb-1">Line Code</label>
                    <input type="text" name="code" placeholder="e.g. LINE-CR-01" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200" />
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Line Name</label>
                    <input type="text" name="name" placeholder="e.g. Line Machining Connecting Rod" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200" />
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Plant Location</label>
                    <select name="plant_id" class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200">
                        ${plantOptions}
                    </select>
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Target OEE (%)</label>
                    <input type="number" name="target_oee" value="85" min="0" max="100" class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200" />
                </div>
            `;
        } else if (type === 'products') {
            const lineOptions = (this.masterData?.lines || []).map(l => `<option value="${l.id}">${l.name} (${l.code})</option>`).join('');
            fieldsHtml = `
                <div>
                    <label class="block text-slate-400 mb-1">Production Line (Lini Produksi) <span class="text-rose-400">*</span></label>
                    <select name="production_line_id" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200">
                        <option value="" disabled selected>-- Pilih Line Produksi --</option>
                        ${lineOptions}
                    </select>
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Part / Model Name (Nama Produk) <span class="text-rose-400">*</span></label>
                    <input type="text" name="name" placeholder="e.g. OP10-1NR-ADM" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200" />
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Part Number / SKU <span class="text-rose-400">*</span></label>
                    <input type="text" name="sku" placeholder="e.g. 13201-0Y070 OP10 ADM" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 font-mono font-bold" />
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Ideal Cycle Time (Detik per Piece) <span class="text-rose-400">*</span></label>
                    <input type="number" step="0.1" name="ideal_cycle_time" placeholder="e.g. 5.5" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 font-mono" />
                </div>
            `;
        } else if (type === 'plants') {
            fieldsHtml = `
                <div>
                    <label class="block text-slate-400 mb-1">Plant Code</label>
                    <input type="text" name="code" placeholder="e.g. PLT-01" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200" />
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Plant Name</label>
                    <input type="text" name="name" placeholder="e.g. Jakarta Main Plant" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200" />
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Location / Address</label>
                    <input type="text" name="location" placeholder="e.g. Kawasan Industri MM2100" class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200" />
                </div>
            `;
        } else if (type === 'downtime') {
            fieldsHtml = `
                <div>
                    <label class="block text-slate-400 mb-1">Reason Code</label>
                    <input type="text" name="code" placeholder="e.g. DT-MCH-01" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200" />
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Reason Name</label>
                    <input type="text" name="name" placeholder="e.g. Tool Broken / Replace" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200" />
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Category</label>
                    <select name="category" class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200">
                        <option value="Mechanical">Mechanical</option>
                        <option value="Electrical">Electrical</option>
                        <option value="Operational">Operational</option>
                        <option value="Quality">Quality</option>
                        <option value="Planned">Planned</option>
                    </select>
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Downtime Type</label>
                    <select name="is_planned" class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200">
                        <option value="0">Unplanned Downtime</option>
                        <option value="1">Planned Downtime</option>
                    </select>
                </div>
            `;
        } else if (type === 'shifts') {
            fieldsHtml = `
                <div>
                    <label class="block text-slate-400 mb-1">Shift Name</label>
                    <input type="text" name="name" placeholder="e.g. Shift 1 (Pagi)" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200" />
                </div>
                <div class="grid grid-cols-2 gap-3">
                    <div>
                        <label class="block text-slate-400 mb-1">Start Time (Jam Masuk)</label>
                        <input type="text" name="start_time" placeholder="07:30" value="07:30:00" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 font-mono" />
                    </div>
                    <div>
                        <label class="block text-slate-400 mb-1">End Time (Jam Pulang)</label>
                        <input type="text" name="end_time" placeholder="16:30" value="16:30:00" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 font-mono" />
                    </div>
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Break Duration / Istirahat (Menit)</label>
                    <input type="number" name="break_duration_minutes" value="60" min="0" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 font-mono" />
                </div>
            `;
        } else if (type === 'groups') {
            const lineOptions = (this.masterData?.lines || []).map(l => `<option value="${l.id}">${l.name} (${l.code})</option>`).join('');
            fieldsHtml = `
                <div>
                    <label class="block text-slate-400 mb-1">Production Line</label>
                    <select name="production_line_id" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200">
                        ${lineOptions}
                    </select>
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Team (Regu)</label>
                    <select name="team" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 font-bold">
                        <option value="Team A">Team A</option>
                        <option value="Team B">Team B</option>
                        <option value="Team C">Team C</option>
                    </select>
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Leader Name / PIC</label>
                    <input type="text" name="leader_name" placeholder="e.g. Budi Santoso" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 font-sans" />
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Supervisor Name</label>
                    <input type="text" name="supervisor_name" placeholder="e.g. Alim Utama" class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 font-sans" />
                </div>
            `;
        } else if (type === 'ng_sections') {
            title = 'Tambah Master Bagian NG';
            fieldsHtml = `
                <div>
                    <label class="block text-slate-400 mb-1">Kode Bagian NG <span class="text-rose-400">*</span></label>
                    <input type="text" name="code" placeholder="e.g. SEC-PIN-BORE" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 font-mono font-bold" />
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Nama Bagian NG <span class="text-rose-400">*</span></label>
                    <input type="text" name="name" placeholder="e.g. Small End (Pin Bore)" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200" />
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Status Aktif</label>
                    <select name="is_active" class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 font-semibold">
                        <option value="1">Aktif (1)</option>
                        <option value="0">Nonaktif (0)</option>
                    </select>
                </div>
            `;
        } else if (type === 'defect_reasons') {
            title = 'Tambah Master Penyebab / Remark Defect';
            const catOptions = (this.masterData.defectCategories || [
                { id: 1, name: 'Dimensi & Geometri' },
                { id: 2, name: 'Kualitas Permukaan & Visual' },
                { id: 3, name: 'Cacat Material & Cor (Foundry)' },
                { id: 4, name: 'Proses Machining & Assembly' },
            ]).map(cat => `<option value="${cat.id}">${cat.name}</option>`).join('');

            fieldsHtml = `
                <div>
                    <label class="block text-slate-400 mb-1">Kategori Defect <span class="text-rose-400">*</span></label>
                    <select name="defect_category_id" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 font-semibold">
                        ${catOptions}
                    </select>
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Nama Penyebab / Remark <span class="text-rose-400">*</span></label>
                    <input type="text" name="name" placeholder="e.g. Diameter Out of Spec" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200" />
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Rekomendasi Tindakan Korektif (Kaizen SOP)</label>
                    <textarea name="countermeasure" rows="3" placeholder="Langkah perbaikan teknis atau SOP Kaizen untuk defect ini..." class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 text-xs"></textarea>
                </div>
            `;
        }

        modalContainer.innerHTML = `
            <div class="fixed inset-0 bg-slate-950/80 backdrop-blur-sm flex items-center justify-center p-4 z-50">
                <div class="bg-slate-900 border border-slate-800 rounded-xl p-6 max-w-md w-full shadow-2xl">
                    <div class="flex items-center justify-between mb-4 pb-3 border-b border-slate-800">
                        <h3 class="font-bold text-sm text-slate-100 flex items-center gap-2">
                            <i data-lucide="plus" class="w-4 h-4 text-cyan-400"></i>
                            ${title}
                        </h3>
                        <button id="btn-close-add-modal" class="text-slate-400 hover:text-slate-200 cursor-pointer">
                            <i data-lucide="x" class="w-4 h-4"></i>
                        </button>
                    </div>

                    <form id="form-add-master" class="space-y-4 text-xs">
                        ${fieldsHtml}
                        <div class="pt-4 flex justify-end gap-2 border-t border-slate-800">
                            <button type="button" id="btn-cancel-add-modal" class="px-3 py-1.5 bg-slate-800 hover:bg-slate-700 text-slate-300 rounded font-medium cursor-pointer">Cancel</button>
                            <button type="submit" class="px-4 py-1.5 bg-cyan-600 hover:bg-cyan-500 text-white font-semibold rounded shadow-md shadow-cyan-600/20 cursor-pointer">Save Record</button>
                        </div>
                    </form>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();
        if (this.currentLang === 'ja') {
            i18n.localizeDom(modalContainer);
        }

        document.getElementById('btn-close-add-modal').addEventListener('click', () => modalContainer.innerHTML = '');
        document.getElementById('btn-cancel-add-modal').addEventListener('click', () => modalContainer.innerHTML = '');

        document.getElementById('form-add-master').addEventListener('submit', async (e) => {
            e.preventDefault();
            const formData = new FormData(e.target);
            const data = Object.fromEntries(formData.entries());

            try {
                if (type === 'machines') await api.createMachine(data);
                else if (type === 'lines') await api.createProductionLine(data);
                else if (type === 'products') await api.createProduct(data);
                else if (type === 'plants') await api.createPlant(data);
                else if (type === 'downtime') await api.createDowntimeReason(data);
                else if (type === 'shifts') await api.createShift(data);
                else if (type === 'groups') await api.createGroup(data);
                else if (type === 'ng_sections') await api.createNgSection(data);
                else if (type === 'defect_reasons') await api.createDefectReason(data);

                this.showNotification(`Master ${type.toUpperCase()} Created`, `New ${type} record (${data.code || data.sku || data.name || ''}) created successfully!`, 'create');
                modalContainer.innerHTML = '';
                await this.loadMasterData(true);
                this.clearDashboardCache();
                await this.loadCurrentTab(false);
            } catch (err) {
                alert('Error creating master record: ' + (err.response?.data?.message || err.message));
            }
        });
    }

    showEditMasterModal(type, id) {
        const modalContainer = document.getElementById('modal-container');
        let fieldsHtml = '';
        let title = `Edit Master ${type.toUpperCase()}`;
        let item = null;

        if (type === 'machines') item = this.masterData.machines?.find(m => m.id == id);
        else if (type === 'lines') item = this.masterData.lines?.find(l => l.id == id);
        else if (type === 'products') item = this.masterData.products?.find(p => p.id == id);
        else if (type === 'plants') item = this.masterData.plants?.find(p => p.id == id);
        else if (type === 'downtime') item = this.masterData.downtimeReasons?.find(d => d.id == id);
        else if (type === 'shifts') item = this.masterData.shifts?.find(s => s.id == id);
        else if (type === 'groups') item = this.masterData.groups?.find(g => g.id == id);
        else if (type === 'ng_sections') item = this.masterData.ngSections?.find(s => s.id == id);
        else if (type === 'defect_reasons') item = this.masterData.defectReasons?.find(r => r.id == id);

        if (!item) {
            alert('Data master tidak ditemukan!');
            return;
        }

        if (type === 'machines') {
            const lineOptions = (this.masterData.lines || []).map(l => 
                `<option value="${l.id}" ${l.id == (item.work_center?.production_line_id || item.work_center_id) ? 'selected' : ''}>${l.name} (${l.code})</option>`
            ).join('');
            fieldsHtml = `
                <div>
                    <label class="block text-slate-400 mb-1">Machine Code (IDMC)</label>
                    <input type="text" name="code" value="${item.code || ''}" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200" />
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Machine Name</label>
                    <input type="text" name="name" value="${item.name || ''}" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200" />
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Production Line / Location</label>
                    <select name="work_center_id" class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200">
                        ${lineOptions}
                    </select>
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Status</label>
                    <select name="status" class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200">
                        <option value="RUNNING" ${item.status === 'RUNNING' ? 'selected' : ''}>RUNNING</option>
                        <option value="IDLE" ${item.status === 'IDLE' ? 'selected' : ''}>IDLE</option>
                        <option value="BREAKDOWN" ${item.status === 'BREAKDOWN' ? 'selected' : ''}>BREAKDOWN</option>
                        <option value="MAINTENANCE" ${item.status === 'MAINTENANCE' ? 'selected' : ''}>MAINTENANCE</option>
                    </select>
                </div>
            `;
        } else if (type === 'lines') {
            const plantOptions = (this.masterData.plants || []).map(p => 
                `<option value="${p.id}" ${p.id == item.plant_id ? 'selected' : ''}>${p.name} (${p.code})</option>`
            ).join('');
            fieldsHtml = `
                <div>
                    <label class="block text-slate-400 mb-1">Line Code</label>
                    <input type="text" name="code" value="${item.code || ''}" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200" />
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Line Name</label>
                    <input type="text" name="name" value="${item.name || ''}" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200" />
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Plant Location</label>
                    <select name="plant_id" class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200">
                        ${plantOptions}
                    </select>
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Target OEE (%)</label>
                    <input type="number" name="target_oee" value="${item.target_oee || 85}" min="0" max="100" class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200" />
                </div>
            `;
        } else if (type === 'products') {
            const lineOptions = (this.masterData?.lines || []).map(l => 
                `<option value="${l.id}" ${l.id == item.production_line_id ? 'selected' : ''}>${l.name} (${l.code})</option>`
            ).join('');
            fieldsHtml = `
                <div>
                    <label class="block text-slate-400 mb-1">Production Line (Lini Produksi) <span class="text-rose-400">*</span></label>
                    <select name="production_line_id" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200">
                        <option value="" disabled ${!item.production_line_id ? 'selected' : ''}>-- Pilih Line Produksi --</option>
                        ${lineOptions}
                    </select>
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Part / Model Name (Nama Produk) <span class="text-rose-400">*</span></label>
                    <input type="text" name="name" value="${item.name || ''}" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200" />
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Part Number / SKU <span class="text-rose-400">*</span></label>
                    <input type="text" name="sku" value="${item.sku || ''}" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 font-mono font-bold" />
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Ideal Cycle Time (Detik per Piece) <span class="text-rose-400">*</span></label>
                    <input type="number" step="0.1" name="ideal_cycle_time" value="${item.ideal_cycle_time || ''}" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 font-mono" />
                </div>
            `;
        } else if (type === 'shifts') {
            fieldsHtml = `
                <div>
                    <label class="block text-slate-400 mb-1">Shift Name</label>
                    <input type="text" name="name" value="${item.name || ''}" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200" />
                </div>
                <div class="grid grid-cols-2 gap-3">
                    <div>
                        <label class="block text-slate-400 mb-1">Start Time (Jam Masuk)</label>
                        <input type="text" name="start_time" value="${(item.start_time || '07:30').slice(0,5)}" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 font-mono" />
                    </div>
                    <div>
                        <label class="block text-slate-400 mb-1">End Time (Jam Pulang)</label>
                        <input type="text" name="end_time" value="${(item.end_time || '16:30').slice(0,5)}" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 font-mono" />
                    </div>
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Break Duration / Istirahat (Menit)</label>
                    <input type="number" name="break_duration_minutes" value="${item.break_duration_minutes || 60}" min="0" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 font-mono" />
                </div>
            `;
        } else if (type === 'groups') {
            title = 'Edit Master Group Leader & Supervisor';
            const lineOptions = (this.masterData?.lines || []).map(l => 
                `<option value="${l.id}" ${l.id == item.production_line_id ? 'selected' : ''}>${l.name} (${l.code})</option>`
            ).join('');
            fieldsHtml = `
                <div>
                    <label class="block text-slate-400 mb-1">Production Line</label>
                    <select name="production_line_id" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200">
                        ${lineOptions}
                    </select>
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Team (Regu)</label>
                    <select name="team" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 font-bold">
                        <option value="Team A" ${item.team === 'Team A' ? 'selected' : ''}>Team A</option>
                        <option value="Team B" ${item.team === 'Team B' ? 'selected' : ''}>Team B</option>
                        <option value="Team C" ${item.team === 'Team C' ? 'selected' : ''}>Team C</option>
                    </select>
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Leader Name / PIC</label>
                    <input type="text" name="leader_name" value="${item.leader_name || ''}" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 font-sans" />
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Supervisor Name</label>
                    <input type="text" name="supervisor_name" value="${item.supervisor_name || ''}" placeholder="e.g. Alim Utama" class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 font-sans" />
                </div>
            `;
        } else if (type === 'ng_sections') {
            title = 'Edit Master Bagian NG';
            fieldsHtml = `
                <div>
                    <label class="block text-slate-400 mb-1">Kode Bagian NG <span class="text-rose-400">*</span></label>
                    <input type="text" name="code" value="${item.code || ''}" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 font-mono font-bold" />
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Nama Bagian NG <span class="text-rose-400">*</span></label>
                    <input type="text" name="name" value="${item.name || ''}" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200" />
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Status Aktif</label>
                    <select name="is_active" class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 font-semibold">
                        <option value="1" ${item.is_active ? 'selected' : ''}>Aktif (1)</option>
                        <option value="0" ${!item.is_active ? 'selected' : ''}>Nonaktif (0)</option>
                    </select>
                </div>
            `;
        } else if (type === 'defect_reasons') {
            title = 'Edit Master Penyebab / Remark Defect';
            const currentCatId = item.defect_category_id || item.defect_category?.id || item.defectCategory?.id;
            const catOptions = (this.masterData.defectCategories || [
                { id: 1, name: 'Dimensi & Geometri' },
                { id: 2, name: 'Kualitas Permukaan & Visual' },
                { id: 3, name: 'Cacat Material & Cor (Foundry)' },
                { id: 4, name: 'Proses Machining & Assembly' },
            ]).map(cat => `<option value="${cat.id}" ${cat.id == currentCatId ? 'selected' : ''}>${cat.name}</option>`).join('');

            fieldsHtml = `
                <div>
                    <label class="block text-slate-400 mb-1">Kategori Defect <span class="text-rose-400">*</span></label>
                    <select name="defect_category_id" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 font-semibold">
                        ${catOptions}
                    </select>
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Nama Penyebab / Remark <span class="text-rose-400">*</span></label>
                    <input type="text" name="name" value="${item.name || ''}" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200" />
                </div>
                <div>
                    <label class="block text-slate-400 mb-1">Rekomendasi Tindakan Korektif (Kaizen SOP)</label>
                    <textarea name="countermeasure" rows="3" placeholder="Langkah perbaikan teknis atau SOP Kaizen untuk defect ini..." class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-1.5 text-slate-200 text-xs">${item.countermeasure || ''}</textarea>
                </div>
            `;
        }

        modalContainer.innerHTML = `
            <div class="fixed inset-0 bg-slate-950/80 backdrop-blur-sm flex items-center justify-center p-4 z-50">
                <div class="bg-slate-900 border border-slate-800 rounded-xl p-6 max-w-md w-full shadow-2xl">
                    <div class="flex items-center justify-between mb-4 pb-3 border-b border-slate-800">
                        <h3 class="font-bold text-sm text-slate-100 flex items-center gap-2">
                            <i data-lucide="pencil" class="w-4 h-4 text-cyan-400"></i>
                            ${title}
                        </h3>
                        <button id="btn-close-edit-modal" class="text-slate-400 hover:text-slate-200 cursor-pointer">
                            <i data-lucide="x" class="w-4 h-4"></i>
                        </button>
                    </div>

                    <form id="form-edit-master" class="space-y-4 text-xs">
                        ${fieldsHtml}
                        <div class="pt-4 flex justify-end gap-2 border-t border-slate-800">
                            <button type="button" id="btn-cancel-edit-modal" class="px-3 py-1.5 bg-slate-800 hover:bg-slate-700 text-slate-300 rounded font-medium cursor-pointer">Cancel</button>
                            <button type="submit" class="px-4 py-1.5 bg-cyan-600 hover:bg-cyan-500 text-white font-semibold rounded shadow-md shadow-cyan-600/20 cursor-pointer">Save Changes</button>
                        </div>
                    </form>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();
        if (this.currentLang === 'ja') {
            i18n.localizeDom(modalContainer);
        }

        document.getElementById('btn-close-edit-modal').addEventListener('click', () => modalContainer.innerHTML = '');
        document.getElementById('btn-cancel-edit-modal').addEventListener('click', () => modalContainer.innerHTML = '');

        document.getElementById('form-edit-master').addEventListener('submit', async (e) => {
            e.preventDefault();
            const formData = new FormData(e.target);
            const data = Object.fromEntries(formData.entries());

            try {
                if (type === 'machines') await api.updateMachine(id, data);
                else if (type === 'lines') await api.updateProductionLine(id, data);
                else if (type === 'products') await api.updateProduct(id, data);
                else if (type === 'plants') await api.updatePlant(id, data);
                else if (type === 'downtime') await api.updateDowntimeReason(id, data);
                else if (type === 'shifts') await api.updateShift(id, data);
                else if (type === 'groups') await api.updateGroup(id, data);
                else if (type === 'ng_sections') await api.updateNgSection(id, data);
                else if (type === 'defect_reasons') await api.updateDefectReason(id, data);

                this.showNotification(`Master ${type.toUpperCase()} Updated`, `Record #${id} (${data.name || data.code || ''}) updated successfully!`, 'update');
                modalContainer.innerHTML = '';
                await this.loadMasterData(true);
                this.clearDashboardCache();
                await this.loadCurrentTab(false);
            } catch (err) {
                alert('Error updating master record: ' + (err.response?.data?.message || err.message));
            }
        });
    }

    showDeleteMasterModal(type, id, name) {
        const modalContainer = document.getElementById('modal-container');

        modalContainer.innerHTML = `
            <div class="fixed inset-0 bg-slate-950/80 backdrop-blur-sm flex items-center justify-center p-4 z-50">
                <div class="bg-slate-900 border border-slate-800 rounded-xl p-6 max-w-md w-full shadow-2xl">
                    <div class="flex items-center gap-3 mb-4 text-rose-400">
                        <div class="w-10 h-10 rounded-full bg-rose-950/80 border border-rose-800/80 flex items-center justify-center flex-shrink-0">
                            <i data-lucide="alert-triangle" class="w-5 h-5"></i>
                        </div>
                        <div>
                            <h3 class="font-bold text-sm text-slate-100">Delete Record</h3>
                            <p class="text-xs text-slate-400">This action will remove the record from master data.</p>
                        </div>
                    </div>

                    <div class="p-3 bg-slate-950 border border-slate-800 rounded-lg text-xs text-slate-300 mb-5">
                        Are you sure you want to delete <span class="font-bold text-rose-400">${name}</span>?
                    </div>

                    <div class="flex justify-end gap-2">
                        <button type="button" id="btn-cancel-delete" class="px-3 py-1.5 bg-slate-800 hover:bg-slate-700 text-slate-300 rounded text-xs font-medium cursor-pointer">Cancel</button>
                        <button type="button" id="btn-confirm-delete" class="px-4 py-1.5 bg-rose-600 hover:bg-rose-500 text-white text-xs font-semibold rounded shadow-md shadow-rose-600/20 cursor-pointer">Delete Record</button>
                    </div>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();
        if (this.currentLang === 'ja') {
            i18n.localizeDom(modalContainer);
        }

        document.getElementById('btn-cancel-delete').addEventListener('click', () => modalContainer.innerHTML = '');

        document.getElementById('btn-confirm-delete').addEventListener('click', async () => {
            try {
                if (type === 'machines') await api.deleteMachine(id);
                else if (type === 'lines') await api.deleteProductionLine(id);
                else if (type === 'products') await api.deleteProduct(id);
                else if (type === 'plants') await api.deletePlant(id);
                else if (type === 'downtime') await api.deleteDowntimeReason(id);
                else if (type === 'shifts') await api.deleteShift(id);
                else if (type === 'groups') await api.deleteGroup(id);
                else if (type === 'ng_sections') await api.deleteNgSection(id);
                else if (type === 'defect_reasons') await api.deleteDefectReason(id);

                this.showNotification(`Master ${type.toUpperCase()} Deleted`, `Record ${name || '#' + id} deleted from database.`, 'delete');
                modalContainer.innerHTML = '';
                await this.loadMasterData(true);
                this.clearDashboardCache();
                await this.loadCurrentTab(false);
            } catch (err) {
                alert('Error deleting record: ' + (err.response?.data?.message || err.message));
            }
        });
    }

    // ==========================================
    // 3B-VIEW-PROD. MODAL POPUP: DETAIL VIEW PRODUCTION RECORD
    // ==========================================
    showViewProductionModal(id, records) {
        const modalContainer = document.getElementById('modal-container');
        const item = (records || []).find(r => r.id == id);
        if (!item) return;

        const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');
        const prodDate = item.production_date ? String(item.production_date).slice(0, 10) : (this.selectedDailyDate || '-');
        const lineName = item.production_line ? item.production_line.name : `Line #${item.production_line_id}`;
        const lineCode = item.production_line ? item.production_line.code : '';
        const machineName = item.machine ? item.machine.name : `Machine #${item.machine_id}`;
        const machineCode = item.machine ? item.machine.code : '';
        const productName = item.product ? item.product.name : `Product #${item.product_id}`;
        const productSku = item.product ? item.product.sku : '-';
        const idealCycle = item.ideal_cycle_time || (item.product?.ideal_cycle_time) || 9.5;
        const shiftName = item.shift ? item.shift.name : `Shift #${item.shift_id}`;
        
        // OEE metrics
        const oeeScore = Number(item.oee_record?.oee ?? 0);
        const availVal = Number(item.oee_record?.availability ?? 0);
        const perfVal = Number(item.oee_record?.performance ?? 0);
        const qualVal = Number(item.oee_record?.quality ?? 0);

        const oeeBadgeClass = oeeScore >= 85 
            ? 'bg-emerald-500/15 text-emerald-400 border-emerald-500/30' 
            : (oeeScore >= 70 ? 'bg-amber-500/15 text-amber-400 border-amber-500/30' : 'bg-rose-500/15 text-rose-400 border-rose-500/30');
        const oeeStatusLabel = oeeScore >= 85 ? 'EXCELLENT / WORLD CLASS' : (oeeScore >= 70 ? 'GOOD / ON TARGET' : 'CRITICAL / NEEDS ACTION');

        // Quantities
        const targetQty = Number(item.target_quantity || 0);
        const totalQty = Number(item.total_quantity || 0);
        const goodQty = Number(item.good_quantity || 0);
        const rejectQty = Number(item.reject_quantity || 0);
        const scrapQty = Number(item.scrap_quantity || 0);
        const rejectRate = totalQty > 0 ? ((rejectQty / totalQty) * 100).toFixed(2) : '0.00';

        // Times
        const plannedTime = Number(item.planned_production_time || 480);
        const plannedDt = Number(item.planned_downtime || 60);
        const runTime = Number(item.run_time || (plannedTime - plannedDt));
        const unplannedDt = Number(item.downtime || 0);

        modalContainer.innerHTML = `
            <div class="fixed inset-0 bg-slate-950/80 backdrop-blur-sm flex items-center justify-center p-3 sm:p-4 z-50 overflow-y-auto">
                <div class="${isLight ? 'bg-white border-slate-200 text-slate-900 shadow-2xl' : 'bg-slate-900 border-slate-800 text-slate-100 shadow-2xl'} border rounded-2xl max-w-3xl w-full my-auto overflow-hidden font-sans">
                    <!-- MODAL HEADER -->
                    <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950 border-slate-800'} px-6 py-4 border-b flex items-center justify-between">
                        <div class="flex items-center gap-3">
                            <div class="w-10 h-10 rounded-xl ${isLight ? 'bg-blue-100 text-blue-700 border-blue-200' : 'bg-blue-950 text-blue-400 border-blue-800'} border flex items-center justify-center shadow-inner">
                                <i data-lucide="file-text" class="w-5 h-5"></i>
                            </div>
                            <div>
                                <div class="flex items-center gap-2">
                                    <h3 class="font-bold text-base ${isLight ? 'text-slate-900' : 'text-slate-100'}">Detail Laporan Produksi #${item.id}</h3>
                                    <span class="px-2 py-0.5 rounded-full text-[10px] font-bold border ${oeeBadgeClass}">${oeeStatusLabel}</span>
                                </div>
                                <p class="text-xs ${isLight ? 'text-slate-500' : 'text-slate-400'} mt-0.5 font-mono">
                                    📅 ${prodDate} &bull; 🏭 ${lineName} &bull; ⏰ ${shiftName}
                                </p>
                            </div>
                        </div>
                        <button id="btn-close-view-prod-modal" class="p-1.5 rounded-lg ${isLight ? 'text-slate-400 hover:text-slate-800 hover:bg-slate-200' : 'text-slate-400 hover:text-slate-100 hover:bg-slate-800'} cursor-pointer transition-colors">
                            <i data-lucide="x" class="w-5 h-5"></i>
                        </button>
                    </div>

                    <div class="p-6 space-y-5 max-h-[75vh] overflow-y-auto text-xs">
                        <!-- TOP KPI SUMMARY TILES -->
                        <div class="grid grid-cols-2 sm:grid-cols-4 gap-3">
                            <!-- OEE -->
                            <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950/70 border-slate-800'} border rounded-xl p-3 text-center">
                                <div class="text-[10px] uppercase font-bold text-slate-400">Overall OEE</div>
                                <div class="text-2xl font-black font-mono mt-1 ${oeeScore >= 85 ? 'text-emerald-400' : (oeeScore >= 70 ? 'text-amber-400' : 'text-rose-400')}">
                                    ${oeeScore.toFixed(1)}%
                                </div>
                                <div class="text-[10px] font-mono text-slate-500 mt-0.5">Target: 85.0%</div>
                            </div>
                            <!-- AVAILABILITY (BLUE) -->
                            <div class="${isLight ? 'bg-sky-50/50 border-sky-200' : 'bg-slate-950/70 border-sky-900/40'} border rounded-xl p-3 text-center">
                                <div class="text-[10px] uppercase font-bold text-sky-400">Availability (A)</div>
                                <div class="text-2xl font-black font-mono mt-1 text-sky-400">
                                    ${availVal.toFixed(1)}%
                                </div>
                                <div class="text-[10px] font-mono text-slate-500 mt-0.5">Target: 90.0%</div>
                            </div>
                            <!-- PERFORMANCE (GREEN) -->
                            <div class="${isLight ? 'bg-emerald-50/50 border-emerald-200' : 'bg-slate-950/70 border-emerald-900/40'} border rounded-xl p-3 text-center">
                                <div class="text-[10px] uppercase font-bold text-emerald-400">Performance (P)</div>
                                <div class="text-2xl font-black font-mono mt-1 text-emerald-400">
                                    ${perfVal.toFixed(1)}%
                                </div>
                                <div class="text-[10px] font-mono text-slate-500 mt-0.5">Target: 95.0%</div>
                            </div>
                            <!-- QUALITY (ORANGE) -->
                            <div class="${isLight ? 'bg-orange-50/50 border-orange-200' : 'bg-slate-950/70 border-orange-900/40'} border rounded-xl p-3 text-center">
                                <div class="text-[10px] uppercase font-bold text-orange-400">Quality Rate (Q)</div>
                                <div class="text-2xl font-black font-mono mt-1 text-orange-400">
                                    ${qualVal.toFixed(1)}%
                                </div>
                                <div class="text-[10px] font-mono text-slate-500 mt-0.5">Target: 99.0%</div>
                            </div>
                        </div>

                        <!-- GRID 2 KOLOM: PARAMETER OPERASIONAL & MUTU / OUTPUT -->
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            <!-- KOLOM KIRI: PARAMETER OPERASIONAL -->
                            <div class="${isLight ? 'bg-slate-50/80 border-slate-200' : 'bg-slate-950/60 border-slate-800'} border rounded-xl p-4 space-y-2.5">
                                <div class="font-bold text-[11px] ${isLight ? 'text-cyan-800' : 'text-cyan-400'} uppercase tracking-wider flex items-center gap-1.5 border-b ${isLight ? 'border-slate-200' : 'border-slate-800/80'} pb-2">
                                    <i data-lucide="sliders" class="w-3.5 h-3.5"></i>
                                    Parameter Operasional & Mesin
                                </div>
                                <div class="space-y-2 text-xs">
                                    <div class="flex justify-between items-center py-1 border-b ${isLight ? 'border-slate-200/60' : 'border-slate-800/40'}">
                                        <span class="text-slate-400">Tanggal Produksi:</span>
                                        <span class="font-mono font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'}">${prodDate}</span>
                                    </div>
                                    <div class="flex justify-between items-center py-1 border-b ${isLight ? 'border-slate-200/60' : 'border-slate-800/40'}">
                                        <span class="text-slate-400">Production Line:</span>
                                        <span class="font-bold text-cyan-400">${lineName} ${lineCode ? `(${lineCode})` : ''}</span>
                                    </div>
                                    <div class="flex justify-between items-center py-1 border-b ${isLight ? 'border-slate-200/60' : 'border-slate-800/40'}">
                                        <span class="text-slate-400">Mesin Input:</span>
                                        <span class="font-mono font-bold ${isLight ? 'text-slate-800' : 'text-slate-200'}">${machineName} ${machineCode ? `[${machineCode}]` : ''}</span>
                                    </div>
                                    <div class="flex justify-between items-center py-1 border-b ${isLight ? 'border-slate-200/60' : 'border-slate-800/40'}">
                                        <span class="text-slate-400">Part SKU & Produk:</span>
                                        <span class="font-bold text-emerald-400 truncate max-w-[180px] text-right" title="${productName}">${productName} <span class="text-slate-400 font-mono text-[10px]">(${productSku})</span></span>
                                    </div>
                                    <div class="flex justify-between items-center py-1 border-b ${isLight ? 'border-slate-200/60' : 'border-slate-800/40'}">
                                        <span class="text-slate-400">Ideal Cycle Time:</span>
                                        <span class="font-mono font-bold text-cyan-400">${idealCycle} detik / piece</span>
                                    </div>
                                    <div class="flex justify-between items-center py-1">
                                        <span class="text-slate-400">Shift Kerja:</span>
                                        <span class="font-bold ${isLight ? 'text-slate-800' : 'text-slate-200'}">${shiftName}</span>
                                    </div>
                                </div>
                            </div>

                            <!-- KOLOM KANAN: KUANTITAS PRODUKSI & MUTU -->
                            <div class="${isLight ? 'bg-slate-50/80 border-slate-200' : 'bg-slate-950/60 border-slate-800'} border rounded-xl p-4 space-y-2.5">
                                <div class="font-bold text-[11px] ${isLight ? 'text-emerald-800' : 'text-emerald-400'} uppercase tracking-wider flex items-center gap-1.5 border-b ${isLight ? 'border-slate-200' : 'border-slate-800/80'} pb-2">
                                    <i data-lucide="package-check" class="w-3.5 h-3.5"></i>
                                    Hasil Output & Analisis Mutu (Quality)
                                </div>
                                <div class="space-y-2 text-xs">
                                    <div class="flex justify-between items-center py-1 border-b ${isLight ? 'border-slate-200/60' : 'border-slate-800/40'}">
                                        <span class="text-slate-400">Target Qty (Planned):</span>
                                        <span class="font-mono font-bold text-cyan-400">${targetQty.toLocaleString()} pcs</span>
                                    </div>
                                    <div class="flex justify-between items-center py-1 border-b ${isLight ? 'border-slate-200/60' : 'border-slate-800/40'}">
                                        <span class="text-slate-400">Total Measuring (Counter):</span>
                                        <span class="font-mono font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'}">${totalQty.toLocaleString()} pcs</span>
                                    </div>
                                    <div class="flex justify-between items-center py-1 border-b ${isLight ? 'border-slate-200/60' : 'border-slate-800/40'}">
                                        <span class="text-slate-400">Finish Good (OK Qty):</span>
                                        <span class="font-mono font-extrabold text-emerald-400">${goodQty.toLocaleString()} pcs</span>
                                    </div>
                                    <div class="flex justify-between items-center py-1 border-b ${isLight ? 'border-slate-200/60' : 'border-slate-800/40'}">
                                        <span class="text-slate-400">Not Good (NG / Reject):</span>
                                        <span class="font-mono font-extrabold text-rose-400">${rejectQty.toLocaleString()} pcs</span>
                                    </div>
                                    <div class="flex justify-between items-center py-1 border-b ${isLight ? 'border-slate-200/60' : 'border-slate-800/40'}">
                                        <span class="text-slate-400">Scrap / Afkir:</span>
                                        <span class="font-mono font-bold text-amber-400">${scrapQty.toLocaleString()} pcs</span>
                                    </div>
                                    <div class="flex justify-between items-center py-1">
                                        <span class="text-slate-400">Reject Rate:</span>
                                        <span class="font-mono font-bold ${Number(rejectRate) > 3 ? 'text-rose-400' : 'text-emerald-400'}">${rejectRate}%</span>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- ANALISIS WAKTU & LOSS BREAKDOWN -->
                        <div class="${isLight ? 'bg-slate-50/80 border-slate-200' : 'bg-slate-950/60 border-slate-800'} border rounded-xl p-4 space-y-3">
                            <div class="font-bold text-[11px] ${isLight ? 'text-amber-800' : 'text-amber-400'} uppercase tracking-wider flex items-center gap-1.5 border-b ${isLight ? 'border-slate-200' : 'border-slate-800/80'} pb-2">
                                <i data-lucide="clock" class="w-3.5 h-3.5"></i>
                                Struktur Waktu Kerja & Downtime (Time Allocation)
                            </div>
                            <div class="grid grid-cols-2 sm:grid-cols-4 gap-3 text-center font-mono">
                                <div class="p-2.5 rounded-lg ${isLight ? 'bg-white border border-slate-200' : 'bg-slate-900 border border-slate-800'}">
                                    <div class="text-[10px] text-slate-400 font-sans">Planned Time</div>
                                    <div class="text-base font-bold text-slate-200 mt-0.5">${plannedTime} mins</div>
                                </div>
                                <div class="p-2.5 rounded-lg ${isLight ? 'bg-white border border-slate-200' : 'bg-slate-900 border border-slate-800'}">
                                    <div class="text-[10px] text-slate-400 font-sans">Break / Istirahat</div>
                                    <div class="text-base font-bold text-slate-200 mt-0.5">${plannedDt} mins</div>
                                </div>
                                <div class="p-2.5 rounded-lg ${isLight ? 'bg-white border border-slate-200' : 'bg-slate-900 border border-slate-800'}">
                                    <div class="text-[10px] text-slate-400 font-sans">Run Time</div>
                                    <div class="text-base font-bold text-emerald-400 mt-0.5">${runTime} mins</div>
                                </div>
                                <div class="p-2.5 rounded-lg ${isLight ? 'bg-white border border-slate-200' : 'bg-slate-900 border border-slate-800'}">
                                    <div class="text-[10px] text-slate-400 font-sans">Unplanned Downtime</div>
                                    <div class="text-base font-bold ${unplannedDt > 0 ? 'text-rose-400' : 'text-slate-400'} mt-0.5">${unplannedDt} mins</div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- MODAL FOOTER -->
                    <div class="px-6 py-4 border-t ${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950 border-slate-800'} flex items-center justify-between gap-3">
                        <button type="button" id="btn-print-slip-from-view" class="px-3.5 py-2 bg-indigo-600/20 hover:bg-indigo-600/30 text-indigo-400 border border-indigo-500/40 rounded-xl text-xs font-bold flex items-center gap-1.5 cursor-pointer transition-all">
                            <i data-lucide="printer" class="w-4 h-4"></i>
                            <span>Cetak Slip Laporan</span>
                        </button>
                        <div class="flex items-center gap-2.5">
                            <button type="button" id="btn-close-view-prod-bottom" class="px-4 py-2 ${isLight ? 'bg-slate-200 hover:bg-slate-300 text-slate-700' : 'bg-slate-800 hover:bg-slate-700 text-slate-300'} rounded-xl text-xs font-semibold cursor-pointer transition-colors">
                                Tutup
                            </button>
                            <button type="button" id="btn-edit-from-view-prod" class="px-4 py-2 bg-cyan-600 hover:bg-cyan-500 text-white rounded-xl text-xs font-bold shadow-md shadow-cyan-600/30 flex items-center gap-1.5 cursor-pointer transition-all">
                                <i data-lucide="pencil" class="w-3.5 h-3.5"></i>
                                <span>Edit Record</span>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();
        if (this.currentLang === 'ja') {
            i18n.localizeDom(modalContainer);
        }

        const closeModal = () => modalContainer.innerHTML = '';
        document.getElementById('btn-close-view-prod-modal')?.addEventListener('click', closeModal);
        document.getElementById('btn-close-view-prod-bottom')?.addEventListener('click', closeModal);

        document.getElementById('btn-edit-from-view-prod')?.addEventListener('click', () => {
            closeModal();
            this.showEditProductionRecordModal(id, records);
        });

        document.getElementById('btn-print-slip-from-view')?.addEventListener('click', () => {
            this.printSingleProductionSlip(item);
        });
    }

    // ==========================================
    // 3B-VIEW-DT. MODAL POPUP: DETAIL VIEW PROBLEM & DOWNTIME RECORD
    // ==========================================
    showViewDowntimeModal(id, downtimes) {
        const modalContainer = document.getElementById('modal-container');
        const item = (downtimes || []).find(dt => dt.id == id);
        if (!item) return;

        const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');
        const startTimeVal = item.start_time ? String(item.start_time).slice(11, 16) : '-';
        const endTimeVal = item.end_time ? String(item.end_time).slice(11, 16) : 'ONGOING';
        const troubleDateVal = item.start_time ? String(item.start_time).slice(0, 10) : (this.selectedDailyDate || '-');
        const durationMins = Math.max(0, item.calculated_duration_minutes || item.duration_minutes || 0);

        const lineName = item.production_line ? item.production_line.name : `Line #${item.production_line_id || '-'}`;
        const machineName = item.machine ? `${item.machine.name} (${item.machine.code})` : `Machine #${item.machine_id || '-'}`;
        const productName = item.product ? `${item.product.name} (SKU: ${item.product.sku})` : 'Tidak terikat produk spesifik';
        const shiftName = item.shift ? item.shift.name : `Shift #${item.shift_id || '-'}`;
        const reasonName = item.downtime_reason ? item.downtime_reason.name : (item.downtimeReason?.name || 'Problem Operasional Mesin');
        const problemType = item.problem_type || 'Mesin';
        const statusVal = item.status ? String(item.status).toUpperCase() : 'CLOSED';
        const picVal = item.pic || '-';
        const causeVal = item.cause || '-';
        const actionVal = item.action_taken || '-';

        modalContainer.innerHTML = `
            <div class="fixed inset-0 bg-slate-950/80 backdrop-blur-sm flex items-center justify-center p-3 sm:p-4 z-50 overflow-y-auto font-sans">
                <div class="${isLight ? 'bg-white border-slate-200 text-slate-900 shadow-2xl' : 'bg-slate-900 border-slate-800 text-slate-100 shadow-2xl'} border rounded-2xl max-w-2xl w-full my-auto overflow-hidden">
                    <!-- MODAL HEADER -->
                    <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950 border-slate-800'} px-6 py-4 border-b flex items-center justify-between">
                        <div class="flex items-center gap-3">
                            <div class="w-10 h-10 rounded-xl ${isLight ? 'bg-amber-100 text-amber-700 border-amber-200' : 'bg-amber-950 text-amber-400 border-amber-800'} border flex items-center justify-center shadow-inner">
                                <i data-lucide="alert-triangle" class="w-5 h-5"></i>
                            </div>
                            <div>
                                <div class="flex items-center gap-2">
                                    <h3 class="font-bold text-base ${isLight ? 'text-slate-900' : 'text-slate-100'}">Detail Log Problem #${item.id}</h3>
                                    <span class="px-2 py-0.5 rounded-full text-[10px] font-bold ${
                                        problemType === 'Problem Mesin Mekanik' ? 'bg-rose-950 text-rose-300 border border-rose-800' :
                                        problemType === 'Problem Mesin Elektrik' ? 'bg-amber-950 text-amber-300 border border-amber-800' :
                                        problemType === 'Problem Tool' ? 'bg-orange-950 text-orange-300 border border-orange-800' :
                                        problemType === 'Planning Downtime' ? 'bg-blue-950 text-blue-300 border border-blue-800' :
                                        problemType === 'Inventory' ? 'bg-purple-950 text-purple-300 border border-purple-800' :
                                        'bg-slate-800 text-slate-300 border border-slate-700'
                                    }">${problemType}</span>
                                    <span class="px-2 py-0.5 rounded-full text-[10px] font-bold ${statusVal === 'OPEN' ? 'bg-rose-950 text-rose-300 border border-rose-800 animate-pulse' : 'bg-emerald-950 text-emerald-300 border border-emerald-800'}">${statusVal}</span>
                                </div>
                                <p class="text-xs ${isLight ? 'text-slate-500' : 'text-slate-400'} mt-0.5 font-mono">
                                    📅 ${troubleDateVal} &bull; 🏭 ${lineName} &bull; ⏰ ${shiftName}
                                </p>
                            </div>
                        </div>
                        <button id="btn-close-view-dt-modal" class="p-1.5 rounded-lg ${isLight ? 'text-slate-400 hover:text-slate-800 hover:bg-slate-200' : 'text-slate-400 hover:text-slate-100 hover:bg-slate-800'} cursor-pointer transition-colors">
                            <i data-lucide="x" class="w-5 h-5"></i>
                        </button>
                    </div>

                    <div class="p-6 space-y-4 max-h-[75vh] overflow-y-auto text-xs">
                        <!-- TIME, DURATION & STATUS TILES -->
                        <div class="grid grid-cols-4 gap-2.5 font-mono text-center">
                            <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950/70 border-slate-800'} border rounded-xl p-2.5">
                                <div class="text-[10px] font-bold text-slate-400 font-sans">Waktu Mulai</div>
                                <div class="text-lg font-bold text-cyan-400 mt-1">${startTimeVal}</div>
                            </div>
                            <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950/70 border-slate-800'} border rounded-xl p-2.5">
                                <div class="text-[10px] font-bold text-slate-400 font-sans">Waktu Selesai</div>
                                <div class="text-lg font-bold ${item.end_time ? 'text-cyan-400' : 'text-rose-400 animate-pulse'} mt-1">${endTimeVal}</div>
                            </div>
                            <div class="${isLight ? 'bg-rose-50/50 border-rose-200' : 'bg-slate-950/70 border-rose-900/40'} border rounded-xl p-2.5">
                                <div class="text-[10px] font-bold text-rose-400 font-sans">Losstime</div>
                                <div class="text-lg font-extrabold text-rose-400 mt-1">${durationMins} mins</div>
                            </div>
                            <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950/70 border-slate-800'} border rounded-xl p-2.5">
                                <div class="text-[10px] font-bold text-slate-400 font-sans">Status</div>
                                <div class="text-sm font-bold ${statusVal === 'OPEN' ? 'text-rose-400' : 'text-emerald-400'} mt-1.5">${statusVal}</div>
                            </div>
                        </div>

                        <!-- PARAMETER LOKASI, MESIN & PIC -->
                        <div class="${isLight ? 'bg-slate-50/80 border-slate-200' : 'bg-slate-950/60 border-slate-800'} border rounded-xl p-4 space-y-2">
                            <div class="font-bold text-[11px] ${isLight ? 'text-amber-800' : 'text-amber-400'} uppercase tracking-wider flex items-center gap-1.5 border-b ${isLight ? 'border-slate-200' : 'border-slate-800/80'} pb-2">
                                <i data-lucide="map-pin" class="w-3.5 h-3.5"></i>
                                Konteks Operasional & Lokasi Problem
                            </div>
                            <div class="grid grid-cols-1 sm:grid-cols-2 gap-x-4 gap-y-2 text-xs">
                                <div class="flex justify-between py-1 border-b ${isLight ? 'border-slate-200/60' : 'border-slate-800/40'}">
                                    <span class="text-slate-400">Tanggal:</span>
                                    <span class="font-mono font-bold text-cyan-400">${troubleDateVal}</span>
                                </div>
                                <div class="flex justify-between py-1 border-b ${isLight ? 'border-slate-200/60' : 'border-slate-800/40'}">
                                    <span class="text-slate-400">Line:</span>
                                    <span class="font-bold text-cyan-400">${lineName}</span>
                                </div>
                                <div class="flex justify-between py-1 border-b ${isLight ? 'border-slate-200/60' : 'border-slate-800/40'}">
                                    <span class="text-slate-400">OP Mesin:</span>
                                    <span class="font-mono font-bold ${isLight ? 'text-slate-800' : 'text-slate-200'}">${machineName}</span>
                                </div>
                                <div class="flex justify-between py-1 border-b ${isLight ? 'border-slate-200/60' : 'border-slate-800/40'}">
                                    <span class="text-slate-400">Shift Kerja:</span>
                                    <span class="font-bold ${isLight ? 'text-slate-800' : 'text-slate-200'}">${shiftName}</span>
                                </div>
                                <div class="flex justify-between py-1 border-b ${isLight ? 'border-slate-200/60' : 'border-slate-800/40'}">
                                    <span class="text-slate-400">Team:</span>
                                    <span class="font-bold text-amber-400 font-mono">${item.team || 'Team A'}</span>
                                </div>
                                <div class="flex justify-between py-1 border-b ${isLight ? 'border-slate-200/60' : 'border-slate-800/40'}">
                                    <span class="text-slate-400">PIC Departemen:</span>
                                    <span class="font-bold text-cyan-300">${picVal}</span>
                                </div>
                                <div class="col-span-1 sm:col-span-2 flex justify-between py-1 border-b ${isLight ? 'border-slate-200/60' : 'border-slate-800/40'}">
                                    <span class="text-slate-400">Type Produk / SKU:</span>
                                    <span class="font-bold text-emerald-400">${productName}</span>
                                </div>
                                <div class="col-span-1 sm:col-span-2 flex justify-between py-1">
                                    <span class="text-slate-400">Kategori Problem:</span>
                                    <span class="font-semibold ${isLight ? 'text-slate-900' : 'text-slate-100'}">${problemType}</span>
                                </div>
                            </div>
                        </div>

                        <!-- MASALAH / GEJALA PROBLEM -->
                        <div class="${isLight ? 'bg-slate-50/80 border-slate-200' : 'bg-slate-950/60 border-slate-800'} border rounded-xl p-4 space-y-2">
                            <div class="font-bold text-[11px] ${isLight ? 'text-rose-800' : 'text-rose-400'} uppercase tracking-wider flex items-center gap-1.5">
                                <i data-lucide="help-circle" class="w-3.5 h-3.5"></i>
                                Problem / Deskripsi Masalah
                            </div>
                            <div class="p-3 rounded-lg ${isLight ? 'bg-white text-slate-800 border-slate-200' : 'bg-slate-900 text-slate-200 border-slate-800'} border font-sans text-xs leading-relaxed whitespace-pre-wrap">
                                ${item.description || 'Tidak ada deskripsi rinci yang dicatat.'}
                            </div>
                        </div>

                        <!-- PENYEBAB (AKAR MASALAH) -->
                        <div class="${isLight ? 'bg-slate-50/80 border-slate-200' : 'bg-slate-950/60 border-slate-800'} border rounded-xl p-4 space-y-2">
                            <div class="font-bold text-[11px] ${isLight ? 'text-amber-800' : 'text-amber-400'} uppercase tracking-wider flex items-center gap-1.5">
                                <i data-lucide="crosshair" class="w-3.5 h-3.5"></i>
                                Penyebab / Akar Masalah (Root Cause)
                            </div>
                            <div class="p-3 rounded-lg ${isLight ? 'bg-white text-slate-800 border-slate-200' : 'bg-slate-900 text-slate-200 border-slate-800'} border font-sans text-xs leading-relaxed whitespace-pre-wrap">
                                ${causeVal}
                            </div>
                        </div>

                        <!-- TINDAKAN PERBAIKAN (CAPA) -->
                        <div class="${isLight ? 'bg-emerald-50/40 border-emerald-200' : 'bg-slate-950/60 border-emerald-900/40'} border rounded-xl p-4 space-y-2">
                            <div class="font-bold text-[11px] ${isLight ? 'text-emerald-800' : 'text-emerald-400'} uppercase tracking-wider flex items-center gap-1.5">
                                <i data-lucide="check-circle-2" class="w-3.5 h-3.5"></i>
                                Tindakan Perbaikan & Countermeasure (CAPA)
                            </div>
                            <div class="p-3 rounded-lg ${isLight ? 'bg-white text-slate-800 border-slate-200' : 'bg-slate-900 text-emerald-300 border-slate-800'} border font-sans text-xs leading-relaxed whitespace-pre-wrap">
                                ${actionVal}
                            </div>
                        </div>
                    </div>

                    <!-- MODAL FOOTER -->
                    <div class="px-6 py-4 border-t ${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950 border-slate-800'} flex items-center justify-end gap-2.5">
                        <button type="button" id="btn-close-view-dt-bottom" class="px-4 py-2 ${isLight ? 'bg-slate-200 hover:bg-slate-300 text-slate-700' : 'bg-slate-800 hover:bg-slate-700 text-slate-300'} rounded-xl text-xs font-semibold cursor-pointer transition-colors">
                            Tutup
                        </button>
                        <button type="button" id="btn-edit-from-view-dt" class="px-4 py-2 bg-cyan-600 hover:bg-cyan-500 text-white rounded-xl text-xs font-bold shadow-md shadow-cyan-600/30 flex items-center gap-1.5 cursor-pointer transition-all">
                            <i data-lucide="pencil" class="w-3.5 h-3.5"></i>
                            <span>Edit Log Problem</span>
                        </button>
                    </div>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();
        if (this.currentLang === 'ja') {
            i18n.localizeDom(modalContainer);
        }

        const closeModal = () => modalContainer.innerHTML = '';
        document.getElementById('btn-close-view-dt-modal')?.addEventListener('click', closeModal);
        document.getElementById('btn-close-view-dt-bottom')?.addEventListener('click', closeModal);

        document.getElementById('btn-edit-from-view-dt')?.addEventListener('click', () => {
            closeModal();
            this.showEditDowntimeModal(id, downtimes);
        });
    }

    // ==========================================
    // 3B-PRINT. PRINT SINGLE PRODUCTION SLIP
    // ==========================================
    printSingleProductionSlip(item) {
        if (!item) return;

        const prodDate = item.production_date ? String(item.production_date).slice(0, 10) : (this.selectedDailyDate || '-');
        const lineName = item.production_line ? item.production_line.name : `Line #${item.production_line_id || '-'}`;
        const lineCode = item.production_line ? item.production_line.code : '';
        const machineName = item.machine ? item.machine.name : `Machine #${item.machine_id || '-'}`;
        const machineCode = item.machine ? item.machine.code : '';
        const productName = item.product ? item.product.name : `Product #${item.product_id || '-'}`;
        const productSku = item.product ? item.product.sku : '-';
        const shiftName = item.shift ? item.shift.name : `Shift #${item.shift_id || '-'}`;
        const idealCycle = item.ideal_cycle_time || (item.product?.ideal_cycle_time) || 9.5;

        // OEE metrics
        const oeeScore = Number(item.oee_record?.oee ?? 0);
        const availVal = Number(item.oee_record?.availability ?? 0);
        const perfVal = Number(item.oee_record?.performance ?? 0);
        const qualVal = Number(item.oee_record?.quality ?? 0);

        const oeeStatusLabel = oeeScore >= 85 ? 'EXCELLENT / WORLD CLASS' : (oeeScore >= 70 ? 'GOOD / ON TARGET' : 'CRITICAL / NEEDS ACTION');
        const oeeColor = oeeScore >= 85 ? '#059669' : (oeeScore >= 70 ? '#D97706' : '#DC2626');

        // Quantities
        const targetQty = Number(item.target_quantity || 0);
        const totalQty = Number(item.total_quantity || 0);
        const goodQty = Number(item.good_quantity || 0);
        const rejectQty = Number(item.reject_quantity || 0);
        const scrapQty = Number(item.scrap_quantity || 0);
        const rejectRate = totalQty > 0 ? ((rejectQty / totalQty) * 100).toFixed(2) : '0.00';
        const yieldRate = totalQty > 0 ? ((goodQty / totalQty) * 100).toFixed(2) : '100.00';

        // Times
        const plannedTime = Number(item.planned_production_time || 480);
        const plannedDt = Number(item.planned_downtime || 60);
        const runTime = Number(item.run_time || (plannedTime - plannedDt));
        const unplannedDt = Number(item.downtime || 0);

        const isJa = this.currentLang === 'ja';
        const datePart = prodDate || new Date().toISOString().slice(0, 10);
        const linePart = lineCode || lineName || '';
        const shiftPart = shiftName ? ` - ${shiftName}` : '';
        const lineStr = linePart ? ` - ${linePart}` : '';
        let slipFilename = this.sanitizeReportFilename(`Slip Laporan Harian Tgl ${datePart}${lineStr}${shiftPart}`);
        if (isJa) slipFilename = i18n.translateText(slipFilename);

        let printHtml = `<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>${slipFilename}</title>
    <style>
        @page {
            size: A4 portrait;
            margin: 8mm 10mm 8mm 10mm;
        }
        * {
            box-sizing: border-box;
            -webkit-print-color-adjust: exact !important;
            print-color-adjust: exact !important;
        }
        html, body {
            margin: 0;
            padding: 0;
            font-family: Arial, Helvetica, sans-serif;
            font-size: 8.5px;
            color: #0f172a;
            background: #ffffff;
            line-height: 1.3;
        }
        .sec-title {
            font-size: 8.5px;
            font-weight: bold;
            text-transform: uppercase;
            background-color: #E2E8F0;
            padding: 2.5px 5px;
            border-left: 3.5px solid #0284C7;
            margin: 6px 0 2.5px 0;
            color: #0f172a;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            font-size: 8px;
        }
        .meta-table td {
            border: 1px solid #cbd5e1;
            padding: 3px 5px;
            vertical-align: middle;
        }
        .data-table th, .data-table td {
            border: 1px solid #475569;
            padding: 3.5px 5px;
            vertical-align: middle;
        }
        .data-table th {
            background-color: #F1F5F9;
            font-weight: bold;
            text-transform: uppercase;
            font-size: 7.5px;
            color: #0f172a;
            text-align: left;
        }
        .signatures {
            width: 100%;
            border-collapse: collapse;
            margin-top: 12px;
            page-break-inside: avoid;
        }
        .signatures td {
            border: none;
            text-align: center;
            padding: 3px;
            vertical-align: top;
        }
        .sig-line {
            border-bottom: 1px solid #000000;
            width: 75%;
            margin: 30px auto 3px auto;
            font-weight: bold;
            font-size: 8px;
        }
    </style>
</head>
<body>
    <!-- KOP SURAT RESMI PT YASUNAGA INDONESIA -->
    <div style="border-bottom: 2.5px double #000000; padding-bottom: 4px; margin-bottom: 6px;">
        <table style="width: 100%; border: none; margin-bottom: 0;">
            <tr>
                <td style="width: 60px; border: none; padding: 0 8px 0 0; vertical-align: middle;">
                    <img src="${this.escapeHtml(this.getCompanyLogo())}" style="max-height: 44px; max-width: 60px; object-fit: contain;" alt="Logo" onerror="this.onerror=null; this.src='/images/yasunaga-logo.png';" />
                </td>
                <td style="border: none; padding: 0; vertical-align: middle;">
                    <div style="font-size: 12px; font-weight: 900; text-transform: uppercase; letter-spacing: 0.5px; line-height: 1.15; color: #000000;">
                        ${this.companyProfile?.company_name || 'PT. YASUNAGA INDONESIA'}
                    </div>
                    <div style="font-size: 8px; font-weight: bold; color: #1e293b; margin-top: 1px;">
                        ${this.companyProfile?.plant_name || 'ENGINE PARTS & AIR PUMP MFG'} • <strong>${this.companyProfile?.plant_code || 'PLT-01'}</strong>
                    </div>
                    <div style="font-size: 7px; color: #475569; margin-top: 1px;">
                        ${this.companyProfile?.address || 'Jl. Modern Industri Raya Kav. 24 Kawasan Industri Modern Cikande, Serang Banten'}
                    </div>
                    <div style="font-size: 7px; color: #64748b;">
                        ${this.companyProfile?.phone ? 'Telp: ' + this.companyProfile.phone : ''} ${this.companyProfile?.email ? '• Email: ' + this.companyProfile.email : ''}
                    </div>
                </td>
                <td style="width: 230px; border: none; padding: 0; text-align: right; vertical-align: top;">
                    <div style="font-size: 10.5px; font-weight: 900; text-transform: uppercase; line-height: 1.2; color: #0284C7;">
                        SLIP LAPORAN HASIL PRODUKSI
                    </div>
                    <div style="font-size: 7.5px; font-weight: bold; color: ${oeeColor}; margin-top: 1px;">
                        ★ ${oeeStatusLabel}
                    </div>
                    <div style="font-size: 7.5px; color: #333333; font-family: monospace; margin-top: 1px;">
                        Doc No: PRD-SLIP-${prodDate.replace(/-/g, '')}-${String(item.id).padStart(4, '0')}
                    </div>
                    <div style="font-size: 7px; color: #64748b; font-family: monospace;">
                        Standard Form: A4-Portrait Production Slip
                    </div>
                </td>
            </tr>
        </table>
    </div>

    <!-- METADATA INFORMATION TABLE -->
    <table class="meta-table" style="margin-bottom: 6px;">
        <tr>
            <td style="width: 16%; font-weight: bold; background-color: #f1f5f9;">Tanggal Produksi:</td>
            <td style="width: 34%;"><strong>${prodDate}</strong></td>
            <td style="width: 16%; font-weight: bold; background-color: #f1f5f9;">Production Line:</td>
            <td style="width: 34%;"><strong>${lineName} ${lineCode ? `(${lineCode})` : ''}</strong></td>
        </tr>
        <tr>
            <td style="font-weight: bold; background-color: #f1f5f9;">Shift Kerja:</td>
            <td><strong>${shiftName}</strong></td>
            <td style="font-weight: bold; background-color: #f1f5f9;">Mesin Operasional:</td>
            <td><strong>${machineName} ${machineCode ? `[${machineCode}]` : ''}</strong></td>
        </tr>
        <tr>
            <td style="font-weight: bold; background-color: #f1f5f9;">Nama Part / SKU:</td>
            <td><strong>${productName}</strong> <span style="color: #64748b; font-family: monospace;">(${productSku})</span></td>
            <td style="font-weight: bold; background-color: #f1f5f9;">Waktu Cetak:</td>
            <td>${new Date().toLocaleString('id-ID', { dateStyle: 'medium', timeStyle: 'short' })} WIB</td>
        </tr>
    </table>

    <!-- 4 MACRO KPI TILES -->
    <div style="display: flex; gap: 6px; margin-bottom: 6px;">
        <div style="flex: 1; border: 1.5px solid ${oeeColor}; border-radius: 5px; padding: 4px 2px; text-align: center; background-color: #f8fafc;">
            <div style="font-size: 7.5px; font-weight: 800; text-transform: uppercase; color: #64748b;">OVERALL OEE</div>
            <div style="font-size: 16px; font-weight: 900; font-family: monospace; color: ${oeeColor}; margin-top: 1px;">
                ${oeeScore.toFixed(1)}%
            </div>
            <div style="font-size: 7px; color: #64748b;">Target: 85.0%</div>
        </div>
        <div style="flex: 1; border: 1px solid #0284c7; border-radius: 5px; padding: 4px 2px; text-align: center; background-color: #f0f9ff;">
            <div style="font-size: 7.5px; font-weight: 800; text-transform: uppercase; color: #0369a1;">AVAILABILITY (A)</div>
            <div style="font-size: 16px; font-weight: 900; font-family: monospace; color: #0284c7; margin-top: 1px;">
                ${availVal.toFixed(1)}%
            </div>
            <div style="font-size: 7px; color: #64748b;">Target: 90.0%</div>
        </div>
        <div style="flex: 1; border: 1px solid #16a34a; border-radius: 5px; padding: 4px 2px; text-align: center; background-color: #f0fdf4;">
            <div style="font-size: 7.5px; font-weight: 800; text-transform: uppercase; color: #15803d;">PERFORMANCE (P)</div>
            <div style="font-size: 16px; font-weight: 900; font-family: monospace; color: #16a34a; margin-top: 1px;">
                ${perfVal.toFixed(1)}%
            </div>
            <div style="font-size: 7px; color: #64748b;">Target: 95.0%</div>
        </div>
        <div style="flex: 1; border: 1px solid #ea580c; border-radius: 5px; padding: 4px 2px; text-align: center; background-color: #fff7ed;">
            <div style="font-size: 7.5px; font-weight: 800; text-transform: uppercase; color: #c2410c;">QUALITY RATE (Q)</div>
            <div style="font-size: 16px; font-weight: 900; font-family: monospace; color: #ea580c; margin-top: 1px;">
                ${qualVal.toFixed(1)}%
            </div>
            <div style="font-size: 7px; color: #64748b;">Target: 99.0%</div>
        </div>
    </div>

    <!-- SECTION I: HASIL OUTPUT PRODUKSI & ANALISIS MUTU -->
    <div class="sec-title">I. HASIL PENCAPAIAN OUTPUT PRODUKSI & ANALISIS MUTU</div>
    <table class="data-table" style="margin-bottom: 6px;">
        <thead>
            <tr>
                <th style="width: 32%;">Parameter Mutu & Output</th>
                <th style="width: 24%; text-align: right;">Jumlah (Pcs)</th>
                <th style="width: 24%;">Rasio / Standar</th>
                <th style="width: 20%; text-align: center;">Status Verifikasi</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td><strong>Target Produksi (Planned Qty)</strong></td>
                <td style="text-align: right; font-weight: bold; font-family: monospace;">${targetQty.toLocaleString()} pcs</td>
                <td>Standard Rencana Shift</td>
                <td style="text-align: center; font-weight: bold; color: #0369a1;">PLANNED</td>
            </tr>
            <tr>
                <td><strong>Total Measuring (Counter Mesin)</strong></td>
                <td style="text-align: right; font-weight: bold; font-family: monospace;">${totalQty.toLocaleString()} pcs</td>
                <td>Output Kotor Counter</td>
                <td style="text-align: center; font-weight: bold; color: ${totalQty >= targetQty ? '#059669' : '#d97706'};">
                    ${totalQty >= targetQty ? 'TERPENUHI' : 'DI BAWAH TARGET'}
                </td>
            </tr>
            <tr style="background-color: #f0fdf4;">
                <td style="color: #166534; font-weight: bold;">Finish Good (Good Output OK)</td>
                <td style="text-align: right; font-weight: 900; color: #166534; font-family: monospace;">${goodQty.toLocaleString()} pcs</td>
                <td style="color: #166534; font-weight: bold;">Quality Yield: ${yieldRate}%</td>
                <td style="text-align: center; font-weight: bold; color: #166534;">PASSED (OK)</td>
            </tr>
            <tr style="${rejectQty > 0 ? 'background-color: #fff1f2;' : ''}">
                <td style="${rejectQty > 0 ? 'color: #9f1239; font-weight: bold;' : ''}">Not Good (NG / Reject)</td>
                <td style="text-align: right; font-weight: 900; ${rejectQty > 0 ? 'color: #9f1239;' : ''} font-family: monospace;">${rejectQty.toLocaleString()} pcs</td>
                <td style="${rejectQty > 0 ? 'color: #9f1239; font-weight: bold;' : ''}">Defect Rate: ${rejectRate}%</td>
                <td style="text-align: center; font-weight: bold; color: ${rejectQty > 0 ? '#be123c' : '#059669'};">
                    ${rejectQty > 0 ? 'DEFECT DETECTED' : 'ZERO DEFECT'}
                </td>
            </tr>
            <tr>
                <td>Scrap / Afkir Material</td>
                <td style="text-align: right; font-weight: bold; color: #b45309; font-family: monospace;">${scrapQty.toLocaleString()} pcs</td>
                <td>Kerugian Material</td>
                <td style="text-align: center; color: #64748b;">${scrapQty > 0 ? 'DISCARDED' : '-'}</td>
            </tr>
            <tr>
                <td>Ideal Cycle Time Desain</td>
                <td style="text-align: right; font-weight: bold; font-family: monospace;">${idealCycle} detik / pc</td>
                <td>Standar Kecepatan Mesin</td>
                <td style="text-align: center; color: #0284c7; font-weight: bold;">STANDARD SPEED</td>
            </tr>
        </tbody>
    </table>

    <!-- SECTION II: STRUKTUR WAKTU KERJA & DOWNTIME -->
    <div class="sec-title">II. STRUKTUR WAKTU KERJA & LOSS AVAILABILITY</div>
    <table class="data-table" style="margin-bottom: 6px;">
        <thead>
            <tr>
                <th style="width: 35%;">Kategori Alokasi Waktu</th>
                <th style="width: 25%; text-align: right;">Durasi (Menit)</th>
                <th style="width: 20%; text-align: right;">Durasi (Jam)</th>
                <th style="width: 20%; text-align: center;">Keterangan</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td><strong>Planned Production Time (Shift Penuh)</strong></td>
                <td style="text-align: right; font-weight: bold; font-family: monospace;">${plannedTime} mins</td>
                <td style="text-align: right; font-family: monospace;">${(plannedTime / 60).toFixed(1)} hrs</td>
                <td style="text-align: center;">Total Jadwal Shift</td>
            </tr>
            <tr>
                <td>Break / Istirahat Terencana</td>
                <td style="text-align: right; font-weight: bold; color: #64748b; font-family: monospace;">${plannedDt} mins</td>
                <td style="text-align: right; font-family: monospace;">${(plannedDt / 60).toFixed(1)} hrs</td>
                <td style="text-align: center;">Planned Break</td>
            </tr>
            <tr style="background-color: #f0fdf4;">
                <td style="color: #166534; font-weight: bold;">Operating Run Time (Mesin Berjalan)</td>
                <td style="text-align: right; font-weight: 900; color: #166534; font-family: monospace;">${runTime} mins</td>
                <td style="text-align: right; font-weight: bold; color: #166534; font-family: monospace;">${(runTime / 60).toFixed(1)} hrs</td>
                <td style="text-align: center; font-weight: bold; color: #166534;">PRODUKTIF</td>
            </tr>
            <tr style="${unplannedDt > 0 ? 'background-color: #fff1f2;' : ''}">
                <td style="${unplannedDt > 0 ? 'color: #9f1239; font-weight: bold;' : ''}">Unplanned Downtime (Waktu Henti Kendala)</td>
                <td style="text-align: right; font-weight: 900; ${unplannedDt > 0 ? 'color: #be123c;' : ''} font-family: monospace;">${unplannedDt} mins</td>
                <td style="text-align: right; font-family: monospace;">${(unplannedDt / 60).toFixed(2)} hrs</td>
                <td style="text-align: center; font-weight: bold; color: ${unplannedDt > 0 ? '#be123c' : '#059669'};">
                    ${unplannedDt > 0 ? 'DOWNTIME LOSS' : 'ZERO BREAKDOWN'}
                </td>
            </tr>
        </tbody>
    </table>

    ${unplannedDt > 0 ? `
        <div style="border: 1px solid #fecdd3; background-color: #fff1f2; border-radius: 5px; padding: 4px 6px; margin-bottom: 5px; font-size: 7.5px; color: #881337;">
            <strong>⚠️ Catatan Kendala Operasional:</strong> Terdapat kehilangan ketersediaan mesin (*Unplanned Downtime*) sebesar <strong>${unplannedDt} menit</strong>. Pastikan tindakan korektif (CAPA) telah diverifikasi pada log trouble mesin.
        </div>
    ` : ''}

    <!-- LEMBAR PENGESAHAN TANDA TANGAN (3 KOLOM RESMI) -->
    <div style="margin-top: 10px; page-break-inside: avoid;">
        <table class="signatures">
            <tr>
                <td style="width: 33.3%;">
                    <div style="font-size: 7.5px; color: #475569;">Dibuat Oleh (Operator / Leader):</div>
                    <div class="sig-line">( ${item.operator_name || 'Leader Shift'} )</div>
                    <div style="font-size: 7px; color: #64748b;">Operator / Shift Leader</div>
                </td>
                <td style="width: 33.3%;">
                    <div style="font-size: 7.5px; color: #475569;">Diperiksa Oleh (QC Inspector):</div>
                    <div class="sig-line">( ${item.inspector_name || 'QC Inspector'} )</div>
                    <div style="font-size: 7px; color: #64748b;">Quality Assurance & Control</div>
                </td>
                <td style="width: 33.3%;">
                    <div style="font-size: 7.5px; color: #475569;">Mengetahui / Disetujui:</div>
                    <div class="sig-line">( ${this.companyProfile?.supervisor || 'Supervisor Produksi'} )</div>
                    <div style="font-size: 7px; color: #64748b;">Supervisor / Dept Head</div>
                </td>
            </tr>
        </table>
    </div>
</body>
</html>`;

        if (isJa) {
            printHtml = i18n.translateHtml(printHtml);
        }

        // Trigger native print preview via hidden iframe (A4 portrait fit to margin, bypasses popup blocker)
        let frame = document.getElementById('print-iframe-target');
        if (!frame) {
            frame = document.createElement('iframe');
            frame.id = 'print-iframe-target';
            frame.style.position = 'fixed';
            frame.style.right = '0';
            frame.style.bottom = '0';
            frame.style.width = '0';
            frame.style.height = '0';
            frame.style.border = '0';
            frame.style.visibility = 'hidden';
            document.body.appendChild(frame);
        }

        const frameDoc = frame.contentWindow.document;
        frameDoc.open();
        frameDoc.write(printHtml);
        frameDoc.close();

        if (isJa) {
            i18n.localizeDom(frameDoc.body);
        }

        const originalDocTitle = document.title;
        this.triggerFramePrint(frame, frameDoc, originalDocTitle, slipFilename);
    }

    showEditProductionRecordModal(id, records) {
        const modalContainer = document.getElementById('modal-container');
        const item = (records || []).find(r => r.id == id);
        if (!item) return;

        const prodDate = item.production_date ? String(item.production_date).slice(0, 10) : (this.selectedDailyDate || new Date().toISOString().slice(0, 10));
        const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');

        const linesOptions = (this.masterData.lines || []).map(l => 
            `<option value="${l.id}" ${item.production_line_id == l.id ? 'selected' : ''}>${l.code} - ${l.name}</option>`
        ).join('');

        const shiftsOptions = (this.masterData.shifts || []).map(s => 
            `<option value="${s.id}" ${item.shift_id == s.id ? 'selected' : ''}>${s.name}</option>`
        ).join('');

        const machinesOptions = (this.masterData.machines || []).map(m => 
            `<option value="${m.id}" ${item.machine_id == m.id ? 'selected' : ''}>${m.code} - ${m.name}</option>`
        ).join('');

        modalContainer.innerHTML = `
            <div class="fixed inset-0 bg-slate-950/80 backdrop-blur-sm flex items-center justify-center p-3 sm:p-4 z-50 overflow-y-auto">
                <div class="${isLight ? 'bg-white border-slate-200 text-slate-900 shadow-2xl' : 'bg-slate-900 border-slate-800 text-slate-100 shadow-2xl'} border rounded-2xl max-w-2xl w-full my-auto overflow-hidden font-sans">
                    <!-- MODAL HEADER -->
                    <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950 border-slate-800'} px-5 py-4 border-b flex items-center justify-between">
                        <div class="flex items-center gap-2.5">
                            <div class="w-8 h-8 rounded-xl ${isLight ? 'bg-cyan-100 text-cyan-700 border-cyan-200' : 'bg-cyan-950 text-cyan-400 border-cyan-800'} border flex items-center justify-center">
                                <i data-lucide="pencil" class="w-4 h-4"></i>
                            </div>
                            <div>
                                <h3 class="font-bold text-sm ${isLight ? 'text-slate-900' : 'text-slate-100'}">Edit Record Produksi #${item.id}</h3>
                                <p class="text-[11px] ${isLight ? 'text-slate-500' : 'text-slate-400'}">Ubah rincian parameter, lini, shift, produk, atau kuantitas output</p>
                            </div>
                        </div>
                        <button type="button" id="btn-close-edit-prod-modal" class="p-1 rounded-lg ${isLight ? 'text-slate-400 hover:text-slate-800 hover:bg-slate-200' : 'text-slate-400 hover:text-slate-100 hover:bg-slate-800'} cursor-pointer transition-colors">
                            <i data-lucide="x" class="w-5 h-5"></i>
                        </button>
                    </div>

                    <form id="form-edit-prod-record" class="p-5 space-y-4 text-xs">
                        <input type="hidden" name="ideal_cycle_time" value="${item.ideal_cycle_time || 9.5}" />
                        <input type="hidden" name="downtime" value="${item.downtime || 0}" />

                        <!-- SEKSI 1: PARAMETER OPERASIONAL DASAR -->
                        <div class="${isLight ? 'bg-slate-50/80 border-slate-200' : 'bg-slate-950/60 border-slate-800'} border rounded-xl p-3.5 space-y-3">
                            <div class="font-bold text-[11px] ${isLight ? 'text-cyan-800' : 'text-cyan-400'} uppercase tracking-wider flex items-center gap-1.5 border-b ${isLight ? 'border-slate-200' : 'border-slate-800/80'} pb-1.5">
                                <i data-lucide="settings-2" class="w-3.5 h-3.5"></i>
                                Parameter Lini, Shift & Produk
                            </div>

                            <div class="grid grid-cols-1 sm:grid-cols-3 gap-3">
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-400'} mb-1 font-medium">📅 Tanggal Produksi</label>
                                    <input type="date" name="production_date" value="${prodDate}" required class="w-full ${isLight ? 'bg-white border-slate-200 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-sans font-medium" />
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-400'} mb-1 font-medium">🏭 Production Line</label>
                                    <select name="production_line_id" required class="w-full ${isLight ? 'bg-white border-slate-200 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-sans font-bold text-cyan-600 dark:text-cyan-400">
                                        ${linesOptions}
                                    </select>
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-400'} mb-1 font-medium">⏰ Shift Kerja</label>
                                    <select name="shift_id" required class="w-full ${isLight ? 'bg-white border-slate-200 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-sans font-semibold">
                                        ${shiftsOptions}
                                    </select>
                                </div>
                            </div>

                            <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-400'} mb-1 font-medium">⚙️ Mesin</label>
                                    <select name="machine_id" required class="w-full ${isLight ? 'bg-white border-slate-200 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-sans font-mono">
                                        ${machinesOptions}
                                    </select>
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-400'} mb-1 font-medium">📦 Nama Produk</label>
                                    <select name="product_id" id="edit-prod-select-product" required class="w-full ${isLight ? 'bg-white border-slate-200 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-sans font-semibold">
                                        <!-- Will be populated dynamically based on line -->
                                    </select>
                                </div>
                            </div>
                        </div>

                        <!-- SEKSI 2: KUANTITAS PRODUKSI -->
                        <div class="${isLight ? 'bg-slate-50/80 border-slate-200' : 'bg-slate-950/60 border-slate-800'} border rounded-xl p-3.5 space-y-3">
                            <div class="flex items-center justify-between border-b ${isLight ? 'border-slate-200' : 'border-slate-800/80'} pb-1.5">
                                <div class="font-bold text-[11px] ${isLight ? 'text-emerald-800' : 'text-emerald-400'} uppercase tracking-wider flex items-center gap-1.5">
                                    <i data-lucide="bar-chart" class="w-3.5 h-3.5"></i>
                                    Kuantitas Output Produksi (Pcs)
                                </div>
                                <span class="text-[10px] text-emerald-500 font-mono">Target = Planned Time × 60 ÷ Cycle Time</span>
                            </div>

                            <div class="grid grid-cols-2 sm:grid-cols-5 gap-2.5">
                                <div>
                                    <label class="block ${isLight ? 'text-slate-600' : 'text-slate-400'} mb-1 font-medium">Target Qty</label>
                                    <input type="number" name="target_quantity" value="${item.target_quantity || 0}" min="0" required class="w-full ${isLight ? 'bg-white border-slate-200 text-cyan-700' : 'bg-slate-900 border-slate-800 text-cyan-400'} border rounded-lg px-2 py-2 font-mono font-bold" />
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-600' : 'text-slate-400'} mb-1 font-medium">Measuring</label>
                                    <input type="number" name="total_quantity" value="${item.total_quantity || 0}" min="0" required class="w-full ${isLight ? 'bg-white border-slate-200 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-lg px-2 py-2 font-mono font-bold" />
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-600' : 'text-slate-400'} mb-1 font-medium">Finish Good</label>
                                    <input type="number" name="good_quantity" value="${item.good_quantity || 0}" min="0" required class="w-full ${isLight ? 'bg-white border-slate-200 text-emerald-700' : 'bg-slate-900 border-slate-800 text-emerald-400'} border rounded-lg px-2 py-2 font-mono font-bold" />
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-600' : 'text-slate-400'} mb-1 font-medium">NG Qty</label>
                                    <input type="number" name="reject_quantity" value="${item.reject_quantity || 0}" min="0" required class="w-full ${isLight ? 'bg-white border-slate-200 text-rose-700' : 'bg-slate-900 border-slate-800 text-rose-400'} border rounded-lg px-2 py-2 font-mono font-bold" />
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-600' : 'text-slate-400'} mb-1 font-medium">Scrap / Afkir</label>
                                    <input type="number" name="scrap_quantity" value="${item.scrap_quantity || 0}" min="0" class="w-full ${isLight ? 'bg-white border-slate-200 text-amber-700' : 'bg-slate-900 border-slate-800 text-amber-400'} border rounded-lg px-2 py-2 font-mono font-bold" />
                                </div>
                            </div>

                            <div class="grid grid-cols-3 gap-3 pt-1">
                                <div>
                                    <label class="block ${isLight ? 'text-slate-600' : 'text-slate-400'} mb-1 font-medium">Planned Time (min)</label>
                                    <input type="number" name="planned_production_time" value="${item.planned_production_time || 480}" required class="w-full ${isLight ? 'bg-white border-slate-200 text-slate-800' : 'bg-slate-900 border-slate-800 text-slate-200'} border rounded-lg px-2.5 py-1.5 font-mono" />
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-600' : 'text-slate-400'} mb-1 font-medium">Break / Istirahat (min)</label>
                                    <input type="number" name="planned_downtime" value="${item.planned_downtime || 60}" required class="w-full ${isLight ? 'bg-white border-slate-200 text-slate-800' : 'bg-slate-900 border-slate-800 text-slate-200'} border rounded-lg px-2.5 py-1.5 font-mono" />
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-600' : 'text-slate-400'} mb-1 font-medium">Run Time (min)</label>
                                    <input type="number" name="run_time" value="${item.run_time || 420}" required class="w-full ${isLight ? 'bg-white border-slate-200 text-emerald-700' : 'bg-slate-900 border-slate-800 text-emerald-400'} border rounded-lg px-2.5 py-1.5 font-mono font-bold" />
                                </div>
                            </div>
                        </div>

                        <!-- MODAL FOOTER -->
                        <div class="pt-3 flex justify-end gap-2 border-t ${isLight ? 'border-slate-200' : 'border-slate-800'}">
                            <button type="button" id="btn-cancel-edit-prod-modal" class="px-4 py-2 ${isLight ? 'bg-slate-100 hover:bg-slate-200 text-slate-700' : 'bg-slate-800 hover:bg-slate-700 text-slate-300'} rounded-xl text-xs font-medium cursor-pointer transition-colors">Batal</button>
                            <button type="submit" class="px-5 py-2 bg-cyan-600 hover:bg-cyan-500 text-white text-xs font-bold rounded-xl shadow-md shadow-cyan-600/30 flex items-center gap-1.5 cursor-pointer transition-all">
                                <i data-lucide="save" class="w-3.5 h-3.5"></i>
                                Simpan Perubahan & Hitung Ulang OEE
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();
        if (this.currentLang === 'ja') {
            i18n.localizeDom(modalContainer);
        }

        // Dynamic Product Selector in Edit Modal
        const editLineSelect = modalContainer.querySelector('select[name="production_line_id"]');
        const editProductSelect = document.getElementById('edit-prod-select-product');
        const editPlannedTime = modalContainer.querySelector('input[name="planned_production_time"]');
        const editPlannedDt = modalContainer.querySelector('input[name="planned_downtime"]');
        const editRunTime = modalContainer.querySelector('input[name="run_time"]');
        const editTargetQty = modalContainer.querySelector('input[name="target_quantity"]');
        const editIdealCycle = modalContainer.querySelector('input[name="ideal_cycle_time"]');
        const editShiftSelect = modalContainer.querySelector('select[name="shift_id"]');

        const editMachineSelect = modalContainer.querySelector('select[name="machine_id"]');

        const populateMachines = (preferredMachineId = null) => {
            if (!editLineSelect || !editMachineSelect) return;
            const lineId = editLineSelect.value;
            const lineMachines = this.getMachinesForLine(lineId);

            editMachineSelect.innerHTML = lineMachines.map(m => 
                `<option value="${m.id}">${m.name} (${m.code})</option>`
            ).join('');

            if (preferredMachineId && lineMachines.some(m => m.id == preferredMachineId)) {
                editMachineSelect.value = preferredMachineId;
            } else {
                const measuringMC = lineMachines.find(m => 
                    (m.name && m.name.toLowerCase().includes('measuring')) || 
                    (m.code && m.code.toLowerCase().includes('measuring'))
                );
                if (measuringMC) {
                    editMachineSelect.value = measuringMC.id;
                } else if (lineMachines.length > 0) {
                    editMachineSelect.value = lineMachines[0].id;
                }
            }
        };

        const populateProducts = () => {
            if (!editLineSelect || !editProductSelect) return;
            const lineId = editLineSelect.value;
            const prods = this.getProductsForLine(lineId);

            if (prods.length === 0) {
                editProductSelect.innerHTML = '<option value="" disabled selected>-- Tidak ada produk untuk lini ini --</option>';
            } else {
                editProductSelect.innerHTML = prods.map(p => `
                    <option value="${p.id}" ${item.product_id == p.id ? 'selected' : ''}>${p.name} — SKU: ${p.sku} (${p.ideal_cycle_time}s)</option>
                `).join('');
            }

            updateTargetCalc();
        };

        const updateTargetCalc = () => {
            const prodId = editProductSelect ? editProductSelect.value : item.product_id;
            const prod = (this.masterData.products || []).find(p => p.id == prodId);
            const cycleTime = prod && prod.ideal_cycle_time ? parseFloat(prod.ideal_cycle_time) : 9.5;
            if (editIdealCycle) editIdealCycle.value = cycleTime;

            const plannedMins = editPlannedTime ? parseFloat(editPlannedTime.value || 0) : 480;
            if (editTargetQty && cycleTime > 0 && plannedMins > 0) {
                editTargetQty.value = Math.floor((plannedMins * 60) / cycleTime);
            }
        };

        if (editLineSelect) {
            editLineSelect.addEventListener('change', () => {
                populateProducts();
                populateMachines();
            });
            populateProducts();
            populateMachines(item.machine_id);
        }
        if (editProductSelect) {
            editProductSelect.addEventListener('change', updateTargetCalc);
        }
        if (editPlannedTime) {
            editPlannedTime.addEventListener('input', updateTargetCalc);
        }

        // Shift change recalculates planned time
        if (editShiftSelect) {
            editShiftSelect.addEventListener('change', () => {
                const sObj = (this.masterData.shifts || []).find(s => s.id == editShiftSelect.value);
                if (sObj) {
                    const startParts = (sObj.start_time || '07:30').split(':');
                    const endParts = (sObj.end_time || '16:30').split(':');
                    let sMins = parseInt(startParts[0]) * 60 + parseInt(startParts[1] || 0);
                    let eMins = parseInt(endParts[0]) * 60 + parseInt(endParts[1] || 0);
                    if (eMins <= sMins) eMins += 24 * 60;
                    const breakMins = parseInt(sObj.break_duration_minutes || 60);
                    const plannedMins = Math.max(0, (eMins - sMins) - breakMins);

                    if (editPlannedTime) editPlannedTime.value = plannedMins;
                    if (editPlannedDt) editPlannedDt.value = breakMins;
                    if (editRunTime) editRunTime.value = Math.max(0, plannedMins - (parseFloat(item.downtime || 0)));
                    updateTargetCalc();
                }
            });
        }

        document.getElementById('btn-close-edit-prod-modal').addEventListener('click', () => modalContainer.innerHTML = '');
        document.getElementById('btn-cancel-edit-prod-modal').addEventListener('click', () => modalContainer.innerHTML = '');

        const formEdit = document.getElementById('form-edit-prod-record');
        formEdit.addEventListener('submit', async (e) => {
            e.preventDefault();
            const formData = new FormData(e.target);
            const data = Object.fromEntries(formData.entries());

            if (!this.validateProductionForm(data, formEdit)) return;

            try {
                await api.updateProductionRecord(id, data);
                this.showNotification('Record Produksi Diperbarui', `Record #${id} berhasil diperbarui & OEE dihitung ulang!`, 'update');
                modalContainer.innerHTML = '';
                await this.renderDailyReport();
            } catch (err) {
                const errorMsg = err.response?.data?.message || err.message || 'Gagal memperbarui record produksi';
                const errorDetail = err.response?.data?.error_detail || '';
                if (errorMsg.includes('tidak boleh sama') || err.response?.status === 422) {
                    this.showAlertModal({
                        title: 'Data sudah ditambahkan tidak boleh sama !',
                        subtitle: errorDetail || 'Kombinasi Line dan Shift pada tanggal tersebut sudah pernah ditambahkan ke sistem.',
                        buttonText: 'Ubah Line / Shift'
                    });
                } else {
                    alert('Gagal memperbarui record produksi: ' + errorMsg);
                }
            }
        });
    }

    showEditDowntimeModal(id, downtimes) {
        const modalContainer = document.getElementById('modal-container');
        const item = (downtimes || []).find(dt => dt.id == id);
        if (!item) return;

        const startTimeVal = item.start_time ? String(item.start_time).slice(11, 16) : '08:00';
        const endTimeVal = item.end_time ? String(item.end_time).slice(11, 16) : '';
        const durVal = Math.max(0, item.calculated_duration_minutes || item.duration_minutes || 0);
        const troubleDateVal = item.start_time ? String(item.start_time).slice(0, 10) : (this.selectedDailyDate || new Date().toISOString().slice(0, 10));

        const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');

        const picOptions = [
            'Leader Team A (Alim Utama)',
            'Leader Team B (Dadi)',
            'Leader Team C (Karyanto)',
            'Leader Team D (Kurniawan)',
            'Maintenance Mekanik',
            'Maintenance Elektrik',
            'Tooling & Dies Team',
            'Quality Control (QC Inspector)',
            'Operator Mesin (OP)',
            'Supervisor Produksi',
            ...(this.masterData.groups || []).map(g => `${g.leader_name} (${g.team})`),
        ];
        const uniquePicOptions = [...new Set(picOptions)];

        const linesOptions = (this.masterData.lines || []).map(l => 
            `<option value="${l.id}" ${item.production_line_id == l.id ? 'selected' : ''}>${l.code} - ${l.name}</option>`
        ).join('');

        const shiftsOptions = (this.masterData.shifts || []).map(s => 
            `<option value="${s.id}" ${item.shift_id == s.id ? 'selected' : ''}>${s.name}</option>`
        ).join('');

        const machinesOptions = (this.masterData.machines || []).map(m => 
            `<option value="${m.id}" ${item.machine_id == m.id ? 'selected' : ''}>${m.code} - ${m.name}</option>`
        ).join('');

        modalContainer.innerHTML = `
            <div class="fixed inset-0 bg-slate-950/80 backdrop-blur-sm flex items-center justify-center p-3 sm:p-4 z-50 overflow-y-auto font-sans">
                <div class="${isLight ? 'bg-white border-slate-200 text-slate-900 shadow-2xl' : 'bg-slate-900 border-slate-800 text-slate-100 shadow-2xl'} border rounded-2xl max-w-2xl w-full my-auto overflow-hidden">
                    <!-- MODAL HEADER -->
                    <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950 border-slate-800'} px-5 py-4 border-b flex items-center justify-between">
                        <div class="flex items-center gap-2.5">
                            <div class="w-8 h-8 rounded-xl ${isLight ? 'bg-amber-100 text-amber-700 border-amber-200' : 'bg-amber-950 text-amber-400 border-amber-800'} border flex items-center justify-center">
                                <i data-lucide="wrench" class="w-4 h-4"></i>
                            </div>
                            <div>
                                <h3 class="font-bold text-sm ${isLight ? 'text-slate-900' : 'text-slate-100'}">Edit Log Problem #${item.id}</h3>
                                <p class="text-[11px] ${isLight ? 'text-slate-500' : 'text-slate-400'}">Ubah data problem, parameter lini, waktu, PIC, dan status penanganan</p>
                            </div>
                        </div>
                        <button id="btn-close-edit-dt-modal" class="p-1 rounded-lg ${isLight ? 'text-slate-400 hover:text-slate-800 hover:bg-slate-200' : 'text-slate-400 hover:text-slate-100 hover:bg-slate-800'} cursor-pointer transition-colors">
                            <i data-lucide="x" class="w-5 h-5"></i>
                        </button>
                    </div>

                    <form id="form-edit-dt-record" class="p-5 space-y-4 text-xs">
                        <datalist id="edit-problem-pic-list">
                            ${uniquePicOptions.map(p => `<option value="${p}"></option>`).join('')}
                        </datalist>

                        <!-- SEKSI 1: Parameter Operasional (Tanggal, Line, Shift, Team, Type Produk, OP Mesin, Kategori Problem) -->
                        <div class="${isLight ? 'bg-slate-50/80 border-slate-200' : 'bg-slate-950/60 border-slate-800'} border rounded-xl p-3.5 space-y-3">
                            <div class="font-bold text-[11px] ${isLight ? 'text-amber-800' : 'text-amber-400'} uppercase tracking-wider flex items-center gap-1.5 border-b ${isLight ? 'border-slate-200' : 'border-slate-800/80'} pb-1.5">
                                <i data-lucide="git-branch" class="w-3.5 h-3.5"></i>
                                Parameter Tanggal, Lini, Shift & Mesin
                            </div>

                            <div class="grid grid-cols-1 sm:grid-cols-4 gap-2.5">
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold">Tanggal*</label>
                                    <input type="date" name="trouble_date" value="${troubleDateVal}" required class="w-full ${isLight ? 'bg-white border-slate-200 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-mono outline-none focus:border-amber-500" />
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold">Line*</label>
                                    <select name="production_line_id" id="edit-dt-line-select" required class="w-full ${isLight ? 'bg-white border-slate-200 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-sans font-bold text-cyan-600 dark:text-cyan-400 outline-none focus:border-amber-500">
                                        ${linesOptions}
                                    </select>
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold">Shift*</label>
                                    <select name="shift_id" required class="w-full ${isLight ? 'bg-white border-slate-200 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-semibold outline-none focus:border-amber-500">
                                        ${shiftsOptions}
                                    </select>
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold">Team*</label>
                                    <select name="team" id="edit-dt-team-select" required class="w-full ${isLight ? 'bg-white border-slate-200 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-bold text-amber-500 outline-none focus:border-amber-500">
                                        <option value="Team A" ${item.team === 'Team A' ? 'selected' : ''}>Team A</option>
                                        <option value="Team B" ${item.team === 'Team B' ? 'selected' : ''}>Team B</option>
                                        <option value="Team C" ${item.team === 'Team C' ? 'selected' : ''}>Team C</option>
                                        <option value="Team D" ${item.team === 'Team D' ? 'selected' : ''}>Team D</option>
                                    </select>
                                </div>
                            </div>

                            <div class="grid grid-cols-1 sm:grid-cols-3 gap-2.5">
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold">Type Produk / SKU*</label>
                                    <select name="product_id" id="edit-dt-product-select" required class="w-full ${isLight ? 'bg-white border-slate-200 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-medium outline-none focus:border-amber-500">
                                        <!-- Populated dynamically based on line -->
                                    </select>
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold">OP Mesin*</label>
                                    <select name="machine_id" required class="w-full ${isLight ? 'bg-white border-slate-200 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-mono outline-none focus:border-amber-500">
                                        ${machinesOptions}
                                    </select>
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold">Kategori Problem*</label>
                                    <select name="problem_type" required class="w-full ${isLight ? 'bg-white border-slate-200 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-bold outline-none focus:border-amber-500">
                                        <option value="Inventory" ${item.problem_type === 'Inventory' ? 'selected' : ''}>Inventory</option>
                                        <option value="Others" ${item.problem_type === 'Others' || item.problem_type === 'Other' ? 'selected' : ''}>Others</option>
                                        <option value="Planning Downtime" ${item.problem_type === 'Planning Downtime' ? 'selected' : ''}>Planning Downtime</option>
                                        <option value="Problem Mesin Elektrik" ${item.problem_type === 'Problem Mesin Elektrik' ? 'selected' : ''}>Problem Mesin Elektrik</option>
                                        <option value="Problem Mesin Mekanik" ${item.problem_type === 'Problem Mesin Mekanik' || item.problem_type === 'Mesin' ? 'selected' : ''}>Problem Mesin Mekanik</option>
                                        <option value="Problem Tool" ${item.problem_type === 'Problem Tool' || item.problem_type === 'Tool' || item.problem_type === 'Dies / Tooling' ? 'selected' : ''}>Problem Tool</option>
                                    </select>
                                </div>
                            </div>
                        </div>

                        <!-- SEKSI 2: Problem, Penyebab, Perbaikan, Losstime, PIC & Status -->
                        <div class="${isLight ? 'bg-slate-50/80 border-slate-200' : 'bg-slate-950/60 border-slate-800'} border rounded-xl p-3.5 space-y-3">
                            <div class="font-bold text-[11px] ${isLight ? 'text-rose-800' : 'text-rose-400'} uppercase tracking-wider flex items-center gap-1.5 border-b ${isLight ? 'border-slate-200' : 'border-slate-800/80'} pb-1.5">
                                <i data-lucide="alert-triangle" class="w-3.5 h-3.5"></i>
                                Detail Problem, Penyebab, Perbaikan & PIC
                            </div>

                            <div>
                                <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold">Problem* (Deskripsi Masalah / Kerusakan)</label>
                                <input type="text" name="description" value="${item.description || ''}" placeholder="Jelaskan detail kendala problem yang terjadi..." required class="w-full ${isLight ? 'bg-white border-slate-200 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-sans outline-none focus:border-amber-500" />
                            </div>

                            <div>
                                <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold">Penyebab* (Akar Masalah / Root Cause)</label>
                                <input type="text" name="cause" value="${item.cause || ''}" placeholder="Jelaskan akar penyebab terjadinya problem..." required class="w-full ${isLight ? 'bg-white border-slate-200 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-sans outline-none focus:border-amber-500" />
                            </div>

                            <div>
                                <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold">Perbaikan* (Tindakan Penanganan / CAPA)</label>
                                <input type="text" name="action_taken" value="${item.action_taken || ''}" placeholder="Langkah perbaikan / countermeasure yang telah dilakukan..." required class="w-full ${isLight ? 'bg-white border-slate-200 text-emerald-800' : 'bg-slate-900 border-slate-800 text-emerald-300'} border rounded-lg px-2.5 py-2 font-sans outline-none focus:border-emerald-500" />
                            </div>

                            <div class="grid grid-cols-3 gap-2.5">
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-medium">Jam Mulai</label>
                                    <input type="time" name="dt_start_time" value="${startTimeVal}" class="w-full ${isLight ? 'bg-white border-slate-200 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-lg px-2 py-2 font-mono outline-none focus:border-amber-500" />
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-medium">Jam Selesai</label>
                                    <input type="time" name="dt_end_time" value="${endTimeVal}" class="w-full ${isLight ? 'bg-white border-slate-200 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-lg px-2 py-2 font-mono outline-none focus:border-amber-500" />
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold flex items-center justify-between">
                                        <span>Losstime (Menit)*</span>
                                        <span class="text-[10px] text-amber-400 font-mono font-bold">(Wajib Angka)</span>
                                    </label>
                                    <input type="number" name="duration_minutes" value="${durVal}" min="1" step="1" required class="w-full ${isLight ? 'bg-white border-slate-200 text-rose-700' : 'bg-slate-900 border-slate-800 text-rose-400'} border rounded-lg px-2 py-2 font-mono font-bold outline-none focus:border-amber-500" />
                                </div>
                            </div>

                            <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold">PIC (Daftar Nama PIC Departemen)*</label>
                                    <input type="text" name="pic" list="edit-problem-pic-list" value="${item.pic || ''}" placeholder="Pilih atau ketik PIC..." required class="w-full ${isLight ? 'bg-white border-slate-200 text-cyan-800' : 'bg-slate-900 border-slate-800 text-cyan-300'} border rounded-lg px-2.5 py-2 font-medium outline-none focus:border-cyan-500" />
                                </div>
                                <div>
                                    <label class="block ${isLight ? 'text-slate-700' : 'text-slate-300'} mb-1 font-semibold">Status (Closed / Open)*</label>
                                    <select name="status" required class="w-full ${isLight ? 'bg-white border-slate-200 text-slate-900' : 'bg-slate-900 border-slate-800 text-slate-100'} border rounded-lg px-2.5 py-2 font-bold outline-none focus:border-amber-500">
                                        <option value="CLOSED" ${String(item.status || '').toUpperCase() !== 'OPEN' ? 'selected' : ''}>CLOSED</option>
                                        <option value="OPEN" ${String(item.status || '').toUpperCase() === 'OPEN' ? 'selected' : ''}>OPEN</option>
                                    </select>
                                </div>
                            </div>
                        </div>

                        <!-- MODAL FOOTER -->
                        <div class="pt-3 flex justify-end gap-2 border-t ${isLight ? 'border-slate-200' : 'border-slate-800'}">
                            <button type="button" id="btn-cancel-edit-dt-modal" class="px-4 py-2 ${isLight ? 'bg-slate-100 hover:bg-slate-200 text-slate-700' : 'bg-slate-800 hover:bg-slate-700 text-slate-300'} rounded-xl text-xs font-medium cursor-pointer transition-colors">Batal</button>
                            <button type="submit" class="px-5 py-2 bg-amber-600 hover:bg-amber-500 text-white text-xs font-bold rounded-xl shadow-md shadow-amber-600/30 flex items-center gap-1.5 cursor-pointer transition-all">
                                <i data-lucide="save" class="w-3.5 h-3.5"></i>
                                Simpan Perubahan Problem Log
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();
        if (this.currentLang === 'ja') {
            i18n.localizeDom(modalContainer);
        }

        const dtLineSelect = document.getElementById('edit-dt-line-select');
        const dtProductSelect = document.getElementById('edit-dt-product-select');
        const dtMachineSelect = modalContainer.querySelector('select[name="machine_id"]');

        const populateDtMachines = (preferredMachineId = null) => {
            if (!dtLineSelect || !dtMachineSelect) return;
            const lineId = dtLineSelect.value;
            const lineMachines = this.getMachinesForLine(lineId);

            dtMachineSelect.innerHTML = lineMachines.map(m => 
                `<option value="${m.id}">${m.name} (${m.code})</option>`
            ).join('');

            if (preferredMachineId && lineMachines.some(m => m.id == preferredMachineId)) {
                dtMachineSelect.value = preferredMachineId;
            } else {
                const measuringMC = lineMachines.find(m => 
                    (m.name && m.name.toLowerCase().includes('measuring')) || 
                    (m.code && m.code.toLowerCase().includes('measuring'))
                );
                if (measuringMC) {
                    dtMachineSelect.value = measuringMC.id;
                } else if (lineMachines.length > 0) {
                    dtMachineSelect.value = lineMachines[0].id;
                }
            }
        };

        const populateDtProducts = () => {
            if (!dtLineSelect || !dtProductSelect) return;
            const lineId = dtLineSelect.value;
            const prods = this.getProductsForLine(lineId);

            if (prods.length === 0) {
                dtProductSelect.innerHTML = '<option value="" disabled selected>-- Tidak ada produk untuk lini ini --</option>';
            } else {
                dtProductSelect.innerHTML = prods.map(p => `
                    <option value="${p.id}" ${item.product_id == p.id ? 'selected' : ''}>${p.name} — SKU: ${p.sku}</option>
                `).join('');
            }
        };

        if (dtLineSelect) {
            dtLineSelect.addEventListener('change', () => {
                populateDtProducts();
                populateDtMachines();
            });
            populateDtProducts();
            populateDtMachines(item.machine_id);
        }

        const startTimeInput = modalContainer.querySelector('input[name="dt_start_time"]');
        const endTimeInput = modalContainer.querySelector('input[name="dt_end_time"]');
        const durInput = modalContainer.querySelector('input[name="duration_minutes"]');

        const updateCalc = () => {
            if (startTimeInput && endTimeInput && startTimeInput.value && endTimeInput.value) {
                const sParts = startTimeInput.value.split(':');
                const eParts = endTimeInput.value.split(':');
                let sMins = parseInt(sParts[0]) * 60 + parseInt(sParts[1] || 0);
                let eMins = parseInt(eParts[0]) * 60 + parseInt(eParts[1] || 0);
                if (eMins < sMins) eMins += 24 * 60;
                if (durInput) durInput.value = Math.max(1, eMins - sMins);
            }
        };

        if (startTimeInput) startTimeInput.addEventListener('change', updateCalc);
        if (endTimeInput) endTimeInput.addEventListener('change', updateCalc);

        document.getElementById('btn-close-edit-dt-modal').addEventListener('click', () => modalContainer.innerHTML = '');
        document.getElementById('btn-cancel-edit-dt-modal').addEventListener('click', () => modalContainer.innerHTML = '');

        const formEdit = document.getElementById('form-edit-dt-record');
        formEdit.addEventListener('submit', async (e) => {
            e.preventDefault();
            const formData = new FormData(e.target);
            const data = Object.fromEntries(formData.entries());

            const durationMins = parseFloat(data.duration_minutes || 0);
            if (isNaN(durationMins) || durationMins <= 0) {
                this.showNotification('⚠️ Losstime Wajib Diisi', 'Durasi losstime harus berupa angka menit yang lebih besar dari 0.', 'delete');
                return;
            }

            try {
                await api.updateDowntime(id, data);
                this.showNotification('Log Problem Diperbarui', `Log problem #${id} berhasil diperbarui!`, 'update');
                modalContainer.innerHTML = '';
                if (data.trouble_date) {
                    this.selectedDailyDate = data.trouble_date;
                }
                await this.renderDailyReport();
            } catch (err) {
                alert('Gagal memperbarui log problem: ' + (err.response?.data?.message || err.message));
            }
        });
    }

    showDeleteProductionModal(id) {
        const modalContainer = document.getElementById('modal-container');
        modalContainer.innerHTML = `
            <div class="fixed inset-0 bg-slate-950/80 backdrop-blur-sm flex items-center justify-center p-4 z-50">
                <div class="bg-slate-900 border border-slate-800 rounded-2xl p-6 max-w-md w-full shadow-2xl">
                    <div class="flex items-center gap-3 mb-4 text-rose-400">
                        <div class="w-10 h-10 rounded-full bg-rose-950/80 border border-rose-800/80 flex items-center justify-center flex-shrink-0 shadow-lg shadow-rose-950/50">
                            <i data-lucide="trash-2" class="w-5 h-5"></i>
                        </div>
                        <div>
                            <h3 class="font-bold text-sm text-slate-100">Hapus Catatan Produksi #${id}</h3>
                            <p class="text-xs text-slate-400">Tindakan ini akan menghapus permanen data laporan & OEE terkait.</p>
                        </div>
                    </div>

                    <div class="p-3 bg-slate-950 border border-slate-800 rounded-xl text-xs text-slate-300 mb-5">
                        Apakah Anda yakin ingin menghapus data laporan produksi <span class="font-bold text-rose-400">Record #${id}</span>?
                    </div>

                    <div class="flex justify-end gap-2">
                        <button type="button" id="btn-cancel-del-prod" class="px-3.5 py-2 bg-slate-800 hover:bg-slate-700 text-slate-300 rounded-lg text-xs font-medium cursor-pointer transition-colors">Batal</button>
                        <button type="button" id="btn-confirm-del-prod" class="px-4 py-2 bg-rose-600 hover:bg-rose-500 text-white text-xs font-semibold rounded-lg shadow-md shadow-rose-600/30 cursor-pointer transition-all">Ya, Hapus Record</button>
                    </div>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();
        if (this.currentLang === 'ja') {
            i18n.localizeDom(modalContainer);
        }

        document.getElementById('btn-cancel-del-prod').addEventListener('click', () => modalContainer.innerHTML = '');
        document.getElementById('btn-confirm-del-prod').addEventListener('click', async () => {
            try {
                await api.deleteProductionRecord(id);
                this.showNotification('Catatan Produksi Dihapus', `Laporan produksi #${id} berhasil dihapus dari database!`, 'delete');
                modalContainer.innerHTML = '';
                await this.renderDailyReport();
            } catch (err) {
                alert('Gagal menghapus catatan produksi: ' + (err.response?.data?.message || err.message));
            }
        });
    }

    showDeleteDowntimeModal(id) {
        const modalContainer = document.getElementById('modal-container');
        modalContainer.innerHTML = `
            <div class="fixed inset-0 bg-slate-950/80 backdrop-blur-sm flex items-center justify-center p-4 z-50">
                <div class="bg-slate-900 border border-slate-800 rounded-2xl p-6 max-w-md w-full shadow-2xl">
                    <div class="flex items-center gap-3 mb-4 text-rose-400">
                        <div class="w-10 h-10 rounded-full bg-rose-950/80 border border-rose-800/80 flex items-center justify-center flex-shrink-0 shadow-lg shadow-rose-950/50">
                            <i data-lucide="trash-2" class="w-5 h-5"></i>
                        </div>
                        <div>
                            <h3 class="font-bold text-sm text-slate-100">Hapus Log Problem #${id}</h3>
                            <p class="text-xs text-slate-400">Tindakan ini akan menghapus log kendala problem/downtime dari riwayat.</p>
                        </div>
                    </div>

                    <div class="p-3 bg-slate-950 border border-slate-800 rounded-xl text-xs text-slate-300 mb-5">
                        Apakah Anda yakin ingin menghapus <span class="font-bold text-rose-400">Log Problem #${id}</span>?
                    </div>

                    <div class="flex justify-end gap-2">
                        <button type="button" id="btn-cancel-del-dt" class="px-3.5 py-2 bg-slate-800 hover:bg-slate-700 text-slate-300 rounded-lg text-xs font-medium cursor-pointer transition-colors">Batal</button>
                        <button type="button" id="btn-confirm-del-dt" class="px-4 py-2 bg-rose-600 hover:bg-rose-500 text-white text-xs font-semibold rounded-lg shadow-md shadow-rose-600/30 cursor-pointer transition-all">Ya, Hapus Log Problem</button>
                    </div>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();
        if (this.currentLang === 'ja') {
            i18n.localizeDom(modalContainer);
        }

        document.getElementById('btn-cancel-del-dt').addEventListener('click', () => modalContainer.innerHTML = '');
        document.getElementById('btn-confirm-del-dt').addEventListener('click', async () => {
            try {
                await api.deleteDowntime(id);
                this.showNotification('Log Problem Dihapus', `Log problem #${id} berhasil dihapus dari database!`, 'delete');
                modalContainer.innerHTML = '';
                await this.renderDailyReport();
            } catch (err) {
                alert('Gagal menghapus log problem: ' + (err.response?.data?.message || err.message));
            }
        });
    }


    // ==========================================
    // 10. USER MANAGEMENT & RBAC PAGE
    // ==========================================
    async renderUsers() {
        const [uRes, rRes] = await Promise.all([
            api.getUsers({ per_page: 100 }),
            api.getRoles(),
        ]);

        const users = uRes.data.data || [];
        const roles = rRes.data.data || [];

        // Role counting
        const countByRole = (roleKey) => {
            return users.filter(u => (u.roles || []).some(r => r.name === roleKey || r.display_name?.toLowerCase().includes(roleKey))).length;
        };

        const superAdminCount = countByRole('super_admin');
        const managerCount = countByRole('production_manager');
        const supervisorCount = users.filter(u => (u.roles || []).some(r => r.name === 'production_supervisor' || r.name === 'production_leader')).length;
        const qcCount = countByRole('quality_control');
        const operatorCount = countByRole('operator');

        const roleColor = (roleName) => {
            const r = (roleName || '').toLowerCase();
            if (r.includes('super')) return 'bg-purple-950/80 text-purple-300 border-purple-800/80';
            if (r.includes('manager')) return 'bg-blue-950/80 text-blue-300 border-blue-800/80';
            if (r.includes('supervisor')) return 'bg-emerald-950/80 text-emerald-300 border-emerald-800/80';
            if (r.includes('leader')) return 'bg-teal-950/80 text-teal-300 border-teal-800/80';
            if (r.includes('quality') || r.includes('qc')) return 'bg-rose-950/80 text-rose-300 border-rose-800/80';
            if (r.includes('operator')) return 'bg-amber-950/80 text-amber-300 border-amber-800/80';
            return 'bg-cyan-950/80 text-cyan-300 border-cyan-800/80';
        };

        const content = document.getElementById('content-body');
        content.innerHTML = `
            <div class="flex flex-wrap items-center justify-between gap-3">
                <div>
                    <h2 class="text-xl font-bold text-slate-100 flex items-center gap-2">
                        <i data-lucide="users" class="w-5 h-5 text-cyan-400"></i>
                        <span>User Management</span>
                    </h2>
                </div>
                <div class="flex items-center gap-2 flex-wrap">
                    <button id="btn-generate-default-users" class="bg-indigo-600 hover:bg-indigo-500 text-white text-xs font-semibold px-3 py-1.5 rounded-lg flex items-center gap-1.5 shadow-md shadow-indigo-600/20 cursor-pointer transition-all">
                        <i data-lucide="sparkles" class="w-4 h-4 text-indigo-200"></i>
                        <span>Generate Master Users (@prodcr.yasunaga.com)</span>
                    </button>
                    <button id="btn-add-user" class="bg-cyan-600 hover:bg-cyan-500 text-white text-xs font-semibold px-3.5 py-1.5 rounded-lg flex items-center gap-1.5 shadow-md shadow-cyan-600/20 cursor-pointer transition-all">
                        <i data-lucide="user-plus" class="w-4 h-4"></i>
                        <span>+ Add User</span>
                    </button>
                </div>
            </div>

            <!-- ROLES SUMMARY STAT CARDS -->
            <div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-3">
                <div class="bg-slate-900 border border-slate-800 rounded-xl p-3.5 flex items-center justify-between">
                    <div>
                        <div class="text-[11px] font-bold text-purple-400 uppercase tracking-wider">Super Admin</div>
                        <div class="text-[10px] text-slate-400 mt-0.5">${superAdminCount} Active Accounts</div>
                    </div>
                    <span class="w-8 h-8 rounded-lg bg-purple-950 border border-purple-800 flex items-center justify-center text-purple-400 font-bold text-xs font-mono">
                        ${superAdminCount}
                    </span>
                </div>

                <div class="bg-slate-900 border border-slate-800 rounded-xl p-3.5 flex items-center justify-between">
                    <div>
                        <div class="text-[11px] font-bold text-blue-400 uppercase tracking-wider">Plant Manager</div>
                        <div class="text-[10px] text-slate-400 mt-0.5">${managerCount} Active Accounts</div>
                    </div>
                    <span class="w-8 h-8 rounded-lg bg-blue-950 border border-blue-800 flex items-center justify-center text-blue-400 font-bold text-xs font-mono">
                        ${managerCount}
                    </span>
                </div>

                <div class="bg-slate-900 border border-slate-800 rounded-xl p-3.5 flex items-center justify-between">
                    <div>
                        <div class="text-[11px] font-bold text-emerald-400 uppercase tracking-wider">Supervisor & Leader</div>
                        <div class="text-[10px] text-slate-400 mt-0.5">${supervisorCount} Active Accounts</div>
                    </div>
                    <span class="w-8 h-8 rounded-lg bg-emerald-950 border border-emerald-800 flex items-center justify-center text-emerald-400 font-bold text-xs font-mono">
                        ${supervisorCount}
                    </span>
                </div>

                <div class="bg-slate-900 border border-slate-800 rounded-xl p-3.5 flex items-center justify-between">
                    <div>
                        <div class="text-[11px] font-bold text-rose-400 uppercase tracking-wider">Quality Control (QC)</div>
                        <div class="text-[10px] text-slate-400 mt-0.5">${qcCount} Active Accounts</div>
                    </div>
                    <span class="w-8 h-8 rounded-lg bg-rose-950 border border-rose-800 flex items-center justify-center text-rose-400 font-bold text-xs font-mono">
                        ${qcCount}
                    </span>
                </div>

                <div class="bg-slate-900 border border-slate-800 rounded-xl p-3.5 flex items-center justify-between col-span-2 sm:col-span-1">
                    <div>
                        <div class="text-[11px] font-bold text-amber-400 uppercase tracking-wider">Lead Operator</div>
                        <div class="text-[10px] text-slate-400 mt-0.5">${operatorCount} Active Accounts</div>
                    </div>
                    <span class="w-8 h-8 rounded-lg bg-amber-950 border border-amber-800 flex items-center justify-center text-amber-400 font-bold text-xs font-mono">
                        ${operatorCount}
                    </span>
                </div>
            </div>

            <!-- SEARCH & FILTER STRIP -->
            <div class="bg-slate-900 border border-slate-800 rounded-xl p-3 flex flex-wrap items-center justify-between gap-3">
                <div class="flex items-center gap-2 flex-1 min-w-[240px]">
                    <div class="relative w-full max-w-xs">
                        <i data-lucide="search" class="w-3.5 h-3.5 absolute left-3 top-2.5 text-slate-500"></i>
                        <input type="text" id="input-search-users" placeholder="Cari nama atau email user..." class="w-full bg-slate-950 border border-slate-800 rounded-lg pl-8 pr-3 py-1.5 text-xs text-slate-200 focus:outline-none focus:border-cyan-500 font-sans" />
                    </div>
                    <select id="select-filter-role" class="bg-slate-950 border border-slate-800 rounded-lg px-2.5 py-1.5 text-xs text-slate-300 focus:outline-none focus:border-cyan-500 font-sans">
                        <option value="">Semua Role Access</option>
                        ${roles.map(r => `<option value="${r.id}">${r.display_name}</option>`).join('')}
                    </select>
                </div>
                <div class="text-xs text-slate-400 font-mono">
                    Total: <strong class="text-cyan-400">${users.length}</strong> Terdaftar • Default Password: <code class="text-amber-400 bg-slate-950 px-1.5 py-0.5 rounded border border-slate-800">password</code>
                </div>
            </div>

            <!-- USERS TABLE -->
            <div class="bg-slate-900 border border-slate-800 rounded-xl shadow-xl overflow-hidden">
                <div class="overflow-x-auto">
                    <table class="w-full text-left text-xs">
                        <thead class="bg-slate-950 text-slate-400 uppercase font-semibold text-[10px] border-b border-slate-800">
                            <tr>
                                <th class="p-3.5 text-center w-12">No</th>
                                <th class="p-3.5">User Profile & Name</th>
                                <th class="p-3.5">Corporate Email (@prodcr.yasunaga.com)</th>
                                <th class="p-3.5">Assigned Role(s)</th>
                                <th class="p-3.5">Account Status</th>
                                <th class="p-3.5 text-right">Actions</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y divide-slate-800/60 font-mono" id="users-table-body">
                            ${users.map((u, idx) => `
                                <tr class="hover:bg-slate-800/40 transition-colors user-row" data-name="${(u.name || '').toLowerCase()}" data-email="${(u.email || '').toLowerCase()}" data-role-id="${(u.roles && u.roles[0]) ? u.roles[0].id : ''}">
                                    <td class="p-3.5 text-center font-bold text-slate-500 font-mono text-[11px]">${idx + 1}</td>
                                    <td class="p-3.5 font-sans">
                                        <div class="flex items-center gap-2.5">
                                            <div class="w-8 h-8 rounded-lg bg-gradient-to-br from-cyan-600 to-blue-700 border border-cyan-400/40 flex items-center justify-center font-bold text-xs text-white shadow-sm flex-shrink-0">
                                                ${(u.name || 'U').slice(0, 2).toUpperCase()}
                                            </div>
                                            <div>
                                                <div class="font-bold text-slate-100">${u.name}</div>
                                                <div class="text-[10px] text-slate-400">ID: #${u.id}</div>
                                            </div>
                                        </div>
                                    </td>
                                    <td class="p-3.5 text-cyan-400 font-semibold font-mono">
                                        ${u.email}
                                    </td>
                                    <td class="p-3.5 font-sans">
                                        ${(u.roles || []).map(r => `<span class="px-2 py-0.5 rounded text-[10px] font-semibold border ${roleColor(r.display_name)} mr-1 inline-block my-0.5">${r.display_name}</span>`).join('')}
                                    </td>
                                    <td class="p-3.5 font-sans">
                                        <span class="px-2 py-0.5 rounded text-[10px] font-bold ${u.is_active ? 'bg-emerald-500/20 text-emerald-400 border border-emerald-500/30' : 'bg-rose-500/20 text-rose-400 border border-rose-500/30'}">
                                            ${u.is_active ? '● ACTIVE' : '○ DEACTIVATED'}
                                        </span>
                                    </td>
                                    <td class="p-3.5 text-right font-sans">
                                        <div class="flex items-center justify-end gap-1.5">
                                            <button class="btn-switch-user px-2 py-1 bg-cyan-950/80 hover:bg-cyan-900 text-cyan-300 border border-cyan-800 rounded text-[11px] font-semibold flex items-center gap-1 cursor-pointer transition-colors" data-id="${u.id}" data-name="${this.escapeHtml(u.name)}" data-email="${this.escapeHtml(u.email)}" data-role="${(u.roles && u.roles[0]) ? this.escapeHtml(u.roles[0].display_name) : 'User'}" data-role-key="${(u.roles && u.roles[0]) ? this.escapeHtml(u.roles[0].name) : 'operator'}" title="Masuk langsung sebagai user ini untuk menguji hak akses">
                                                <i data-lucide="log-in" class="w-3 h-3 text-cyan-400"></i>
                                                <span>Login As</span>
                                            </button>
                                            <button class="btn-edit-user px-2 py-1 bg-slate-800 hover:bg-slate-700 text-slate-200 border border-slate-700 rounded text-[11px] font-medium flex items-center gap-1 cursor-pointer transition-colors" data-id="${u.id}">
                                                <i data-lucide="pencil" class="w-3 h-3 text-slate-400"></i>
                                                <span>Edit</span>
                                            </button>
                                            <button class="btn-delete-user px-2 py-1 bg-rose-950/50 hover:bg-rose-900 text-rose-300 border border-rose-900 rounded text-[11px] font-medium flex items-center gap-1 cursor-pointer transition-colors" data-id="${u.id}" data-name="${this.escapeHtml(u.name)}">
                                                <i data-lucide="trash-2" class="w-3 h-3 text-rose-400"></i>
                                                <span>Delete</span>
                                            </button>
                                        </div>
                                    </td>
                                </tr>
                            `).join('')}
                        </tbody>
                    </table>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();

        // Search & Filter event listeners
        const searchInput = document.getElementById('input-search-users');
        const roleFilter = document.getElementById('select-filter-role');

        const filterRows = () => {
            const q = (searchInput?.value || '').toLowerCase().trim();
            const roleId = roleFilter?.value || '';

            document.querySelectorAll('.user-row').forEach(row => {
                const name = row.getAttribute('data-name');
                const email = row.getAttribute('data-email');
                const rowRoleId = row.getAttribute('data-role-id');

                const matchesQuery = !q || name.includes(q) || email.includes(q);
                const matchesRole = !roleId || rowRoleId === roleId;

                if (matchesQuery && matchesRole) {
                    row.classList.remove('hidden');
                } else {
                    row.classList.add('hidden');
                }
            });
        };

        if (searchInput) searchInput.addEventListener('input', filterRows);
        if (roleFilter) roleFilter.addEventListener('change', filterRows);

        // Generate default master users button
        const btnGen = document.getElementById('btn-generate-default-users');
        if (btnGen) {
            btnGen.addEventListener('click', async () => {
                try {
                    btnGen.disabled = true;
                    btnGen.innerHTML = `<span class="inline-block animate-spin mr-1.5">⟳</span> Generating...`;
                    const res = await api.generateDefaultUsers();
                    this.showNotification('Master User Terbentuk', res.data.message || 'Akun pengguna default berhasil dibuat!', 'success');
                    await this.renderUsers();
                } catch (err) {
                    alert('Gagal men-generate user: ' + (err.response?.data?.message || err.message));
                    if (btnGen) {
                        btnGen.disabled = false;
                        btnGen.innerHTML = `<i data-lucide="sparkles" class="w-4 h-4 text-indigo-200"></i><span>Generate Master Users</span>`;
                        if (window.lucide) window.lucide.createIcons();
                    }
                }
            });
        }

        // Add user button
        const btnAdd = document.getElementById('btn-add-user');
        if (btnAdd) {
            btnAdd.addEventListener('click', () => {
                this.showAddUserModal(roles);
            });
        }

        // Action Buttons in rows
        document.querySelectorAll('.btn-switch-user').forEach(btn => {
            btn.addEventListener('click', () => {
                const id = btn.getAttribute('data-id');
                const name = btn.getAttribute('data-name');
                const email = btn.getAttribute('data-email');
                const role = btn.getAttribute('data-role');
                const roleKey = btn.getAttribute('data-role-key');

                this.login({
                    id: Number(id),
                    name,
                    email,
                    roles: [role],
                    role_keys: [roleKey],
                });
            });
        });

        document.querySelectorAll('.btn-edit-user').forEach(btn => {
            btn.addEventListener('click', () => {
                const id = btn.getAttribute('data-id');
                const user = users.find(u => u.id == id);
                if (user) {
                    this.showEditUserModal(user, roles);
                }
            });
        });

        document.querySelectorAll('.btn-delete-user').forEach(btn => {
            btn.addEventListener('click', () => {
                const id = btn.getAttribute('data-id');
                const name = btn.getAttribute('data-name');
                this.showDeleteUserModal(id, name);
            });
        });

        if (this.currentLang === 'ja') {
            i18n.localizeDom(document.getElementById('content-body') || document.body);
        }
    }

    showAddUserModal(roles) {
        const modalContainer = document.getElementById('modal-container');
        modalContainer.innerHTML = `
            <div class="fixed inset-0 bg-slate-950/80 backdrop-blur-sm flex items-center justify-center p-4 z-50">
                <div class="bg-slate-900 border border-slate-800 rounded-xl p-6 max-w-md w-full shadow-2xl">
                    <div class="flex items-center justify-between mb-4 pb-3 border-b border-slate-800">
                        <h3 class="font-bold text-sm text-slate-100 flex items-center gap-2">
                            <i data-lucide="user-plus" class="w-4 h-4 text-cyan-400"></i>
                            Tambah Akun Pengguna Baru (RBAC)
                        </h3>
                        <button id="btn-close-user-modal" class="text-slate-400 hover:text-slate-200 cursor-pointer">
                            <i data-lucide="x" class="w-4 h-4"></i>
                        </button>
                    </div>

                    <form id="form-add-user" class="space-y-3.5 text-xs">
                        <div>
                            <label class="block text-slate-300 font-semibold mb-1">Nama Lengkap Pengguna <span class="text-rose-400">*</span></label>
                            <input type="text" id="add-user-name" name="name" required placeholder="Contoh: Budi Santoso" class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-2 text-slate-100 font-sans" />
                        </div>

                        <div>
                            <label class="block text-slate-300 font-semibold mb-1 flex items-center justify-between">
                                <span>Corporate Email / Username <span class="text-rose-400">*</span></span>
                                <span class="text-[10px] text-cyan-400 font-mono">@prodcr.yasunaga.com</span>
                            </label>
                            <input type="email" id="add-user-email" name="email" required placeholder="budi.santoso@prodcr.yasunaga.com" class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-2 text-slate-100 font-mono" />
                        </div>

                        <div>
                            <label class="block text-slate-300 font-semibold mb-1">Password <span class="text-[10px] text-slate-500 font-mono">(Default: password)</span></label>
                            <input type="text" name="password" value="password" required class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-2 text-slate-100 font-mono" />
                        </div>

                        <div>
                            <label class="block text-slate-300 font-semibold mb-1">Role & Hak Akses (RBAC) <span class="text-rose-400">*</span></label>
                            <select name="role_id" required class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-2 text-slate-100 font-sans font-semibold">
                                ${roles.map(r => `<option value="${r.id}">${r.display_name}</option>`).join('')}
                            </select>
                        </div>

                        <div class="flex items-center gap-2 pt-1">
                            <input type="checkbox" id="add-user-active" name="is_active" checked class="rounded bg-slate-950 border-slate-700 text-cyan-500" />
                            <label for="add-user-active" class="text-slate-300 cursor-pointer">Status Akun Aktif (Active)</label>
                        </div>

                        <div class="flex justify-end gap-2 pt-3 border-t border-slate-800">
                            <button type="button" id="btn-cancel-add-user" class="px-3 py-1.5 bg-slate-800 hover:bg-slate-700 text-slate-300 rounded text-xs font-medium cursor-pointer">Batal</button>
                            <button type="submit" class="px-4 py-1.5 bg-cyan-600 hover:bg-cyan-500 text-white rounded text-xs font-semibold shadow-md shadow-cyan-600/20 cursor-pointer">Simpan Akun</button>
                        </div>
                    </form>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();
        if (this.currentLang === 'ja') {
            i18n.localizeDom(modalContainer);
        }

        // Auto generator email as user types name
        const nameInput = document.getElementById('add-user-name');
        const emailInput = document.getElementById('add-user-email');
        if (nameInput && emailInput) {
            nameInput.addEventListener('input', () => {
                const val = nameInput.value.trim().toLowerCase().replace(/[^a-z0-9]/g, '.').replace(/\.+/g, '.');
                if (val) {
                    emailInput.value = `${val}@prodcr.yasunaga.com`;
                }
            });
        }

        const closeModal = () => modalContainer.innerHTML = '';
        document.getElementById('btn-close-user-modal')?.addEventListener('click', closeModal);
        document.getElementById('btn-cancel-add-user')?.addEventListener('click', closeModal);

        document.getElementById('form-add-user')?.addEventListener('submit', async (e) => {
            e.preventDefault();
            const formData = new FormData(e.target);
            const data = {
                name: formData.get('name'),
                email: formData.get('email'),
                password: formData.get('password') || 'password',
                role_ids: [Number(formData.get('role_id'))],
                is_active: formData.get('is_active') === 'on' || formData.get('is_active') === '1',
            };

            try {
                await api.createUser(data);
                this.showNotification('User Baru Dibuat', `Akun untuk ${data.name} (${data.email}) berhasil didaftarkan!`, 'create');
                closeModal();
                await this.renderUsers();
            } catch (err) {
                alert('Gagal membuat user: ' + (err.response?.data?.message || err.message));
            }
        });
    }

    showEditUserModal(user, roles) {
        const modalContainer = document.getElementById('modal-container');
        const currentRoleId = (user.roles && user.roles[0]) ? user.roles[0].id : '';

        modalContainer.innerHTML = `
            <div class="fixed inset-0 bg-slate-950/80 backdrop-blur-sm flex items-center justify-center p-4 z-50">
                <div class="bg-slate-900 border border-slate-800 rounded-xl p-6 max-w-md w-full shadow-2xl">
                    <div class="flex items-center justify-between mb-4 pb-3 border-b border-slate-800">
                        <h3 class="font-bold text-sm text-slate-100 flex items-center gap-2">
                            <i data-lucide="pencil" class="w-4 h-4 text-cyan-400"></i>
                            Edit Akun Pengguna (#${user.id})
                        </h3>
                        <button id="btn-close-edit-user-modal" class="text-slate-400 hover:text-slate-200 cursor-pointer">
                            <i data-lucide="x" class="w-4 h-4"></i>
                        </button>
                    </div>

                    <form id="form-edit-user" class="space-y-3.5 text-xs">
                        <div>
                            <label class="block text-slate-300 font-semibold mb-1">Nama Lengkap Pengguna <span class="text-rose-400">*</span></label>
                            <input type="text" name="name" value="${this.escapeHtml(user.name)}" required class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-2 text-slate-100 font-sans" />
                        </div>

                        <div>
                            <label class="block text-slate-300 font-semibold mb-1 flex items-center justify-between">
                                <span>Corporate Email / Username <span class="text-rose-400">*</span></span>
                                <span class="text-[10px] text-cyan-400 font-mono">@prodcr.yasunaga.com</span>
                            </label>
                            <input type="email" name="email" value="${this.escapeHtml(user.email)}" required class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-2 text-slate-100 font-mono" />
                        </div>

                        <div>
                            <label class="block text-slate-300 font-semibold mb-1">Ganti Password <span class="text-[10px] text-slate-500 font-mono">(Kosongkan jika tidak ingin mengubah)</span></label>
                            <input type="text" name="password" placeholder="Masukkan password baru..." class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-2 text-slate-100 font-mono" />
                        </div>

                        <div>
                            <label class="block text-slate-300 font-semibold mb-1">Role & Hak Akses (RBAC) <span class="text-rose-400">*</span></label>
                            <select name="role_id" required class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-2 text-slate-100 font-sans font-semibold">
                                ${roles.map(r => `<option value="${r.id}" ${r.id == currentRoleId ? 'selected' : ''}>${r.display_name}</option>`).join('')}
                            </select>
                        </div>

                        <div class="flex items-center gap-2 pt-1">
                            <input type="checkbox" id="edit-user-active" name="is_active" ${user.is_active ? 'checked' : ''} class="rounded bg-slate-950 border-slate-700 text-cyan-500" />
                            <label for="edit-user-active" class="text-slate-300 cursor-pointer">Status Akun Aktif (Active)</label>
                        </div>

                        <div class="flex justify-end gap-2 pt-3 border-t border-slate-800">
                            <button type="button" id="btn-cancel-edit-user" class="px-3 py-1.5 bg-slate-800 hover:bg-slate-700 text-slate-300 rounded text-xs font-medium cursor-pointer">Batal</button>
                            <button type="submit" class="px-4 py-1.5 bg-cyan-600 hover:bg-cyan-500 text-white rounded text-xs font-semibold shadow-md shadow-cyan-600/20 cursor-pointer">Update Akun</button>
                        </div>
                    </form>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();
        if (this.currentLang === 'ja') {
            i18n.localizeDom(modalContainer);
        }

        const closeModal = () => modalContainer.innerHTML = '';
        document.getElementById('btn-close-edit-user-modal')?.addEventListener('click', closeModal);
        document.getElementById('btn-cancel-edit-user')?.addEventListener('click', closeModal);

        document.getElementById('form-edit-user')?.addEventListener('submit', async (e) => {
            e.preventDefault();
            const formData = new FormData(e.target);
            const data = {
                name: formData.get('name'),
                email: formData.get('email'),
                role_ids: [Number(formData.get('role_id'))],
                is_active: formData.get('is_active') === 'on' || formData.get('is_active') === '1',
            };

            const pass = formData.get('password');
            if (pass && pass.trim()) {
                data.password = pass.trim();
            }

            try {
                await api.updateUser(user.id, data);
                this.showNotification('Akun Diperbarui', `Akun ${data.name} berhasil diperbarui!`, 'update');
                closeModal();
                await this.renderUsers();
            } catch (err) {
                alert('Gagal mengupdate user: ' + (err.response?.data?.message || err.message));
            }
        });
    }

    showDeleteUserModal(id, name) {
        const modalContainer = document.getElementById('modal-container');
        modalContainer.innerHTML = `
            <div class="fixed inset-0 bg-slate-950/80 backdrop-blur-sm flex items-center justify-center p-4 z-50">
                <div class="bg-slate-900 border border-slate-800 rounded-xl p-6 max-w-md w-full shadow-2xl">
                    <div class="flex items-center gap-3 mb-4 text-rose-400">
                        <div class="w-10 h-10 rounded-full bg-rose-950/80 border border-rose-800/80 flex items-center justify-center flex-shrink-0">
                            <i data-lucide="alert-triangle" class="w-5 h-5"></i>
                        </div>
                        <div>
                            <h3 class="font-bold text-sm text-slate-100">Hapus Akun Pengguna</h3>
                            <p class="text-xs text-slate-400">Tindakan ini akan menghapus akun user dari sistem.</p>
                        </div>
                    </div>

                    <div class="p-3 bg-slate-950 border border-slate-800 rounded-lg text-xs text-slate-300 mb-5">
                        Apakah Anda yakin ingin menghapus akun <span class="font-bold text-rose-400">${name}</span>?
                    </div>

                    <div class="flex justify-end gap-2">
                        <button type="button" id="btn-cancel-delete-user" class="px-3 py-1.5 bg-slate-800 hover:bg-slate-700 text-slate-300 rounded text-xs font-medium cursor-pointer">Batal</button>
                        <button type="button" id="btn-confirm-delete-user" class="px-4 py-1.5 bg-rose-600 hover:bg-rose-500 text-white text-xs font-semibold rounded shadow-md shadow-rose-600/20 cursor-pointer">Hapus Akun</button>
                    </div>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();
        if (this.currentLang === 'ja') {
            i18n.localizeDom(modalContainer);
        }

        const closeModal = () => modalContainer.innerHTML = '';
        document.getElementById('btn-cancel-delete-user')?.addEventListener('click', closeModal);

        document.getElementById('btn-confirm-delete-user')?.addEventListener('click', async () => {
            try {
                await api.deleteUser(id);
                this.showNotification('Akun Dihapus', `Akun ${name} berhasil dihapus dari sistem.`, 'delete');
                closeModal();
                await this.renderUsers();
            } catch (err) {
                alert('Gagal menghapus akun: ' + (err.response?.data?.message || err.message));
            }
        });
    }

    // ==========================================
    // 11. SYSTEM SETTINGS PAGE
    // ==========================================
    async renderSettings() {
        const res = await api.getSettings();
        const settings = res.data.data;

        const targetOee = settings.find(s => s.key === 'target_oee')?.value || '85.0';
        const targetAvail = settings.find(s => s.key === 'target_availability')?.value || '90.0';
        const targetPerf = settings.find(s => s.key === 'target_performance')?.value || '95.0';
        const targetQual = settings.find(s => s.key === 'target_quality')?.value || '99.0';
        const tvRefresh = settings.find(s => s.key === 'auto_refresh_interval')?.value || '30';

        const content = document.getElementById('content-body');
        content.innerHTML = `
            <div class="mb-4">
                <h2 class="text-xl font-bold text-slate-100 flex items-center gap-2">
                    <i data-lucide="sliders" class="w-5 h-5 text-cyan-400"></i>
                    <span>System Settings</span>
                </h2>
            </div>

            <div class="bg-slate-900 border border-slate-800 rounded-xl p-6 shadow-xl max-w-2xl">
                <form id="form-system-settings" class="space-y-4 text-xs">
                    <h3 class="font-bold text-sm text-cyan-400 mb-3 border-b border-slate-800 pb-2">Global OEE Target Thresholds</h3>
                    
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="block text-slate-300 font-semibold mb-1">Target OEE (%)</label>
                            <input type="number" step="0.1" name="target_oee" value="${targetOee}" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-2 text-slate-100 font-mono" />
                            <p class="text-[10px] text-slate-500 mt-1">World Class Benchmark: 85.0%</p>
                        </div>
                        <div>
                            <label class="block text-slate-300 font-semibold mb-1">Target Availability (%)</label>
                            <input type="number" step="0.1" name="target_availability" value="${targetAvail}" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-2 text-slate-100 font-mono" />
                            <p class="text-[10px] text-slate-500 mt-1">World Class Benchmark: 90.0%</p>
                        </div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="block text-slate-300 font-semibold mb-1">Target Performance (%)</label>
                            <input type="number" step="0.1" name="target_performance" value="${targetPerf}" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-2 text-slate-100 font-mono" />
                            <p class="text-[10px] text-slate-500 mt-1">World Class Benchmark: 95.0%</p>
                        </div>
                        <div>
                            <label class="block text-slate-300 font-semibold mb-1">Target Quality (%)</label>
                            <input type="number" step="0.1" name="target_quality" value="${targetQual}" required class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-2 text-slate-100 font-mono" />
                            <p class="text-[10px] text-slate-500 mt-1">World Class Benchmark: 99.0%</p>
                        </div>
                    </div>

                    <h3 class="font-bold text-sm text-cyan-400 pt-3 mb-3 border-b border-slate-800 pb-2">Production Floor TV Mode Settings</h3>

                    <div>
                        <label class="block text-slate-300 font-semibold mb-1">TV Auto-Refresh Interval (Seconds)</label>
                        <select name="auto_refresh_interval" class="w-full bg-slate-950 border border-slate-800 rounded px-3 py-2 text-slate-100 font-mono">
                            <option value="15" ${tvRefresh === '15' ? 'selected' : ''}>15 Seconds</option>
                            <option value="30" ${tvRefresh === '30' ? 'selected' : ''}>30 Seconds (Default)</option>
                            <option value="60" ${tvRefresh === '60' ? 'selected' : ''}>1 Minute</option>
                            <option value="300" ${tvRefresh === '300' ? 'selected' : ''}>5 Minutes</option>
                        </select>
                    </div>

                    <div class="pt-4 border-t border-slate-800 flex justify-end">
                        <button type="submit" class="bg-cyan-600 hover:bg-cyan-500 text-white text-xs font-semibold px-4 py-2 rounded-lg shadow-md shadow-cyan-600/20">
                            Save System Settings
                        </button>
                    </div>
                </form>
            </div>
        `;

        document.getElementById('form-system-settings').addEventListener('submit', async (e) => {
            e.preventDefault();
            const formData = new FormData(e.target);

            try {
                for (let [key, val] of formData.entries()) {
                    await api.updateSetting(key, val);
                }
                alert('System settings updated successfully!');
                await this.loadMasterData();
            } catch (err) {
                alert('Error saving settings: ' + err.message);
            }
        });
    }

    // ==========================================
    // APEXCHARTS HELPER RENDERING METHODS
    // ==========================================
    renderGlobalOeeAndProductionTrendChart(dataPayload = {}, kpi = {}, mode = 'lines_oee') {
        const el = document.getElementById('chart-global-oee-trend');
        if (!el || !window.ApexCharts) return;

        const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');

        // Extract lines (FX-1 to FX-11) and historical trends
        const lines = Array.isArray(dataPayload)
            ? dataPayload
            : (dataPayload?.lines && dataPayload.lines.length > 0 ? dataPayload.lines : []);

        const trends = Array.isArray(dataPayload)
            ? dataPayload
            : (dataPayload?.trends && dataPayload.trends.length > 0 ? dataPayload.trends : []);

        const isLineMode = mode.startsWith('lines_');

        let rawPoints = [];
        if (isLineMode) {
            rawPoints = lines.length > 0 ? lines : [
                { code: 'FX-1', name: 'FX-1', oee: 98.2, availability: 98.1, performance: 111.8, quality: 89.5, target_quantity: 647, actual_quantity: 615, good_quantity: 550, reject_quantity: 13, downtime_mins: 15 },
                { code: 'FX-2', name: 'FX-2', oee: 0, availability: 0, performance: 0, quality: 0, target_quantity: 0, actual_quantity: 0, good_quantity: 0, reject_quantity: 0, downtime_mins: 0 },
                { code: 'FX-3', name: 'FX-3', oee: 0, availability: 0, performance: 0, quality: 0, target_quantity: 0, actual_quantity: 0, good_quantity: 0, reject_quantity: 0, downtime_mins: 0 },
                { code: 'FX-4', name: 'FX-4', oee: 0, availability: 0, performance: 0, quality: 0, target_quantity: 0, actual_quantity: 0, good_quantity: 0, reject_quantity: 0, downtime_mins: 0 },
                { code: 'FX-5', name: 'FX-5', oee: 0, availability: 0, performance: 0, quality: 0, target_quantity: 0, actual_quantity: 0, good_quantity: 0, reject_quantity: 0, downtime_mins: 0 },
                { code: 'FX-6', name: 'FX-6', oee: 0, availability: 0, performance: 0, quality: 0, target_quantity: 0, actual_quantity: 0, good_quantity: 0, reject_quantity: 0, downtime_mins: 0 },
                { code: 'FX-7', name: 'FX-7', oee: 0, availability: 0, performance: 0, quality: 0, target_quantity: 0, actual_quantity: 0, good_quantity: 0, reject_quantity: 0, downtime_mins: 0 },
                { code: 'FX-8', name: 'FX-8', oee: 0, availability: 0, performance: 0, quality: 0, target_quantity: 0, actual_quantity: 0, good_quantity: 0, reject_quantity: 0, downtime_mins: 0 },
                { code: 'FX-9', name: 'FX-9', oee: 0, availability: 0, performance: 0, quality: 0, target_quantity: 0, actual_quantity: 0, good_quantity: 0, reject_quantity: 0, downtime_mins: 0 },
                { code: 'FX-10', name: 'FX-10', oee: 0, availability: 0, performance: 0, quality: 0, target_quantity: 0, actual_quantity: 0, good_quantity: 0, reject_quantity: 0, downtime_mins: 0 },
                { code: 'FX-11', name: 'FX-11', oee: 104.8, availability: 94.9, performance: 117.8, quality: 93.7, target_quantity: 3771, actual_quantity: 3640, good_quantity: 3418, reject_quantity: 32, downtime_mins: 55 },
            ];
        } else {
            rawPoints = trends.length > 0 ? trends : [
                { label: 'Today', date: 'Today', oee: Number(kpi.oee || 0), availability: Number(kpi.availability || 0), performance: Number(kpi.performance || 0), quality: Number(kpi.quality || 0), target_qty: Number(kpi.total_target_qty || 0), actual_qty: Number(kpi.total_actual_qty || 0), good_qty: Number(kpi.total_good_qty || 0), reject_qty: Number(kpi.total_reject_qty || 0), downtime_mins: Number(kpi.total_downtime_minutes || 0) }
            ];
        }

        const categories = rawPoints.map(i => i.code || i.name || i.label || i.date);

        let series = [];
        let colors = [];
        let yaxisConfig = [];
        let annotations = {};

        if (mode === 'lines_prod' || mode === 'prod') {
            // MODE: Output Per Line (Pcs)
            series = [
                { name: 'Target Plan (Pcs)', type: 'line', data: rawPoints.map(i => Number(i.target_quantity ?? i.target_qty ?? 0)) },
                { name: 'Aktual Output (Pcs)', type: 'area', data: rawPoints.map(i => Number(i.actual_quantity ?? i.actual_qty ?? 0)) },
                { name: 'Good Output OK (Pcs)', type: 'area', data: rawPoints.map(i => Number(i.good_quantity ?? i.good_qty ?? 0)) },
                { name: 'Defect / NG (Pcs)', type: 'line', data: rawPoints.map(i => Number(i.reject_quantity ?? i.reject_qty ?? 0)) },
            ];
            colors = ['#94a3b8', '#10b981', '#06b6d4', '#f43f5e'];
            yaxisConfig = [{
                labels: {
                    style: { colors: isLight ? '#475569' : '#94a3b8', fontSize: '11px', fontFamily: 'monospace' },
                    formatter: (v) => Number(v || 0).toLocaleString() + ' pcs'
                },
                title: { text: 'Output Produksi (Pcs)', style: { color: isLight ? '#475569' : '#94a3b8', fontSize: '11px', fontWeight: 600 } }
            }];
        } else if (mode === 'lines_dual' || mode === 'dual') {
            // MODE: Multi-Axis (OEE % on Left Axis, Actual Output Pcs on Right Axis)
            series = [
                { name: 'Overall OEE (%)', type: 'area', data: rawPoints.map(i => Number(i.oee || 0)) },
                { name: 'Aktual Output (Pcs)', type: 'line', data: rawPoints.map(i => Number(i.actual_quantity ?? i.actual_qty ?? 0)) },
                { name: 'Defect NG (Pcs)', type: 'line', data: rawPoints.map(i => Number(i.reject_quantity ?? i.reject_qty ?? 0)) },
            ];
            colors = ['#06b6d4', '#10b981', '#f43f5e'];
            yaxisConfig = [
                {
                    seriesName: 'Overall OEE (%)',
                    max: 120,
                    labels: {
                        style: { colors: '#06b6d4', fontSize: '11px', fontFamily: 'monospace' },
                        formatter: (v) => Number(v || 0).toFixed(0) + '%'
                    },
                    title: { text: 'OEE Rate (%)', style: { color: '#06b6d4', fontSize: '11px', fontWeight: 600 } }
                },
                {
                    seriesName: 'Aktual Output (Pcs)',
                    opposite: true,
                    labels: {
                        style: { colors: '#10b981', fontSize: '11px', fontFamily: 'monospace' },
                        formatter: (v) => Number(v || 0).toLocaleString() + ' pcs'
                    },
                    title: { text: 'Output Produksi (Pcs)', style: { color: '#10b981', fontSize: '11px', fontWeight: 600 } }
                }
            ];
        } else {
            // MODE: OEE Semua Line (FX-1 s/d FX-11) or Date Trend
            series = [
                { name: 'Overall OEE (%)', type: 'area', data: rawPoints.map(i => Number(i.oee || 0)) },
                { name: 'Availability (%)', type: 'line', data: rawPoints.map(i => Number(i.availability || 0)) },
                { name: 'Performance (%)', type: 'line', data: rawPoints.map(i => Number(i.performance || 0)) },
                { name: 'Quality Rate (%)', type: 'line', data: rawPoints.map(i => Number(i.quality || 0)) },
            ];
            colors = ['#06b6d4', '#38bdf8', '#10b981', '#f59e0b'];
            yaxisConfig = [{
                min: 0,
                max: 120,
                labels: {
                    style: { colors: isLight ? '#475569' : '#94a3b8', fontSize: '11px', fontFamily: 'monospace' },
                    formatter: (v) => Number(v || 0).toFixed(0) + '%'
                },
                title: { text: 'OEE & KPI Rate (%)', style: { color: isLight ? '#475569' : '#94a3b8', fontSize: '11px', fontWeight: 600 } }
            }];

            annotations = {
                yaxis: [
                    {
                        y: 85,
                        borderColor: '#10b981',
                        strokeDashArray: 4,
                        label: {
                            borderColor: '#10b981',
                            style: {
                                color: '#ffffff',
                                background: '#059669',
                                fontSize: '10px',
                                fontWeight: 700,
                                padding: { left: 6, right: 6, top: 2, bottom: 2 }
                            },
                            text: 'World Class Std (85%)'
                        }
                    }
                ]
            };
        }

        const isOeeMode = (mode === 'lines_oee' || mode === 'lines_dual' || mode === 'oee' || mode === 'date_trend');
        const discreteMarkers = [];

        if (isOeeMode) {
            rawPoints.forEach((pt, idx) => {
                const oeeVal = Number(pt.oee || 0);
                let color = '#ef4444'; // Red (< 70%)
                if (oeeVal >= 85) {
                    color = '#10b981'; // Green (>= 85% World Class)
                } else if (oeeVal >= 70) {
                    color = '#f59e0b'; // Yellow (70% - 84.9%)
                }
                discreteMarkers.push({
                    seriesIndex: 0,
                    dataPointIndex: idx,
                    fillColor: color,
                    strokeColor: isLight ? '#ffffff' : '#070d1e',
                    size: 7,
                    shape: 'circle'
                });
            });
        }

        const options = {
            chart: {
                type: 'area',
                height: 320,
                background: 'transparent',
                toolbar: {
                    show: true,
                    tools: {
                        download: true,
                        selection: false,
                        zoom: true,
                        zoomin: true,
                        zoomout: true,
                        pan: false,
                        reset: true
                    }
                },
                animations: {
                    enabled: true,
                    easing: 'easeinout',
                    speed: 800,
                    animateGradually: { enabled: true, delay: 100 },
                    dynamicAnimation: { enabled: true, speed: 400 }
                },
                dropShadow: {
                    enabled: !isLight,
                    top: 2,
                    left: 0,
                    blur: 6,
                    color: '#06b6d4',
                    opacity: 0.25
                },
                events: {
                    mounted: (chartContext) => {
                        this.attachOeeSonarRipples(chartContext, rawPoints, isOeeMode);
                    },
                    updated: (chartContext) => {
                        this.attachOeeSonarRipples(chartContext, rawPoints, isOeeMode);
                    }
                }
            },
            theme: { mode: isLight ? 'light' : 'dark' },
            colors: colors,
            series: series,
            annotations: annotations,
            dataLabels: {
                enabled: isOeeMode,
                enabledOnSeries: [0], // ONLY show data point value on Series 0 (Overall OEE), keeping the chart clean and uncluttered
                formatter: function (val, opts) {
                    if (opts.seriesIndex !== 0) return '';
                    const num = Number(val || 0);
                    if (num === 0) return '0%';
                    return num.toFixed(1) + '%';
                },
                offsetY: -9,
                style: {
                    fontSize: '11px',
                    fontFamily: 'JetBrains Mono, monospace',
                    fontWeight: 800,
                    colors: rawPoints.map(pt => {
                        const v = Number(pt.oee || 0);
                        if (v >= 85) return '#10b981'; // Green
                        if (v >= 70) return '#f59e0b'; // Yellow
                        return '#ef4444'; // Red
                    })
                },
                background: {
                    enabled: true,
                    foreColor: isLight ? '#0f172a' : '#ffffff',
                    padding: 4,
                    borderRadius: 5,
                    borderWidth: 1,
                    borderColor: isLight ? '#cbd5e1' : '#1e293b',
                    opacity: isLight ? 0.95 : 0.9,
                    dropShadow: {
                        enabled: true,
                        top: 1,
                        left: 1,
                        blur: 3,
                        color: '#000000',
                        opacity: 0.4
                    }
                }
            },
            stroke: {
                curve: 'smooth',
                width: (mode === 'lines_dual' || mode === 'dual') ? [3.5, 3, 2] : ((mode === 'lines_prod' || mode === 'prod') ? [2.5, 3.5, 2.5, 2] : [3.5, 2.5, 2.5, 2.5]),
                dashArray: (mode === 'lines_prod' || mode === 'prod') ? [5, 0, 0, 0] : [0, 0, 0, 0]
            },
            fill: {
                type: 'gradient',
                gradient: {
                    shade: isLight ? 'light' : 'dark',
                    type: 'vertical',
                    shadeIntensity: 0.5,
                    opacityFrom: isLight ? 0.35 : 0.45,
                    opacityTo: 0.05,
                    stops: [0, 90, 100]
                }
            },
            markers: {
                size: (mode === 'lines_prod' || mode === 'prod') ? 5 : [6.5, 0, 0, 0],
                discrete: discreteMarkers,
                strokeColors: isLight ? '#ffffff' : '#070d1e',
                strokeWidth: 2,
                strokeOpacity: 0.95,
                fillOpacity: 1,
                shape: 'circle',
                hover: {
                    size: 9,
                    strokeWidth: 3
                }
            },
            grid: {
                borderColor: isLight ? '#e2e8f0' : '#1e293b',
                strokeDashArray: 4,
                xaxis: { lines: { show: true } },
                yaxis: { lines: { show: true } },
                padding: { top: 10, right: 20, bottom: 0, left: 15 }
            },
            xaxis: {
                categories: categories,
                labels: {
                    style: {
                        colors: isLight ? '#475569' : '#94a3b8',
                        fontSize: '11px',
                        fontWeight: 700,
                        fontFamily: 'monospace'
                    }
                },
                axisBorder: { color: isLight ? '#cbd5e1' : '#334155' },
                axisTicks: { color: isLight ? '#cbd5e1' : '#334155' }
            },
            yaxis: yaxisConfig,
            legend: {
                position: 'top',
                horizontalAlign: 'right',
                labels: { colors: isLight ? '#1e293b' : '#cbd5e1' },
                fontSize: '11px',
                fontFamily: 'inherit',
                fontWeight: 600,
                markers: { radius: 12 }
            },
            tooltip: {
                shared: true,
                intersect: false,
                theme: isLight ? 'light' : 'dark',
                custom: function({ series, seriesIndex, dataPointIndex, w }) {
                    const point = rawPoints[dataPointIndex] || {};
                    const label = categories[dataPointIndex] || 'Line / Point';
                    const oee = Number(point.oee || 0).toFixed(2);
                    const avail = Number(point.availability || 0).toFixed(2);
                    const perf = Number(point.performance || 0).toFixed(2);
                    const qual = Number(point.quality || 0).toFixed(2);
                    const target = Number(point.target_quantity ?? point.target_qty ?? 0).toLocaleString();
                    const actual = Number(point.actual_quantity ?? point.actual_qty ?? 0).toLocaleString();
                    const good = Number(point.good_quantity ?? point.good_qty ?? 0).toLocaleString();
                    const reject = Number(point.reject_quantity ?? point.reject_qty ?? 0).toLocaleString();
                    const totalAct = Number(point.actual_quantity ?? point.actual_qty ?? 0);
                    const totalRej = Number(point.reject_quantity ?? point.reject_qty ?? 0);
                    const defectRate = totalAct > 0 ? ((totalRej / totalAct) * 100).toFixed(2) : '0.00';
                    const dt = (point.downtime_mins || 0);
                    const productSummary = point.product_summary || (point.products_produced?.length ? point.products_produced.join(', ') : '');

                    return `
                        <div class="p-3.5 bg-slate-950/95 border border-slate-700/80 backdrop-blur-md rounded-xl shadow-2xl text-slate-200 text-xs font-sans min-w-[260px]">
                            <div class="flex items-center justify-between border-b border-slate-800 pb-2 mb-2">
                                <div>
                                    <span class="font-bold text-cyan-400 flex items-center gap-1.5 font-mono text-sm">
                                        <i data-lucide="git-branch" class="w-4 h-4 text-cyan-400"></i> ${label}
                                    </span>
                                    ${productSummary ? `<div class="text-[10px] text-amber-400 font-mono truncate max-w-[180px] mt-0.5">${productSummary}</div>` : ''}
                                </div>
                                <span class="px-2.5 py-0.5 rounded-full text-[10.5px] font-mono font-bold ${Number(oee) >= 85 ? 'bg-emerald-950 text-emerald-400 border border-emerald-800' : (Number(oee) > 0 ? 'bg-amber-950 text-amber-400 border border-amber-800' : 'bg-slate-900 text-slate-400 border border-slate-700')}">
                                    OEE: ${oee}%
                                </span>
                            </div>

                            <div class="grid grid-cols-3 gap-1.5 py-1 text-[10.5px] font-mono border-b border-slate-800/80 mb-2">
                                <div class="text-sky-400">A: <strong>${avail}%</strong></div>
                                <div class="text-emerald-400">P: <strong>${perf}%</strong></div>
                                <div class="text-amber-400">Q: <strong>${qual}%</strong></div>
                            </div>

                            <div class="space-y-1.5 text-[11px]">
                                <div class="flex items-center justify-between">
                                    <span class="text-slate-400">Target Plan:</span>
                                    <span class="font-mono font-bold text-slate-200">${target} pcs</span>
                                </div>
                                <div class="flex items-center justify-between">
                                    <span class="text-slate-400">Aktual Output:</span>
                                    <span class="font-mono font-bold text-emerald-400">${actual} pcs</span>
                                </div>
                                <div class="flex items-center justify-between">
                                    <span class="text-slate-400">Good Output (OK):</span>
                                    <span class="font-mono font-bold text-cyan-300">${good} pcs</span>
                                </div>
                                <div class="flex items-center justify-between">
                                    <span class="text-slate-400">Defect NG:</span>
                                    <span class="font-mono font-bold text-rose-400">${reject} pcs (${defectRate}%)</span>
                                </div>
                                <div class="flex items-center justify-between pt-1 border-t border-slate-800/60 text-[10.5px]">
                                    <span class="text-slate-400">Total Downtime:</span>
                                    <span class="font-mono font-bold text-amber-400">${dt} min</span>
                                </div>
                            </div>
                        </div>
                    `;
                }
            }
        };

        if (this.charts['globalOeeTrend']) this.charts['globalOeeTrend'].destroy();
        this.charts['globalOeeTrend'] = new window.ApexCharts(el, options);
        this.charts['globalOeeTrend'].render();
    }

    attachOeeSonarRipples(chartContext, rawPoints, isOeeMode) {
        if (!isOeeMode || !chartContext?.el) return;
        setTimeout(() => {
            try {
                const svgEl = chartContext.el.querySelector('svg.apexcharts-svg');
                if (!svgEl) return;

                // Clear previous sonar rings
                svgEl.querySelectorAll('.oee-sonar-ring, .oee-sonar-ring-2').forEach(el => el.remove());

                // Find Series 0 markers
                const markers = svgEl.querySelectorAll('.apexcharts-series[seriesName*="OEE"] .apexcharts-marker, .apexcharts-series-markers-0 .apexcharts-marker, .apexcharts-series[data\\:realIndex="0"] .apexcharts-marker');

                markers.forEach((marker, idx) => {
                    const pt = rawPoints[idx];
                    if (!pt) return;
                    const oeeVal = Number(pt.oee || 0);
                    let color = '#ef4444'; // Red (< 70%)
                    if (oeeVal >= 85) color = '#10b981'; // Green (>= 85%)
                    else if (oeeVal >= 70) color = '#f59e0b'; // Yellow (70% - 84.9%)

                    const cx = marker.getAttribute('cx') || marker.getAttribute('j');
                    const cy = marker.getAttribute('cy') || marker.getAttribute('val');
                    if (!cx || !cy) return;

                    const parent = marker.parentElement;
                    if (!parent) return;

                    // Sonar Ripple Wave 1
                    const ring1 = document.createElementNS('http://www.w3.org/2000/svg', 'circle');
                    ring1.setAttribute('cx', cx);
                    ring1.setAttribute('cy', cy);
                    ring1.setAttribute('r', '7');
                    ring1.setAttribute('fill', 'none');
                    ring1.setAttribute('stroke', color);
                    ring1.setAttribute('class', 'oee-sonar-ring');
                    parent.insertBefore(ring1, marker);

                    // Sonar Ripple Wave 2
                    const ring2 = document.createElementNS('http://www.w3.org/2000/svg', 'circle');
                    ring2.setAttribute('cx', cx);
                    ring2.setAttribute('cy', cy);
                    ring2.setAttribute('r', '7');
                    ring2.setAttribute('fill', 'none');
                    ring2.setAttribute('stroke', color);
                    ring2.setAttribute('class', 'oee-sonar-ring-2');
                    parent.insertBefore(ring2, marker);
                });
            } catch (err) {
                console.warn('Sonar ripple attachment skipped:', err);
            }
        }, 200);
    }

    renderOeeTrendChart(data) {
        return this.renderGlobalOeeAndProductionTrendChart(data, this.kpi || {}, 'oee');
    }

    renderSixLossesChart(data, targetContainerId = 'chart-six-losses', targetBadgeId = 'six-losses-total-badge') {
        const el = document.getElementById(targetContainerId);
        const badgeEl = document.getElementById(targetBadgeId);
        if (!el) return;

        const isLight = this.theme === 'light';
        const totalMins = (data || []).reduce((acc, i) => acc + (Number(i.minutes) || 0), 0);
        const totalHours = (totalMins / 60).toFixed(1);

        const isJa = this.currentLang === 'ja';
        const jaLossLabels = {
            'Equipment Failure': '設備故障 (Equipment Failure)',
            'Setup & Adjustment': '段取り・調整 (Setup & Adj)',
            'Idling & Minor Stops': 'チョコ停・空転 (Minor Stops)',
            'Reduced Speed': '速度低下 (Speed Loss)',
            'Process Defects': '工程不良 (Process Defects)',
            'Reduced Yield (Scrap)': '立上り・歩留ロス (Yield Loss)'
        };

        const jaCatLabels = {
            'Availability': '時間稼働率',
            'Performance': '性能稼働率',
            'Quality': '良品率'
        };

        if (badgeEl) {
            badgeEl.innerHTML = totalMins > 0 
                ? `<span class="px-2.5 py-1 rounded-lg ${isLight ? 'bg-rose-100 text-rose-700 border border-rose-300' : 'bg-rose-950/60 border border-rose-800/60 text-rose-400'} font-mono font-bold text-xs flex items-center gap-1.5 shadow-sm">
                    <span class="w-1.5 h-1.5 rounded-full bg-rose-500 animate-pulse"></span>
                    ${totalHours}h ${isJa ? '総損失時間' : 'Total Loss'}
                   </span>`
                : `<span class="px-2.5 py-1 rounded-lg ${isLight ? 'bg-emerald-100 text-emerald-700 border border-emerald-300' : 'bg-emerald-950/60 border border-emerald-800/60 text-emerald-400'} font-mono font-bold text-xs flex items-center gap-1.5 shadow-sm">
                    <span class="w-1.5 h-1.5 rounded-full bg-emerald-500"></span>
                    ${isJa ? 'ロスなし (0時間)' : 'Zero Losses'}
                   </span>`;
        }

        const configMap = {
            'Equipment Failure': { category: 'Availability', color: '#f43f5e', gradient: 'from-rose-600 to-rose-400', tagClass: isLight ? 'text-rose-700 border-rose-300 bg-rose-50' : 'text-rose-400 border-rose-800/40 bg-rose-950/30' },
            'Setup & Adjustment': { category: 'Availability', color: '#f97316', gradient: 'from-orange-600 to-orange-400', tagClass: isLight ? 'text-orange-700 border-orange-300 bg-orange-50' : 'text-orange-400 border-orange-800/40 bg-orange-950/30' },
            'Idling & Minor Stops': { category: 'Performance', color: '#eab308', gradient: 'from-amber-600 to-amber-400', tagClass: isLight ? 'text-amber-700 border-amber-300 bg-amber-50' : 'text-amber-400 border-amber-800/40 bg-amber-950/30' },
            'Reduced Speed': { category: 'Performance', color: '#0284c7', gradient: 'from-cyan-600 to-cyan-400', tagClass: isLight ? 'text-sky-700 border-sky-300 bg-sky-50' : 'text-cyan-400 border-cyan-800/40 bg-cyan-950/30' },
            'Process Defects': { category: 'Quality', color: '#a855f7', gradient: 'from-purple-600 to-purple-400', tagClass: isLight ? 'text-purple-700 border-purple-300 bg-purple-50' : 'text-purple-400 border-purple-800/40 bg-purple-950/30' },
            'Reduced Yield (Scrap)': { category: 'Quality', color: '#ec4899', gradient: 'from-pink-600 to-pink-400', tagClass: isLight ? 'text-pink-700 border-pink-300 bg-pink-50' : 'text-pink-400 border-pink-800/40 bg-pink-950/30' }
        };

        // Calculate Pillar Totals
        let availMins = 0;
        let perfMins = 0;
        let qualMins = 0;

        (data || []).forEach(item => {
            const mins = Number(item.minutes) || 0;
            const cat = item.category || configMap[item.loss]?.category || '';
            if (cat.toLowerCase().includes('avail') || item.loss.includes('Equipment') || item.loss.includes('Setup')) {
                availMins += mins;
            } else if (cat.toLowerCase().includes('perf') || item.loss.includes('Idling') || item.loss.includes('Speed')) {
                perfMins += mins;
            } else {
                qualMins += mins;
            }
        });

        el.innerHTML = `
            <div class="space-y-3 my-auto">
                ${(data || []).map(item => {
                    const cfg = configMap[item.loss] || { category: 'Loss', color: '#3b82f6', gradient: 'from-blue-600 to-blue-400', tagClass: isLight ? 'text-blue-700' : 'text-blue-400' };
                    const mins = Number(item.minutes) || 0;
                    const pctOfTotal = totalMins > 0 ? (mins / totalMins) * 100 : 0;
                    const durationText = mins >= 60 
                        ? `${(mins / 60).toFixed(1)} hrs <span class="${isLight ? 'text-slate-500' : 'text-slate-400'} font-normal text-[10px]">(${mins}m)</span>` 
                        : (mins > 0 ? `${mins} mins` : `<span class="${isLight ? 'text-slate-400' : 'text-slate-500'} font-normal text-xs">0 m</span>`);
                    const displayName = isJa ? (jaLossLabels[item.loss] || item.loss) : item.loss;
                    const displayCat = isJa ? (jaCatLabels[cfg.category] || cfg.category) : cfg.category;

                    return `
                        <div class="space-y-1.5">
                            <div class="flex items-center justify-between text-xs">
                                <div class="flex items-center gap-2 truncate pr-2">
                                    <span class="w-2 h-2 rounded-full flex-shrink-0" style="background-color: ${cfg.color}; box-shadow: 0 0 6px ${cfg.color}40;"></span>
                                    <span class="font-bold ${mins > 0 ? (isLight ? 'text-slate-900' : 'text-slate-200') : (isLight ? 'text-slate-400' : 'text-slate-500')} truncate" title="${item.loss}">${displayName}</span>
                                    <span class="text-[9px] px-1.5 py-0.2 rounded font-semibold border ${cfg.tagClass}">${displayCat}</span>
                                </div>
                                <div class="flex items-center gap-2 flex-shrink-0 font-mono">
                                    <span class="text-xs font-bold ${mins > 0 ? (isLight ? 'text-slate-900' : 'text-slate-100') : (isLight ? 'text-slate-400' : 'text-slate-500')}">${durationText}</span>
                                    <span class="text-[10px] font-bold px-1.5 py-0.5 rounded ${mins > 0 ? (isLight ? 'bg-slate-100 text-sky-700 border border-slate-300' : 'bg-slate-950 text-cyan-400 border border-slate-800') : 'text-slate-400 bg-transparent'} min-w-[44px] text-right">${pctOfTotal.toFixed(1)}%</span>
                                </div>
                            </div>
                            <div class="w-full h-2 rounded-full ${isLight ? 'bg-slate-200' : 'bg-slate-950'} border ${isLight ? 'border-slate-300' : 'border-slate-800/80'} overflow-hidden">
                                <div class="h-full rounded-full bg-gradient-to-r ${cfg.gradient} transition-all duration-1000 ease-out" style="width: ${pctOfTotal}%;"></div>
                            </div>
                        </div>
                    `;
                }).join('')}
            </div>

            <!-- PILLAR SUMMARY FOOTER -->
            <div class="grid grid-cols-3 gap-2.5 pt-3 mt-3 border-t ${isLight ? 'border-slate-200' : 'border-slate-800'} font-mono text-center">
                <div class="${isLight ? 'bg-slate-50 border border-slate-200' : 'bg-slate-950/70 border border-slate-800/80'} rounded-lg p-2">
                    <div class="text-[10px] ${isLight ? 'text-slate-600' : 'text-slate-400'} uppercase font-sans font-bold flex items-center justify-center gap-1">
                        <span class="w-1.5 h-1.5 rounded-full bg-rose-500"></span> ${isJa ? '時間稼働率' : 'Availability'}
                    </div>
                    <div class="text-xs font-bold ${isLight ? 'text-rose-600' : 'text-rose-400'} mt-0.5">${(availMins / 60).toFixed(1)}h <span class="text-[10px] font-normal ${isLight ? 'text-slate-500' : 'text-slate-400'}">(${availMins}m)</span></div>
                </div>
                <div class="${isLight ? 'bg-slate-50 border border-slate-200' : 'bg-slate-950/70 border border-slate-800/80'} rounded-lg p-2">
                    <div class="text-[10px] ${isLight ? 'text-slate-600' : 'text-slate-400'} uppercase font-sans font-bold flex items-center justify-center gap-1">
                        <span class="w-1.5 h-1.5 rounded-full bg-amber-500"></span> ${isJa ? '性能稼働率' : 'Performance'}
                    </div>
                    <div class="text-xs font-bold ${isLight ? 'text-amber-600' : 'text-amber-400'} mt-0.5">${(perfMins / 60).toFixed(1)}h <span class="text-[10px] font-normal ${isLight ? 'text-slate-500' : 'text-slate-400'}">(${perfMins}m)</span></div>
                </div>
                <div class="${isLight ? 'bg-slate-50 border border-slate-200' : 'bg-slate-950/70 border border-slate-800/80'} rounded-lg p-2">
                    <div class="text-[10px] ${isLight ? 'text-slate-600' : 'text-slate-400'} uppercase font-sans font-bold flex items-center justify-center gap-1">
                        <span class="w-1.5 h-1.5 rounded-full bg-purple-500"></span> ${isJa ? '良品率' : 'Quality'}
                    </div>
                    <div class="text-xs font-bold ${isLight ? 'text-purple-600' : 'text-purple-400'} mt-0.5">${(qualMins / 60).toFixed(1)}h <span class="text-[10px] font-normal ${isLight ? 'text-slate-500' : 'text-slate-400'}">(${qualMins}m)</span></div>
                </div>
            </div>
        `;
    }

    renderParetoDowntimeChart(data) {
        const el = document.getElementById('chart-pareto-downtime');
        if (!el || !window.ApexCharts) return;

        const isLight = this.theme === 'light';
        const options = {
            chart: { type: 'line', height: '100%', background: 'transparent', toolbar: { show: false } },
            theme: { mode: isLight ? 'light' : 'dark' },
            colors: ['#f59e0b', '#0284c7'],
            stroke: { width: [0, 3], curve: 'smooth' },
            plotOptions: { bar: { columnWidth: '40%', borderRadius: 4 } },
            series: [
                { name: 'Duration (mins)', type: 'column', data: data.map(i => i.duration_minutes) },
                { name: 'Cumulative %', type: 'line', data: data.map(i => i.cumulative_percentage) },
            ],
            xaxis: { 
                categories: data.map(i => i.reason), 
                labels: { style: { colors: isLight ? '#475569' : '#64748b', fontSize: '10px' } },
                axisBorder: { color: isLight ? '#cbd5e1' : '#334155' }
            },
            yaxis: [
                { title: { text: 'Minutes' }, labels: { style: { colors: isLight ? '#475569' : '#64748b' } } },
                { opposite: true, max: 100, title: { text: 'Cumulative %' }, labels: { style: { colors: isLight ? '#475569' : '#64748b' }, formatter: (v) => v + '%' } },
            ],
            grid: { borderColor: isLight ? '#e2e8f0' : '#1e293b', strokeDashArray: 4 },
            tooltip: { theme: isLight ? 'light' : 'dark' }
        };

        if (this.charts['paretoDowntime']) this.charts['paretoDowntime'].destroy();
        this.charts['paretoDowntime'] = new window.ApexCharts(el, options);
        this.charts['paretoDowntime'].render();
    }

    renderParetoDefectChart(data, summary) {
        const el = document.getElementById('chart-pareto-defect');
        if (!el || !window.ApexCharts) return;

        const isLight = this.theme === 'light';

        if (!data || data.length === 0) {
            el.innerHTML = `
                <div class="h-64 flex flex-col items-center justify-center text-slate-500 py-12">
                    <i data-lucide="check-circle" class="w-12 h-12 text-emerald-400 mb-2 opacity-60"></i>
                    <p class="text-sm font-semibold ${isLight ? 'text-slate-700' : 'text-slate-300'}">Tidak ada defect / reject tercatat</p>
                    <p class="text-xs ${isLight ? 'text-slate-500' : 'text-slate-500'}">Semua produk lolos inspeksi quality dalam periode ini.</p>
                </div>
            `;
            if (window.lucide) window.lucide.createIcons();
            return;
        }

        const options = {
            chart: {
                type: 'line',
                height: 340,
                background: 'transparent',
                toolbar: {
                    show: true,
                    tools: {
                        download: true,
                        selection: false,
                        zoom: false,
                        zoomin: false,
                        zoomout: false,
                        pan: false,
                        reset: false
                    }
                },
                animations: {
                    enabled: true,
                    easing: 'easeinout',
                    speed: 700,
                    dynamicAnimation: { enabled: true, speed: 350 }
                }
            },
            theme: { mode: isLight ? 'light' : 'dark' },
            colors: ['#e11d48', '#0284c7'],
            stroke: {
                width: [0, 3.5],
                curve: 'smooth'
            },
            plotOptions: {
                bar: {
                    columnWidth: '45%',
                    borderRadius: 6,
                    borderRadiusApplication: 'end',
                    dataLabels: {
                        position: 'top'
                    }
                }
            },
            dataLabels: {
                enabled: true,
                enabledOnSeries: [0, 1],
                formatter: function (val, opts) {
                    if (opts.seriesIndex === 0) return val > 0 ? val + ' pcs' : '';
                    return val + '%';
                },
                offsetY: -6,
                style: {
                    fontSize: '10px',
                    fontFamily: 'monospace',
                    fontWeight: 700,
                    colors: isLight ? ['#991b1b', '#0369a1'] : ['#fca5a5', '#67e8f9']
                },
                background: {
                    enabled: true,
                    foreColor: isLight ? '#ffffff' : '#0f172a',
                    padding: 3,
                    borderRadius: 4,
                    borderWidth: 1,
                    borderColor: isLight ? '#cbd5e1' : '#334155',
                    opacity: 0.95
                }
            },
            series: [
                {
                    name: 'Jumlah Reject (Pcs)',
                    type: 'column',
                    data: data.map(i => i.reject_quantity)
                },
                {
                    name: 'Persentase Kumulatif (%)',
                    type: 'line',
                    data: data.map(i => i.cumulative_percentage)
                }
            ],
            xaxis: {
                categories: data.map(i => i.reason),
                labels: {
                    style: {
                        colors: isLight ? '#334155' : '#94a3b8',
                        fontSize: '11px',
                        fontWeight: 500
                    },
                    rotate: -30,
                    rotateAlways: data.length > 4,
                    trim: true,
                    maxHeight: 80
                },
                axisBorder: { color: isLight ? '#cbd5e1' : '#334155' },
                axisTicks: { color: isLight ? '#cbd5e1' : '#334155' }
            },
            yaxis: [
                {
                    title: {
                        text: 'Jumlah Reject (Pcs)',
                        style: { color: '#e11d48', fontSize: '11px', fontWeight: 600 }
                    },
                    labels: {
                        style: { colors: isLight ? '#334155' : '#94a3b8', fontSize: '10px' },
                        formatter: (v) => Math.round(v)
                    },
                    min: 0,
                    forceNiceScale: true
                },
                {
                    opposite: true,
                    max: 100,
                    min: 0,
                    title: {
                        text: 'Persentase Kumulatif (%)',
                        style: { color: '#0284c7', fontSize: '11px', fontWeight: 600 }
                    },
                    labels: {
                        style: { colors: isLight ? '#334155' : '#94a3b8', fontSize: '10px' },
                        formatter: (v) => v + '%'
                    }
                }
            ],
            annotations: {
                yaxis: [
                    {
                        y: 80,
                        y2: null,
                        strokeDashArray: 5,
                        borderColor: '#10b981',
                        borderWidth: 2,
                        opacity: 0.9,
                        yAxisIndex: 1,
                        label: {
                            borderColor: '#10b981',
                            style: {
                                color: '#047857',
                                background: '#d1fae5',
                                fontSize: '10px',
                                fontWeight: 700
                            },
                            text: '80% Pareto Cutoff (Vital Few)'
                        }
                    }
                ]
            },
            grid: {
                borderColor: isLight ? '#e2e8f0' : '#1e293b',
                strokeDashArray: 4,
                xaxis: { lines: { show: false } },
                yaxis: { lines: { show: true } }
            },
            tooltip: {
                theme: isLight ? 'light' : 'dark',
                shared: true,
                intersect: false,
                y: {
                    formatter: function (y, { seriesIndex, dataPointIndex }) {
                        if (typeof y !== "undefined") {
                            if (seriesIndex === 0) {
                                const item = data[dataPointIndex];
                                return `${y} pcs (${item ? item.percentage : 0}%)`;
                            }
                            return `${y}%`;
                        }
                        return y;
                    }
                }
            },
            legend: {
                position: 'top',
                horizontalAlign: 'right',
                labels: { colors: isLight ? '#1e293b' : '#cbd5e1' },
                fontSize: '11px'
            }
        };

        if (this.charts['paretoDefect']) this.charts['paretoDefect'].destroy();
        this.charts['paretoDefect'] = new window.ApexCharts(el, options);
        this.charts['paretoDefect'].render();
    }

    renderDefectComponentChart(compDist) {
        const el = document.getElementById('chart-defect-component');
        if (!el || !window.ApexCharts) return;

        const isLight = this.theme === 'light';
        const validItems = (compDist || []).filter(c => c.quantity > 0);
        if (validItems.length === 0) {
            el.innerHTML = `<div class="h-44 flex items-center justify-center ${isLight ? 'text-slate-400' : 'text-slate-500'} text-xs py-8">Belum ada distribusi komponen</div>`;
            return;
        }

        const options = {
            chart: {
                type: 'donut',
                height: 220,
                background: 'transparent'
            },
            theme: { mode: isLight ? 'light' : 'dark' },
            colors: ['#e11d48', '#f59e0b', '#0284c7', '#8b5cf6'],
            series: validItems.map(c => c.quantity),
            labels: validItems.map(c => c.name),
            plotOptions: {
                pie: {
                    donut: {
                        size: '72%',
                        labels: {
                            show: true,
                            total: {
                                show: true,
                                label: 'Total NG',
                                color: isLight ? '#475569' : '#94a3b8',
                                fontSize: '11px',
                                formatter: () => validItems.reduce((acc, c) => acc + c.quantity, 0) + ' pcs'
                            },
                            value: {
                                color: isLight ? '#0f172a' : '#f1f5f9',
                                fontSize: '18px',
                                fontWeight: 700,
                                formatter: (v) => v + ' pcs'
                            }
                        }
                    }
                }
            },
            dataLabels: { enabled: false },
            legend: {
                position: 'bottom',
                horizontalAlign: 'center',
                labels: { colors: isLight ? '#334155' : '#94a3b8' },
                fontSize: '11px'
            },
            stroke: { show: true, colors: [isLight ? '#ffffff' : '#0f172a'], width: 2 }
        };

        if (this.charts['defectComponent']) this.charts['defectComponent'].destroy();
        this.charts['defectComponent'] = new window.ApexCharts(el, options);
        this.charts['defectComponent'].render();
    }

    exportQualityPareto(topDefects, topSections, summary) {
        if (!XLSX) {
            alert('SheetJS Excel library belum siap.');
            return;
        }

        const wb = XLSX.utils.book_new();

        // Sheet 1: Top 10 Defect Reasons
        const defectRows = (topDefects || []).map(d => ({
            'Rank': d.rank,
            'Penyebab Defect / Remark': d.reason,
            'Bagian NG (Section)': d.section,
            'Tipe Komponen': d.component_type,
            'Jumlah Reject (Pcs)': d.reject_quantity,
            'Kontribusi (%)': d.percentage,
            'Kumulatif (%)': d.cumulative_percentage,
            'Klasifikasi Pareto': d.is_vital_few ? 'VITAL FEW (80%)' : 'USEFUL MANY (20%)',
            'Rekomendasi Tindakan Korektif': d.action_recommendation
        }));
        const wsDefects = XLSX.utils.json_to_sheet(defectRows);
        XLSX.utils.book_append_sheet(wb, wsDefects, 'Top Defect Reasons');

        // Sheet 2: Top NG Sections
        const sectionRows = (topSections || []).map(s => ({
            'Rank': s.rank,
            'Nama Bagian NG': s.section,
            'Jumlah Reject (Pcs)': s.reject_quantity,
            'Persentase (%)': s.percentage,
            'Kumulatif (%)': s.cumulative_percentage
        }));
        const wsSections = XLSX.utils.json_to_sheet(sectionRows);
        XLSX.utils.book_append_sheet(wb, wsSections, 'Bagian NG Pareto');

        // Sheet 3: Summary KPI
        const summaryRows = [
            { 'Indikator': 'Total Reject Pcs', 'Nilai': summary.total_reject_pcs || 0 },
            { 'Indikator': 'Total Output Produksi', 'Nilai': summary.total_output_pcs || 0 },
            { 'Indikator': 'Defect Rate (PPM)', 'Nilai': summary.ppm_defect_rate || 0 },
            { 'Indikator': 'Quality Yield Rate (%)', 'Nilai': (summary.quality_rate || 100) + '%' },
            { 'Indikator': 'Top #1 Defect Name', 'Nilai': summary.top_defect_name || '-' },
            { 'Indikator': 'Top #1 Defect Volume', 'Nilai': (summary.top_defect_qty || 0) + ' pcs' },
            { 'Indikator': 'Top #1 Critical Section', 'Nilai': summary.top_section_name || '-' },
            { 'Indikator': 'Vital Few Defect Types', 'Nilai': summary.vital_few_count || 0 },
            { 'Indikator': 'Vital Few Volume Percentage', 'Nilai': (summary.vital_few_percentage || 0) + '%' },
        ];
        const wsSummary = XLSX.utils.json_to_sheet(summaryRows);
        XLSX.utils.book_append_sheet(wb, wsSummary, 'Ringkasan Eksekutif');

        const fileName = `Analisis_Pareto_Defect_${new Date().toISOString().slice(0, 10)}.xlsx`;
        XLSX.writeFile(wb, fileName);
        this.showNotification('Excel Export Berhasil', `File ${fileName} berhasil diunduh.`, 'download');
    }

    // ==========================================
    // UI HELPERS & BADGES
    // ==========================================
    kpiCard(title, value, subtext, icon, color, gradient) {
        return `
            <div class="bg-gradient-to-br ${gradient} bg-slate-900 border rounded-xl p-5 shadow-xl relative overflow-hidden">
                <div class="flex items-center justify-between mb-3">
                    <span class="text-xs font-semibold text-slate-400 uppercase tracking-wider">${title}</span>
                    <div class="w-8 h-8 rounded-lg bg-slate-950/60 border border-slate-800 ${color} flex items-center justify-center">
                        <i data-lucide="${icon}" class="w-4 h-4"></i>
                    </div>
                </div>
                <div class="text-3xl font-extrabold text-slate-100 font-mono tracking-tight">${value}</div>
                <div class="text-[11px] text-slate-400 mt-2 flex items-center gap-1">
                    <span>${subtext}</span>
                </div>
            </div>
        `;
    }

    getStatusBadge(status) {
        switch (status) {
            case 'EXCELLENT': return 'bg-emerald-500/15 text-emerald-600 dark:text-emerald-400 border-emerald-500/30';
            case 'GOOD': return 'bg-cyan-500/15 text-cyan-600 dark:text-cyan-400 border-cyan-500/30';
            case 'WARNING': return 'bg-amber-500/15 text-amber-600 dark:text-amber-400 border-amber-500/30';
            default: return 'bg-rose-500/15 text-rose-600 dark:text-rose-400 border-rose-500/30';
        }
    }

    getMachineStatusBadge(status) {
        const s = (status || '').toUpperCase();
        switch (s) {
            case 'RUNNING':
                return 'bg-emerald-500/15 text-emerald-600 dark:text-emerald-400 border-emerald-500/30 status-pulse-running';
            case 'BREAKDOWN':
            case 'STOP':
            case 'STOPPED':
                return 'bg-rose-500/15 text-rose-600 dark:text-rose-400 border-rose-500/40 status-pulse-breakdown';
            case 'IDLE':
                return 'bg-amber-500/15 text-amber-600 dark:text-amber-400 border-amber-500/30 status-pulse-warning';
            case 'MAINTENANCE':
                return 'bg-indigo-500/15 text-indigo-600 dark:text-indigo-400 border-indigo-500/30';
            default:
                return 'bg-slate-500/15 text-slate-500 dark:text-slate-400 border-slate-600/30';
        }
    }

    getStatusIcon(status) {
        const s = (status || '').toUpperCase();
        switch (s) {
            case 'RUNNING':
                return '<span class="inline-block w-1.5 h-1.5 rounded-full bg-emerald-500"></span>';
            case 'BREAKDOWN':
            case 'STOP':
            case 'STOPPED':
                return '<span class="inline-block text-[9px] font-mono font-bold leading-none">■</span>';
            case 'IDLE':
                return '<span class="inline-block text-[9px] font-mono font-bold leading-none">▲</span>';
            case 'MAINTENANCE':
                return '<span class="inline-block text-[9px] font-mono font-bold leading-none">⧗</span>';
            default:
                return '<span class="inline-block w-1.5 h-1.5 rounded-full bg-slate-400"></span>';
        }
    }

    showAlertModal({ title, subtitle, errors = [], type = 'warning', buttonText = 'Perbaiki Isian Data' }) {
        return new Promise((resolve) => {
            let container = document.getElementById('custom-dialog-container');
            if (!container) {
                container = document.createElement('div');
                container.id = 'custom-dialog-container';
                document.body.appendChild(container);
            }

            const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');
            const isJa = this.currentLang === 'ja';

            let displayTitle = isJa ? (i18n.translateText(title) || (title === 'Data Tidak Dapat Disimpan' ? 'データを保存できません' : title)) : (title || 'Data Tidak Dapat Disimpan');
            let displaySubtitle = isJa ? (i18n.translateText(subtitle) || (subtitle === 'Harap lengkapi isian data yang masih kosong berikut:' ? '以下の必須項目を入力または確認してください:' : subtitle)) : (subtitle || 'Harap lengkapi isian data yang masih kosong berikut:');
            let displayButtonText = isJa ? (i18n.translateText(buttonText) || (buttonText === 'Perbaiki Isian Data' ? '入力内容を確認・修正する' : buttonText)) : buttonText;
            let displayErrors = errors;
            if (isJa && Array.isArray(errors)) {
                displayErrors = errors.map(err => i18n.translateText(err));
            }

            container.innerHTML = `
                <div class="fixed inset-0 bg-slate-950/80 backdrop-blur-md flex items-center justify-center p-4 z-[99999] animate-fadeIn">
                    <div class="${isLight ? 'bg-white border-rose-200 text-slate-900 shadow-2xl' : 'bg-slate-900 border-rose-900/60 text-slate-100 shadow-2xl'} border max-w-md w-full rounded-2xl p-6 relative overflow-hidden transform transition-all">
                        <!-- TOP GLOW ACCENT -->
                        <div class="absolute top-0 left-0 right-0 h-1.5 bg-gradient-to-r from-rose-500 via-amber-500 to-rose-500"></div>

                        <div class="flex items-start gap-3.5 mb-4">
                            <div class="w-11 h-11 rounded-2xl ${isLight ? 'bg-rose-100 border-rose-200 text-rose-600' : 'bg-rose-950/80 border-rose-800 text-rose-400'} border flex items-center justify-center flex-shrink-0 shadow-lg">
                                <i data-lucide="${type === 'danger' || type === 'error' ? 'shield-alert' : 'alert-triangle'}" class="w-6 h-6 animate-bounce"></i>
                            </div>
                            <div class="flex-1">
                                <h3 class="font-black text-base ${isLight ? 'text-slate-900' : 'text-white'} leading-snug">
                                    ${displayTitle}
                                </h3>
                                <p class="text-xs ${isLight ? 'text-slate-600' : 'text-slate-400'} mt-1">
                                    ${displaySubtitle}
                                </p>
                            </div>
                        </div>

                        ${displayErrors && displayErrors.length > 0 ? `
                            <div class="space-y-2 mb-5 max-h-56 overflow-y-auto pr-1">
                                ${displayErrors.map((err, idx) => `
                                    <div class="flex items-start gap-2.5 p-2.5 rounded-xl ${isLight ? 'bg-rose-50/90 border-rose-200 text-rose-950' : 'bg-rose-950/40 border-rose-900/40 text-rose-200'} border text-xs font-sans">
                                        <span class="w-4 h-4 rounded-full bg-rose-500 text-white flex items-center justify-center text-[10px] font-mono font-bold flex-shrink-0 mt-0.5">${idx + 1}</span>
                                        <div class="flex-1 leading-relaxed">${err}</div>
                                    </div>
                                `).join('')}
                            </div>
                        ` : ''}

                        <div class="flex items-center justify-end gap-2 pt-3 border-t ${isLight ? 'border-slate-200' : 'border-slate-800'}">
                            <button id="btn-custom-alert-ok" class="w-full py-2.5 rounded-xl bg-gradient-to-r from-rose-600 to-amber-600 hover:from-rose-500 hover:to-amber-500 text-white font-bold text-xs shadow-lg shadow-rose-600/30 cursor-pointer transition-all flex items-center justify-center gap-2">
                                <i data-lucide="check" class="w-4 h-4"></i>
                                <span>${displayButtonText}</span>
                            </button>
                        </div>
                    </div>
                </div>
            `;

            if (window.lucide) window.lucide.createIcons();

            const btn = document.getElementById('btn-custom-alert-ok');
            if (btn) {
                btn.addEventListener('click', () => {
                    container.innerHTML = '';
                    resolve(true);
                });
            }
        });
    }

    showConfirmModal({ title, subtitle, confirmText = 'Ya, Lanjutkan', cancelText = 'Batal', type = 'warning' }) {
        return new Promise((resolve) => {
            let container = document.getElementById('custom-dialog-container');
            if (!container) {
                container = document.createElement('div');
                container.id = 'custom-dialog-container';
                document.body.appendChild(container);
            }

            const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');
            const isJa = this.currentLang === 'ja';

            let displayTitle = isJa ? (i18n.translateText(title) || (title === 'Konfirmasi Tindakan' ? '実行確認' : title)) : (title || 'Konfirmasi Tindakan');
            let displaySubtitle = isJa ? (i18n.translateText(subtitle) || (subtitle === 'Apakah Anda yakin ingin melanjutkan tindakan ini?' ? 'この処理を実行してもよろしいですか？' : subtitle)) : (subtitle || 'Apakah Anda yakin ingin melanjutkan tindakan ini?');
            let displayConfirmText = isJa ? (i18n.translateText(confirmText) || (confirmText === 'Ya, Lanjutkan' ? 'はい、実行します' : confirmText)) : confirmText;
            let displayCancelText = isJa ? (i18n.translateText(cancelText) || (cancelText === 'Batal' ? 'キャンセル' : cancelText)) : cancelText;

            container.innerHTML = `
                <div class="fixed inset-0 bg-slate-950/80 backdrop-blur-md flex items-center justify-center p-4 z-[99999] animate-fadeIn">
                    <div class="${isLight ? 'bg-white border-slate-200 text-slate-900 shadow-2xl' : 'bg-slate-900 border-slate-800 text-slate-100 shadow-2xl'} border max-w-md w-full rounded-2xl p-6 relative overflow-hidden">
                        <div class="flex items-start gap-3.5 mb-4">
                            <div class="w-11 h-11 rounded-2xl ${type === 'danger' ? (isLight ? 'bg-rose-100 text-rose-600 border-rose-200' : 'bg-rose-950 text-rose-400 border-rose-800') : (isLight ? 'bg-amber-100 text-amber-600 border-amber-200' : 'bg-amber-950 text-amber-400 border-amber-800')} border flex items-center justify-center flex-shrink-0 shadow-lg">
                                <i data-lucide="${type === 'danger' ? 'trash-2' : 'help-circle'}" class="w-6 h-6"></i>
                            </div>
                            <div class="flex-1">
                                <h3 class="font-black text-base ${isLight ? 'text-slate-900' : 'text-white'} leading-snug">
                                    ${displayTitle}
                                </h3>
                                <p class="text-xs ${isLight ? 'text-slate-600' : 'text-slate-400'} mt-1 leading-relaxed">
                                    ${displaySubtitle}
                                </p>
                            </div>
                        </div>

                        <div class="flex items-center justify-end gap-2.5 pt-3 border-t ${isLight ? 'border-slate-200' : 'border-slate-800'}">
                            <button id="btn-custom-confirm-cancel" class="px-4 py-2 rounded-xl text-xs font-medium ${isLight ? 'text-slate-600 hover:text-slate-900 hover:bg-slate-100' : 'text-slate-400 hover:text-slate-200 hover:bg-slate-800'} transition-colors cursor-pointer">
                                ${displayCancelText}
                            </button>
                            <button id="btn-custom-confirm-yes" class="px-5 py-2 rounded-xl text-xs font-bold ${type === 'danger' ? 'bg-rose-600 hover:bg-rose-500 text-white shadow-rose-600/30' : 'bg-amber-600 hover:bg-amber-500 text-white shadow-amber-600/30'} shadow-lg cursor-pointer transition-all">
                                ${displayConfirmText}
                            </button>
                        </div>
                    </div>
                </div>
            `;

            if (window.lucide) window.lucide.createIcons();

            document.getElementById('btn-custom-confirm-cancel')?.addEventListener('click', () => {
                container.innerHTML = '';
                resolve(false);
            });

            document.getElementById('btn-custom-confirm-yes')?.addEventListener('click', () => {
                container.innerHTML = '';
                resolve(true);
            });
        });
    }

    async showValidationModal({ title, subtitle, errors, invalidInputs, formEl }) {
        await this.showAlertModal({
            title: title || 'Periksa Isian Form Data',
            subtitle: subtitle || 'Ditemukan beberapa isian yang masih kosong atau tidak valid:',
            errors: errors,
            buttonText: 'Perbaiki Isian Data Sekarang'
        });

        if (invalidInputs && invalidInputs.length > 0) {
            invalidInputs[0].focus();
            invalidInputs[0].scrollIntoView({ behavior: 'smooth', block: 'center' });
        }
    }

    validateProductionForm(data, formEl) {
        const errors = [];
        const invalidInputs = [];

        // Helper to mark input as erroneous and add auto-clear on input
        const markInvalid = (fieldName, message) => {
            errors.push(message);
            if (formEl) {
                const inputEl = formEl.querySelector(`[name="${fieldName}"]`);
                if (inputEl) {
                    inputEl.classList.add('border-rose-500', 'ring-2', 'ring-rose-500/50', 'bg-rose-500/10');
                    invalidInputs.push(inputEl);

                    const clearHandler = () => {
                        inputEl.classList.remove('border-rose-500', 'ring-2', 'ring-rose-500/50', 'bg-rose-500/10');
                        inputEl.removeEventListener('input', clearHandler);
                        inputEl.removeEventListener('change', clearHandler);
                    };
                    inputEl.addEventListener('input', clearHandler);
                    inputEl.addEventListener('change', clearHandler);
                }
            }
        };

        // Reset previous validation borders
        if (formEl) {
            formEl.querySelectorAll('input, select, textarea').forEach(el => {
                el.classList.remove('border-rose-500', 'ring-2', 'ring-rose-500/50', 'bg-rose-500/10');
            });
        }

        // 1. CONTEXT / IDENTITY VALIDATION
        if (!data.production_line_id || String(data.production_line_id).trim() === '') {
            markInvalid('production_line_id', '<strong>Line Produksi:</strong> Wajib dipilih dari daftar line aktif.');
        }
        if (!data.machine_id || String(data.machine_id).trim() === '') {
            markInvalid('machine_id', '<strong>Mesin (Machine):</strong> Wajib dipilih.');
        }
        if (!data.product_id || String(data.product_id).trim() === '') {
            markInvalid('product_id', '<strong>Produk / Part SKU:</strong> Wajib dipilih.');
        }
        if (!data.shift_id || String(data.shift_id).trim() === '') {
            markInvalid('shift_id', '<strong>Shift Kerja:</strong> Wajib dipilih.');
        }
        if (!data.production_date || String(data.production_date).trim() === '') {
            markInvalid('production_date', '<strong>Tanggal Produksi:</strong> Wajib diisi dengan tanggal yang valid.');
        }

        // 1B. DUPLICATE VALIDATION (1 Line tidak boleh menggunakan Shift yang sama pada tanggal yang sama)
        const checkRecords = (this.currentDailyRecords && Array.isArray(this.currentDailyRecords)) ? this.currentDailyRecords : null;
        if (checkRecords && data.production_line_id && data.shift_id) {
            const prodDate = data.production_date || this.selectedDailyDate;
            const duplicate = checkRecords.find(r => 
                String(r.production_line_id) === String(data.production_line_id) &&
                String(r.shift_id) === String(data.shift_id) &&
                (r.production_date === prodDate || (!data.production_date && r.production_date === this.selectedDailyDate))
            );

            if (duplicate) {
                const lineObj = (this.masterData.lines || []).find(l => l.id == data.production_line_id);
                const shiftObj = (this.masterData.shifts || []).find(s => s.id == data.shift_id);
                const lineName = lineObj ? lineObj.name : `Line #${data.production_line_id}`;
                const shiftName = shiftObj ? shiftObj.name : `Shift #${data.shift_id}`;
                
                markInvalid('shift_id', `<strong>Data sudah ditambahkan tidak boleh sama !</strong><br/>Kombinasi Line: <strong>${lineName}</strong> dengan <strong>${shiftName}</strong> pada tanggal <strong>${prodDate}</strong> sudah pernah di-input sebelumnya. Dalam 1 Line tidak boleh menggunakan Shift yang sama.`);
                markInvalid('production_line_id', `Silakan pilih Line lain (misal: Line FX-1 s/d FX-11) atau pilih Shift yang belum di-input pada tanggal ini.`);
            }
        }

        // 2. PRODUCTION QUANTITY VALIDATION
        const targetQty = parseFloat(data.target_quantity);
        const totalQty = parseFloat(data.total_quantity);
        const goodQty = parseFloat(data.good_quantity);
        const rejectQty = parseFloat(data.reject_quantity || 0);

        // Check if all primary output metrics are 0 / uninitialized
        if ((isNaN(targetQty) || targetQty <= 0) && (isNaN(totalQty) || totalQty <= 0) && (isNaN(goodQty) || goodQty <= 0)) {
            markInvalid('target_quantity', '<strong>Data Tidak Dapat Diproses:</strong> Target Produksi, Measuring, dan Finish Good bernilai 0 / kosong. Wajib mengisi data hasil produksi secara lengkap dan nyata terlebih dahulu.');
            markInvalid('total_quantity', 'Total Measuring wajib diisi dengan hasil output aktual.');
            markInvalid('good_quantity', 'Finish Good wajib diisi dengan jumlah part OK yang diproduksi.');
        } else {
            if (isNaN(targetQty) || targetQty <= 0 || String(data.target_quantity).trim() === '') {
                markInvalid('target_quantity', '<strong>Target Qty Tidak Boleh 0:</strong> Target produksi harus lebih dari 0 pcs (contoh: 500 pcs).');
            }

            if (isNaN(totalQty) || totalQty < 0 || String(data.total_quantity).trim() === '') {
                markInvalid('total_quantity', '<strong>Total Measuring (Output Aktual):</strong> Wajib diisi angka non-negatif.');
            }

            if (isNaN(goodQty) || goodQty < 0 || String(data.good_quantity).trim() === '') {
                markInvalid('good_quantity', '<strong>Finish Good (Barang OK):</strong> Wajib diisi angka non-negatif.');
            }

            if (isNaN(rejectQty) || rejectQty < 0 || String(data.reject_quantity).trim() === '') {
                markInvalid('reject_quantity', '<strong>Not Good (NG Qty):</strong> Wajib diisi angka non-negatif (minimal 0 pcs).');
            }

            if (totalQty === 0 && goodQty === 0 && rejectQty === 0) {
                markInvalid('total_quantity', '<strong>Data Tidak Dapat Diproses:</strong> Total Measuring dan Finish Good bernilai 0 pcs. Wajib mengisi data hasil produksi aktual.');
            }
        }

        // 3. MATHEMATICAL INTEGRITY (Finish Good + Reject <= Total Measuring)
        if (!isNaN(totalQty) && !isNaN(goodQty) && !isNaN(rejectQty)) {
            const sumOutput = goodQty + rejectQty;
            if (sumOutput > totalQty) {
                markInvalid('good_quantity', `<strong>Ketidaksinkronan Hasil Output:</strong> Finish Good (${goodQty.toLocaleString()}) + NG (${rejectQty.toLocaleString()}) = <strong>${sumOutput.toLocaleString()} pcs</strong>, melebihi Total Measuring (${totalQty.toLocaleString()} pcs).`);
                markInvalid('reject_quantity', 'Periksa kembali nilai Finish Good atau NG Qty agar sesuai dengan Total Measuring.');
            }
        }

        // 4. OPERATING TIME & OEE PARAMETERS
        const plannedTime = parseFloat(data.planned_production_time);
        if (isNaN(plannedTime) || plannedTime <= 0 || String(data.planned_production_time).trim() === '') {
            markInvalid('planned_production_time', '<strong>Planned Production Time:</strong> Wajib diisi durasi waktu kerja shift (harus > 0 menit, misal: 480 menit).');
        }

        const plannedDowntime = parseFloat(data.planned_downtime);
        if (isNaN(plannedDowntime) || plannedDowntime < 0 || String(data.planned_downtime).trim() === '') {
            markInvalid('planned_downtime', '<strong>Planned Downtime / Istirahat:</strong> Wajib diisi angka menit (minimal 0).');
        }

        const runTime = parseFloat(data.run_time);
        if (isNaN(runTime) || runTime < 0 || String(data.run_time).trim() === '') {
            markInvalid('run_time', '<strong>Operating Run Time:</strong> Wajib diisi angka durasi operasional mesin (minimal 0 menit).');
        } else if (!isNaN(plannedTime) && runTime > plannedTime) {
            markInvalid('run_time', `<strong>Batas Waktu Operasi:</strong> Run Time (${runTime} menit) tidak boleh melebihi Planned Production Time (${plannedTime} menit).`);
        }

        // 5. TROUBLE DOWNTIME LOG VALIDATION
        if (data.downtime_logs && Array.isArray(data.downtime_logs) && data.downtime_logs.length > 0) {
            let totalDtLogsDur = 0;
            data.downtime_logs.forEach((dt, idx) => {
                const dtDur = parseFloat(dt.duration_minutes || 0);
                const hasDtDesc = dt.description && dt.description.trim() !== '';
                const hasDtStart = dt.start_time && dt.start_time.trim() !== '';
                totalDtLogsDur += dtDur;

                if (dtDur > 0 || hasDtDesc || hasDtStart) {
                    if (dtDur > 0 && !hasDtDesc) {
                        markInvalid(`dt_description_${idx}`, `<strong>Trouble Log #${idx + 1}:</strong> Deskripsi Gejala Trouble wajib diisi karena durasi tercatat ${dtDur} menit.`);
                    }
                    if (hasDtDesc && !hasDtStart) {
                        markInvalid(`dt_start_time_${idx}`, `<strong>Trouble Log #${idx + 1}:</strong> Jam Start Trouble wajib diisi.`);
                    }
                }
            });
            if (!isNaN(plannedTime) && totalDtLogsDur > plannedTime) {
                markInvalid('run_time', `<strong>Total Durasi Kendala (${totalDtLogsDur} menit)</strong> melebihi Planned Production Time (${plannedTime} menit).`);
            }
        } else {
            const dtDur = parseFloat(data.dt_duration_minutes || 0);
            const hasDtDesc = data.dt_description && data.dt_description.trim() !== '';
            const hasDtStart = data.dt_start_time && data.dt_start_time.trim() !== '';

            if (dtDur > 0 || hasDtDesc || hasDtStart) {
                if (dtDur > 0 && !hasDtDesc) {
                    markInvalid('dt_description', `<strong>Deskripsi Gejala Trouble:</strong> Wajib diisi karena durasi kendala tercatat ${dtDur} menit.`);
                }
                if (hasDtDesc && !hasDtStart) {
                    markInvalid('dt_start_time', '<strong>Jam Start Trouble:</strong> Wajib diisi jika terdapat catatan trouble kendala mesin.');
                }
                if (!isNaN(plannedTime) && dtDur > plannedTime) {
                    markInvalid('dt_duration_minutes', `<strong>Durasi Trouble:</strong> Durasi kendala (${dtDur} menit) tidak boleh melebihi total jam kerja shift (${plannedTime} menit).`);
                }
            }
        }

        // 6. IF ERRORS OCCUR, SHOW PROFESSIONAL MODAL
        if (errors.length > 0) {
            this.showValidationModal({
                title: 'Periksa Kembali Isian Form Produksi',
                subtitle: `Ditemukan ${errors.length} data yang belum lengkap atau tidak valid:`,
                errors: errors,
                invalidInputs: invalidInputs,
                formEl: formEl
            });
            return false;
        }

        return true;
    }

    // Modal Form for Production Entry
    showProductionModal() {
        const modalContainer = document.getElementById('modal-container');
        modalContainer.innerHTML = `
            <div class="fixed inset-0 bg-slate-950/80 backdrop-blur-sm flex items-center justify-center p-4 z-50 overflow-y-auto">
                <div class="bg-slate-900 border border-slate-800 rounded-2xl p-6 max-w-3xl w-full shadow-2xl my-8">
                    <div class="flex items-center justify-between mb-4 pb-3 border-b border-slate-800">
                        <h3 class="font-bold text-sm text-slate-100 flex items-center gap-2">
                            <i data-lucide="plus-circle" class="w-4 h-4 text-emerald-400"></i>
                            Entry Production & OEE Record
                        </h3>
                        <button id="btn-close-modal" class="text-slate-400 hover:text-slate-200">
                            <i data-lucide="x" class="w-4 h-4"></i>
                        </button>
                    </div>

                    <form id="form-production-entry" class="space-y-4 text-xs">
                        <!-- LANGKAH 1: KONTEKS TANGGAL, LINE & MESIN -->
                        <div class="form-step-card bg-slate-950/60 border border-slate-800 rounded-xl p-4 space-y-3">
                            <div class="flex items-center gap-2 border-b border-slate-800/80 pb-2">
                                <span class="w-5 h-5 rounded-full bg-cyan-500/20 text-cyan-400 font-bold text-xs flex items-center justify-center border border-cyan-500/40">1</span>
                                <h4 class="font-bold text-xs text-slate-100 uppercase tracking-wider">Tanggal Laporan, Line, Mesin, Produk & Shift</h4>
                            </div>
                            <div class="grid grid-cols-3 gap-3">
                                <div>
                                    <label class="block text-slate-400 mb-1 font-medium flex items-center gap-1">
                                        <i data-lucide="calendar" class="w-3.5 h-3.5 text-cyan-400"></i>
                                        Tanggal Produksi
                                    </label>
                                    <input type="date" name="production_date" value="${new Date().toISOString().slice(0,10)}" required class="w-full bg-slate-900 border border-slate-800 rounded-lg px-2.5 py-2 text-slate-100 font-mono font-bold" />
                                </div>
                                <div>
                                    <label class="block text-slate-400 mb-1 font-medium">Production Line</label>
                                    <select name="production_line_id" required class="w-full bg-slate-900 border border-slate-800 rounded-lg px-2.5 py-2 text-slate-100">
                                        ${this.masterData.lines.map(l => `<option value="${l.id}">${l.name}</option>`).join('')}
                                    </select>
                                </div>
                                <div>
                                    <label class="block text-slate-400 mb-1 font-medium">Mesin Input</label>
                                    <select name="machine_id" required class="w-full bg-slate-900 border border-slate-800 rounded-lg px-2.5 py-2 text-slate-100 font-sans">
                                        <!-- Populated dynamically based on line -->
                                    </select>
                                </div>
                            </div>

                            <div class="grid grid-cols-2 gap-3">
                                <div>
                                    <label class="block text-slate-400 mb-1 font-medium">Nama Produk</label>
                                    <select name="product_id" required class="w-full bg-slate-900 border border-slate-800 rounded-lg px-2.5 py-2 text-slate-100 font-sans">
                                        ${(() => {
                                            const initLineId = this.masterData.lines?.[0]?.id;
                                            const initProds = this.getProductsForLine(initLineId);
                                            if (initProds.length === 0) return '<option value="" disabled selected>-- Pilih Line Terlebih Dahulu --</option>';
                                            return initProds.map(p => `<option value="${p.id}">${p.name} — SKU: ${p.sku} (${p.ideal_cycle_time}s)</option>`).join('');
                                        })()}
                                    </select>
                                </div>
                                <div>
                                    <label class="block text-slate-400 mb-1 font-medium">Shift Kerja</label>
                                    <select name="shift_id" required class="w-full bg-slate-900 border border-slate-800 rounded-lg px-2.5 py-2 text-slate-100">
                                        ${this.masterData.shifts.map(s => `<option value="${s.id}">${s.name}</option>`).join('')}
                                    </select>
                                </div>
                            </div>
                        </div>

                        <!-- LANGKAH 2: HASIL PRODUKSI & WAKTU -->
                        <div class="form-step-card bg-slate-950/60 border border-slate-800 rounded-xl p-4 space-y-3">
                            <div class="flex items-center justify-between border-b border-slate-800/80 pb-2">
                                <div class="flex items-center gap-2">
                                    <span class="w-5 h-5 rounded-full bg-emerald-500/20 text-emerald-400 font-bold text-xs flex items-center justify-center border border-emerald-500/40">2</span>
                                    <h4 class="font-bold text-xs text-slate-100 uppercase tracking-wider">Hasil Output & Jam Kerja</h4>
                                </div>
                                <span class="text-[10px] text-emerald-400 font-mono">Target Qty Otomatis = Planned Time × 60 ÷ Cycle Time</span>
                            </div>
                            <div class="grid grid-cols-2 md:grid-cols-5 gap-2.5">
                                <div>
                                    <label class="block text-slate-400 mb-1 font-medium flex items-center gap-1">
                                        Target Qty
                                        <span class="text-[10px] text-cyan-400 font-mono font-bold">(Auto)</span>
                                    </label>
                                    <input type="number" name="target_quantity" value="0" min="0" placeholder="0" required class="w-full bg-slate-900 border border-slate-800 rounded-lg px-2 py-2 text-cyan-400 font-mono font-bold" />
                                </div>
                                <div>
                                    <label class="block text-slate-400 mb-1 font-medium">Total Measuring</label>
                                    <input type="number" name="total_quantity" value="0" min="0" placeholder="0" required class="w-full bg-slate-900 border border-slate-800 rounded-lg px-2 py-2 text-slate-100 font-mono font-bold" />
                                </div>
                                <div>
                                    <label class="block text-slate-400 mb-1 font-medium">Finish Good</label>
                                    <input type="number" name="good_quantity" value="0" min="0" placeholder="0" required class="w-full bg-slate-900 border border-slate-800 rounded-lg px-2 py-2 text-emerald-400 font-mono font-bold" />
                                </div>
                                <div>
                                    <label class="block text-slate-400 mb-1 font-medium">Not Good (NG)</label>
                                    <input type="number" name="reject_quantity" value="0" min="0" placeholder="0" required class="w-full bg-slate-900 border border-slate-800 rounded-lg px-2 py-2 text-rose-400 font-mono font-bold" />
                                </div>
                                <div>
                                    <label class="block text-slate-400 mb-1 font-medium">Scrap / Afkir</label>
                                    <input type="number" name="scrap_quantity" value="0" min="0" placeholder="0" required class="w-full bg-slate-900 border border-slate-800 rounded-lg px-2 py-2 text-amber-400 font-mono font-bold" />
                                </div>
                            </div>

                            <div class="grid grid-cols-3 gap-3 pt-1">
                                <div>
                                    <label class="block text-slate-400 mb-1 font-medium">Planned Time (min)</label>
                                    <input type="number" name="planned_production_time" value="480" required class="w-full bg-slate-900 border border-slate-800 rounded-lg px-2.5 py-1.5 text-slate-200 font-mono" />
                                </div>
                                <div>
                                    <label class="block text-slate-400 mb-1 font-medium">Planned Downtime</label>
                                    <input type="number" name="planned_downtime" value="60" required class="w-full bg-slate-900 border border-slate-800 rounded-lg px-2.5 py-1.5 text-slate-200 font-mono" />
                                </div>
                                <div>
                                    <label class="block text-slate-400 mb-1 font-medium">Run Time (min)</label>
                                    <input type="number" name="run_time" value="420" required class="w-full bg-slate-900 border border-slate-800 rounded-lg px-2.5 py-1.5 text-emerald-400 font-mono font-bold" />
                                </div>
                            </div>
                        </div>

                        <!-- LANGKAH 3: LOG TROUBLE & DOWNTIME DINAMIS (MULTI-ROW) -->
                        <div class="form-step-card bg-slate-950/60 border border-amber-500/30 rounded-xl p-4 space-y-3">
                            <div class="flex flex-wrap items-center justify-between border-b border-slate-800/80 pb-2 gap-2">
                                <div class="flex items-center gap-2">
                                    <span class="w-5 h-5 rounded-full bg-amber-500/20 text-amber-400 font-bold text-xs flex items-center justify-center border border-amber-500/40">3</span>
                                    <h4 class="font-bold text-xs text-amber-300 uppercase tracking-wider">Trouble Log & Loss Time (Opsional)</h4>
                                </div>
                                <div class="flex items-center gap-2">
                                    <span id="modal-dt-total-badge" class="px-2.5 py-0.5 rounded-full text-[11px] font-mono font-bold bg-amber-950/80 text-amber-400 border border-amber-800">
                                        Total Losstime: 0 Menit
                                    </span>
                                    <button type="button" id="btn-modal-add-trouble" class="px-3 py-1.5 rounded-lg text-xs font-bold bg-amber-600 hover:bg-amber-500 text-white shadow-md flex items-center gap-1.5 cursor-pointer transition-all">
                                        <i data-lucide="plus" class="w-3.5 h-3.5"></i>
                                        <span>+ Tambah Trouble Log</span>
                                    </button>
                                </div>
                            </div>

                            <div id="modal-trouble-logs-container" class="space-y-3 pt-1">
                                <!-- Dynamic Trouble Log Rows Container -->
                            </div>
                        </div>

                        <input type="hidden" name="downtime" value="0" />
                        <input type="hidden" name="idle_time" value="0" />
                        <input type="hidden" name="ideal_cycle_time" value="9.5" />
                        <input type="hidden" name="production_date" value="${new Date().toISOString().slice(0,10)}" />

                        <div class="pt-4 flex justify-end gap-2 border-t border-slate-800">
                            <button type="button" id="btn-cancel-modal" class="px-4 py-2 bg-slate-800 hover:bg-slate-700 text-slate-300 rounded-lg font-medium cursor-pointer transition-colors">Cancel</button>
                            <button type="submit" class="px-5 py-2 bg-emerald-600 hover:bg-emerald-500 text-white font-semibold rounded-lg shadow-md shadow-emerald-600/20 cursor-pointer transition-all">Save & Calculate OEE</button>
                        </div>
                    </form>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();
        if (this.currentLang === 'ja') {
            i18n.localizeDom(modalContainer);
        }

        // 1. Dynamic Trouble Logs State Management
        let modalTroubleLogsState = [];
        const modalTroubleContainer = document.getElementById('modal-trouble-logs-container');
        const modalTotalDtBadge = document.getElementById('modal-dt-total-badge');
        const modalAddTroubleBtn = document.getElementById('btn-modal-add-trouble');
        const modalMainDowntimeInput = document.querySelector('#form-production-entry input[name="downtime"]');
        const modalPlannedTimeInput = document.querySelector('#form-production-entry input[name="planned_production_time"]');
        const modalPlannedDtInput = document.querySelector('#form-production-entry input[name="planned_downtime"]');
        const modalRunTimeInput = document.querySelector('#form-production-entry input[name="run_time"]');
        const modalTargetQtyInput = document.querySelector('#form-production-entry input[name="target_quantity"]');
        const modalIdealCycleInput = document.querySelector('#form-production-entry input[name="ideal_cycle_time"]');
        const lineSelect = document.querySelector('#form-production-entry select[name="production_line_id"]');
        const productSelect = document.querySelector('#form-production-entry select[name="product_id"]');
        const modalShiftSelect = document.querySelector('#form-production-entry select[name="shift_id"]');

        const updateModalRunTimeAndDowntime = () => {
            let totalDtMins = 0;
            modalTroubleLogsState.forEach(r => {
                totalDtMins += parseFloat(r.duration_minutes || 0);
            });

            if (modalTotalDtBadge) {
                modalTotalDtBadge.textContent = `Total Losstime: ${totalDtMins} Menit`;
            }
            if (modalMainDowntimeInput) {
                modalMainDowntimeInput.value = totalDtMins;
            }
            if (modalRunTimeInput && modalPlannedTimeInput && modalPlannedDtInput) {
                const plannedMins = parseFloat(modalPlannedTimeInput.value || 0);
                const breakMins = parseFloat(modalPlannedDtInput.value || 0);
                modalRunTimeInput.value = Math.max(0, plannedMins - breakMins - totalDtMins);
            }
        };

        const getModalLeaderName = (teamVal) => {
            if (!lineSelect) return 'Belum Didaftar';
            const lineId = lineSelect.value;
            const match = (this.masterData.groups || []).find(g => 
                g.production_line_id == lineId && 
                (g.team === teamVal || g.team.toLowerCase().includes((teamVal || '').toLowerCase()) || (teamVal || '').toLowerCase().includes(g.team.toLowerCase()))
            );
            return match ? match.leader_name : 'Belum Didaftar';
        };

        const renderModalTroubleRows = () => {
            if (!modalTroubleContainer) return;

            if (modalTroubleLogsState.length === 0) {
                modalTroubleContainer.innerHTML = `
                    <div class="text-center py-4 px-4 bg-slate-900/40 border border-dashed border-slate-800 rounded-xl text-slate-400 text-xs">
                        <i data-lucide="check-circle" class="w-4 h-4 text-emerald-400 inline mr-1"></i>
                        <span>Produksi lancar tanpa kendala. Klik <strong class="text-amber-300 cursor-pointer" id="btn-empty-add-trouble">+ Tambah Trouble Log</strong> jika ada stoppage/downtime shift ini.</span>
                    </div>
                `;
                const emptyAddBtn = document.getElementById('btn-empty-add-trouble');
                if (emptyAddBtn) {
                    emptyAddBtn.addEventListener('click', () => {
                        modalAddTroubleBtn.click();
                    });
                }
            } else {
                modalTroubleContainer.innerHTML = modalTroubleLogsState.map((row, idx) => `
                    <div class="trouble-row-card bg-slate-900 border border-slate-800 rounded-xl p-3 space-y-3 hover:border-amber-500/40 transition-colors">
                        <div class="flex items-center justify-between border-b border-slate-800/80 pb-2">
                            <div class="flex items-center gap-2">
                                <span class="px-2 py-0.5 rounded text-[10px] font-mono font-bold bg-amber-950 text-amber-300 border border-amber-800">
                                    Trouble #${idx + 1}
                                </span>
                                <span class="text-[11px] text-slate-300 font-sans font-medium">${row.problem_type || 'Mesin'} (${row.duration_minutes || 0} Menit)</span>
                            </div>
                            <button type="button" data-del-modal-dt-idx="${idx}" class="p-1 rounded-lg text-slate-400 hover:text-rose-400 hover:bg-rose-950/40 border border-transparent hover:border-rose-800 transition-colors cursor-pointer" title="Hapus Baris Trouble Ini">
                                <i data-lucide="trash-2" class="w-3.5 h-3.5"></i>
                            </button>
                        </div>

                        <div class="grid grid-cols-1 md:grid-cols-3 gap-3">
                            <div>
                                <label class="block text-slate-400 mb-1 font-medium">Team</label>
                                <select data-modal-dt-idx="${idx}" data-modal-dt-field="team" class="w-full bg-slate-950 border border-slate-800 rounded-lg px-2.5 py-1.5 text-slate-100 font-sans font-bold text-xs">
                                    <option value="Team A" ${row.team === 'Team A' ? 'selected' : ''}>Team A</option>
                                    <option value="Team B" ${row.team === 'Team B' ? 'selected' : ''}>Team B</option>
                                    <option value="Team C" ${row.team === 'Team C' ? 'selected' : ''}>Team C</option>
                                </select>
                            </div>
                            <div>
                                <label class="block text-slate-400 mb-1 font-medium flex items-center gap-1">
                                    <i data-lucide="user-check" class="w-3.5 h-3.5 text-cyan-400"></i>
                                    Leader Name / PIC
                                </label>
                                <input type="text" data-modal-dt-idx="${idx}" data-modal-dt-field="leader_name" value="${row.leader_name || getModalLeaderName(row.team)}" readonly class="w-full bg-slate-950/60 border border-slate-800 rounded-lg px-2.5 py-1.5 text-cyan-400 font-mono font-bold text-xs" placeholder="Auto-populated..." />
                            </div>
                            <div>
                                <label class="block text-slate-400 mb-1 font-medium">Jenis Trouble</label>
                                <select data-modal-dt-idx="${idx}" data-modal-dt-field="problem_type" class="w-full bg-slate-950 border border-slate-800 rounded-lg px-2.5 py-1.5 text-slate-100 font-sans text-xs">
                                    <option value="Inventory" ${row.problem_type === 'Inventory' ? 'selected' : ''}>Inventory</option>
                                    <option value="Others" ${row.problem_type === 'Others' || row.problem_type === 'Other' ? 'selected' : ''}>Others</option>
                                    <option value="Planning Downtime" ${row.problem_type === 'Planning Downtime' ? 'selected' : ''}>Planning Downtime</option>
                                    <option value="Problem Mesin Elektrik" ${row.problem_type === 'Problem Mesin Elektrik' ? 'selected' : ''}>Problem Mesin Elektrik</option>
                                    <option value="Problem Mesin Mekanik" ${row.problem_type === 'Problem Mesin Mekanik' || row.problem_type === 'Mesin' ? 'selected' : ''}>Problem Mesin Mekanik</option>
                                    <option value="Problem Tool" ${row.problem_type === 'Problem Tool' || row.problem_type === 'Tool' || row.problem_type === 'Dies / Tooling' ? 'selected' : ''}>Problem Tool</option>
                                </select>
                            </div>
                        </div>

                        <div class="grid grid-cols-3 gap-2.5">
                            <div>
                                <label class="block text-slate-400 mb-1">Jam Start</label>
                                <input type="time" data-modal-dt-idx="${idx}" data-modal-dt-field="start_time" value="${row.start_time || ''}" class="w-full bg-slate-950 border border-slate-800 rounded-lg px-2 py-1.5 text-slate-100 font-mono text-xs" />
                            </div>
                            <div>
                                <label class="block text-slate-400 mb-1">Jam Selesai</label>
                                <input type="time" data-modal-dt-idx="${idx}" data-modal-dt-field="end_time" value="${row.end_time || ''}" class="w-full bg-slate-950 border border-slate-800 rounded-lg px-2 py-1.5 text-slate-100 font-mono text-xs" />
                            </div>
                            <div>
                                <label class="block text-slate-400 mb-1">Durasi (Menit)</label>
                                <input type="number" min="0" data-modal-dt-idx="${idx}" data-modal-dt-field="duration_minutes" value="${row.duration_minutes || 0}" placeholder="0" class="w-full bg-slate-950 border border-slate-800 rounded-lg px-2 py-1.5 text-rose-400 font-mono font-bold text-xs" />
                            </div>
                        </div>

                        <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
                            <div>
                                <label class="block text-slate-400 mb-1">Deskripsi Gejala Trouble</label>
                                <textarea data-modal-dt-idx="${idx}" data-modal-dt-field="description" rows="2" placeholder="Detail kendala yang terjadi..." class="w-full bg-slate-950 border border-slate-800 rounded-lg px-2.5 py-1.5 text-slate-100 font-sans text-xs">${row.description || ''}</textarea>
                            </div>
                            <div>
                                <label class="block text-slate-400 mb-1">Tindakan Perbaikan (Countermeasure)</label>
                                <textarea data-modal-dt-idx="${idx}" data-modal-dt-field="action_taken" rows="2" placeholder="Langkah perbaikan yang telah dilakukan..." class="w-full bg-slate-950 border border-slate-800 rounded-lg px-2.5 py-1.5 text-slate-100 font-sans text-xs">${row.action_taken || ''}</textarea>
                            </div>
                        </div>
                    </div>
                `).join('');
            }

            if (window.lucide) window.lucide.createIcons();
            if (this.currentLang === 'ja') {
                i18n.localizeDom(modalTroubleContainer);
            }

            // Bind Event Listeners on Modal Trouble Inputs
            modalTroubleContainer.querySelectorAll('select, input, textarea').forEach(el => {
                el.addEventListener('input', (e) => {
                    const idx = parseInt(e.target.getAttribute('data-modal-dt-idx'));
                    const field = e.target.getAttribute('data-modal-dt-field');
                    if (modalTroubleLogsState[idx]) {
                        modalTroubleLogsState[idx][field] = e.target.value;

                        if (field === 'team') {
                            const leader = getModalLeaderName(e.target.value);
                            modalTroubleLogsState[idx].leader_name = leader;
                            const leaderInp = modalTroubleContainer.querySelector(`input[data-modal-dt-idx="${idx}"][data-modal-dt-field="leader_name"]`);
                            if (leaderInp) leaderInp.value = leader;
                        }

                        if (field === 'start_time' || field === 'end_time') {
                            const sTime = modalTroubleLogsState[idx].start_time;
                            const eTime = modalTroubleLogsState[idx].end_time;
                            if (sTime && eTime) {
                                const sParts = sTime.split(':');
                                const eParts = eTime.split(':');
                                let sMins = parseInt(sParts[0]) * 60 + parseInt(sParts[1] || 0);
                                let eMins = parseInt(eParts[0]) * 60 + parseInt(eParts[1] || 0);
                                if (eMins < sMins) eMins += 24 * 60;
                                const dur = Math.max(0, eMins - sMins);
                                modalTroubleLogsState[idx].duration_minutes = dur;
                                const durInp = modalTroubleContainer.querySelector(`input[data-modal-dt-idx="${idx}"][data-modal-dt-field="duration_minutes"]`);
                                if (durInp) durInp.value = dur;
                            }
                        }

                        if (field === 'duration_minutes') {
                            modalTroubleLogsState[idx].duration_minutes = parseFloat(e.target.value || 0);
                        }

                        updateModalRunTimeAndDowntime();
                    }
                });
            });

            // Bind Delete Trouble Row Buttons
            modalTroubleContainer.querySelectorAll('[data-del-modal-dt-idx]').forEach(btn => {
                btn.addEventListener('click', (e) => {
                    const idx = parseInt(e.currentTarget.getAttribute('data-del-modal-dt-idx'));
                    modalTroubleLogsState.splice(idx, 1);
                    renderModalTroubleRows();
                    updateModalRunTimeAndDowntime();
                });
            });

            updateModalRunTimeAndDowntime();
        };

        if (modalAddTroubleBtn) {
            modalAddTroubleBtn.addEventListener('click', () => {
                const defaultTeam = 'Team A';
                modalTroubleLogsState.push({
                    team: defaultTeam,
                    leader_name: getModalLeaderName(defaultTeam),
                    problem_type: 'Mesin',
                    start_time: '',
                    end_time: '',
                    duration_minutes: 0,
                    description: '',
                    action_taken: '',
                    is_planned: false
                });
                renderModalTroubleRows();
            });
        }

        renderModalTroubleRows();

        // 2. Auto-calculation of Target Qty based on Product Ideal Cycle Time & Planned Time
        const calcModalTargetQty = () => {
            const selectedProdId = productSelect ? productSelect.value : null;
            const selectedProd = (this.masterData.products || []).find(p => p.id == selectedProdId);
            const cycleTime = selectedProd && selectedProd.ideal_cycle_time ? parseFloat(selectedProd.ideal_cycle_time) : 9.5;

            if (modalIdealCycleInput) modalIdealCycleInput.value = cycleTime;

            const plannedMins = modalPlannedTimeInput ? parseFloat(modalPlannedTimeInput.value || 0) : 480;
            if (modalTargetQtyInput && cycleTime > 0 && plannedMins > 0) {
                // Target Qty = (Planned Time in minutes * 60 seconds) / (Ideal Cycle Time in seconds)
                const computedTarget = Math.floor((plannedMins * 60) / cycleTime);
                modalTargetQtyInput.value = computedTarget;
            }
        };

        const modalMachineSelect = document.querySelector('#form-production-entry select[name="machine_id"]');

        // Cascading Line -> Machines Filter (strictly for chosen Line, auto-select Measuring Machine)
        const filterMachinesByLine = () => {
            if (!lineSelect || !modalMachineSelect) return;
            const selectedLineId = lineSelect.value;
            const lineMachines = this.getMachinesForLine(selectedLineId);

            modalMachineSelect.innerHTML = lineMachines.map(m => 
                `<option value="${m.id}">${m.name} (${m.code})</option>`
            ).join('');

            const measuringMC = lineMachines.find(m => 
                (m.name && m.name.toLowerCase().includes('measuring')) || 
                (m.code && m.code.toLowerCase().includes('measuring'))
            );
            if (measuringMC) {
                modalMachineSelect.value = measuringMC.id;
            } else if (lineMachines.length > 0) {
                modalMachineSelect.value = lineMachines[0].id;
            }
        };

        // Cascading Line -> Product Filter
        const filterProductsByLine = () => {
            if (!lineSelect || !productSelect) return;
            const selectedLineId = lineSelect.value;
            const lineProducts = this.getProductsForLine(selectedLineId);

            if (lineProducts.length === 0) {
                productSelect.innerHTML = '<option value="" disabled selected>-- Tidak ada produk untuk lini ini --</option>';
            } else {
                productSelect.innerHTML = lineProducts.map(p => `
                    <option value="${p.id}" class="bg-slate-900 text-slate-100">${p.name} — SKU: ${p.sku} (${p.ideal_cycle_time}s)</option>
                `).join('');
            }

            calcModalTargetQty();
        };

        if (lineSelect) {
            lineSelect.addEventListener('change', () => {
                filterProductsByLine();
                filterMachinesByLine();
                // Update trouble log leader names if any
                modalTroubleLogsState.forEach(r => {
                    r.leader_name = getModalLeaderName(r.team);
                });
                renderModalTroubleRows();
            });
            filterProductsByLine();
            filterMachinesByLine();
        }

        if (productSelect) {
            productSelect.addEventListener('change', calcModalTargetQty);
        }

        // Auto-calculate Planned Time & Break Duration on Shift selection
        const updateShiftTimeCalculation = () => {
            if (!modalShiftSelect) return;
            const shiftId = modalShiftSelect.value;
            const shiftObj = (this.masterData.shifts || []).find(s => s.id == shiftId);
            if (!shiftObj) return;

            const startStr = shiftObj.start_time || '07:30';
            const endStr = shiftObj.end_time || '16:30';
            const breakMins = parseInt(shiftObj.break_duration_minutes || 60);

            const startParts = startStr.split(':');
            const endParts = endStr.split(':');

            let startMins = parseInt(startParts[0]) * 60 + parseInt(startParts[1] || 0);
            let endMins = parseInt(endParts[0]) * 60 + parseInt(endParts[1] || 0);

            if (endMins <= startMins) {
                endMins += 24 * 60; // Cross-midnight shift
            }

            const totalDurationMins = endMins - startMins;
            const plannedTimeMins = Math.max(0, totalDurationMins - breakMins);

            if (modalPlannedTimeInput) modalPlannedTimeInput.value = plannedTimeMins;
            if (modalPlannedDtInput) modalPlannedDtInput.value = breakMins;
            
            calcModalTargetQty();
            updateModalRunTimeAndDowntime();
        };

        if (modalShiftSelect) {
            modalShiftSelect.addEventListener('change', updateShiftTimeCalculation);
            updateShiftTimeCalculation();
        }

        if (modalPlannedTimeInput) {
            modalPlannedTimeInput.addEventListener('input', () => {
                calcModalTargetQty();
                updateModalRunTimeAndDowntime();
            });
        }
        if (modalPlannedDtInput) {
            modalPlannedDtInput.addEventListener('input', updateModalRunTimeAndDowntime);
        }

        document.getElementById('btn-close-modal').addEventListener('click', () => modalContainer.innerHTML = '');
        document.getElementById('btn-cancel-modal').addEventListener('click', () => modalContainer.innerHTML = '');

        const formProd = document.getElementById('form-production-entry');
        if (formProd) {
            formProd.addEventListener('submit', async (e) => {
                e.preventDefault();
                const formData = new FormData(e.target);
                const data = Object.fromEntries(formData.entries());

                // Build downtime_logs array from dynamic state
                const prodDate = data.production_date || new Date().toISOString().slice(0,10);
                const validTroubleLogs = modalTroubleLogsState.filter(r => 
                    (r.duration_minutes > 0) || (r.description && r.description.trim() !== '') || (r.start_time && r.start_time.trim() !== '')
                ).map(r => ({
                    team: r.team || null,
                    problem_type: r.problem_type || 'Mesin',
                    start_time: r.start_time ? `${prodDate} ${r.start_time}:00` : `${prodDate} 08:00:00`,
                    end_time: r.end_time ? `${prodDate} ${r.end_time}:00` : null,
                    duration_minutes: parseFloat(r.duration_minutes || 0),
                    description: r.description || null,
                    action_taken: r.action_taken || null,
                    is_planned: false
                }));

                data.downtime_logs = validTroubleLogs;

                if (!this.validateProductionForm(data, formProd)) return;

                try {
                    await api.createProductionRecord(data);
                    this.showNotification('Production & Downtime Record Saved', `Log produksi & ${validTroubleLogs.length} Laporan Trouble Log berhasil disimpan!`, 'create');
                    modalContainer.innerHTML = '';
                    this.loadCurrentTab();
                } catch (err) {
                    const errorMsg = err.response?.data?.message || err.message || 'Error submitting production record';
                    const errorDetail = err.response?.data?.error_detail || '';
                    const params = err.response?.data?.parameters || null;

                    if (errorMsg.includes('tidak boleh sama') || err.response?.status === 422) {
                        let paramListHtml = '';
                        if (params) {
                            paramListHtml = `
                                <div class="mt-3 p-3 bg-slate-950/90 border border-slate-800 rounded-xl space-y-1.5 font-mono text-[11px] text-left">
                                    <div class="text-amber-400 font-bold mb-1 flex items-center gap-1.5">
                                        <i data-lucide="alert-triangle" class="w-3.5 h-3.5 text-amber-400"></i>
                                        Rincian Parameter yang Sudah Terdaftar:
                                    </div>
                                    <div class="grid grid-cols-2 gap-1 text-slate-300">
                                        <div>🏭 Line: <strong class="text-cyan-400 font-sans">${params.line}</strong></div>
                                        <div>⏰ Shift: <strong class="text-cyan-400 font-sans">${params.shift}</strong></div>
                                        <div>📅 Tanggal: <strong class="text-slate-200">${params.tanggal}</strong></div>
                                        <div>⚙️ Mesin: <strong class="text-slate-200">${params.mesin}</strong></div>
                                    </div>
                                    <div class="text-slate-300 truncate">📦 Produk: <strong class="text-emerald-400 font-sans">${params.produk}</strong></div>
                                    <div class="text-slate-400 text-[10px] pt-1.5 border-t border-slate-800/80 flex items-center justify-between">
                                        <span>Target: <strong class="text-slate-200">${params.target_quantity?.toLocaleString() || 0}</strong></span>
                                        <span>Measuring: <strong class="text-cyan-400">${params.total_quantity?.toLocaleString() || 0}</strong></span>
                                        <span>OK: <strong class="text-emerald-400">${params.good_quantity?.toLocaleString() || 0}</strong></span>
                                        <span>NG: <strong class="text-rose-400">${params.reject_quantity?.toLocaleString() || 0}</strong></span>
                                    </div>
                                </div>
                            `;
                        }

                        this.showAlertModal({
                            title: 'Data sudah ditambahkan tidak boleh sama !',
                            subtitle: (errorDetail || 'Kombinasi Line dan Shift pada tanggal tersebut sudah pernah ditambahkan ke sistem.') + paramListHtml,
                            buttonText: 'Mengerti, Ubah Shift / Line'
                        });

                        const shiftInp = formProd.querySelector('[name="shift_id"]');
                        const lineInp = formProd.querySelector('[name="production_line_id"]');
                        if (shiftInp) shiftInp.classList.add('border-rose-500', 'ring-2', 'ring-rose-500/50', 'bg-rose-500/10');
                        if (lineInp) lineInp.classList.add('border-rose-500', 'ring-2', 'ring-rose-500/50', 'bg-rose-500/10');
                    } else {
                        this.showNotification('⚠️ Gagal Menyimpan', errorMsg, 'delete');
                    }
                }
            });
        }
    }

    toggleTvMode() {
        this.tvMode = !this.tvMode;
        const sidebar = document.getElementById('sidebar');
        if (this.tvMode) {
            sidebar.classList.add('hidden');
            document.body.classList.add('tv-mode-active');
            if (document.documentElement.requestFullscreen) {
                document.documentElement.requestFullscreen().catch(() => {});
            }
            // TV Floor Display Mode defaults to ALL LINES and ALL MEASURING MACHINES
            this.filters.line_id = '';
            this.filters.machine_id = 'all_measuring';
            
            // Sync filter bar controls
            const lineSelect = document.getElementById('filter-line');
            const mcSelect = document.getElementById('filter-machine');
            if (lineSelect) lineSelect.value = '';
            if (mcSelect) mcSelect.value = 'all_measuring';

            this.renderTvDisplay();
        } else {
            sidebar.classList.remove('hidden');
            document.body.classList.remove('tv-mode-active');
            if (document.exitFullscreen) {
                document.exitFullscreen().catch(() => {});
            }
            this.loadCurrentTab();
        }
    }

    async renderTvDisplay() {
        if (!this.tvSubTab) this.tvSubTab = 'overview';

        // If in TV mode with no specific machine filter selected, default to all measuring machines across all lines
        if (!this.filters.machine_id) {
            this.filters.machine_id = 'all_measuring';
        }

        const [kpiRes, trendRes, realtimeRes, sixLossRes] = await Promise.all([
            api.getOeeKpi(this.filters),
            api.getOeeTrend(this.filters),
            api.getRealtimeStatus(this.filters),
            api.getSixBigLosses(this.filters),
        ]);

        const kpi = kpiRes.data.data;
        const trends = trendRes.data.data;
        const machines = realtimeRes.data.data || [];
        const sixLosses = sixLossRes.data.data;

        // Dynamic Machine Status Counts from Database
        const runningCount = machines.filter(m => m.status === 'RUNNING').length;
        const idleCount = machines.filter(m => m.status === 'IDLE').length;
        const stopMachines = machines.filter(m => ['STOP', 'STOPPED', 'BREAKDOWN', 'MAINTENANCE', 'OFFLINE'].includes((m.status || '').toUpperCase()));
        const stopCount = stopMachines.length;
        const totalMachines = machines.length;

        // Helper calculations for 4 Macro KPI Cards
        const oeeVal = kpi.oee != null ? Number(kpi.oee) : 0;
        const availVal = kpi.availability != null ? Number(kpi.availability) : 0;
        const perfVal = kpi.performance != null ? Number(kpi.performance) : 0;
        const qualVal = kpi.quality != null ? Number(kpi.quality) : 0;

        const prevOee = kpi.previous_oee != null ? Number(kpi.previous_oee) : null;
        const prevAvail = kpi.previous_availability != null ? Number(kpi.previous_availability) : null;
        const prevPerf = kpi.previous_performance != null ? Number(kpi.previous_performance) : null;
        const prevQual = kpi.previous_quality != null ? Number(kpi.previous_quality) : null;

        const calcDelta = (curr, prev) => {
            if (prev == null) return { delta: 0, str: '±0.00%', isUp: false, isZero: true };
            const diff = Number((curr - prev).toFixed(2));
            if (diff > 0) return { delta: diff, str: `▲ +${diff.toFixed(2)}%`, isUp: true, isZero: false };
            if (diff < 0) return { delta: diff, str: `▼ ${diff.toFixed(2)}%`, isUp: false, isZero: false };
            return { delta: 0, str: '±0.00%', isUp: false, isZero: true };
        };

        const oeeDiff = calcDelta(oeeVal, prevOee);
        const availDiff = calcDelta(availVal, prevAvail);
        const perfDiff = calcDelta(perfVal, prevPerf);
        const qualDiff = calcDelta(qualVal, prevQual);

        const getDeltaBadge = (d) => {
            if (d.isZero) {
                return `<span class="inline-flex items-center gap-1 rounded-full bg-slate-800/80 text-slate-400 border border-slate-700 px-2 py-0.5 text-[11px] font-mono font-bold">${d.str}</span>`;
            }
            if (d.isUp) {
                return `<span class="inline-flex items-center gap-1 rounded-full bg-emerald-950/70 text-emerald-400 border border-emerald-800/50 px-2 py-0.5 text-[11px] font-mono font-bold">${d.str}</span>`;
            }
            return `<span class="inline-flex items-center gap-1 rounded-full bg-rose-950/70 text-rose-400 border border-rose-800/50 px-2 py-0.5 text-[11px] font-mono font-bold">${d.str}</span>`;
        };

        // Determine Status Visuals for each Metric
        // 1. Overall OEE: Dynamic (Bagus = Hijau, Kurang = Orange, Jelek = Merah)
        const oeeStatus = oeeVal >= 85 
            ? { label: 'Optimal / World Class', color: '#10b981', grad: 'from-emerald-600 to-emerald-400', tagClass: 'bg-emerald-500/10 text-emerald-400 border-emerald-500/30', benchBadge: 'text-emerald-400 bg-emerald-950/40 border-emerald-800/40' }
            : (oeeVal >= 65 
                ? { label: 'Warning / Minor Loss', color: '#f97316', grad: 'from-amber-600 to-orange-500', tagClass: 'bg-orange-500/10 text-orange-400 border-orange-500/30', benchBadge: 'text-orange-400 bg-orange-950/40 border-orange-800/40' }
                : { label: 'Critical / Breakdown', color: '#ef4444', grad: 'from-rose-600 to-red-500', tagClass: 'bg-rose-500/10 text-rose-400 border-rose-500/30', benchBadge: 'text-rose-400 bg-rose-950/40 border-rose-800/40' });

        // 2. Availability (A): Tema Biru (Sky/Blue)
        const availStatus = availVal >= 90
            ? { label: 'On Target (>=90%)', color: '#0ea5e9', grad: 'from-blue-600 to-sky-400', tagClass: 'bg-sky-500/10 text-sky-400 border-sky-500/30' }
            : (availVal >= 80
                ? { label: 'Warning (80-89%)', color: '#38bdf8', grad: 'from-blue-600 to-cyan-400', tagClass: 'bg-sky-500/10 text-sky-300 border-sky-500/30' }
                : { label: 'Needs Improvement', color: '#ef4444', grad: 'from-rose-600 to-rose-400', tagClass: 'bg-rose-500/10 text-rose-400 border-rose-500/30' });

        // 3. Performance (P): Tema Hijau (Emerald/Green)
        const perfStatus = perfVal >= 95
            ? { label: 'On Target (>=95%)', color: '#10b981', grad: 'from-emerald-600 to-teal-400', tagClass: 'bg-emerald-500/10 text-emerald-400 border-emerald-500/30' }
            : (perfVal >= 85
                ? { label: 'Warning (85-94%)', color: '#22c55e', grad: 'from-emerald-500 to-green-400', tagClass: 'bg-emerald-500/10 text-emerald-300 border-emerald-500/30' }
                : { label: 'Needs Improvement', color: '#ef4444', grad: 'from-rose-600 to-rose-400', tagClass: 'bg-rose-500/10 text-rose-400 border-rose-500/30' });

        // 4. Quality Rate (Q): Tema Merah / Jingga (Orange/Red)
        const qualStatus = qualVal >= 99
            ? { label: 'Zero Defect Target', color: '#f97316', grad: 'from-orange-600 to-amber-400', tagClass: 'bg-orange-500/10 text-orange-400 border-orange-500/30' }
            : (qualVal >= 95
                ? { label: 'Warning (95-98%)', color: '#ea580c', grad: 'from-orange-600 to-rose-400', tagClass: 'bg-amber-500/10 text-amber-400 border-amber-500/30' }
                : { label: 'High Rejects (<95%)', color: '#ef4444', grad: 'from-rose-600 to-red-500', tagClass: 'bg-rose-500/10 text-rose-400 border-rose-500/30' });

        // Period & Line & Machine Display Text
        const selectedLine = this.filters.line_id ? (this.masterData.lines || []).find(l => l.id == this.filters.line_id)?.name : 'All Production Lines';
        let selectedMachineText = 'All Machines';
        if (this.filters.machine_id === 'all_measuring') {
            selectedMachineText = '⭐ All Measuring Machines';
        } else if (this.filters.machine_id) {
            const mcObj = (this.masterData.machines || []).find(m => m.id == this.filters.machine_id);
            selectedMachineText = mcObj ? `${mcObj.name} (${mcObj.code})` : `Machine #${this.filters.machine_id}`;
        }
        const periodText = this.filters.period ? this.filters.period.replace('_', ' ').toUpperCase() : 'LATEST DATA';

        const content = document.getElementById('content-body');
        content.innerHTML = `
            <!-- TV MODE HEADER -->
            <div class="flex flex-wrap items-center justify-between bg-slate-900 border border-slate-800 rounded-xl px-5 py-3 shadow-xl mb-4 gap-3">
                <div class="flex items-center gap-4">
                    <div class="flex items-center gap-2.5">
                        <span class="text-2xl font-black tracking-tight text-white flex items-center gap-1.5">
                            <i data-lucide="tv" class="w-6 h-6 text-cyan-400"></i>
                            <span class="text-cyan-400">OEE</span>
                            <span class="text-xs px-2 py-0.5 rounded bg-cyan-950 text-cyan-300 border border-cyan-700 font-mono font-bold tracking-wider">TV FLOOR DISPLAY</span>
                        </span>
                    </div>

                    <div class="hidden sm:flex items-center gap-2 text-xs font-mono text-slate-300 bg-slate-950 border border-slate-800 px-3 py-1 rounded-lg">
                        <span class="text-slate-500 font-sans">Line:</span>
                        <span class="text-cyan-400 font-bold">${selectedLine}</span>
                        <span class="text-slate-600">•</span>
                        <span class="text-slate-500 font-sans">Machine:</span>
                        <span class="text-emerald-400 font-bold">${selectedMachineText}</span>
                        <span class="text-slate-600">•</span>
                        <span class="text-slate-500 font-sans">Period:</span>
                        <span class="text-amber-400 font-bold">${periodText}</span>
                    </div>
                </div>

                <div class="flex items-center gap-3">
                    <span class="flex items-center gap-1.5 px-3 py-1 rounded-full bg-emerald-950/90 text-emerald-400 border border-emerald-700 text-xs font-bold tracking-wide">
                        <span class="w-2 h-2 rounded-full bg-emerald-400 animate-ping"></span> LIVE REALTIME
                    </span>

                    <button id="btn-exit-tv" class="flex items-center gap-1.5 px-3 py-1.5 rounded-lg bg-rose-950/80 hover:bg-rose-900 text-rose-300 border border-rose-800 text-xs font-bold transition-all cursor-pointer shadow-sm">
                        <i data-lucide="x" class="w-4 h-4"></i>
                        <span>Exit TV Mode</span>
                    </button>
                </div>
            </div>

            <!-- SECTION 1: MACRO KPIS OVERVIEW (4 OEE METRIC CARDS) -->
            <div class="mb-4">
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
                    <!-- CARD 1: OVERALL OEE (Dynamic: Green/Orange/Red) -->
                    <div class="relative overflow-hidden bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl flex flex-col justify-between">
                        <div class="flex items-center justify-between mb-1">
                            <div class="flex items-center gap-1.5">
                                <span class="w-2.5 h-2.5 rounded-full" style="background-color: ${oeeStatus.color}; box-shadow: 0 0 10px ${oeeStatus.color}"></span>
                                <span class="text-xs font-bold uppercase tracking-wider text-slate-300">Overall OEE</span>
                            </div>
                            ${getDeltaBadge(oeeDiff)}
                        </div>

                        <div class="my-1.5 flex items-baseline justify-between">
                            <div class="flex items-baseline space-x-1">
                                <span class="font-mono text-3xl font-extrabold tracking-tight text-white">${oeeVal.toFixed(2)}</span>
                                <span class="font-mono text-sm font-semibold text-slate-400">%</span>
                            </div>
                            <div class="text-right font-mono">
                                <span class="block text-[10px] uppercase font-bold text-slate-400">Target</span>
                                <span class="text-xs font-bold text-slate-200">85.00%</span>
                            </div>
                        </div>

                        <!-- Half-Arc Gauge Visual -->
                        <div class="relative my-0.5">
                            <div id="tv-gauge-oee" class="flex justify-center -my-1.5"></div>
                            <div class="flex justify-between items-center text-[10px] font-mono font-bold text-slate-500 mt-2 px-3">
                                <span>0%</span>
                                <span class="${oeeStatus.benchBadge} font-semibold px-1.5 py-0.5 rounded border">Benchmark: 85%</span>
                                <span>100%</span>
                            </div>
                        </div>

                        <!-- Progress Bar with Target Notch at 85% -->
                        <div class="my-2.5">
                            <div class="relative h-1.5 w-full overflow-hidden rounded-full bg-slate-950 border border-slate-800">
                                <div class="h-full rounded-full bg-gradient-to-r ${oeeStatus.grad} transition-all duration-500" style="width: ${Math.min(oeeVal, 100)}%;"></div>
                                <div class="absolute top-0 bottom-0 w-0.5 bg-white shadow-[0_0_4px_#fff]" style="left: 85%;" title="Target: 85.00%"></div>
                            </div>
                        </div>

                        <div class="pt-2.5 border-t border-slate-800/80 flex justify-between items-center text-[11px] font-mono text-slate-400">
                            <span class="text-[10px] font-bold px-1.5 py-0.5 rounded border ${oeeStatus.tagClass}">${oeeStatus.label}</span>
                            <span>Prev: <strong class="text-slate-200">${prevOee != null ? prevOee.toFixed(2) + '%' : '0.00%'}</strong></span>
                        </div>
                    </div>

                    <!-- CARD 2: AVAILABILITY (Blue Theme) -->
                    <div class="relative overflow-hidden bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl flex flex-col justify-between">
                        <div class="flex items-center justify-between mb-1">
                            <div class="flex items-center gap-1.5">
                                <span class="w-2.5 h-2.5 rounded-full" style="background-color: ${availStatus.color}; box-shadow: 0 0 10px ${availStatus.color}"></span>
                                <span class="text-xs font-bold uppercase tracking-wider text-slate-300">Availability (A)</span>
                            </div>
                            ${getDeltaBadge(availDiff)}
                        </div>

                        <div class="my-1.5 flex items-baseline justify-between">
                            <div class="flex items-baseline space-x-1">
                                <span class="font-mono text-3xl font-extrabold tracking-tight text-white">${availVal.toFixed(2)}</span>
                                <span class="font-mono text-sm font-semibold text-slate-400">%</span>
                            </div>
                            <div class="text-right font-mono">
                                <span class="block text-[10px] uppercase font-bold text-slate-400">Target</span>
                                <span class="text-xs font-bold text-slate-200">90.00%</span>
                            </div>
                        </div>

                        <!-- Half-Arc Gauge Visual -->
                        <div class="relative my-0.5">
                            <div id="tv-gauge-avail" class="flex justify-center -my-1.5"></div>
                            <div class="flex justify-between items-center text-[10px] font-mono font-bold text-slate-500 mt-2 px-3">
                                <span>0%</span>
                                <span class="text-sky-400 font-semibold bg-sky-950/40 px-1.5 py-0.5 rounded border border-sky-800/40">Benchmark: 90%</span>
                                <span>100%</span>
                            </div>
                        </div>

                        <!-- Progress Bar with Target Notch at 90% -->
                        <div class="my-2.5">
                            <div class="relative h-1.5 w-full overflow-hidden rounded-full bg-slate-950 border border-slate-800">
                                <div class="h-full rounded-full bg-gradient-to-r ${availStatus.grad} transition-all duration-500" style="width: ${Math.min(availVal, 100)}%;"></div>
                                <div class="absolute top-0 bottom-0 w-0.5 bg-white shadow-[0_0_4px_#fff]" style="left: 90%;" title="Target: 90.00%"></div>
                            </div>
                        </div>

                        <div class="pt-2.5 border-t border-slate-800/80 flex justify-between items-center text-[11px] font-mono text-slate-400">
                            <span class="text-[10px] font-bold px-1.5 py-0.5 rounded border ${availStatus.tagClass}">${availStatus.label}</span>
                            <span>Prev: <strong class="text-slate-200">${prevAvail != null ? prevAvail.toFixed(2) + '%' : '0.00%'}</strong></span>
                        </div>
                    </div>

                    <!-- CARD 3: PERFORMANCE (Green Theme) -->
                    <div class="relative overflow-hidden bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl flex flex-col justify-between">
                        <div class="flex items-center justify-between mb-1">
                            <div class="flex items-center gap-1.5">
                                <span class="w-2.5 h-2.5 rounded-full" style="background-color: ${perfStatus.color}; box-shadow: 0 0 10px ${perfStatus.color}"></span>
                                <span class="text-xs font-bold uppercase tracking-wider text-slate-300">Performance (P)</span>
                            </div>
                            ${getDeltaBadge(perfDiff)}
                        </div>

                        <div class="my-1.5 flex items-baseline justify-between">
                            <div class="flex items-baseline space-x-1">
                                <span class="font-mono text-3xl font-extrabold tracking-tight text-white">${perfVal.toFixed(2)}</span>
                                <span class="font-mono text-sm font-semibold text-slate-400">%</span>
                            </div>
                            <div class="text-right font-mono">
                                <span class="block text-[10px] uppercase font-bold text-slate-400">Target</span>
                                <span class="text-xs font-bold text-slate-200">95.00%</span>
                            </div>
                        </div>

                        <!-- Half-Arc Gauge Visual -->
                        <div class="relative my-0.5">
                            <div id="tv-gauge-perf" class="flex justify-center -my-1.5"></div>
                            <div class="flex justify-between items-center text-[10px] font-mono font-bold text-slate-500 mt-2 px-3">
                                <span>0%</span>
                                <span class="text-emerald-400 font-semibold bg-emerald-950/40 px-1.5 py-0.5 rounded border border-emerald-800/40">Benchmark: 95%</span>
                                <span>100%</span>
                            </div>
                        </div>

                        <!-- Progress Bar with Target Notch at 95% -->
                        <div class="my-2.5">
                            <div class="relative h-1.5 w-full overflow-hidden rounded-full bg-slate-950 border border-slate-800">
                                <div class="h-full rounded-full bg-gradient-to-r ${perfStatus.grad} transition-all duration-500" style="width: ${Math.min(perfVal, 100)}%;"></div>
                                <div class="absolute top-0 bottom-0 w-0.5 bg-white shadow-[0_0_4px_#fff]" style="left: 95%;" title="Target: 95.00%"></div>
                            </div>
                        </div>

                        <div class="pt-2.5 border-t border-slate-800/80 flex justify-between items-center text-[11px] font-mono text-slate-400">
                            <span class="text-[10px] font-bold px-1.5 py-0.5 rounded border ${perfStatus.tagClass}">${perfStatus.label}</span>
                            <span>Prev: <strong class="text-slate-200">${prevPerf != null ? prevPerf.toFixed(2) + '%' : '0.00%'}</strong></span>
                        </div>
                    </div>

                    <!-- CARD 4: QUALITY (Orange/Red Theme) -->
                    <div class="relative overflow-hidden bg-slate-900 border border-slate-800 rounded-xl p-4 shadow-xl flex flex-col justify-between">
                        <div class="flex items-center justify-between mb-1">
                            <div class="flex items-center gap-1.5">
                                <span class="w-2.5 h-2.5 rounded-full" style="background-color: ${qualStatus.color}; box-shadow: 0 0 10px ${qualStatus.color}"></span>
                                <span class="text-xs font-bold uppercase tracking-wider text-slate-300">Quality Rate (Q)</span>
                            </div>
                            ${getDeltaBadge(qualDiff)}
                        </div>

                        <div class="my-1.5 flex items-baseline justify-between">
                            <div class="flex items-baseline space-x-1">
                                <span class="font-mono text-3xl font-extrabold tracking-tight text-white">${qualVal.toFixed(2)}</span>
                                <span class="font-mono text-sm font-semibold text-slate-400">%</span>
                            </div>
                            <div class="text-right font-mono">
                                <span class="block text-[10px] uppercase font-bold text-slate-400">Target</span>
                                <span class="text-xs font-bold text-slate-200">99.00%</span>
                            </div>
                        </div>

                        <!-- Half-Arc Gauge Visual -->
                        <div class="relative my-0.5">
                            <div id="tv-gauge-qual" class="flex justify-center -my-1.5"></div>
                            <div class="flex justify-between items-center text-[10px] font-mono font-bold text-slate-500 mt-2 px-3">
                                <span>0%</span>
                                <span class="text-orange-400 font-semibold bg-orange-950/40 px-1.5 py-0.5 rounded border border-orange-800/40">Benchmark: 99%</span>
                                <span>100%</span>
                            </div>
                        </div>

                        <!-- Progress Bar with Target Notch at 99% -->
                        <div class="my-2.5">
                            <div class="relative h-1.5 w-full overflow-hidden rounded-full bg-slate-950 border border-slate-800">
                                <div class="h-full rounded-full bg-gradient-to-r ${qualStatus.grad} transition-all duration-500" style="width: ${Math.min(qualVal, 100)}%;"></div>
                                <div class="absolute top-0 bottom-0 w-0.5 bg-white shadow-[0_0_4px_#fff]" style="left: 99%;" title="Target: 99.00%"></div>
                            </div>
                        </div>

                        <div class="pt-2.5 border-t border-slate-800/80 flex justify-between items-center text-[11px] font-mono text-slate-400">
                            <span class="text-[10px] font-bold px-1.5 py-0.5 rounded border ${qualStatus.tagClass}">${qualStatus.label}</span>
                            <span>Prev: <strong class="text-slate-200">${prevQual != null ? prevQual.toFixed(2) + '%' : '0.00%'}</strong></span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- SECTION 2: MACHINE STATUS OVERVIEW (LEFT) + SIX BIG LOSSES (RIGHT) -->
            <div class="grid grid-cols-1 lg:grid-cols-12 gap-4 items-start">
                <!-- MACHINE STATUS OVERVIEW (7 COLS) -->
                <div class="lg:col-span-7 bg-slate-900 border border-slate-800 rounded-xl p-5 shadow-xl flex flex-col justify-between">
                    <div>
                        <div class="flex items-center justify-between pb-3 mb-3 border-b border-slate-800">
                            <div>
                                <h3 class="text-sm font-bold text-slate-100 flex items-center gap-2">
                                    <i data-lucide="cpu" class="w-4 h-4 text-cyan-400"></i>
                                    Machine Status Overview
                                </h3>
                                <p class="text-[11px] text-slate-400 mt-0.5">
                                    Live Factory Status: 
                                    <span class="text-emerald-400 font-bold">${runningCount} Running</span>, 
                                    <span class="text-amber-400 font-bold">${idleCount} Idle</span>, 
                                    <span class="text-rose-400 font-bold">${stopCount} Stop</span>
                                </p>
                            </div>
                            <div class="font-mono text-xs text-slate-400">
                                Total: <strong class="text-slate-200">${totalMachines} Machines</strong>
                            </div>
                        </div>

                        <!-- STOP / BREAKDOWN MACHINES HIGHLIGHTS ALERT (IF ANY) -->
                        ${stopCount > 0 ? `
                            <div class="mb-4 bg-rose-950/60 border-2 border-rose-500/90 rounded-xl p-3.5 shadow-lg shadow-rose-950/60">
                                <div class="flex items-center justify-between pb-2 border-b border-rose-800/80 mb-2.5">
                                    <div class="flex items-center gap-2">
                                        <span class="flex h-3 w-3 relative">
                                            <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-rose-400 opacity-75"></span>
                                            <span class="relative inline-flex rounded-full h-3 w-3 bg-rose-500"></span>
                                        </span>
                                        <span class="text-xs font-black uppercase tracking-wider text-rose-200">
                                            ATTENTION: STOPPED / BREAKDOWN MACHINES (${stopCount})
                                        </span>
                                    </div>
                                    <span class="text-[10px] font-mono font-bold text-rose-300 bg-rose-900/90 px-2 py-0.5 rounded border border-rose-700">
                                        NEEDS IMMEDIATE ACTION
                                    </span>
                                </div>
                                <div class="grid grid-cols-1 md:grid-cols-2 gap-2.5">
                                    ${stopMachines.map(m => `
                                        <div class="bg-slate-950 border-2 border-rose-500/80 rounded-lg p-2.5 flex items-center justify-between shadow-md">
                                            <div class="truncate pr-2">
                                                <div class="flex items-center gap-2 mb-1">
                                                    <span class="font-mono font-black text-xs text-rose-400">${m.machine_code}</span>
                                                    <span class="inline-flex items-center gap-1 px-1.5 py-0.5 rounded text-[10px] font-bold border border-rose-500 bg-rose-500/20 text-rose-300">
                                                        ${this.getStatusIcon(m.status)} ${m.status}
                                                    </span>
                                                </div>
                                                <div class="text-xs font-bold text-slate-200 truncate">${m.machine_name} • <span class="text-slate-400 font-normal">${m.line_name || 'N/A'}</span></div>
                                                <div class="text-[10px] text-amber-400 font-mono mt-0.5 truncate">${m.product_name || 'No Active SKU'}</div>
                                            </div>
                                            <div class="text-right font-mono flex-shrink-0">
                                                <div class="text-base font-black text-rose-400">${m.oee != null ? Number(m.oee).toFixed(1) + '%' : '0.0%'}</div>
                                                <div class="text-[10px] text-slate-400">${m.total_count || 0} pcs</div>
                                            </div>
                                        </div>
                                    `).join('')}
                                </div>
                            </div>
                        ` : ''}

                        <!-- ALL MACHINES GRID -->
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-2.5 max-h-[420px] overflow-y-auto pr-1">
                            ${machines.length === 0 ? '<div class="col-span-2 text-center text-slate-500 py-8 text-xs font-sans">No machine records found.</div>' : ''}
                            ${machines.map(m => `
                                <div class="bg-slate-950 border ${['STOP', 'STOPPED', 'BREAKDOWN'].includes((m.status || '').toUpperCase()) ? 'border-rose-600/80 bg-rose-950/20' : 'border-slate-800'} rounded-lg p-2.5 flex items-center justify-between hover:border-slate-700 transition-all shadow-sm">
                                    <div class="truncate pr-2">
                                        <div class="flex items-center gap-2 mb-1">
                                            <span class="font-mono font-bold text-xs text-cyan-400">${m.machine_code}</span>
                                            <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded text-[10px] font-bold border ${this.getMachineStatusBadge(m.status)}">
                                                ${this.getStatusIcon(m.status)}
                                                <span>${m.status}</span>
                                            </span>
                                        </div>
                                        <div class="text-xs font-semibold text-slate-200 truncate">${m.machine_name} • <span class="text-slate-400 font-normal">${m.line_name || 'N/A'}</span></div>
                                        <div class="text-[10px] text-amber-400 font-medium truncate flex items-center gap-1 mt-1 bg-amber-950/40 border border-amber-800/40 px-1.5 py-0.5 rounded w-fit max-w-full">
                                            <i data-lucide="package" class="w-3 h-3 text-amber-400 flex-shrink-0"></i>
                                            <span class="truncate">${m.product_name || 'No Active SKU'}</span>
                                        </div>
                                    </div>
                                    <div class="text-right font-mono flex-shrink-0">
                                        <div class="text-sm font-bold text-slate-100">${m.oee != null ? Number(m.oee).toFixed(2) + '%' : '0.00%'} <span class="text-[10px] text-slate-400 font-normal">OEE</span></div>
                                        <div class="text-[10px] text-slate-400 mt-0.5">${m.total_count || 0} / ${m.target_count || 0} pcs</div>
                                    </div>
                                </div>
                            `).join('')}
                        </div>
                    </div>
                </div>

                <!-- SIX BIG LOSSES BREAKDOWN (5 COLS) -->
                <div id="card-tv-six-losses" class="lg:col-span-5 bg-slate-900 border border-slate-800 rounded-xl p-5 shadow-xl flex flex-col justify-between h-fit self-start cursor-pointer group hover:border-cyan-500/60 transition-all">
                    <div>
                        <div class="flex items-center justify-between pb-3 mb-3 border-b border-slate-800">
                            <div>
                                <h3 class="text-sm font-bold text-slate-100 flex items-center gap-2 group-hover:text-cyan-400 transition-colors">
                                    <i data-lucide="pie-chart" class="w-4 h-4 text-rose-400 group-hover:scale-110 transition-transform"></i>
                                    <span>Six Big Losses Breakdown</span>
                                    <span class="text-[9px] font-mono font-bold px-1.5 py-0.5 rounded bg-cyan-950 text-cyan-400 border border-cyan-800 flex items-center gap-1 opacity-80 group-hover:opacity-100"><i data-lucide="maximize-2" class="w-3 h-3"></i> Detail</span>
                                </h3>
                                <p class="text-[11px] text-slate-400 mt-0.5">Total downtime & speed loss across 6 TPM pillars &bull; <span class="text-cyan-400 underline">Klik untuk detail</span></p>
                            </div>
                            <div id="tv-six-losses-total-badge"></div>
                        </div>

                        <div id="tv-chart-six-losses"></div>
                    </div>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();

        // Render Speedometer Gauges with Needle Indicator
        this.renderSpeedometerGauge('#tv-gauge-oee', oeeVal, oeeStatus.color);
        this.renderSpeedometerGauge('#tv-gauge-avail', availVal, availStatus.color);
        this.renderSpeedometerGauge('#tv-gauge-perf', perfVal, perfStatus.color);
        this.renderSpeedometerGauge('#tv-gauge-qual', qualVal, qualStatus.color);

        // Render Six Big Losses Pareto
        this.renderSixLossesChart(sixLosses, 'tv-chart-six-losses', 'tv-six-losses-total-badge');

        // Bind TV Six Losses Card Click
        const tvSixLossesCard = document.getElementById('card-tv-six-losses');
        if (tvSixLossesCard) {
            tvSixLossesCard.addEventListener('click', () => {
                this.showSixBigLossesDetailModal(sixLosses);
            });
        }

        const btnExit = document.getElementById('btn-exit-tv');
        if (btnExit) {
            btnExit.addEventListener('click', () => {
                this.toggleTvMode();
            });
        }

        if (this.currentLang === 'ja') {
            i18n.localizeDom(document.getElementById('content-body') || document.body);
        }
    }

    renderSpeedometerGauge(selector, value, color) {
        const el = document.querySelector(selector);
        if (!el) return;

        const isLight = this.theme === 'light';
        const numVal = Math.min(Math.max(Number(value) || 0, 0), 100);
        const arcRadius = 62;
        const arcCircumference = Math.PI * arcRadius; // ~194.78
        const arcOffset = arcCircumference * (1 - numVal / 100);
        const rotationAngle = (numVal - 50) * 1.8; // -90deg at 0%, 0deg at 50%, +90deg at 100%

        const trackBg = isLight ? '#E2E8F0' : '#1E293B';
        const textColor = isLight ? '#0F172A' : '#F8FAFC';
        const hubOuter = isLight ? '#CBD5E1' : '#334155';
        const hubInner = isLight ? '#0F172A' : '#F8FAFC';
        const gaugeId = 'gauge-' + Math.random().toString(36).substr(2, 9);

        el.innerHTML = `
            <div class="relative flex flex-col items-center justify-center w-full">
                <svg viewBox="0 0 180 96" class="w-44 h-24 overflow-visible">
                    <defs>
                        <!-- Gradient for the active arc -->
                        <linearGradient id="grad-${gaugeId}" x1="0%" y1="0%" x2="100%" y2="0%">
                            <stop offset="0%" stop-color="${color}" stop-opacity="0.65" />
                            <stop offset="100%" stop-color="${color}" stop-opacity="1" />
                        </linearGradient>
                        <!-- Needle Glow Filter -->
                        <filter id="glow-${gaugeId}" x="-20%" y="-20%" width="140%" height="140%">
                            <feDropShadow dx="0" dy="0" stdDeviation="2" flood-color="${color}" flood-opacity="0.65"/>
                        </filter>
                    </defs>

                    <!-- Background Track Arc (Half-Circle) -->
                    <path 
                        d="M 28 88 A 62 62 0 0 1 152 88" 
                        fill="none" 
                        stroke="${trackBg}" 
                        stroke-width="10" 
                        stroke-linecap="round"
                    />

                    <!-- Active Colored Value Arc -->
                    <path 
                        d="M 28 88 A 62 62 0 0 1 152 88" 
                        fill="none" 
                        stroke="url(#grad-${gaugeId})" 
                        stroke-width="10" 
                        stroke-linecap="round"
                        stroke-dasharray="${arcCircumference.toFixed(2)}"
                        stroke-dashoffset="${arcCircumference.toFixed(2)}"
                        class="gauge-arc-active"
                        style="transition: stroke-dashoffset 1.2s cubic-bezier(0.34, 1.4, 0.64, 1);"
                    />

                    <!-- Target Benchmark Tick (at 85% = +63deg from 0) -->
                    <line x1="144" y1="61" x2="152" y2="58" stroke="#FFFFFF" stroke-width="2" stroke-linecap="round" opacity="0.9" />

                    <!-- Live Value Display Inside Dial -->
                    <text x="90" y="74" text-anchor="middle" font-family="'JetBrains Mono', monospace" font-size="16" font-weight="800" fill="${textColor}">
                        ${numVal.toFixed(1)}%
                    </text>

                    <!-- Animated Needle Group (Jarum Indicator) -->
                    <g 
                        id="needle-${gaugeId}" 
                        class="gauge-needle"
                        style="transform-origin: 90px 88px; transform: rotate(-90deg); transition: transform 1.2s cubic-bezier(0.34, 1.56, 0.64, 1);"
                        filter="url(#glow-${gaugeId})"
                    >
                        <!-- Needle Blade Pointer -->
                        <polygon points="87.5,88 92.5,88 90,26" fill="${color}" />
                        <line x1="90" y1="88" x2="90" y2="28" stroke="#FFFFFF" stroke-width="1" opacity="0.85" />

                        <!-- Center Pivot Hub Outer -->
                        <circle cx="90" cy="88" r="7" fill="${hubOuter}" />
                        <!-- Center Pivot Hub Core -->
                        <circle cx="90" cy="88" r="4" fill="${hubInner}" />
                    </g>
                </svg>
            </div>
        `;

        // Animate needle sweep and arc fill after DOM injection
        requestAnimationFrame(() => {
            requestAnimationFrame(() => {
                const needle = el.querySelector(`#needle-${gaugeId}`);
                const activeArc = el.querySelector('.gauge-arc-active');
                if (needle) {
                    needle.style.transform = `rotate(${rotationAngle}deg)`;
                }
                if (activeArc) {
                    activeArc.style.strokeDashoffset = `${arcOffset.toFixed(2)}`;
                }
            });
        });
    }

    renderSparkline(selector, data, color) {
        const isLight = this.theme === 'light';
        const options = {
            series: [{ data }],
            chart: {
                type: 'area',
                height: 38,
                sparkline: { enabled: true }
            },
            stroke: { curve: 'smooth', width: 2 },
            fill: {
                type: 'gradient',
                gradient: { opacityFrom: isLight ? 0.35 : 0.45, opacityTo: 0.02 }
            },
            colors: [color],
            tooltip: { enabled: false }
        };

        const el = document.querySelector(selector);
        if (el) {
            el.innerHTML = '';
            const chart = new ApexCharts(el, options);
            chart.render();
        }
    }

    // ==========================================
    // 12. SYSTEM SETTINGS & COMPANY PROFILE PAGE
    // ==========================================
    async renderSettings() {
        const content = document.getElementById('content-body');
        if (!content) return;

        // Fetch latest profile
        let profile = this.companyProfile;
        try {
            const res = await api.getCompanyProfile();
            if (res.data?.data) {
                profile = res.data.data;
                this.companyProfile = profile;
                this.applyCompanyBranding();
            }
        } catch (e) {
            console.error('Error fetching company profile:', e);
        }

        content.innerHTML = `
            <div class="space-y-6">
                <!-- TOP HEADER -->
                <div class="flex flex-wrap items-center justify-between gap-4">
                    <div>
                        <h2 class="text-xl font-bold text-white flex items-center gap-2">
                            <i data-lucide="settings" class="w-5 h-5 text-cyan-400"></i>
                            <span>System Settings</span>
                        </h2>
                    </div>

                    <!-- TAB NAVIGATION SWITCHER -->
                    <div class="flex items-center gap-1.5 bg-slate-900 border border-slate-800 p-1 rounded-xl">
                        <button id="btn-tab-setting-company" class="tab-setting-btn px-3.5 py-1.5 rounded-lg text-xs font-bold transition-all bg-cyan-600 text-white shadow-md shadow-cyan-600/20 flex items-center gap-1.5 cursor-pointer">
                            <i data-lucide="building-2" class="w-3.5 h-3.5"></i>
                            <span>Profil Perusahaan & Kop</span>
                        </button>
                        <button id="btn-tab-setting-oee" class="tab-setting-btn px-3.5 py-1.5 rounded-lg text-xs font-medium transition-all text-slate-400 hover:text-slate-200 flex items-center gap-1.5 cursor-pointer">
                            <i data-lucide="target" class="w-3.5 h-3.5"></i>
                            <span>Target OEE & Standar</span>
                        </button>
                        <button id="btn-tab-setting-system" class="tab-setting-btn px-3.5 py-1.5 rounded-lg text-xs font-medium transition-all text-slate-400 hover:text-slate-200 flex items-center gap-1.5 cursor-pointer">
                            <i data-lucide="sliders" class="w-3.5 h-3.5"></i>
                            <span>Sistem & Tampilan</span>
                        </button>
                    </div>
                </div>

                <!-- TAB 1: PROFIL PERUSAHAAN & KOP LAPORAN -->
                <div id="setting-pane-company" class="setting-pane space-y-6">
                    <!-- LIVE KOP LAPORAN PREVIEW CARD -->
                    <div class="bg-slate-900 border border-slate-800 rounded-2xl p-5 shadow-xl">
                        <div class="flex items-center justify-between border-b border-slate-800 pb-3 mb-4">
                            <div>
                                <h3 class="text-sm font-bold text-slate-100 flex items-center gap-2">
                                    <i data-lucide="eye" class="w-4 h-4 text-cyan-400"></i>
                                    Preview Kop Laporan
                                </h3>
                            </div>
                            <span class="px-2.5 py-1 rounded-full text-[10px] font-mono font-bold bg-cyan-950 text-cyan-400 border border-cyan-800">
                                REAL-TIME PREVIEW
                            </span>
                        </div>

                        <!-- LIVE LETTERHEAD BOX (SIMULASI KERTAS DOKUMEN CETAK) -->
                        <div class="bg-white text-slate-900 rounded-xl p-4 shadow-md font-sans border border-slate-300">
                            <div class="flex items-center justify-between pb-3 mb-2 border-b-2 border-slate-900 gap-3">
                                <div class="flex items-center gap-3.5 min-w-0">
                                    <div id="preview-logo-box" class="w-16 h-12 flex items-center justify-center flex-shrink-0 bg-slate-50 border border-slate-200 rounded p-1">
                                        ${profile.company_logo 
                                            ? `<img id="preview-logo-img" src="${this.escapeHtml(profile.company_logo)}" class="max-h-full max-w-full object-contain" alt="Logo" />` 
                                            : `<div id="preview-logo-placeholder" class="text-[9px] font-bold text-slate-400 text-center font-mono">NO LOGO</div>`
                                        }
                                    </div>
                                    <div class="min-w-0">
                                        <div id="preview-company-name" class="text-xs font-black uppercase text-slate-950 tracking-wider">${profile.company_name || 'PT. CONNECTING ROD INDONESIA'}</div>
                                        <div id="preview-plant-name" class="text-[10px] font-bold text-slate-800">${profile.plant_name || 'CONNECTING ROD MANUFACTURE'} • <span id="preview-plant-code">${profile.plant_code || 'PLT-01'}</span></div>
                                        <div id="preview-address" class="text-[9px] text-slate-600 truncate max-w-md">${profile.address || 'Kawasan Industri Cikarang, Bekasi, Jawa Barat'}</div>
                                        <div id="preview-contact" class="text-[8.5px] text-slate-500 font-mono">
                                            Telp: <span id="preview-phone">${profile.phone || '+62 21 8990 1234'}</span> | Email: <span id="preview-email">${profile.email || 'production.mes@connectingrod.co.id'}</span> | Web: <span id="preview-web">${profile.website || 'www.connectingrod-mfg.co.id'}</span>
                                        </div>
                                    </div>
                                </div>
                                <div class="text-right flex-shrink-0">
                                    <div class="text-[10.5px] font-black uppercase text-slate-950">LAPORAN KINERJA PRODUKSI & OEE</div>
                                    <div class="text-[9px] font-mono text-slate-700 font-bold">Doc ID: <span id="preview-doc-id">${profile.doc_prefix || 'MES-OEE'}</span>-2026-08-28</div>
                                    <div class="text-[8px] text-slate-500 font-mono">Form: A4-Portrait Shift Handover Protocol</div>
                                </div>
                            </div>
                            <div class="h-0.5 border-t border-slate-900"></div>
                            <div class="py-2 text-[9.5px] text-slate-400 font-mono italic text-center">
                                [ Isi Konten Laporan: Ringkasan OEE, Matriks Produksi Seluruh Lini, Six Big Losses, Trouble Log, & Tanda Tangan ]
                            </div>
                        </div>
                    </div>

                    <!-- LOGO UPLOADER & FORM METADATA PERUSAHAAN (GRID 4:8) -->
                    <div class="grid grid-cols-1 lg:grid-cols-12 gap-6">
                        <!-- KOLOM KIRI: UPLOAD LOGO PERUSAHAAN (4 COLS) -->
                        <div class="lg:col-span-4 bg-slate-900 border border-slate-800 rounded-2xl p-5 shadow-xl flex flex-col justify-between">
                            <div>
                                <h3 class="text-sm font-bold text-slate-100 flex items-center gap-2 mb-1">
                                    <i data-lucide="image" class="w-4 h-4 text-cyan-400"></i>
                                    Upload Logo Perusahaan
                                </h3>
                                <p class="text-xs text-slate-400 mb-4">Logo akan digunakan pada Kop Surat Laporan, Sidebar Navigasi, dan Title Bar.</p>

                                <!-- DRAG & DROP UPLOAD BOX -->
                                <div id="logo-drop-zone" class="border-2 border-dashed border-slate-700 hover:border-cyan-500 rounded-2xl p-4 text-center cursor-pointer transition-all bg-slate-950/60 group">
                                    <input type="file" id="input-logo-file" accept="image/png, image/jpeg, image/svg+xml, image/webp" class="hidden" />
                                    
                                    <div id="logo-display-box" class="flex flex-col items-center justify-center py-3">
                                        ${profile.company_logo ? `
                                            <div class="relative group/img mb-3">
                                                <img id="form-logo-thumb" src="${this.escapeHtml(profile.company_logo)}" class="max-h-24 max-w-48 object-contain rounded-lg p-2 bg-slate-900 border border-slate-800 shadow-md" alt="Company Logo" />
                                            </div>
                                            <span class="text-xs font-bold text-emerald-400 flex items-center gap-1">
                                                <i data-lucide="check" class="w-3.5 h-3.5"></i> Logo Aktif Terpasang
                                            </span>
                                            <span class="text-[10px] text-slate-500 mt-1">Klik atau drag untuk mengganti logo</span>
                                        ` : `
                                            <div class="w-12 h-12 rounded-xl bg-cyan-950/60 border border-cyan-800/80 text-cyan-400 flex items-center justify-center mb-2.5 group-hover:scale-110 transition-transform">
                                                <i data-lucide="upload-cloud" class="w-6 h-6"></i>
                                            </div>
                                            <div class="text-xs font-bold text-slate-200">Klik / Tarik File Logo ke Sini</div>
                                            <span class="text-[10px] text-slate-500 mt-0.5">Format: PNG, JPG, SVG, WebP (Maks. 2MB)</span>
                                        `}
                                    </div>
                                </div>

                                <div class="mt-3 flex gap-2">
                                    <button type="button" id="btn-trigger-upload" class="flex-1 py-2 px-3 bg-cyan-600/20 hover:bg-cyan-600/30 text-cyan-400 border border-cyan-500/30 rounded-xl text-xs font-bold flex items-center justify-center gap-1.5 cursor-pointer transition-colors">
                                        <i data-lucide="folder-open" class="w-3.5 h-3.5"></i>
                                        Pilih Gambar
                                    </button>
                                    <button type="button" id="btn-remove-logo" class="py-2 px-3 bg-rose-600/20 hover:bg-rose-600/30 text-rose-400 border border-rose-500/30 rounded-xl text-xs font-bold flex items-center justify-center gap-1.5 cursor-pointer transition-colors" title="Hapus Logo">
                                        <i data-lucide="trash-2" class="w-3.5 h-3.5"></i>
                                        Reset
                                    </button>
                                </div>
                            </div>

                            <div class="mt-4 p-3 bg-slate-950 border border-slate-800 rounded-xl text-[11px] text-slate-400 font-sans space-y-1">
                                <div class="font-bold text-slate-300 flex items-center gap-1">
                                    <i data-lucide="info" class="w-3 h-3 text-cyan-400"></i> Rekomendasi Logo:
                                </div>
                                <div>• Gunakan format PNG transparan atau SVG.</div>
                                <div>• Resolusi ideal: 400x120 piksel (Horizontal).</div>
                            </div>
                        </div>

                        <!-- KOLOM KANAN: FORM METADATA PERUSAHAAN (8 COLS) -->
                        <div class="lg:col-span-8 bg-slate-900 border border-slate-800 rounded-2xl p-5 shadow-xl">
                            <form id="form-company-profile" class="space-y-4">
                                <div class="border-b border-slate-800 pb-3">
                                    <h3 class="text-sm font-bold text-slate-100 flex items-center gap-2">
                                        <i data-lucide="file-badge-2" class="w-4 h-4 text-emerald-400"></i>
                                        Informasi Legal & Operasional Pabrik
                                    </h3>
                                    <p class="text-xs text-slate-400">Data ini akan menjadi kop resmi pada seluruh pelaporan hasil produksi dan kalkulasi OEE.</p>
                                </div>

                                <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
                                    <div>
                                        <label class="block text-xs text-slate-300 mb-1 font-medium">Nama Perusahaan (PT / Corporate) <span class="text-rose-400">*</span></label>
                                        <input type="text" id="input-comp-name" name="company_name" value="${profile.company_name || ''}" required class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-2 text-xs text-slate-100 focus:outline-none focus:border-cyan-500 font-bold" />
                                    </div>
                                    <div>
                                        <label class="block text-xs text-slate-300 mb-1 font-medium">Tagline / Bidang Manufaktur</label>
                                        <input type="text" id="input-comp-tagline" name="company_tagline" value="${profile.company_tagline || ''}" class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-2 text-xs text-slate-100 focus:outline-none focus:border-cyan-500" />
                                    </div>
                                </div>

                                <div class="grid grid-cols-1 md:grid-cols-3 gap-3">
                                    <div>
                                        <label class="block text-xs text-slate-300 mb-1 font-medium">Nama Plant / Pabrik <span class="text-rose-400">*</span></label>
                                        <input type="text" id="input-plant-name" name="plant_name" value="${profile.plant_name || ''}" required class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-2 text-xs text-slate-100 focus:outline-none focus:border-cyan-500 font-bold" />
                                    </div>
                                    <div>
                                        <label class="block text-xs text-slate-300 mb-1 font-medium">Kode Plant (Site ID) <span class="text-rose-400">*</span></label>
                                        <input type="text" id="input-plant-code" name="plant_code" value="${profile.plant_code || ''}" required class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-2 text-xs text-cyan-400 font-mono font-bold focus:outline-none focus:border-cyan-500" />
                                    </div>
                                    <div>
                                        <label class="block text-xs text-slate-300 mb-1 font-medium">Lokasi Kawasan Industri</label>
                                        <input type="text" id="input-plant-loc" name="plant_location" value="${profile.plant_location || ''}" class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-2 text-xs text-slate-100 focus:outline-none focus:border-cyan-500" />
                                    </div>
                                </div>

                                <div>
                                    <label class="block text-xs text-slate-300 mb-1 font-medium">Alamat Lengkap Pabrik</label>
                                    <textarea id="input-address" name="address" rows="2" class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-2 text-xs text-slate-100 focus:outline-none focus:border-cyan-500 font-sans">${profile.address || ''}</textarea>
                                </div>

                                <div class="grid grid-cols-1 md:grid-cols-3 gap-3">
                                    <div>
                                        <label class="block text-xs text-slate-300 mb-1 font-medium">No. Telepon / Hunting</label>
                                        <input type="text" id="input-phone" name="phone" value="${profile.phone || ''}" class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-2 text-xs text-slate-100 focus:outline-none focus:border-cyan-500 font-mono" />
                                    </div>
                                    <div>
                                        <label class="block text-xs text-slate-300 mb-1 font-medium">Email Departemen Produksi</label>
                                        <input type="email" id="input-email" name="email" value="${profile.email || ''}" class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-2 text-xs text-slate-100 focus:outline-none focus:border-cyan-500 font-mono" />
                                    </div>
                                    <div>
                                        <label class="block text-xs text-slate-300 mb-1 font-medium">Website Perusahaan</label>
                                        <input type="text" id="input-website" name="website" value="${profile.website || ''}" class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-2 text-xs text-slate-100 focus:outline-none focus:border-cyan-500 font-mono" />
                                    </div>
                                </div>

                                <div class="grid grid-cols-1 md:grid-cols-2 gap-3 pt-2 border-t border-slate-800">
                                    <div>
                                        <label class="block text-xs text-slate-300 mb-1 font-medium">Prefix Kode Dokumen Laporan</label>
                                        <input type="text" id="input-doc-prefix" name="doc_prefix" value="${profile.doc_prefix || 'MES-OEE'}" class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-2 text-xs text-amber-400 font-mono font-bold focus:outline-none focus:border-cyan-500" />
                                        <span class="text-[10px] text-slate-500">Contoh output: MES-OEE-2026-08-28</span>
                                    </div>
                                    <div>
                                        <label class="block text-xs text-slate-300 mb-1 font-medium">Judul Aplikasi (Browser Title Bar)</label>
                                        <input type="text" id="input-app-title" name="app_title" value="${profile.app_title || 'LinePulse | Smart Production Performance Monitoring'}" class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-2 text-xs text-slate-100 focus:outline-none focus:border-cyan-500" />
                                    </div>
                                </div>

                                <div class="pt-3 flex justify-end gap-3 border-t border-slate-800">
                                    <button type="submit" id="btn-save-company-profile" class="px-6 py-2.5 bg-emerald-600 hover:bg-emerald-500 text-white font-bold rounded-xl shadow-lg shadow-emerald-600/30 flex items-center gap-2 cursor-pointer transition-all text-xs">
                                        <i data-lucide="save" class="w-4 h-4"></i>
                                        <span>Simpan Profil Perusahaan & Kop Surat</span>
                                    </button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>

                <!-- TAB 2: BASELINE TARGET OEE -->
                <div id="setting-pane-oee" class="setting-pane hidden space-y-6">
                    <div class="bg-slate-900 border border-slate-800 rounded-2xl p-5 shadow-xl">
                        <div class="border-b border-slate-800 pb-3 mb-4">
                            <h3 class="text-sm font-bold text-slate-100 flex items-center gap-2">
                                <i data-lucide="gauge" class="w-4 h-4 text-amber-400"></i>
                                Standar Baseline Target OEE (World Class TPM Metrics)
                            </h3>
                            <p class="text-xs text-slate-400">Konfigurasi target indikator utama yang digunakan pada gauge jarum, kartu KPI makro, dan deviasi laporan.</p>
                        </div>

                        <form id="form-oee-settings" class="space-y-4 max-w-2xl">
                            <div class="grid grid-cols-2 gap-4">
                                <div>
                                    <label class="block text-xs text-slate-300 mb-1 font-medium">Target OEE Baseline (%)</label>
                                    <input type="number" step="0.1" name="target_oee" value="85.0" class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-2 text-xs font-mono font-bold text-emerald-400" />
                                    <span class="text-[10px] text-slate-500">Standar World Class OEE: 85.0%</span>
                                </div>
                                <div>
                                    <label class="block text-xs text-slate-300 mb-1 font-medium">Target Availability (%)</label>
                                    <input type="number" step="0.1" name="target_avail" value="90.0" class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-2 text-xs font-mono font-bold text-cyan-400" />
                                    <span class="text-[10px] text-slate-500">Standar Ketersediaan Mesin: 90.0%</span>
                                </div>
                            </div>
                            <div class="grid grid-cols-2 gap-4">
                                <div>
                                    <label class="block text-xs text-slate-300 mb-1 font-medium">Target Performance (%)</label>
                                    <input type="number" step="0.1" name="target_perf" value="95.0" class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-2 text-xs font-mono font-bold text-amber-400" />
                                    <span class="text-[10px] text-slate-500">Standar Efisiensi Kecepatan: 95.0%</span>
                                </div>
                                <div>
                                    <label class="block text-xs text-slate-300 mb-1 font-medium">Target Quality Rate (%)</label>
                                    <input type="number" step="0.1" name="target_qual" value="99.0" class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-2 text-xs font-mono font-bold text-purple-400" />
                                    <span class="text-[10px] text-slate-500">Standar Mutu (Zero Defect): 99.0%</span>
                                </div>
                            </div>
                            <div class="pt-3 flex justify-end">
                                <button type="submit" class="px-5 py-2 bg-amber-600 hover:bg-amber-500 text-white font-bold rounded-xl text-xs flex items-center gap-2 cursor-pointer shadow-md shadow-amber-600/20">
                                    <i data-lucide="save" class="w-4 h-4"></i> Simpan Parameter Target
                                </button>
                            </div>
                        </form>
                    </div>
                </div>

                <!-- TAB 3: SISTEM & TAMPILAN -->
                <div id="setting-pane-system" class="setting-pane hidden space-y-6">
                    <div class="bg-slate-900 border border-slate-800 rounded-2xl p-5 shadow-xl">
                        <div class="border-b border-slate-800 pb-3 mb-4">
                            <h3 class="text-sm font-bold text-slate-100 flex items-center gap-2">
                                <i data-lucide="sliders" class="w-4 h-4 text-purple-400"></i>
                                Preferensi Antarmuka & Interval Auto-Refresh
                            </h3>
                            <p class="text-xs text-slate-400">Pengaturan tema visual, interval pembacaan sensor data, dan notifikasi suara sistem.</p>
                        </div>

                        <div class="space-y-4 max-w-xl text-xs">
                            <div class="flex items-center justify-between p-3 bg-slate-950 border border-slate-800 rounded-xl">
                                <div>
                                    <div class="font-bold text-slate-200">Mode Tampilan Dashboard</div>
                                    <div class="text-slate-400 text-[11px]">Pilih skema warna antarmuka aplikasi.</div>
                                </div>
                                <select id="setting-theme-select" class="bg-slate-900 border border-slate-800 text-slate-200 rounded-lg px-2.5 py-1.5">
                                    <option value="dark" ${this.theme === 'dark' ? 'selected' : ''}>Dark Industrial (Default)</option>
                                    <option value="light" ${this.theme === 'light' ? 'selected' : ''}>Light Clean Paper</option>
                                </select>
                            </div>

                            <div class="flex items-center justify-between p-3 bg-slate-950 border border-slate-800 rounded-xl">
                                <div>
                                    <div class="font-bold text-slate-200">Interval Auto-Refresh Telemetri</div>
                                    <div class="text-slate-400 text-[11px]">Frekuensi pembaruan data OEE secara otomatis.</div>
                                </div>
                                <select id="setting-refresh-select" class="bg-slate-900 border border-slate-800 text-slate-200 rounded-lg px-2.5 py-1.5 font-mono">
                                    <option value="15000">Setiap 15 Detik</option>
                                    <option value="30000" selected>Setiap 30 Detik (Standar)</option>
                                    <option value="60000">Setiap 1 Menit</option>
                                    <option value="0">Nonaktif (Manual)</option>
                                </select>
                            </div>

                            <div class="flex items-center justify-between p-3 bg-slate-950 border border-slate-800 rounded-xl">
                                <div>
                                    <div class="font-bold text-slate-200">Sound Effects & Notifikasi Suara</div>
                                    <div class="text-slate-400 text-[11px]">Memutar audio konfirmasi saat transaksi data berhasil disimpan.</div>
                                </div>
                                <input type="checkbox" checked class="w-4 h-4 rounded text-cyan-500 bg-slate-900 border-slate-700 cursor-pointer" />
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();

        // 1. Tab Switcher Logic
        const tabCompany = document.getElementById('btn-tab-setting-company');
        const tabOee = document.getElementById('btn-tab-setting-oee');
        const tabSystem = document.getElementById('btn-tab-setting-system');

        const paneCompany = document.getElementById('setting-pane-company');
        const paneOee = document.getElementById('setting-pane-oee');
        const paneSystem = document.getElementById('setting-pane-system');

        const switchSettingTab = (activeBtn, activePane) => {
            [tabCompany, tabOee, tabSystem].forEach(b => {
                b.className = 'tab-setting-btn px-3.5 py-1.5 rounded-lg text-xs font-medium transition-all text-slate-400 hover:text-slate-200 flex items-center gap-1.5 cursor-pointer';
            });
            [paneCompany, paneOee, paneSystem].forEach(p => p.classList.add('hidden'));

            activeBtn.className = 'tab-setting-btn px-3.5 py-1.5 rounded-lg text-xs font-bold transition-all bg-cyan-600 text-white shadow-md shadow-cyan-600/20 flex items-center gap-1.5 cursor-pointer';
            activePane.classList.remove('hidden');
        };

        if (tabCompany) tabCompany.addEventListener('click', () => switchSettingTab(tabCompany, paneCompany));
        if (tabOee) tabOee.addEventListener('click', () => switchSettingTab(tabOee, paneOee));
        if (tabSystem) tabSystem.addEventListener('click', () => switchSettingTab(tabSystem, paneSystem));

        // 2. Real-time Live Kop Preview Typing Sync
        const syncKopPreview = () => {
            const cName = document.getElementById('input-comp-name')?.value || 'PT. CONNECTING ROD INDONESIA';
            const pName = document.getElementById('input-plant-name')?.value || 'CONNECTING ROD MANUFACTURE';
            const pCode = document.getElementById('input-plant-code')?.value || 'PLT-01';
            const addr = document.getElementById('input-address')?.value || 'Kawasan Industri Cikarang, Bekasi';
            const phone = document.getElementById('input-phone')?.value || '-';
            const email = document.getElementById('input-email')?.value || '-';
            const web = document.getElementById('input-website')?.value || '-';
            const docPrefix = document.getElementById('input-doc-prefix')?.value || 'MES-OEE';

            const prevCName = document.getElementById('preview-company-name');
            const prevPName = document.getElementById('preview-plant-name');
            const prevPCode = document.getElementById('preview-plant-code');
            const prevAddr = document.getElementById('preview-address');
            const prevPhone = document.getElementById('preview-phone');
            const prevEmail = document.getElementById('preview-email');
            const prevWeb = document.getElementById('preview-web');
            const prevDocId = document.getElementById('preview-doc-id');

            if (prevCName) prevCName.textContent = cName;
            if (prevPName) prevPName.innerHTML = `${pName} • <span id="preview-plant-code">${pCode}</span>`;
            if (prevAddr) prevAddr.textContent = addr;
            if (prevPhone) prevPhone.textContent = phone;
            if (prevEmail) prevEmail.textContent = email;
            if (prevWeb) prevWeb.textContent = web;
            if (prevDocId) prevDocId.textContent = docPrefix;
        };

        ['input-comp-name', 'input-plant-name', 'input-plant-code', 'input-address', 'input-phone', 'input-email', 'input-website', 'input-doc-prefix'].forEach(id => {
            const el = document.getElementById(id);
            if (el) el.addEventListener('input', syncKopPreview);
        });

        // 3. Logo Upload Handling (Base64 File Reader)
        let activeBase64Logo = profile.company_logo || '';

        const fileInput = document.getElementById('input-logo-file');
        const btnTrigger = document.getElementById('btn-trigger-upload');
        const btnRemove = document.getElementById('btn-remove-logo');
        const dropZone = document.getElementById('logo-drop-zone');

        if (btnTrigger && fileInput) {
            btnTrigger.addEventListener('click', () => fileInput.click());
        }
        if (dropZone && fileInput) {
            dropZone.addEventListener('click', () => fileInput.click());
            dropZone.addEventListener('dragover', (e) => {
                e.preventDefault();
                dropZone.classList.add('border-cyan-400', 'bg-cyan-950/20');
            });
            dropZone.addEventListener('dragleave', () => {
                dropZone.classList.remove('border-cyan-400', 'bg-cyan-950/20');
            });
            dropZone.addEventListener('drop', (e) => {
                e.preventDefault();
                dropZone.classList.remove('border-cyan-400', 'bg-cyan-950/20');
                if (e.dataTransfer.files && e.dataTransfer.files[0]) {
                    handleLogoFile(e.dataTransfer.files[0]);
                }
            });
        }

        const handleLogoFile = (file) => {
            if (!file) return;
            if (!file.type.match(/image\/(png|jpeg|jpg|svg\+xml|webp)/)) {
                this.showNotification('Format Tidak Didukung', 'Harap pilih file gambar PNG, JPG, SVG, atau WebP.', 'delete');
                return;
            }
            if (file.size > 3 * 1024 * 1024) {
                this.showNotification('Ukuran Terlalu Besar', 'Maksimal ukuran file logo adalah 3MB.', 'delete');
                return;
            }

            const reader = new FileReader();
            reader.onload = (e) => {
                activeBase64Logo = e.target.result;
                
                // Update Logo Display Box in Form
                const displayBox = document.getElementById('logo-display-box');
                if (displayBox) {
                    displayBox.innerHTML = `
                        <div class="relative group/img mb-3">
                            <img id="form-logo-thumb" src="${this.escapeHtml(activeBase64Logo)}" class="max-h-24 max-w-48 object-contain rounded-lg p-2 bg-slate-900 border border-slate-800 shadow-md" alt="Company Logo" />
                        </div>
                        <span class="text-xs font-bold text-emerald-400 flex items-center gap-1">
                            <i data-lucide="check" class="w-3.5 h-3.5"></i> Logo Siap Disimpan
                        </span>
                        <span class="text-[10px] text-slate-500 mt-1">Klik atau drag untuk mengganti logo</span>
                    `;
                }

                // Update Preview Letterhead Box
                const previewBox = document.getElementById('preview-logo-box');
                if (previewBox) {
                    previewBox.innerHTML = `<img id="preview-logo-img" src="${this.escapeHtml(activeBase64Logo)}" class="max-h-full max-w-full object-contain" alt="Logo" />`;
                }

                if (window.lucide) window.lucide.createIcons();
            };
            reader.readAsDataURL(file);
        };

        if (fileInput) {
            fileInput.addEventListener('change', (e) => {
                if (e.target.files && e.target.files[0]) {
                    handleLogoFile(e.target.files[0]);
                }
            });
        }

        if (btnRemove) {
            btnRemove.addEventListener('click', (e) => {
                e.stopPropagation();
                activeBase64Logo = '';
                if (fileInput) fileInput.value = '';

                const displayBox = document.getElementById('logo-display-box');
                if (displayBox) {
                    displayBox.innerHTML = `
                        <div class="w-12 h-12 rounded-xl bg-cyan-950/60 border border-cyan-800/80 text-cyan-400 flex items-center justify-center mb-2.5">
                            <i data-lucide="upload-cloud" class="w-6 h-6"></i>
                        </div>
                        <div class="text-xs font-bold text-slate-200">Klik / Tarik File Logo ke Sini</div>
                        <span class="text-[10px] text-slate-500 mt-0.5">Format: PNG, JPG, SVG, WebP (Maks. 2MB)</span>
                    `;
                }
                const previewBox = document.getElementById('preview-logo-box');
                if (previewBox) {
                    previewBox.innerHTML = `<div id="preview-logo-placeholder" class="text-[9px] font-bold text-slate-400 text-center font-mono">NO LOGO</div>`;
                }
                if (window.lucide) window.lucide.createIcons();
            });
        }

        // 4. Form Submit: Save Company Profile
        const formProfile = document.getElementById('form-company-profile');
        if (formProfile) {
            formProfile.addEventListener('submit', async (e) => {
                e.preventDefault();
                const formData = new FormData(formProfile);
                const payload = Object.fromEntries(formData.entries());
                payload.company_logo = activeBase64Logo;
                payload.letterhead_enabled = true;

                const submitBtn = document.getElementById('btn-save-company-profile');
                if (submitBtn) {
                    submitBtn.disabled = true;
                    submitBtn.innerHTML = `<div class="w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></div> Menyimpan...`;
                }

                try {
                    const saveRes = await api.saveCompanyProfile(payload);
                    this.companyProfile = saveRes.data.data;
                    this.applyCompanyBranding();
                    this.playClingSound();
                    this.showNotification('Profil Perusahaan Tersimpan', 'Informasi kop surat dan logo perusahaan berhasil diperbarui ke seluruh sistem.', 'update');
                } catch (err) {
                    console.error('Error saving company profile:', err);
                    this.showNotification('Gagal Menyimpan', err.response?.data?.message || 'Terjadi kesalahan saat menyimpan profil.', 'delete');
                } finally {
                    if (submitBtn) {
                        submitBtn.disabled = false;
                        submitBtn.innerHTML = `<i data-lucide="save" class="w-4 h-4"></i> <span>Simpan Profil Perusahaan & Kop Surat</span>`;
                        if (window.lucide) window.lucide.createIcons();
                    }
                }
            });
        }

        // 5. Theme and System Settings Handler
        const themeSelect = document.getElementById('setting-theme-select');
        if (themeSelect) {
            themeSelect.addEventListener('change', (e) => {
                this.theme = e.target.value;
                this.applyTheme();
                this.showNotification('Tema Diperbarui', `Tema aplikasi diubah ke ${this.theme === 'dark' ? 'Dark Mode' : 'Light Mode'}.`, 'status');
            });
        }

        if (this.currentLang === 'ja') {
            i18n.localizeDom(document.getElementById('content-body') || document.body);
        }
    }

    // ==========================================
    // 13. DATABASE MANAGEMENT & GO-LIVE CLEANUP MODULE
    // ==========================================
    async renderDatabaseManagement() {
        const content = document.getElementById('content-body');
        if (!content) return;

        const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');

        content.innerHTML = `
            <div class="h-96 flex items-center justify-center">
                <div class="flex flex-col items-center gap-3">
                    <div class="w-10 h-10 border-4 border-cyan-500 border-t-transparent rounded-full animate-spin"></div>
                    <span class="text-xs text-slate-400 font-mono">Memuat status database & environment...</span>
                </div>
            </div>
        `;

        let dbData = null;
        try {
            const res = await api.getDatabaseInfo();
            dbData = res.data?.data;
        } catch (err) {
            console.error('Error loading database info:', err);
            content.innerHTML = `
                <div class="p-6 bg-rose-900/20 border border-rose-800/50 rounded-2xl text-rose-300">
                    <h3 class="font-bold text-sm flex items-center gap-2">
                        <i data-lucide="alert-triangle" class="w-4 h-4"></i>
                        Gagal Mengambil Diagnostik Database
                    </h3>
                    <p class="text-xs mt-1 text-slate-400">${err.response?.data?.message || err.message}</p>
                    <button id="btn-retry-db-info" class="mt-4 px-4 py-2 bg-cyan-600 hover:bg-cyan-500 text-white rounded-xl text-xs font-bold cursor-pointer transition-all">
                        Coba Lagi
                    </button>
                </div>
            `;
            if (window.lucide) window.lucide.createIcons();
            document.getElementById('btn-retry-db-info')?.addEventListener('click', () => this.renderDatabaseManagement());
            return;
        }

        const db = dbData.database || {};
        const env = dbData.environment || {};
        const tables = dbData.tables || { transactional: [], master: [] };

        content.innerHTML = `
            <div class="space-y-6">
                <!-- TOP HEADER -->
                <div class="flex flex-col md:flex-row md:items-center justify-between gap-4 pb-2 border-b ${isLight ? 'border-slate-200' : 'border-slate-800/80'}">
                    <div>
                        <div class="flex items-center gap-2.5">
                            <div class="w-9 h-9 rounded-xl ${isLight ? 'bg-indigo-100 text-indigo-700 border-indigo-200' : 'bg-gradient-to-br from-indigo-950 to-blue-950 text-indigo-400 border-indigo-800/80'} border flex items-center justify-center shadow-inner">
                                <i data-lucide="hard-drive" class="w-5 h-5"></i>
                            </div>
                            <div>
                                <h2 class="text-lg font-bold ${isLight ? 'text-slate-900' : 'text-white'} flex items-center gap-2">
                                    Manajemen Database & Pembersihan Go-Live
                                </h2>
                                <p class="text-xs ${isLight ? 'text-slate-500' : 'text-slate-400'} mt-0.5">
                                    Diagnostik spesifikasi teknis database, informasi server runtime, dan pembersihan massal data uji coba untuk persiapan Live.
                                </p>
                            </div>
                        </div>
                    </div>

                    <!-- ACTION SHORTCUT BUTTONS -->
                    <div class="flex flex-wrap items-center gap-2.5">
                        <button id="btn-export-db-backup" class="px-3.5 py-2 bg-indigo-600/20 hover:bg-indigo-600/30 text-indigo-300 border border-indigo-500/40 rounded-xl text-xs font-bold flex items-center gap-2 cursor-pointer transition-all shadow-sm">
                            <i data-lucide="download" class="w-4 h-4"></i>
                            <span>Export Backup (JSON)</span>
                        </button>
                        <button id="btn-optimize-database" class="px-3.5 py-2 bg-cyan-600/20 hover:bg-cyan-600/30 text-cyan-300 border border-cyan-500/40 rounded-xl text-xs font-bold flex items-center gap-2 cursor-pointer transition-all shadow-sm">
                            <i data-lucide="zap" class="w-4 h-4"></i>
                            <span>Optimasi & Defrag DB</span>
                        </button>
                        <button id="btn-refresh-db-info" class="p-2 ${isLight ? 'bg-slate-100 hover:bg-slate-200 text-slate-700 border-slate-300' : 'bg-slate-900 hover:bg-slate-800 text-slate-400 border-slate-800'} border rounded-xl cursor-pointer transition-all" title="Refresh Informasi Database">
                            <i data-lucide="rotate-cw" class="w-4 h-4"></i>
                        </button>
                    </div>
                </div>

                <!-- TOP 4 TECHNICAL DIAGNOSTICS TILES -->
                <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
                    <!-- TILE 1: DATABASE ENGINE & HOST -->
                    <div class="${isLight ? 'bg-white border-slate-200 shadow-md' : 'bg-slate-900/80 border-slate-800 shadow-xl'} border rounded-2xl p-4.5 space-y-2">
                        <div class="flex items-center justify-between">
                            <span class="text-[10px] font-bold uppercase tracking-wider text-slate-400 font-mono flex items-center gap-1.5">
                                <i data-lucide="database" class="w-3.5 h-3.5 text-cyan-400"></i>
                                Database Engine
                            </span>
                            <span class="px-2 py-0.5 rounded-full text-[9px] font-mono font-bold bg-cyan-950 text-cyan-400 border border-cyan-800">
                                ACTIVE
                            </span>
                        </div>
                        <div class="text-base font-extrabold ${isLight ? 'text-slate-900' : 'text-slate-100'} truncate" title="${db.server_version || 'MySQL / MariaDB'}">
                            ${db.driver ? db.driver.toUpperCase() : 'MYSQL'} / ${(db.server_version || 'MariaDB').split('-')[0]}
                        </div>
                        <div class="text-xs font-mono text-slate-400 space-y-0.5">
                            <div class="truncate">Host: <span class="text-cyan-400 font-semibold">${db.host}:${db.port}</span></div>
                            <div class="truncate">DB Name: <span class="font-bold ${isLight ? 'text-slate-800' : 'text-slate-200'}">${db.database_name}</span></div>
                        </div>
                    </div>

                    <!-- TILE 2: STORAGE SIZE & ENCODING -->
                    <div class="${isLight ? 'bg-white border-slate-200 shadow-md' : 'bg-slate-900/80 border-slate-800 shadow-xl'} border rounded-2xl p-4.5 space-y-2">
                        <div class="flex items-center justify-between">
                            <span class="text-[10px] font-bold uppercase tracking-wider text-slate-400 font-mono flex items-center gap-1.5">
                                <i data-lucide="pie-chart" class="w-3.5 h-3.5 text-indigo-400"></i>
                                Storage & Capacity
                            </span>
                            <span class="px-2 py-0.5 rounded-full text-[9px] font-mono font-bold bg-indigo-950 text-indigo-400 border border-indigo-800">
                                HEALTHY
                            </span>
                        </div>
                        <div class="text-2xl font-black font-mono text-indigo-400">
                            ${db.total_size_mb || '0.00'} <span class="text-xs font-medium text-slate-400">MB</span>
                        </div>
                        <div class="text-[11px] font-mono text-slate-400 truncate" title="${db.charset_collation}">
                            Encoding: <span class="${isLight ? 'text-slate-800' : 'text-slate-300'}">${(db.charset_collation || 'utf8mb4').split(' / ')[0]}</span>
                        </div>
                    </div>

                    <!-- TILE 3: SERVER RUNTIME ENVIRONMENT -->
                    <div class="${isLight ? 'bg-white border-slate-200 shadow-md' : 'bg-slate-900/80 border-slate-800 shadow-xl'} border rounded-2xl p-4.5 space-y-2">
                        <div class="flex items-center justify-between">
                            <span class="text-[10px] font-bold uppercase tracking-wider text-slate-400 font-mono flex items-center gap-1.5">
                                <i data-lucide="server" class="w-3.5 h-3.5 text-emerald-400"></i>
                                Server Runtime
                            </span>
                            <span class="px-2 py-0.5 rounded-full text-[9px] font-mono font-bold ${env.app_env === 'production' ? 'bg-emerald-950 text-emerald-400 border-emerald-800' : 'bg-amber-950 text-amber-400 border-amber-800'}">
                                ${env.app_env ? env.app_env.toUpperCase() : 'LOCAL'}
                            </span>
                        </div>
                        <div class="text-base font-extrabold ${isLight ? 'text-slate-900' : 'text-slate-100'}">
                            PHP v${env.php_version}
                        </div>
                        <div class="text-xs font-mono text-slate-400 space-y-0.5">
                            <div class="truncate">Laravel: <span class="text-emerald-400 font-bold">v${env.laravel_version}</span></div>
                            <div class="truncate">OS: <span class="${isLight ? 'text-slate-800' : 'text-slate-300'}">${env.server_os || 'Windows/Linux'}</span></div>
                        </div>
                    </div>

                    <!-- TILE 4: TOTAL ROW BREAKDOWN -->
                    <div class="${isLight ? 'bg-white border-slate-200 shadow-md' : 'bg-slate-900/80 border-slate-800 shadow-xl'} border rounded-2xl p-4.5 space-y-2">
                        <div class="flex items-center justify-between">
                            <span class="text-[10px] font-bold uppercase tracking-wider text-slate-400 font-mono flex items-center gap-1.5">
                                <i data-lucide="layers" class="w-3.5 h-3.5 text-amber-400"></i>
                                Status Volume Data
                            </span>
                            <span class="px-2 py-0.5 rounded-full text-[9px] font-mono font-bold bg-amber-950 text-amber-400 border border-amber-800">
                                STATS
                            </span>
                        </div>
                        <div class="flex items-baseline gap-2">
                            <span class="text-2xl font-black font-mono text-rose-400">${db.total_transaction_rows || 0}</span>
                            <span class="text-xs text-slate-400">Rows Transaksi</span>
                        </div>
                        <div class="text-xs font-mono text-slate-400">
                            Master Data: <span class="text-emerald-400 font-bold">${db.total_master_rows || 0}</span> Baris (Terlindungi 🔒)
                        </div>
                    </div>
                </div>

                <!-- SECTION: PEMBERSIHAN DATA GO-LIVE (SUITE PEMBERSIH MASSAL) -->
                <div class="${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-2xl p-6 shadow-xl space-y-5">
                    <!-- SECTION TITLE & SAFETY SHIELD BADGE -->
                    <div class="flex flex-col md:flex-row md:items-center justify-between gap-3 border-b ${isLight ? 'border-slate-200' : 'border-slate-800/80'} pb-4">
                        <div>
                            <h3 class="text-sm font-bold ${isLight ? 'text-slate-900' : 'text-slate-100'} flex items-center gap-2">
                                <i data-lucide="shield-check" class="w-5 h-5 text-emerald-400"></i>
                                Pembersihan Data Massal untuk Persiapan Live (Go-Live Preparation)
                            </h3>
                            <p class="text-xs ${isLight ? 'text-slate-500' : 'text-slate-400'} mt-1">
                                Bersihkan data uji coba / simulasi agar aplikasi bersih dan siap digunakan secara resmi oleh operator & supervisor lini.
                            </p>
                        </div>
                        <div class="flex items-center gap-2">
                            <span class="px-3 py-1 rounded-full text-xs font-bold bg-emerald-500/10 text-emerald-400 border border-emerald-500/30 flex items-center gap-1.5">
                                <i data-lucide="lock" class="w-3.5 h-3.5"></i>
                                Master Data Shield: Aktif & Terproteksi
                            </span>
                        </div>
                    </div>

                    <!-- SAFETY NOTICE CALLOUT -->
                    <div class="p-4 rounded-xl ${isLight ? 'bg-emerald-50 border-emerald-200 text-emerald-900' : 'bg-emerald-950/40 border-emerald-800/60 text-emerald-300'} border text-xs leading-relaxed flex items-start gap-3">
                        <div class="p-1 rounded-lg ${isLight ? 'bg-emerald-200' : 'bg-emerald-900/80'} shrink-0 mt-0.5">
                            <i data-lucide="info" class="w-4 h-4 text-emerald-400"></i>
                        </div>
                        <div class="space-y-1">
                            <div class="font-bold text-emerald-400 uppercase tracking-wider text-[11px]">Jaminan Keamanan Master Data</div>
                            <div>
                                Tindakan pembersihan data transaksional di bawah ini **TIDAK AKAN** menghapus akun user, setting target OEE, daftar mesin (225 unit), lini produksi (27 line), part produk (69 item), seksi defect (522 seksi), alasan downtime, shift kerja, maupun profil kop surat perusahaan.
                            </div>
                        </div>
                    </div>

                    <!-- 4 GRID ACTION CARDS FOR CLEANUP -->
                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4 pt-1">
                        <!-- ACTION CARD 1: FACTORY RESET (ALL TRANSACTIONS) -->
                        <div class="${isLight ? 'bg-rose-50/50 border-rose-200' : 'bg-rose-950/20 border-rose-800/40'} border rounded-2xl p-4.5 flex flex-col justify-between space-y-4 hover:border-rose-500/60 transition-all">
                            <div class="space-y-2">
                                <div class="flex items-center justify-between">
                                    <div class="w-8 h-8 rounded-xl bg-rose-500/20 text-rose-400 border border-rose-500/30 flex items-center justify-center">
                                        <i data-lucide="trash-2" class="w-4 h-4"></i>
                                    </div>
                                    <span class="px-2 py-0.5 rounded text-[9px] font-mono font-bold bg-rose-500/20 text-rose-300 border border-rose-500/30">
                                        REKOMENDASI GO-LIVE
                                    </span>
                                </div>
                                <h4 class="font-bold text-xs ${isLight ? 'text-rose-900' : 'text-rose-200'}">Pembersihan Total Transaksi</h4>
                                <p class="text-[11px] ${isLight ? 'text-slate-600' : 'text-slate-400'} leading-snug">
                                    Bersihkan seluruh record hasil produksi, kalkulasi OEE, trouble log downtime, dan defect NG.
                                </p>
                            </div>
                            <button type="button" data-action="clean-scope" data-scope="all_transactions" data-label="Seluruh Data Uji Coba Transaksional" class="w-full py-2 px-3 bg-rose-600 hover:bg-rose-500 text-white font-bold text-xs rounded-xl shadow-md shadow-rose-600/30 flex items-center justify-center gap-1.5 cursor-pointer transition-all">
                                <i data-lucide="trash-2" class="w-3.5 h-3.5"></i>
                                <span>Bersihkan Semua Transaksi</span>
                            </button>
                        </div>

                        <!-- ACTION CARD 2: PRODUKSI & OEE ONLY -->
                        <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950/60 border-slate-800'} border rounded-2xl p-4.5 flex flex-col justify-between space-y-4 hover:border-cyan-500/40 transition-all">
                            <div class="space-y-2">
                                <div class="flex items-center justify-between">
                                    <div class="w-8 h-8 rounded-xl bg-cyan-500/20 text-cyan-400 border border-cyan-500/30 flex items-center justify-center">
                                        <i data-lucide="clipboard-edit" class="w-4 h-4"></i>
                                    </div>
                                    <span class="px-2 py-0.5 rounded text-[9px] font-mono text-slate-400 border border-slate-700">
                                        PRODUKSI & OEE
                                    </span>
                                </div>
                                <h4 class="font-bold text-xs ${isLight ? 'text-slate-900' : 'text-slate-200'}">Data Laporan Produksi</h4>
                                <p class="text-[11px] text-slate-400 leading-snug">
                                    Hanya membersihkan tabel record produksi harian, snapshot kalkulasi OEE (A/P/Q), dan ringkasan shift.
                                </p>
                            </div>
                            <button type="button" data-action="clean-scope" data-scope="production" data-label="Data Laporan Produksi & OEE" class="w-full py-2 px-3 bg-cyan-600/20 hover:bg-cyan-600/30 text-cyan-300 border border-cyan-500/40 font-bold text-xs rounded-xl flex items-center justify-center gap-1.5 cursor-pointer transition-all">
                                <i data-lucide="eraser" class="w-3.5 h-3.5"></i>
                                <span>Bersihkan Data Produksi</span>
                            </button>
                        </div>

                        <!-- ACTION CARD 3: DOWNTIME ONLY -->
                        <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950/60 border-slate-800'} border rounded-2xl p-4.5 flex flex-col justify-between space-y-4 hover:border-amber-500/40 transition-all">
                            <div class="space-y-2">
                                <div class="flex items-center justify-between">
                                    <div class="w-8 h-8 rounded-xl bg-amber-500/20 text-amber-400 border border-amber-500/30 flex items-center justify-center">
                                        <i data-lucide="alert-triangle" class="w-4 h-4"></i>
                                    </div>
                                    <span class="px-2 py-0.5 rounded text-[9px] font-mono text-slate-400 border border-slate-700">
                                        TROUBLE LOG
                                    </span>
                                </div>
                                <h4 class="font-bold text-xs ${isLight ? 'text-slate-900' : 'text-slate-200'}">Log Trouble & Downtime</h4>
                                <p class="text-[11px] text-slate-400 leading-snug">
                                    Hanya membersihkan riwayat pencatatan breakdown mesin, kendala perbaikan, dan deskripsi CAPA.
                                </p>
                            </div>
                            <button type="button" data-action="clean-scope" data-scope="downtime" data-label="Log Trouble & Downtime" class="w-full py-2 px-3 bg-amber-600/20 hover:bg-amber-600/30 text-amber-300 border border-amber-500/40 font-bold text-xs rounded-xl flex items-center justify-center gap-1.5 cursor-pointer transition-all">
                                <i data-lucide="eraser" class="w-3.5 h-3.5"></i>
                                <span>Bersihkan Log Trouble</span>
                            </button>
                        </div>

                        <!-- ACTION CARD 4: NG & QUALITY ONLY -->
                        <div class="${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950/60 border-slate-800'} border rounded-2xl p-4.5 flex flex-col justify-between space-y-4 hover:border-orange-500/40 transition-all">
                            <div class="space-y-2">
                                <div class="flex items-center justify-between">
                                    <div class="w-8 h-8 rounded-xl bg-orange-500/20 text-orange-400 border border-orange-500/30 flex items-center justify-center">
                                        <i data-lucide="shield-alert" class="w-4 h-4"></i>
                                    </div>
                                    <span class="px-2 py-0.5 rounded text-[9px] font-mono text-slate-400 border border-slate-700">
                                        DEFECT & MUTU
                                    </span>
                                </div>
                                <h4 class="font-bold text-xs ${isLight ? 'text-slate-900' : 'text-slate-200'}">Rekap Defect & NG Mutu</h4>
                                <p class="text-[11px] text-slate-400 leading-snug">
                                    Hanya membersihkan antrean laporan NG, rincian item reject part, dan catatan sampling QC.
                                </p>
                            </div>
                            <button type="button" data-action="clean-scope" data-scope="ng_quality" data-label="Rekap Defect Mutu & NG Records" class="w-full py-2 px-3 bg-orange-600/20 hover:bg-orange-600/30 text-orange-300 border border-orange-500/40 font-bold text-xs rounded-xl flex items-center justify-center gap-1.5 cursor-pointer transition-all">
                                <i data-lucide="eraser" class="w-3.5 h-3.5"></i>
                                <span>Bersihkan Data Defect NG</span>
                            </button>
                        </div>
                    </div>
                </div>

                <!-- SECTION: DETAIL TABEL DATABASE (2 KOLOM: TRANSAKSI VS MASTER) -->
                <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
                    <!-- KOLOM 1: TABEL TRANSAKSI (DAPAT DIBERSIHKAN) -->
                    <div class="${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-2xl p-5 shadow-xl space-y-4">
                        <div class="flex items-center justify-between border-b ${isLight ? 'border-slate-200' : 'border-slate-800/80'} pb-3">
                            <div>
                                <h3 class="text-sm font-bold ${isLight ? 'text-rose-900' : 'text-rose-400'} flex items-center gap-2">
                                    <i data-lucide="list-filter" class="w-4 h-4"></i>
                                    Tabel Data Transaksional (${tables.transactional.length} Tabel)
                                </h3>
                                <p class="text-xs ${isLight ? 'text-slate-500' : 'text-slate-400'} mt-0.5">Tabel yang berisi riwayat aktivitas dan dapat dibersihkan untuk Go-Live.</p>
                            </div>
                            <span class="px-2.5 py-1 rounded-full text-[10px] font-mono font-bold bg-rose-950 text-rose-300 border border-rose-800">
                                TRANSAKSI
                            </span>
                        </div>

                        <div class="overflow-x-auto">
                            <table class="w-full text-left text-xs">
                                <thead class="${isLight ? 'bg-slate-100 text-slate-700' : 'bg-slate-950 text-slate-400'} uppercase font-semibold text-[10px] border-b ${isLight ? 'border-slate-200' : 'border-slate-800'}">
                                    <tr>
                                        <th class="p-2.5">Nama Tabel</th>
                                        <th class="p-2.5">Deskripsi Fungsi</th>
                                        <th class="p-2.5 text-right">Jumlah Baris</th>
                                        <th class="p-2.5 text-right">Ukuran</th>
                                    </tr>
                                </thead>
                                <tbody class="divide-y ${isLight ? 'divide-slate-200' : 'divide-slate-800/60'} font-mono">
                                    ${tables.transactional.map(t => `
                                        <tr class="${isLight ? 'hover:bg-slate-50' : 'hover:bg-slate-800/40'} transition-colors">
                                            <td class="p-2.5 font-bold text-cyan-400">${t.table}</td>
                                            <td class="p-2.5 font-sans ${isLight ? 'text-slate-700' : 'text-slate-300'} text-[11px]">${t.label}</td>
                                            <td class="p-2.5 text-right font-extrabold ${t.count > 0 ? 'text-amber-400' : 'text-slate-500'}">${t.count.toLocaleString()}</td>
                                            <td class="p-2.5 text-right text-slate-400 text-[10px]">${t.size_kb} KB</td>
                                        </tr>
                                    `).join('')}
                                </tbody>
                            </table>
                        </div>
                    </div>

                    <!-- KOLOM 2: TABEL MASTER DATA & KONFIGURASI (TERLINDUNGI) -->
                    <div class="${isLight ? 'bg-white border-slate-200' : 'bg-slate-900 border-slate-800'} border rounded-2xl p-5 shadow-xl space-y-4">
                        <div class="flex items-center justify-between border-b ${isLight ? 'border-slate-200' : 'border-slate-800/80'} pb-3">
                            <div>
                                <h3 class="text-sm font-bold ${isLight ? 'text-emerald-900' : 'text-emerald-400'} flex items-center gap-2">
                                    <i data-lucide="lock" class="w-4 h-4"></i>
                                    Tabel Master & Konfigurasi (${tables.master.length} Tabel)
                                </h3>
                                <p class="text-xs ${isLight ? 'text-slate-500' : 'text-slate-400'} mt-0.5">Master data terproteksi permanen dan tidak akan tersentuh saat pembersihan.</p>
                            </div>
                            <span class="px-2.5 py-1 rounded-full text-[10px] font-mono font-bold bg-emerald-950 text-emerald-300 border border-emerald-800">
                                🔒 TERPROTEKSI
                            </span>
                        </div>

                        <div class="overflow-x-auto max-h-[420px] overflow-y-auto">
                            <table class="w-full text-left text-xs">
                                <thead class="${isLight ? 'bg-slate-100 text-slate-700' : 'bg-slate-950 text-slate-400'} uppercase font-semibold text-[10px] border-b ${isLight ? 'border-slate-200' : 'border-slate-800'} sticky top-0 bg-slate-900">
                                    <tr>
                                        <th class="p-2.5">Nama Tabel</th>
                                        <th class="p-2.5">Kategori Master</th>
                                        <th class="p-2.5 text-right">Data Tersimpan</th>
                                        <th class="p-2.5 text-center">Status</th>
                                    </tr>
                                </thead>
                                <tbody class="divide-y ${isLight ? 'divide-slate-200' : 'divide-slate-800/60'} font-mono">
                                    ${tables.master.map(t => `
                                        <tr class="${isLight ? 'hover:bg-slate-50' : 'hover:bg-slate-800/40'} transition-colors">
                                            <td class="p-2.5 font-bold ${isLight ? 'text-slate-800' : 'text-slate-200'}">${t.table}</td>
                                            <td class="p-2.5 font-sans ${isLight ? 'text-slate-700' : 'text-slate-400'} text-[11px]">${t.label}</td>
                                            <td class="p-2.5 text-right font-bold text-emerald-400">${t.count.toLocaleString()}</td>
                                            <td class="p-2.5 text-center">
                                                <span class="px-1.5 py-0.5 rounded text-[9px] font-mono font-bold bg-emerald-500/10 text-emerald-400 border border-emerald-500/30">
                                                    AMAN
                                                </span>
                                            </td>
                                        </tr>
                                    `).join('')}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();

        // 1. Refresh Event Handler
        document.getElementById('btn-refresh-db-info')?.addEventListener('click', () => {
            this.showNotification('Memperbarui Status', 'Mengambil data metrik database terbaru...', 'status');
            this.renderDatabaseManagement();
        });

        // 2. Export Backup Event Handler
        document.getElementById('btn-export-db-backup')?.addEventListener('click', () => {
            const exportUrl = api.getBackupExportUrl();
            const link = document.createElement('a');
            link.href = exportUrl;
            link.setAttribute('download', `Backup_OEE_Yasunaga_${new Date().toISOString().slice(0,10)}.json`);
            document.body.appendChild(link);
            link.click();
            document.body.removeChild(link);
            this.showNotification('Ekspor Backup Dimulai', 'File backup JSON transaksi sedang diunduh ke komputer Anda.', 'success');
        });

        // 3. Optimize Database Event Handler
        document.getElementById('btn-optimize-database')?.addEventListener('click', async (e) => {
            const btn = e.currentTarget;
            btn.disabled = true;
            btn.innerHTML = `<div class="w-3.5 h-3.5 border-2 border-cyan-400 border-t-transparent rounded-full animate-spin"></div> <span>Mengoptimasi...</span>`;
            try {
                const res = await api.optimizeDatabase();
                this.showNotification('Database Dioptimasi', res.data?.message || 'Tabel database berhasil dioptimasi & defragmentasi.', 'success');
                await this.renderDatabaseManagement();
            } catch (err) {
                alert('Gagal mengoptimasi database: ' + (err.response?.data?.message || err.message));
            } finally {
                btn.disabled = false;
                btn.innerHTML = `<i data-lucide="zap" class="w-4 h-4"></i> <span>Optimasi & Defrag DB</span>`;
                if (window.lucide) window.lucide.createIcons();
            }
        });

        // 4. Cleanup Scope Triggers
        document.querySelectorAll('[data-action="clean-scope"]').forEach(btn => {
            btn.addEventListener('click', (e) => {
                const scope = e.currentTarget.getAttribute('data-scope');
                const label = e.currentTarget.getAttribute('data-label');
                this.showCleanConfirmationModal(scope, label);
            });
        });

        if (this.currentLang === 'ja') {
            i18n.localizeDom(document.getElementById('content-body') || document.body);
        }
    }

    // ==========================================
    // 13B. CLEANUP CONFIRMATION MODAL WITH TYPE-TO-CONFIRM SECURITY
    // ==========================================
    showCleanConfirmationModal(scope, label) {
        const modalContainer = document.getElementById('modal-container');
        if (!modalContainer) return;

        const isLight = this.theme === 'light' || document.documentElement.classList.contains('light');

        modalContainer.innerHTML = `
            <div class="fixed inset-0 bg-slate-950/85 backdrop-blur-sm flex items-center justify-center p-4 z-50 overflow-y-auto">
                <div class="${isLight ? 'bg-white border-slate-200 text-slate-900 shadow-2xl' : 'bg-slate-900 border-slate-800 text-slate-100 shadow-2xl'} border rounded-2xl max-w-lg w-full my-auto overflow-hidden font-sans">
                    <!-- MODAL HEADER -->
                    <div class="${isLight ? 'bg-rose-50 border-rose-200' : 'bg-rose-950/40 border-rose-900/50'} px-6 py-4 border-b flex items-center justify-between">
                        <div class="flex items-center gap-3 text-rose-400">
                            <div class="w-10 h-10 rounded-xl bg-rose-500/20 border border-rose-500/40 flex items-center justify-center shrink-0">
                                <i data-lucide="alert-octagon" class="w-5 h-5"></i>
                            </div>
                            <div>
                                <h3 class="font-bold text-sm ${isLight ? 'text-rose-900' : 'text-rose-100'}">Konfirmasi Pembersihan Data Go-Live</h3>
                                <p class="text-[11px] ${isLight ? 'text-rose-700' : 'text-rose-300'}">Tindakan ini memerlukan verifikasi frasa keamanan.</p>
                            </div>
                        </div>
                        <button type="button" id="btn-close-clean-modal" class="p-1 rounded-lg ${isLight ? 'text-slate-400 hover:text-slate-800 hover:bg-slate-200' : 'text-slate-400 hover:text-slate-100 hover:bg-slate-800'} cursor-pointer transition-colors">
                            <i data-lucide="x" class="w-5 h-5"></i>
                        </button>
                    </div>

                    <!-- MODAL BODY -->
                    <div class="p-6 space-y-4 text-xs">
                        <div class="p-3.5 rounded-xl ${isLight ? 'bg-slate-100 border-slate-300' : 'bg-slate-950/80 border-slate-800'} border space-y-2">
                            <div class="text-[11px] text-slate-400">Target Cakupan Pembersihan:</div>
                            <div class="font-bold text-sm text-rose-400 flex items-center gap-2">
                                <i data-lucide="trash-2" class="w-4 h-4"></i>
                                <span>${label}</span>
                            </div>
                        </div>

                        <div class="p-3.5 rounded-xl ${isLight ? 'bg-emerald-50 border-emerald-200 text-emerald-800' : 'bg-emerald-950/30 border-emerald-800/50 text-emerald-300'} border space-y-1 text-[11.5px] leading-relaxed">
                            <div class="font-bold flex items-center gap-1.5 text-emerald-400">
                                <i data-lucide="shield-check" class="w-4 h-4"></i>
                                <span>Jaminan Keamanan Master Data</span>
                            </div>
                            <div>
                                Master Data (Akun User, Mesin, Lini, Part Produk, Seksi Defect, Shift, Alasan Downtime, dan Pengaturan Kop Surat) <strong>TIDAK AKAN DIHAPUS</strong>.
                            </div>
                        </div>

                        <div class="space-y-2 pt-1">
                            <label class="block font-semibold ${isLight ? 'text-slate-700' : 'text-slate-300'} text-xs">
                                Untuk mengonfirmasi, ketik kata <span class="font-mono font-bold text-rose-400 bg-rose-950/40 px-1.5 py-0.5 rounded border border-rose-800/50">BERSIHKAN</span> pada kotak di bawah:
                            </label>
                            <input type="text" id="input-clean-confirm-phrase" placeholder="Ketik BERSIHKAN di sini..." autocomplete="off" class="w-full ${isLight ? 'bg-white border-slate-300 text-slate-900 focus:border-rose-500' : 'bg-slate-950 border-slate-700 text-slate-100 focus:border-rose-500'} border-2 rounded-xl px-3 py-2.5 font-mono font-bold text-xs uppercase tracking-wider outline-none transition-all" />
                        </div>
                    </div>

                    <!-- MODAL FOOTER -->
                    <div class="px-6 py-4 border-t ${isLight ? 'bg-slate-50 border-slate-200' : 'bg-slate-950 border-slate-800'} flex items-center justify-end gap-2.5">
                        <button type="button" id="btn-cancel-clean-modal" class="px-4 py-2 ${isLight ? 'bg-slate-200 hover:bg-slate-300 text-slate-700' : 'bg-slate-800 hover:bg-slate-700 text-slate-300'} rounded-xl text-xs font-semibold cursor-pointer transition-colors">
                            Batal
                        </button>
                        <button type="button" id="btn-execute-clean-modal" disabled class="px-5 py-2 bg-rose-600 disabled:opacity-40 disabled:cursor-not-allowed hover:bg-rose-500 text-white font-bold text-xs rounded-xl shadow-lg shadow-rose-600/30 flex items-center gap-1.5 cursor-pointer transition-all">
                            <i data-lucide="trash-2" class="w-3.5 h-3.5"></i>
                            <span>Konfirmasi & Bersihkan Data</span>
                        </button>
                    </div>
                </div>
            </div>
        `;

        if (window.lucide) window.lucide.createIcons();
        if (this.currentLang === 'ja') {
            i18n.localizeDom(modalContainer);
        }

        const closeModal = () => {
            modalContainer.innerHTML = '';
        };

        document.getElementById('btn-close-clean-modal')?.addEventListener('click', closeModal);
        document.getElementById('btn-cancel-clean-modal')?.addEventListener('click', closeModal);

        const phraseInput = document.getElementById('input-clean-confirm-phrase');
        const executeBtn = document.getElementById('btn-execute-clean-modal');

        if (phraseInput && executeBtn) {
            phraseInput.addEventListener('input', (e) => {
                const val = e.target.value.trim().toUpperCase();
                executeBtn.disabled = (val !== 'BERSIHKAN' && val !== 'CONFIRM');
            });

            executeBtn.addEventListener('click', async () => {
                executeBtn.disabled = true;
                executeBtn.innerHTML = `<div class="w-3.5 h-3.5 border-2 border-white border-t-transparent rounded-full animate-spin"></div> <span>Membersihkan data...</span>`;

                try {
                    const res = await api.cleanTransactions({
                        scope: scope,
                        confirmation_phrase: phraseInput.value.trim().toUpperCase()
                    });

                    closeModal();
                    this.showNotification(
                        'Pembersihan Data Berhasil', 
                        res.data?.message || `Data transaksi uji coba (${label}) telah dibersihkan!`, 
                        'success'
                    );

                    await this.loadMasterData(true);
                    await this.renderDatabaseManagement();
                } catch (err) {
                    alert('Gagal membersihkan data: ' + (err.response?.data?.message || err.message));
                    executeBtn.disabled = false;
                    executeBtn.innerHTML = `<i data-lucide="trash-2" class="w-3.5 h-3.5"></i> <span>Konfirmasi & Bersihkan Data</span>`;
                    if (window.lucide) window.lucide.createIcons();
                }
            });
        }
    }
}

// Bootstrap Application on DOM load
document.addEventListener('DOMContentLoaded', () => {
    window.app = new OeeApp();
    window.app.init();
});
